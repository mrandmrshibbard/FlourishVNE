{
  "name": "flourish-vnbundle-exporter",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Converts a Flourish .flourish project into a runtime-agnostic .vnbundle (console-porting pipeline, Component-B half). Reads the engine's export; never touches console SDKs.",
  "bin": { "flourish-export": "dist/cli.js" },
  "scripts": {
    "build": "tsc",
    "export": "node dist/cli.js"
  },
  "dependencies": {
    "fflate": "^0.8.2"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "@types/node": "^22.9.0"
  }
}
