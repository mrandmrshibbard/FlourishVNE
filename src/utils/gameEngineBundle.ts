/**
 * AUTO-GENERATED FILE - DO NOT EDIT
 * Generated by: npm run generate:engine-bundle
 * Source: dist-standalone/game-engine.js
 * Size: 1235.05 KB
 */

export const GAME_ENGINE_BUNDLE = `var GameEngine = (function(exports, jsxRuntime2, React2, ReactDOM2, reactDom) {
  "use strict";
  var UIElementType = /* @__PURE__ */ ((UIElementType2) => {
    UIElementType2["Button"] = "Button";
    UIElementType2["Text"] = "Text";
    UIElementType2["Image"] = "Image";
    UIElementType2["SaveSlotGrid"] = "SaveSlotGrid";
    UIElementType2["SettingsSlider"] = "SettingsSlider";
    UIElementType2["SettingsToggle"] = "SettingsToggle";
    UIElementType2["CharacterPreview"] = "CharacterPreview";
    UIElementType2["TextInput"] = "TextInput";
    UIElementType2["Dropdown"] = "Dropdown";
    UIElementType2["Checkbox"] = "Checkbox";
    UIElementType2["AssetCycler"] = "AssetCycler";
    UIElementType2["CGGallery"] = "CGGallery";
    UIElementType2["Inventory"] = "Inventory";
    UIElementType2["HotSpot"] = "HotSpot";
    UIElementType2["draggableImageElement"] = "draggableImageElement";
    UIElementType2["Meter"] = "Meter";
    UIElementType2["Customizer"] = "Customizer";
    UIElementType2["Timer"] = "Timer";
    UIElementType2["Custom"] = "Custom";
    return UIElementType2;
  })(UIElementType || {});
  var UIActionType = /* @__PURE__ */ ((UIActionType2) => {
    UIActionType2["None"] = "None";
    UIActionType2["StartNewGame"] = "StartNewGame";
    UIActionType2["GoToScreen"] = "GoToScreen";
    UIActionType2["LoadGame"] = "LoadGame";
    UIActionType2["SaveGame"] = "SaveGame";
    UIActionType2["DeleteSave"] = "DeleteSave";
    UIActionType2["ShowPhone"] = "ShowPhone";
    UIActionType2["HidePhone"] = "HidePhone";
    UIActionType2["ShowPhoneText"] = "ShowPhoneText";
    UIActionType2["HidePhoneText"] = "HidePhoneText";
    UIActionType2["ShowPhoneHistory"] = "ShowPhoneHistory";
    UIActionType2["ShowPhoneContacts"] = "ShowPhoneContacts";
    UIActionType2["OpenPhoneApp"] = "OpenPhoneApp";
    UIActionType2["ShowMap"] = "ShowMap";
    UIActionType2["ShowMiniGame"] = "ShowMiniGame";
    UIActionType2["ReturnToGame"] = "ReturnToGame";
    UIActionType2["ReturnToPreviousScreen"] = "ReturnToPreviousScreen";
    UIActionType2["QuitToTitle"] = "QuitToTitle";
    UIActionType2["ExitGame"] = "ExitGame";
    UIActionType2["JumpToScene"] = "JumpToScene";
    UIActionType2["JumpToLabel"] = "JumpToLabel";
    UIActionType2["SetVariable"] = "SetVariable";
    UIActionType2["ResetVariable"] = "ResetVariable";
    UIActionType2["PlaySound"] = "PlaySound";
    UIActionType2["PlayMusic"] = "PlayMusic";
    UIActionType2["StopMusic"] = "StopMusic";
    UIActionType2["ShowSpotlight"] = "ShowSpotlight";
    UIActionType2["HideSpotlight"] = "HideSpotlight";
    UIActionType2["ShowFlashlight"] = "ShowFlashlight";
    UIActionType2["HideFlashlight"] = "HideFlashlight";
    UIActionType2["CycleLayerAsset"] = "CycleLayerAsset";
    UIActionType2["ToggleScreen"] = "ToggleScreen";
    UIActionType2["OpenURL"] = "OpenURL";
    UIActionType2["PlayAnimation"] = "PlayAnimation";
    UIActionType2["ChangeImage"] = "ChangeImage";
    UIActionType2["ShowElement"] = "ShowElement";
    UIActionType2["HideElement"] = "HideElement";
    UIActionType2["ContinueGame"] = "ContinueGame";
    UIActionType2["ShowLog"] = "ShowLog";
    UIActionType2["ToggleAutoAdvance"] = "ToggleAutoAdvance";
    UIActionType2["ToggleSkip"] = "ToggleSkip";
    UIActionType2["SkipBackward"] = "SkipBackward";
    UIActionType2["OpenPauseMenu"] = "OpenPauseMenu";
    UIActionType2["CallCommonEvent"] = "CallCommonEvent";
    UIActionType2["GiveItem"] = "GiveItem";
    UIActionType2["UseItem"] = "UseItem";
    UIActionType2["DestroyItem"] = "DestroyItem";
    UIActionType2["UseSelectedItem"] = "UseSelectedItem";
    UIActionType2["CarryItem"] = "CarryItem";
    UIActionType2["RestockCollection"] = "RestockCollection";
    UIActionType2["BuyItem"] = "BuyItem";
    UIActionType2["SellItem"] = "SellItem";
    UIActionType2["BuySelectedItem"] = "BuySelectedItem";
    UIActionType2["SellSelectedItem"] = "SellSelectedItem";
    UIActionType2["StartTimer"] = "StartTimer";
    UIActionType2["StopTimer"] = "StopTimer";
    UIActionType2["SetTimeOfDay"] = "SetTimeOfDay";
    UIActionType2["ClearUiPalette"] = "ClearUiPalette";
    return UIActionType2;
  })(UIActionType || {});
  const RESET_ALL_VARIABLES = "__ALL_VARIABLES__";
  const DEFAULT_FONT = {
    family: "Inter, system-ui, sans-serif",
    size: 16,
    color: "#ffffff",
    weight: "normal",
    italic: false
  };
  function migrateHotZoneElement(hz) {
    const baseProps = {
      id: hz.id,
      name: hz.name || "Element",
      x: hz.x,
      y: hz.y,
      width: hz.width,
      height: hz.height,
      anchorX: 0,
      anchorY: 0,
      conditions: hz.conditions,
      interactive: true,
      draggable: hz.draggable,
      snapBack: hz.snapBack,
      snapToHotSpot: hz.snapToHotSpot,
      hideOnDrop: hz.hideOnDrop,
      actions: hz.actions,
      clickSoundId: hz.clickSoundId ?? null,
      hoverSoundId: hz.hoverSoundId ?? null
    };
    const elType = hz.elementType || "image";
    switch (elType) {
      case "image": {
        const out = {
          ...baseProps,
          type: UIElementType.Image,
          background: hz.imageId ? { type: "image", assetId: hz.imageId } : { type: "color", value: "#00000000" },
          image: hz.imageId ? { type: "image", id: hz.imageId } : null,
          objectFit: "contain"
        };
        return out;
      }
      case "video": {
        const out = {
          ...baseProps,
          type: UIElementType.Image,
          background: hz.videoId ? { type: "video", assetId: hz.videoId } : { type: "color", value: "#00000000" },
          image: null,
          objectFit: "contain"
        };
        return out;
      }
      case "text": {
        const out = {
          ...baseProps,
          type: UIElementType.Text,
          text: hz.text || hz.name || "",
          font: hz.font || DEFAULT_FONT,
          textAlign: "center",
          verticalAlign: "middle"
        };
        return out;
      }
      case "button": {
        const out = {
          ...baseProps,
          type: UIElementType.Button,
          text: hz.text || hz.name || "",
          font: hz.font || DEFAULT_FONT,
          action: hz.actions && hz.actions[0] || { type: UIActionType.None },
          actions: hz.actions,
          image: hz.imageId ? { type: "image", id: hz.imageId } : null,
          hoverImage: null,
          clickSoundId: hz.clickSoundId ?? null,
          hoverSoundId: hz.hoverSoundId ?? null,
          backgroundColor: hz.backgroundColor
        };
        return out;
      }
      case "textInput": {
        const out = {
          ...baseProps,
          type: UIElementType.TextInput,
          placeholder: hz.placeholder || "",
          variableId: hz.variableId,
          font: hz.font || DEFAULT_FONT,
          backgroundColor: hz.backgroundColor,
          borderColor: hz.borderColor,
          maxLength: hz.maxLength
        };
        return out;
      }
      case "draggableImageElement": {
        const out = {
          ...baseProps,
          type: UIElementType.draggableImageElement,
          image: hz.imageId ? { type: "image", id: hz.imageId } : null,
          hoverImage: hz.hoverImageId ? { type: "image", id: hz.hoverImageId } : null,
          draggableImageElementRegions: hz.draggableImageElementRegions
        };
        return out;
      }
    }
    return null;
  }
  function migrateHotSpot(spot) {
    return {
      id: spot.id,
      name: spot.name || "Hot Spot",
      type: UIElementType.HotSpot,
      x: spot.x,
      y: spot.y,
      width: spot.width,
      height: spot.height,
      anchorX: 0,
      anchorY: 0,
      interactive: true,
      shape: spot.shape,
      trigger: spot.trigger || "click",
      acceptedElementIds: spot.acceptedElementIds,
      actions: spot.actions,
      conditions: spot.conditions,
      highlightColor: spot.highlightColor,
      visible: spot.visible
    };
  }
  function migrateScreenToUnified(screen) {
    const legacy = screen;
    let result = screen;
    const hasLegacyHotZoneElements = legacy.hotZoneElements && Object.keys(legacy.hotZoneElements).length > 0;
    const hasLegacyHotSpots = legacy.hotSpots && Object.keys(legacy.hotSpots).length > 0;
    const stillCarriesLegacyData = legacy.screenType === "hotzone" || hasLegacyHotZoneElements || hasLegacyHotSpots;
    if (stillCarriesLegacyData) {
      const newElements = { ...screen.elements || {} };
      for (const hz of Object.values(legacy.hotZoneElements || {})) {
        const migrated = migrateHotZoneElement(hz);
        if (migrated && !newElements[migrated.id]) newElements[migrated.id] = migrated;
      }
      for (const spot of Object.values(legacy.hotSpots || {})) {
        if (!newElements[spot.id]) newElements[spot.id] = migrateHotSpot(spot);
      }
      const { screenType, hotZoneElements, hotSpots, ...rest } = legacy;
      const prevBackup = rest._legacyHotZone || {};
      result = {
        ...rest,
        elements: newElements,
        _legacyHotZone: {
          // Prefer the freshly-stripped data; fall back to anything previously stashed
          // (Phase 1 saved \`screenType\` here without the maps, so the maps may live
          // either in legacy fields we just stripped or in the existing backup).
          hotSpots: hotSpots ?? (prevBackup == null ? void 0 : prevBackup.hotSpots),
          hotZoneElements: hotZoneElements ?? (prevBackup == null ? void 0 : prevBackup.hotZoneElements),
          screenType: screenType ?? (prevBackup == null ? void 0 : prevBackup.screenType)
        }
      };
    } else if (legacy.screenType !== void 0) {
      const { screenType, ...rest } = legacy;
      const prevBackup = rest._legacyHotZone || {};
      result = {
        ...rest,
        _legacyHotZone: {
          ...prevBackup,
          screenType: (prevBackup == null ? void 0 : prevBackup.screenType) ?? screenType
        }
      };
    }
    const backup = result._legacyHotZone;
    if (backup && (backup.hotZoneElements || backup.hotSpots)) {
      const shouldBeInteractive = /* @__PURE__ */ new Set();
      for (const id of Object.keys(backup.hotZoneElements || {})) shouldBeInteractive.add(id);
      for (const id of Object.keys(backup.hotSpots || {})) shouldBeInteractive.add(id);
      if (shouldBeInteractive.size > 0) {
        let changed = false;
        const recovered = {};
        for (const [id, el] of Object.entries(result.elements || {})) {
          if (shouldBeInteractive.has(id) && !el.interactive) {
            recovered[id] = { ...el, interactive: true };
            changed = true;
          } else {
            recovered[id] = el;
          }
        }
        if (changed) {
          result = { ...result, elements: recovered };
        }
      }
    }
    return result;
  }
  function migrateProjectToUnifiedScreens(project) {
    if (!project || !project.uiScreens) return project;
    let anyChanged = false;
    const newScreens = {};
    for (const [id, screen] of Object.entries(project.uiScreens)) {
      const migrated = migrateScreenToUnified(screen);
      if (migrated !== screen) anyChanged = true;
      newScreens[id] = migrated;
    }
    if (!anyChanged) return project;
    return {
      ...project,
      uiScreens: newScreens
    };
  }
  const RETIRED_COMMAND_TYPES = /* @__PURE__ */ new Set(["ShowdraggableImageElement", "HidedraggableImageElement"]);
  function stripCommands(commands) {
    if (!commands || commands.length === 0) return commands;
    let hasRetired = false;
    for (const c of commands) {
      if (RETIRED_COMMAND_TYPES.has(c.type)) {
        hasRetired = true;
        break;
      }
    }
    if (!hasRetired) return commands;
    return commands.filter((c) => !RETIRED_COMMAND_TYPES.has(c.type));
  }
  function migrateProjectRemoveLegacyCommands(project) {
    if (!project) return project;
    let changed = false;
    let scenes = project.scenes;
    if (scenes) {
      const next = {};
      for (const [id, scene] of Object.entries(scenes)) {
        const stripped = stripCommands(scene.commands);
        if (stripped !== scene.commands) {
          changed = true;
          next[id] = { ...scene, commands: stripped };
        } else {
          next[id] = scene;
        }
      }
      scenes = next;
    }
    let commonEvents = project.commonEvents;
    if (commonEvents) {
      const next = {};
      for (const [id, ce] of Object.entries(commonEvents)) {
        const stripped = stripCommands(ce == null ? void 0 : ce.commands);
        if (stripped !== (ce == null ? void 0 : ce.commands)) {
          changed = true;
          next[id] = { ...ce, commands: stripped };
        } else {
          next[id] = ce;
        }
      }
      commonEvents = next;
    }
    if (!changed) return project;
    return { ...project, scenes, ...commonEvents ? { commonEvents } : {} };
  }
  const generateId\$8 = () => Math.random().toString(36).substring(2, 9);
  const statVarName = (statName, characterName) => characterName ? \`\${characterName} — \${statName}\` : statName;
  const makeStatVar = (id, name, stat) => ({
    id,
    name,
    type: "number",
    defaultValue: stat.defaultValue,
    scope: "global",
    min: stat.min,
    max: stat.max
  });
  const statReducer = (state, action) => {
    var _a, _b, _c;
    switch (action.type) {
      case "ADD_STAT": {
        const p = action.payload;
        const statId = p.id || \`stat-\${generateId\$8()}\`;
        const stats = state.stats || {};
        const min = p.min ?? 0;
        const max = p.max ?? 100;
        const defaultValue = p.defaultValue ?? min;
        const appliesTo = p.appliesTo || "global";
        const characterIds = appliesTo === "characters" ? p.characterIds || [] : void 0;
        let variables = state.variables;
        const variableIds = {};
        const targets2 = appliesTo === "characters" ? characterIds || [] : ["global"];
        for (const target of targets2) {
          const charName = target === "global" ? void 0 : (_a = state.characters[target]) == null ? void 0 : _a.name;
          if (target !== "global" && !charName) continue;
          const varId = \`var-\${generateId\$8()}\`;
          variables = { ...variables, [varId]: makeStatVar(varId, statVarName(p.name, charName), { min, max, defaultValue }) };
          variableIds[target] = varId;
        }
        const newStat = {
          id: statId,
          name: p.name,
          description: p.description,
          icon: p.icon ?? null,
          min,
          max,
          defaultValue,
          color: p.color,
          appliesTo,
          characterIds,
          variableIds,
          order: p.order ?? Object.keys(stats).length
        };
        return { ...state, variables, stats: { ...stats, [statId]: newStat } };
      }
      case "UPDATE_STAT": {
        const { statId, updates } = action.payload;
        const stats = state.stats || {};
        const existing = stats[statId];
        if (!existing) return state;
        const merged = { ...existing, ...updates };
        let variables = state.variables;
        const rangeChanged = updates.min !== void 0 || updates.max !== void 0 || updates.defaultValue !== void 0;
        if (updates.name || rangeChanged) {
          const next = { ...variables };
          for (const [target, varId] of Object.entries(existing.variableIds)) {
            const v = next[varId];
            if (!v) continue;
            const charName = target === "global" ? void 0 : (_b = state.characters[target]) == null ? void 0 : _b.name;
            next[varId] = {
              ...v,
              name: updates.name ? statVarName(merged.name, charName) : v.name,
              min: merged.min,
              max: merged.max,
              defaultValue: rangeChanged ? merged.defaultValue : v.defaultValue
            };
          }
          variables = next;
        }
        return { ...state, variables, stats: { ...stats, [statId]: merged } };
      }
      case "DELETE_STAT": {
        const { statId, deleteVariables } = action.payload;
        const stats = state.stats || {};
        const existing = stats[statId];
        if (!existing) return state;
        const { [statId]: _removed, ...remainingStats } = stats;
        let variables = state.variables;
        if (deleteVariables) {
          const next = { ...variables };
          for (const varId of Object.values(existing.variableIds)) delete next[varId];
          variables = next;
        }
        return { ...state, variables, stats: remainingStats };
      }
      case "SET_STAT_CHARACTERS": {
        const { statId, characterIds } = action.payload;
        const stats = state.stats || {};
        const existing = stats[statId];
        if (!existing || existing.appliesTo !== "characters") return state;
        const prev = new Set(existing.characterIds || []);
        const next = new Set(characterIds);
        let variables = { ...state.variables };
        const variableIds = { ...existing.variableIds };
        for (const charId of prev) {
          if (!next.has(charId)) {
            const varId = variableIds[charId];
            if (varId) delete variables[varId];
            delete variableIds[charId];
          }
        }
        for (const charId of next) {
          if (!prev.has(charId)) {
            const charName = (_c = state.characters[charId]) == null ? void 0 : _c.name;
            if (!charName) continue;
            const varId = \`var-\${generateId\$8()}\`;
            variables[varId] = makeStatVar(varId, statVarName(existing.name, charName), existing);
            variableIds[charId] = varId;
          }
        }
        return {
          ...state,
          variables,
          stats: { ...stats, [statId]: { ...existing, characterIds: [...next], variableIds } }
        };
      }
      default:
        return state;
    }
  };
  var CommandType = /* @__PURE__ */ ((CommandType2) => {
    CommandType2["Dialogue"] = "Dialogue";
    CommandType2["SetBackground"] = "SetBackground";
    CommandType2["ShowCharacter"] = "ShowCharacter";
    CommandType2["HideCharacter"] = "HideCharacter";
    CommandType2["SetCharacterLayer"] = "SetCharacterLayer";
    CommandType2["Choice"] = "Choice";
    CommandType2["BranchStart"] = "BranchStart";
    CommandType2["BranchElseIf"] = "BranchElseIf";
    CommandType2["BranchElse"] = "BranchElse";
    CommandType2["BranchEnd"] = "BranchEnd";
    CommandType2["SetVariable"] = "SetVariable";
    CommandType2["TextInput"] = "TextInput";
    CommandType2["Jump"] = "Jump";
    CommandType2["Label"] = "Label";
    CommandType2["JumpToLabel"] = "JumpToLabel";
    CommandType2["PlayMusic"] = "PlayMusic";
    CommandType2["StopMusic"] = "StopMusic";
    CommandType2["PlaySoundEffect"] = "PlaySoundEffect";
    CommandType2["StopSoundEffect"] = "StopSoundEffect";
    CommandType2["PlayMovie"] = "PlayMovie";
    CommandType2["StopMovie"] = "StopMovie";
    CommandType2["Wait"] = "Wait";
    CommandType2["ShakeScreen"] = "ShakeScreen";
    CommandType2["TintScreen"] = "TintScreen";
    CommandType2["PanZoomScreen"] = "PanZoomScreen";
    CommandType2["ResetScreenEffects"] = "ResetScreenEffects";
    CommandType2["FlashScreen"] = "FlashScreen";
    CommandType2["SetScreenOverlayEffect"] = "SetScreenOverlayEffect";
    CommandType2["ShowScreen"] = "ShowScreen";
    CommandType2["HideScreen"] = "HideScreen";
    CommandType2["ShowText"] = "ShowText";
    CommandType2["ShowImage"] = "ShowImage";
    CommandType2["HideText"] = "HideText";
    CommandType2["HideImage"] = "HideImage";
    CommandType2["ShowButton"] = "ShowButton";
    CommandType2["HideButton"] = "HideButton";
    CommandType2["ShowItem"] = "ShowItem";
    CommandType2["CreditRoll"] = "CreditRoll";
    CommandType2["Group"] = "Group";
    CommandType2["RunScript"] = "RunScript";
    CommandType2["SpawnParticles"] = "SpawnParticles";
    CommandType2["StopParticles"] = "StopParticles";
    CommandType2["CallCommonEvent"] = "CallCommonEvent";
    CommandType2["ShowHotSpot"] = "ShowHotSpot";
    CommandType2["HideHotSpot"] = "HideHotSpot";
    CommandType2["TweenElement"] = "TweenElement";
    CommandType2["MoveCharacter"] = "MoveCharacter";
    CommandType2["GiveItem"] = "GiveItem";
    CommandType2["UseItem"] = "UseItem";
    CommandType2["DestroyItem"] = "DestroyItem";
    CommandType2["RestockCollection"] = "RestockCollection";
    CommandType2["BuyItem"] = "BuyItem";
    CommandType2["SellItem"] = "SellItem";
    CommandType2["Lightning"] = "Lightning";
    CommandType2["Flashlight"] = "Flashlight";
    CommandType2["Spotlight"] = "Spotlight";
    CommandType2["Fireworks"] = "Fireworks";
    CommandType2["PlaceLights"] = "PlaceLights";
    CommandType2["ClearLights"] = "ClearLights";
    CommandType2["ShowPhone"] = "ShowPhone";
    CommandType2["HidePhone"] = "HidePhone";
    CommandType2["ShowPhoneText"] = "ShowPhoneText";
    CommandType2["HidePhoneText"] = "HidePhoneText";
    CommandType2["PhoneIncomingText"] = "PhoneIncomingText";
    CommandType2["PhoneIncomingCall"] = "PhoneIncomingCall";
    CommandType2["StartPhoneCall"] = "StartPhoneCall";
    CommandType2["ShowMap"] = "ShowMap";
    CommandType2["PhoneNotify"] = "PhoneNotify";
    CommandType2["StartTimer"] = "StartTimer";
    CommandType2["StopTimer"] = "StopTimer";
    CommandType2["SetTimeOfDay"] = "SetTimeOfDay";
    CommandType2["ShowMiniGame"] = "ShowMiniGame";
    return CommandType2;
  })(CommandType || {});
  const REACTIVE_VISUAL_TYPES = /* @__PURE__ */ new Set([
    "SetBackground",
    "ShowImage",
    "ShowText",
    "ShowButton",
    "ShowItem",
    "ShowCharacter",
    "ShowHotSpot"
    /* ShowHotSpot */
  ]);
  function migrateItemCountVariableBounds(project) {
    var _a, _b;
    if (!project || !project.variables) return project;
    if (!project.items && !project.itemCollections) return project;
    let changed = false;
    const variables = { ...project.variables };
    const ensureNumberVar = (id, name, internal) => {
      if (!id) return;
      const existing = variables[id];
      if (!existing) {
        variables[id] = {
          id,
          name,
          type: "number",
          defaultValue: 0,
          scope: "global",
          min: 0,
          ...internal ? { isInternal: true } : {}
        };
        changed = true;
      } else if (existing.type === "number" && existing.min === void 0) {
        variables[id] = { ...existing, min: 0 };
        changed = true;
      }
    };
    for (const item of Object.values(project.items || {})) {
      if (item == null ? void 0 : item.countVariableId) ensureNumberVar(item.countVariableId, item.name || "Item", false);
    }
    for (const collection of Object.values(project.itemCollections || {})) {
      for (const entry of (collection == null ? void 0 : collection.entries) || []) {
        if (entry == null ? void 0 : entry.countVariableId) {
          const itemName = ((_b = (_a = project.items) == null ? void 0 : _a[entry.itemId]) == null ? void 0 : _b.name) || "Item";
          ensureNumberVar(entry.countVariableId, \`\${itemName} — \${collection.name || "List"}\`, true);
        }
      }
    }
    return changed ? { ...project, variables } : project;
  }
  function migrateStatVariables(project) {
    var _a;
    if (!project || !project.stats || !project.variables) return project;
    let changed = false;
    const variables = { ...project.variables };
    const stats = { ...project.stats };
    for (const [statId, stat] of Object.entries(stats)) {
      if (!(stat == null ? void 0 : stat.variableIds)) continue;
      let statChanged = false;
      const variableIds = { ...stat.variableIds };
      let characterIds = stat.characterIds;
      for (const [target, varId] of Object.entries(stat.variableIds)) {
        const char = target === "global" ? void 0 : (_a = project.characters) == null ? void 0 : _a[target];
        if (target !== "global" && !char) {
          if (variables[varId]) delete variables[varId];
          delete variableIds[target];
          if (characterIds) characterIds = characterIds.filter((id) => id !== target);
          statChanged = true;
          continue;
        }
        if (!variables[varId]) {
          variables[varId] = {
            id: varId,
            name: statVarName(stat.name, char == null ? void 0 : char.name),
            type: "number",
            defaultValue: stat.defaultValue ?? stat.min ?? 0,
            scope: "global",
            min: stat.min,
            max: stat.max
          };
          changed = true;
        }
      }
      if (stat.appliesTo === "characters" && characterIds) {
        const alive = characterIds.filter((id) => {
          var _a2;
          return !!((_a2 = project.characters) == null ? void 0 : _a2[id]);
        });
        if (alive.length !== characterIds.length) {
          characterIds = alive;
          statChanged = true;
        }
      }
      if (statChanged) {
        stats[statId] = { ...stat, variableIds, characterIds };
        changed = true;
      }
    }
    return changed ? { ...project, variables, stats } : project;
  }
  function repairOrphanBranchMarkers(project) {
    var _a;
    if (!(project == null ? void 0 : project.scenes)) return project;
    let changedAny = false;
    const newScenes = { ...project.scenes };
    for (const sceneId in project.scenes) {
      const scene = project.scenes[sceneId];
      if (!((_a = scene == null ? void 0 : scene.commands) == null ? void 0 : _a.length)) continue;
      const cmds = scene.commands;
      const endIds = /* @__PURE__ */ new Set();
      for (const c of cmds) {
        if (c.type === CommandType.BranchEnd) endIds.add(c.branchId);
      }
      let changed = false;
      const out = [];
      for (const c of cmds) {
        out.push(c);
        if (c.type === CommandType.BranchStart) {
          const bid = c.branchId;
          if (bid && !endIds.has(bid)) {
            out.push({ id: \`cmd-\${Math.random().toString(36).substring(2, 9)}\`, type: CommandType.BranchEnd, branchId: bid });
            endIds.add(bid);
            changed = true;
          }
        }
      }
      if (changed) {
        newScenes[sceneId] = { ...scene, commands: out };
        changedAny = true;
      }
    }
    return changedAny ? { ...project, scenes: newScenes } : project;
  }
  function migrateInventorySlotButton(project) {
    if (!project || !project.uiScreens) return project;
    let anyChanged = false;
    const newScreens = {};
    for (const [sid, screen] of Object.entries(project.uiScreens)) {
      const elements = screen.elements;
      if (!elements) {
        newScreens[sid] = screen;
        continue;
      }
      let screenChanged = false;
      const newElements = {};
      for (const [eid, el] of Object.entries(elements)) {
        if (el && el.type === UIElementType.Inventory && el.showUseButton !== void 0 && el.slotButton === void 0) {
          const { showUseButton, ...rest } = el;
          newElements[eid] = { ...rest, slotButton: showUseButton ? "use" : "none" };
          screenChanged = true;
        } else {
          newElements[eid] = el;
        }
      }
      newScreens[sid] = screenChanged ? { ...screen, elements: newElements } : screen;
      if (screenChanged) anyChanged = true;
    }
    if (!anyChanged) return project;
    return { ...project, uiScreens: newScreens };
  }
  function migrateMapLocationActions(project) {
    const maps = project.maps;
    if (!maps) return project;
    let anyChanged = false;
    const nextMaps = {};
    for (const [mapId, map] of Object.entries(maps)) {
      const locations = map == null ? void 0 : map.locations;
      if (!Array.isArray(locations)) {
        nextMaps[mapId] = map;
        continue;
      }
      let mapChanged = false;
      const nextLocations = locations.map((loc) => {
        if (!loc || !loc.targetSceneId) return loc;
        const actions = Array.isArray(loc.actions) ? [...loc.actions] : [];
        const alreadyJumps = actions.some((a) => a && a.type === UIActionType.JumpToScene && a.targetSceneId === loc.targetSceneId);
        if (!alreadyJumps) actions.push({ type: UIActionType.JumpToScene, targetSceneId: loc.targetSceneId });
        mapChanged = true;
        const { targetSceneId, ...rest } = loc;
        return { ...rest, actions };
      });
      if (mapChanged) {
        anyChanged = true;
        nextMaps[mapId] = { ...map, locations: nextLocations };
      } else nextMaps[mapId] = map;
    }
    return anyChanged ? { ...project, maps: nextMaps } : project;
  }
  const projectReducer = (state, action) => {
    switch (action.type) {
      case "SET_PROJECT":
        return migrateMapLocationActions(repairOrphanBranchMarkers(migrateStatVariables(migrateItemCountVariableBounds(migrateInventorySlotButton(migrateProjectRemoveLegacyCommands(migrateProjectToUnifiedScreens(action.payload)))))));
      case "UPDATE_PROJECT": {
        return {
          ...state,
          ...action.payload
        };
      }
      case "UPDATE_PROJECT_TITLE": {
        return {
          ...state,
          title: action.payload.title
        };
      }
      default:
        if (action && action.type && !action.type.startsWith("UPDATE_PROJECT_")) {
          return state;
        }
        return state;
    }
  };
  const assetReducer = (state, action) => {
    switch (action.type) {
      case "ADD_ASSET": {
        const { assetType, asset } = action.payload;
        console.log("[assetReducer] ADD_ASSET:", { assetType, assetId: asset.id, assetName: asset.name });
        console.log("[assetReducer] Current state[assetType]:", state[assetType]);
        const newState = {
          ...state,
          [assetType]: {
            ...state[assetType],
            [asset.id]: asset
          }
        };
        console.log("[assetReducer] New state[assetType]:", newState[assetType]);
        return newState;
      }
      case "UPDATE_ASSET": {
        const { assetType, assetId, updates } = action.payload;
        const asset = state[assetType][assetId];
        if (!asset) return state;
        return {
          ...state,
          [assetType]: {
            ...state[assetType],
            [assetId]: { ...asset, ...updates }
          }
        };
      }
      case "DELETE_ASSET": {
        const { assetType, assetId } = action.payload;
        const { [assetId]: _, ...remainingAssets } = state[assetType];
        const fallbackId = Object.keys(remainingAssets)[0];
        let newState = { ...state, [assetType]: remainingAssets };
        const newScenes = JSON.parse(JSON.stringify(state.scenes));
        let scenesChanged = false;
        for (const sceneId in newScenes) {
          newScenes[sceneId].commands = newScenes[sceneId].commands.map((cmd) => {
            if (assetType === "backgrounds" && cmd.type === CommandType.SetBackground && cmd.backgroundId === assetId) {
              scenesChanged = true;
              return { ...cmd, backgroundId: fallbackId || "" };
            }
            if (assetType === "images" && cmd.type === CommandType.ShowImage && cmd.imageId === assetId) {
              scenesChanged = true;
              return { ...cmd, imageId: fallbackId || "" };
            }
            if (assetType === "images" && cmd.type === CommandType.SetBackground && cmd.backgroundId === assetId) {
              scenesChanged = true;
              return { ...cmd, backgroundId: fallbackId || "" };
            }
            if (assetType === "audio" && (cmd.type === CommandType.PlayMusic || cmd.type === CommandType.PlaySoundEffect) && cmd.audioId === assetId) {
              scenesChanged = true;
              return { ...cmd, audioId: fallbackId || "" };
            }
            if (assetType === "videos" && cmd.type === CommandType.PlayMovie && cmd.videoId === assetId) {
              scenesChanged = true;
              return { ...cmd, videoId: fallbackId || "" };
            }
            return cmd;
          });
        }
        if (scenesChanged) {
          newState.scenes = newScenes;
        }
        return newState;
      }
      default:
        return state;
    }
  };
  const generateId\$7 = () => Math.random().toString(36).substring(2, 9);
  const characterReducer = (state, action) => {
    var _a, _b;
    switch (action.type) {
      case "ADD_CHARACTER": {
        const { name, color } = action.payload;
        const newId = \`char-\${generateId\$7()}\`;
        const newExprId = \`expr-\${generateId\$7()}\`;
        const newExpression = { id: newExprId, name: "Default", layerConfiguration: {} };
        const newCharacter = {
          id: newId,
          name,
          color,
          baseImageUrl: null,
          layers: {},
          expressions: { [newExprId]: newExpression }
        };
        return {
          ...state,
          characters: {
            ...state.characters,
            [newId]: newCharacter
          }
        };
      }
      case "DELETE_CHARACTER": {
        const { characterId } = action.payload;
        const { [characterId]: _, ...remaining } = state.characters;
        const fallbackId = Object.keys(remaining)[0];
        const newScenes = JSON.parse(JSON.stringify(state.scenes));
        for (const sceneId in newScenes) {
          newScenes[sceneId].commands = newScenes[sceneId].commands.map((cmd) => {
            if (cmd.type === CommandType.Dialogue && cmd.characterId === characterId) {
              return { ...cmd, characterId: null };
            }
            if ((cmd.type === CommandType.ShowCharacter || cmd.type === CommandType.HideCharacter) && cmd.characterId === characterId) {
              if (fallbackId) {
                return { ...cmd, characterId: fallbackId };
              }
            }
            return cmd;
          }).filter(Boolean);
        }
        return {
          ...state,
          characters: remaining,
          scenes: newScenes
        };
      }
      case "UPDATE_CHARACTER": {
        const { characterId, updates } = action.payload;
        const character = state.characters[characterId];
        if (!character) return state;
        const updatedCharacter = { ...character, ...updates };
        return {
          ...state,
          characters: {
            ...state.characters,
            [characterId]: updatedCharacter
          }
        };
      }
      case "ADD_CHARACTER_LAYER": {
        const { characterId, name } = action.payload;
        const character = state.characters[characterId];
        if (!character) return state;
        const newLayerId = \`layer-\${generateId\$7()}\`;
        const newLayer = { id: newLayerId, name, assets: {} };
        const newLayers = { ...character.layers, [newLayerId]: newLayer };
        return { ...state, characters: { ...state.characters, [characterId]: { ...character, layers: newLayers } } };
      }
      case "UPDATE_CHARACTER_LAYER": {
        const { characterId, layerId, name } = action.payload;
        const character = state.characters[characterId];
        if (!(character == null ? void 0 : character.layers[layerId])) return state;
        const updatedLayer = { ...character.layers[layerId], name };
        const newLayers = { ...character.layers, [layerId]: updatedLayer };
        return { ...state, characters: { ...state.characters, [characterId]: { ...character, layers: newLayers } } };
      }
      case "DELETE_CHARACTER_LAYER": {
        const { characterId, layerId } = action.payload;
        const character = state.characters[characterId];
        if (!character) return state;
        const { [layerId]: _, ...remainingLayers } = character.layers;
        const newExpressions = { ...character.expressions };
        for (const exprId in newExpressions) {
          delete newExpressions[exprId].layerConfiguration[layerId];
        }
        return { ...state, characters: { ...state.characters, [characterId]: { ...character, layers: remainingLayers, expressions: newExpressions } } };
      }
      case "ADD_LAYER_ASSET": {
        const { characterId, layerId, name, imageUrl, videoUrl, isVideo, loop, autoplay } = action.payload;
        const character = state.characters[characterId];
        if (!(character == null ? void 0 : character.layers[layerId])) return state;
        const newAssetId = action.payload.id || \`asset-\${generateId\$7()}\`;
        const newAsset = {
          id: newAssetId,
          name,
          imageUrl,
          videoUrl,
          isVideo,
          loop,
          autoplay
        };
        const newAssets = { ...character.layers[layerId].assets, [newAssetId]: newAsset };
        const newLayers = { ...character.layers, [layerId]: { ...character.layers[layerId], assets: newAssets } };
        return { ...state, characters: { ...state.characters, [characterId]: { ...character, layers: newLayers } } };
      }
      case "DELETE_LAYER_ASSET": {
        const { characterId, layerId, assetId } = action.payload;
        const character = state.characters[characterId];
        if (!((_a = character == null ? void 0 : character.layers[layerId]) == null ? void 0 : _a.assets[assetId])) return state;
        const { [assetId]: _, ...remainingAssets } = character.layers[layerId].assets;
        const newLayers = { ...character.layers, [layerId]: { ...character.layers[layerId], assets: remainingAssets } };
        const newExpressions = { ...character.expressions };
        for (const exprId in newExpressions) {
          if (newExpressions[exprId].layerConfiguration[layerId] === assetId) {
            newExpressions[exprId].layerConfiguration[layerId] = null;
          }
        }
        return { ...state, characters: { ...state.characters, [characterId]: { ...character, layers: newLayers, expressions: newExpressions } } };
      }
      case "UPDATE_LAYER_ASSET": {
        const { characterId, layerId, assetId, updates } = action.payload;
        const character = state.characters[characterId];
        const asset = (_b = character == null ? void 0 : character.layers[layerId]) == null ? void 0 : _b.assets[assetId];
        if (!asset) return state;
        const newAssets = { ...character.layers[layerId].assets, [assetId]: { ...asset, ...updates } };
        const newLayers = { ...character.layers, [layerId]: { ...character.layers[layerId], assets: newAssets } };
        return { ...state, characters: { ...state.characters, [characterId]: { ...character, layers: newLayers } } };
      }
      case "ADD_EXPRESSION": {
        const { characterId, name } = action.payload;
        const character = state.characters[characterId];
        if (!character) return state;
        const newExprId = \`expr-\${generateId\$7()}\`;
        const newExpression = { id: newExprId, name, layerConfiguration: {} };
        Object.keys(character.layers).forEach((layerId) => {
          newExpression.layerConfiguration[layerId] = null;
        });
        const newExpressions = { ...character.expressions, [newExprId]: newExpression };
        return { ...state, characters: { ...state.characters, [characterId]: { ...character, expressions: newExpressions } } };
      }
      case "UPDATE_EXPRESSION": {
        const { characterId, expressionId, updates } = action.payload;
        const character = state.characters[characterId];
        if (!(character == null ? void 0 : character.expressions[expressionId])) return state;
        const newExpression = { ...character.expressions[expressionId], ...updates };
        const newExpressions = { ...character.expressions, [expressionId]: newExpression };
        return { ...state, characters: { ...state.characters, [characterId]: { ...character, expressions: newExpressions } } };
      }
      case "DELETE_EXPRESSION": {
        const { characterId, expressionId } = action.payload;
        const character = state.characters[characterId];
        if (!character) return state;
        const { [expressionId]: _, ...remainingExpressions } = character.expressions;
        const newScenes = JSON.parse(JSON.stringify(state.scenes));
        const firstExprId = Object.keys(remainingExpressions)[0];
        for (const sceneId in newScenes) {
          newScenes[sceneId].commands.forEach((cmd) => {
            if (cmd.type === CommandType.ShowCharacter && cmd.expressionId === expressionId) {
              cmd.expressionId = firstExprId || "";
            }
          });
        }
        return { ...state, scenes: newScenes, characters: { ...state.characters, [characterId]: { ...character, expressions: remainingExpressions } } };
      }
      case "REORDER_CHARACTERS": {
        const { characterIds } = action.payload;
        const newCharacters = {};
        characterIds.forEach((id) => {
          if (state.characters[id]) newCharacters[id] = state.characters[id];
        });
        for (const id in state.characters) {
          if (!newCharacters[id]) newCharacters[id] = state.characters[id];
        }
        return { ...state, characters: newCharacters };
      }
      default:
        return state;
    }
  };
  const generateId\$6 = () => Math.random().toString(36).substring(2, 9);
  const sceneReducer = (state, action) => {
    switch (action.type) {
      case "ADD_SCENE": {
        const newId = \`scene-\${generateId\$6()}\`;
        const newScene = { id: newId, name: action.payload.name, commands: [] };
        return {
          ...state,
          scenes: { ...state.scenes, [newId]: newScene }
        };
      }
      case "UPDATE_SCENE": {
        const { sceneId, name } = action.payload;
        return {
          ...state,
          scenes: { ...state.scenes, [sceneId]: { ...state.scenes[sceneId], name } }
        };
      }
      case "UPDATE_SCENE_CONFIG": {
        const { sceneId, updates } = action.payload;
        return {
          ...state,
          scenes: { ...state.scenes, [sceneId]: { ...state.scenes[sceneId], ...updates } }
        };
      }
      case "DELETE_SCENE": {
        const { sceneId } = action.payload;
        if (Object.keys(state.scenes).length <= 1) return state;
        const { [sceneId]: _, ...remainingScenes } = state.scenes;
        let newStartSceneId = state.startSceneId;
        if (state.startSceneId === sceneId) {
          newStartSceneId = Object.keys(remainingScenes)[0] || "";
        }
        for (const sId in remainingScenes) {
          remainingScenes[sId].commands = remainingScenes[sId].commands.map((cmd) => {
            if (cmd.type === CommandType.Jump && cmd.targetSceneId === sceneId) {
              return { ...cmd, targetSceneId: newStartSceneId };
            }
            if (cmd.type === CommandType.Choice) {
              const choiceCmd = cmd;
              const newOptions = choiceCmd.options.map((opt) => {
                if (opt.targetSceneId === sceneId) {
                  return { ...opt, targetSceneId: newStartSceneId };
                }
                if (opt.actions) {
                  const newActions = opt.actions.map((action2) => {
                    if (action2.type === UIActionType.JumpToScene && action2.targetSceneId === sceneId) {
                      return { ...action2, targetSceneId: newStartSceneId };
                    }
                    return action2;
                  });
                  return { ...opt, actions: newActions };
                }
                return opt;
              });
              return { ...cmd, options: newOptions };
            }
            return cmd;
          });
        }
        return {
          ...state,
          scenes: remainingScenes,
          startSceneId: newStartSceneId
        };
      }
      case "DUPLICATE_SCENE": {
        const { sceneId } = action.payload;
        const originalScene = state.scenes[sceneId];
        if (!originalScene) return state;
        const newId = \`scene-\${generateId\$6()}\`;
        const branchIdRemap = /* @__PURE__ */ new Map();
        const duplicatedScene = {
          ...originalScene,
          id: newId,
          name: \`\${originalScene.name} (Copy)\`,
          commands: originalScene.commands.map((cmd) => {
            const copy = { ...cmd, id: \`cmd-\${generateId\$6()}\` };
            if (copy.branchId) {
              let mapped = branchIdRemap.get(copy.branchId);
              if (!mapped) {
                mapped = \`branch-\${generateId\$6()}\`;
                branchIdRemap.set(copy.branchId, mapped);
              }
              copy.branchId = mapped;
            }
            return copy;
          })
        };
        return {
          ...state,
          scenes: { ...state.scenes, [newId]: duplicatedScene }
        };
      }
      case "REORDER_SCENES": {
        const { sceneIds } = action.payload;
        const newScenes = {};
        sceneIds.forEach((id) => {
          if (state.scenes[id]) {
            newScenes[id] = state.scenes[id];
          }
        });
        return {
          ...state,
          scenes: newScenes
        };
      }
      case "SET_START_SCENE": {
        return { ...state, startSceneId: action.payload.sceneId };
      }
      case "UPDATE_SCENE_COMMANDS": {
        const { sceneId, commands } = action.payload;
        return {
          ...state,
          scenes: {
            ...state.scenes,
            [sceneId]: {
              ...state.scenes[sceneId],
              commands
            }
          }
        };
      }
      case "ADD_COMMAND": {
        const { sceneId, command } = action.payload;
        const scene = state.scenes[sceneId];
        const newCommands = [...scene.commands, { ...command, id: \`cmd-\${generateId\$6()}\` }];
        return {
          ...state,
          scenes: { ...state.scenes, [sceneId]: { ...scene, commands: newCommands } }
        };
      }
      case "UPDATE_COMMAND": {
        const { sceneId, commandIndex, command } = action.payload;
        const scene = state.scenes[sceneId];
        const newCommands = [...scene.commands];
        newCommands[commandIndex] = command;
        return {
          ...state,
          scenes: { ...state.scenes, [sceneId]: { ...scene, commands: newCommands } }
        };
      }
      case "DELETE_COMMAND": {
        const { sceneId, commandIndex } = action.payload;
        const scene = state.scenes[sceneId];
        const commandToDelete = scene.commands[commandIndex];
        if ((commandToDelete == null ? void 0 : commandToDelete.type) === CommandType.BranchStart) {
          const branchId = commandToDelete.branchId;
          const isBranchMarker = (cmd) => (cmd.type === CommandType.BranchStart || cmd.type === CommandType.BranchElseIf || cmd.type === CommandType.BranchElse || cmd.type === CommandType.BranchEnd) && cmd.branchId === branchId;
          const newCommands2 = scene.commands.filter((cmd) => !isBranchMarker(cmd));
          return {
            ...state,
            scenes: { ...state.scenes, [sceneId]: { ...scene, commands: newCommands2 } }
          };
        }
        if ((commandToDelete == null ? void 0 : commandToDelete.type) === CommandType.BranchEnd) {
          return state;
        }
        const newCommands = scene.commands.filter((_, index) => index !== commandIndex);
        return {
          ...state,
          scenes: { ...state.scenes, [sceneId]: { ...scene, commands: newCommands } }
        };
      }
      case "MOVE_COMMAND": {
        const { sceneId, fromIndex, toIndex } = action.payload;
        if (fromIndex === toIndex) return state;
        const scene = state.scenes[sceneId];
        const newCommands = [...scene.commands];
        const movedCommand = newCommands[fromIndex];
        if (movedCommand.type === CommandType.BranchStart) {
          const branchCmd = movedCommand;
          const branchEndIndex = newCommands.findIndex(
            (cmd, i) => i > fromIndex && cmd.type === CommandType.BranchEnd && cmd.branchId === branchCmd.branchId
          );
          if (branchEndIndex !== -1) {
            const branchLength = branchEndIndex - fromIndex + 1;
            const branchCommands = newCommands.splice(fromIndex, branchLength);
            let adjustedToIndex = toIndex;
            if (toIndex > fromIndex) {
              adjustedToIndex -= branchLength;
            }
            newCommands.splice(adjustedToIndex, 0, ...branchCommands);
            return {
              ...state,
              scenes: { ...state.scenes, [sceneId]: { ...scene, commands: newCommands } }
            };
          }
        }
        if (movedCommand.type === CommandType.BranchEnd) {
          return state;
        }
        const [removed] = newCommands.splice(fromIndex, 1);
        newCommands.splice(toIndex, 0, removed);
        return {
          ...state,
          scenes: { ...state.scenes, [sceneId]: { ...scene, commands: newCommands } }
        };
      }
      case "TOGGLE_GROUP_COLLAPSE": {
        const { sceneId, groupId } = action.payload;
        const scene = state.scenes[sceneId];
        const newCommands = scene.commands.map((cmd) => {
          if (cmd.id === groupId && cmd.type === CommandType.Group) {
            return { ...cmd, collapsed: !cmd.collapsed };
          }
          return cmd;
        });
        return {
          ...state,
          scenes: { ...state.scenes, [sceneId]: { ...scene, commands: newCommands } }
        };
      }
      case "TOGGLE_BRANCH_COLLAPSE": {
        const { sceneId, branchId } = action.payload;
        const scene = state.scenes[sceneId];
        const newCommands = scene.commands.map(
          (cmd) => cmd.type === CommandType.BranchStart && cmd.branchId === branchId ? { ...cmd, isCollapsed: !cmd.isCollapsed } : cmd
        );
        return {
          ...state,
          scenes: { ...state.scenes, [sceneId]: { ...scene, commands: newCommands } }
        };
      }
      case "ADD_COMMAND_TO_GROUP": {
        const { sceneId, groupId, commandId } = action.payload;
        const scene = state.scenes[sceneId];
        const newCommands = scene.commands.map((cmd) => {
          if (cmd.id === groupId && cmd.type === CommandType.Group) {
            const groupCmd = cmd;
            return { ...groupCmd, commandIds: [...groupCmd.commandIds || [], commandId] };
          }
          return cmd;
        });
        return {
          ...state,
          scenes: { ...state.scenes, [sceneId]: { ...scene, commands: newCommands } }
        };
      }
      case "REMOVE_COMMAND_FROM_GROUP": {
        const { sceneId, groupId, commandId } = action.payload;
        const scene = state.scenes[sceneId];
        const newCommands = scene.commands.map((cmd) => {
          if (cmd.id === groupId && cmd.type === CommandType.Group) {
            const groupCmd = cmd;
            return { ...groupCmd, commandIds: (groupCmd.commandIds || []).filter((id) => id !== commandId) };
          }
          return cmd;
        });
        return {
          ...state,
          scenes: { ...state.scenes, [sceneId]: { ...scene, commands: newCommands } }
        };
      }
      case "RENAME_GROUP": {
        const { sceneId, groupId, name } = action.payload;
        const scene = state.scenes[sceneId];
        const newCommands = scene.commands.map((cmd) => {
          if (cmd.id === groupId && cmd.type === CommandType.Group) {
            const groupCmd = cmd;
            return { ...groupCmd, name };
          }
          return cmd;
        });
        return {
          ...state,
          scenes: { ...state.scenes, [sceneId]: { ...scene, commands: newCommands } }
        };
      }
      case "REORDER_COMMANDS_IN_GROUP": {
        const { sceneId, groupId, commandIds } = action.payload;
        const scene = state.scenes[sceneId];
        const newCommands = scene.commands.map((cmd) => {
          if (cmd.id === groupId && cmd.type === CommandType.Group) {
            const groupCmd = cmd;
            return { ...groupCmd, commandIds };
          }
          return cmd;
        });
        return {
          ...state,
          scenes: { ...state.scenes, [sceneId]: { ...scene, commands: newCommands } }
        };
      }
      default:
        return state;
    }
  };
  const generateId\$5 = (prefix) => \`\${prefix}-\${Math.random().toString(36).substring(2, 9)}\`;
  const createDefaultUIScreens = () => {
    const titleScreenId = generateId\$5("screen");
    const saveScreenId = generateId\$5("screen");
    const loadScreenId = generateId\$5("screen");
    const settingsScreenId = generateId\$5("screen");
    const pauseScreenId = generateId\$5("screen");
    const defaultFont = { family: "Poppins, sans-serif", size: 24, color: "#f0e6ff", weight: "normal", italic: false };
    const titleFont = { family: "Poppins, sans-serif", size: 64, color: "#f0e6ff", weight: "bold", italic: false };
    const headerFont = { family: "Poppins, sans-serif", size: 48, color: "#f0e6ff", weight: "bold", italic: false };
    const titleScreenElements = {};
    const titleTextId = generateId\$5("el");
    titleScreenElements[titleTextId] = { id: titleTextId, name: "Title Text", type: UIElementType.Text, text: "My Visual Novel", x: 50, y: 30, width: 60, height: 15, anchorX: 0.5, anchorY: 0.5, font: titleFont, textAlign: "center", verticalAlign: "middle" };
    const newGameBtnId = generateId\$5("el");
    titleScreenElements[newGameBtnId] = { id: newGameBtnId, name: "New Game Button", type: UIElementType.Button, text: "New Game", x: 50, y: 55, width: 20, height: 8, anchorX: 0.5, anchorY: 0.5, font: defaultFont, action: { type: UIActionType.StartNewGame }, image: null, hoverImage: null, clickSoundId: null, hoverSoundId: null };
    const loadGameBtnId = generateId\$5("el");
    titleScreenElements[loadGameBtnId] = { id: loadGameBtnId, name: "Load Game Button", type: UIElementType.Button, text: "Load Game", x: 50, y: 65, width: 20, height: 8, anchorX: 0.5, anchorY: 0.5, font: defaultFont, action: { type: UIActionType.GoToScreen, targetScreenId: loadScreenId }, image: null, hoverImage: null, clickSoundId: null, hoverSoundId: null };
    const settingsBtnId = generateId\$5("el");
    titleScreenElements[settingsBtnId] = { id: settingsBtnId, name: "Settings Button", type: UIElementType.Button, text: "Settings", x: 50, y: 75, width: 20, height: 8, anchorX: 0.5, anchorY: 0.5, font: defaultFont, action: { type: UIActionType.GoToScreen, targetScreenId: settingsScreenId }, image: null, hoverImage: null, clickSoundId: null, hoverSoundId: null };
    const saveScreenElements = {};
    const saveHeaderId = generateId\$5("el");
    saveScreenElements[saveHeaderId] = { id: saveHeaderId, name: "Header", type: UIElementType.Text, text: "Save Game", x: 50, y: 10, width: 60, height: 10, anchorX: 0.5, anchorY: 0.5, font: headerFont, textAlign: "center", verticalAlign: "middle" };
    const saveGridId = generateId\$5("el");
    saveScreenElements[saveGridId] = { id: saveGridId, name: "Save Slots", type: UIElementType.SaveSlotGrid, slotCount: 8, font: defaultFont, emptySlotText: "[ Empty Slot ]", x: 50, y: 50, width: 80, height: 65, anchorX: 0.5, anchorY: 0.5 };
    const saveBackBtnId = generateId\$5("el");
    saveScreenElements[saveBackBtnId] = { id: saveBackBtnId, name: "Back Button", type: UIElementType.Button, text: "Back", x: 50, y: 90, width: 20, height: 8, anchorX: 0.5, anchorY: 0.5, font: defaultFont, action: { type: UIActionType.ReturnToPreviousScreen }, image: null, hoverImage: null, clickSoundId: null, hoverSoundId: null };
    const loadScreenElements = {};
    const loadHeaderId = generateId\$5("el");
    loadScreenElements[loadHeaderId] = { id: loadHeaderId, name: "Header", type: UIElementType.Text, text: "Load Game", x: 50, y: 10, width: 60, height: 10, anchorX: 0.5, anchorY: 0.5, font: headerFont, textAlign: "center", verticalAlign: "middle" };
    const loadGridId = generateId\$5("el");
    loadScreenElements[loadGridId] = { id: loadGridId, name: "Load Slots", type: UIElementType.SaveSlotGrid, slotCount: 8, font: defaultFont, emptySlotText: "[ Empty Slot ]", x: 50, y: 50, width: 80, height: 65, anchorX: 0.5, anchorY: 0.5 };
    const loadBackBtnId = generateId\$5("el");
    loadScreenElements[loadBackBtnId] = { id: loadBackBtnId, name: "Back Button", type: UIElementType.Button, text: "Back", x: 50, y: 90, width: 20, height: 8, anchorX: 0.5, anchorY: 0.5, font: defaultFont, action: { type: UIActionType.ReturnToPreviousScreen }, image: null, hoverImage: null, clickSoundId: null, hoverSoundId: null };
    const settingsScreenElements = {};
    const settingsHeaderId = generateId\$5("el");
    settingsScreenElements[settingsHeaderId] = { id: settingsHeaderId, name: "Header", type: UIElementType.Text, text: "Settings", x: 50, y: 10, width: 60, height: 10, anchorX: 0.5, anchorY: 0.5, font: headerFont, textAlign: "center", verticalAlign: "middle" };
    const musicLabelId = generateId\$5("el");
    settingsScreenElements[musicLabelId] = { id: musicLabelId, name: "Music Volume Label", type: UIElementType.Text, text: "Music Volume", x: 35, y: 30, width: 20, height: 5, anchorX: 0.5, anchorY: 0.5, font: defaultFont, textAlign: "left", verticalAlign: "middle" };
    const musicSliderId = generateId\$5("el");
    settingsScreenElements[musicSliderId] = { id: musicSliderId, name: "Music Volume Slider", type: UIElementType.SettingsSlider, setting: "musicVolume", x: 65, y: 30, width: 40, height: 5, anchorX: 0.5, anchorY: 0.5 };
    const sfxLabelId = generateId\$5("el");
    settingsScreenElements[sfxLabelId] = { id: sfxLabelId, name: "SFX Volume Label", type: UIElementType.Text, text: "Sound FX Volume", x: 35, y: 40, width: 20, height: 5, anchorX: 0.5, anchorY: 0.5, font: defaultFont, textAlign: "left", verticalAlign: "middle" };
    const sfxSliderId = generateId\$5("el");
    settingsScreenElements[sfxSliderId] = { id: sfxSliderId, name: "SFX Volume Slider", type: UIElementType.SettingsSlider, setting: "sfxVolume", x: 65, y: 40, width: 40, height: 5, anchorX: 0.5, anchorY: 0.5 };
    const ambientLabelId = generateId\$5("el");
    settingsScreenElements[ambientLabelId] = { id: ambientLabelId, name: "Ambient Volume Label", type: UIElementType.Text, text: "Ambient Volume", x: 35, y: 50, width: 20, height: 5, anchorX: 0.5, anchorY: 0.5, font: defaultFont, textAlign: "left", verticalAlign: "middle" };
    const ambientSliderId = generateId\$5("el");
    settingsScreenElements[ambientSliderId] = { id: ambientSliderId, name: "Ambient Volume Slider", type: UIElementType.SettingsSlider, setting: "ambientVolume", x: 65, y: 50, width: 40, height: 5, anchorX: 0.5, anchorY: 0.5 };
    const textSpeedLabelId = generateId\$5("el");
    settingsScreenElements[textSpeedLabelId] = { id: textSpeedLabelId, name: "Text Speed Label", type: UIElementType.Text, text: "Text Speed", x: 35, y: 60, width: 20, height: 5, anchorX: 0.5, anchorY: 0.5, font: defaultFont, textAlign: "left", verticalAlign: "middle" };
    const textSpeedSliderId = generateId\$5("el");
    settingsScreenElements[textSpeedSliderId] = { id: textSpeedSliderId, name: "Text Speed Slider", type: UIElementType.SettingsSlider, setting: "textSpeed", x: 65, y: 60, width: 40, height: 5, anchorX: 0.5, anchorY: 0.5 };
    const skipToggleId = generateId\$5("el");
    settingsScreenElements[skipToggleId] = { id: skipToggleId, name: "Enable Skip Toggle", type: UIElementType.SettingsToggle, setting: "enableSkip", text: "Enable Skip", x: 50, y: 70, width: 30, height: 5, anchorX: 0.5, anchorY: 0.5, font: defaultFont };
    const backBtnId = generateId\$5("el");
    settingsScreenElements[backBtnId] = { id: backBtnId, name: "Back Button", type: UIElementType.Button, text: "Back", x: 50, y: 85, width: 20, height: 8, anchorX: 0.5, anchorY: 0.5, font: defaultFont, action: { type: UIActionType.ReturnToPreviousScreen }, image: null, hoverImage: null, clickSoundId: null, hoverSoundId: null };
    const pauseScreenElements = {};
    const returnBtnId = generateId\$5("el");
    pauseScreenElements[returnBtnId] = { id: returnBtnId, name: "Return Button", type: UIElementType.Button, text: "Return to Game", x: 50, y: 30, width: 30, height: 8, anchorX: 0.5, anchorY: 0.5, font: defaultFont, action: { type: UIActionType.ReturnToGame }, image: null, hoverImage: null, clickSoundId: null, hoverSoundId: null };
    const pauseSaveBtnId = generateId\$5("el");
    pauseScreenElements[pauseSaveBtnId] = { id: pauseSaveBtnId, name: "Save Game Button", type: UIElementType.Button, text: "Save Game", x: 50, y: 40, width: 30, height: 8, anchorX: 0.5, anchorY: 0.5, font: defaultFont, action: { type: UIActionType.GoToScreen, targetScreenId: saveScreenId }, image: null, hoverImage: null, clickSoundId: null, hoverSoundId: null };
    const pauseLoadBtnId = generateId\$5("el");
    pauseScreenElements[pauseLoadBtnId] = { id: pauseLoadBtnId, name: "Load Game Button", type: UIElementType.Button, text: "Load Game", x: 50, y: 50, width: 30, height: 8, anchorX: 0.5, anchorY: 0.5, font: defaultFont, action: { type: UIActionType.GoToScreen, targetScreenId: loadScreenId }, image: null, hoverImage: null, clickSoundId: null, hoverSoundId: null };
    const pauseSettingsBtnId = generateId\$5("el");
    pauseScreenElements[pauseSettingsBtnId] = { id: pauseSettingsBtnId, name: "Settings Button", type: UIElementType.Button, text: "Settings", x: 50, y: 60, width: 30, height: 8, anchorX: 0.5, anchorY: 0.5, font: defaultFont, action: { type: UIActionType.GoToScreen, targetScreenId: settingsScreenId }, image: null, hoverImage: null, clickSoundId: null, hoverSoundId: null };
    const pauseQuitBtnId = generateId\$5("el");
    pauseScreenElements[pauseQuitBtnId] = { id: pauseQuitBtnId, name: "Quit Button", type: UIElementType.Button, text: "Quit to Title", x: 50, y: 70, width: 30, height: 8, anchorX: 0.5, anchorY: 0.5, font: defaultFont, action: { type: UIActionType.QuitToTitle }, image: null, hoverImage: null, clickSoundId: null, hoverSoundId: null };
    const screens = {
      [titleScreenId]: {
        id: titleScreenId,
        name: "Title Screen",
        background: { type: "color", value: "#1a102c" },
        music: { audioId: null, policy: "continue" },
        ambientNoise: { audioId: null, policy: "continue" },
        elements: titleScreenElements
      },
      [saveScreenId]: { id: saveScreenId, name: "Save Screen", background: { type: "color", value: "#1a102c" }, music: { audioId: null, policy: "continue" }, ambientNoise: { audioId: null, policy: "continue" }, elements: saveScreenElements },
      [loadScreenId]: { id: loadScreenId, name: "Load Screen", background: { type: "color", value: "#1a102c" }, music: { audioId: null, policy: "continue" }, ambientNoise: { audioId: null, policy: "continue" }, elements: loadScreenElements },
      [settingsScreenId]: {
        id: settingsScreenId,
        name: "Settings Screen",
        background: { type: "color", value: "#1a102c" },
        music: { audioId: null, policy: "continue" },
        ambientNoise: { audioId: null, policy: "continue" },
        elements: settingsScreenElements
      },
      [pauseScreenId]: {
        id: pauseScreenId,
        name: "Pause Menu",
        background: { type: "color", value: "#1a102c" },
        music: { audioId: null, policy: "continue" },
        ambientNoise: { audioId: null, policy: "continue" },
        elements: pauseScreenElements
      }
    };
    const specialIds = { titleScreenId, saveScreenId, loadScreenId, settingsScreenId, pauseScreenId };
    return { screens, specialIds };
  };
  const generateId\$4 = () => Math.random().toString(36).substring(2, 9);
  const uiReducer = (state, action) => {
    var _a, _b;
    switch (action.type) {
      case "UPDATE_UI": {
        return {
          ...state,
          ui: {
            ...state.ui,
            ...action.payload
          }
        };
      }
      case "UPDATE_UI_CONFIG": {
        const { key, value } = action.payload;
        return {
          ...state,
          ui: {
            ...state.ui,
            [key]: value
          }
        };
      }
      case "UPDATE_UI_FONT_CONFIG": {
        const { target, property, value } = action.payload;
        return {
          ...state,
          ui: {
            ...state.ui,
            [target]: {
              ...state.ui[target],
              [property]: value
            }
          }
        };
      }
      case "RESTORE_DEFAULT_UI_SCREENS": {
        const { screens: defaultScreens, specialIds } = createDefaultUIScreens();
        return {
          ...state,
          uiScreens: {
            ...state.uiScreens,
            ...defaultScreens
          },
          ui: {
            ...state.ui,
            titleScreenId: specialIds.titleScreenId,
            settingsScreenId: specialIds.settingsScreenId,
            saveScreenId: specialIds.saveScreenId,
            loadScreenId: specialIds.loadScreenId,
            pauseScreenId: specialIds.pauseScreenId
          }
        };
      }
      case "ADD_TEXTBOX_THEME": {
        const id = action.payload.id || \`tbtheme-\${generateId\$4()}\`;
        const theme = { id, name: action.payload.name };
        return { ...state, textboxThemes: { ...state.textboxThemes || {}, [id]: theme } };
      }
      case "UPDATE_TEXTBOX_THEME": {
        const { themeId, updates } = action.payload;
        const existing = (_a = state.textboxThemes) == null ? void 0 : _a[themeId];
        if (!existing) return state;
        return { ...state, textboxThemes: { ...state.textboxThemes, [themeId]: { ...existing, ...updates, id: themeId } } };
      }
      case "DELETE_TEXTBOX_THEME": {
        if (!((_b = state.textboxThemes) == null ? void 0 : _b[action.payload.themeId])) return state;
        const next = { ...state.textboxThemes };
        delete next[action.payload.themeId];
        return { ...state, textboxThemes: next };
      }
      case "ADD_UI_SCREEN": {
        const { name, id } = action.payload;
        const newId = id || \`screen-\${generateId\$4()}\`;
        const newScreen = {
          id: newId,
          name,
          background: { type: "color", value: "#0f172a" },
          music: { audioId: null, policy: "continue", volume: 0.8 },
          ambientNoise: { audioId: null, policy: "continue", volume: 0.8 },
          elements: {},
          effects: []
        };
        return { ...state, uiScreens: { ...state.uiScreens, [newId]: newScreen } };
      }
      case "UPDATE_UI_SCREEN": {
        const { screenId, updates } = action.payload;
        const screen = state.uiScreens[screenId];
        if (!screen) return state;
        return { ...state, uiScreens: { ...state.uiScreens, [screenId]: { ...screen, ...updates } } };
      }
      case "DELETE_UI_SCREEN": {
        const { screenId } = action.payload;
        const specialScreenIds = [
          state.ui.titleScreenId,
          state.ui.settingsScreenId,
          state.ui.saveScreenId,
          state.ui.loadScreenId,
          state.ui.pauseScreenId
        ];
        if (specialScreenIds.includes(screenId)) {
          console.warn(\`Attempted to delete a special UI screen (\${screenId}), which is not allowed.\`);
          return state;
        }
        const { [screenId]: _, ...remainingScreens } = state.uiScreens;
        const newUiConfig = { ...state.ui };
        let uiConfigChanged = false;
        if (state.ui.titleScreenId === screenId) {
          newUiConfig.titleScreenId = null;
          uiConfigChanged = true;
        }
        if (state.ui.settingsScreenId === screenId) {
          newUiConfig.settingsScreenId = null;
          uiConfigChanged = true;
        }
        if (state.ui.saveScreenId === screenId) {
          newUiConfig.saveScreenId = null;
          uiConfigChanged = true;
        }
        if (state.ui.loadScreenId === screenId) {
          newUiConfig.loadScreenId = null;
          uiConfigChanged = true;
        }
        const cleanedScreens = Object.keys(remainingScreens).reduce((acc, sId) => {
          const screen = remainingScreens[sId];
          let screenModified = false;
          const newElements = Object.keys(screen.elements).reduce((elAcc, elId) => {
            const element = screen.elements[elId];
            if (element.type === UIElementType.Button) {
              const buttonAction = element.action;
              if (buttonAction.type === UIActionType.GoToScreen && buttonAction.targetScreenId === screenId) {
                elAcc[elId] = { ...element, action: { type: UIActionType.None } };
                screenModified = true;
              } else {
                elAcc[elId] = element;
              }
            } else {
              elAcc[elId] = element;
            }
            return elAcc;
          }, {});
          if (screenModified) {
            acc[sId] = { ...screen, elements: newElements };
          } else {
            acc[sId] = screen;
          }
          return acc;
        }, {});
        return {
          ...state,
          uiScreens: cleanedScreens,
          ...uiConfigChanged && { ui: newUiConfig }
        };
      }
      case "DUPLICATE_UI_SCREEN": {
        const { screenId } = action.payload;
        console.log("[DUPLICATE_UI_SCREEN] Duplicating screen:", screenId);
        const originalScreen = state.uiScreens[screenId];
        if (!originalScreen) {
          console.error("[DUPLICATE_UI_SCREEN] Screen not found:", screenId);
          return state;
        }
        const newScreenId = \`screen-\${generateId\$4()}\`;
        const newScreen = JSON.parse(JSON.stringify(originalScreen));
        newScreen.id = newScreenId;
        newScreen.name = \`Copy of \${originalScreen.name}\`;
        const newElements = {};
        for (const element of Object.values(originalScreen.elements)) {
          const newElementId = \`elem-\${generateId\$4()}\`;
          newElements[newElementId] = { ...element, id: newElementId };
        }
        newScreen.elements = newElements;
        console.log("[DUPLICATE_UI_SCREEN] Created new screen:", newScreenId, "with name:", newScreen.name);
        return {
          ...state,
          uiScreens: {
            ...state.uiScreens,
            [newScreenId]: newScreen
          }
        };
      }
      case "ADD_UI_ELEMENT": {
        const { screenId, element } = action.payload;
        const screen = state.uiScreens[screenId];
        if (!screen) return state;
        const newElements = { ...screen.elements, [element.id]: element };
        return { ...state, uiScreens: { ...state.uiScreens, [screenId]: { ...screen, elements: newElements } } };
      }
      case "UPDATE_UI_ELEMENT": {
        const { screenId, elementId, updates } = action.payload;
        const screen = state.uiScreens[screenId];
        const element = screen == null ? void 0 : screen.elements[elementId];
        if (!element) return state;
        const newElement = { ...element, ...updates };
        const newElements = { ...screen.elements, [elementId]: newElement };
        return { ...state, uiScreens: { ...state.uiScreens, [screenId]: { ...screen, elements: newElements } } };
      }
      case "DELETE_UI_ELEMENT": {
        const { screenId, elementId } = action.payload;
        const screen = state.uiScreens[screenId];
        if (!screen) return state;
        const { [elementId]: _, ...remainingElements } = screen.elements;
        return { ...state, uiScreens: { ...state.uiScreens, [screenId]: { ...screen, elements: remainingElements } } };
      }
      default:
        return state;
    }
  };
  const generateId\$3 = () => Math.random().toString(36).substring(2, 9);
  const variableReducer = (state, action) => {
    switch (action.type) {
      case "ADD_VARIABLE": {
        const newId = action.payload.id || \`var-\${generateId\$3()}\`;
        const newVar = { id: newId, name: action.payload.name, type: action.payload.type, defaultValue: action.payload.defaultValue };
        return {
          ...state,
          variables: { ...state.variables, [newId]: newVar }
        };
      }
      case "UPDATE_VARIABLE": {
        const { variableId, updates } = action.payload;
        return {
          ...state,
          variables: { ...state.variables, [variableId]: { ...state.variables[variableId], ...updates } }
        };
      }
      case "DELETE_VARIABLE": {
        const { variableId } = action.payload;
        console.log("[variableReducer] DELETE_VARIABLE received for:", variableId);
        console.log("[variableReducer] Current variables:", Object.keys(state.variables));
        const { [variableId]: deletedVar, ...remainingVars } = state.variables;
        console.log("[variableReducer] Deleted variable:", (deletedVar == null ? void 0 : deletedVar.name) || "NOT FOUND");
        console.log("[variableReducer] Remaining variables:", Object.keys(remainingVars));
        const newScenes = { ...state.scenes };
        for (const sceneId in newScenes) {
          newScenes[sceneId].commands = newScenes[sceneId].commands.filter((cmd) => !(cmd.type === CommandType.SetVariable && cmd.variableId === variableId)).map((cmd) => {
            let newCmd = { ...cmd };
            if (newCmd.conditions) {
              const filteredConditions = newCmd.conditions.filter((c) => c.variableId !== variableId);
              if (filteredConditions.length === 0) {
                delete newCmd.conditions;
              } else {
                newCmd.conditions = filteredConditions;
              }
            }
            if (newCmd.type === CommandType.Choice) {
              const newOptions = newCmd.options.map((opt) => {
                const newOpt = { ...opt };
                if (newOpt.conditions) {
                  newOpt.conditions = newOpt.conditions.filter((c) => c.variableId !== variableId);
                  if (newOpt.conditions.length === 0) {
                    delete newOpt.conditions;
                  }
                }
                if (opt.actions) {
                  const filteredActions = opt.actions.filter((action2) => {
                    if (action2.type === UIActionType.SetVariable) {
                      return action2.variableId !== variableId;
                    }
                    return true;
                  });
                  newOpt.actions = filteredActions;
                }
                return newOpt;
              });
              return { ...newCmd, options: newOptions };
            }
            return newCmd;
          });
        }
        return {
          ...state,
          variables: remainingVars,
          scenes: newScenes
        };
      }
      default:
        return state;
    }
  };
  const scriptReducer = (state, action) => {
    switch (action.type) {
      case "ADD_SCRIPT": {
        const { script } = action.payload;
        return {
          ...state,
          scripts: {
            ...state.scripts || {},
            [script.id]: script
          }
        };
      }
      case "UPDATE_SCRIPT": {
        const { scriptId, updates } = action.payload;
        const scripts = state.scripts || {};
        const existing = scripts[scriptId];
        if (!existing) return state;
        return {
          ...state,
          scripts: {
            ...scripts,
            [scriptId]: {
              ...existing,
              ...updates,
              updatedAt: (/* @__PURE__ */ new Date()).toISOString()
            }
          }
        };
      }
      case "DELETE_SCRIPT": {
        const { scriptId } = action.payload;
        const scripts = state.scripts || {};
        const { [scriptId]: _, ...remaining } = scripts;
        const newScenes = JSON.parse(JSON.stringify(state.scenes));
        for (const sceneId in newScenes) {
          newScenes[sceneId].commands = newScenes[sceneId].commands.map((cmd) => {
            if (cmd.type === "RunScript" && cmd.scriptId === scriptId) {
              return { ...cmd, scriptId: "" };
            }
            return cmd;
          });
        }
        return {
          ...state,
          scripts: remaining,
          scenes: newScenes
        };
      }
      default:
        return state;
    }
  };
  const generateId\$2 = () => Math.random().toString(36).substring(2, 9);
  const commonEventReducer = (state, action) => {
    switch (action.type) {
      case "ADD_COMMON_EVENT": {
        const { commonEvent } = action.payload;
        return {
          ...state,
          commonEvents: {
            ...state.commonEvents || {},
            [commonEvent.id]: commonEvent
          }
        };
      }
      case "UPDATE_COMMON_EVENT": {
        const { commonEventId, updates } = action.payload;
        const commonEvents = state.commonEvents || {};
        const existing = commonEvents[commonEventId];
        if (!existing) return state;
        return {
          ...state,
          commonEvents: {
            ...commonEvents,
            [commonEventId]: {
              ...existing,
              ...updates,
              updatedAt: (/* @__PURE__ */ new Date()).toISOString()
            }
          }
        };
      }
      case "DELETE_COMMON_EVENT": {
        const { commonEventId } = action.payload;
        const commonEvents = state.commonEvents || {};
        const { [commonEventId]: _, ...remaining } = commonEvents;
        const newScenes = JSON.parse(JSON.stringify(state.scenes));
        for (const sceneId in newScenes) {
          newScenes[sceneId].commands = newScenes[sceneId].commands.map((cmd) => {
            if (cmd.type === "CallCommonEvent" && cmd.commonEventId === commonEventId) {
              return { ...cmd, commonEventId: "" };
            }
            return cmd;
          });
        }
        return {
          ...state,
          commonEvents: remaining,
          scenes: newScenes
        };
      }
      case "DUPLICATE_COMMON_EVENT": {
        const { commonEventId } = action.payload;
        const commonEvents = state.commonEvents || {};
        const original = commonEvents[commonEventId];
        if (!original) return state;
        const now = (/* @__PURE__ */ new Date()).toISOString();
        const newId = \`ce-\${generateId\$2()}\`;
        const duplicate = {
          ...JSON.parse(JSON.stringify(original)),
          id: newId,
          name: \`\${original.name} (copy)\`,
          createdAt: now,
          updatedAt: now,
          // Re-generate command IDs
          commands: original.commands.map((cmd) => ({
            ...JSON.parse(JSON.stringify(cmd)),
            id: \`cmd-\${generateId\$2()}\`
          }))
        };
        return {
          ...state,
          commonEvents: {
            ...commonEvents,
            [newId]: duplicate
          }
        };
      }
      case "ADD_COMMON_EVENT_COMMAND": {
        const { commonEventId, command, index } = action.payload;
        const commonEvents = state.commonEvents || {};
        const existing = commonEvents[commonEventId];
        if (!existing) return state;
        const newCmd = { ...command, id: command.id || \`cmd-\${generateId\$2()}\` };
        const commands = [...existing.commands];
        if (index !== void 0 && index >= 0 && index <= commands.length) {
          commands.splice(index, 0, newCmd);
        } else {
          commands.push(newCmd);
        }
        return {
          ...state,
          commonEvents: {
            ...commonEvents,
            [commonEventId]: {
              ...existing,
              commands,
              updatedAt: (/* @__PURE__ */ new Date()).toISOString()
            }
          }
        };
      }
      case "UPDATE_COMMON_EVENT_COMMAND": {
        const { commonEventId, commandIndex, updates } = action.payload;
        const commonEvents = state.commonEvents || {};
        const existing = commonEvents[commonEventId];
        if (!existing || commandIndex < 0 || commandIndex >= existing.commands.length) return state;
        const commands = [...existing.commands];
        commands[commandIndex] = { ...commands[commandIndex], ...updates };
        return {
          ...state,
          commonEvents: {
            ...commonEvents,
            [commonEventId]: {
              ...existing,
              commands,
              updatedAt: (/* @__PURE__ */ new Date()).toISOString()
            }
          }
        };
      }
      case "DELETE_COMMON_EVENT_COMMAND": {
        const { commonEventId, commandIndex } = action.payload;
        const commonEvents = state.commonEvents || {};
        const existing = commonEvents[commonEventId];
        if (!existing || commandIndex < 0 || commandIndex >= existing.commands.length) return state;
        const commands = [...existing.commands];
        commands.splice(commandIndex, 1);
        return {
          ...state,
          commonEvents: {
            ...commonEvents,
            [commonEventId]: {
              ...existing,
              commands,
              updatedAt: (/* @__PURE__ */ new Date()).toISOString()
            }
          }
        };
      }
      case "REORDER_COMMON_EVENT_COMMANDS": {
        const { commonEventId, fromIndex, toIndex } = action.payload;
        const commonEvents = state.commonEvents || {};
        const existing = commonEvents[commonEventId];
        if (!existing) return state;
        const commands = [...existing.commands];
        const [moved] = commands.splice(fromIndex, 1);
        commands.splice(toIndex, 0, moved);
        return {
          ...state,
          commonEvents: {
            ...commonEvents,
            [commonEventId]: {
              ...existing,
              commands,
              updatedAt: (/* @__PURE__ */ new Date()).toISOString()
            }
          }
        };
      }
      case "ADD_COMMON_EVENT_PARAMETER": {
        const { commonEventId, parameter } = action.payload;
        const commonEvents = state.commonEvents || {};
        const existing = commonEvents[commonEventId];
        if (!existing) return state;
        return {
          ...state,
          commonEvents: {
            ...commonEvents,
            [commonEventId]: {
              ...existing,
              parameters: [...existing.parameters, parameter],
              updatedAt: (/* @__PURE__ */ new Date()).toISOString()
            }
          }
        };
      }
      case "UPDATE_COMMON_EVENT_PARAMETER": {
        const { commonEventId, parameterId, updates } = action.payload;
        const commonEvents = state.commonEvents || {};
        const existing = commonEvents[commonEventId];
        if (!existing) return state;
        const parameters = existing.parameters.map(
          (p) => p.id === parameterId ? { ...p, ...updates } : p
        );
        return {
          ...state,
          commonEvents: {
            ...commonEvents,
            [commonEventId]: {
              ...existing,
              parameters,
              updatedAt: (/* @__PURE__ */ new Date()).toISOString()
            }
          }
        };
      }
      case "DELETE_COMMON_EVENT_PARAMETER": {
        const { commonEventId, parameterId } = action.payload;
        const commonEvents = state.commonEvents || {};
        const existing = commonEvents[commonEventId];
        if (!existing) return state;
        const stripArg = (cmd) => {
          if (cmd.type === "CallCommonEvent" && cmd.commonEventId === commonEventId && cmd.arguments && parameterId in cmd.arguments) {
            const { [parameterId]: _drop, ...restArgs } = cmd.arguments;
            return { ...cmd, arguments: restArgs };
          }
          return cmd;
        };
        const newScenes = {};
        for (const sceneId in state.scenes) {
          const scene = state.scenes[sceneId];
          newScenes[sceneId] = { ...scene, commands: scene.commands.map(stripArg) };
        }
        const updatedEvents = {};
        for (const ceId in commonEvents) {
          const ce = commonEvents[ceId];
          updatedEvents[ceId] = { ...ce, commands: ce.commands.map(stripArg) };
        }
        updatedEvents[commonEventId] = {
          ...updatedEvents[commonEventId],
          parameters: existing.parameters.filter((p) => p.id !== parameterId),
          updatedAt: (/* @__PURE__ */ new Date()).toISOString()
        };
        return {
          ...state,
          scenes: newScenes,
          commonEvents: updatedEvents
        };
      }
      default:
        return state;
    }
  };
  const pluginReducer = (state, action) => {
    switch (action.type) {
      case "INSTALL_PLUGIN": {
        const { plugin } = action.payload;
        const plugins = state.plugins || {};
        const registry = state.pluginRegistry || {};
        return {
          ...state,
          plugins: {
            ...plugins,
            [plugin.manifest.id]: plugin
          },
          pluginRegistry: {
            ...registry,
            [plugin.manifest.id]: {
              pluginId: plugin.manifest.id,
              enabled: true,
              config: plugin.config || {},
              registeredCommands: [],
              registeredEffects: []
            }
          }
        };
      }
      case "UPDATE_PLUGIN": {
        const { pluginId, manifest, source, resources } = action.payload;
        const plugins = state.plugins || {};
        const existing = plugins[pluginId];
        if (!existing) return state;
        return {
          ...state,
          plugins: {
            ...plugins,
            [pluginId]: {
              ...existing,
              manifest,
              source,
              ...resources !== void 0 ? { resources } : {}
            }
          }
          // pluginRegistry (enabled + config) and pluginStorage (data) intentionally untouched.
        };
      }
      case "UNINSTALL_PLUGIN": {
        const { pluginId } = action.payload;
        const plugins = { ...state.plugins || {} };
        const registry = { ...state.pluginRegistry || {} };
        delete plugins[pluginId];
        delete registry[pluginId];
        return {
          ...state,
          plugins,
          pluginRegistry: registry
        };
      }
      case "ENABLE_PLUGIN": {
        const { pluginId } = action.payload;
        const plugins = state.plugins || {};
        const registry = state.pluginRegistry || {};
        const plugin = plugins[pluginId];
        if (!plugin) return state;
        return {
          ...state,
          plugins: {
            ...plugins,
            [pluginId]: { ...plugin, state: "enabled", lastToggled: (/* @__PURE__ */ new Date()).toISOString() }
          },
          pluginRegistry: {
            ...registry,
            [pluginId]: { ...registry[pluginId], enabled: true }
          }
        };
      }
      case "DISABLE_PLUGIN": {
        const { pluginId } = action.payload;
        const plugins = state.plugins || {};
        const registry = state.pluginRegistry || {};
        const plugin = plugins[pluginId];
        if (!plugin) return state;
        return {
          ...state,
          plugins: {
            ...plugins,
            [pluginId]: { ...plugin, state: "disabled", lastToggled: (/* @__PURE__ */ new Date()).toISOString() }
          },
          pluginRegistry: {
            ...registry,
            [pluginId]: { ...registry[pluginId], enabled: false }
          }
        };
      }
      case "UPDATE_PLUGIN_CONFIG": {
        const { pluginId, config } = action.payload;
        const plugins = state.plugins || {};
        const registry = state.pluginRegistry || {};
        const plugin = plugins[pluginId];
        if (!plugin) return state;
        return {
          ...state,
          plugins: {
            ...plugins,
            [pluginId]: { ...plugin, config }
          },
          pluginRegistry: {
            ...registry,
            [pluginId]: { ...registry[pluginId], config }
          }
        };
      }
      case "UPDATE_PLUGIN_REGISTRY": {
        const { pluginId, entry } = action.payload;
        const registry = state.pluginRegistry || {};
        const existing = registry[pluginId];
        if (!existing) return state;
        return {
          ...state,
          pluginRegistry: {
            ...registry,
            [pluginId]: { ...existing, ...entry }
          }
        };
      }
      case "SET_PLUGIN_STORAGE": {
        const { pluginId, key, value } = action.payload;
        const storage = state.pluginStorage || {};
        const pluginBucket = storage[pluginId] || {};
        return {
          ...state,
          pluginStorage: {
            ...storage,
            [pluginId]: { ...pluginBucket, [key]: value }
          }
        };
      }
      case "SET_PLUGIN_BUILD_INCLUDED": {
        const { pluginId, included } = action.payload;
        const plugins = state.plugins || {};
        const p = plugins[pluginId];
        if (!p) return state;
        return {
          ...state,
          plugins: { ...plugins, [pluginId]: { ...p, includeInBuild: included } }
        };
      }
      case "SET_PLUGIN_HIDE_IN_TESTPLAY": {
        const { pluginId, hidden } = action.payload;
        const plugins = state.plugins || {};
        const p = plugins[pluginId];
        if (!p) return state;
        return {
          ...state,
          plugins: { ...plugins, [pluginId]: { ...p, hideInTestPlay: hidden } }
        };
      }
      case "SET_PLUGIN_RESOURCES": {
        const { pluginId, resources } = action.payload;
        const plugins = state.plugins || {};
        const p = plugins[pluginId];
        if (!p) return state;
        return {
          ...state,
          plugins: { ...plugins, [pluginId]: { ...p, resources } }
        };
      }
      default:
        return state;
    }
  };
  const generateId\$1 = () => Math.random().toString(36).substring(2, 9);
  const makeEntryCountVar = (id, name, startQty, scope) => ({
    id,
    name,
    type: "number",
    defaultValue: Math.max(0, Math.floor(startQty) || 0),
    scope: scope || "global",
    min: 0,
    // stock never goes negative
    isInternal: true
    // hidden from the Variables manager (kept calm); still works by id everywhere
  });
  const itemReducer = (state, action) => {
    var _a, _b, _c, _d;
    switch (action.type) {
      case "ADD_ITEM": {
        const p = action.payload;
        const itemId = p.id || \`item-\${generateId\$1()}\`;
        const items = state.items || {};
        let variables = state.variables;
        let countVariableId = p.countVariableId;
        if (!countVariableId || !variables[countVariableId]) {
          countVariableId = countVariableId || \`var-\${generateId\$1()}\`;
          const countVar = {
            id: countVariableId,
            name: p.name,
            type: "number",
            defaultValue: 0,
            scope: p.scope || "global",
            min: 0
            // inventory counts never go negative (Use/Destroy clamp here)
          };
          variables = { ...variables, [countVariableId]: countVar };
        }
        const newItem = {
          id: itemId,
          name: p.name,
          description: p.description,
          icon: p.icon ?? null,
          countVariableId,
          usable: p.usable ?? false,
          useEffect: p.useEffect,
          price: p.price,
          unique: p.unique,
          category: p.category,
          order: p.order ?? Object.keys(items).length
        };
        return {
          ...state,
          variables,
          items: { ...items, [itemId]: newItem }
        };
      }
      case "UPDATE_ITEM": {
        const { itemId, updates } = action.payload;
        const items = state.items || {};
        const existing = items[itemId];
        if (!existing) return state;
        const merged = { ...existing, ...updates };
        let variables = state.variables;
        if (updates.name && existing.countVariableId && variables[existing.countVariableId]) {
          variables = {
            ...variables,
            [existing.countVariableId]: { ...variables[existing.countVariableId], name: updates.name }
          };
        }
        return {
          ...state,
          variables,
          items: { ...items, [itemId]: merged }
        };
      }
      case "DELETE_ITEM": {
        const { itemId, deleteCountVariable } = action.payload;
        const items = state.items || {};
        const existing = items[itemId];
        if (!existing) return state;
        const { [itemId]: _removed, ...remainingItems } = items;
        let variables = state.variables;
        if (deleteCountVariable && existing.countVariableId && variables[existing.countVariableId]) {
          const { [existing.countVariableId]: _v, ...rest } = variables;
          variables = rest;
        }
        return {
          ...state,
          variables,
          items: remainingItems
        };
      }
      case "ADD_ITEM_COLLECTION": {
        const p = action.payload;
        const id = p.id || \`coll-\${generateId\$1()}\`;
        const collections = state.itemCollections || {};
        const newCollection = {
          id,
          name: p.name,
          entries: [],
          order: Object.keys(collections).length
        };
        return { ...state, itemCollections: { ...collections, [id]: newCollection } };
      }
      case "UPDATE_ITEM_COLLECTION": {
        const { collectionId, updates } = action.payload;
        const collections = state.itemCollections || {};
        const existing = collections[collectionId];
        if (!existing) return state;
        const merged = { ...existing, ...updates };
        let variables = state.variables;
        if (updates.name && updates.name !== existing.name) {
          merged.entries.forEach((e) => {
            var _a2, _b2;
            const v = variables[e.countVariableId];
            if (v) {
              const itemName = ((_b2 = (_a2 = state.items) == null ? void 0 : _a2[e.itemId]) == null ? void 0 : _b2.name) || "Item";
              variables = { ...variables, [e.countVariableId]: { ...v, name: \`\${itemName} — \${updates.name}\` } };
            }
          });
        }
        return { ...state, variables, itemCollections: { ...collections, [collectionId]: merged } };
      }
      case "DELETE_ITEM_COLLECTION": {
        const { collectionId, deleteCountVariables } = action.payload;
        const collections = state.itemCollections || {};
        const existing = collections[collectionId];
        if (!existing) return state;
        const { [collectionId]: _removed, ...remaining } = collections;
        let variables = state.variables;
        if (deleteCountVariables && !existing.tracksOwnedItems) {
          variables = { ...variables };
          existing.entries.forEach((e) => {
            delete variables[e.countVariableId];
          });
        }
        return { ...state, variables, itemCollections: remaining };
      }
      case "ADD_COLLECTION_ENTRY": {
        const { collectionId, itemId, startQty, scope } = action.payload;
        const collections = state.itemCollections || {};
        const existing = collections[collectionId];
        if (!existing) return state;
        if (existing.entries.some((e) => e.itemId === itemId)) return state;
        if (existing.tracksOwnedItems) {
          const ownedVarId = (_b = (_a = state.items) == null ? void 0 : _a[itemId]) == null ? void 0 : _b.countVariableId;
          if (!ownedVarId) return state;
          const entry2 = { itemId, countVariableId: ownedVarId };
          return { ...state, itemCollections: { ...collections, [collectionId]: { ...existing, entries: [...existing.entries, entry2] } } };
        }
        const countVariableId = \`var-\${generateId\$1()}\`;
        const itemName = ((_d = (_c = state.items) == null ? void 0 : _c[itemId]) == null ? void 0 : _d.name) || "Item";
        const countVar = makeEntryCountVar(countVariableId, \`\${itemName} — \${existing.name}\`, startQty ?? 0, scope);
        const entry = { itemId, countVariableId, startQty: startQty ?? 0 };
        const merged = { ...existing, entries: [...existing.entries, entry] };
        return {
          ...state,
          variables: { ...state.variables, [countVariableId]: countVar },
          itemCollections: { ...collections, [collectionId]: merged }
        };
      }
      case "UPDATE_COLLECTION_ENTRY": {
        const { collectionId, itemId, updates } = action.payload;
        const collections = state.itemCollections || {};
        const existing = collections[collectionId];
        if (!existing) return state;
        const idx = existing.entries.findIndex((e) => e.itemId === itemId);
        if (idx < 0) return state;
        const entry = existing.entries[idx];
        const mergedEntry = { ...entry, ...updates };
        const entries = [...existing.entries];
        entries[idx] = mergedEntry;
        let variables = state.variables;
        if (updates.startQty !== void 0 && variables[entry.countVariableId]) {
          variables = {
            ...variables,
            [entry.countVariableId]: { ...variables[entry.countVariableId], defaultValue: Math.max(0, Math.floor(updates.startQty) || 0) }
          };
        }
        return { ...state, variables, itemCollections: { ...collections, [collectionId]: { ...existing, entries } } };
      }
      case "REMOVE_COLLECTION_ENTRY": {
        const { collectionId, itemId, deleteCountVariable } = action.payload;
        const collections = state.itemCollections || {};
        const existing = collections[collectionId];
        if (!existing) return state;
        const entry = existing.entries.find((e) => e.itemId === itemId);
        if (!entry) return state;
        let variables = state.variables;
        if (deleteCountVariable !== false && !existing.tracksOwnedItems && variables[entry.countVariableId]) {
          const { [entry.countVariableId]: _v, ...rest } = variables;
          variables = rest;
        }
        return {
          ...state,
          variables,
          itemCollections: { ...collections, [collectionId]: { ...existing, entries: existing.entries.filter((e) => e.itemId !== itemId) } }
        };
      }
      default:
        return state;
    }
  };
  const reducers = [
    projectReducer,
    assetReducer,
    characterReducer,
    sceneReducer,
    uiReducer,
    variableReducer,
    scriptReducer,
    commonEventReducer,
    pluginReducer,
    itemReducer,
    statReducer
  ];
  const rootReducer = (state, action) => {
    for (const reducer of reducers) {
      const newState = reducer(state, action);
      if (newState !== state) {
        return newState;
      }
    }
    return state;
  };
  const DB_NAME = "flourish-vne";
  const DB_VERSION = 1;
  const PROJECT_STORE = "projects";
  const META_STORE = "metadata";
  function openDB() {
    return new Promise((resolve, reject) => {
      const request = indexedDB.open(DB_NAME, DB_VERSION);
      request.onupgradeneeded = () => {
        const db = request.result;
        if (!db.objectStoreNames.contains(PROJECT_STORE)) {
          db.createObjectStore(PROJECT_STORE);
        }
        if (!db.objectStoreNames.contains(META_STORE)) {
          db.createObjectStore(META_STORE);
        }
      };
      request.onsuccess = () => resolve(request.result);
      request.onerror = () => reject(request.error);
    });
  }
  async function saveProjectToIDB(project) {
    const db = await openDB();
    const tx = db.transaction([PROJECT_STORE, META_STORE], "readwrite");
    const projectStore = tx.objectStore(PROJECT_STORE);
    const metaStore = tx.objectStore(META_STORE);
    const key = \`autosave:\${project.id}\`;
    projectStore.put(project, key);
    const meta = {
      key,
      projectId: project.id,
      title: project.title || "Untitled Project",
      savedAt: Date.now(),
      isAutoSave: true
    };
    metaStore.put(meta, key);
    return new Promise((resolve, reject) => {
      tx.oncomplete = () => {
        db.close();
        resolve();
      };
      tx.onerror = () => {
        db.close();
        reject(tx.error);
      };
    });
  }
  const LOG_LEVELS = {
    debug: 0,
    info: 1,
    warn: 2,
    error: 3
  };
  let currentLevel = "warn";
  function shouldLog(level) {
    return LOG_LEVELS[level] >= LOG_LEVELS[currentLevel];
  }
  function formatPrefix(module) {
    return \`[\${module}]\`;
  }
  function createLogger(module) {
    const prefix = formatPrefix(module);
    return {
      debug: (...args) => {
        if (shouldLog("debug")) console.log(prefix, ...args);
      },
      info: (...args) => {
        if (shouldLog("info")) console.info(prefix, ...args);
      },
      warn: (...args) => {
        if (shouldLog("warn")) console.warn(prefix, ...args);
      },
      error: (...args) => {
        if (shouldLog("error")) console.error(prefix, ...args);
      }
    };
  }
  class WorkflowTracker {
    constructor() {
      this.actions = [];
      this.patterns = /* @__PURE__ */ new Map();
      this.maxActions = 1e3;
    }
    static getInstance() {
      if (!WorkflowTracker.instance) {
        WorkflowTracker.instance = new WorkflowTracker();
      }
      return WorkflowTracker.instance;
    }
    /**
     * Start tracking an action
     */
    startAction(action, context) {
      this.currentAction = action;
      this.currentActionStart = /* @__PURE__ */ new Date();
    }
    /**
     * End tracking current action
     */
    endAction(success = true) {
      if (!this.currentAction || !this.currentActionStart) return;
      const duration = Date.now() - this.currentActionStart.getTime();
      const workflowAction = {
        id: \`action_\${Date.now()}_\${Math.random().toString(36).substr(2, 9)}\`,
        action: this.currentAction,
        context: "",
        timestamp: this.currentActionStart,
        duration,
        success
      };
      this.actions.unshift(workflowAction);
      if (this.actions.length > this.maxActions) {
        this.actions = this.actions.slice(0, this.maxActions);
      }
      this.analyzePatterns();
      this.currentAction = void 0;
      this.currentActionStart = void 0;
    }
    /**
     * Track simple action without duration
     */
    trackAction(action, context) {
      this.startAction(action, context);
      this.endAction(true);
    }
    /**
     * Analyze workflow patterns
     */
    analyzePatterns() {
      if (this.actions.length < 3) return;
      const recentActions = this.actions.slice(0, 5).map((a) => a.action);
      const patternKey = recentActions.join("->");
      const existing = this.patterns.get(patternKey);
      if (existing) {
        existing.frequency++;
        existing.lastOccurred = /* @__PURE__ */ new Date();
      } else {
        this.patterns.set(patternKey, {
          id: patternKey,
          actions: recentActions,
          frequency: 1,
          averageDuration: 0,
          lastOccurred: /* @__PURE__ */ new Date()
        });
      }
    }
    /**
     * Get workflow statistics
     */
    getStatistics() {
      const totalActions = this.actions.length;
      const successfulActions = this.actions.filter((a) => a.success).length;
      const actionsWithDuration = this.actions.filter((a) => a.duration !== void 0);
      const averageActionTime = actionsWithDuration.length > 0 ? actionsWithDuration.reduce((sum, a) => sum + (a.duration || 0), 0) / actionsWithDuration.length : 0;
      const actionCounts = /* @__PURE__ */ new Map();
      this.actions.forEach((a) => {
        actionCounts.set(a.action, (actionCounts.get(a.action) || 0) + 1);
      });
      const mostCommonActions = Array.from(actionCounts.entries()).map(([action, count]) => ({ action, count })).sort((a, b) => b.count - a.count).slice(0, 5);
      const commonPatterns = Array.from(this.patterns.values()).sort((a, b) => b.frequency - a.frequency).slice(0, 5);
      const efficiencyScore = totalActions > 0 ? Math.min(100, Math.round(successfulActions / totalActions * 100)) : 100;
      return {
        totalActions,
        averageActionTime,
        mostCommonActions,
        commonPatterns,
        efficiencyScore
      };
    }
    /**
     * Get optimization suggestions
     */
    getOptimizationSuggestions() {
      const suggestions = [];
      const stats = this.getStatistics();
      if (stats.mostCommonActions.length > 0) {
        const topAction = stats.mostCommonActions[0];
        if (topAction.count > 10) {
          suggestions.push(\`Consider using keyboard shortcuts for "\${topAction.action}" (used \${topAction.count} times)\`);
        }
      }
      if (stats.commonPatterns.length > 0) {
        const topPattern = stats.commonPatterns[0];
        if (topPattern.frequency > 3) {
          suggestions.push(\`You frequently perform: \${topPattern.actions.join(" → ")}. Consider using a template!\`);
        }
      }
      if (stats.efficiencyScore < 80) {
        suggestions.push("Try using the preview feature more often to catch issues early");
      }
      return suggestions;
    }
    /**
     * Clear tracking data
     */
    clearData() {
      this.actions = [];
      this.patterns.clear();
    }
  }
  WorkflowTracker.getInstance();
  const ToastContext = React2.createContext(null);
  const useToast = () => {
    const context = React2.useContext(ToastContext);
    if (!context) {
      throw new Error("useToast must be used within a ToastProvider");
    }
    return context;
  };
  const CheckIcon = () => /* @__PURE__ */ jsxRuntime2.jsx("svg", { className: "w-5 h-5", fill: "none", stroke: "currentColor", viewBox: "0 0 24 24", children: /* @__PURE__ */ jsxRuntime2.jsx("path", { strokeLinecap: "round", strokeLinejoin: "round", strokeWidth: 2, d: "M5 13l4 4L19 7" }) });
  const ErrorIcon = () => /* @__PURE__ */ jsxRuntime2.jsx("svg", { className: "w-5 h-5", fill: "none", stroke: "currentColor", viewBox: "0 0 24 24", children: /* @__PURE__ */ jsxRuntime2.jsx("path", { strokeLinecap: "round", strokeLinejoin: "round", strokeWidth: 2, d: "M6 18L18 6M6 6l12 12" }) });
  const WarningIcon = () => /* @__PURE__ */ jsxRuntime2.jsx("svg", { className: "w-5 h-5", fill: "none", stroke: "currentColor", viewBox: "0 0 24 24", children: /* @__PURE__ */ jsxRuntime2.jsx("path", { strokeLinecap: "round", strokeLinejoin: "round", strokeWidth: 2, d: "M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z" }) });
  const InfoIcon = () => /* @__PURE__ */ jsxRuntime2.jsx("svg", { className: "w-5 h-5", fill: "none", stroke: "currentColor", viewBox: "0 0 24 24", children: /* @__PURE__ */ jsxRuntime2.jsx("path", { strokeLinecap: "round", strokeLinejoin: "round", strokeWidth: 2, d: "M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z" }) });
  const CloseIcon = () => /* @__PURE__ */ jsxRuntime2.jsx("svg", { className: "w-4 h-4", fill: "none", stroke: "currentColor", viewBox: "0 0 24 24", children: /* @__PURE__ */ jsxRuntime2.jsx("path", { strokeLinecap: "round", strokeLinejoin: "round", strokeWidth: 2, d: "M6 18L18 6M6 6l12 12" }) });
  const TOAST_STYLES = {
    success: {
      bg: "bg-emerald-900/95",
      border: "border-emerald-500",
      icon: "text-emerald-400",
      iconComponent: CheckIcon
    },
    error: {
      bg: "bg-red-900/95",
      border: "border-red-500",
      icon: "text-red-400",
      iconComponent: ErrorIcon
    },
    warning: {
      bg: "bg-amber-900/95",
      border: "border-amber-500",
      icon: "text-amber-400",
      iconComponent: WarningIcon
    },
    info: {
      bg: "bg-sky-900/95",
      border: "border-sky-500",
      icon: "text-sky-400",
      iconComponent: InfoIcon
    }
  };
  const ToastItem = ({ toast, onRemove }) => {
    const [isExiting, setIsExiting] = React2.useState(false);
    const style = TOAST_STYLES[toast.type];
    const IconComponent = style.iconComponent;
    const handleRemove = React2.useCallback(() => {
      setIsExiting(true);
      setTimeout(() => onRemove(toast.id), 200);
    }, [toast.id, onRemove]);
    React2.useEffect(() => {
      if (toast.duration !== 0) {
        const timer = setTimeout(handleRemove, toast.duration || 3e3);
        return () => clearTimeout(timer);
      }
    }, [toast.duration, handleRemove]);
    return /* @__PURE__ */ jsxRuntime2.jsxs(
      "div",
      {
        className: \`
                flex items-start gap-3 px-4 py-3 rounded-lg border shadow-lg backdrop-blur-sm
                \${style.bg} \${style.border}
                transform transition-all duration-200 ease-out
                \${isExiting ? "opacity-0 translate-x-4" : "opacity-100 translate-x-0"}
            \`,
        role: "alert",
        children: [
          /* @__PURE__ */ jsxRuntime2.jsx("span", { className: \`flex-shrink-0 \${style.icon}\`, children: /* @__PURE__ */ jsxRuntime2.jsx(IconComponent, {}) }),
          /* @__PURE__ */ jsxRuntime2.jsxs("div", { className: "flex-1 min-w-0", children: [
            /* @__PURE__ */ jsxRuntime2.jsx("p", { className: "text-sm text-white", children: toast.message }),
            toast.action && /* @__PURE__ */ jsxRuntime2.jsx(
              "button",
              {
                onClick: () => {
                  var _a;
                  (_a = toast.action) == null ? void 0 : _a.onClick();
                  handleRemove();
                },
                className: "mt-1 text-xs font-medium text-sky-300 hover:text-sky-200 underline",
                children: toast.action.label
              }
            )
          ] }),
          /* @__PURE__ */ jsxRuntime2.jsx(
            "button",
            {
              onClick: handleRemove,
              className: "flex-shrink-0 text-slate-400 hover:text-white transition-colors",
              "aria-label": "Dismiss",
              children: /* @__PURE__ */ jsxRuntime2.jsx(CloseIcon, {})
            }
          )
        ]
      }
    );
  };
  const ToastProvider = ({ children }) => {
    const [toasts, setToasts] = React2.useState([]);
    const toastIdRef = React2.useRef(0);
    const removeToast = React2.useCallback((id) => {
      setToasts((prev) => prev.filter((t) => t.id !== id));
    }, []);
    const addToast = React2.useCallback(
      (message, type = "info", options) => {
        const id = \`toast-\${++toastIdRef.current}\`;
        const newToast = {
          id,
          message,
          type,
          duration: options == null ? void 0 : options.duration,
          action: options == null ? void 0 : options.action
        };
        setToasts((prev) => [...prev, newToast]);
      },
      []
    );
    const success = React2.useCallback(
      (message, options) => addToast(message, "success", options),
      [addToast]
    );
    const error = React2.useCallback(
      (message, options) => addToast(message, "error", { duration: 5e3, ...options }),
      [addToast]
    );
    const warning = React2.useCallback(
      (message, options) => addToast(message, "warning", { duration: 4e3, ...options }),
      [addToast]
    );
    const info = React2.useCallback(
      (message, options) => addToast(message, "info", options),
      [addToast]
    );
    return /* @__PURE__ */ jsxRuntime2.jsxs(ToastContext.Provider, { value: { toasts, addToast, removeToast, success, error, warning, info }, children: [
      children,
      /* @__PURE__ */ jsxRuntime2.jsx(
        "div",
        {
          className: "fixed bottom-4 right-4 z-[9999] flex flex-col gap-2 max-w-sm w-full pointer-events-none",
          "aria-live": "polite",
          "aria-label": "Notifications",
          children: toasts.map((toast) => /* @__PURE__ */ jsxRuntime2.jsx("div", { className: "pointer-events-auto", children: /* @__PURE__ */ jsxRuntime2.jsx(ToastItem, { toast, onRemove: removeToast }) }, toast.id))
        }
      )
    ] });
  };
  const ENGINE_VERSION = "2.0.0";
  const EXT_DB_STORAGE_PREFIX = "__extdb__";
  const compareVersions = (a, b) => {
    const pa = a.split(".").map(Number);
    const pb = b.split(".").map(Number);
    for (let i = 0; i < 3; i++) {
      const d = (pa[i] || 0) - (pb[i] || 0);
      if (d !== 0) return d;
    }
    return 0;
  };
  class PluginManagerService {
    constructor() {
      this.loadedPlugins = /* @__PURE__ */ new Map();
      this.registeredCommands = /* @__PURE__ */ new Map();
      this.registeredEffects = /* @__PURE__ */ new Map();
      this.registeredPanels = /* @__PURE__ */ new Map();
      this.registeredMenuItems = /* @__PURE__ */ new Map();
      this.registeredDbCategories = /* @__PURE__ */ new Map();
      this.registeredUIElementTypes = /* @__PURE__ */ new Map();
      this.listeners = /* @__PURE__ */ new Set();
      this.host = null;
      this.runtime = null;
      this.testPlayActive = false;
    }
    static getInstance() {
      if (!PluginManagerService.instance) {
        PluginManagerService.instance = new PluginManagerService();
      }
      return PluginManagerService.instance;
    }
    /** Set the editor-level host bridge (project/dispatch/toast). Called once at app mount. */
    setHost(bridge) {
      this.host = bridge;
    }
    /** Set (or clear with null) the runtime bridge for live variable access during play. */
    setRuntime(bridge) {
      this.runtime = bridge;
    }
    // ── Lifecycle (signatures match PluginManagerUI usage) ────────────────────
    /**
     * Install a plugin from source: validate, check deps/engine, dispatch INSTALL,
     * load hooks, and fire onLoad/onEnable. Returns the created VNPlugin (throws on error).
     */
    loadPlugin(source, project, dispatch, resources) {
      var _a, _b;
      const manifest = this.extractManifest(source);
      if (!manifest) throw new Error("Plugin must define a \`manifest\` object.");
      const { valid, errors } = validatePluginManifest(manifest);
      if (!valid) throw new Error(errors.join(" "));
      if ((project.plugins || {})[manifest.id]) {
        throw new Error(\`A plugin with id "\${manifest.id}" is already installed.\`);
      }
      if (manifest.engineVersion && compareVersions(manifest.engineVersion, ENGINE_VERSION) > 0) {
        throw new Error(\`Requires engine \${manifest.engineVersion}+ (current: \${ENGINE_VERSION}).\`);
      }
      for (const dep of manifest.dependencies || []) {
        if (!(project.plugins || {})[dep]) {
          throw new Error(\`Missing dependency: "\${dep}". Install it first.\`);
        }
      }
      const plugin = createPlugin(manifest, source, {}, resources);
      dispatch({ type: "INSTALL_PLUGIN", payload: { plugin } });
      const api2 = this.createPluginAPI(manifest);
      const hooks = this.parsePluginSource(source, api2, manifest.target);
      this.loadedPlugins.set(manifest.id, { hooks, api: api2 });
      try {
        (_a = hooks.onLoad) == null ? void 0 : _a.call(hooks, api2);
      } catch (e) {
        console.error(\`[Plugin \${manifest.id}] onLoad failed:\`, e);
      }
      try {
        (_b = hooks.onEnable) == null ? void 0 : _b.call(hooks, api2);
      } catch (e) {
        console.error(\`[Plugin \${manifest.id}] onEnable failed:\`, e);
      }
      this.notifyListeners();
      return plugin;
    }
    enablePlugin(pluginId, project, dispatch) {
      var _a, _b, _c, _d;
      dispatch({ type: "ENABLE_PLUGIN", payload: { pluginId } });
      const plugin = (project.plugins || {})[pluginId];
      if (!plugin) return;
      let loaded = this.loadedPlugins.get(pluginId);
      if (!loaded) {
        const api2 = this.createPluginAPI(plugin.manifest);
        const hooks = this.parsePluginSource(plugin.source, api2, plugin.manifest.target);
        loaded = { hooks, api: api2 };
        this.loadedPlugins.set(pluginId, loaded);
        try {
          (_b = (_a = loaded.hooks).onLoad) == null ? void 0 : _b.call(_a, loaded.api);
        } catch (e) {
          console.error(e);
        }
      }
      try {
        (_d = (_c = loaded.hooks).onEnable) == null ? void 0 : _d.call(_c, loaded.api);
      } catch (e) {
        console.error(\`[Plugin \${pluginId}] onEnable failed:\`, e);
      }
      this.notifyListeners();
    }
    disablePlugin(pluginId, _project, dispatch) {
      var _a, _b;
      dispatch({ type: "DISABLE_PLUGIN", payload: { pluginId } });
      const loaded = this.loadedPlugins.get(pluginId);
      try {
        (_b = loaded == null ? void 0 : (_a = loaded.hooks).onDisable) == null ? void 0 : _b.call(_a, loaded.api);
      } catch (e) {
        console.error(\`[Plugin \${pluginId}] onDisable failed:\`, e);
      }
      this.unregisterFor(pluginId);
      this.loadedPlugins.delete(pluginId);
      this.notifyListeners();
    }
    /**
     * Update an installed plugin to a new version from source — WITHOUT uninstall/reinstall, so the
     * user's config + stored data survive. Validates the new manifest, requires the SAME plugin id,
     * tears down the running old instance, swaps code/manifest/resources in state, then re-loads and
     * re-enables if it was enabled. Returns the new manifest (throws on error). Allows downgrades
     * (the UI warns); does not force a version bump.
     */
    updatePlugin(pluginId, source, project, dispatch, resources) {
      var _a, _b, _c, _d;
      const existing = (project.plugins || {})[pluginId];
      if (!existing) throw new Error("That plugin is not installed.");
      const manifest = this.extractManifest(source);
      if (!manifest) throw new Error("Plugin must define a \`manifest\` object.");
      const { valid, errors } = validatePluginManifest(manifest);
      if (!valid) throw new Error(errors.join(" "));
      if (manifest.id !== pluginId) {
        throw new Error(\`This file is a different plugin ("\${manifest.id}"), not an update for "\${existing.manifest.name}" ("\${pluginId}").\`);
      }
      if (manifest.engineVersion && compareVersions(manifest.engineVersion, ENGINE_VERSION) > 0) {
        throw new Error(\`Requires engine \${manifest.engineVersion}+ (current: \${ENGINE_VERSION}).\`);
      }
      const wasEnabled = existing.state === "enabled";
      const loaded = this.loadedPlugins.get(pluginId);
      try {
        (_b = loaded == null ? void 0 : (_a = loaded.hooks).onDisable) == null ? void 0 : _b.call(_a, loaded.api);
      } catch (e) {
        console.error(\`[Plugin \${pluginId}] onDisable failed:\`, e);
      }
      this.unregisterFor(pluginId);
      this.loadedPlugins.delete(pluginId);
      dispatch({ type: "UPDATE_PLUGIN", payload: { pluginId, manifest, source, ...resources !== void 0 ? { resources } : {} } });
      const api2 = this.createPluginAPI(manifest);
      const hooks = this.parsePluginSource(source, api2, manifest.target);
      this.loadedPlugins.set(pluginId, { hooks, api: api2 });
      try {
        (_c = hooks.onLoad) == null ? void 0 : _c.call(hooks, api2);
      } catch (e) {
        console.error(\`[Plugin \${pluginId}] onLoad failed:\`, e);
      }
      if (wasEnabled) {
        try {
          (_d = hooks.onEnable) == null ? void 0 : _d.call(hooks, api2);
        } catch (e) {
          console.error(\`[Plugin \${pluginId}] onEnable failed:\`, e);
        }
      }
      this.notifyListeners();
      return manifest;
    }
    uninstallPlugin(pluginId, dispatch) {
      var _a, _b;
      const loaded = this.loadedPlugins.get(pluginId);
      try {
        (_b = loaded == null ? void 0 : (_a = loaded.hooks).onUninstall) == null ? void 0 : _b.call(_a, loaded.api);
      } catch (e) {
        console.error(\`[Plugin \${pluginId}] onUninstall failed:\`, e);
      }
      this.unregisterFor(pluginId);
      this.loadedPlugins.delete(pluginId);
      dispatch({ type: "UNINSTALL_PLUGIN", payload: { pluginId } });
      this.notifyListeners();
    }
    /** Ensure exactly the project's enabled plugins are loaded (load missing, unload stale). */
    ensureLoaded(project) {
      var _a, _b;
      const plugins = project.plugins || {};
      const enabledIds = new Set(Object.values(plugins).filter((p) => p.state === "enabled").map((p) => p.manifest.id));
      for (const id of Array.from(this.loadedPlugins.keys())) {
        if (!enabledIds.has(id)) {
          this.unregisterFor(id);
          this.loadedPlugins.delete(id);
        }
      }
      for (const plugin of Object.values(plugins)) {
        if (plugin.state !== "enabled") continue;
        if (this.loadedPlugins.has(plugin.manifest.id)) continue;
        try {
          const api2 = this.createPluginAPI(plugin.manifest);
          const hooks = this.parsePluginSource(plugin.source, api2, plugin.manifest.target);
          this.loadedPlugins.set(plugin.manifest.id, { hooks, api: api2 });
          (_a = hooks.onLoad) == null ? void 0 : _a.call(hooks, api2);
          (_b = hooks.onEnable) == null ? void 0 : _b.call(hooks, api2);
        } catch (e) {
          console.error(\`[Plugin \${plugin.manifest.id}] failed to load:\`, e);
        }
      }
      this.notifyListeners();
    }
    unregisterFor(pluginId) {
      for (const [key, v] of Array.from(this.registeredCommands.entries())) {
        if (v.pluginId === pluginId) this.registeredCommands.delete(key);
      }
      for (const [key, v] of Array.from(this.registeredEffects.entries())) {
        if (v.pluginId === pluginId) this.registeredEffects.delete(key);
      }
      for (const [key, v] of Array.from(this.registeredPanels.entries())) {
        if (v.pluginId === pluginId) this.registeredPanels.delete(key);
      }
      for (const [key, v] of Array.from(this.registeredMenuItems.entries())) {
        if (v.pluginId === pluginId) this.registeredMenuItems.delete(key);
      }
      for (const [key, v] of Array.from(this.registeredDbCategories.entries())) {
        if (v.pluginId === pluginId) this.registeredDbCategories.delete(key);
      }
      for (const [key, v] of Array.from(this.registeredUIElementTypes.entries())) {
        if (v.pluginId === pluginId) this.registeredUIElementTypes.delete(key);
      }
    }
    // ── Registries (read by the palette / inspector / executor) ───────────────
    getRegisteredCommands() {
      return Array.from(this.registeredCommands.values()).map((v) => v.def);
    }
    getCommand(type) {
      var _a;
      return (_a = this.registeredCommands.get(type)) == null ? void 0 : _a.def;
    }
    getRegisteredEffects() {
      return Array.from(this.registeredEffects.values()).map((v) => v.def);
    }
    getEffect(type) {
      var _a;
      return (_a = this.registeredEffects.get(type)) == null ? void 0 : _a.def;
    }
    /** All editor panels contributed by enabled extensions (with their owning plugin id). */
    getRegisteredPanels() {
      return Array.from(this.registeredPanels.values()).map((v) => ({ panel: v.def, pluginId: v.pluginId }));
    }
    /** All editor menu items / tools contributed by enabled extensions (with their owning plugin id). */
    getRegisteredMenuItems() {
      return Array.from(this.registeredMenuItems.values()).map((v) => ({ item: v.def, pluginId: v.pluginId }));
    }
    /** All custom database categories contributed by enabled extensions (with their owning plugin id). */
    getRegisteredDbCategories() {
      return Array.from(this.registeredDbCategories.values()).map((v) => ({ category: v.def, pluginId: v.pluginId }));
    }
    /** All custom UI element types contributed by enabled extensions (with their owning plugin id). */
    getRegisteredUIElementTypes() {
      return Array.from(this.registeredUIElementTypes.values()).map((v) => ({ def: v.def, pluginId: v.pluginId }));
    }
    /** Look up one custom UI element type by its (namespaced) type id — used by the renderer. */
    getUIElementType(type) {
      var _a;
      return (_a = this.registeredUIElementTypes.get(type)) == null ? void 0 : _a.def;
    }
    /** Build the editor context handed to a panel's render() (project read, dispatch, notify, storage). */
    buildPanelContext(pluginId) {
      const self = this;
      return {
        getProject: () => {
          var _a;
          return ((_a = self.host) == null ? void 0 : _a.getProject()) || null;
        },
        dispatch: (action) => {
          var _a;
          return (_a = self.host) == null ? void 0 : _a.dispatch(action);
        },
        notify: (message, type) => {
          var _a;
          return (_a = self.host) == null ? void 0 : _a.notify(message, type);
        },
        getStorage: (key) => {
          var _a, _b, _c, _d;
          return (_d = (_c = (_b = (_a = self.host) == null ? void 0 : _a.getProject()) == null ? void 0 : _b.pluginStorage) == null ? void 0 : _c[pluginId]) == null ? void 0 : _d[key];
        },
        setStorage: (key, value) => {
          var _a;
          return (_a = self.host) == null ? void 0 : _a.dispatch({ type: "SET_PLUGIN_STORAGE", payload: { pluginId, key, value } });
        }
      };
    }
    /** Get the live PluginAPI for a loaded plugin (used to run a custom command's handler). */
    getApi(pluginId) {
      var _a;
      return (_a = this.loadedPlugins.get(pluginId)) == null ? void 0 : _a.api;
    }
    /** The plugin id that owns a registered command type (e.g. "pluginId.cmd" → "pluginId"). */
    getCommandOwner(type) {
      var _a;
      return (_a = this.registeredCommands.get(type)) == null ? void 0 : _a.pluginId;
    }
    setTestPlayActive(active) {
      var _a, _b, _c, _d, _e;
      if (this.testPlayActive === active) return;
      this.testPlayActive = active;
      const project = (_a = this.host) == null ? void 0 : _a.getProject();
      if (!project) return;
      for (const [id, loaded] of this.loadedPlugins.entries()) {
        const p = (project.plugins || {})[id];
        if (!p || !p.hideInTestPlay) continue;
        try {
          if (active) (_c = (_b = loaded.hooks).onDisable) == null ? void 0 : _c.call(_b, loaded.api);
          else (_e = (_d = loaded.hooks).onEnable) == null ? void 0 : _e.call(_d, loaded.api);
        } catch (e) {
          console.error(\`[PluginManager] test-play \${active ? "hide" : "restore"} failed for \${id}:\`, e);
        }
      }
    }
    // ── Hooks ─────────────────────────────────────────────────────────────────
    /** Invoke a lifecycle hook across all loaded plugins (synchronous; errors isolated). */
    invokeHook(hookName, ...args) {
      for (const [pluginId, loaded] of this.loadedPlugins.entries()) {
        const fn = loaded.hooks[hookName];
        if (typeof fn === "function") {
          try {
            fn(loaded.api, ...args);
          } catch (err) {
            console.error(\`[PluginManager] Hook \${hookName} failed for \${pluginId}:\`, err);
          }
        }
      }
    }
    // ── Listeners ─────────────────────────────────────────────────────────────
    addListener(cb) {
      this.listeners.add(cb);
    }
    removeListener(cb) {
      this.listeners.delete(cb);
    }
    notifyListeners() {
      this.listeners.forEach((cb) => {
        try {
          cb();
        } catch {
        }
      });
    }
    // ── Source parsing / sandbox ──────────────────────────────────────────────
    /** Public: read a plugin's manifest from its source WITHOUT installing it (for an install preview /
     *  trust prompt). Returns null if the source has no valid manifest. */
    peekManifest(source) {
      return this.extractManifest(source);
    }
    /** Extract just the manifest from source (run in a throwaway sandbox), for validation. */
    extractManifest(source) {
      try {
        const fn = new Function(\`
                "use strict";
                \${this.sandboxPreamble()}
                \${source}
                return typeof manifest !== 'undefined' ? manifest : (typeof plugin !== 'undefined' && plugin ? plugin.manifest : undefined);
            \`);
        return fn() || null;
      } catch (err) {
        console.error("[PluginManager] Failed to read manifest:", err);
        return null;
      }
    }
    /** Editor extensions (\`target: editor|both\`) run on the author's machine with FULL trust (DOM /
     *  network allowed) and are never shipped to players — so they get NO sandbox shadowing. Runtime
     *  plugins (default) stay sandboxed because they're bundled into exported games run by others. */
    sandboxPreamble(target) {
      if (target === "editor" || target === "both") return "";
      const blocked = [
        "document",
        "window",
        "globalThis",
        "self",
        "fetch",
        "XMLHttpRequest",
        "WebSocket",
        "localStorage",
        "sessionStorage",
        "indexedDB",
        "Function",
        "setTimeout",
        "setInterval",
        "clearTimeout",
        "clearInterval",
        "requestAnimationFrame",
        "queueMicrotask",
        "importScripts",
        "require"
      ];
      return blocked.map((g) => \`var \${g} = undefined;\`).join("\\n");
    }
    parsePluginSource(source, api2, target) {
      var _a;
      const fullTrust = target === "editor" || target === "both";
      try {
        if (!fullTrust && /\\.\\s*constructor\\b/.test(source)) {
          throw new Error('Access to ".constructor" is blocked in the plugin sandbox.');
        }
        if (!fullTrust && /\\beval\\s*\\(/.test(source)) {
          throw new Error("eval() is blocked in the plugin sandbox.");
        }
        const hooksFn = new Function("api", \`
                "use strict";
                \${this.sandboxPreamble(target)}
                \${source}
                return typeof plugin !== 'undefined' ? plugin : {};
            \`);
        return hooksFn(api2) || {};
      } catch (err) {
        console.error("[PluginManager] Failed to parse plugin source:", err);
        (_a = this.host) == null ? void 0 : _a.notify(\`Plugin failed to load: \${err.message}\`, "error");
        return {};
      }
    }
    // ── PluginAPI ─────────────────────────────────────────────────────────────
    createPluginAPI(manifest) {
      const pluginId = manifest.id;
      const self = this;
      const project = () => {
        var _a;
        return ((_a = self.host) == null ? void 0 : _a.getProject()) || null;
      };
      return {
        manifest,
        getVariable: (nameOrId) => {
          if (self.runtime) return self.runtime.getVariable(nameOrId);
          const p = project();
          if (!p) return void 0;
          const v = p.variables[nameOrId] || Object.values(p.variables).find((x) => x.name.toLowerCase() === nameOrId.toLowerCase());
          return v ? v.defaultValue : void 0;
        },
        setVariable: (nameOrId, value) => {
          if (self.runtime) {
            self.runtime.setVariable(nameOrId, value);
            return;
          }
          console.warn(\`[Plugin \${pluginId}] setVariable("\${nameOrId}") ignored — only writable during gameplay.\`);
        },
        getProjectInfo: () => {
          const p = project();
          return {
            title: (p == null ? void 0 : p.title) || "",
            version: p == null ? void 0 : p.version,
            sceneCount: p ? Object.keys(p.scenes).length : 0,
            characterCount: p ? Object.keys(p.characters).length : 0
          };
        },
        getScenes: () => {
          const p = project();
          return p ? Object.entries(p.scenes).map(([id, scene]) => ({ id, name: scene.name })) : [];
        },
        getCharacters: () => {
          const p = project();
          return p ? Object.entries(p.characters).map(([id, char]) => ({ id, name: char.name })) : [];
        },
        notify: (message, type) => {
          var _a, _b, _c;
          (_c = ((_a = self.runtime) == null ? void 0 : _a.notify) || ((_b = self.host) == null ? void 0 : _b.notify)) == null ? void 0 : _c(message, type);
        },
        log: (...args) => console.log(\`[Plugin \${pluginId}]\`, ...args),
        getConfig: () => {
          var _a, _b, _c;
          const defaults = {};
          for (const f of manifest.settings || []) if (f.defaultValue !== void 0) defaults[f.name] = f.defaultValue;
          const saved = ((_c = (_b = (_a = project()) == null ? void 0 : _a.plugins) == null ? void 0 : _b[pluginId]) == null ? void 0 : _c.config) || {};
          return { ...defaults, ...saved };
        },
        getResource: (name) => {
          var _a, _b, _c, _d;
          return (_d = (_c = (_b = (_a = project()) == null ? void 0 : _a.plugins) == null ? void 0 : _b[pluginId]) == null ? void 0 : _c.resources) == null ? void 0 : _d[name];
        },
        getStorage: (key) => {
          var _a, _b;
          const p = project();
          return (_b = (_a = p == null ? void 0 : p.pluginStorage) == null ? void 0 : _a[pluginId]) == null ? void 0 : _b[key];
        },
        setStorage: (key, value) => {
          var _a;
          (_a = self.host) == null ? void 0 : _a.dispatch({ type: "SET_PLUGIN_STORAGE", payload: { pluginId, key, value } });
        },
        registerCommand: (commandDef) => {
          const fullType = commandDef.type.startsWith(pluginId + ".") ? commandDef.type : \`\${pluginId}.\${commandDef.type}\`;
          self.registeredCommands.set(fullType, { def: { ...commandDef, type: fullType }, pluginId });
          self.notifyListeners();
        },
        registerEffect: (effectDef) => {
          const fullType = effectDef.type.startsWith(pluginId + ".") ? effectDef.type : \`\${pluginId}.\${effectDef.type}\`;
          self.registeredEffects.set(fullType, { def: { ...effectDef, type: fullType }, pluginId });
          self.notifyListeners();
        },
        registerPanel: (panel) => {
          const fullId = panel.id.startsWith(pluginId + ".") ? panel.id : \`\${pluginId}.\${panel.id}\`;
          self.registeredPanels.set(fullId, { def: { ...panel, id: fullId }, pluginId });
          self.notifyListeners();
        },
        registerMenuItem: (item) => {
          const fullId = item.id.startsWith(pluginId + ".") ? item.id : \`\${pluginId}.\${item.id}\`;
          self.registeredMenuItems.set(fullId, { def: { ...item, id: fullId }, pluginId });
          self.notifyListeners();
        },
        registerDatabaseCategory: (category) => {
          const fullId = category.id.startsWith(pluginId + ".") ? category.id : \`\${pluginId}.\${category.id}\`;
          self.registeredDbCategories.set(fullId, { def: { ...category, id: fullId }, pluginId });
          self.notifyListeners();
        },
        getRecords: (categoryId) => {
          var _a, _b, _c;
          const fullId = categoryId.startsWith(pluginId + ".") ? categoryId : \`\${pluginId}.\${categoryId}\`;
          const recs = (_c = (_b = (_a = project()) == null ? void 0 : _a.pluginStorage) == null ? void 0 : _b[pluginId]) == null ? void 0 : _c[\`\${EXT_DB_STORAGE_PREFIX}\${fullId}\`];
          return Array.isArray(recs) ? recs : [];
        },
        registerUIElementType: (def) => {
          const fullType = def.type.startsWith(pluginId + ".") ? def.type : \`\${pluginId}.\${def.type}\`;
          self.registeredUIElementTypes.set(fullType, { def: { ...def, type: fullType }, pluginId });
          self.notifyListeners();
        }
      };
    }
  }
  function createPlugin(manifest, source, config = {}, resources) {
    return {
      manifest,
      state: "enabled",
      installedAt: (/* @__PURE__ */ new Date()).toISOString(),
      config,
      source,
      ...resources && Object.keys(resources).length > 0 ? { resources } : {}
    };
  }
  function validatePluginManifest(manifest) {
    const errors = [];
    if (!manifest.id || !/^[a-z0-9.-]+\$/i.test(manifest.id)) {
      errors.push("Plugin ID must contain only alphanumeric characters, dots, and hyphens.");
    }
    if (!manifest.name || manifest.name.trim().length === 0) errors.push("Plugin name is required.");
    if (!manifest.version || !/^\\d+\\.\\d+\\.\\d+/.test(manifest.version)) {
      errors.push("Plugin version must follow semantic versioning (e.g. 1.0.0).");
    }
    if (!manifest.description) errors.push("Plugin description is required.");
    if (!manifest.author) errors.push("Plugin author is required.");
    if (!manifest.category) errors.push("Plugin category is required.");
    return { valid: errors.length === 0, errors };
  }
  const pluginManager = PluginManagerService.getInstance();
  const ASSET_SCHEME = "flourish-asset";
  const api = () => typeof window !== "undefined" ? window.electronAPI : void 0;
  const isElectronAssetStore = () => {
    var _a;
    return !!((_a = api()) == null ? void 0 : _a.writeProjectAsset);
  };
  const isExternalRef = (value) => !!value && !/^(data:|https?:|blob:|flourish-asset:|file:)/i.test(value) && value.includes("assets/");
  const assetUrl = (projectId, relPath) => \`\${ASSET_SCHEME}://\${projectId}/\${relPath.replace(/^\\/+/, "")}\`;
  const resolveFieldUrl = (projectId, value) => {
    if (!value) return null;
    if (isExternalRef(value)) return isElectronAssetStore() ? assetUrl(projectId, value) : value;
    return value;
  };
  const extFromDataUrl = (dataUrl) => {
    const m = dataUrl.match(/^data:([^;,]+)/);
    const mime = m ? m[1] : "";
    const sub = mime.split("/")[1] || "bin";
    const map = { jpeg: "jpg", "svg+xml": "svg", mpeg: "mp3", quicktime: "mov", "x-wav": "wav" };
    return (map[sub] || sub).toLowerCase().replace(/[^a-z0-9]/g, "");
  };
  const dataUrlToBytes = (dataUrl) => {
    const comma = dataUrl.indexOf(",");
    const b64 = dataUrl.slice(comma + 1);
    const bin = atob(b64);
    const out = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; i++) out[i] = bin.charCodeAt(i);
    return out;
  };
  async function writeAssetBytes(projectId, type, id, ext, bytes) {
    const a = api();
    if (!(a == null ? void 0 : a.writeProjectAsset)) return null;
    const res = await a.writeProjectAsset(projectId, type, id, ext, bytes);
    return (res == null ? void 0 : res.success) ? res.relPath : null;
  }
  async function writeAssetDataUrl(projectId, type, id, dataUrl) {
    return writeAssetBytes(projectId, type, id, extFromDataUrl(dataUrl), dataUrlToBytes(dataUrl));
  }
  const isAssetsExternalized = (project) => project.assetStorage === "external";
  function collectAssetFields(p) {
    const refs = [];
    const pushColl = (coll, type, keys) => {
      if (!coll) return;
      for (const a of Object.values(coll)) {
        for (const key of keys) {
          if (typeof a[key] === "string" && a[key]) refs.push({ type, id: a.id, obj: a, key, label: a.name || a.id });
        }
      }
    };
    pushColl(p.images, "images", ["imageUrl", "videoUrl"]);
    pushColl(p.backgrounds, "backgrounds", ["imageUrl", "videoUrl"]);
    pushColl(p.audio, "audio", ["audioUrl"]);
    pushColl(p.videos, "videos", ["videoUrl"]);
    pushColl(p.fonts, "fonts", ["fontUrl"]);
    for (const c of Object.values(p.characters || {})) {
      const cl = c.name || c.id;
      if (typeof c.baseImageUrl === "string" && c.baseImageUrl) refs.push({ type: "characters", id: \`\${c.id}-base\`, obj: c, key: "baseImageUrl", label: cl });
      if (typeof c.baseVideoUrl === "string" && c.baseVideoUrl) refs.push({ type: "characters", id: \`\${c.id}-base\`, obj: c, key: "baseVideoUrl", label: cl });
      if (typeof c.fontUrl === "string" && c.fontUrl) refs.push({ type: "characters", id: \`\${c.id}-font\`, obj: c, key: "fontUrl", label: \`\${cl} font\` });
      for (const layer of Object.values(c.layers || {})) {
        for (const asset of Object.values(layer.assets || {})) {
          if (typeof asset.imageUrl === "string" && asset.imageUrl) refs.push({ type: "characters", id: asset.id, obj: asset, key: "imageUrl", label: cl });
          if (typeof asset.videoUrl === "string" && asset.videoUrl) refs.push({ type: "characters", id: asset.id, obj: asset, key: "videoUrl", label: cl });
        }
      }
    }
    return refs;
  }
  const isBase64 = (v) => v.startsWith("data:");
  const isBareRef = (v) => /^assets\\//.test(v) && !v.startsWith("data:");
  const dataUrlBytes = (v) => {
    const i = v.indexOf(",");
    return i < 0 ? 0 : Math.floor((v.length - i - 1) * 0.75);
  };
  async function externalizeProjectAssets(project, onProgress) {
    if (!isElectronAssetStore()) return { project, changed: false, migratedCount: 0 };
    const pid = project.id;
    const p = project;
    const pending = collectAssetFields(p).filter((r) => {
      const v = r.obj[r.key];
      return isBase64(v) || isBareRef(v);
    });
    const total = pending.length;
    let changed = false;
    let migratedCount = 0;
    let bytes = 0;
    let done = 0;
    if (total > 0) onProgress == null ? void 0 : onProgress({ done: 0, total, bytes: 0, label: "" });
    for (const r of pending) {
      const v = r.obj[r.key];
      try {
        if (isBase64(v)) {
          const rel = await writeAssetDataUrl(pid, r.type, r.id, v);
          if (rel) {
            r.obj[r.key] = assetUrl(pid, rel);
            changed = true;
            migratedCount++;
            bytes += dataUrlBytes(v);
          }
        } else if (isBareRef(v)) {
          r.obj[r.key] = assetUrl(pid, v);
          changed = true;
        }
      } catch (e) {
        console.warn("[assetMigration] write failed for", r.type, r.id, e);
      }
      done++;
      onProgress == null ? void 0 : onProgress({ done, total, bytes, label: r.label });
    }
    if (changed || !isAssetsExternalized(project)) {
      p.assetStorage = "external";
    }
    return { project: p, changed, migratedCount };
  }
  const fmtBytes = (n) => {
    if (!n) return "0 B";
    const units = ["B", "KB", "MB", "GB", "TB"];
    const i = Math.min(units.length - 1, Math.floor(Math.log(n) / Math.log(1024)));
    return \`\${(n / Math.pow(1024, i)).toFixed(i === 0 ? 0 : 1)} \${units[i]}\`;
  };
  const Spinner = () => /* @__PURE__ */ jsxRuntime2.jsxs("svg", { className: "w-4 h-4 animate-spin text-sky-400", viewBox: "0 0 24 24", fill: "none", "aria-hidden": true, children: [
    /* @__PURE__ */ jsxRuntime2.jsx("circle", { className: "opacity-25", cx: "12", cy: "12", r: "10", stroke: "currentColor", strokeWidth: "4" }),
    /* @__PURE__ */ jsxRuntime2.jsx("path", { className: "opacity-75", fill: "currentColor", d: "M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4z" })
  ] });
  const MigrationStatusBar = ({ status }) => {
    if (!status) return null;
    const pct = status.total > 0 ? Math.round(status.done / status.total * 100) : 0;
    return /* @__PURE__ */ jsxRuntime2.jsx("div", { className: "fixed bottom-4 left-1/2 -translate-x-1/2 z-[10000] w-[min(92vw,30rem)] pointer-events-none", children: /* @__PURE__ */ jsxRuntime2.jsxs("div", { className: "rounded-lg border border-sky-500 bg-slate-900/95 shadow-xl backdrop-blur-sm px-4 py-3", children: [
      /* @__PURE__ */ jsxRuntime2.jsxs("div", { className: "flex items-center gap-2 mb-2", children: [
        /* @__PURE__ */ jsxRuntime2.jsx(Spinner, {}),
        /* @__PURE__ */ jsxRuntime2.jsx("p", { className: "text-sm font-medium text-white", children: "Updating project storage…" }),
        /* @__PURE__ */ jsxRuntime2.jsxs("span", { className: "ml-auto text-xs text-slate-400 tabular-nums", children: [
          status.done,
          "/",
          status.total
        ] })
      ] }),
      /* @__PURE__ */ jsxRuntime2.jsx("div", { className: "h-2 w-full rounded-full bg-slate-700 overflow-hidden", children: /* @__PURE__ */ jsxRuntime2.jsx(
        "div",
        {
          className: "h-full bg-sky-500 transition-all duration-200 ease-out",
          style: { width: \`\${pct}%\` }
        }
      ) }),
      /* @__PURE__ */ jsxRuntime2.jsxs("div", { className: "mt-1.5 flex items-center justify-between gap-3", children: [
        /* @__PURE__ */ jsxRuntime2.jsx("p", { className: "text-xs text-slate-400 truncate", children: status.label ? \`Moving \${status.label}…\` : "Preparing assets…" }),
        status.bytes > 0 && /* @__PURE__ */ jsxRuntime2.jsx("span", { className: "text-xs text-slate-500 tabular-nums flex-shrink-0", children: fmtBytes(status.bytes) })
      ] })
    ] }) });
  };
  const log = createLogger("ProjectContext");
  const AUTO_SAVE_INTERVAL = 2 * 60 * 1e3;
  const COALESCE_MS = 300;
  const NON_UNDOABLE_ACTIONS = /* @__PURE__ */ new Set([
    "UPDATE_PROJECT_TITLE"
  ]);
  const ProjectContext = React2.createContext(null);
  const ProjectProvider = ({ children, initialProject }) => {
    const toast = useToast();
    const [history, setHistory] = React2.useState(() => ({
      past: [],
      present: migrateMapLocationActions(repairOrphanBranchMarkers(migrateStatVariables(migrateItemCountVariableBounds(migrateInventorySlotButton(migrateProjectRemoveLegacyCommands(migrateProjectToUnifiedScreens(initialProject))))))),
      future: []
    }));
    const [lastAutoSave, setLastAutoSave] = React2.useState(null);
    const [isDirty, setIsDirty] = React2.useState(false);
    const [migration, setMigration] = React2.useState(null);
    const isSyncing = React2.useRef(false);
    const historyRef = React2.useRef(history);
    historyRef.current = history;
    const lastActionTime = React2.useRef(0);
    const lastActionType = React2.useRef("");
    const dispatchWithHistory = React2.useCallback((action) => {
      setHistory((prev) => {
        var _a;
        const newPresent = rootReducer(prev.present, action);
        if (newPresent === prev.present) {
          return prev;
        }
        WorkflowTracker.getInstance().trackAction(action.type, "editor");
        setIsDirty(true);
        const now = Date.now();
        const shouldCoalesce = action.type === lastActionType.current && now - lastActionTime.current < COALESCE_MS;
        const skipUndo = NON_UNDOABLE_ACTIONS.has(action.type);
        lastActionTime.current = now;
        lastActionType.current = action.type;
        let newPast;
        if (shouldCoalesce || skipUndo) {
          newPast = prev.past;
        } else {
          newPast = [...prev.past.slice(-20 + 1), prev.present];
        }
        const newHistory = {
          past: newPast,
          present: newPresent,
          future: []
        };
        if (!isSyncing.current && ((_a = window.electronAPI) == null ? void 0 : _a.syncProjectState)) {
          window.electronAPI.syncProjectState(newPresent);
        }
        return newHistory;
      });
    }, []);
    React2.useEffect(() => {
      if (!isElectronAssetStore()) return;
      let cancelled = false;
      (async () => {
        try {
          const { project: migrated, changed, migratedCount } = await externalizeProjectAssets(
            historyRef.current.present,
            (p) => {
              if (!cancelled) setMigration(p);
            }
          );
          if (cancelled) return;
          setMigration(null);
          if (!changed) return;
          setHistory((prev) => ({ ...prev, present: { ...migrated } }));
          try {
            await saveProjectToIDB(migrated);
          } catch {
          }
          if (migratedCount > 0) {
            try {
              toast.success(\`Moved \${migratedCount} asset\${migratedCount === 1 ? "" : "s"} into the new project storage\`);
            } catch {
            }
          }
        } catch (err) {
          log.warn("Asset externalization failed:", err);
          if (!cancelled) setMigration(null);
        }
      })();
      return () => {
        cancelled = true;
      };
    }, [initialProject.id]);
    React2.useEffect(() => {
      var _a;
      if ((_a = window.electronAPI) == null ? void 0 : _a.onProjectStateUpdate) {
        window.electronAPI.onProjectStateUpdate((projectData) => {
          isSyncing.current = true;
          setHistory((prev) => ({
            past: [...prev.past.slice(-20 + 1), prev.present],
            present: projectData,
            future: []
          }));
          isSyncing.current = false;
        });
      }
    }, []);
    React2.useEffect(() => {
      const autoSave = async () => {
        const project = historyRef.current.present;
        try {
          await saveProjectToIDB(project);
          setLastAutoSave(Date.now());
        } catch (err) {
          log.warn("Auto-save failed:", err);
        }
      };
      autoSave();
      const intervalId = setInterval(autoSave, AUTO_SAVE_INTERVAL);
      return () => clearInterval(intervalId);
    }, []);
    React2.useEffect(() => {
      const id = setTimeout(() => {
        saveProjectToIDB(history.present).then(() => setLastAutoSave(Date.now())).catch((err) => log.warn("Debounced auto-save failed:", err));
      }, 1200);
      return () => clearTimeout(id);
    }, [history.present]);
    React2.useEffect(() => {
      return () => {
        saveProjectToIDB(historyRef.current.present).catch((err) => log.warn("Save on editor exit failed:", err));
      };
    }, []);
    React2.useEffect(() => {
      const handleBeforeUnload = () => {
        const project = historyRef.current.present;
        try {
          saveProjectToIDB(project);
        } catch (err) {
          log.warn("Emergency save on unload failed:", err);
        }
      };
      window.addEventListener("beforeunload", handleBeforeUnload);
      return () => window.removeEventListener("beforeunload", handleBeforeUnload);
    }, []);
    const undo = React2.useCallback(() => {
      setHistory((prev) => {
        if (prev.past.length === 0) return prev;
        const newPast = prev.past.slice(0, -1);
        const newPresent = prev.past[prev.past.length - 1];
        return {
          past: newPast,
          present: newPresent,
          future: [prev.present, ...prev.future]
        };
      });
      toast.info("Undo", { duration: 1200 });
    }, [toast]);
    const redo = React2.useCallback(() => {
      setHistory((prev) => {
        if (prev.future.length === 0) return prev;
        const newFuture = prev.future.slice(1);
        const newPresent = prev.future[0];
        return {
          past: [...prev.past, prev.present],
          present: newPresent,
          future: newFuture
        };
      });
      toast.info("Redo", { duration: 1200 });
    }, [toast]);
    const markSaved = React2.useCallback(() => {
      setIsDirty(false);
    }, []);
    React2.useEffect(() => {
      const handleKeyDown = (e) => {
        if ((e.ctrlKey || e.metaKey) && !e.shiftKey && e.key === "z") {
          e.preventDefault();
          undo();
        } else if ((e.ctrlKey || e.metaKey) && (e.shiftKey && e.key === "z" || e.key === "y")) {
          e.preventDefault();
          redo();
        }
      };
      window.addEventListener("keydown", handleKeyDown);
      return () => window.removeEventListener("keydown", handleKeyDown);
    }, [undo, redo]);
    React2.useEffect(() => {
      if (typeof window !== "undefined") {
        window.__FLOURISH_PROJECT__ = history.present;
      }
    }, [history.present]);
    React2.useEffect(() => {
      pluginManager.setHost({
        getProject: () => historyRef.current.present,
        dispatch: dispatchWithHistory,
        notify: (message, type = "info") => {
          try {
            toast.addToast(message, type);
          } catch {
          }
        }
      });
      try {
        pluginManager.ensureLoaded(historyRef.current.present);
      } catch (e) {
        log.warn("Plugin ensureLoaded failed:", e);
      }
    }, [dispatchWithHistory, toast]);
    return /* @__PURE__ */ jsxRuntime2.jsxs(ProjectContext.Provider, { value: {
      project: history.present,
      dispatch: dispatchWithHistory,
      undo,
      redo,
      canUndo: history.past.length > 0,
      canRedo: history.future.length > 0,
      lastAutoSave,
      isDirty,
      markSaved
    }, children: [
      children,
      /* @__PURE__ */ jsxRuntime2.jsx(MigrationStatusBar, { status: migration })
    ] });
  };
  const useProject = () => {
    const context = React2.useContext(ProjectContext);
    if (!context) {
      throw new Error("useProject must be used within a ProjectProvider");
    }
    return context;
  };
  function resolveBoolLabels(variable, fallbackTrue, fallbackFalse) {
    return {
      yes: ((variable == null ? void 0 : variable.trueLabel) ?? "").trim() || fallbackTrue,
      no: ((variable == null ? void 0 : variable.falseLabel) ?? "").trim() || fallbackFalse
    };
  }
  const getAssetNameFromId = (assetId, project) => {
    const background = project.backgrounds[assetId];
    if (background) return background.name;
    const image = project.images[assetId];
    if (image) return image.name;
    const video = project.videos[assetId];
    if (video) return video.name;
    const audio = project.audio[assetId];
    if (audio) return audio.name;
    for (const character of Object.values(project.characters)) {
      for (const layer of Object.values(character.layers)) {
        const asset = layer.assets[assetId];
        if (asset) return asset.name;
      }
    }
    return null;
  };
  const formatValue = (variable, value, project) => {
    if (variable.type === "boolean") {
      const truthy = value === true || String(value).toLowerCase() === "true";
      const { yes, no } = resolveBoolLabels(variable, "Yes", "No");
      return truthy ? yes : no;
    }
    const stringValue = String(value);
    if (stringValue.startsWith("asset-")) {
      const assetName = getAssetNameFromId(stringValue, project);
      return assetName || stringValue;
    }
    return stringValue;
  };
  const interpolateVariables = (text, variables, project) => {
    if (!text) return text;
    let result = text.replace(/\\{([^}]+)\\}/g, (match, placeholder) => {
      const trimmedPlaceholder = placeholder.trim();
      const variableByName = Object.values(project.variables).find((v) => v.name === trimmedPlaceholder);
      if (variableByName) {
        const value = variables[variableByName.id];
        return value !== void 0 ? formatValue(variableByName, value, project) : match;
      }
      const variableById = project.variables[trimmedPlaceholder];
      if (variableById) {
        const value = variables[variableById.id];
        return value !== void 0 ? formatValue(variableById, value, project) : match;
      }
      return match;
    });
    return result;
  };
  const generateId = () => \`opt-\${Math.random().toString(36).substring(2, 9)}\`;
  const generateBranchId = () => \`branch-\${Math.random().toString(36).substring(2, 9)}\`;
  const createCommand = (type, project, options = {}) => {
    const customDef = pluginManager.getCommand(type);
    if (customDef) {
      const params = {};
      for (const p of customDef.parameters || []) params[p.name] = p.defaultValue ?? (p.type === "number" ? 0 : p.type === "boolean" ? false : "");
      return { type, params };
    }
    const firstCharId = Object.keys(project.characters)[0];
    const firstBgId = Object.keys(project.backgrounds)[0];
    const firstImageId = Object.keys(project.images || {})[0];
    const firstAudioId = Object.keys(project.audio)[0];
    const firstVideoId = Object.keys(project.videos)[0];
    const firstVarId = Object.keys(project.variables)[0];
    const firstSceneId = Object.keys(project.scenes)[0];
    const firstScreenId = Object.keys(project.uiScreens)[0];
    const firstChar = firstCharId ? project.characters[firstCharId] : null;
    const firstExpressionId = firstChar ? Object.keys(firstChar.expressions)[0] : "";
    switch (type) {
      case CommandType.Dialogue: {
        const command = { type, characterId: null, text: "New dialogue..." };
        return command;
      }
      case CommandType.SetBackground: {
        const command = { type, backgroundId: firstBgId || firstImageId || "", transition: "fade", duration: 1 };
        return command;
      }
      case CommandType.ShowCharacter: {
        const command = { type, characterId: firstCharId || "", expressionId: firstExpressionId || "", position: "center", transition: "fade", duration: 1 };
        return command;
      }
      case CommandType.HideCharacter: {
        const command = { type, characterId: firstCharId || "", transition: "fade", duration: 1 };
        return command;
      }
      case CommandType.SetCharacterLayer: {
        const command = { type, characterId: firstCharId || "", layers: [], duration: 0.3 };
        return command;
      }
      case CommandType.Choice: {
        const command = {
          type,
          options: [{
            id: generateId(),
            text: "Option 1",
            actions: [{ type: UIActionType.JumpToScene, targetSceneId: firstSceneId || "" }]
          }]
        };
        return command;
      }
      case CommandType.BranchStart: {
        const branchId = options.branchId || generateBranchId();
        const command = { type, name: "New Branch", color: "#38bdf8", branchId, isCollapsed: false };
        return command;
      }
      case CommandType.BranchElseIf: {
        const branchId = options.branchId || generateBranchId();
        const command = { type, branchId };
        return command;
      }
      case CommandType.BranchElse: {
        const branchId = options.branchId || generateBranchId();
        const command = { type, branchId };
        return command;
      }
      case CommandType.BranchEnd: {
        const branchId = options.branchId || generateBranchId();
        const command = { type, branchId };
        return command;
      }
      case CommandType.PlayMusic: {
        const command = { type, audioId: firstAudioId || "", loop: true, fadeDuration: 1 };
        return command;
      }
      case CommandType.StopMusic: {
        const command = { type, fadeDuration: 1 };
        return command;
      }
      case CommandType.PlaySoundEffect: {
        const command = { type, audioId: firstAudioId || "" };
        return command;
      }
      case CommandType.StopSoundEffect: {
        const command = { type, audioId: "", fadeDuration: 0 };
        return command;
      }
      case CommandType.PlayMovie: {
        const command = { type, videoId: firstVideoId || "", waitsForCompletion: true, displayMode: "fullscreen", loop: false, x: 0, y: 0, width: 100, height: 100, opacity: 1, objectFit: "cover" };
        return command;
      }
      case CommandType.StopMovie: {
        const command = { type };
        return command;
      }
      case CommandType.SetVariable: {
        const firstVar = project.variables[firstVarId || ""];
        let defaultValue = 0;
        if (firstVar) {
          if (firstVar.type === "boolean") {
            defaultValue = false;
          } else if (firstVar.type === "number") {
            defaultValue = 0;
          } else {
            defaultValue = "";
          }
        }
        const command = { type, variableId: firstVarId || "", operator: "set", value: defaultValue };
        return command;
      }
      case CommandType.TextInput: {
        const command = { type, variableId: firstVarId || "", prompt: "Enter your name:", placeholder: "Type here...", maxLength: 50 };
        return command;
      }
      case CommandType.Jump: {
        const command = { type, targetSceneId: firstSceneId || "" };
        return command;
      }
      case CommandType.Wait: {
        const command = { type, duration: 1, waitForInput: true };
        return command;
      }
      case CommandType.ShakeScreen: {
        const command = { type, duration: 0.5, intensity: 5 };
        return command;
      }
      case CommandType.TintScreen: {
        const command = { type, color: "#00000080", duration: 1 };
        return command;
      }
      case CommandType.PanZoomScreen: {
        const command = { type, zoom: 1.2, panX: 0, panY: 0, duration: 1 };
        return command;
      }
      case CommandType.ResetScreenEffects: {
        const command = { type, duration: 1 };
        return command;
      }
      case CommandType.FlashScreen: {
        const command = { type, color: "#FFFFFF", duration: 0.5 };
        return command;
      }
      case CommandType.Lightning: {
        const command = { type, color: "#EAF2FF", intensity: 0.9, duration: 0.7, flashes: 2, thunderSfxId: null, thunderDelay: 0.6 };
        return command;
      }
      case CommandType.Fireworks: {
        const command = { type, colors: [], bursts: 3, duration: 2.5, intensity: 1, burstHeight: 0.7, sfxId: null, sfxDelay: 0.3, sfxPerBurst: false };
        return command;
      }
      case CommandType.PlaceLights: {
        const command = { type, lights: [], aboveCharacters: false };
        return command;
      }
      case CommandType.ClearLights: {
        const command = { type };
        return command;
      }
      case CommandType.ShowPhone:
      case CommandType.HidePhone:
      case CommandType.HidePhoneText: {
        return { type };
      }
      case CommandType.ShowPhoneText: {
        const command = { type, senderId: firstCharId || "player", text: "", choices: [] };
        return command;
      }
      case CommandType.PhoneIncomingText: {
        const command = { type, senderId: firstCharId || "player", text: "", presentation: "notify", showBadge: true, replies: [] };
        return command;
      }
      case CommandType.PhoneIncomingCall: {
        const command = { type, callerId: firstCharId || "player", mode: "modal", ringDurationMs: 12e3, onTimeout: "missed", acceptActions: [], declineActions: [], timeoutActions: [], showBadge: true };
        return command;
      }
      case CommandType.StartPhoneCall: {
        const command = { type, contactId: firstCharId || "", dialingMs: 1200, blocking: true, conversation: { lines: [] } };
        return command;
      }
      case CommandType.ShowMap: {
        const command = { type, mapId: Object.keys(project.maps || {})[0] || "", allowCancel: false };
        return command;
      }
      case CommandType.ShowMiniGame: {
        const command = { type, gameId: Object.keys(project.miniGames || {})[0] || "" };
        return command;
      }
      case CommandType.PhoneNotify: {
        const command = { type, title: "", text: "", icon: "chat", showBadge: true, presentation: "notify", tapActions: [] };
        return command;
      }
      case CommandType.Flashlight: {
        const command = { type, enabled: true, radius: 22, softness: 0.6, darkness: 0.85, color: "#000000", toggleKey: "f", sfxId: null };
        return command;
      }
      case CommandType.Spotlight: {
        const command = { type, enabled: true, spotlightId: "main", sourceX: 50, sourceY: 0, aimAngle: 0, intensity: 0.85, beamWidth: 45, sourceWidth: 8, height: 100, falloff: 0.5, color: "#fff3d6", followMouse: true, swivelMax: 30, toggleKey: "f", sfxId: null };
        return command;
      }
      case CommandType.SetScreenOverlayEffect: {
        const command = { type, effectType: "crtScanlines", intensity: 0.5, duration: 0 };
        return command;
      }
      case CommandType.ShowScreen: {
        const command = { type, screenId: firstScreenId || "" };
        return command;
      }
      case CommandType.HideScreen: {
        const command = { type, screenId: "", all: false };
        return command;
      }
      case CommandType.ShowText: {
        const command = {
          type,
          text: "Sample Text",
          x: 50,
          y: 50,
          fontSize: 24,
          fontFamily: "Arial",
          color: "#FFFFFF",
          width: 400,
          fontWeight: "normal",
          fontStyle: "normal",
          letterSpacing: 0,
          textShadow: { enabled: false, offsetX: 2, offsetY: 2, blur: 4, color: "#000000" },
          textGradient: { enabled: false, type: "linear", angle: 90, colors: ["#ff00a5", "#8a2be2"] },
          textBorder: { enabled: false, width: 1, color: "#000000" },
          textAlign: "center",
          verticalAlign: "middle",
          transition: "fade",
          duration: 0.5
        };
        return command;
      }
      case CommandType.ShowImage: {
        const command = {
          type,
          imageId: firstImageId || "",
          x: 50,
          y: 50,
          width: 200,
          height: 200,
          rotation: 0,
          opacity: 1,
          scaleX: 1,
          scaleY: 1,
          transition: "fade",
          duration: 0.5
        };
        return command;
      }
      case CommandType.Label: {
        const command = { type, labelId: "new_label" };
        return command;
      }
      case CommandType.JumpToLabel: {
        const command = { type, labelId: "new_label" };
        return command;
      }
      case CommandType.HideText: {
        const command = {
          type,
          targetCommandId: "",
          transition: "fade",
          duration: 0.5
        };
        return command;
      }
      case CommandType.HideImage: {
        const command = {
          type,
          targetCommandId: "",
          transition: "fade",
          duration: 0.5
        };
        return command;
      }
      case CommandType.ShowButton: {
        const command = {
          type,
          text: "Button",
          x: 50,
          y: 80,
          width: 20,
          height: 8,
          anchorX: 0.5,
          anchorY: 0.5,
          backgroundColor: "#6366f1",
          textColor: "#ffffff",
          fontSize: 18,
          fontWeight: "normal",
          borderRadius: 8,
          opacity: 1,
          image: null,
          hoverImage: null,
          onClick: { type: "None" },
          clickSound: null,
          waitForClick: false,
          transition: "fade",
          duration: 0.3,
          showConditions: []
        };
        return command;
      }
      case CommandType.ShowItem: {
        const firstItemId = Object.keys(project.items || {})[0] || "";
        const command = {
          type,
          itemId: firstItemId,
          quantity: 1,
          x: 50,
          y: 50,
          width: 10,
          height: 10,
          anchorX: 0.5,
          anchorY: 0.5,
          opacity: 1,
          image: null,
          hoverImage: null,
          giveOnClick: true,
          removeAfterPickup: true,
          pickUpOnce: true,
          actions: [],
          clickSound: null,
          showConditions: [],
          transition: "fade",
          duration: 0.3
        };
        return command;
      }
      case CommandType.GiveItem: {
        const firstItemId = Object.keys(project.items || {})[0] || "";
        return { type, itemId: firstItemId, quantity: 1 };
      }
      case CommandType.UseItem: {
        const firstItemId = Object.keys(project.items || {})[0] || "";
        return { type, itemId: firstItemId };
      }
      case CommandType.DestroyItem: {
        const firstItemId = Object.keys(project.items || {})[0] || "";
        return { type, itemId: firstItemId, quantity: 1, all: false };
      }
      case CommandType.RestockCollection: {
        const firstCollectionId = Object.keys(project.itemCollections || {})[0] || "";
        return { type, collectionId: firstCollectionId };
      }
      case CommandType.BuyItem: {
        const firstCollectionId = Object.keys(project.itemCollections || {})[0] || "";
        const firstItemId = Object.keys(project.items || {})[0] || "";
        return { type, itemId: firstItemId, collectionId: firstCollectionId, quantity: 1 };
      }
      case CommandType.SellItem: {
        const firstCollectionId = Object.keys(project.itemCollections || {})[0] || "";
        const firstItemId = Object.keys(project.items || {})[0] || "";
        return { type, itemId: firstItemId, collectionId: firstCollectionId, quantity: 1 };
      }
      case CommandType.HideButton: {
        const command = {
          type,
          targetCommandId: "",
          transition: "fade",
          duration: 0.3
        };
        return command;
      }
      case CommandType.Group: {
        const command = {
          type,
          name: "New Group",
          commandIds: [],
          collapsed: false
        };
        return command;
      }
      case CommandType.CreditRoll: {
        const command = {
          type,
          entries: [
            { kind: "heading", label: "Credits" },
            { kind: "credit", label: "Created with", value: "Flourish Visual Novel Engine" }
          ],
          duration: 15,
          scrollSpeed: 60,
          backgroundColor: "#000000FF",
          textColor: "#FFFFFF",
          allowSkip: true,
          onComplete: "advance",
          backgrounds: []
        };
        return command;
      }
      case CommandType.RunScript: {
        const firstScriptId = Object.keys(project.scripts || {})[0];
        const command = { type, scriptId: firstScriptId || "", waitForCompletion: true };
        return command;
      }
      case CommandType.SpawnParticles: {
        const command = {
          type,
          // Empty by default so the runtime derives a unique tag per command
          // (\`particles_<id>\`). A hardcoded shared tag made multiple emitters
          // collide and made Stop Particles ambiguous. Users can still name it.
          particleTag: "",
          config: {
            preset: "fireflies",
            shape: "circle",
            colors: ["#FFFF66", "#CCFF33"],
            emitRate: 10,
            lifetime: 3,
            speedMin: 10,
            speedMax: 30,
            sizeMin: 2,
            sizeMax: 6,
            gravity: 0,
            wind: 0,
            directionMin: 0,
            directionMax: 360,
            emitterX: 50,
            emitterY: 50,
            emitterWidth: 100,
            emitterHeight: 100,
            fadeOut: true,
            shrink: false,
            rotationSpeed: 0,
            opacity: 0.9
          },
          duration: 0
        };
        return command;
      }
      case CommandType.StopParticles: {
        const command = {
          type,
          particleTag: "",
          fadeDuration: 1
        };
        return command;
      }
      case CommandType.ShowHotSpot: {
        return {
          type,
          name: "Hot Spot",
          x: 40,
          y: 40,
          width: 20,
          height: 20,
          shape: "rect",
          trigger: "click",
          actions: [],
          conditions: [],
          acceptedTag: "",
          highlightColor: "rgba(99,102,241,0.35)",
          visible: false,
          advanceOnTrigger: false
        };
      }
      case CommandType.HideHotSpot: {
        return {
          type,
          targetCommandId: ""
        };
      }
      case CommandType.CallCommonEvent: {
        return {
          type,
          commonEventId: ""
        };
      }
      case CommandType.TweenElement: {
        return {
          type,
          targetId: "",
          targetType: "character",
          duration: 1,
          easing: "easeInOutCubic",
          waitForCompletion: true
        };
      }
      case CommandType.MoveCharacter: {
        const firstCharId2 = Object.keys(project.characters || {})[0] || "";
        return {
          type,
          characterId: firstCharId2,
          toPosition: "center",
          duration: 1,
          easing: "easeInOutCubic",
          waitForCompletion: true
        };
      }
      case CommandType.StartTimer: {
        const firstNumVar = Object.values(project.variables || {}).find((v) => v.type === "number");
        return {
          type,
          timerId: "",
          variableId: (firstNumVar == null ? void 0 : firstNumVar.id) || "",
          mode: "countdown",
          duration: 10,
          from: 0,
          interval: 1,
          loop: false,
          onComplete: []
        };
      }
      case CommandType.StopTimer: {
        return { type, timerId: "" };
      }
      case CommandType.SetTimeOfDay: {
        return { type, mode: "set", hour: 18, hours: 1, transitionDuration: 2 };
      }
      default:
        return null;
    }
  };
  function combineConditions(conditions, evalOne) {
    if (!conditions || conditions.length === 0) return true;
    let result = evalOne(conditions[0]);
    for (let i = 1; i < conditions.length; i++) {
      const condition = conditions[i];
      const met = evalOne(condition);
      if ((condition.connector ?? "and") === "or") {
        result = result || met;
      } else {
        result = result && met;
      }
    }
    return result;
  }
  function isHotSpotElement(el) {
    return el.type === UIElementType.HotSpot;
  }
  function hotSpotElementToLegacy(el) {
    return {
      id: el.id,
      name: el.name,
      shape: el.shape,
      trigger: el.trigger || "click",
      x: el.x,
      y: el.y,
      width: el.width,
      height: el.height,
      acceptedElementIds: el.acceptedElementIds,
      actions: el.actions || [],
      conditions: el.conditions,
      highlightColor: el.highlightColor,
      visible: el.visible
    };
  }
  function elementToLegacyHotZoneElement(el) {
    var _a, _b, _c, _d;
    const anyEl = el;
    switch (el.type) {
      case UIElementType.draggableImageElement: {
        const m = el;
        return {
          id: m.id,
          name: m.name,
          elementType: "draggableImageElement",
          imageId: ((_a = m.image) == null ? void 0 : _a.id) ?? "",
          hoverImageId: (_b = m.hoverImage) == null ? void 0 : _b.id,
          draggableImageElementRegions: m.draggableImageElementRegions,
          x: m.x,
          y: m.y,
          width: m.width,
          height: m.height,
          draggable: anyEl.draggable,
          snapBack: anyEl.snapBack,
          snapToHotSpot: anyEl.snapToHotSpot,
          hideOnDrop: anyEl.hideOnDrop,
          conditions: m.conditions,
          actions: anyEl.actions,
          clickSoundId: anyEl.clickSoundId,
          hoverSoundId: anyEl.hoverSoundId
        };
      }
      case UIElementType.Image: {
        const img = el;
        const bg = img.background;
        const isVideo = (bg == null ? void 0 : bg.type) === "video";
        const isImage = (bg == null ? void 0 : bg.type) === "image";
        const assetId = (bg && (bg.type === "image" || bg.type === "video") ? bg.assetId : null) ?? ((_c = img.image) == null ? void 0 : _c.id) ?? null;
        return {
          id: img.id,
          name: img.name,
          elementType: isVideo ? "video" : "image",
          imageId: isImage ? assetId : "",
          videoId: isVideo ? assetId ?? void 0 : void 0,
          videoLoop: anyEl.videoLoop,
          videoMuted: anyEl.videoMuted,
          x: img.x,
          y: img.y,
          width: img.width,
          height: img.height,
          draggable: anyEl.draggable,
          snapBack: anyEl.snapBack,
          snapToHotSpot: anyEl.snapToHotSpot,
          hideOnDrop: anyEl.hideOnDrop,
          conditions: img.conditions,
          actions: anyEl.actions,
          clickSoundId: anyEl.clickSoundId,
          hoverSoundId: anyEl.hoverSoundId
        };
      }
      case UIElementType.Text: {
        const t = el;
        return {
          id: t.id,
          name: t.name,
          elementType: "text",
          imageId: "",
          text: t.text,
          font: t.font,
          x: t.x,
          y: t.y,
          width: t.width,
          height: t.height,
          draggable: anyEl.draggable,
          snapBack: anyEl.snapBack,
          snapToHotSpot: anyEl.snapToHotSpot,
          hideOnDrop: anyEl.hideOnDrop,
          conditions: t.conditions,
          actions: anyEl.actions,
          clickSoundId: anyEl.clickSoundId,
          hoverSoundId: anyEl.hoverSoundId
        };
      }
      case UIElementType.Button: {
        const b = el;
        return {
          id: b.id,
          name: b.name,
          elementType: "button",
          imageId: ((_d = b.image) == null ? void 0 : _d.id) ?? "",
          text: b.text,
          font: b.font,
          backgroundColor: b.backgroundColor,
          x: b.x,
          y: b.y,
          width: b.width,
          height: b.height,
          draggable: anyEl.draggable,
          snapBack: anyEl.snapBack,
          snapToHotSpot: anyEl.snapToHotSpot,
          hideOnDrop: anyEl.hideOnDrop,
          conditions: b.conditions,
          actions: b.actions ?? anyEl.actions,
          clickSoundId: b.clickSoundId ?? null,
          hoverSoundId: b.hoverSoundId ?? null
        };
      }
      case UIElementType.TextInput: {
        const ti = el;
        return {
          id: ti.id,
          name: ti.name,
          elementType: "textInput",
          imageId: "",
          placeholder: ti.placeholder,
          variableId: ti.variableId,
          font: ti.font,
          backgroundColor: ti.backgroundColor,
          borderColor: ti.borderColor,
          maxLength: ti.maxLength,
          x: ti.x,
          y: ti.y,
          width: ti.width,
          height: ti.height,
          draggable: anyEl.draggable,
          snapBack: anyEl.snapBack,
          snapToHotSpot: anyEl.snapToHotSpot,
          hideOnDrop: anyEl.hideOnDrop,
          conditions: ti.conditions,
          actions: anyEl.actions,
          clickSoundId: anyEl.clickSoundId,
          hoverSoundId: anyEl.hoverSoundId
        };
      }
      default:
        return null;
    }
  }
  function deriveHotSpotsFromScreen(screen) {
    const out = {};
    for (const el of Object.values(screen.elements || {})) {
      if (isHotSpotElement(el)) {
        out[el.id] = hotSpotElementToLegacy(el);
      }
    }
    return out;
  }
  function deriveInteractiveElementsFromScreen(screen) {
    const out = {};
    for (const el of Object.values(screen.elements || {})) {
      if (el.type === UIElementType.HotSpot) continue;
      if (el.type === UIElementType.draggableImageElement || el.draggable === true) {
        const legacy = elementToLegacyHotZoneElement(el);
        if (legacy) out[el.id] = legacy;
      }
    }
    return out;
  }
  const VariablesIcon = ({ className, title, ...props }) => /* @__PURE__ */ jsxRuntime2.jsxs("svg", { xmlns: "http://www.w3.org/2000/svg", viewBox: "0 0 20 20", fill: "currentColor", className: \`w-5 h-5 \${className || ""}\`, ...props, children: [
    title && /* @__PURE__ */ jsxRuntime2.jsx("title", { children: title }),
    /* @__PURE__ */ jsxRuntime2.jsx("path", { fillRule: "evenodd", d: "M4.5 2A1.5 1.5 0 003 3.5v13A1.5 1.5 0 004.5 18h11a1.5 1.5 0 001.5-1.5V7.621a1.5 1.5 0 00-.44-1.06l-4.12-4.122A1.5 1.5 0 0011.378 2H4.5zm4.75 6.75a.75.75 0 00-1.5 0v2.546l-.943-1.048a.75.75 0 00-1.114 1.004l2.25 2.5a.75.75 0 001.114 0l2.25-2.5a.75.75 0 10-1.114-1.004l-.943 1.048V8.75zm2.5 5.5a.75.75 0 000 1.5h2.5a.75.75 0 000-1.5h-2.5z", clipRule: "evenodd" })
  ] });
  const XMarkIcon = ({ className, title, ...props }) => /* @__PURE__ */ jsxRuntime2.jsxs("svg", { xmlns: "http://www.w3.org/2000/svg", viewBox: "0 0 20 20", fill: "currentColor", className: \`w-5 h-5 \${className || ""}\`, ...props, children: [
    title && /* @__PURE__ */ jsxRuntime2.jsx("title", { children: title }),
    /* @__PURE__ */ jsxRuntime2.jsx("path", { d: "M6.28 5.22a.75.75 0 0 0-1.06 1.06L8.94 10l-3.72 3.72a.75.75 0 1 0 1.06 1.06L10 11.06l3.72 3.72a.75.75 0 1 0 1.06-1.06L11.06 10l3.72-3.72a.75.75 0 0 0-1.06-1.06L10 8.94 6.28 5.22Z" })
  ] });
  const buildOrientationTransform = (o) => {
    if (!o) return "";
    const parts = [];
    if (o.rotation) parts.push(\`rotate(\${o.rotation}deg)\`);
    const sx = o.flipX ? -1 : 1;
    const sy = o.flipY ? -1 : 1;
    if (sx !== 1 || sy !== 1) parts.push(\`scale(\${sx}, \${sy})\`);
    return parts.join(" ");
  };
  const fontSettingsToStyle = (settings) => {
    var _a, _b, _c;
    const px = (n) => \`calc(var(--font-scale, 1) * \${n}px)\`;
    if (!settings) {
      return { fontSize: px(16), color: "#ffffff", textAlign: "left" };
    }
    const style = {
      fontFamily: settings.family,
      fontSize: px(settings.size),
      color: settings.color,
      fontWeight: settings.weight,
      fontStyle: settings.italic ? "italic" : "normal",
      textAlign: settings.align || "left",
      letterSpacing: settings.letterSpacing ? px(settings.letterSpacing) : void 0
    };
    if (((_a = settings.textShadow) == null ? void 0 : _a.enabled) && (!((_b = settings.textGradient) == null ? void 0 : _b.enabled) || settings.textGradient.colors.length < 2)) {
      const ts = settings.textShadow;
      style.textShadow = \`\${px(ts.offsetX)} \${px(ts.offsetY)} \${px(ts.blur)} \${ts.color}\`;
    }
    if ((_c = settings.textBorder) == null ? void 0 : _c.enabled) {
      style.WebkitTextStroke = \`\${px(settings.textBorder.width)} \${settings.textBorder.color}\`;
    }
    return style;
  };
  const extractTextGradientStyle = (settings) => {
    var _a, _b;
    if (!settings || !((_a = settings.textGradient) == null ? void 0 : _a.enabled) || settings.textGradient.colors.length < 2) return null;
    const grad = settings.textGradient;
    const style = {
      background: grad.type === "radial" ? \`radial-gradient(circle, \${grad.colors.join(", ")})\` : \`linear-gradient(\${grad.angle}deg, \${grad.colors.join(", ")})\`,
      WebkitBackgroundClip: "text",
      WebkitTextFillColor: "transparent",
      backgroundClip: "text",
      // inline-block gives the gradient a proper box to clip against; without it an inline
      // span mis-clips/repaints the gradient (paints the line box) when colors change.
      display: "inline-block"
    };
    if ((_b = settings.textShadow) == null ? void 0 : _b.enabled) {
      const px = (n) => \`calc(var(--font-scale, 1) * \${n}px)\`;
      const ts = settings.textShadow;
      style.filter = \`drop-shadow(\${px(ts.offsetX)} \${px(ts.offsetY)} \${px(ts.blur)} \${ts.color})\`;
    }
    return style;
  };
  const buildTextEffectStyles = (effects, options) => {
    var _a, _b, _c, _d;
    const px = (n) => \`calc(var(--font-scale, 1) * \${n}px)\`;
    const hasGradient = !!(((_a = effects.textGradient) == null ? void 0 : _a.enabled) && effects.textGradient.colors.length >= 2);
    const containerStyle = {};
    let gradientSpanStyle = null;
    if ((_b = effects.textBorder) == null ? void 0 : _b.enabled) {
      containerStyle.WebkitTextStroke = \`\${px(effects.textBorder.width)} \${effects.textBorder.color}\`;
    }
    if (hasGradient) {
      const g = effects.textGradient;
      gradientSpanStyle = {
        background: g.type === "radial" ? \`radial-gradient(circle, \${g.colors.join(", ")})\` : \`linear-gradient(\${g.angle}deg, \${g.colors.join(", ")})\`,
        WebkitBackgroundClip: "text",
        WebkitTextFillColor: "transparent",
        backgroundClip: "text",
        // inline-block gives the gradient a proper box to clip against (fixes the
        // canvas mis-render when adjusting gradient colors).
        display: "inline-block"
      };
      if ((_c = effects.textShadow) == null ? void 0 : _c.enabled) {
        const ts = effects.textShadow;
        gradientSpanStyle.filter = \`drop-shadow(\${px(ts.offsetX)} \${px(ts.offsetY)} \${px(ts.blur)} \${ts.color})\`;
      }
    } else if ((_d = effects.textShadow) == null ? void 0 : _d.enabled) {
      const ts = effects.textShadow;
      containerStyle.textShadow = \`\${px(ts.offsetX)} \${px(ts.offsetY)} \${px(ts.blur)} \${ts.color}\`;
    }
    return { containerStyle, gradientSpanStyle };
  };
  const TrimmedVideo = React2.forwardRef(
    ({ src, trimStart, trimEnd, loop, onSegmentEnd, children, ...rest }, externalRef) => {
      const innerRef = React2.useRef(null);
      const setRefs = (el) => {
        innerRef.current = el;
        if (typeof externalRef === "function") externalRef(el);
        else if (externalRef) externalRef.current = el;
      };
      const start = trimStart != null && trimStart > 0 ? trimStart : 0;
      const end = trimEnd != null && trimEnd > start ? trimEnd : void 0;
      const trimmed = start > 0 || end != null;
      React2.useEffect(() => {
        const el = innerRef.current;
        if (!el || !trimmed) return;
        const seekToStart = () => {
          try {
            el.currentTime = start;
          } catch {
          }
        };
        const onMeta = () => seekToStart();
        let firedSegmentEnd = false;
        const onTime = () => {
          if (end != null && el.currentTime >= end) {
            if (loop) {
              try {
                el.currentTime = start;
                const p = el.play();
                if (p && p.catch) p.catch(() => {
                });
              } catch {
              }
            } else {
              try {
                el.pause();
                el.currentTime = end;
              } catch {
              }
              if (!firedSegmentEnd) {
                firedSegmentEnd = true;
                onSegmentEnd == null ? void 0 : onSegmentEnd();
              }
            }
          } else if (start > 0 && el.currentTime < start - 0.3) {
            seekToStart();
          }
        };
        const onEnded = () => {
          if (loop) {
            seekToStart();
            const p = el.play();
            if (p && p.catch) p.catch(() => {
            });
          }
        };
        if (el.readyState >= 1) seekToStart();
        el.addEventListener("loadedmetadata", onMeta);
        el.addEventListener("timeupdate", onTime);
        el.addEventListener("ended", onEnded);
        return () => {
          el.removeEventListener("loadedmetadata", onMeta);
          el.removeEventListener("timeupdate", onTime);
          el.removeEventListener("ended", onEnded);
        };
      }, [src, start, end, loop, trimmed, onSegmentEnd]);
      const nativeLoop = trimmed ? false : loop;
      return /* @__PURE__ */ jsxRuntime2.jsx("video", { ref: setRefs, src, loop: nativeLoop, ...rest, children });
    }
  );
  TrimmedVideo.displayName = "TrimmedVideo";
  function resolveVideoTrim(perUse, asset) {
    return {
      start: (perUse == null ? void 0 : perUse.trimStart) ?? (asset == null ? void 0 : asset.trimStart),
      end: (perUse == null ? void 0 : perUse.trimEnd) ?? (asset == null ? void 0 : asset.trimEnd)
    };
  }
  const BUILTIN_OVERLAY_EFFECT_TYPES = [
    "crtScanlines",
    "chromaticGlitch",
    "sunbeams",
    "shimmer",
    "rain",
    "snowAsh",
    "fog",
    "haze",
    "smoke",
    "fireworks"
  ];
  function clamp01(value) {
    if (Number.isNaN(value)) return 0;
    return Math.max(0, Math.min(1, value));
  }
  function normalizeOverlayEffects(effects) {
    if (!effects || effects.length === 0) return [];
    const byType = /* @__PURE__ */ new Map();
    for (const effect of effects) {
      const intensity = clamp01(effect.intensity ?? 0);
      if (intensity <= 0 && !effect.intensityVariableId) continue;
      byType.set(effect.type, {
        ...effect,
        intensity,
        variant: effect.type === "snowAsh" ? effect.variant ?? "snow" : effect.variant
      });
    }
    return Array.from(byType.values());
  }
  function upsertOverlayEffect(effects, next) {
    const normalized = normalizeOverlayEffects(effects);
    const intensity = clamp01(next.intensity ?? 0);
    const without = normalized.filter((e) => e.type !== next.type);
    if (intensity <= 0 && !next.intensityVariableId) return without;
    return normalizeOverlayEffects([
      ...without,
      {
        ...next,
        intensity,
        variant: next.type === "snowAsh" ? next.variant ?? "snow" : next.variant,
        color: next.color,
        params: next.params
      }
    ]);
  }
  const PHONE_GLYPHS = {
    chat: "💬",
    contacts: "👥",
    gallery: "🖼️",
    map: "🗺️",
    call: "📞",
    heart: "❤️",
    star: "⭐",
    settings: "⚙️",
    home: "🏠",
    back: "‹",
    close: "✕",
    phone: "📱"
  };
  const basePhone = (context) => ({
    open: false,
    messages: [],
    ...context.playerState.uiState.phone || {}
  });
  const phoneThreadKey = (m) => {
    const k = m.contactId ?? (m.senderId !== "player" ? m.senderId : "");
    return k === "__misc__" ? "" : k;
  };
  const countPhoneThread = (messages, key) => messages.filter((m) => phoneThreadKey(m) === key).length;
  const collectToCameraRoll = (roll, msg) => {
    if (!msg.image) return roll;
    const cur = roll || [];
    if (cur.some((e) => e.id === msg.id)) return roll;
    return [...cur, { id: msg.id, image: msg.image, senderId: msg.senderId, contactId: msg.contactId, caption: msg.text || void 0, order: cur.length }];
  };
  const handleShowPhone = (_command, context) => {
    return {
      advance: true,
      updates: { uiState: { phone: { ...basePhone(context), open: true, view: "home", notification: null } } }
    };
  };
  const handleHidePhone = (_command, context) => {
    return {
      advance: true,
      updates: { uiState: { phone: { ...basePhone(context), open: false, waiting: false, pendingChoices: void 0, notification: null } } }
    };
  };
  const handleShowPhoneText = (command, context) => {
    const prev = basePhone(context);
    const messages = prev.messages;
    const msg = {
      id: \`\${command.id}-\${messages.length}\`,
      senderId: command.senderId,
      text: command.text,
      ...command.portrait ? { portrait: command.portrait } : {},
      ...command.image ? { image: command.image } : {},
      // Tag the conversation thread so the Messages inbox can group: character messages thread
      // by sender; player messages stick to the thread that's open (else the legacy bucket).
      ...command.senderId !== "player" ? { contactId: command.senderId } : prev.activeContactId && prev.activeContactId !== "__misc__" ? { contactId: prev.activeContactId } : {}
    };
    const hasChoices = !!(command.choices && command.choices.length > 0);
    const nextMessages = [...messages, msg];
    const threadKey = phoneThreadKey(msg);
    return {
      // When the message offers replies, pause the scene — the in-phone reply buttons resume it.
      advance: !hasChoices,
      updates: {
        uiState: {
          phone: {
            ...prev,
            open: true,
            view: "chat",
            // Focus this character's thread (so the chat shows just their conversation).
            activeContactId: command.senderId !== "player" ? command.senderId : prev.activeContactId,
            messages: nextMessages,
            cameraRoll: collectToCameraRoll(prev.cameraRoll, msg),
            threadLastRead: { ...prev.threadLastRead || {}, [threadKey]: countPhoneThread(nextMessages, threadKey) },
            waiting: hasChoices,
            pendingChoices: hasChoices ? command.choices : void 0,
            typing: null
          }
        }
      }
    };
  };
  const handleHidePhoneText = (_command, context) => {
    return {
      advance: true,
      updates: { uiState: { phone: { ...basePhone(context), messages: [], waiting: false, pendingChoices: void 0, typing: null } } }
    };
  };
  const PhoneGlyph = ({ name }) => /* @__PURE__ */ jsxRuntime2.jsx("span", { style: { fontSize: "1.4em", lineHeight: 1 }, children: name && PHONE_GLYPHS[name] || "●" });
  function resolvePhonePortrait(source, character, assetResolver) {
    var _a, _b;
    const baseUrl = (character == null ? void 0 : character.id) ? assetResolver(character.id, "image") : null;
    const mode = (source == null ? void 0 : source.mode) || "base";
    if (mode === "custom") {
      const ci = source == null ? void 0 : source.customImage;
      const url = ci ? assetResolver(ci.id, ci.type === "video" ? "video" : "image") : null;
      return url ? [url] : baseUrl ? [baseUrl] : [];
    }
    if (mode === "expression" && character) {
      const stack = [];
      if (!(source == null ? void 0 : source.hideBase) && baseUrl) stack.push(baseUrl);
      const expr = (source == null ? void 0 : source.expressionId) ? (_a = character.expressions) == null ? void 0 : _a[source.expressionId] : void 0;
      if (expr) {
        for (const layer of Object.values(character.layers || {})) {
          const assetId = (_b = expr.layerConfiguration) == null ? void 0 : _b[layer.id];
          if (!assetId) continue;
          const url = assetResolver(assetId, "image");
          if (url) stack.push(url);
        }
      }
      return stack;
    }
    return baseUrl ? [baseUrl] : [];
  }
  const PhonePortrait = ({ urls, size, fit, objectPosition }) => {
    if (urls.length === 0) return null;
    return /* @__PURE__ */ jsxRuntime2.jsx("div", { style: { width: size, height: size, borderRadius: "9999px", overflow: "hidden", flexShrink: 0, position: "relative", background: "rgba(0,0,0,0.2)" }, children: urls.map((u, i) => /* @__PURE__ */ jsxRuntime2.jsx("img", { src: u, alt: "", style: { position: "absolute", inset: 0, width: "100%", height: "100%", objectFit: fit || "cover", objectPosition: objectPosition || "center" } }, i)) });
  };
  const PhoneMedia = ({ media, assetResolver, style, caption }) => {
    const [zoomed, setZoomed] = React2.useState(false);
    const url = assetResolver(media.id, media.type === "video" ? "video" : "image");
    if (!url) return null;
    const thumb = media.type === "video" ? /* @__PURE__ */ jsxRuntime2.jsx("video", { src: url, muted: true, loop: true, playsInline: true, autoPlay: true, style: { width: "100%", height: "100%", objectFit: "cover", display: "block" } }) : /* @__PURE__ */ jsxRuntime2.jsx("img", { src: url, alt: "", style: { width: "100%", height: "100%", objectFit: "cover", display: "block" } });
    return /* @__PURE__ */ jsxRuntime2.jsxs(jsxRuntime2.Fragment, { children: [
      /* @__PURE__ */ jsxRuntime2.jsx("div", { onClick: (e) => {
        e.stopPropagation();
        setZoomed(true);
      }, style: { cursor: "zoom-in", overflow: "hidden", ...style }, children: thumb }),
      zoomed && reactDom.createPortal(
        /* @__PURE__ */ jsxRuntime2.jsxs("div", { onClick: () => setZoomed(false), style: { position: "fixed", inset: 0, zIndex: 1e4, background: "rgba(0,0,0,0.92)", display: "flex", flexDirection: "column", alignItems: "center", justifyContent: "center", cursor: "zoom-out", animation: "fade-in 0.15s ease-out" }, children: [
          media.type === "video" ? /* @__PURE__ */ jsxRuntime2.jsx("video", { src: url, controls: true, autoPlay: true, playsInline: true, style: { maxWidth: "92%", maxHeight: "86%" } }) : /* @__PURE__ */ jsxRuntime2.jsx("img", { src: url, alt: "", style: { maxWidth: "92%", maxHeight: "86%", objectFit: "contain" } }),
          caption && /* @__PURE__ */ jsxRuntime2.jsx("div", { style: { color: "#fff", marginTop: 10, fontSize: 14, opacity: 0.85, maxWidth: "80%", textAlign: "center" }, children: caption })
        ] }),
        document.body
      )
    ] });
  };
  const renderContactsRoster = (ctx) => {
    const { ui, project, phone, variables, assetResolver, evaluateConditions: evaluateConditions2, onContactCall, onContactMessage, playTap } = ctx;
    const contacts = (ui.phoneContacts || []).filter((c) => {
      var _a;
      return !((_a = c.conditions) == null ? void 0 : _a.length) || evaluateConditions2(c.conditions, variables);
    });
    const sortedContacts = [...contacts].sort((a, b) => (b.pinned ? 1 : 0) - (a.pinned ? 1 : 0));
    const contactAvatarSize = \`\${ui.phoneContactAvatarSize ?? 2.4}em\`;
    const lastMessageFor = (cid) => {
      for (let i = phone.messages.length - 1; i >= 0; i--) {
        const m = phone.messages[i];
        if (m.senderId === cid || m.contactId === cid) return m.text;
      }
      return "";
    };
    return sortedContacts.length === 0 ? /* @__PURE__ */ jsxRuntime2.jsx("div", { style: { opacity: 0.5, textAlign: "center", marginTop: 12, fontSize: "0.8em" }, children: "No contacts" }) : sortedContacts.map((c) => {
      const char = project.characters[c.characterId];
      const curls = resolvePhonePortrait(c.avatar, char, assetResolver);
      const name = c.displayName || (char == null ? void 0 : char.name) || "Unknown";
      const status = c.statusText ? interpolateVariables(c.statusText, variables, project) : "";
      const preview = lastMessageFor(c.characterId);
      return /* @__PURE__ */ jsxRuntime2.jsxs("div", { style: { display: "flex", alignItems: "center", gap: 8, padding: "6px 8px", borderRadius: 12, background: ui.phoneContactRowColor || ui.phoneHistoryRowColor || "rgba(255,255,255,0.05)", color: ui.phoneContactTextColor || ui.phoneHistoryTextColor || "#fff" }, children: [
        ui.phoneShowAvatars !== false && /* @__PURE__ */ jsxRuntime2.jsx(PhonePortrait, { urls: curls, size: contactAvatarSize, fit: ui.phoneContactAvatarFit }),
        /* @__PURE__ */ jsxRuntime2.jsxs("div", { style: { flex: 1, minWidth: 0 }, children: [
          /* @__PURE__ */ jsxRuntime2.jsx("div", { style: { whiteSpace: "nowrap", overflow: "hidden", textOverflow: "ellipsis", fontWeight: 600, ...ui.phoneContactNameFont ? fontSettingsToStyle(ui.phoneContactNameFont) : {} }, children: name }),
          status && /* @__PURE__ */ jsxRuntime2.jsx("div", { style: { fontSize: "0.7em", opacity: 0.75, ...ui.phoneContactStatusFont ? fontSettingsToStyle(ui.phoneContactStatusFont) : {} }, children: status }),
          preview && /* @__PURE__ */ jsxRuntime2.jsx("div", { style: { fontSize: "0.7em", opacity: 0.6, whiteSpace: "nowrap", overflow: "hidden", textOverflow: "ellipsis" }, children: interpolateVariables(preview, variables, project) })
        ] }),
        /* @__PURE__ */ jsxRuntime2.jsxs("div", { style: { display: "flex", gap: 4, flexShrink: 0 }, children: [
          !c.hideCall && /* @__PURE__ */ jsxRuntime2.jsx("button", { onClick: () => {
            playTap();
            onContactCall(c.characterId);
          }, title: ui.phoneContactCallLabel || "Call", style: { display: "flex", alignItems: "center", gap: 3, padding: "4px 8px", borderRadius: 9999, border: "none", cursor: "pointer", background: ui.phoneCallAcceptColor || "#22c55e", color: "#fff", fontSize: "0.7em" }, children: "📞" }),
          !c.hideMessage && /* @__PURE__ */ jsxRuntime2.jsx("button", { onClick: () => {
            playTap();
            onContactMessage(c.characterId);
          }, title: ui.phoneContactMessageLabel || "Message", style: { display: "flex", alignItems: "center", gap: 3, padding: "4px 8px", borderRadius: 9999, border: "none", cursor: "pointer", background: ui.phoneOutgoingBubbleColor || "#2f6bff", color: "#fff", fontSize: "0.7em" }, children: "💬" })
        ] })
      ] }, c.id);
    });
  };
  const fireAppButton = (ctx, b) => {
    ctx.playTap();
    if (b.appId) ctx.openApp(b.appId);
    else if (b.action) ctx.onAction(b.action);
  };
  const HomeWidgets = ({ ctx }) => {
    const { ui, project, variables, assetResolver, evaluateConditions: evaluateConditions2 } = ctx;
    const widgets = (ui.phoneHomeWidgets || []).filter((w) => {
      var _a;
      return !((_a = w.conditions) == null ? void 0 : _a.length) || evaluateConditions2(w.conditions, variables);
    });
    if (!widgets.length) return null;
    return /* @__PURE__ */ jsxRuntime2.jsx(jsxRuntime2.Fragment, { children: widgets.map((w) => {
      const font = w.font ? fontSettingsToStyle(w.font) : {};
      const style = { position: "absolute", left: \`\${w.x}%\`, top: \`\${w.y}%\`, width: \`\${w.width}%\`, ...w.height ? { height: \`\${w.height}%\` } : {}, pointerEvents: "none", color: w.color || "#fff", textShadow: "0 1px 4px rgba(0,0,0,0.6)", ...font };
      if (w.type === "image" && w.image) {
        const url = assetResolver(w.image.id, w.image.type === "video" ? "video" : "image");
        if (!url) return null;
        return /* @__PURE__ */ jsxRuntime2.jsx("div", { style, children: w.image.type === "video" ? /* @__PURE__ */ jsxRuntime2.jsx("video", { src: url, autoPlay: true, loop: true, muted: true, playsInline: true, style: { width: "100%", height: "100%", objectFit: "contain" } }) : /* @__PURE__ */ jsxRuntime2.jsx("img", { src: url, alt: "", style: { width: "100%", height: "100%", objectFit: "contain" } }) }, w.id);
      }
      const text = w.type === "clock" ? ui.phoneClockText || "" : w.text || "";
      return /* @__PURE__ */ jsxRuntime2.jsx("div", { style: { ...style, display: "flex", alignItems: "center", justifyContent: "center", textAlign: "center" }, children: interpolateVariables(text, variables, project) }, w.id);
    }) });
  };
  const HomeApp = {
    id: "home",
    glyph: "home",
    defaultLabel: "Home",
    render: (ctx) => {
      const { ui, assetResolver, buttons } = ctx;
      if (ui.phoneButtonLayout !== "grid") return /* @__PURE__ */ jsxRuntime2.jsx(HomeWidgets, { ctx });
      const cols = Math.max(1, ui.phoneHomeGridColumns ?? 3);
      const iconPct = Math.max(6, Math.min(40, ui.phoneHomeIconSize ?? 18));
      const labelFont = ui.phoneHomeLabelFont ? fontSettingsToStyle(ui.phoneHomeLabelFont) : {};
      return /* @__PURE__ */ jsxRuntime2.jsxs(jsxRuntime2.Fragment, { children: [
        /* @__PURE__ */ jsxRuntime2.jsx(HomeWidgets, { ctx }),
        /* @__PURE__ */ jsxRuntime2.jsx("div", { style: { display: "grid", gridTemplateColumns: \`repeat(\${cols}, 1fr)\`, gap: "4%", padding: "4% 2%", alignContent: "start" }, children: buttons.map((b) => {
          const customIcon = b.iconImage ? assetResolver(b.iconImage.id, "image") : null;
          return /* @__PURE__ */ jsxRuntime2.jsxs(
            "button",
            {
              onClick: () => fireAppButton(ctx, b),
              title: b.label || "",
              style: { display: "flex", flexDirection: "column", alignItems: "center", gap: 4, background: "transparent", border: "none", cursor: "pointer", color: ui.phoneButtonIconColor || "#cbd5e1", padding: 0 },
              children: [
                /* @__PURE__ */ jsxRuntime2.jsx("span", { style: {
                  width: \`\${iconPct / (100 / cols) * 100}%\`,
                  maxWidth: "86%",
                  aspectRatio: "1",
                  display: "flex",
                  alignItems: "center",
                  justifyContent: "center",
                  background: ui.phoneHomeIconBgColor || "transparent",
                  borderRadius: ui.phoneHomeIconRadius ?? 14,
                  containerType: "size"
                }, children: customIcon ? /* @__PURE__ */ jsxRuntime2.jsx("img", { src: customIcon, alt: "", style: { width: "86cqmin", height: "86cqmin", objectFit: "contain" } }) : /* @__PURE__ */ jsxRuntime2.jsx("span", { style: { fontSize: "64cqmin", lineHeight: 1 }, children: b.builtinIcon && PHONE_GLYPHS[b.builtinIcon] || "●" }) }),
                ui.phoneHomeShowLabels !== false && b.label && /* @__PURE__ */ jsxRuntime2.jsx("span", { style: { fontSize: "0.65em", whiteSpace: "nowrap", overflow: "hidden", textOverflow: "ellipsis", maxWidth: "100%", textShadow: "0 1px 3px rgba(0,0,0,0.7)", ...labelFont }, children: b.label })
              ]
            },
            b.id
          );
        }) })
      ] });
    }
  };
  const ChatApp = {
    id: "chat",
    glyph: "chat",
    defaultLabel: "Messages",
    usesChatBackground: true,
    render: (ctx) => {
      var _a;
      const { ui, project, phone, variables, assetResolver, evaluateConditions: evaluateConditions2, onAction, onReply, playTap, onOpenThread } = ctx;
      const bodyFont = ui.phoneFont ? fontSettingsToStyle(ui.phoneFont) : {};
      const chatAvatarSize = \`\${ui.phoneChatAvatarSize ?? 2.2}em\`;
      const activeContactId = phone.activeContactId;
      if (!activeContactId) {
        const groups = /* @__PURE__ */ new Map();
        phone.messages.forEach((m, idx) => {
          const key = phoneThreadKey(m);
          if (!groups.has(key)) groups.set(key, []);
          groups.get(key).push({ m, idx });
        });
        const rows = Array.from(groups.entries()).map(([key, entries]) => {
          const contact = key ? (ui.phoneContacts || []).find((c) => c.characterId === key) : void 0;
          const char = key ? project.characters[key] : null;
          const name = (contact == null ? void 0 : contact.displayName) || (char == null ? void 0 : char.name) || (key ? "?" : ui.phoneMessagesHeader || "Messages");
          const urls = char ? resolvePhonePortrait(contact == null ? void 0 : contact.avatar, char, assetResolver) : [];
          const last = entries[entries.length - 1];
          const preview = last.m.text ? interpolateVariables(last.m.text, variables, project) : last.m.image ? "📷 Photo" : "";
          const unread = Math.max(0, entries.length - ((phone.threadLastRead || {})[key] ?? 0));
          return { key, name, urls, preview, unread, recency: last.idx };
        });
        const played = phone.playedConversations || [];
        (ui.phoneContacts || []).forEach((c) => {
          var _a2;
          if (groups.has(c.characterId) || c.hideMessage) return;
          if (((_a2 = c.conditions) == null ? void 0 : _a2.length) && !evaluateConditions2(c.conditions, variables)) return;
          const available = (c.textConversations || []).some((e) => {
            var _a3, _b, _c;
            return !!((_b = (_a3 = e.conversation) == null ? void 0 : _a3.lines) == null ? void 0 : _b.length) && (!e.once || !played.includes(e.id)) && (!((_c = e.conditions) == null ? void 0 : _c.length) || evaluateConditions2(e.conditions, variables));
          });
          if (!available) return;
          const char = project.characters[c.characterId];
          rows.push({ key: c.characterId, name: c.displayName || (char == null ? void 0 : char.name) || "?", urls: char ? resolvePhonePortrait(c.avatar, char, assetResolver) : [], preview: ui.phoneMessagesNewHint || "New conversation", unread: 1, recency: Number.MAX_SAFE_INTEGER });
        });
        rows.sort((a, b) => b.recency - a.recency);
        return /* @__PURE__ */ jsxRuntime2.jsxs(jsxRuntime2.Fragment, { children: [
          /* @__PURE__ */ jsxRuntime2.jsx("div", { style: { fontWeight: 700, fontSize: "0.9em", marginBottom: 4, ...ui.phoneTitleFont ? fontSettingsToStyle(ui.phoneTitleFont) : {} }, children: ui.phoneMessagesHeader || "Messages" }),
          rows.length === 0 && /* @__PURE__ */ jsxRuntime2.jsx("div", { style: { opacity: 0.5, textAlign: "center", marginTop: 12, fontSize: "0.8em" }, children: ui.phoneMessagesEmptyText || "No messages yet" }),
          rows.map((r) => /* @__PURE__ */ jsxRuntime2.jsxs(
            "div",
            {
              onClick: () => {
                if (onOpenThread) {
                  playTap();
                  onOpenThread(r.key);
                }
              },
              style: { display: "flex", alignItems: "center", gap: 8, padding: "6px 8px", borderRadius: 12, cursor: "pointer", background: ui.phoneContactRowColor || ui.phoneHistoryRowColor || "rgba(255,255,255,0.05)", color: ui.phoneContactTextColor || ui.phoneHistoryTextColor || "#fff" },
              children: [
                ui.phoneShowAvatars !== false && r.urls.length > 0 && /* @__PURE__ */ jsxRuntime2.jsx(PhonePortrait, { urls: r.urls, size: \`\${ui.phoneContactAvatarSize ?? 2.4}em\`, fit: ui.phoneContactAvatarFit }),
                /* @__PURE__ */ jsxRuntime2.jsxs("div", { style: { flex: 1, minWidth: 0 }, children: [
                  /* @__PURE__ */ jsxRuntime2.jsx("div", { style: { whiteSpace: "nowrap", overflow: "hidden", textOverflow: "ellipsis", fontWeight: 600, ...ui.phoneContactNameFont ? fontSettingsToStyle(ui.phoneContactNameFont) : {} }, children: r.name }),
                  r.preview && /* @__PURE__ */ jsxRuntime2.jsx("div", { style: { fontSize: "0.7em", opacity: 0.6, whiteSpace: "nowrap", overflow: "hidden", textOverflow: "ellipsis" }, children: r.preview })
                ] }),
                r.unread > 0 && /* @__PURE__ */ jsxRuntime2.jsx("span", { style: { flexShrink: 0, minWidth: 18, height: 18, padding: "0 5px", borderRadius: 9999, background: ui.phoneBadgeColor || "#ef4444", color: ui.phoneBadgeTextColor || "#fff", fontSize: "0.65em", display: "inline-flex", alignItems: "center", justifyContent: "center", fontWeight: 700 }, children: r.unread > 9 ? "9+" : r.unread })
              ]
            },
            r.key || "·"
          ))
        ] });
      }
      const effectiveReplies = phone.pendingReplies && phone.pendingReplies.length ? phone.pendingReplies : (phone.pendingChoices || []).map((o) => ({ id: o.id, text: o.text, conditions: o.conditions, actions: o.actions }));
      const threadKey = activeContactId === "__misc__" ? "" : activeContactId;
      const threadMessages = phone.messages.filter((m) => phoneThreadKey(m) === threadKey);
      const activeContact = (ui.phoneContacts || []).find((c) => c.characterId === activeContactId);
      const activeContactName = (activeContact == null ? void 0 : activeContact.displayName) || ((_a = project.characters[activeContactId]) == null ? void 0 : _a.name) || (activeContactId === "__misc__" ? ui.phoneMessagesHeader || "Messages" : "");
      return /* @__PURE__ */ jsxRuntime2.jsxs(jsxRuntime2.Fragment, { children: [
        activeContactId && /* @__PURE__ */ jsxRuntime2.jsxs("div", { style: { display: "flex", alignItems: "center", gap: 6, marginBottom: 2 }, children: [
          /* @__PURE__ */ jsxRuntime2.jsx("button", { onClick: () => onOpenThread ? onOpenThread(null) : onAction({ type: UIActionType.ShowPhoneContacts }), title: "Back", style: { background: "transparent", border: "none", cursor: "pointer", color: "inherit", fontSize: "1.1em", lineHeight: 1, padding: "0 4px" }, children: "‹" }),
          /* @__PURE__ */ jsxRuntime2.jsx("span", { style: { fontWeight: 600, fontSize: "0.85em" }, children: activeContactName })
        ] }),
        threadMessages.map((m) => {
          const mine = m.senderId === "player";
          const char = mine ? null : project.characters[m.senderId];
          const portraitUrls = mine ? [] : resolvePhonePortrait(m.portrait, char, assetResolver);
          return /* @__PURE__ */ jsxRuntime2.jsxs("div", { style: { display: "flex", flexDirection: mine ? "row-reverse" : "row", gap: 6, alignItems: "flex-end" }, children: [
            ui.phoneShowAvatars !== false && !mine && /* @__PURE__ */ jsxRuntime2.jsx(PhonePortrait, { urls: portraitUrls, size: chatAvatarSize, fit: ui.phoneChatAvatarFit }),
            /* @__PURE__ */ jsxRuntime2.jsxs("div", { style: { maxWidth: "76%" }, children: [
              !mine && (char == null ? void 0 : char.name) && /* @__PURE__ */ jsxRuntime2.jsx("div", { style: { fontSize: "0.7em", opacity: 0.75, marginBottom: 1, color: char.color }, children: char.name }),
              m.image && /* @__PURE__ */ jsxRuntime2.jsx(PhoneMedia, { media: m.image, assetResolver, caption: m.text ? interpolateVariables(m.text, variables, project) : void 0, style: { borderRadius: 14, marginBottom: m.text ? 3 : 0, aspectRatio: "4 / 3" } }),
              m.text && /* @__PURE__ */ jsxRuntime2.jsx("div", { style: { padding: "6px 10px", borderRadius: 14, wordBreak: "break-word", background: mine ? ui.phoneOutgoingBubbleColor || "#2f6bff" : ui.phoneIncomingBubbleColor || "#2a2f3a", color: ui.phoneBubbleTextColor || "#fff" }, children: interpolateVariables(m.text, variables, project) })
            ] })
          ] }, m.id);
        }),
        phone.typing && (() => {
          const tchar = phone.typing.senderId === "player" ? null : project.characters[phone.typing.senderId];
          const turls = tchar ? resolvePhonePortrait(void 0, tchar, assetResolver) : [];
          return /* @__PURE__ */ jsxRuntime2.jsxs("div", { style: { display: "flex", gap: 6, alignItems: "flex-end" }, children: [
            ui.phoneShowAvatars !== false && /* @__PURE__ */ jsxRuntime2.jsx(PhonePortrait, { urls: turls, size: chatAvatarSize, fit: ui.phoneChatAvatarFit }),
            /* @__PURE__ */ jsxRuntime2.jsx("div", { style: { padding: "8px 12px", borderRadius: 14, background: ui.phoneIncomingBubbleColor || "#2a2f3a", display: "inline-flex", gap: 4, alignItems: "center" }, children: [0, 1, 2].map((i) => /* @__PURE__ */ jsxRuntime2.jsx("span", { style: { width: 6, height: 6, borderRadius: "9999px", background: ui.phoneTypingColor || ui.phoneBubbleTextColor || "#fff", animation: \`vn-phone-typing 1s \${i * 0.2}s infinite\` } }, i)) })
          ] });
        })(),
        effectiveReplies.length > 0 && /* @__PURE__ */ jsxRuntime2.jsx("div", { style: { display: "flex", flexDirection: "column", gap: 6, marginTop: 4 }, children: effectiveReplies.filter((o) => {
          var _a2;
          return !((_a2 = o.conditions) == null ? void 0 : _a2.length) || evaluateConditions2(o.conditions, variables);
        }).map((o) => /* @__PURE__ */ jsxRuntime2.jsx("button", { onClick: () => {
          playTap();
          onReply(o);
        }, style: { alignSelf: "flex-end", maxWidth: "82%", padding: "6px 12px", borderRadius: 14, border: "none", cursor: "pointer", ...bodyFont, background: ui.phoneOutgoingBubbleColor || "#2f6bff", color: ui.phoneBubbleTextColor || "#fff" }, children: interpolateVariables(o.text, variables, project) }, o.id)) })
      ] });
    }
  };
  const HistoryApp = {
    id: "history",
    glyph: "call",
    defaultLabel: "Recents",
    render: (ctx) => {
      const { ui, project, phone, assetResolver, variables, playTap, onAction } = ctx;
      const notifs = [...phone.notifications || []].reverse();
      return /* @__PURE__ */ jsxRuntime2.jsxs(jsxRuntime2.Fragment, { children: [
        notifs.length > 0 && /* @__PURE__ */ jsxRuntime2.jsxs("div", { style: { display: "flex", flexDirection: "column", gap: 6, marginBottom: 4 }, children: [
          /* @__PURE__ */ jsxRuntime2.jsx("div", { style: { fontSize: "0.7em", fontWeight: 700, opacity: 0.65, textTransform: "uppercase", letterSpacing: 0.5 }, children: "Notifications" }),
          notifs.map((e) => {
            const echar = !e.senderId || e.senderId === "player" ? null : project.characters[e.senderId];
            const eurls = resolvePhonePortrait(e.portrait, echar, assetResolver);
            const eIconImg = e.iconImage ? assetResolver(e.iconImage.id, "image") : null;
            const taps = e.tapActions || [];
            return /* @__PURE__ */ jsxRuntime2.jsxs(
              "div",
              {
                onClick: taps.length ? () => {
                  playTap();
                  taps.forEach((a) => onAction(a));
                } : void 0,
                style: { display: "flex", alignItems: "center", gap: 8, padding: "6px 8px", borderRadius: 10, cursor: taps.length ? "pointer" : "default", background: ui.phoneHistoryRowColor || "rgba(255,255,255,0.05)", color: ui.phoneHistoryTextColor || "#fff", opacity: e.read ? 0.65 : 1 },
                children: [
                  eurls.length > 0 ? /* @__PURE__ */ jsxRuntime2.jsx(PhonePortrait, { urls: eurls, size: "2em" }) : eIconImg ? /* @__PURE__ */ jsxRuntime2.jsx("img", { src: eIconImg, alt: "", style: { width: "2em", height: "2em", objectFit: "contain", flexShrink: 0 } }) : /* @__PURE__ */ jsxRuntime2.jsx("span", { style: { fontSize: "1.3em", flexShrink: 0 }, children: e.icon && PHONE_GLYPHS[e.icon] || "🔔" }),
                  /* @__PURE__ */ jsxRuntime2.jsxs("div", { style: { flex: 1, minWidth: 0 }, children: [
                    (e.title || (echar == null ? void 0 : echar.name)) && /* @__PURE__ */ jsxRuntime2.jsx("div", { style: { whiteSpace: "nowrap", overflow: "hidden", textOverflow: "ellipsis", fontWeight: e.read ? 400 : 700 }, children: e.title || (echar == null ? void 0 : echar.name) }),
                    /* @__PURE__ */ jsxRuntime2.jsx("div", { style: { fontSize: "0.7em", opacity: 0.8, whiteSpace: "nowrap", overflow: "hidden", textOverflow: "ellipsis" }, children: interpolateVariables(e.text, variables, project) })
                  ] }),
                  !e.read && /* @__PURE__ */ jsxRuntime2.jsx("span", { style: { width: 8, height: 8, borderRadius: 9999, background: ui.phoneBadgeColor || "#ef4444", flexShrink: 0 } })
                ]
              },
              e.id
            );
          }),
          /* @__PURE__ */ jsxRuntime2.jsx("div", { style: { fontSize: "0.7em", fontWeight: 700, opacity: 0.65, textTransform: "uppercase", letterSpacing: 0.5, marginTop: 2 }, children: "Calls" })
        ] }),
        phone.callLog && phone.callLog.length > 0 ? [...phone.callLog].reverse().map((entry) => {
          const caller = entry.callerId === "player" ? null : project.characters[entry.callerId];
          const purls = resolvePhonePortrait(entry.portrait, caller, assetResolver);
          const icon = entry.status === "missed" ? "↙" : entry.status === "accepted" ? "↗" : "⊘";
          const tint = entry.status === "missed" ? "#ef4444" : entry.status === "accepted" ? "#22c55e" : "#9ca3af";
          return /* @__PURE__ */ jsxRuntime2.jsxs("div", { style: { display: "flex", alignItems: "center", gap: 8, padding: "6px 8px", borderRadius: 10, background: ui.phoneHistoryRowColor || "rgba(255,255,255,0.05)", color: ui.phoneHistoryTextColor || "#fff" }, children: [
            /* @__PURE__ */ jsxRuntime2.jsx(PhonePortrait, { urls: purls, size: "2em" }),
            /* @__PURE__ */ jsxRuntime2.jsxs("div", { style: { flex: 1, minWidth: 0 }, children: [
              /* @__PURE__ */ jsxRuntime2.jsx("div", { style: { whiteSpace: "nowrap", overflow: "hidden", textOverflow: "ellipsis" }, children: (caller == null ? void 0 : caller.name) || "Unknown" }),
              /* @__PURE__ */ jsxRuntime2.jsxs("div", { style: { fontSize: "0.7em", opacity: 0.8, color: tint }, children: [
                entry.direction === "outgoing" ? "↗" : icon,
                " ",
                entry.status,
                entry.durationMs ? \` · \${fmtCallTime(entry.durationMs)}\` : ""
              ] })
            ] })
          ] }, entry.id);
        }) : /* @__PURE__ */ jsxRuntime2.jsx("div", { style: { opacity: 0.5, textAlign: "center", marginTop: 12, fontSize: "0.8em" }, children: "No recent calls" })
      ] });
    }
  };
  const ContactsApp = {
    id: "contacts",
    glyph: "contacts",
    defaultLabel: "Contacts",
    render: (ctx) => ctx.ui.phoneContactsRegion ? null : renderContactsRoster(ctx)
  };
  const fmtCallTime = (ms) => \`\${Math.floor(ms / 6e4)}:\${String(Math.floor(ms / 1e3) % 60).padStart(2, "0")}\`;
  const CallApp = {
    id: "call",
    glyph: "phone",
    defaultLabel: "Call",
    render: (ctx) => {
      const { ui, project, phone, variables, assetResolver, evaluateConditions: evaluateConditions2, playTap, onCallReply, onEndCall } = ctx;
      const ac = phone.activeCall;
      if (!ac) return /* @__PURE__ */ jsxRuntime2.jsx("div", { style: { opacity: 0.5, textAlign: "center", marginTop: 12, fontSize: "0.8em" }, children: "No active call" });
      const char = ac.contactId === "player" ? null : project.characters[ac.contactId];
      const contact = ac.contactId === "player" ? void 0 : (ui.phoneContacts || []).find((c) => c.characterId === ac.contactId);
      const name = (contact == null ? void 0 : contact.displayName) || (char == null ? void 0 : char.name) || "Unknown";
      const purls = resolvePhonePortrait(ac.portrait || (contact == null ? void 0 : contact.avatar), char, assetResolver);
      const bodyFont = ui.phoneFont ? fontSettingsToStyle(ui.phoneFont) : {};
      const endBtn = (label) => /* @__PURE__ */ jsxRuntime2.jsxs(
        "button",
        {
          onClick: () => {
            playTap();
            onEndCall == null ? void 0 : onEndCall();
          },
          style: { alignSelf: "center", display: "flex", alignItems: "center", gap: 6, padding: "8px 18px", borderRadius: 9999, border: "none", cursor: "pointer", background: ui.phoneCallEndColor || "#ef4444", color: "#fff", fontWeight: 600, ...bodyFont },
          children: [
            ui.phoneCallEndImage ? /* @__PURE__ */ jsxRuntime2.jsx("img", { src: assetResolver(ui.phoneCallEndImage.id, "image") || void 0, alt: "", style: { width: "1.2em", height: "1.2em", objectFit: "contain" } }) : /* @__PURE__ */ jsxRuntime2.jsx("span", { children: ui.phoneCallEndIcon && PHONE_GLYPHS[ui.phoneCallEndIcon] || "📵" }),
            /* @__PURE__ */ jsxRuntime2.jsx("span", { children: ui.phoneCallEndLabel || "End Call" })
          ]
        }
      );
      const header = /* @__PURE__ */ jsxRuntime2.jsxs("div", { style: { display: "flex", flexDirection: "column", alignItems: "center", gap: 4, paddingTop: 6, flexShrink: 0 }, children: [
        /* @__PURE__ */ jsxRuntime2.jsx(PhonePortrait, { urls: purls, size: "3.4em", fit: ui.phoneCallPortraitFit }),
        /* @__PURE__ */ jsxRuntime2.jsx("div", { style: { fontWeight: 700, ...ui.phoneCallNameFont ? fontSettingsToStyle(ui.phoneCallNameFont) : {} }, children: name }),
        ac.phase === "dialing" && /* @__PURE__ */ jsxRuntime2.jsx("div", { style: { fontSize: "0.75em", opacity: 0.75 }, children: ui.phoneCallDialingText || "Calling…" }),
        ac.phase === "active" && /* @__PURE__ */ jsxRuntime2.jsx("div", { style: { fontSize: "0.75em", color: ui.phoneCallTimerColor || "rgba(255,255,255,0.7)" }, children: fmtCallTime(ac.elapsedMs) }),
        ac.phase === "ended" && /* @__PURE__ */ jsxRuntime2.jsxs("div", { style: { fontSize: "0.75em", opacity: 0.75 }, children: [
          "Call ended · ",
          fmtCallTime(ac.elapsedMs)
        ] })
      ] });
      if (ac.phase === "dialing") {
        return /* @__PURE__ */ jsxRuntime2.jsxs("div", { style: { display: "flex", flexDirection: "column", gap: 14, height: "100%", justifyContent: "center" }, children: [
          header,
          endBtn()
        ] });
      }
      return /* @__PURE__ */ jsxRuntime2.jsxs("div", { style: { display: "flex", flexDirection: "column", gap: 8, minHeight: "100%" }, children: [
        header,
        /* @__PURE__ */ jsxRuntime2.jsx("div", { style: { display: "flex", flexDirection: "column", gap: 6, flex: 1 }, children: ac.transcript.map((m) => {
          const mine = m.senderId === "player";
          return /* @__PURE__ */ jsxRuntime2.jsx("div", { style: { display: "flex", flexDirection: mine ? "row-reverse" : "row" }, children: /* @__PURE__ */ jsxRuntime2.jsxs("div", { style: { maxWidth: "80%" }, children: [
            m.image && /* @__PURE__ */ jsxRuntime2.jsx(PhoneMedia, { media: m.image, assetResolver, style: { borderRadius: 12, marginBottom: m.text ? 3 : 0, aspectRatio: "4 / 3" } }),
            m.text && /* @__PURE__ */ jsxRuntime2.jsx("div", { style: { padding: "5px 10px", borderRadius: 12, fontStyle: "italic", wordBreak: "break-word", background: mine ? ui.phoneCallLineOutgoingColor || ui.phoneOutgoingBubbleColor || "#2f6bff" : ui.phoneCallLineIncomingColor || ui.phoneIncomingBubbleColor || "#2a2f3a", color: ui.phoneBubbleTextColor || "#fff", opacity: 0.94 }, children: interpolateVariables(m.text, variables, project) })
          ] }) }, m.id);
        }) }),
        ac.phase === "active" && (ac.pendingReplies || []).filter((o) => {
          var _a;
          return !((_a = o.conditions) == null ? void 0 : _a.length) || evaluateConditions2(o.conditions, variables);
        }).length > 0 && /* @__PURE__ */ jsxRuntime2.jsx("div", { style: { display: "flex", flexDirection: "column", gap: 6 }, children: (ac.pendingReplies || []).filter((o) => {
          var _a;
          return !((_a = o.conditions) == null ? void 0 : _a.length) || evaluateConditions2(o.conditions, variables);
        }).map((o) => /* @__PURE__ */ jsxRuntime2.jsx("button", { onClick: () => {
          playTap();
          onCallReply == null ? void 0 : onCallReply(o);
        }, style: { alignSelf: "flex-end", maxWidth: "82%", padding: "6px 12px", borderRadius: 14, border: "none", cursor: "pointer", ...bodyFont, background: ui.phoneCallLineOutgoingColor || ui.phoneOutgoingBubbleColor || "#2f6bff", color: ui.phoneBubbleTextColor || "#fff" }, children: interpolateVariables(o.text, variables, project) }, o.id)) }),
        ac.phase === "active" && /* @__PURE__ */ jsxRuntime2.jsx("div", { style: { paddingBottom: 6, display: "flex", justifyContent: "center" }, children: endBtn() })
      ] });
    }
  };
  const GalleryAppBody = ({ ctx }) => {
    const { ui, project, phone, variables, assetResolver } = ctx;
    const cg = project.cgGallery;
    const cgEntries = cg ? Object.values(cg.entries || {}).sort((a, b) => (a.order ?? 0) - (b.order ?? 0)) : [];
    const showCG = (ui.phoneGalleryShowCG ?? true) && cgEntries.length > 0;
    const [tab, setTab] = React2.useState("photos");
    const roll = [...phone.cameraRoll || []].reverse();
    const cols = Math.max(1, ui.phoneGalleryColumns ?? (cg == null ? void 0 : cg.columns) ?? 3);
    const isUnlocked = (entry) => {
      if (!entry.unlockable) return true;
      if (!entry.unlockVariableId) return true;
      const val = variables[entry.unlockVariableId];
      return val === true || val === "true" || val === 1;
    };
    const lockedUrl = (cg == null ? void 0 : cg.lockedPlaceholderAssetId) ? assetResolver(cg.lockedPlaceholderAssetId, "image") : null;
    const tabBtn = (id, label) => /* @__PURE__ */ jsxRuntime2.jsx("button", { onClick: () => {
      ctx.playTap();
      setTab(id);
    }, style: { flex: 1, padding: "4px 0", borderRadius: 8, border: "none", cursor: "pointer", fontWeight: 600, fontSize: "0.75em", background: tab === id ? ui.phoneButtonActiveColor || ui.phoneOutgoingBubbleColor || "#2f6bff" : "rgba(255,255,255,0.08)", color: "#fff" }, children: label });
    const grid = (children) => /* @__PURE__ */ jsxRuntime2.jsx("div", { style: { display: "grid", gridTemplateColumns: \`repeat(\${cols}, 1fr)\`, gap: 6 }, children });
    return /* @__PURE__ */ jsxRuntime2.jsxs("div", { style: { display: "flex", flexDirection: "column", gap: 8 }, children: [
      showCG && /* @__PURE__ */ jsxRuntime2.jsxs("div", { style: { display: "flex", gap: 6 }, children: [
        tabBtn("photos", ui.phoneGalleryPhotosLabel || "Photos"),
        tabBtn("cg", ui.phoneGalleryCGLabel || "Collection")
      ] }),
      tab === "photos" || !showCG ? roll.length === 0 ? /* @__PURE__ */ jsxRuntime2.jsx("div", { style: { opacity: 0.5, textAlign: "center", marginTop: 12, fontSize: "0.8em" }, children: ui.phoneGalleryEmptyText || "No photos yet" }) : grid(roll.map((e) => /* @__PURE__ */ jsxRuntime2.jsx(
        PhoneMedia,
        {
          media: e.image,
          assetResolver,
          caption: e.caption ? interpolateVariables(e.caption, variables, project) : void 0,
          style: { borderRadius: 8, aspectRatio: "1" }
        },
        e.id
      ))) : grid(cgEntries.map((entry) => {
        const unlocked = isUnlocked(entry);
        if (!unlocked) {
          return /* @__PURE__ */ jsxRuntime2.jsx("div", { style: { borderRadius: 8, aspectRatio: "1", overflow: "hidden", background: "rgba(0,0,0,0.35)", display: "flex", alignItems: "center", justifyContent: "center" }, children: lockedUrl ? /* @__PURE__ */ jsxRuntime2.jsx("img", { src: lockedUrl, alt: "", style: { width: "100%", height: "100%", objectFit: "cover" } }) : /* @__PURE__ */ jsxRuntime2.jsx("span", { style: { opacity: 0.5 }, children: "🔒" }) }, entry.id);
        }
        return /* @__PURE__ */ jsxRuntime2.jsx(PhoneMedia, { media: { type: "image", id: entry.assetId }, assetResolver, caption: entry.name, style: { borderRadius: 8, aspectRatio: "1" } }, entry.id);
      }))
    ] });
  };
  const GalleryApp = {
    id: "gallery",
    glyph: "gallery",
    defaultLabel: "Gallery",
    render: (ctx) => /* @__PURE__ */ jsxRuntime2.jsx(GalleryAppBody, { ctx })
  };
  const MapSurface = ({ map, project, variables, assetResolver, evaluateConditions: evaluateConditions2, onLocationTap, travelAllowed = true, lockedText }) => {
    const [confirmId, setConfirmId] = React2.useState(null);
    const [gateMsg, setGateMsg] = React2.useState(false);
    const zoomEnabled = map.allowZoom !== false;
    const maxZoom = Math.max(1.2, Math.min(8, map.maxZoom ?? 3));
    const containerRef = React2.useRef(null);
    const [view, setView] = React2.useState({ s: 1, tx: 0, ty: 0 });
    const pointers = React2.useRef(/* @__PURE__ */ new Map());
    const dragRef = React2.useRef(null);
    const movedRef = React2.useRef(false);
    const rect = () => {
      var _a;
      return (_a = containerRef.current) == null ? void 0 : _a.getBoundingClientRect();
    };
    const clampView = React2.useCallback((v) => {
      const r = rect();
      const W = (r == null ? void 0 : r.width) ?? 0, H = (r == null ? void 0 : r.height) ?? 0;
      const s = Math.max(1, Math.min(maxZoom, v.s));
      if (s <= 1.0001) return { s: 1, tx: 0, ty: 0 };
      return { s, tx: Math.min(0, Math.max(W * (1 - s), v.tx)), ty: Math.min(0, Math.max(H * (1 - s), v.ty)) };
    }, [maxZoom]);
    const zoomTo = React2.useCallback((nextS, fx, fy) => {
      setView((prev) => {
        const s2 = Math.max(1, Math.min(maxZoom, nextS));
        const cx = (fx - prev.tx) / prev.s, cy = (fy - prev.ty) / prev.s;
        return clampView({ s: s2, tx: fx - cx * s2, ty: fy - cy * s2 });
      });
    }, [maxZoom, clampView]);
    const bump = (factor) => {
      const r = rect();
      if (r) zoomTo(view.s * factor, r.width / 2, r.height / 2);
    };
    const resetView = () => setView({ s: 1, tx: 0, ty: 0 });
    const onWheel = (e) => {
      if (!zoomEnabled) return;
      const r = rect();
      if (!r) return;
      zoomTo(view.s * (e.deltaY < 0 ? 1.15 : 1 / 1.15), e.clientX - r.left, e.clientY - r.top);
    };
    const onPointerDown = (e) => {
      var _a, _b;
      if (!zoomEnabled) return;
      pointers.current.set(e.pointerId, { x: e.clientX, y: e.clientY });
      (_b = (_a = e.currentTarget).setPointerCapture) == null ? void 0 : _b.call(_a, e.pointerId);
      movedRef.current = false;
      if (pointers.current.size === 2) {
        const [a, b] = [...pointers.current.values()];
        dragRef.current = { lastX: 0, lastY: 0, pinchDist: Math.hypot(a.x - b.x, a.y - b.y) };
      } else {
        dragRef.current = { lastX: e.clientX, lastY: e.clientY, pinchDist: 0 };
      }
    };
    const onPointerMove = (e) => {
      if (!zoomEnabled || !pointers.current.has(e.pointerId) || !dragRef.current) return;
      pointers.current.set(e.pointerId, { x: e.clientX, y: e.clientY });
      const r = rect();
      if (!r) return;
      if (pointers.current.size >= 2) {
        const [a, b] = [...pointers.current.values()];
        const dist = Math.hypot(a.x - b.x, a.y - b.y);
        if (dragRef.current.pinchDist > 0) zoomTo(view.s * (dist / dragRef.current.pinchDist), (a.x + b.x) / 2 - r.left, (a.y + b.y) / 2 - r.top);
        dragRef.current.pinchDist = dist;
        movedRef.current = true;
      } else if (view.s > 1) {
        const dx = e.clientX - dragRef.current.lastX, dy = e.clientY - dragRef.current.lastY;
        if (Math.abs(dx) + Math.abs(dy) > 3) movedRef.current = true;
        dragRef.current.lastX = e.clientX;
        dragRef.current.lastY = e.clientY;
        setView((prev) => clampView({ s: prev.s, tx: prev.tx + dx, ty: prev.ty + dy }));
      }
    };
    const onPointerUp = (e) => {
      pointers.current.delete(e.pointerId);
      const rest = [...pointers.current.values()];
      dragRef.current = rest.length === 1 ? { lastX: rest[0].x, lastY: rest[0].y, pinchDist: 0 } : null;
      if (rest.length === 0) window.setTimeout(() => {
        movedRef.current = false;
      }, 0);
    };
    const zoomBtnStyle = {
      width: 32,
      height: 32,
      borderRadius: "50%",
      border: "none",
      cursor: "pointer",
      background: "rgba(12,14,20,0.72)",
      color: "#fff",
      fontSize: 20,
      fontWeight: 700,
      lineHeight: 1,
      display: "flex",
      alignItems: "center",
      justifyContent: "center",
      boxShadow: "0 2px 8px rgba(0,0,0,0.45)",
      backdropFilter: "blur(2px)"
    };
    const bg = map.backgroundImage ? assetResolver(map.backgroundImage.id, map.backgroundImage.type === "video" ? "video" : "image") : null;
    const markerSize = Math.max(2, map.markerSize ?? 6);
    const tap = (loc) => {
      if (!travelAllowed) {
        setGateMsg(true);
        window.setTimeout(() => setGateMsg(false), 2200);
        return;
      }
      if (map.confirmTravel && confirmId !== loc.id) {
        setConfirmId(loc.id);
        return;
      }
      setConfirmId(null);
      onLocationTap(loc);
    };
    return /* @__PURE__ */ jsxRuntime2.jsxs(
      "div",
      {
        ref: containerRef,
        onWheel: zoomEnabled ? onWheel : void 0,
        onPointerDown: zoomEnabled ? onPointerDown : void 0,
        onPointerMove: zoomEnabled ? onPointerMove : void 0,
        onPointerUp: zoomEnabled ? onPointerUp : void 0,
        onPointerCancel: zoomEnabled ? onPointerUp : void 0,
        onDoubleClick: zoomEnabled ? () => view.s > 1 ? resetView() : bump(1.8) : void 0,
        style: { position: "relative", width: "100%", height: "100%", overflow: "hidden", background: "#0b0d12", touchAction: zoomEnabled ? "none" : void 0, cursor: zoomEnabled && view.s > 1 ? "grab" : void 0 },
        children: [
          /* @__PURE__ */ jsxRuntime2.jsxs("div", { style: { position: "absolute", inset: 0, transformOrigin: "0 0", transform: \`translate(\${view.tx}px, \${view.ty}px) scale(\${view.s})\`, willChange: view.s > 1 ? "transform" : void 0 }, children: [
            bg && (map.backgroundImage.type === "video" ? /* @__PURE__ */ jsxRuntime2.jsx("video", { src: bg, autoPlay: true, loop: true, muted: true, playsInline: true, style: { position: "absolute", inset: 0, width: "100%", height: "100%", objectFit: "cover" } }) : /* @__PURE__ */ jsxRuntime2.jsx("img", { src: bg, alt: "", style: { position: "absolute", inset: 0, width: "100%", height: "100%", objectFit: "cover" } })),
            map.locations.map((loc) => {
              var _a;
              const unlocked = !((_a = loc.conditions) == null ? void 0 : _a.length) || evaluateConditions2(loc.conditions, variables);
              const lockedLook = loc.lockedAppearance || "hidden";
              if (!unlocked && lockedLook === "hidden") return null;
              const style = loc.markerStyle || "icon";
              const markerUrl = style === "image" && loc.markerImage ? assetResolver(loc.markerImage.id, "image") : null;
              const displayLabel = unlocked ? interpolateVariables(loc.label || loc.name, variables, project) : loc.lockedLabel || "???";
              const clickable = unlocked;
              const dim = !unlocked;
              const isRegion = style === "region";
              const w = isRegion ? loc.width ?? 12 : markerSize;
              const h = isRegion ? loc.height ?? 12 : void 0;
              const locFont = loc.labelFont || map.labelFont;
              const labelStyle = locFont ? fontSettingsToStyle(locFont) : {};
              const labelImg = unlocked && loc.labelImage ? assetResolver(loc.labelImage.id, "image") : null;
              const showLabel = loc.showLabel !== false;
              return /* @__PURE__ */ jsxRuntime2.jsxs(
                "div",
                {
                  onClick: clickable ? (e) => {
                    e.stopPropagation();
                    if (movedRef.current) return;
                    tap(loc);
                  } : void 0,
                  title: displayLabel,
                  style: {
                    position: "absolute",
                    left: \`\${loc.x}%\`,
                    top: \`\${loc.y}%\`,
                    width: \`\${w}%\`,
                    ...isRegion ? { height: \`\${h}%\` } : { aspectRatio: "1" },
                    transform: isRegion ? void 0 : "translate(-50%, -50%)",
                    display: "flex",
                    flexDirection: "column",
                    alignItems: "center",
                    justifyContent: "center",
                    cursor: clickable ? "pointer" : "default",
                    opacity: dim ? 0.45 : 1,
                    containerType: "size"
                  },
                  children: [
                    !isRegion && (!unlocked && lockedLook === "lockedIcon" ? /* @__PURE__ */ jsxRuntime2.jsx("span", { style: { fontSize: "62cqmin", lineHeight: 1, filter: "drop-shadow(0 2px 4px rgba(0,0,0,0.7))" }, children: "🔒" }) : markerUrl ? /* @__PURE__ */ jsxRuntime2.jsx("img", { src: markerUrl, alt: "", style: { width: "92cqmin", height: "92cqmin", objectFit: "contain", filter: "drop-shadow(0 2px 4px rgba(0,0,0,0.6))" } }) : /* @__PURE__ */ jsxRuntime2.jsx("span", { style: { fontSize: "70cqmin", lineHeight: 1, color: map.markerColor || "#ef4444", filter: "drop-shadow(0 2px 4px rgba(0,0,0,0.7))" }, children: loc.builtinIcon && PHONE_GLYPHS[loc.builtinIcon] || "📍" })),
                    showLabel && (labelImg ? /* @__PURE__ */ jsxRuntime2.jsx("img", { src: labelImg, alt: "", style: { position: "absolute", top: "100%", marginTop: 2, height: 24, maxWidth: 140, objectFit: "contain", pointerEvents: "none", filter: "drop-shadow(0 1px 3px rgba(0,0,0,0.9))" } }) : /* @__PURE__ */ jsxRuntime2.jsx("span", { style: { position: "absolute", top: "100%", marginTop: 2, whiteSpace: "nowrap", fontSize: 11, fontWeight: 600, color: "#fff", textShadow: "0 1px 3px rgba(0,0,0,0.9)", ...labelStyle }, children: displayLabel })),
                    confirmId === loc.id && /* @__PURE__ */ jsxRuntime2.jsxs("div", { onClick: (e) => e.stopPropagation(), style: { position: "absolute", bottom: "110%", left: "50%", transform: "translateX(-50%)", background: "rgba(10,12,18,0.95)", borderRadius: 10, padding: "6px 10px", display: "flex", gap: 6, alignItems: "center", whiteSpace: "nowrap", zIndex: 5, boxShadow: "0 6px 20px rgba(0,0,0,0.5)" }, children: [
                      /* @__PURE__ */ jsxRuntime2.jsx("span", { style: { color: "#fff", fontSize: 12 }, children: (map.confirmText || "Travel to {name}?").replace("{name}", displayLabel) }),
                      /* @__PURE__ */ jsxRuntime2.jsx("button", { onClick: () => {
                        setConfirmId(null);
                        onLocationTap(loc);
                      }, style: { border: "none", borderRadius: 8, padding: "3px 10px", cursor: "pointer", background: "#22c55e", color: "#fff", fontWeight: 700 }, children: "✓" }),
                      /* @__PURE__ */ jsxRuntime2.jsx("button", { onClick: () => setConfirmId(null), style: { border: "none", borderRadius: 8, padding: "3px 10px", cursor: "pointer", background: "rgba(255,255,255,0.15)", color: "#fff" }, children: "✕" })
                    ] })
                  ]
                },
                loc.id
              );
            })
          ] }),
          zoomEnabled && /* @__PURE__ */ jsxRuntime2.jsxs(
            "div",
            {
              style: { position: "absolute", right: 8, bottom: 8, display: "flex", flexDirection: "column", gap: 6, zIndex: 6 },
              onPointerDown: (e) => e.stopPropagation(),
              onDoubleClick: (e) => e.stopPropagation(),
              children: [
                /* @__PURE__ */ jsxRuntime2.jsx("button", { type: "button", "aria-label": "Zoom in", title: "Zoom in", onClick: () => bump(1.4), style: zoomBtnStyle, children: "+" }),
                /* @__PURE__ */ jsxRuntime2.jsx("button", { type: "button", "aria-label": "Zoom out", title: "Zoom out", onClick: () => bump(1 / 1.4), style: zoomBtnStyle, children: "−" }),
                view.s > 1.0001 && /* @__PURE__ */ jsxRuntime2.jsx("button", { type: "button", "aria-label": "Reset zoom", title: "Reset zoom", onClick: resetView, style: { ...zoomBtnStyle, fontSize: 15 }, children: "⤢" })
              ]
            }
          ),
          gateMsg && /* @__PURE__ */ jsxRuntime2.jsx("div", { style: { position: "absolute", bottom: 10, left: "50%", transform: "translateX(-50%)", background: "rgba(10,12,18,0.92)", color: "#fff", borderRadius: 10, padding: "6px 14px", fontSize: 12, whiteSpace: "nowrap", animation: "fade-in 0.2s ease-out" }, children: lockedText || "You can't leave right now." })
        ]
      }
    );
  };
  const MapApp = {
    id: "map",
    glyph: "map",
    defaultLabel: "Map",
    isEnabled: (ui, project) => !!ui.phoneMapId && !!(project.maps || {})[ui.phoneMapId],
    render: (ctx) => {
      var _a;
      const { ui, project, variables, assetResolver, evaluateConditions: evaluateConditions2, onAction, playTap } = ctx;
      const map = ui.phoneMapId ? (project.maps || {})[ui.phoneMapId] : null;
      if (!map) return null;
      const travelAllowed = !((_a = ui.phoneMapTravelConditions) == null ? void 0 : _a.length) || evaluateConditions2(ui.phoneMapTravelConditions, variables);
      return /* @__PURE__ */ jsxRuntime2.jsx("div", { style: { margin: "-8px -10px", height: "calc(100% + 16px)" }, children: /* @__PURE__ */ jsxRuntime2.jsx(
        MapSurface,
        {
          map,
          project,
          variables,
          assetResolver,
          evaluateConditions: evaluateConditions2,
          travelAllowed,
          lockedText: ui.phoneMapTravelLockedText,
          onLocationTap: (loc) => {
            playTap();
            onAction({ type: UIActionType.HidePhone });
            (loc.actions || []).forEach((a) => onAction(a));
            if (loc.targetSceneId) onAction({ type: UIActionType.JumpToScene, targetSceneId: loc.targetSceneId });
          }
        }
      ) });
    }
  };
  const SettingsApp = {
    id: "settings",
    glyph: "settings",
    defaultLabel: "Settings",
    isEnabled: (ui) => !!(ui.phoneWallpapers && ui.phoneWallpapers.length > 0),
    render: (ctx) => {
      const { ui, phone, variables, assetResolver, evaluateConditions: evaluateConditions2, playTap, onSetWallpaper } = ctx;
      const options = (ui.phoneWallpapers || []).filter((w) => {
        var _a;
        return !((_a = w.conditions) == null ? void 0 : _a.length) || evaluateConditions2(w.conditions, variables);
      });
      const current = phone.wallpaperId || null;
      const tile = (key, selected, onClick, inner, label) => /* @__PURE__ */ jsxRuntime2.jsxs(
        "button",
        {
          onClick: () => {
            playTap();
            onClick();
          },
          style: { position: "relative", aspectRatio: "9 / 16", borderRadius: 10, overflow: "hidden", border: "none", padding: 0, cursor: "pointer", background: "rgba(255,255,255,0.06)", boxShadow: selected ? \`0 0 0 3px \${ui.phoneButtonActiveColor || "#2f6bff"}\` : "inset 0 0 0 1px rgba(255,255,255,0.12)" },
          children: [
            inner,
            label && /* @__PURE__ */ jsxRuntime2.jsx("span", { style: { position: "absolute", bottom: 4, left: 0, right: 0, textAlign: "center", fontSize: 10, color: "#fff", textShadow: "0 1px 3px rgba(0,0,0,0.9)" }, children: label }),
            selected && /* @__PURE__ */ jsxRuntime2.jsx("span", { style: { position: "absolute", top: 4, right: 4, width: 18, height: 18, borderRadius: 9999, background: ui.phoneButtonActiveColor || "#2f6bff", color: "#fff", fontSize: 12, display: "flex", alignItems: "center", justifyContent: "center" }, children: "✓" })
          ]
        },
        key
      );
      const defaultBg = ui.phoneWallpaperImage ? assetResolver(ui.phoneWallpaperImage.id, ui.phoneWallpaperImage.type === "video" ? "video" : "image") : null;
      return /* @__PURE__ */ jsxRuntime2.jsxs("div", { style: { display: "flex", flexDirection: "column", gap: 8 }, children: [
        /* @__PURE__ */ jsxRuntime2.jsx("div", { style: { fontSize: "0.75em", fontWeight: 700, opacity: 0.8 }, children: ui.phoneSettingsWallpaperLabel || "Wallpaper" }),
        /* @__PURE__ */ jsxRuntime2.jsxs("div", { style: { display: "grid", gridTemplateColumns: "repeat(3, 1fr)", gap: 6 }, children: [
          tile(
            "__default__",
            !current,
            () => onSetWallpaper == null ? void 0 : onSetWallpaper(null),
            defaultBg ? ui.phoneWallpaperImage.type === "video" ? /* @__PURE__ */ jsxRuntime2.jsx("video", { src: defaultBg, muted: true, loop: true, playsInline: true, autoPlay: true, style: { width: "100%", height: "100%", objectFit: "cover" } }) : /* @__PURE__ */ jsxRuntime2.jsx("img", { src: defaultBg, alt: "", style: { width: "100%", height: "100%", objectFit: "cover" } }) : /* @__PURE__ */ jsxRuntime2.jsx("span", { style: { position: "absolute", inset: 0, display: "flex", alignItems: "center", justifyContent: "center", color: "rgba(255,255,255,0.5)", fontSize: 18 }, children: "—" }),
            "Default"
          ),
          options.map((w) => {
            const url = assetResolver((w.thumbnail || w.image).id, w.thumbnail ? "image" : w.image.type === "video" ? "video" : "image");
            const isVid = !w.thumbnail && w.image.type === "video";
            return tile(
              w.id,
              current === w.id,
              () => onSetWallpaper == null ? void 0 : onSetWallpaper(w.id),
              url ? isVid ? /* @__PURE__ */ jsxRuntime2.jsx("video", { src: url, muted: true, loop: true, playsInline: true, autoPlay: true, style: { width: "100%", height: "100%", objectFit: "cover" } }) : /* @__PURE__ */ jsxRuntime2.jsx("img", { src: url, alt: "", style: { width: "100%", height: "100%", objectFit: "cover" } }) : null,
              w.name
            );
          })
        ] })
      ] });
    }
  };
  const PHONE_APPS = {
    home: HomeApp,
    chat: ChatApp,
    history: HistoryApp,
    contacts: ContactsApp,
    gallery: GalleryApp,
    map: MapApp,
    settings: SettingsApp,
    call: CallApp
  };
  function resolvePhoneApp(view, ui, project) {
    const app = PHONE_APPS[view];
    if (app && (!app.isEnabled || app.isEnabled(ui, project))) return app;
    return PHONE_APPS.home;
  }
  ["chat", "contacts", "history", "gallery", "map", "settings"].map((id) => ({ id, glyph: PHONE_APPS[id].glyph, label: PHONE_APPS[id].defaultLabel }));
  function resolveVarNumber(variables, variableId, fallback, opts) {
    let value = fallback;
    if (variableId && variables) {
      const raw = variables[variableId];
      const n = typeof raw === "number" ? raw : parseFloat(String(raw));
      if (Number.isFinite(n)) value = n * ((opts == null ? void 0 : opts.scale) ?? 1);
    }
    if ((opts == null ? void 0 : opts.min) !== void 0) value = Math.max(opts.min, value);
    if ((opts == null ? void 0 : opts.max) !== void 0) value = Math.min(opts.max, value);
    return value;
  }
  const SAMPLE_MAX = 128;
  const SAMPLE_MIN_MS = 300;
  const GRID_COLS = 24;
  const GRID_ROWS = 14;
  function buildStamp(style, sizePx, customImg) {
    const d = Math.max(4, Math.ceil(sizePx));
    const c = document.createElement("canvas");
    c.width = d;
    c.height = d;
    const ctx = c.getContext("2d");
    if (!ctx) return c;
    const r = d / 2;
    if (style === "custom" && customImg) {
      ctx.drawImage(customImg, 0, 0, d, d);
    } else if (style === "hardRound") {
      ctx.fillStyle = "#fff";
      ctx.beginPath();
      ctx.arc(r, r, r, 0, Math.PI * 2);
      ctx.fill();
    } else if (style === "sponge") {
      for (let i = 0; i < 46; i++) {
        const a = Math.random() * Math.PI * 2;
        const rr = Math.sqrt(Math.random()) * r * 0.9;
        const cr = r * (0.08 + Math.random() * 0.16);
        const g = ctx.createRadialGradient(r + Math.cos(a) * rr, r + Math.sin(a) * rr, 0, r + Math.cos(a) * rr, r + Math.sin(a) * rr, cr);
        g.addColorStop(0, "rgba(255,255,255,0.85)");
        g.addColorStop(1, "rgba(255,255,255,0)");
        ctx.fillStyle = g;
        ctx.beginPath();
        ctx.arc(r + Math.cos(a) * rr, r + Math.sin(a) * rr, cr, 0, Math.PI * 2);
        ctx.fill();
      }
    } else {
      const g = ctx.createRadialGradient(r, r, 0, r, r, r);
      g.addColorStop(0, "rgba(255,255,255,1)");
      g.addColorStop(0.65, "rgba(255,255,255,0.9)");
      g.addColorStop(1, "rgba(255,255,255,0)");
      ctx.fillStyle = g;
      ctx.fillRect(0, 0, d, d);
    }
    return c;
  }
  const WipeGameSurface = ({ ctx }) => {
    const stage = ctx.stage;
    const { assetResolver } = ctx;
    const hostRef = React2.useRef(null);
    const canvasRef = React2.useRef(null);
    const [box, setBox] = React2.useState({ w: 0, h: 0 });
    const [won, setWon] = React2.useState(false);
    const [clearing, setClearing] = React2.useState(false);
    const drawRef = React2.useRef({ ctx2d: null, dpr: 1, stamp: null, brushPx: 0, last: null, initialOpaque: 0, lastSampleAt: 0, tainted: false, visited: /* @__PURE__ */ new Set(), stamped: false, wonReported: false });
    const coverType = stage.coverType || "color";
    const coverUrl = coverType === "image" && stage.coverImageId ? assetResolver(stage.coverImageId, "image") : null;
    const coverMaskUrl = stage.coverMaskImageId ? assetResolver(stage.coverMaskImageId, "image") : null;
    const revealUrl = stage.revealImageId ? assetResolver(stage.revealImageId, "image") : null;
    const customBrushUrl = stage.brushStyle === "custom" && stage.customBrushImageId ? assetResolver(stage.customBrushImageId, "image") : null;
    const winFrac = Math.min(100, Math.max(1, stage.winRevealPercent ?? 70)) / 100;
    React2.useEffect(() => {
      const el = hostRef.current;
      if (!el) return;
      const obs = new ResizeObserver(() => setBox({ w: el.clientWidth, h: el.clientHeight }));
      obs.observe(el);
      setBox({ w: el.clientWidth, h: el.clientHeight });
      return () => obs.disconnect();
    }, []);
    React2.useEffect(() => {
      const canvas = canvasRef.current;
      const d = drawRef.current;
      if (!canvas || box.w <= 0 || box.h <= 0) return;
      const dpr = Math.max(1, Math.min(2, window.devicePixelRatio || 1));
      canvas.width = Math.floor(box.w * dpr);
      canvas.height = Math.floor(box.h * dpr);
      const c2d = canvas.getContext("2d");
      if (!c2d) return;
      c2d.setTransform(dpr, 0, 0, dpr, 0, 0);
      d.ctx2d = c2d;
      d.dpr = dpr;
      d.last = null;
      d.initialOpaque = 0;
      d.lastSampleAt = 0;
      d.tainted = false;
      d.visited = /* @__PURE__ */ new Set();
      d.stamped = false;
      d.wonReported = false;
      setWon(false);
      setClearing(false);
      const brushPct = Math.min(25, Math.max(4, stage.brushSize ?? 10));
      d.brushPx = Math.max(8, Math.min(box.w, box.h) * brushPct / 100);
      const finishSetup = (customImg) => {
        d.stamp = buildStamp(stage.brushStyle || "softRound", d.brushPx * dpr, customImg);
      };
      if (customBrushUrl) {
        const img = new Image();
        img.onload = () => finishSetup(img);
        img.onerror = () => finishSetup(null);
        img.src = customBrushUrl;
      } else finishSetup(null);
      const measureBaseline = () => {
        const s = sampleOpaque(canvas);
        if (s == null) {
          d.tainted = true;
          d.initialOpaque = 0;
        } else d.initialOpaque = s;
      };
      const applyMaskThenMeasure = () => {
        if (!coverMaskUrl) {
          measureBaseline();
          return;
        }
        const m = new Image();
        m.onload = () => {
          c2d.save();
          c2d.globalCompositeOperation = "destination-in";
          c2d.drawImage(m, 0, 0, box.w, box.h);
          c2d.restore();
          measureBaseline();
        };
        m.onerror = () => measureBaseline();
        m.src = coverMaskUrl;
      };
      c2d.globalCompositeOperation = "source-over";
      c2d.clearRect(0, 0, box.w, box.h);
      if (coverType === "image" && coverUrl) {
        const img = new Image();
        img.onload = () => {
          const scale = Math.max(box.w / img.naturalWidth, box.h / img.naturalHeight);
          const w = img.naturalWidth * scale, h = img.naturalHeight * scale;
          c2d.drawImage(img, (box.w - w) / 2, (box.h - h) / 2, w, h);
          applyMaskThenMeasure();
        };
        img.onerror = () => {
          c2d.fillStyle = stage.coverColor || "#9ca3af";
          c2d.fillRect(0, 0, box.w, box.h);
          applyMaskThenMeasure();
        };
        img.src = coverUrl;
      } else if (coverType === "frost") {
        c2d.fillStyle = "rgba(226, 236, 244, 0.94)";
        c2d.fillRect(0, 0, box.w, box.h);
        c2d.fillStyle = "rgba(255,255,255,0.5)";
        for (let i = 0; i < 900; i++) {
          c2d.globalAlpha = 0.15 + Math.random() * 0.5;
          c2d.fillRect(Math.random() * box.w, Math.random() * box.h, 1 + Math.random() * 2, 1 + Math.random() * 2);
        }
        c2d.globalAlpha = 1;
        applyMaskThenMeasure();
      } else {
        c2d.fillStyle = stage.coverColor || "#9ca3af";
        c2d.fillRect(0, 0, box.w, box.h);
        applyMaskThenMeasure();
      }
    }, [box.w, box.h, coverType, coverUrl, coverMaskUrl, stage.coverColor, stage.brushStyle, stage.brushSize, customBrushUrl, stage.id]);
    const sampleOpaque = (canvas) => {
      try {
        const scale = SAMPLE_MAX / Math.max(canvas.width, canvas.height);
        const sw = Math.max(1, Math.floor(canvas.width * scale));
        const sh = Math.max(1, Math.floor(canvas.height * scale));
        const sc = document.createElement("canvas");
        sc.width = sw;
        sc.height = sh;
        const sctx = sc.getContext("2d", { willReadFrequently: true });
        if (!sctx) return null;
        sctx.drawImage(canvas, 0, 0, sw, sh);
        const data = sctx.getImageData(0, 0, sw, sh).data;
        let opaque = 0;
        for (let i = 3; i < data.length; i += 4) if (data[i] > 128) opaque++;
        return opaque;
      } catch {
        return null;
      }
    };
    const checkProgress = (force) => {
      var _a;
      const d = drawRef.current;
      const canvas = canvasRef.current;
      if (!canvas || d.wonReported || !d.stamped) return;
      const now = performance.now();
      if (!force && now - d.lastSampleAt < SAMPLE_MIN_MS) return;
      d.lastSampleAt = now;
      let frac = 0;
      if (!d.tainted && d.initialOpaque > 0) {
        const s = sampleOpaque(canvas);
        if (s == null) d.tainted = true;
        else frac = 1 - s / d.initialOpaque;
      }
      if (d.tainted || d.initialOpaque === 0) {
        frac = d.visited.size / (GRID_COLS * GRID_ROWS);
      }
      (_a = ctx.reportProgress) == null ? void 0 : _a.call(ctx, Math.min(1, frac));
      if (frac >= winFrac) {
        d.wonReported = true;
        setWon(true);
        if (stage.autoClearOnWin !== false) {
          setClearing(true);
          window.setTimeout(() => ctx.reportWin(), 450);
        } else {
          ctx.reportWin();
        }
      }
    };
    const stampAt = (x, y, angle) => {
      const d = drawRef.current;
      const c2d = d.ctx2d;
      if (!c2d) return;
      c2d.save();
      c2d.globalCompositeOperation = "destination-out";
      if (ctx.stage.brushStyle === "scratch") {
        c2d.translate(x, y);
        c2d.rotate(angle);
        c2d.fillStyle = "#fff";
        for (let i = 0; i < 3; i++) {
          const off = (i - 1) * d.brushPx * 0.18 + (Math.random() - 0.5) * d.brushPx * 0.1;
          c2d.fillRect(-d.brushPx / 2, off - d.brushPx * 0.045, d.brushPx, d.brushPx * 0.09);
        }
      } else if (d.stamp) {
        const s = d.brushPx;
        c2d.drawImage(d.stamp, x - s / 2, y - s / 2, s, s);
      }
      c2d.restore();
      d.stamped = true;
      const gx = Math.min(GRID_COLS - 1, Math.max(0, Math.floor(x / Math.max(1, box.w) * GRID_COLS)));
      const gy = Math.min(GRID_ROWS - 1, Math.max(0, Math.floor(y / Math.max(1, box.h) * GRID_ROWS)));
      d.visited.add(gy * GRID_COLS + gx);
    };
    const pointFrom = (e) => {
      const rect = canvasRef.current.getBoundingClientRect();
      return { x: e.clientX - rect.left, y: e.clientY - rect.top };
    };
    const onPointerDown = (e) => {
      var _a, _b;
      if (won) return;
      (_b = (_a = e.currentTarget).setPointerCapture) == null ? void 0 : _b.call(_a, e.pointerId);
      const p = pointFrom(e);
      drawRef.current.last = p;
      stampAt(p.x, p.y, 0);
      ctx.playInteractSound();
    };
    const onPointerMove = (e) => {
      const d = drawRef.current;
      if (won || !d.last) return;
      const p = pointFrom(e);
      const dx = p.x - d.last.x, dy = p.y - d.last.y;
      const dist = Math.hypot(dx, dy);
      const step = Math.max(2, d.brushPx / 3);
      const angle = Math.atan2(dy, dx);
      for (let t = step; t <= dist; t += step) {
        stampAt(d.last.x + dx * t / dist, d.last.y + dy * t / dist, angle);
      }
      if (dist >= step) d.last = p;
      checkProgress(false);
    };
    const endStroke = () => {
      drawRef.current.last = null;
      checkProgress(true);
    };
    return /* @__PURE__ */ jsxRuntime2.jsxs("div", { ref: hostRef, style: { position: "relative", width: "100%", height: "100%", overflow: "hidden" }, children: [
      revealUrl && /* @__PURE__ */ jsxRuntime2.jsx(
        "img",
        {
          src: revealUrl,
          alt: "",
          draggable: false,
          style: { position: "absolute", inset: 0, width: "100%", height: "100%", objectFit: "contain", pointerEvents: "none" }
        }
      ),
      /* @__PURE__ */ jsxRuntime2.jsx(
        "canvas",
        {
          ref: canvasRef,
          onPointerDown,
          onPointerMove,
          onPointerUp: endStroke,
          onPointerCancel: endStroke,
          onPointerLeave: endStroke,
          style: {
            position: "absolute",
            inset: 0,
            width: "100%",
            height: "100%",
            touchAction: "none",
            opacity: clearing ? 0 : 1,
            transition: clearing ? "opacity 0.4s ease-out" : void 0
          }
        }
      )
    ] });
  };
  const CARD_ASPECT = 4 / 3;
  const MemoryGameSurface = ({ ctx }) => {
    const stage = ctx.stage;
    const faces = stage.faces || [];
    const hostRef = React2.useRef(null);
    const [host, setHost] = React2.useState({ w: 0, h: 0 });
    const [flipped, setFlipped] = React2.useState([]);
    const [matched, setMatched] = React2.useState(/* @__PURE__ */ new Set());
    const lockRef = React2.useRef(false);
    const timerRef = React2.useRef(null);
    const wonRef = React2.useRef(false);
    const deck = React2.useMemo(() => {
      const cards = faces.flatMap((f, fi) => [{ faceId: f.id, faceIndex: fi }, { faceId: f.id, faceIndex: fi }]);
      for (let i = cards.length - 1; i > 0; i--) {
        const j = Math.floor(Math.random() * (i + 1));
        [cards[i], cards[j]] = [cards[j], cards[i]];
      }
      return cards;
    }, [stage.id]);
    React2.useEffect(() => {
      const el = hostRef.current;
      if (!el) return;
      const obs = new ResizeObserver(() => setHost({ w: el.clientWidth, h: el.clientHeight }));
      obs.observe(el);
      setHost({ w: el.clientWidth, h: el.clientHeight });
      return () => obs.disconnect();
    }, []);
    React2.useEffect(() => () => {
      if (timerRef.current != null) clearTimeout(timerRef.current);
    }, []);
    const backUrl = stage.cardBackImageId ? ctx.assetResolver(stage.cardBackImageId, "image") : null;
    if (!faces.length) {
      return /* @__PURE__ */ jsxRuntime2.jsxs("button", { onClick: () => ctx.reportWin(), style: { position: "absolute", inset: 0, width: "100%", height: "100%", background: "transparent", border: "none", cursor: "pointer", display: "flex", flexDirection: "column", alignItems: "center", justifyContent: "center", gap: 8, color: "rgba(255,255,255,0.85)", fontSize: 15 }, children: [
        /* @__PURE__ */ jsxRuntime2.jsx("span", { style: { fontSize: 32 }, children: "🃏" }),
        /* @__PURE__ */ jsxRuntime2.jsx("span", { children: "No card faces yet — add them in the Mini Games tab." }),
        /* @__PURE__ */ jsxRuntime2.jsx("span", { style: { fontSize: 12, opacity: 0.7 }, children: "Tap to continue" })
      ] });
    }
    const count = deck.length;
    const cols = Math.min(count, Math.max(2, stage.cols || Math.ceil(Math.sqrt(count))));
    const rows = Math.ceil(count / cols);
    const gap = 8;
    const cardW = host.w && host.h ? Math.max(24, Math.min((host.w - (cols - 1) * gap) / cols, (host.h - (rows - 1) * gap) / rows / CARD_ASPECT)) : 0;
    const cardH = cardW * CARD_ASPECT;
    const tap = (idx) => {
      var _a;
      if (lockRef.current || wonRef.current || matched.has(idx) || flipped.includes(idx)) return;
      ctx.playInteractSound();
      const next = [...flipped, idx];
      setFlipped(next);
      if (next.length < 2) return;
      const [a, b] = next;
      if (deck[a].faceId === deck[b].faceId) {
        ctx.playSound(stage.matchSfxId ?? null);
        ctx.reportHit();
        const m = new Set(matched);
        m.add(a);
        m.add(b);
        setMatched(m);
        setFlipped([]);
        (_a = ctx.reportProgress) == null ? void 0 : _a.call(ctx, m.size / count);
        if (m.size === count && !wonRef.current) {
          wonRef.current = true;
          timerRef.current = window.setTimeout(() => ctx.reportWin(), 450);
        }
      } else {
        ctx.reportMistake();
        lockRef.current = true;
        timerRef.current = window.setTimeout(() => {
          setFlipped([]);
          lockRef.current = false;
        }, Math.max(200, stage.flipBackDelayMs ?? 900));
      }
    };
    return /* @__PURE__ */ jsxRuntime2.jsx("div", { ref: hostRef, style: { position: "absolute", inset: 0, display: "flex", alignItems: "center", justifyContent: "center" }, children: /* @__PURE__ */ jsxRuntime2.jsx("div", { style: { display: "grid", gridTemplateColumns: \`repeat(\${cols}, \${cardW}px)\`, gridAutoRows: \`\${cardH}px\`, gap }, children: deck.map((card, idx) => {
      const up = matched.has(idx) || flipped.includes(idx);
      const face = faces[card.faceIndex];
      const faceUrl = (face == null ? void 0 : face.imageId) ? ctx.assetResolver(face.imageId, "image") : null;
      return /* @__PURE__ */ jsxRuntime2.jsx(
        "button",
        {
          onClick: () => tap(idx),
          style: { position: "relative", width: cardW, height: cardH, padding: 0, border: "none", background: "transparent", cursor: up ? "default" : "pointer", perspective: "600px", touchAction: "manipulation" },
          children: /* @__PURE__ */ jsxRuntime2.jsxs("div", { style: { position: "absolute", inset: 0, transformStyle: "preserve-3d", transition: "transform 0.35s ease", transform: up ? "rotateY(180deg)" : "rotateY(0deg)" }, children: [
            /* @__PURE__ */ jsxRuntime2.jsx("div", { style: { position: "absolute", inset: 0, backfaceVisibility: "hidden", WebkitBackfaceVisibility: "hidden", borderRadius: Math.max(4, cardW * 0.08), overflow: "hidden", border: "1px solid rgba(255,255,255,0.25)", background: backUrl ? void 0 : "linear-gradient(135deg,#334155,#1e293b)", display: "flex", alignItems: "center", justifyContent: "center" }, children: backUrl ? /* @__PURE__ */ jsxRuntime2.jsx("img", { src: backUrl, alt: "", draggable: false, style: { width: "100%", height: "100%", objectFit: "cover", pointerEvents: "none" } }) : /* @__PURE__ */ jsxRuntime2.jsx("span", { style: { fontSize: cardW * 0.4, opacity: 0.5 }, children: "✦" }) }),
            /* @__PURE__ */ jsxRuntime2.jsx("div", { style: { position: "absolute", inset: 0, backfaceVisibility: "hidden", WebkitBackfaceVisibility: "hidden", transform: "rotateY(180deg)", borderRadius: Math.max(4, cardW * 0.08), overflow: "hidden", border: matched.has(idx) ? "2px solid rgba(52,211,153,0.9)" : "1px solid rgba(255,255,255,0.35)", background: "#0f172a", display: "flex", alignItems: "center", justifyContent: "center" }, children: faceUrl ? /* @__PURE__ */ jsxRuntime2.jsx("img", { src: faceUrl, alt: "", draggable: false, style: { width: "100%", height: "100%", objectFit: "cover", pointerEvents: "none" } }) : /* @__PURE__ */ jsxRuntime2.jsx("span", { style: { fontSize: cardW * 0.35, color: "#94a3b8" }, children: card.faceIndex + 1 }) })
          ] })
        },
        idx
      );
    }) }) });
  };
  function useImageBox(hostRef, url) {
    const [hostSize, setHostSize] = React2.useState({ w: 0, h: 0 });
    const [natural, setNatural] = React2.useState(null);
    React2.useEffect(() => {
      const el = hostRef.current;
      if (!el) return;
      const obs = new ResizeObserver(() => setHostSize({ w: el.clientWidth, h: el.clientHeight }));
      obs.observe(el);
      setHostSize({ w: el.clientWidth, h: el.clientHeight });
      return () => obs.disconnect();
    }, [hostRef.current]);
    React2.useEffect(() => {
      setNatural(null);
      if (!url) return;
      let live = true;
      const img = new Image();
      img.onload = () => {
        if (live) setNatural({ w: img.naturalWidth || 16, h: img.naturalHeight || 9 });
      };
      img.src = url;
      return () => {
        live = false;
      };
    }, [url]);
    return React2.useMemo(() => {
      if (!hostSize.w || !hostSize.h) return { left: 0, top: 0, width: 0, height: 0 };
      if (!natural) return { left: 0, top: 0, width: hostSize.w, height: hostSize.h };
      const scale = Math.min(hostSize.w / natural.w, hostSize.h / natural.h);
      const w = natural.w * scale, h = natural.h * scale;
      return { left: (hostSize.w - w) / 2, top: (hostSize.h - h) / 2, width: w, height: h };
    }, [hostSize, natural]);
  }
  const HiddenObjectGameSurface = ({ ctx }) => {
    const stage = ctx.stage;
    const hotspots = stage.hotspots || [];
    const url = stage.sceneImageId ? ctx.assetResolver(stage.sceneImageId, "image") : null;
    const hostRef = React2.useRef(null);
    const box = useImageBox(hostRef, url);
    const [found, setFound] = React2.useState(/* @__PURE__ */ new Set());
    const [miss, setMiss] = React2.useState(null);
    const [hintId, setHintId] = React2.useState(null);
    const [hintReadyAt, setHintReadyAt] = React2.useState(0);
    const [nowTick, setNowTick] = React2.useState(0);
    const timersRef = React2.useRef([]);
    const wonRef = React2.useRef(false);
    React2.useEffect(() => () => {
      timersRef.current.forEach((id) => clearTimeout(id));
    }, []);
    const pushTimer = (fn, ms) => {
      const id = window.setTimeout(fn, ms);
      timersRef.current.push(id);
    };
    if (!url || !hotspots.length) {
      return /* @__PURE__ */ jsxRuntime2.jsxs("button", { onClick: () => ctx.reportWin(), style: { position: "absolute", inset: 0, width: "100%", height: "100%", background: "transparent", border: "none", cursor: "pointer", display: "flex", flexDirection: "column", alignItems: "center", justifyContent: "center", gap: 8, color: "rgba(255,255,255,0.85)", fontSize: 15 }, children: [
        /* @__PURE__ */ jsxRuntime2.jsx("span", { style: { fontSize: 32 }, children: "🔍" }),
        /* @__PURE__ */ jsxRuntime2.jsx("span", { children: !url ? "No scene image yet — pick one in the Mini Games tab." : "No objects placed yet — place them in the Mini Games tab." }),
        /* @__PURE__ */ jsxRuntime2.jsx("span", { style: { fontSize: 12, opacity: 0.7 }, children: "Tap to continue" })
      ] });
    }
    const hit = (h, px, py) => {
      if ((h.shape || "circle") === "rect") {
        return px >= h.x && px <= h.x + h.w && py >= h.y && py <= h.y + h.h;
      }
      const cx = h.x + h.w / 2, cy = h.y + h.h / 2;
      const dx = (px - cx) / (h.w / 2), dy = (py - cy) / (h.h / 2);
      return dx * dx + dy * dy <= 1;
    };
    const tap = (e) => {
      var _a;
      if (wonRef.current || !box.width || !box.height) return;
      const rect = hostRef.current.getBoundingClientRect();
      const ix = e.clientX - rect.left - box.left;
      const iy = e.clientY - rect.top - box.top;
      if (ix < 0 || iy < 0 || ix > box.width || iy > box.height) return;
      const px = ix / box.width * 100, py = iy / box.height * 100;
      const target = hotspots.find((h) => !found.has(h.id) && hit(h, px, py));
      if (target) {
        ctx.playInteractSound();
        ctx.reportHit();
        const next = new Set(found);
        next.add(target.id);
        setFound(next);
        setHintId((cur) => cur === target.id ? null : cur);
        (_a = ctx.reportProgress) == null ? void 0 : _a.call(ctx, next.size / hotspots.length);
        if (next.size === hotspots.length && !wonRef.current) {
          wonRef.current = true;
          pushTimer(() => ctx.reportWin(), 450);
        }
      } else {
        ctx.reportMistake();
        setMiss({ x: px, y: py, key: Date.now() });
        pushTimer(() => setMiss((m) => m && Date.now() - m.key >= 550 ? null : m), 600);
      }
    };
    const hintCooldownMs = Math.max(1, stage.hintCooldownSec ?? 10) * 1e3;
    const hintRemaining = Math.max(0, hintReadyAt - nowTick);
    const useHint = () => {
      const unfound = hotspots.filter((h) => !found.has(h.id));
      if (!unfound.length || hintRemaining > 0) return;
      ctx.playInteractSound();
      const pick = unfound[Math.floor(Math.random() * unfound.length)];
      setHintId(pick.id);
      setHintReadyAt(Date.now() + hintCooldownMs);
      pushTimer(() => setHintId((cur) => cur === pick.id ? null : cur), 1800);
    };
    React2.useEffect(() => {
      if (!stage.hintButton || hintReadyAt <= Date.now()) return;
      const id = window.setInterval(() => setNowTick(Date.now()), 500);
      setNowTick(Date.now());
      return () => clearInterval(id);
    }, [hintReadyAt, stage.hintButton]);
    const pctRect = (h) => ({
      position: "absolute",
      left: \`\${h.x}%\`,
      top: \`\${h.y}%\`,
      width: \`\${h.w}%\`,
      height: \`\${h.h}%\`
    });
    return /* @__PURE__ */ jsxRuntime2.jsxs("div", { ref: hostRef, style: { position: "absolute", inset: 0, touchAction: "manipulation" }, onPointerDown: tap, children: [
      /* @__PURE__ */ jsxRuntime2.jsx("style", { children: "@keyframes mgMissRipple{0%{transform:scale(0.4);opacity:0.9}100%{transform:scale(1.6);opacity:0}}@keyframes mgHintPulse{0%,100%{transform:scale(1);opacity:0.9}50%{transform:scale(1.25);opacity:0.4}}@keyframes mgFoundPop{0%{transform:scale(0.5);opacity:0}70%{transform:scale(1.15);opacity:1}100%{transform:scale(1)}}" }),
      /* @__PURE__ */ jsxRuntime2.jsx(
        "img",
        {
          src: url,
          alt: "",
          draggable: false,
          style: { position: "absolute", left: box.left, top: box.top, width: box.width, height: box.height, pointerEvents: "none" }
        }
      ),
      /* @__PURE__ */ jsxRuntime2.jsxs("div", { style: { position: "absolute", left: box.left, top: box.top, width: box.width, height: box.height, pointerEvents: "none" }, children: [
        stage.showFoundMarkers !== false && hotspots.filter((h) => found.has(h.id)).map((h) => {
          const foundUrl = h.foundImageId ? ctx.assetResolver(h.foundImageId, "image") : null;
          return /* @__PURE__ */ jsxRuntime2.jsx("div", { style: { ...pctRect(h), animation: "mgFoundPop 0.3s ease-out" }, children: foundUrl ? /* @__PURE__ */ jsxRuntime2.jsx("img", { src: foundUrl, alt: "", draggable: false, style: { width: "100%", height: "100%", objectFit: "contain" } }) : /* @__PURE__ */ jsxRuntime2.jsx("div", { style: { width: "100%", height: "100%", border: "3px solid rgba(52,211,153,0.95)", borderRadius: (h.shape || "circle") === "circle" ? "50%" : 8, display: "flex", alignItems: "center", justifyContent: "center", color: "#34d399", fontWeight: 700, textShadow: "0 1px 4px rgba(0,0,0,0.8)", fontSize: "min(4vmin, 22px)" }, children: "✓" }) }, h.id);
        }),
        hintId && (() => {
          const h = hotspots.find((x) => x.id === hintId);
          if (!h) return null;
          return /* @__PURE__ */ jsxRuntime2.jsx("div", { style: { ...pctRect(h), border: "3px solid rgba(250,204,21,0.95)", borderRadius: (h.shape || "circle") === "circle" ? "50%" : 8, animation: "mgHintPulse 0.6s ease-in-out infinite" } });
        })(),
        miss && /* @__PURE__ */ jsxRuntime2.jsx("div", { style: { position: "absolute", left: \`\${miss.x}%\`, top: \`\${miss.y}%\`, width: "6%", aspectRatio: "1", transform: "translate(-50%, -50%)", border: "3px solid rgba(248,113,113,0.9)", borderRadius: "50%", animation: "mgMissRipple 0.55s ease-out forwards" } }, miss.key)
      ] }),
      stage.showCounter !== false && /* @__PURE__ */ jsxRuntime2.jsxs("div", { style: { position: "absolute", top: 8, right: 10, padding: "3px 12px", borderRadius: 999, background: "rgba(0,0,0,0.6)", color: "#fff", fontSize: 14, fontWeight: 600, pointerEvents: "none" }, children: [
        found.size,
        " / ",
        hotspots.length
      ] }),
      !!stage.hintButton && found.size < hotspots.length && /* @__PURE__ */ jsxRuntime2.jsx(
        "button",
        {
          onClick: (e) => {
            e.stopPropagation();
            useHint();
          },
          onPointerDown: (e) => e.stopPropagation(),
          disabled: hintRemaining > 0,
          style: { position: "absolute", left: 10, bottom: 10, padding: "7px 16px", borderRadius: 999, border: "1px solid rgba(255,255,255,0.4)", background: "rgba(0,0,0,0.55)", color: "#fff", fontSize: 13, cursor: hintRemaining > 0 ? "default" : "pointer", opacity: hintRemaining > 0 ? 0.55 : 1 },
          children: hintRemaining > 0 ? \`\${stage.hintLabel || "💡 Hint"} (\${Math.ceil(hintRemaining / 1e3)})\` : stage.hintLabel || "💡 Hint"
        }
      )
    ] });
  };
  function sliceCellStyle(url, cols, rows, col, row) {
    return {
      backgroundImage: \`url("\${url}")\`,
      backgroundSize: \`\${cols * 100}% \${rows * 100}%\`,
      backgroundPosition: \`\${cols <= 1 ? 0 : col / (cols - 1) * 100}% \${rows <= 1 ? 0 : row / (rows - 1) * 100}%\`,
      backgroundRepeat: "no-repeat"
    };
  }
  const SlidingGameSurface = ({ ctx }) => {
    const stage = ctx.stage;
    const url = stage.imageId ? ctx.assetResolver(stage.imageId, "image") : null;
    const n = Math.max(3, Math.min(5, stage.gridSize || 3));
    const cellCount = n * n;
    const blankTile = cellCount - 1;
    const hostRef = React2.useRef(null);
    const box = useImageBox(hostRef, url);
    const [won, setWon] = React2.useState(false);
    const wonRef = React2.useRef(false);
    const timerRef = React2.useRef(null);
    const initialPos = React2.useMemo(() => {
      const pos2 = Array.from({ length: cellCount }, (_, i) => i);
      const cellOf = (tile) => pos2[tile];
      const tileAt = (cell) => pos2.indexOf(cell);
      let prevBlankCell = -1;
      const moves = Math.max(10, stage.shuffleMoves ?? 80);
      for (let m = 0; m < moves; m++) {
        const b = cellOf(blankTile);
        const r = Math.floor(b / n), c = b % n;
        const neighbors = [
          r > 0 ? b - n : -1,
          r < n - 1 ? b + n : -1,
          c > 0 ? b - 1 : -1,
          c < n - 1 ? b + 1 : -1
        ].filter((x) => x >= 0 && x !== prevBlankCell);
        const pick = neighbors[Math.floor(Math.random() * neighbors.length)];
        const t = tileAt(pick);
        pos2[t] = b;
        pos2[blankTile] = pick;
        prevBlankCell = b;
      }
      if (pos2.every((cell, tile) => cell === tile)) {
        const b = pos2[blankTile];
        const swap = b % n > 0 ? b - 1 : b + 1;
        const t = pos2.indexOf(swap);
        pos2[t] = b;
        pos2[blankTile] = swap;
      }
      return pos2;
    }, [stage.id, n]);
    const [pos, setPos] = React2.useState(initialPos);
    React2.useEffect(() => () => {
      if (timerRef.current != null) clearTimeout(timerRef.current);
    }, []);
    if (!url) {
      return /* @__PURE__ */ jsxRuntime2.jsxs("button", { onClick: () => ctx.reportWin(), style: { position: "absolute", inset: 0, width: "100%", height: "100%", background: "transparent", border: "none", cursor: "pointer", display: "flex", flexDirection: "column", alignItems: "center", justifyContent: "center", gap: 8, color: "rgba(255,255,255,0.85)", fontSize: 15 }, children: [
        /* @__PURE__ */ jsxRuntime2.jsx("span", { style: { fontSize: 32 }, children: "🔢" }),
        /* @__PURE__ */ jsxRuntime2.jsx("span", { children: "No puzzle image yet — pick one in the Mini Games tab." }),
        /* @__PURE__ */ jsxRuntime2.jsx("span", { style: { fontSize: 12, opacity: 0.7 }, children: "Tap to continue" })
      ] });
    }
    const tap = (tile) => {
      var _a;
      if (wonRef.current || won) return;
      const cell = pos[tile], blank = pos[blankTile];
      const sameRow = Math.floor(cell / n) === Math.floor(blank / n) && Math.abs(cell - blank) === 1;
      const sameCol = cell % n === blank % n && Math.abs(cell - blank) === n;
      if (!sameRow && !sameCol) return;
      ctx.playInteractSound();
      const next = [...pos];
      next[tile] = blank;
      next[blankTile] = cell;
      setPos(next);
      const correct = next.reduce((acc, c, t) => acc + (c === t ? 1 : 0), 0);
      (_a = ctx.reportProgress) == null ? void 0 : _a.call(ctx, correct / cellCount);
      if (next.every((c, t) => c === t)) {
        wonRef.current = true;
        setWon(true);
        timerRef.current = window.setTimeout(() => ctx.reportWin(), 650);
      }
    };
    const cellPct = 100 / n;
    const gapPx = 2;
    return /* @__PURE__ */ jsxRuntime2.jsxs("div", { ref: hostRef, style: { position: "absolute", inset: 0 }, children: [
      /* @__PURE__ */ jsxRuntime2.jsx("div", { style: { position: "absolute", left: box.left, top: box.top, width: box.width, height: box.height, background: "rgba(0,0,0,0.45)", borderRadius: 6, overflow: "hidden" }, children: Array.from({ length: cellCount }, (_, tile) => {
        if (tile === blankTile && !won) return null;
        const cell = pos[tile];
        const col = tile % n, row = Math.floor(tile / n);
        return /* @__PURE__ */ jsxRuntime2.jsx(
          "button",
          {
            onClick: () => tap(tile),
            style: {
              position: "absolute",
              left: \`\${cell % n * cellPct}%\`,
              top: \`\${Math.floor(cell / n) * cellPct}%\`,
              width: \`\${cellPct}%\`,
              height: \`\${cellPct}%\`,
              padding: 0,
              border: "none",
              cursor: won ? "default" : "pointer",
              transition: "left 0.16s ease, top 0.16s ease, opacity 0.4s ease",
              boxSizing: "border-box",
              boxShadow: won ? "none" : \`inset 0 0 0 \${gapPx}px rgba(0,0,0,0.55)\`,
              touchAction: "manipulation",
              ...tile === blankTile ? { opacity: won ? 1 : 0, animation: "fade-in 0.5s ease-out" } : {},
              ...sliceCellStyle(url, n, n, col, row)
            },
            children: stage.showNumbers && !won && tile !== blankTile && /* @__PURE__ */ jsxRuntime2.jsx("span", { style: { position: "absolute", left: 4, top: 2, fontSize: "max(11px, 1.6vmin)", fontWeight: 700, color: "#fff", textShadow: "0 1px 3px rgba(0,0,0,0.9)" }, children: tile + 1 })
          },
          tile
        );
      }) }),
      stage.showReference !== false && !won && /* @__PURE__ */ jsxRuntime2.jsx(
        "img",
        {
          src: url,
          alt: "",
          draggable: false,
          style: { position: "absolute", top: 6, left: 6, width: "16%", maxHeight: "22%", objectFit: "contain", borderRadius: 4, border: "1px solid rgba(255,255,255,0.5)", boxShadow: "0 2px 8px rgba(0,0,0,0.6)", pointerEvents: "none", opacity: 0.9 }
        }
      )
    ] });
  };
  const DEFAULT_COLORS = ["#ef4444", "#f97316", "#facc15", "#22c55e", "#3b82f6", "#8b5cf6", "#ec4899", "#8d5524", "#ffffff", "#1e293b"];
  function resolvePalettes(inline, paletteIds, project) {
    const out = [];
    (inline || []).forEach((p) => {
      var _a;
      if ((_a = p.colors) == null ? void 0 : _a.length) out.push(p);
    });
    const shared = project.artPalettes;
    (paletteIds || []).forEach((id) => {
      var _a;
      const p = (shared || []).find((x) => x.id === id);
      if (p && ((_a = p.colors) == null ? void 0 : _a.length) && !out.some((x) => x.id === p.id)) out.push(p);
    });
    if (!out.length) out.push({ id: "default", name: "Colors", colors: DEFAULT_COLORS });
    return out;
  }
  const SwatchBar = ({ palettes, activeColor, onPick, trailing }) => {
    const [paletteIdx, setPaletteIdx] = React2.useState(0);
    const palette = palettes[Math.min(paletteIdx, palettes.length - 1)] || palettes[0];
    return /* @__PURE__ */ jsxRuntime2.jsxs("div", { style: { flexShrink: 0, display: "flex", flexDirection: "column", gap: 4, padding: "6px 8px", margin: 4, borderRadius: 10, background: "rgba(0,0,0,0.55)" }, children: [
      palettes.length > 1 && /* @__PURE__ */ jsxRuntime2.jsx("div", { style: { display: "flex", gap: 6, flexWrap: "wrap" }, children: palettes.map((p, i) => /* @__PURE__ */ jsxRuntime2.jsx(
        "button",
        {
          onClick: () => setPaletteIdx(i),
          style: {
            padding: "2px 10px",
            borderRadius: 999,
            fontSize: 11,
            cursor: "pointer",
            border: \`1px solid \${i === paletteIdx ? "rgba(255,255,255,0.85)" : "rgba(255,255,255,0.25)"}\`,
            background: i === paletteIdx ? "rgba(255,255,255,0.18)" : "transparent",
            color: i === paletteIdx ? "#fff" : "rgba(255,255,255,0.65)"
          },
          children: p.name || \`Palette \${i + 1}\`
        },
        p.id
      )) }),
      /* @__PURE__ */ jsxRuntime2.jsxs("div", { style: { display: "flex", alignItems: "center", gap: 7, overflowX: "auto", paddingBottom: 2 }, children: [
        ((palette == null ? void 0 : palette.colors) || DEFAULT_COLORS).map((c, i) => {
          const active = c.toLowerCase() === activeColor.toLowerCase();
          return /* @__PURE__ */ jsxRuntime2.jsx(
            "button",
            {
              onClick: () => onPick(c),
              "aria-label": c,
              style: {
                flexShrink: 0,
                width: 30,
                height: 30,
                borderRadius: "50%",
                cursor: "pointer",
                background: c,
                border: active ? "3px solid #fff" : "2px solid rgba(255,255,255,0.35)",
                boxShadow: active ? "0 0 0 2px rgba(0,0,0,0.5), 0 0 10px rgba(255,255,255,0.4)" : "0 1px 3px rgba(0,0,0,0.5)",
                transform: active ? "scale(1.12)" : void 0,
                transition: "transform 0.12s, border 0.12s"
              }
            },
            \`\${c}-\${i}\`
          );
        }),
        trailing
      ] })
    ] });
  };
  const maskStyles = (clip) => {
    if (clip.maskUrl) {
      const size = clip.maskFit === "contain" ? "contain" : "100% 100%";
      return {
        WebkitMaskImage: \`url("\${clip.maskUrl}")\`,
        maskImage: \`url("\${clip.maskUrl}")\`,
        WebkitMaskSize: size,
        maskSize: size,
        WebkitMaskRepeat: "no-repeat",
        maskRepeat: "no-repeat",
        WebkitMaskPosition: "center",
        maskPosition: "center"
      };
    }
    if (clip.shapeClip) return { clipPath: clip.shapeClip, WebkitClipPath: clip.shapeClip };
    return {};
  };
  const ColorableRegion = ({ clip, fillColor, tintMode, pulse, strokeApiRef }) => {
    const canvasRef = React2.useRef(null);
    const hostRef = React2.useRef(null);
    const blend = (tintMode || "multiply") === "multiply" ? "multiply" : "normal";
    const clipCss = React2.useMemo(() => maskStyles(clip), [clip.maskUrl, clip.maskFit, clip.shapeClip]);
    React2.useEffect(() => {
      if (!strokeApiRef) return;
      const host = hostRef.current, canvas = canvasRef.current;
      if (!host || !canvas) return;
      const rect = host.getBoundingClientRect();
      const w = Math.max(2, Math.min(768, Math.round(rect.width || 512)));
      const h = Math.max(2, Math.round(w * ((rect.height || 288) / Math.max(1, rect.width || 512))));
      canvas.width = w;
      canvas.height = h;
      const g = canvas.getContext("2d");
      const api2 = {
        stamp: (xPct, yPct, color, sizePct) => {
          if (!g) return;
          const r = Math.max(2, sizePct / 100 * w) / 2;
          const x = xPct / 100 * w, y = yPct / 100 * h;
          const grad = g.createRadialGradient(x, y, r * 0.25, x, y, r);
          grad.addColorStop(0, color);
          grad.addColorStop(1, \`\${color}00\`);
          g.fillStyle = grad;
          g.beginPath();
          g.arc(x, y, r, 0, Math.PI * 2);
          g.fill();
        },
        clear: () => {
          g == null ? void 0 : g.clearRect(0, 0, canvas.width, canvas.height);
        }
      };
      strokeApiRef(api2);
      return () => strokeApiRef(null);
    }, [!!strokeApiRef]);
    return /* @__PURE__ */ jsxRuntime2.jsxs("div", { ref: hostRef, style: { position: "absolute", inset: 0, pointerEvents: "none" }, children: [
      fillColor && /* @__PURE__ */ jsxRuntime2.jsx("div", { style: { position: "absolute", inset: 0, background: fillColor, mixBlendMode: blend, ...clipCss, transition: "background 0.2s" } }),
      strokeApiRef && /* @__PURE__ */ jsxRuntime2.jsx("canvas", { ref: canvasRef, style: { position: "absolute", inset: 0, width: "100%", height: "100%", mixBlendMode: blend, ...clipCss } }),
      pulse && /* @__PURE__ */ jsxRuntime2.jsx("div", { style: { position: "absolute", inset: 0, background: "#ffffff", ...clipCss, animation: "mgRegionPulse 1.6s ease-in-out infinite", pointerEvents: "none" } })
    ] });
  };
  const RegionPulseStyle = () => /* @__PURE__ */ jsxRuntime2.jsx("style", { children: "@keyframes mgRegionPulse{0%,100%{opacity:0.04}50%{opacity:0.16}}" });
  function useMaskHitTesters(urls) {
    const testersRef = React2.useRef({});
    React2.useEffect(() => {
      let alive = true;
      urls.forEach((url) => {
        if (!url || testersRef.current[url]) return;
        const img = new Image();
        img.onload = () => {
          if (!alive) return;
          try {
            const w = Math.max(8, Math.min(160, img.naturalWidth || 160));
            const h = Math.max(8, Math.round(w * ((img.naturalHeight || 90) / Math.max(1, img.naturalWidth || 160))));
            const c = document.createElement("canvas");
            c.width = w;
            c.height = h;
            const g = c.getContext("2d", { willReadFrequently: true });
            if (!g) throw new Error("no ctx");
            g.drawImage(img, 0, 0, w, h);
            const data = g.getImageData(0, 0, w, h).data;
            testersRef.current[url] = (xPct, yPct) => {
              const x = Math.max(0, Math.min(w - 1, Math.round(xPct / 100 * w)));
              const y = Math.max(0, Math.min(h - 1, Math.round(yPct / 100 * h)));
              return data[(y * w + x) * 4 + 3] > 25;
            };
          } catch {
            testersRef.current[url] = () => true;
          }
        };
        img.onerror = () => {
          if (alive) testersRef.current[url] = () => true;
        };
        img.src = url;
      });
      return () => {
        alive = false;
      };
    }, [urls.filter(Boolean).join("|")]);
    return testersRef;
  }
  const TAP_SLOP_PX\$1 = 7;
  const AssembleGameSurface = ({ ctx }) => {
    var _a;
    const stage = ctx.stage;
    const mode = stage.sourceMode || (stage.sliceImageId ? "slice" : "pieces");
    const sliceUrl = stage.sliceImageId ? ctx.assetResolver(stage.sliceImageId, "image") : null;
    const baseUrl = stage.baseImageId ? ctx.assetResolver(stage.baseImageId, "image") : null;
    const trayRight = stage.trayPosition === "right";
    const tolerance = Math.max(2, stage.snapTolerancePct ?? 8);
    const boardRef = React2.useRef(null);
    const surfaceRef = React2.useRef(null);
    const box = useImageBox(boardRef, mode === "slice" ? sliceUrl : baseUrl);
    const pieces = React2.useMemo(() => {
      if (mode === "slice" && sliceUrl) {
        const cols = Math.max(2, Math.min(8, stage.sliceCols ?? 3));
        const rows = Math.max(2, Math.min(8, stage.sliceRows ?? 3));
        const list = [];
        for (let r = 0; r < rows; r++) for (let c = 0; c < cols; c++) {
          list.push({
            key: \`cell-\${c}-\${r}\`,
            target: { x: c * 100 / cols, y: r * 100 / rows, w: 100 / cols, h: 100 / rows },
            sliceStyle: sliceCellStyle(sliceUrl, cols, rows, c, r),
            showGhost: false
            // slice mode's guide is the faint full image
          });
        }
        return list;
      }
      return (stage.pieces || []).map((p) => ({
        key: p.id,
        target: p.target,
        imgUrl: p.imageId ? ctx.assetResolver(p.imageId, "image") : null,
        showGhost: p.showGhost !== false,
        colorSlot: p.colorSlot
      }));
    }, [stage.id, sliceUrl, mode]);
    const trayOrder = React2.useMemo(() => {
      const order = pieces.map((p) => p.key);
      for (let i = order.length - 1; i > 0; i--) {
        const j = Math.floor(Math.random() * (i + 1));
        [order[i], order[j]] = [order[j], order[i]];
      }
      return order;
    }, [pieces]);
    const [placed, setPlaced] = React2.useState(/* @__PURE__ */ new Set());
    const [drag, setDrag] = React2.useState(null);
    const wonRef = React2.useRef(false);
    const timerRef = React2.useRef(null);
    React2.useEffect(() => () => {
      if (timerRef.current != null) clearTimeout(timerRef.current);
    }, []);
    const coloring = stage.coloring;
    const coloringOn = !!(coloring == null ? void 0 : coloring.enabled);
    const needAllColored = coloringOn && coloring.requireAllColored !== false;
    const allowBrush = coloringOn && !!coloring.allowBrush;
    const brushSizePct = Math.max(2, Math.min(20, (coloring == null ? void 0 : coloring.brushSizePct) ?? 7));
    const colorPalettes = React2.useMemo(
      () => coloringOn ? resolvePalettes(coloring.palettes, coloring.paletteIds, ctx.project) : [],
      // eslint-disable-next-line react-hooks/exhaustive-deps
      [stage.id, coloringOn, ctx.project]
    );
    const [activeColor, setActiveColor] = React2.useState(((_a = colorPalettes[0]) == null ? void 0 : _a.colors[0]) || "#ef4444");
    const [fills, setFills] = React2.useState({});
    const coloredRef = React2.useRef(/* @__PURE__ */ new Set());
    const strokeApisRef = React2.useRef({});
    const colorDragRef = React2.useRef(null);
    const allPlaced = placed.size >= pieces.length && pieces.length > 0;
    const canColorNow = coloringOn && (coloring.freeOrder ? true : allPlaced);
    const reportCombinedProgress = (placedCount, coloredCount) => {
      var _a2;
      (_a2 = ctx.reportProgress) == null ? void 0 : _a2.call(ctx, coloringOn && needAllColored ? (placedCount + coloredCount) / (pieces.length * 2) : placedCount / pieces.length);
    };
    const maybeWin = (placedCount, coloredCount) => {
      if (wonRef.current || placedCount < pieces.length) return;
      if (needAllColored && coloredCount < pieces.length) return;
      if (coloringOn && !needAllColored) return;
      wonRef.current = true;
      timerRef.current = window.setTimeout(() => ctx.reportWin(), 500);
    };
    const markColored = (key, color) => {
      var _a2;
      coloredRef.current.add(key);
      const piece = pieces.find((p) => p.key === key);
      if (piece == null ? void 0 : piece.colorSlot) (_a2 = ctx.reportColors) == null ? void 0 : _a2.call(ctx, { [piece.colorSlot]: color });
      reportCombinedProgress(placed.size, coloredRef.current.size);
      maybeWin(placed.size, coloredRef.current.size);
    };
    const empty = mode === "slice" ? !sliceUrl : !(stage.pieces || []).length;
    if (empty) {
      return /* @__PURE__ */ jsxRuntime2.jsxs("button", { onClick: () => ctx.reportWin(), style: { position: "absolute", inset: 0, width: "100%", height: "100%", background: "transparent", border: "none", cursor: "pointer", display: "flex", flexDirection: "column", alignItems: "center", justifyContent: "center", gap: 8, color: "rgba(255,255,255,0.85)", fontSize: 15 }, children: [
        /* @__PURE__ */ jsxRuntime2.jsx("span", { style: { fontSize: 32 }, children: "🧩" }),
        /* @__PURE__ */ jsxRuntime2.jsx("span", { children: mode === "slice" ? "No puzzle image yet — pick one in the Mini Games tab." : "No pieces yet — add them in the Mini Games tab." }),
        /* @__PURE__ */ jsxRuntime2.jsx("span", { style: { fontSize: 12, opacity: 0.7 }, children: "Tap to continue" })
      ] });
    }
    const surfacePoint = (e) => {
      const rect = surfaceRef.current.getBoundingClientRect();
      return { x: e.clientX - rect.left, y: e.clientY - rect.top };
    };
    const startDrag = (key) => (e) => {
      var _a2, _b;
      if (wonRef.current || placed.has(key)) return;
      (_b = (_a2 = surfaceRef.current) == null ? void 0 : _a2.setPointerCapture) == null ? void 0 : _b.call(_a2, e.pointerId);
      setDrag({ key, ...surfacePoint(e) });
    };
    const moveDrag = (e) => {
      if (!drag) return;
      const p = surfacePoint(e);
      setDrag((d) => d ? { ...d, x: p.x, y: p.y } : d);
    };
    const endDrag = (e) => {
      if (!drag) return;
      const piece = pieces.find((p) => p.key === drag.key);
      setDrag(null);
      if (!piece || !box.width || !box.height) return;
      const boardRect = boardRef.current.getBoundingClientRect();
      const surfRect = surfaceRef.current.getBoundingClientRect();
      const bx = (drag.x + surfRect.left - boardRect.left - box.left) / box.width * 100;
      const by = (drag.y + surfRect.top - boardRect.top - box.top) / box.height * 100;
      const distTo = (p) => {
        const cx = p.target.x + p.target.w / 2, cy = p.target.y + p.target.h / 2;
        return Math.hypot(bx - cx, by - cy);
      };
      if (distTo(piece) <= tolerance) {
        ctx.playInteractSound();
        ctx.reportHit();
        const next = new Set(placed);
        next.add(piece.key);
        setPlaced(next);
        reportCombinedProgress(next.size, coloredRef.current.size);
        maybeWin(next.size, coloredRef.current.size);
        return;
      }
      const wrong = pieces.find((p) => p.key !== piece.key && !placed.has(p.key) && distTo(p) <= tolerance);
      if (wrong) ctx.reportMistake();
    };
    const boardPct = (e) => {
      const host = boardRef.current;
      if (!host || !box.width || !box.height) return null;
      const rect = host.getBoundingClientRect();
      return { x: (e.clientX - rect.left - box.left) / box.width * 100, y: (e.clientY - rect.top - box.top) / box.height * 100 };
    };
    const hitPlacedPiece = (bx, by) => {
      for (let i = pieces.length - 1; i >= 0; i--) {
        const p = pieces[i];
        if (!placed.has(p.key)) continue;
        const t = p.target;
        if (bx >= t.x && bx <= t.x + t.w && by >= t.y && by <= t.y + t.h) return p.key;
      }
      return null;
    };
    const fillPiece = (key) => {
      var _a2;
      setFills((f) => ({ ...f, [key]: activeColor }));
      (_a2 = strokeApisRef.current[key]) == null ? void 0 : _a2.clear();
      ctx.playInteractSound();
      markColored(key, activeColor);
    };
    const colorPointerDown = (e) => {
      var _a2, _b;
      if (!canColorNow || wonRef.current || drag) return;
      const p = boardPct(e);
      if (!p) return;
      const key = hitPlacedPiece(p.x, p.y);
      if (!key) return;
      (_b = (_a2 = surfaceRef.current) == null ? void 0 : _a2.setPointerCapture) == null ? void 0 : _b.call(_a2, e.pointerId);
      colorDragRef.current = { key, startX: e.clientX, startY: e.clientY, moved: false };
    };
    const colorPointerMove = (e) => {
      var _a2, _b;
      const d = colorDragRef.current;
      if (!d || !allowBrush) return;
      if (!d.moved && Math.hypot(e.clientX - d.startX, e.clientY - d.startY) < TAP_SLOP_PX\$1) return;
      d.moved = true;
      const p = boardPct(e);
      if (!p) return;
      const t = (_a2 = pieces.find((x) => x.key === d.key)) == null ? void 0 : _a2.target;
      if (!t) return;
      (_b = strokeApisRef.current[d.key]) == null ? void 0 : _b.stamp((p.x - t.x) / t.w * 100, (p.y - t.y) / t.h * 100, activeColor, brushSizePct * (100 / Math.max(1, t.w)));
    };
    const colorPointerUp = () => {
      const d = colorDragRef.current;
      colorDragRef.current = null;
      if (!d || wonRef.current) return;
      if (d.moved) {
        ctx.playInteractSound();
        markColored(d.key, activeColor);
      } else fillPiece(d.key);
    };
    const pieceArt = (p, style) => p.sliceStyle ? /* @__PURE__ */ jsxRuntime2.jsx("div", { style: { ...style, ...p.sliceStyle } }) : p.imgUrl ? /* @__PURE__ */ jsxRuntime2.jsx("img", { src: p.imgUrl, alt: "", draggable: false, style: { ...style, objectFit: "contain" } }) : /* @__PURE__ */ jsxRuntime2.jsx("div", { style: { ...style, background: "#334155", borderRadius: 6 } });
    const trayPieces = trayOrder.filter((k) => !placed.has(k) && (drag == null ? void 0 : drag.key) !== k);
    const dragPiece = drag ? pieces.find((p) => p.key === drag.key) : null;
    const dragW = dragPiece && box.width ? dragPiece.target.w / 100 * box.width : 60;
    const dragH = dragPiece && box.height ? dragPiece.target.h / 100 * box.height : 60;
    const showSwatches = coloringOn && canColorNow;
    return /* @__PURE__ */ jsxRuntime2.jsxs(
      "div",
      {
        ref: surfaceRef,
        onPointerMove: (e) => {
          moveDrag(e);
          colorPointerMove(e);
        },
        onPointerUp: (e) => {
          endDrag();
          colorPointerUp();
        },
        onPointerCancel: (e) => {
          endDrag();
          colorPointerUp();
        },
        style: { position: "absolute", inset: 0, display: "flex", flexDirection: "column", touchAction: "none" },
        children: [
          /* @__PURE__ */ jsxRuntime2.jsx(RegionPulseStyle, {}),
          /* @__PURE__ */ jsxRuntime2.jsxs("div", { style: { position: "relative", flex: 1, minHeight: 0, display: "flex", flexDirection: trayRight ? "row" : "column" }, children: [
            /* @__PURE__ */ jsxRuntime2.jsxs(
              "div",
              {
                ref: boardRef,
                onPointerDown: colorPointerDown,
                style: { position: "relative", flex: 1, minWidth: 0, minHeight: 0, cursor: showSwatches ? "crosshair" : void 0 },
                children: [
                  baseUrl && mode !== "slice" && /* @__PURE__ */ jsxRuntime2.jsx("img", { src: baseUrl, alt: "", draggable: false, style: { position: "absolute", left: box.left, top: box.top, width: box.width, height: box.height, pointerEvents: "none" } }),
                  /* @__PURE__ */ jsxRuntime2.jsxs("div", { style: { position: "absolute", left: box.left, top: box.top, width: box.width, height: box.height }, children: [
                    mode === "slice" && sliceUrl && /* @__PURE__ */ jsxRuntime2.jsx("img", { src: sliceUrl, alt: "", draggable: false, style: { position: "absolute", inset: 0, width: "100%", height: "100%", opacity: 0.15, pointerEvents: "none" } }),
                    pieces.map((p) => {
                      const rect = { position: "absolute", left: \`\${p.target.x}%\`, top: \`\${p.target.y}%\`, width: \`\${p.target.w}%\`, height: \`\${p.target.h}%\` };
                      if (placed.has(p.key)) {
                        return /* @__PURE__ */ jsxRuntime2.jsxs("div", { style: { ...rect, animation: "fade-in 0.2s ease-out" }, children: [
                          pieceArt(p, { width: "100%", height: "100%" }),
                          coloringOn && /* @__PURE__ */ jsxRuntime2.jsx(
                            ColorableRegion,
                            {
                              clip: p.imgUrl ? { maskUrl: p.imgUrl, maskFit: "contain" } : {},
                              fillColor: fills[p.key] || null,
                              tintMode: (coloring == null ? void 0 : coloring.tintMode) || "multiply",
                              pulse: canColorNow && !coloredRef.current.has(p.key),
                              strokeApiRef: allowBrush ? ((api2) => {
                                strokeApisRef.current[p.key] = api2;
                              }) : void 0
                            }
                          )
                        ] }, p.key);
                      }
                      if (p.showGhost && p.imgUrl) {
                        return /* @__PURE__ */ jsxRuntime2.jsx("div", { style: { ...rect, opacity: 0.28, filter: "grayscale(1)", pointerEvents: "none" }, children: /* @__PURE__ */ jsxRuntime2.jsx("img", { src: p.imgUrl, alt: "", draggable: false, style: { width: "100%", height: "100%", objectFit: "contain" } }) }, p.key);
                      }
                      if (mode === "slice") {
                        return /* @__PURE__ */ jsxRuntime2.jsx("div", { style: { ...rect, border: "1px dashed rgba(255,255,255,0.25)", boxSizing: "border-box", pointerEvents: "none" } }, p.key);
                      }
                      return /* @__PURE__ */ jsxRuntime2.jsx("div", { style: { ...rect, border: "2px dashed rgba(255,255,255,0.35)", borderRadius: 8, boxSizing: "border-box", pointerEvents: "none" } }, p.key);
                    })
                  ] })
                ]
              }
            ),
            /* @__PURE__ */ jsxRuntime2.jsxs("div", { style: {
              flexShrink: 0,
              display: "flex",
              gap: 8,
              padding: 8,
              alignItems: "center",
              background: "rgba(0,0,0,0.5)",
              borderRadius: 10,
              margin: 4,
              ...trayRight ? { flexDirection: "column", width: "18%", minWidth: 84, overflowY: "auto" } : { flexDirection: "row", height: "20%", minHeight: 72, overflowX: "auto" }
            }, children: [
              trayPieces.map((k) => {
                const p = pieces.find((x) => x.key === k);
                return /* @__PURE__ */ jsxRuntime2.jsx(
                  "div",
                  {
                    onPointerDown: startDrag(k),
                    style: { flexShrink: 0, cursor: "grab", touchAction: "none", ...trayRight ? { width: "84%" } : { height: "84%" }, aspectRatio: \`\${Math.max(0.2, p.target.w / Math.max(0.1, p.target.h))}\` },
                    children: pieceArt(p, { width: "100%", height: "100%", pointerEvents: "none" })
                  },
                  k
                );
              }),
              trayPieces.length === 0 && !drag && /* @__PURE__ */ jsxRuntime2.jsx("span", { style: { color: "rgba(255,255,255,0.4)", fontSize: 12, margin: "auto" }, children: "—" })
            ] })
          ] }),
          showSwatches && /* @__PURE__ */ jsxRuntime2.jsx(
            SwatchBar,
            {
              palettes: colorPalettes,
              activeColor,
              onPick: (c) => {
                setActiveColor(c);
                ctx.playInteractSound();
              },
              trailing: !needAllColored && allPlaced ? /* @__PURE__ */ jsxRuntime2.jsx(
                "button",
                {
                  onClick: () => {
                    if (!wonRef.current) {
                      wonRef.current = true;
                      ctx.reportWin();
                    }
                  },
                  style: { flexShrink: 0, marginLeft: 6, padding: "6px 16px", borderRadius: 999, border: "1px solid rgba(255,255,255,0.4)", background: "rgba(16,185,129,0.85)", color: "#fff", fontSize: 13, fontWeight: 700, cursor: "pointer" },
                  children: "✓ Done"
                }
              ) : void 0
            }
          ),
          drag && dragPiece && /* @__PURE__ */ jsxRuntime2.jsx("div", { style: { position: "absolute", left: drag.x, top: drag.y, width: dragW, height: dragH, transform: "translate(-50%, -50%)", pointerEvents: "none", zIndex: 20, filter: "drop-shadow(0 4px 10px rgba(0,0,0,0.6))" }, children: pieceArt(dragPiece, { width: "100%", height: "100%" }) })
        ]
      }
    );
  };
  const TAP_SLOP_PX = 7;
  const PaintGameSurface = ({ ctx }) => {
    var _a;
    const stage = ctx.stage;
    const regions = stage.regions || [];
    const baseUrl = stage.baseImageId ? ctx.assetResolver(stage.baseImageId, "image") : null;
    const tintMode = stage.tintMode || "multiply";
    const allowBrush = !!stage.allowBrush;
    const requireAll = stage.requireAllRegions !== false;
    const palettes = React2.useMemo(() => resolvePalettes(stage.palettes, stage.paletteIds, ctx.project), [stage.id, ctx.project]);
    const [activeColor, setActiveColor] = React2.useState(((_a = palettes[0]) == null ? void 0 : _a.colors[0]) || "#ef4444");
    const boardRef = React2.useRef(null);
    const box = useImageBox(boardRef, baseUrl);
    const maskUrl = (r) => r.maskImageId ? ctx.assetResolver(r.maskImageId, "image") : null;
    const maskUrls = React2.useMemo(() => regions.map(maskUrl), [stage.id, ctx.project]);
    const testers = useMaskHitTesters(maskUrls);
    const [fills, setFills] = React2.useState({});
    const paintedRef = React2.useRef(/* @__PURE__ */ new Set());
    const strokeApisRef = React2.useRef({});
    const wonRef = React2.useRef(false);
    const dragRef = React2.useRef(null);
    if (!regions.length) {
      return /* @__PURE__ */ jsxRuntime2.jsxs("button", { onClick: () => ctx.reportWin(), style: { position: "absolute", inset: 0, width: "100%", height: "100%", background: "transparent", border: "none", cursor: "pointer", display: "flex", flexDirection: "column", alignItems: "center", justifyContent: "center", gap: 8, color: "rgba(255,255,255,0.85)", fontSize: 15 }, children: [
        /* @__PURE__ */ jsxRuntime2.jsx("span", { style: { fontSize: 32 }, children: "🎨" }),
        /* @__PURE__ */ jsxRuntime2.jsx("span", { children: "No paint regions yet — add them in the Mini Games tab." }),
        /* @__PURE__ */ jsxRuntime2.jsx("span", { style: { fontSize: 12, opacity: 0.7 }, children: "Tap to continue" })
      ] });
    }
    const clipFor = (r, i) => {
      const u = maskUrls[i];
      if (u) return { maskUrl: u, maskFit: "stretch" };
      const x = r.x ?? 30, y = r.y ?? 30, w = r.w ?? 40, h = r.h ?? 40;
      if ((r.shape || "rect") === "ellipse") {
        return { shapeClip: \`ellipse(\${w / 2}% \${h / 2}% at \${x + w / 2}% \${y + h / 2}%)\` };
      }
      return { shapeClip: \`inset(\${y}% \${Math.max(0, 100 - x - w)}% \${Math.max(0, 100 - y - h)}% \${x}%)\` };
    };
    const hitRegion = (xPct, yPct) => {
      for (let i = regions.length - 1; i >= 0; i--) {
        const r = regions[i];
        const u = maskUrls[i];
        if (u) {
          const test = testers.current[u];
          if (test ? test(xPct, yPct) : false) return r.id;
        } else {
          const x = r.x ?? 30, y = r.y ?? 30, w = r.w ?? 40, h = r.h ?? 40;
          if ((r.shape || "rect") === "ellipse") {
            const dx = (xPct - (x + w / 2)) / (w / 2), dy = (yPct - (y + h / 2)) / (h / 2);
            if (dx * dx + dy * dy <= 1) return r.id;
          } else if (xPct >= x && xPct <= x + w && yPct >= y && yPct <= y + h) return r.id;
        }
      }
      return null;
    };
    const boardPct = (e) => {
      const host = boardRef.current;
      if (!host || !box.width || !box.height) return null;
      const rect = host.getBoundingClientRect();
      return {
        x: (e.clientX - rect.left - box.left) / box.width * 100,
        y: (e.clientY - rect.top - box.top) / box.height * 100
      };
    };
    const markPainted = (key, color) => {
      var _a2, _b;
      paintedRef.current.add(key);
      const r = regions.find((x) => x.id === key);
      if (r == null ? void 0 : r.colorSlot) (_a2 = ctx.reportColors) == null ? void 0 : _a2.call(ctx, { [r.colorSlot]: color });
      (_b = ctx.reportProgress) == null ? void 0 : _b.call(ctx, paintedRef.current.size / regions.length);
      if (requireAll && paintedRef.current.size >= regions.length && !wonRef.current) {
        wonRef.current = true;
        window.setTimeout(() => ctx.reportWin(), 450);
      }
    };
    const fillRegion = (key) => {
      var _a2;
      setFills((f) => ({ ...f, [key]: activeColor }));
      (_a2 = strokeApisRef.current[key]) == null ? void 0 : _a2.clear();
      ctx.playInteractSound();
      markPainted(key, activeColor);
    };
    const brushSizePct = Math.max(2, Math.min(20, stage.brushSizePct ?? 7));
    const onPointerDown = (e) => {
      var _a2, _b;
      if (wonRef.current) return;
      const p = boardPct(e);
      if (!p) return;
      (_b = (_a2 = e.currentTarget).setPointerCapture) == null ? void 0 : _b.call(_a2, e.pointerId);
      const key = hitRegion(p.x, p.y);
      dragRef.current = { regionKey: key, startX: e.clientX, startY: e.clientY, moved: false };
    };
    const onPointerMove = (e) => {
      var _a2;
      const d = dragRef.current;
      if (!d || !allowBrush || !d.regionKey) return;
      if (!d.moved && Math.hypot(e.clientX - d.startX, e.clientY - d.startY) < TAP_SLOP_PX) return;
      d.moved = true;
      const p = boardPct(e);
      if (!p) return;
      (_a2 = strokeApisRef.current[d.regionKey]) == null ? void 0 : _a2.stamp(p.x, p.y, activeColor, brushSizePct);
    };
    const onPointerUp = () => {
      const d = dragRef.current;
      dragRef.current = null;
      if (!d || wonRef.current || !d.regionKey) return;
      if (d.moved) {
        ctx.playInteractSound();
        markPainted(d.regionKey, activeColor);
      } else {
        fillRegion(d.regionKey);
      }
    };
    return /* @__PURE__ */ jsxRuntime2.jsxs("div", { style: { position: "absolute", inset: 0, display: "flex", flexDirection: "column" }, children: [
      /* @__PURE__ */ jsxRuntime2.jsx(RegionPulseStyle, {}),
      /* @__PURE__ */ jsxRuntime2.jsx(
        "div",
        {
          ref: boardRef,
          onPointerDown,
          onPointerMove,
          onPointerUp,
          onPointerCancel: onPointerUp,
          style: { position: "relative", flex: 1, minHeight: 0, touchAction: "none", cursor: "crosshair" },
          children: /* @__PURE__ */ jsxRuntime2.jsxs("div", { style: { position: "absolute", left: box.left, top: box.top, width: box.width, height: box.height, pointerEvents: "none" }, children: [
            baseUrl && /* @__PURE__ */ jsxRuntime2.jsx("img", { src: baseUrl, alt: "", draggable: false, style: { position: "absolute", inset: 0, width: "100%", height: "100%" } }),
            regions.map((r, i) => /* @__PURE__ */ jsxRuntime2.jsx(
              ColorableRegion,
              {
                clip: clipFor(r, i),
                fillColor: fills[r.id] || null,
                tintMode,
                pulse: !paintedRef.current.has(r.id),
                strokeApiRef: allowBrush ? ((api2) => {
                  strokeApisRef.current[r.id] = api2;
                }) : void 0
              },
              r.id
            ))
          ] })
        }
      ),
      /* @__PURE__ */ jsxRuntime2.jsx(
        SwatchBar,
        {
          palettes,
          activeColor,
          onPick: (c) => {
            setActiveColor(c);
            ctx.playInteractSound();
          },
          trailing: !requireAll ? /* @__PURE__ */ jsxRuntime2.jsx(
            "button",
            {
              onClick: () => {
                if (!wonRef.current) {
                  wonRef.current = true;
                  ctx.reportWin();
                }
              },
              style: { flexShrink: 0, marginLeft: 6, padding: "6px 16px", borderRadius: 999, border: "1px solid rgba(255,255,255,0.4)", background: "rgba(16,185,129,0.85)", color: "#fff", fontSize: 13, fontWeight: 700, cursor: "pointer" },
              children: "✓ Done"
            }
          ) : void 0
        }
      )
    ] });
  };
  const normKey = (raw) => {
    const l = (raw || "").toLowerCase().trim();
    if (l === " " || l === "space" || l === "spacebar") return " ";
    if (l === "up" || l === "arrowup") return "arrowup";
    if (l === "down" || l === "arrowdown") return "arrowdown";
    if (l === "left" || l === "arrowleft") return "arrowleft";
    if (l === "right" || l === "arrowright") return "arrowright";
    if (l === "esc" || l === "escape") return "escape";
    if (l === "return" || l === "enter") return "enter";
    return l;
  };
  const keyCapLabel = (raw) => {
    switch (normKey(raw || " ")) {
      case " ":
        return "SPACE";
      case "arrowup":
        return "↑";
      case "arrowdown":
        return "↓";
      case "arrowleft":
        return "←";
      case "arrowright":
        return "→";
      case "enter":
        return "⏎";
      case "escape":
        return "ESC";
      default:
        return (raw || "?").toUpperCase();
    }
  };
  const QteGameSurface = ({ ctx }) => {
    const stage = ctx.stage;
    const prompts = stage.prompts || [];
    const gapMs = Math.max(0, stage.gapMs ?? 400);
    const onMiss = stage.onMiss || "retry";
    const showPips = stage.showProgressPips !== false;
    const [idx, setIdx] = React2.useState(0);
    const [phase, setPhase] = React2.useState("gap");
    const doneRef = React2.useRef(false);
    const idxRef = React2.useRef(0);
    const phaseRef = React2.useRef("gap");
    phaseRef.current = phase;
    idxRef.current = idx;
    const ringRef = React2.useRef(null);
    const rafRef = React2.useRef(null);
    const timerRef = React2.useRef(null);
    const deadlineRef = React2.useRef(0);
    const windowMsRef = React2.useRef(1500);
    const prompt = prompts[idx];
    const clearTimers = () => {
      if (timerRef.current != null) {
        clearTimeout(timerRef.current);
        timerRef.current = null;
      }
      if (rafRef.current != null) {
        cancelAnimationFrame(rafRef.current);
        rafRef.current = null;
      }
    };
    const schedule = (fn, ms) => {
      if (timerRef.current != null) clearTimeout(timerRef.current);
      timerRef.current = window.setTimeout(fn, ms);
    };
    const startPrompt = (i) => {
      if (doneRef.current) return;
      const p = prompts[i];
      if (!p) return;
      const windowMs = Math.max(300, p.windowMs ?? 1500);
      windowMsRef.current = windowMs;
      deadlineRef.current = performance.now() + windowMs;
      if (p.bgImageId !== void 0) ctx.setBackground(p.bgImageId ?? null);
      setIdx(i);
      setPhase("active");
      const tick = () => {
        if (doneRef.current || phaseRef.current !== "active" || idxRef.current !== i) return;
        const left = deadlineRef.current - performance.now();
        const ring = ringRef.current;
        if (ring) {
          const frac = Math.max(0, left / windowMsRef.current);
          ring.style.transform = \`translate(-50%, -50%) scale(\${1 + frac * 0.9})\`;
          ring.style.opacity = String(0.35 + (1 - frac) * 0.65);
          ring.style.borderColor = frac < 0.25 ? "#ef4444" : "#facc15";
        }
        if (left <= 0) {
          miss(i);
          return;
        }
        rafRef.current = requestAnimationFrame(tick);
      };
      rafRef.current = requestAnimationFrame(tick);
      schedule(() => {
        if (!doneRef.current && phaseRef.current === "active" && idxRef.current === i) miss(i);
      }, windowMs + 40);
    };
    const hit = (i) => {
      var _a, _b, _c, _d;
      if (doneRef.current || phaseRef.current !== "active" || idxRef.current !== i) return;
      clearTimers();
      ctx.playInteractSound();
      ctx.reportHit(((_a = prompts[i]) == null ? void 0 : _a.hitExpressionId) ?? null, ((_b = prompts[i]) == null ? void 0 : _b.hitFeedback) ?? null, { x: ((_c = prompts[i]) == null ? void 0 : _c.xPct) ?? 50, y: ((_d = prompts[i]) == null ? void 0 : _d.yPct) ?? 50 });
      setPhase("hitFlash");
      const isLast = i >= prompts.length - 1;
      schedule(() => {
        if (doneRef.current) return;
        if (isLast) {
          doneRef.current = true;
          ctx.reportWin();
          return;
        }
        setPhase("gap");
        schedule(() => startPrompt(i + 1), gapMs);
      }, 160);
    };
    const miss = (i) => {
      var _a, _b, _c, _d;
      if (doneRef.current || phaseRef.current !== "active" || idxRef.current !== i) return;
      clearTimers();
      ctx.reportMistake(((_a = prompts[i]) == null ? void 0 : _a.missExpressionId) ?? null, ((_b = prompts[i]) == null ? void 0 : _b.missFeedback) ?? null, { x: ((_c = prompts[i]) == null ? void 0 : _c.xPct) ?? 50, y: ((_d = prompts[i]) == null ? void 0 : _d.yPct) ?? 50 });
      setPhase("missFlash");
      if (onMiss === "fail") {
        doneRef.current = true;
        schedule(() => ctx.reportFail(), 350);
        return;
      }
      const restartIdx = onMiss === "retryPrompt" ? i : 0;
      schedule(() => {
        if (!doneRef.current) {
          setPhase("gap");
          schedule(() => startPrompt(restartIdx), Math.max(350, gapMs));
        }
      }, 420);
    };
    React2.useEffect(() => {
      if (!prompts.length) return;
      doneRef.current = false;
      schedule(() => startPrompt(0), 350);
      return () => {
        doneRef.current = true;
        clearTimers();
      };
    }, [stage.id]);
    React2.useEffect(() => {
      const handler = (e) => {
        if (doneRef.current || phaseRef.current !== "active") return;
        const p = prompts[idxRef.current];
        if (!p || p.kind !== "key") return;
        const want = normKey(p.key || " ");
        const got = normKey(e.key);
        if (got === want) {
          e.preventDefault();
          e.stopPropagation();
          hit(idxRef.current);
        }
      };
      window.addEventListener("keydown", handler, true);
      return () => window.removeEventListener("keydown", handler, true);
    }, [stage.id]);
    if (!prompts.length) {
      return /* @__PURE__ */ jsxRuntime2.jsxs("button", { onClick: () => ctx.reportWin(), style: { position: "absolute", inset: 0, width: "100%", height: "100%", background: "transparent", border: "none", cursor: "pointer", display: "flex", flexDirection: "column", alignItems: "center", justifyContent: "center", gap: 8, color: "rgba(255,255,255,0.85)", fontSize: 15 }, children: [
        /* @__PURE__ */ jsxRuntime2.jsx("span", { style: { fontSize: 32 }, children: "⚡" }),
        /* @__PURE__ */ jsxRuntime2.jsx("span", { children: "No prompts yet — add them in the Mini Games tab." }),
        /* @__PURE__ */ jsxRuntime2.jsx("span", { style: { fontSize: 12, opacity: 0.7 }, children: "Tap to continue" })
      ] });
    }
    const px = (prompt == null ? void 0 : prompt.xPct) ?? 50, py = (prompt == null ? void 0 : prompt.yPct) ?? 50;
    const imgUrl = (prompt == null ? void 0 : prompt.imageId) ? ctx.assetResolver(prompt.imageId, "image") : null;
    const isKey = (prompt == null ? void 0 : prompt.kind) === "key";
    const keyLabel = isKey ? keyCapLabel(prompt.key) : "";
    const showTarget = phase === "active" || phase === "hitFlash" || phase === "missFlash";
    const effMissFb = (prompt == null ? void 0 : prompt.missFeedback) ?? stage.missFeedback;
    const customMissShown = !!effMissFb && effMissFb.enabled !== false && !!(effMissFb.text || effMissFb.imageId);
    return /* @__PURE__ */ jsxRuntime2.jsxs("div", { style: { position: "absolute", inset: 0, touchAction: "none", userSelect: "none" }, children: [
      /* @__PURE__ */ jsxRuntime2.jsx("style", { children: "@keyframes mgQtePop{0%{transform:translate(-50%,-50%) scale(0.5);opacity:0}100%{transform:translate(-50%,-50%) scale(1);opacity:1}}" }),
      showTarget && prompt && /* @__PURE__ */ jsxRuntime2.jsxs("div", { style: { position: "absolute", left: \`\${px}%\`, top: \`\${py}%\`, width: 0, height: 0 }, children: [
        phase === "active" && /* @__PURE__ */ jsxRuntime2.jsx("div", { ref: ringRef, style: {
          position: "absolute",
          left: 0,
          top: 0,
          width: 108,
          height: 108,
          borderRadius: "50%",
          border: "4px solid #facc15",
          transform: "translate(-50%, -50%) scale(1.9)",
          opacity: 0.35,
          pointerEvents: "none",
          boxShadow: "0 0 14px rgba(250,204,21,0.35)"
        } }),
        /* @__PURE__ */ jsxRuntime2.jsx(
          "button",
          {
            onPointerDown: (e) => {
              e.preventDefault();
              if (!isKey) hit(idxRef.current);
            },
            style: {
              position: "absolute",
              left: 0,
              top: 0,
              transform: "translate(-50%, -50%)",
              width: 88,
              height: 88,
              borderRadius: "50%",
              cursor: isKey ? "default" : "pointer",
              border: \`4px solid \${phase === "hitFlash" ? "#34d399" : phase === "missFlash" ? "#ef4444" : "rgba(255,255,255,0.9)"}\`,
              background: imgUrl ? "rgba(0,0,0,0.35)" : "rgba(15,23,42,0.85)",
              display: "flex",
              alignItems: "center",
              justifyContent: "center",
              overflow: "hidden",
              color: "#fff",
              animation: "mgQtePop 0.18s ease-out",
              boxShadow: phase === "hitFlash" ? "0 0 22px rgba(52,211,153,0.8)" : phase === "missFlash" ? "0 0 22px rgba(239,68,68,0.8)" : "0 2px 12px rgba(0,0,0,0.6)",
              touchAction: "none"
            },
            children: imgUrl ? /* @__PURE__ */ jsxRuntime2.jsx("img", { src: imgUrl, alt: "", draggable: false, style: { width: "100%", height: "100%", objectFit: "contain", pointerEvents: "none" } }) : isKey ? /* @__PURE__ */ jsxRuntime2.jsx("span", { style: { fontSize: keyLabel.length > 2 ? 15 : 26, fontWeight: 800, letterSpacing: 1 }, children: keyLabel }) : /* @__PURE__ */ jsxRuntime2.jsx("span", { style: { fontSize: prompt.label && prompt.label.length > 3 ? 14 : 22, fontWeight: 800, padding: "0 6px", textAlign: "center", lineHeight: 1.15 }, children: prompt.label || "👆" })
          }
        ),
        isKey && phase === "active" && /* @__PURE__ */ jsxRuntime2.jsx("div", { style: { position: "absolute", left: 0, top: 56, transform: "translateX(-50%)", whiteSpace: "nowrap", color: "rgba(255,255,255,0.75)", fontSize: 12, textShadow: "0 1px 3px rgba(0,0,0,0.8)", pointerEvents: "none" }, children: prompt.label || "Press the key!" }),
        phase === "missFlash" && onMiss !== "fail" && !customMissShown && /* @__PURE__ */ jsxRuntime2.jsxs("div", { style: { position: "absolute", left: 0, top: -68, transform: "translateX(-50%)", whiteSpace: "nowrap", color: "#fca5a5", fontSize: 15, fontWeight: 700, textShadow: "0 1px 4px rgba(0,0,0,0.8)", pointerEvents: "none" }, children: [
          "↺ ",
          onMiss === "retryPrompt" ? "Try again!" : "From the top!"
        ] })
      ] }),
      showPips && prompts.length > 1 && /* @__PURE__ */ jsxRuntime2.jsx("div", { style: { position: "absolute", left: "50%", bottom: 8, transform: "translateX(-50%)", display: "flex", gap: 7, pointerEvents: "none" }, children: prompts.map((p, i) => {
        const done = i < idx || i === idx && (phase === "hitFlash" && i === prompts.length - 1);
        const current = i === idx && phase !== "gap";
        return /* @__PURE__ */ jsxRuntime2.jsx("span", { style: {
          width: 10,
          height: 10,
          borderRadius: "50%",
          background: done ? "#34d399" : current ? "#facc15" : "rgba(255,255,255,0.25)",
          boxShadow: current ? "0 0 8px rgba(250,204,21,0.7)" : void 0,
          transition: "background 0.2s"
        } }, p.id);
      }) })
    ] });
  };
  const StubSurface = ({ ctx }) => /* @__PURE__ */ jsxRuntime2.jsxs(
    "button",
    {
      onClick: () => ctx.reportWin(),
      style: {
        position: "absolute",
        inset: 0,
        width: "100%",
        height: "100%",
        background: "transparent",
        border: "none",
        cursor: "pointer",
        display: "flex",
        flexDirection: "column",
        alignItems: "center",
        justifyContent: "center",
        gap: 8,
        color: "rgba(255,255,255,0.85)",
        fontSize: 15
      },
      children: [
        /* @__PURE__ */ jsxRuntime2.jsx("span", { style: { fontSize: 32 }, children: "🚧" }),
        /* @__PURE__ */ jsxRuntime2.jsx("span", { children: "This mini game isn't available in this version." }),
        /* @__PURE__ */ jsxRuntime2.jsx("span", { style: { fontSize: 12, opacity: 0.7 }, children: "Tap to continue" })
      ]
    }
  );
  const stub = (label) => ({ label, Surface: StubSurface });
  const MINI_GAMES = {
    wipe: { label: "Wipe away", Surface: WipeGameSurface },
    assemble: { label: "Assemble the pieces", Surface: AssembleGameSurface },
    paint: { label: "Painting", Surface: PaintGameSurface },
    memory: { label: "Memory match", Surface: MemoryGameSurface },
    hidden: { label: "Hidden objects", Surface: HiddenObjectGameSurface },
    sliding: { label: "Sliding puzzle", Surface: SlidingGameSurface },
    qte: { label: "Quick taps", Surface: QteGameSurface }
  };
  function resolveMiniGameDef(stageType) {
    return MINI_GAMES[stageType] || stub("Mini game");
  }
  function characterExpressionUrls(project, characterId, expressionId) {
    var _a, _b, _c, _d, _e;
    const ch = (_a = project.characters) == null ? void 0 : _a[characterId];
    if (!ch) return [];
    const urls = [];
    if (ch.baseImageUrl) urls.push(resolveFieldUrl(project.id, ch.baseImageUrl) || ch.baseImageUrl);
    const expr = expressionId ? (_b = ch.expressions) == null ? void 0 : _b[expressionId] : Object.values(ch.expressions || {})[0];
    const cfg = expr == null ? void 0 : expr.layerConfiguration;
    if (cfg) {
      for (const layerId of Object.keys(cfg)) {
        const assetId = cfg[layerId];
        if (!assetId) continue;
        const asset = (_e = (_d = (_c = ch.layers) == null ? void 0 : _c[layerId]) == null ? void 0 : _d.assets) == null ? void 0 : _e[assetId];
        if (asset == null ? void 0 : asset.imageUrl) urls.push(resolveFieldUrl(project.id, asset.imageUrl) || asset.imageUrl);
      }
    }
    return urls;
  }
  const MessageBody = ({ cfg, variables, project, assetResolver, scale = 1 }) => {
    const imgUrl = cfg.imageId ? assetResolver(cfg.imageId, "image") : null;
    return /* @__PURE__ */ jsxRuntime2.jsxs("div", { style: {
      display: "flex",
      flexDirection: "column",
      alignItems: "center",
      gap: 10 * scale,
      maxWidth: "80%",
      padding: cfg.panelColor ? \`\${18 * scale}px \${32 * scale}px\` : 0,
      borderRadius: 16 * scale,
      background: cfg.panelColor || "transparent"
    }, children: [
      imgUrl && /* @__PURE__ */ jsxRuntime2.jsx("img", { src: imgUrl, alt: "", draggable: false, style: { maxWidth: \`\${40 * scale}vmin\`, maxHeight: \`\${30 * scale}vmin\`, objectFit: "contain", pointerEvents: "none" } }),
      cfg.text && /* @__PURE__ */ jsxRuntime2.jsx("div", { style: {
        fontSize: (cfg.fontSize ?? 48) * scale,
        fontFamily: cfg.fontFamily || void 0,
        color: cfg.color || "#ffffff",
        fontWeight: cfg.bold === false ? "normal" : "bold",
        fontStyle: cfg.italic ? "italic" : "normal",
        textAlign: "center",
        whiteSpace: "pre-wrap",
        textShadow: "0 2px 10px rgba(0,0,0,0.7)"
      }, children: interpolateVariables(cfg.text, variables, project) })
    ] });
  };
  const MiniGameFrame = ({ game, project, variables, assetResolver, playSound, onResolve, isEditorPreview }) => {
    const stages = game.stages || [];
    const [stageIndex, setStageIndex] = React2.useState(0);
    const [attempt, setAttempt] = React2.useState(0);
    const [phase, setPhase] = React2.useState(game.introText ? "intro" : "play");
    const [progress, setProgress] = React2.useState(0);
    const [remainingMs, setRemainingMs] = React2.useState(null);
    const [mistakes, setMistakes] = React2.useState(0);
    const mistakesRef = React2.useRef(0);
    const failCauseRef = React2.useRef("time");
    const resolvedRef = React2.useRef(false);
    const timersRef = React2.useRef([]);
    const deadlineRef = React2.useRef(null);
    const rafRef = React2.useRef(null);
    const [reaction, setReaction] = React2.useState("idle");
    const reactionOverrideRef = React2.useRef(null);
    const reactionTimerRef = React2.useRef(null);
    const hitsRef = React2.useRef(0);
    const missesRef = React2.useRef(0);
    const [bgOverride, setBgOverride] = React2.useState(null);
    const cutsceneNextRef = React2.useRef("nextStage");
    const [feedbackFlash, setFeedbackFlash] = React2.useState(null);
    const feedbackTimerRef = React2.useRef(null);
    const feedbackNonceRef = React2.useRef(0);
    const stage = stages[stageIndex];
    const timeLimitMs = (game.timeLimitSec ?? 0) > 0 ? game.timeLimitSec * 1e3 : 0;
    const pushTimer = (fn, ms) => {
      const id = window.setTimeout(fn, ms);
      timersRef.current.push(id);
    };
    const phaseRef = React2.useRef(phase);
    phaseRef.current = phase;
    const slotColorsRef = React2.useRef({});
    const computeScore = () => {
      const hits = hitsRef.current, misses = missesRef.current, total = hits + misses;
      return { hits, misses, accuracy: total > 0 ? Math.round(hits / total * 100) : 100 };
    };
    const resolve = (kind) => {
      if (resolvedRef.current) return;
      resolvedRef.current = true;
      if (rafRef.current != null) cancelAnimationFrame(rafRef.current);
      onResolve(kind, kind === "win" ? { ...slotColorsRef.current } : void 0, computeScore());
    };
    const flashReaction = (kind, override) => {
      if (!game.character || phaseRef.current === "won" || phaseRef.current === "failed") return;
      reactionOverrideRef.current = override ?? null;
      setReaction(kind);
      if (reactionTimerRef.current != null) clearTimeout(reactionTimerRef.current);
      reactionTimerRef.current = window.setTimeout(() => {
        setReaction("idle");
        reactionOverrideRef.current = null;
      }, Math.max(120, game.character.reactMs ?? 500));
      timersRef.current.push(reactionTimerRef.current);
    };
    const retryAttempt = () => {
      resolvedRef.current = false;
      mistakesRef.current = 0;
      setMistakes(0);
      hitsRef.current = 0;
      missesRef.current = 0;
      setProgress(0);
      setBgOverride(null);
      setReaction("idle");
      setFeedbackFlash(null);
      if (game.retryScope !== "stage") setStageIndex(0);
      setAttempt((a) => a + 1);
      setPhase("play");
    };
    const triggerFail = (cause) => {
      if (resolvedRef.current || phaseRef.current === "won" || phaseRef.current === "failed") return;
      failCauseRef.current = cause;
      setPhase("failed");
      setReaction("fail");
      playSound(game.failSfxId ?? null);
      const retry = game.failMode === "retry";
      const fm = game.failMessage;
      const banner = fm && fm.enabled !== false && (fm.text || fm.imageId);
      const holdMs = (fm == null ? void 0 : fm.enabled) === false ? 150 : banner ? Math.max(150, fm.durationMs ?? 1200) : retry ? 650 : 750;
      pushTimer(() => {
        if (retry) retryAttempt();
        else resolve("fail");
      }, holdMs);
    };
    const showFeedback = (kind, override, pos) => {
      const cfg = override ?? (kind === "hit" ? stage == null ? void 0 : stage.hitFeedback : stage == null ? void 0 : stage.missFeedback);
      if (!cfg || cfg.enabled === false || !(cfg.text || cfg.imageId)) return;
      if (feedbackTimerRef.current != null) clearTimeout(feedbackTimerRef.current);
      feedbackNonceRef.current = feedbackNonceRef.current + 1 | 0;
      setFeedbackFlash({ cfg, pos, nonce: feedbackNonceRef.current });
      feedbackTimerRef.current = window.setTimeout(() => setFeedbackFlash(null), Math.max(250, cfg.durationMs ?? 900));
      timersRef.current.push(feedbackTimerRef.current);
    };
    const handleMistake = (override, feedbackOverride, pos) => {
      if (resolvedRef.current || phaseRef.current !== "play") return;
      missesRef.current += 1;
      flashReaction("miss", override);
      showFeedback("miss", feedbackOverride, pos);
      const limit = game.mistakeLimit ?? 0;
      if (limit <= 0) return;
      mistakesRef.current += 1;
      setMistakes(mistakesRef.current);
      if (mistakesRef.current >= limit) triggerFail("mistakes");
    };
    const handleHit = (override, feedbackOverride, pos) => {
      if (resolvedRef.current || phaseRef.current !== "play") return;
      hitsRef.current += 1;
      flashReaction("hit", override);
      showFeedback("hit", feedbackOverride, pos);
    };
    React2.useEffect(() => {
      if (!timeLimitMs || phase === "intro") return;
      deadlineRef.current = performance.now() + timeLimitMs;
      setRemainingMs(timeLimitMs);
      const tick = () => {
        if (resolvedRef.current) return;
        const left = deadlineRef.current - performance.now();
        setRemainingMs(Math.max(0, left));
        if (left <= 0) {
          triggerFail("time");
          return;
        }
        rafRef.current = requestAnimationFrame(tick);
      };
      rafRef.current = requestAnimationFrame(tick);
      return () => {
        if (rafRef.current != null) cancelAnimationFrame(rafRef.current);
      };
    }, [timeLimitMs, phase === "intro", attempt]);
    React2.useEffect(() => () => {
      timersRef.current.forEach((id) => clearTimeout(id));
    }, []);
    const enterWin = () => {
      setPhase("won");
      setReaction("win");
      playSound(game.winSfxId ?? null);
      const wm = game.winMessage;
      const banner = wm && wm.enabled !== false && (wm.text || wm.imageId);
      const holdMs = (wm == null ? void 0 : wm.enabled) === false ? 150 : banner ? Math.max(150, wm.durationMs ?? 1200) : 750;
      pushTimer(() => resolve("win"), holdMs);
    };
    const advanceToNextStage = () => {
      if (game.mistakesPerStage) {
        mistakesRef.current = 0;
        setMistakes(0);
      }
      setStageIndex((i) => i + 1);
      setProgress(0);
      setBgOverride(null);
      setReaction("idle");
      setFeedbackFlash(null);
      setPhase("play");
    };
    const dismissCutscene = () => {
      if (phaseRef.current !== "cutscene") return;
      if (cutsceneNextRef.current === "win") enterWin();
      else advanceToNextStage();
    };
    const handleStageWin = () => {
      if (resolvedRef.current || phaseRef.current === "stageWon" || phaseRef.current === "cutscene" || phaseRef.current === "won" || phaseRef.current === "failed") return;
      const isLast = stageIndex >= stages.length - 1;
      const cut = stage == null ? void 0 : stage.cutscene;
      const hasCut = !!cut && !!(cut.text || cut.imageId || cut.characterId);
      playSound((stage == null ? void 0 : stage.stageWinSfxId) ?? game.interactSfxId ?? null);
      if (hasCut) {
        cutsceneNextRef.current = isLast ? "win" : "nextStage";
        setPhase("cutscene");
        if (cut.durationMs && cut.durationMs > 0) pushTimer(dismissCutscene, cut.durationMs);
        return;
      }
      if (isLast) enterWin();
      else {
        setPhase("stageWon");
        pushTimer(advanceToNextStage, 750);
      }
    };
    const cursorUrl = (stage == null ? void 0 : stage.cursorImageId) ? assetResolver(stage.cursorImageId, "image") : null;
    const cursorRef = React2.useRef(null);
    const surfaceRef = React2.useRef(null);
    const moveCursor = (e) => {
      const el = cursorRef.current, host = surfaceRef.current;
      if (!el || !host) return;
      const rect = host.getBoundingClientRect();
      el.style.transform = \`translate3d(\${e.clientX - rect.left}px, \${e.clientY - rect.top}px, 0) translate(-50%, -50%)\`;
      el.style.opacity = "1";
    };
    const hideCursor = () => {
      if (cursorRef.current) cursorRef.current.style.opacity = "0";
    };
    const cursorSizePct = Math.min(30, Math.max(2, (stage == null ? void 0 : stage.cursorSizePct) ?? 6));
    if (!stage) return null;
    const def = resolveMiniGameDef(stage.stageType);
    const Surface = def.Surface;
    const activeBgId = bgOverride ?? game.backgroundImage ?? null;
    const bgUrl = activeBgId ? assetResolver(activeBgId, "image") : null;
    const char = game.character;
    const reactionExprId = char ? reaction === "hit" ? reactionOverrideRef.current || char.hitExpressionId || char.idleExpressionId || null : reaction === "miss" ? reactionOverrideRef.current || char.missExpressionId || char.idleExpressionId || null : reaction === "win" ? char.winExpressionId || char.idleExpressionId || null : reaction === "fail" ? char.failExpressionId || char.idleExpressionId || null : char.idleExpressionId || null : null;
    const charUrls = char ? characterExpressionUrls(project, char.characterId, reactionExprId) : [];
    const ctx = {
      stage,
      game,
      project,
      variables,
      assetResolver,
      playInteractSound: () => playSound(game.interactSfxId ?? null),
      playSound,
      reportWin: handleStageWin,
      reportMistake: handleMistake,
      reportHit: handleHit,
      setBackground: (id) => setBgOverride(id),
      reportFail: () => triggerFail("mistakes"),
      reportProgress: setProgress,
      reportColors: (slots) => {
        Object.assign(slotColorsRef.current, slots);
      },
      isEditorPreview
    };
    const timerFrac = timeLimitMs && remainingMs != null ? remainingMs / timeLimitMs : 1;
    const blocking = phase !== "play";
    return (
      // game.fontFamily cascades to ALL mini-game text (title/instructions/labels/keycaps/cutscene/
      // Skip/pips…); per-message fonts still override via MessageBody. Absent = inherit app default.
      /* @__PURE__ */ jsxRuntime2.jsxs("div", { style: { position: "absolute", inset: 0, background: game.backgroundColor || "rgba(0,0,0,0.75)", display: "flex", flexDirection: "column", userSelect: "none", fontFamily: game.fontFamily || void 0 }, children: [
        bgUrl && /* @__PURE__ */ jsxRuntime2.jsx("img", { src: bgUrl, alt: "", draggable: false, style: { position: "absolute", inset: 0, width: "100%", height: "100%", objectFit: "cover", pointerEvents: "none" } }),
        /* @__PURE__ */ jsxRuntime2.jsxs("div", { style: { position: "relative", zIndex: 2, padding: "14px 18px 8px", textAlign: "center", color: "#fff", textShadow: "0 1px 4px rgba(0,0,0,0.8)", pointerEvents: "none" }, children: [
          (game.title || game.name) && /* @__PURE__ */ jsxRuntime2.jsx("div", { style: { fontSize: 22, fontWeight: 700 }, children: game.title || game.name }),
          game.instructions && /* @__PURE__ */ jsxRuntime2.jsx("div", { style: { fontSize: 14, opacity: 0.9, marginTop: 2 }, children: game.instructions }),
          stage.instructions && phase !== "intro" && /* @__PURE__ */ jsxRuntime2.jsx("div", { style: { fontSize: 14, opacity: 0.95, marginTop: 4, fontStyle: "italic" }, children: stage.instructions }),
          stages.length > 1 && phase !== "intro" && /* @__PURE__ */ jsxRuntime2.jsx("div", { style: { fontSize: 12, opacity: 0.75, marginTop: 4 }, children: \`Step \${Math.min(stageIndex + 1, stages.length)} of \${stages.length}\` }),
          timeLimitMs > 0 && game.showTimer !== false && phase !== "intro" && /* @__PURE__ */ jsxRuntime2.jsx("div", { style: { margin: "8px auto 0", width: "min(420px, 70%)", height: 6, borderRadius: 3, background: "rgba(255,255,255,0.25)", overflow: "hidden" }, children: /* @__PURE__ */ jsxRuntime2.jsx("div", { style: { width: \`\${Math.max(0, Math.min(1, timerFrac)) * 100}%\`, height: "100%", borderRadius: 3, background: timerFrac < 0.25 ? "#ef4444" : "#facc15", transition: "width 0.1s linear" } }) }),
          (game.mistakeLimit ?? 0) > 0 && game.showMistakes !== false && phase !== "intro" && /* @__PURE__ */ jsxRuntime2.jsx("div", { style: { marginTop: 5, fontSize: 17, letterSpacing: 3, lineHeight: 1 }, children: Array.from({ length: game.mistakeLimit }).map((_, i) => /* @__PURE__ */ jsxRuntime2.jsx("span", { style: { color: "#f87171", opacity: i < game.mistakeLimit - mistakes ? 1 : 0.22, transition: "opacity 0.25s" }, children: "♥" }, i)) })
        ] }),
        /* @__PURE__ */ jsxRuntime2.jsxs(
          "div",
          {
            ref: surfaceRef,
            style: { position: "relative", zIndex: 1, flex: 1, minHeight: 0, margin: "4px 4% 12px", cursor: cursorUrl ? "none" : void 0 },
            onPointerMove: cursorUrl ? moveCursor : void 0,
            onPointerDown: cursorUrl ? moveCursor : void 0,
            onPointerLeave: cursorUrl ? hideCursor : void 0,
            onPointerUp: cursorUrl ? ((e) => {
              if (e.pointerType !== "mouse") hideCursor();
            }) : void 0,
            children: [
              /* @__PURE__ */ jsxRuntime2.jsx(Surface, { ctx }, \`\${stage.id}-\${attempt}\`),
              char && charUrls.length > 0 && /* @__PURE__ */ jsxRuntime2.jsx("div", { style: {
                position: "absolute",
                left: \`\${char.x ?? 22}%\`,
                top: \`\${char.y ?? 8}%\`,
                height: \`\${char.size ?? 62}%\`,
                aspectRatio: "3 / 4",
                transform: char.flipX ? "scaleX(-1)" : void 0,
                pointerEvents: "none",
                zIndex: 15,
                transition: "transform 0.12s ease-out"
              }, children: charUrls.map((u, i) => /* @__PURE__ */ jsxRuntime2.jsx(
                "img",
                {
                  src: u,
                  alt: "",
                  draggable: false,
                  style: { position: "absolute", inset: 0, width: "100%", height: "100%", objectFit: "contain" }
                },
                i
              )) }),
              cursorUrl && /* @__PURE__ */ jsxRuntime2.jsx(
                "img",
                {
                  ref: cursorRef,
                  src: cursorUrl,
                  alt: "",
                  draggable: false,
                  style: { position: "absolute", left: 0, top: 0, width: \`\${cursorSizePct}%\`, opacity: 0, pointerEvents: "none", zIndex: 30, willChange: "transform" }
                }
              ),
              phase === "play" && progress > 0 && progress < 1 && /* @__PURE__ */ jsxRuntime2.jsxs("div", { style: { position: "absolute", right: 10, bottom: 10, zIndex: 20, padding: "2px 10px", borderRadius: 999, background: "rgba(0,0,0,0.55)", color: "#fff", fontSize: 12, pointerEvents: "none" }, children: [
                Math.round(progress * 100),
                "%"
              ] }),
              feedbackFlash && phase === "play" && /* @__PURE__ */ jsxRuntime2.jsxs("div", { style: {
                position: "absolute",
                left: feedbackFlash.pos ? \`\${feedbackFlash.pos.x}%\` : "50%",
                top: feedbackFlash.pos ? \`\${feedbackFlash.pos.y}%\` : "40%",
                transform: "translate(-50%, -50%)",
                zIndex: 22,
                pointerEvents: "none"
              }, children: [
                /* @__PURE__ */ jsxRuntime2.jsx("style", { children: "@keyframes mgPopIn{0%{transform:scale(0.6);opacity:0}70%{transform:scale(1.06);opacity:1}100%{transform:scale(1)}}" }),
                /* @__PURE__ */ jsxRuntime2.jsx("div", { style: {
                  animation: (feedbackFlash.cfg.animation ?? "pop") === "pop" ? "mgPopIn 0.3s ease-out" : (feedbackFlash.cfg.animation ?? "pop") === "fade" ? "fade-in 0.25s ease-out" : void 0
                }, children: /* @__PURE__ */ jsxRuntime2.jsx(MessageBody, { cfg: feedbackFlash.cfg, variables, project, assetResolver, scale: 0.6 }) }, feedbackFlash.nonce)
              ] }),
              blocking && phase !== "intro" && (() => {
                const isWin = phase === "won";
                const isFail = phase === "failed";
                const cfg = isWin ? game.winMessage : isFail ? game.failMessage : void 0;
                const msgHidden = (isWin || isFail) && (cfg == null ? void 0 : cfg.enabled) === false;
                const custom = (isWin || isFail) && !msgHidden && cfg && (cfg.text || cfg.imageId) ? cfg : null;
                const dim = msgHidden ? false : custom ? custom.dimBackground !== false : true;
                const anim = custom ? custom.animation ?? "pop" : "fade";
                return /* @__PURE__ */ jsxRuntime2.jsxs("div", { style: { position: "absolute", inset: 0, zIndex: 25, display: "flex", alignItems: "center", justifyContent: "center", background: dim ? "rgba(0,0,0,0.35)" : "transparent", animation: "fade-in 0.15s ease-out" }, children: [
                  /* @__PURE__ */ jsxRuntime2.jsx("style", { children: "@keyframes mgPopIn{0%{transform:scale(0.6);opacity:0}70%{transform:scale(1.06);opacity:1}100%{transform:scale(1)}}" }),
                  msgHidden ? null : custom ? /* @__PURE__ */ jsxRuntime2.jsx("div", { style: { animation: anim === "pop" ? "mgPopIn 0.35s ease-out" : anim === "fade" ? "fade-in 0.3s ease-out" : void 0 }, children: /* @__PURE__ */ jsxRuntime2.jsx(MessageBody, { cfg: custom, variables, project, assetResolver }) }) : /* @__PURE__ */ jsxRuntime2.jsx("div", { style: { fontSize: 72, textShadow: "0 2px 12px rgba(0,0,0,0.7)" }, children: isFail ? game.failMode === "retry" ? "↻" : failCauseRef.current === "mistakes" ? "✖" : "⏰" : "✓" })
                ] });
              })()
            ]
          }
        ),
        !!game.skippable && phase !== "won" && phase !== "failed" && /* @__PURE__ */ jsxRuntime2.jsx(
          "button",
          {
            onClick: () => {
              playSound(game.interactSfxId ?? null);
              resolve("skip");
            },
            style: { position: "absolute", right: 14, bottom: 12, zIndex: 3, padding: "7px 16px", borderRadius: 999, border: "1px solid rgba(255,255,255,0.4)", background: "rgba(0,0,0,0.55)", color: "#fff", fontSize: 13, cursor: "pointer" },
            children: game.skipLabel || "Skip"
          }
        ),
        phase === "cutscene" && stage.cutscene && (() => {
          const cut = stage.cutscene;
          const cutImg = cut.imageId ? assetResolver(cut.imageId, "image") : null;
          const cutCharUrls = cut.characterId ? characterExpressionUrls(project, cut.characterId, cut.expressionId ?? null) : [];
          return /* @__PURE__ */ jsxRuntime2.jsxs(
            "button",
            {
              onClick: () => {
                playSound(game.interactSfxId ?? null);
                dismissCutscene();
              },
              style: { position: "absolute", inset: 0, zIndex: 26, background: cut.backgroundColor || "rgba(0,0,0,0.82)", border: "none", cursor: "pointer", display: "flex", flexDirection: "column", alignItems: "center", justifyContent: "center", gap: 14, color: "#fff", animation: "fade-in 0.25s ease-out" },
              children: [
                cutCharUrls.length > 0 && /* @__PURE__ */ jsxRuntime2.jsx("div", { style: { position: "relative", height: "46%", aspectRatio: "3 / 4" }, children: cutCharUrls.map((u, i) => /* @__PURE__ */ jsxRuntime2.jsx("img", { src: u, alt: "", draggable: false, style: { position: "absolute", inset: 0, width: "100%", height: "100%", objectFit: "contain" } }, i)) }),
                cutImg && /* @__PURE__ */ jsxRuntime2.jsx("img", { src: cutImg, alt: "", draggable: false, style: { maxWidth: "50vmin", maxHeight: "34vmin", objectFit: "contain" } }),
                cut.text && /* @__PURE__ */ jsxRuntime2.jsx("span", { style: { fontSize: 19, maxWidth: "74%", whiteSpace: "pre-wrap", textAlign: "center", textShadow: "0 1px 4px rgba(0,0,0,0.85)" }, children: interpolateVariables(cut.text, variables, project) }),
                !cut.durationMs && /* @__PURE__ */ jsxRuntime2.jsx("span", { style: { fontSize: 13, opacity: 0.8 }, children: "▶ Tap to continue" })
              ]
            }
          );
        })(),
        phase === "intro" && /* @__PURE__ */ jsxRuntime2.jsxs(
          "button",
          {
            onClick: () => {
              playSound(game.interactSfxId ?? null);
              setPhase("play");
            },
            style: { position: "absolute", inset: 0, zIndex: 4, background: "rgba(0,0,0,0.6)", border: "none", cursor: "pointer", display: "flex", flexDirection: "column", alignItems: "center", justifyContent: "center", gap: 12, color: "#fff" },
            children: [
              /* @__PURE__ */ jsxRuntime2.jsx("span", { style: { fontSize: 18, maxWidth: "70%", whiteSpace: "pre-wrap", textShadow: "0 1px 4px rgba(0,0,0,0.8)" }, children: game.introText }),
              /* @__PURE__ */ jsxRuntime2.jsx("span", { style: { fontSize: 13, opacity: 0.8 }, children: "▶ Tap to start" })
            ]
          }
        )
      ] })
    );
  };
  function resolvePlayerCharacterId(project, variables) {
    var _a;
    const varId = (_a = project.ui) == null ? void 0 : _a.playerCharacterVarId;
    if (!varId) return null;
    const raw = variables == null ? void 0 : variables[varId];
    const id = raw == null ? "" : String(raw);
    return id && project.characters[id] ? id : null;
  }
  function resolvePlayerCharacterName(project, variables) {
    var _a;
    const varId = (_a = project.ui) == null ? void 0 : _a.playerCharacterNameVarId;
    if (!varId) return null;
    const raw = variables == null ? void 0 : variables[varId];
    const name = raw == null ? "" : String(raw);
    return name.trim() ? name : null;
  }
  function resolveCommandCharacterId(command, project, variables) {
    if (command.characterSource === "player") {
      return resolvePlayerCharacterId(project, variables);
    }
    return command.characterId ?? null;
  }
  function getEffect(effects, type) {
    return effects.find((e) => e.type === type);
  }
  function ep(params, key, fallback = 0.5) {
    if (!params) return fallback;
    const v = params[key];
    if (typeof v !== "number") return fallback;
    return clamp01(v);
  }
  function parseColor(hex, defaultColor) {
    if (!hex) return defaultColor;
    const match = hex.match(/^#?([a-f\\d]{2})([a-f\\d]{2})([a-f\\d]{2})\$/i);
    if (!match) return defaultColor;
    return {
      r: parseInt(match[1], 16),
      g: parseInt(match[2], 16),
      b: parseInt(match[3], 16)
    };
  }
  function createNoise() {
    const permutation = Array.from({ length: 256 }, () => Math.floor(Math.random() * 256));
    const p = [...permutation, ...permutation];
    const fade = (t) => t * t * t * (t * (t * 6 - 15) + 10);
    const lerp2 = (a, b, t) => a + t * (b - a);
    const grad = (hash, x) => hash & 1 ? x : -x;
    return (x) => {
      const X = Math.floor(x) & 255;
      x -= Math.floor(x);
      const u = fade(x);
      return lerp2(grad(p[X], x), grad(p[X + 1], x - 1), u);
    };
  }
  const PluginEffectCanvas = ({ def, effect, width, height }) => {
    const canvasRef = React2.useRef(null);
    const intensity = clamp01(effect.intensity ?? 0);
    React2.useEffect(() => {
      const canvas = canvasRef.current;
      if (!canvas || !def.render || intensity <= 0 || width <= 0 || height <= 0) return;
      canvas.width = width;
      canvas.height = height;
      const ctx = canvas.getContext("2d");
      if (!ctx) return;
      const start = performance.now();
      let raf = 0;
      let stopped = false;
      const loop = () => {
        if (stopped) return;
        ctx.clearRect(0, 0, width, height);
        try {
          def.render(ctx, { width, height, intensity, color: effect.color, params: effect.params, timeMs: performance.now() - start });
        } catch (e) {
          console.error(\`[Plugin effect "\${def.type}"] render error (stopping):\`, e);
          stopped = true;
          return;
        }
        raf = requestAnimationFrame(loop);
      };
      raf = requestAnimationFrame(loop);
      return () => {
        stopped = true;
        cancelAnimationFrame(raf);
      };
    }, [def, intensity, width, height, effect.color, effect.params]);
    if (intensity <= 0) return null;
    return /* @__PURE__ */ jsxRuntime2.jsx("canvas", { ref: canvasRef, className: "vnfx-canvas", "aria-hidden": true });
  };
  function runCloudSim(canvas, w, h, o) {
    const dpr = Math.max(1, Math.min(2, window.devicePixelRatio || 1));
    canvas.width = Math.floor(w * dpr);
    canvas.height = Math.floor(h * dpr);
    const ctx = canvas.getContext("2d");
    if (!ctx) return () => {
    };
    ctx.setTransform(dpr, 0, 0, dpr, 0, 0);
    const count = Math.max(4, Math.floor(o.blobCount * (0.5 + o.intensity)));
    const blobs = Array.from({ length: count }).map(() => ({
      x: Math.random() * w,
      y: Math.random() * h,
      r: o.sizeMin + Math.random() * (o.sizeMax - o.sizeMin),
      vx: (o.vx + (Math.random() * 2 - 1) * o.vRand) * o.speedMul,
      vy: (o.vy + (Math.random() * 2 - 1) * o.vRand * 0.6) * o.speedMul,
      phase: Math.random() * Math.PI * 2,
      phaseSpeed: 0.15 + Math.random() * 0.35,
      op: o.baseOpacity * (0.55 + Math.random() * 0.45)
    }));
    let raf = 0;
    let last = performance.now();
    const draw = (now) => {
      const dt = Math.min(0.05, (now - last) / 1e3);
      last = now;
      ctx.clearRect(0, 0, w, h);
      for (const b of blobs) {
        b.phase += dt * b.phaseSpeed;
        b.x += (b.vx + Math.sin(b.phase) * o.swirl) * dt;
        b.y += (b.vy + Math.cos(b.phase * 0.7) * o.swirl * 0.5) * dt;
        if (b.x - b.r > w) b.x = -b.r;
        if (b.x + b.r < 0) b.x = w + b.r;
        if (b.y - b.r > h) b.y = -b.r;
        if (b.y + b.r < 0) b.y = h + b.r;
        const a = b.op * o.intensity;
        const g = ctx.createRadialGradient(b.x, b.y, 0, b.x, b.y, b.r);
        g.addColorStop(0, \`rgba(\${o.color.r}, \${o.color.g}, \${o.color.b}, \${a})\`);
        g.addColorStop(0.6, \`rgba(\${o.color.r}, \${o.color.g}, \${o.color.b}, \${a * 0.5})\`);
        g.addColorStop(1, \`rgba(\${o.color.r}, \${o.color.g}, \${o.color.b}, 0)\`);
        ctx.fillStyle = g;
        ctx.beginPath();
        ctx.arc(b.x, b.y, b.r, 0, Math.PI * 2);
        ctx.fill();
      }
      raf = requestAnimationFrame(draw);
    };
    raf = requestAnimationFrame(draw);
    return () => cancelAnimationFrame(raf);
  }
  function runFireworksSim(canvas, w, h, o) {
    const dpr = Math.max(1, Math.min(2, window.devicePixelRatio || 1));
    canvas.width = Math.floor(w * dpr);
    canvas.height = Math.floor(h * dpr);
    const ctx = canvas.getContext("2d");
    if (!ctx) return () => {
    };
    ctx.setTransform(dpr, 0, 0, dpr, 0, 0);
    const palette = (o.colors && o.colors.length ? o.colors : ["#ff3b3b", "#ffd23b", "#3bff6b", "#3b9bff", "#ff7bef", "#ffffff"]).map((c) => parseColor(c, { r: 255, g: 255, b: 255 }));
    const pick = () => palette[Math.floor(Math.random() * palette.length)];
    const intensity = clamp01(o.intensity ?? 1);
    const speedMul = o.speedMul ?? 1;
    const maxBursts = o.maxBursts ?? 3;
    let rockets = [];
    let parts = [];
    let launched = 0;
    let acc = 0;
    let nextLaunch = 0;
    const launchGap = () => ((o.continuous ? 520 : 300) + Math.random() * 260) / speedMul;
    const hf = clamp01(o.heightFrac ?? 0.7);
    const launch = () => {
      const tx = w * (0.12 + Math.random() * 0.76);
      const ty = Math.max(h * 0.06, h * (0.85 - hf * 0.72) + (Math.random() - 0.5) * h * 0.1);
      const vy = -(h * (0.7 + hf * 0.5 + Math.random() * 0.15));
      rockets.push({ x: tx, y: h + 4, vy, targetY: ty, color: pick() });
      launched++;
    };
    const explode = (x, y, color) => {
      var _a;
      const n = 46 + Math.floor(Math.random() * 42);
      const base = Math.min(w, h);
      for (let i = 0; i < n; i++) {
        const a = Math.random() * Math.PI * 2;
        const sp = (0.18 + Math.random() * 0.55) * base;
        parts.push({ x, y, vx: Math.cos(a) * sp, vy: Math.sin(a) * sp, life: 1, decay: 0.45 + Math.random() * 0.55, size: 1.2 + Math.random() * 1.8, color });
      }
      (_a = o.onExplode) == null ? void 0 : _a.call(o);
    };
    let raf = 0;
    let stopped = false;
    let idleCalled = false;
    let last = performance.now();
    const draw = (now) => {
      var _a;
      if (stopped) return;
      const dt = Math.min(0.05, (now - last) / 1e3);
      last = now;
      acc += dt * 1e3;
      if (acc >= nextLaunch && (o.continuous || launched < maxBursts)) {
        launch();
        nextLaunch = acc + launchGap();
      }
      ctx.clearRect(0, 0, w, h);
      ctx.globalCompositeOperation = "lighter";
      rockets = rockets.filter((r) => {
        r.vy += h * 0.45 * dt;
        r.y += r.vy * dt;
        ctx.fillStyle = \`rgba(\${r.color.r}, \${r.color.g}, \${r.color.b}, \${0.85 * intensity})\`;
        ctx.fillRect(r.x - 1, r.y, 2, 7);
        if (r.y <= r.targetY || r.vy >= 0) {
          explode(r.x, r.y, r.color);
          return false;
        }
        return true;
      });
      parts = parts.filter((p) => {
        p.vy += h * 0.32 * dt;
        p.x += p.vx * dt;
        p.y += p.vy * dt;
        p.life -= p.decay * dt;
        if (p.life <= 0) return false;
        const a = clamp01(p.life) * intensity;
        const g = ctx.createRadialGradient(p.x, p.y, 0, p.x, p.y, p.size * 2.2);
        g.addColorStop(0, \`rgba(\${p.color.r}, \${p.color.g}, \${p.color.b}, \${a})\`);
        g.addColorStop(1, \`rgba(\${p.color.r}, \${p.color.g}, \${p.color.b}, 0)\`);
        ctx.fillStyle = g;
        ctx.beginPath();
        ctx.arc(p.x, p.y, p.size * 2.2, 0, Math.PI * 2);
        ctx.fill();
        return true;
      });
      ctx.globalCompositeOperation = "source-over";
      if (!o.continuous && launched >= maxBursts && rockets.length === 0 && parts.length === 0) {
        if (!idleCalled) {
          idleCalled = true;
          (_a = o.onIdle) == null ? void 0 : _a.call(o);
        }
        return;
      }
      raf = requestAnimationFrame(draw);
    };
    raf = requestAnimationFrame(draw);
    return () => {
      stopped = true;
      cancelAnimationFrame(raf);
    };
  }
  const ScreenOverlayEffects = ({
    effects,
    width,
    height,
    className
  }) => {
    var _a, _b, _c, _d, _e, _f, _g, _h, _i, _j, _k, _l, _m, _n, _o, _p, _q, _r, _s;
    const normalized = React2.useMemo(() => normalizeOverlayEffects(effects), [effects]);
    const safeWidth = Math.max(0, Math.min(width, 4096));
    const safeHeight = Math.max(0, Math.min(height, 4096));
    const scanlines = getEffect(normalized, "crtScanlines");
    const chroma = getEffect(normalized, "chromaticGlitch");
    const sunbeams = getEffect(normalized, "sunbeams");
    const shimmer = getEffect(normalized, "shimmer");
    const rain = getEffect(normalized, "rain");
    const snowAsh = getEffect(normalized, "snowAsh");
    const fog = getEffect(normalized, "fog");
    const haze = getEffect(normalized, "haze");
    const smoke = getEffect(normalized, "smoke");
    const fireworks = getEffect(normalized, "fireworks");
    const pluginEffects = React2.useMemo(() => {
      const builtins = new Set(BUILTIN_OVERLAY_EFFECT_TYPES);
      return normalized.filter((e) => !builtins.has(e.type) && clamp01(e.intensity) > 0).map((e) => ({ effect: e, def: pluginManager.getEffect(e.type) })).filter((x) => !!x.def && typeof x.def.render === "function");
    }, [normalized]);
    const rainCanvasRef = React2.useRef(null);
    const snowCanvasRef = React2.useRef(null);
    const sunbeamsCanvasRef = React2.useRef(null);
    const shimmerCanvasRef = React2.useRef(null);
    const fogCanvasRef = React2.useRef(null);
    const hazeCanvasRef = React2.useRef(null);
    const smokeCanvasRef = React2.useRef(null);
    const fireworksCanvasRef = React2.useRef(null);
    const minDim = Math.min(safeWidth, safeHeight);
    React2.useEffect(() => {
      const intensity = clamp01((fog == null ? void 0 : fog.intensity) ?? 0);
      const c = fogCanvasRef.current;
      if (!c || intensity <= 0 || safeWidth <= 0 || safeHeight <= 0) return;
      const color = parseColor(fog == null ? void 0 : fog.color, { r: 205, g: 210, b: 216 });
      const speed = 0.3 + ep(fog == null ? void 0 : fog.params, "speed") * 1.4;
      return runCloudSim(c, safeWidth, safeHeight, {
        intensity,
        color,
        blobCount: 16,
        sizeMin: minDim * 0.28,
        sizeMax: minDim * 0.55,
        vx: 16,
        vy: 0,
        vRand: 10,
        baseOpacity: 0.5,
        swirl: 6,
        speedMul: speed
      });
    }, [fog == null ? void 0 : fog.intensity, fog == null ? void 0 : fog.color, (_a = fog == null ? void 0 : fog.params) == null ? void 0 : _a.speed, safeWidth, safeHeight, minDim]);
    React2.useEffect(() => {
      const intensity = clamp01((haze == null ? void 0 : haze.intensity) ?? 0);
      const c = hazeCanvasRef.current;
      if (!c || intensity <= 0 || safeWidth <= 0 || safeHeight <= 0) return;
      const color = parseColor(haze == null ? void 0 : haze.color, { r: 225, g: 222, b: 210 });
      const speed = 0.3 + ep(haze == null ? void 0 : haze.params, "speed") * 1.4;
      return runCloudSim(c, safeWidth, safeHeight, {
        intensity,
        color,
        blobCount: 10,
        sizeMin: minDim * 0.45,
        sizeMax: minDim * 0.8,
        vx: 7,
        vy: 0,
        vRand: 4,
        baseOpacity: 0.22,
        swirl: 3,
        speedMul: speed
      });
    }, [haze == null ? void 0 : haze.intensity, haze == null ? void 0 : haze.color, (_b = haze == null ? void 0 : haze.params) == null ? void 0 : _b.speed, safeWidth, safeHeight, minDim]);
    React2.useEffect(() => {
      const intensity = clamp01((smoke == null ? void 0 : smoke.intensity) ?? 0);
      const c = smokeCanvasRef.current;
      if (!c || intensity <= 0 || safeWidth <= 0 || safeHeight <= 0) return;
      const color = parseColor(smoke == null ? void 0 : smoke.color, { r: 70, g: 72, b: 76 });
      const speed = 0.3 + ep(smoke == null ? void 0 : smoke.params, "speed") * 1.4;
      return runCloudSim(c, safeWidth, safeHeight, {
        intensity,
        color,
        blobCount: 14,
        sizeMin: minDim * 0.18,
        sizeMax: minDim * 0.42,
        vx: 6,
        vy: -26,
        vRand: 14,
        baseOpacity: 0.42,
        swirl: 16,
        speedMul: speed
      });
    }, [smoke == null ? void 0 : smoke.intensity, smoke == null ? void 0 : smoke.color, (_c = smoke == null ? void 0 : smoke.params) == null ? void 0 : _c.speed, safeWidth, safeHeight, minDim]);
    React2.useEffect(() => {
      const intensity = clamp01((rain == null ? void 0 : rain.intensity) ?? 0);
      const canvas = rainCanvasRef.current;
      if (!canvas || intensity <= 0 || safeWidth <= 0 || safeHeight <= 0) return;
      const dpr = Math.max(1, Math.min(2, window.devicePixelRatio || 1));
      canvas.width = Math.floor(safeWidth * dpr);
      canvas.height = Math.floor(safeHeight * dpr);
      const ctx = canvas.getContext("2d");
      if (!ctx) return;
      ctx.setTransform(dpr, 0, 0, dpr, 0, 0);
      const baseColor = parseColor(rain == null ? void 0 : rain.color, { r: 180, g: 210, b: 255 });
      const p = rain == null ? void 0 : rain.params;
      const windMul = 0.2 + ep(p, "windStrength") * 1.8;
      const lenMul = 0.4 + ep(p, "dropLength") * 1.2;
      const speedMul = 0.3 + ep(p, "speed") * 1.4;
      const dropCount = Math.floor(100 + intensity * 600);
      const drops = Array.from({ length: dropCount }).map(() => ({
        x: Math.random() * safeWidth,
        y: Math.random() * safeHeight,
        len: (12 + Math.random() * 22) * lenMul,
        speed: (600 + Math.random() * 1e3) * speedMul,
        thickness: 1 + Math.random() * 1.8,
        wind: (-80 + Math.random() * 160) * windMul,
        splashTime: 0,
        splashX: 0,
        splashY: 0
      }));
      let raf = 0;
      let last = performance.now();
      const draw = (now) => {
        const dt = Math.min(0.05, (now - last) / 1e3);
        last = now;
        ctx.clearRect(0, 0, safeWidth, safeHeight);
        ctx.lineCap = "round";
        for (const d of drops) {
          d.x += d.wind * dt;
          d.y += d.speed * dt;
          if (d.y - d.len > safeHeight) {
            d.splashTime = 0.15;
            d.splashX = d.x;
            d.splashY = safeHeight - 5;
            d.y = -Math.random() * safeHeight * 0.3;
            d.x = Math.random() * safeWidth;
          }
          if (d.x < -50) d.x = safeWidth + 50;
          if (d.x > safeWidth + 50) d.x = -50;
          const alpha = 0.1 + intensity * 0.25;
          ctx.strokeStyle = \`rgba(\${baseColor.r}, \${baseColor.g}, \${baseColor.b}, \${alpha})\`;
          ctx.lineWidth = d.thickness;
          ctx.beginPath();
          ctx.moveTo(d.x, d.y);
          ctx.lineTo(d.x + d.wind * 0.025, d.y + d.len);
          ctx.stroke();
          if (d.splashTime > 0) {
            d.splashTime -= dt;
            const splashProgress = 1 - d.splashTime / 0.15;
            const splashAlpha = (1 - splashProgress) * alpha * 0.8;
            const splashSize = 3 + splashProgress * 8;
            ctx.fillStyle = \`rgba(\${baseColor.r}, \${baseColor.g}, \${baseColor.b}, \${splashAlpha})\`;
            ctx.beginPath();
            ctx.arc(d.splashX - splashSize, d.splashY, 1.5, 0, Math.PI * 2);
            ctx.arc(d.splashX + splashSize, d.splashY, 1.5, 0, Math.PI * 2);
            ctx.arc(d.splashX, d.splashY - splashSize * 0.5, 1, 0, Math.PI * 2);
            ctx.fill();
          }
        }
        raf = requestAnimationFrame(draw);
      };
      raf = requestAnimationFrame(draw);
      return () => cancelAnimationFrame(raf);
    }, [rain == null ? void 0 : rain.intensity, rain == null ? void 0 : rain.color, (_d = rain == null ? void 0 : rain.params) == null ? void 0 : _d.windStrength, (_e = rain == null ? void 0 : rain.params) == null ? void 0 : _e.dropLength, (_f = rain == null ? void 0 : rain.params) == null ? void 0 : _f.speed, safeWidth, safeHeight]);
    React2.useEffect(() => {
      const intensity = clamp01((snowAsh == null ? void 0 : snowAsh.intensity) ?? 0);
      const canvas = snowCanvasRef.current;
      if (!canvas || intensity <= 0 || safeWidth <= 0 || safeHeight <= 0) return;
      const variant = (snowAsh == null ? void 0 : snowAsh.variant) ?? "snow";
      const defaultSnowColor = { r: 255, g: 255, b: 255 };
      const defaultAshColor = { r: 120, g: 115, b: 110 };
      const baseColor = parseColor(snowAsh == null ? void 0 : snowAsh.color, variant === "snow" ? defaultSnowColor : defaultAshColor);
      const dpr = Math.max(1, Math.min(2, window.devicePixelRatio || 1));
      canvas.width = Math.floor(safeWidth * dpr);
      canvas.height = Math.floor(safeHeight * dpr);
      const ctx = canvas.getContext("2d");
      if (!ctx) return;
      ctx.setTransform(dpr, 0, 0, dpr, 0, 0);
      const sp = snowAsh == null ? void 0 : snowAsh.params;
      const sizeMul = 0.4 + ep(sp, "particleSize") * 1.2;
      const sWindMul = 0.2 + ep(sp, "windStrength") * 1.8;
      const sSpeedMul = 0.3 + ep(sp, "speed") * 1.4;
      const count = Math.floor(80 + intensity * 420);
      const particles = Array.from({ length: count }).map(() => ({
        x: Math.random() * safeWidth,
        y: Math.random() * safeHeight,
        r: (variant === "snow" ? 1.2 + Math.random() * 2.8 : 0.8 + Math.random() * 1.8) * sizeMul,
        vx: ((variant === "snow" ? -25 : -40) + Math.random() * 80) * sWindMul,
        vy: ((variant === "snow" ? 25 : 55) + Math.random() * (variant === "snow" ? 70 : 130)) * sSpeedMul,
        wobblePhase: Math.random() * Math.PI * 2,
        wobbleSpeed: 1.5 + Math.random() * 2.5,
        wobbleAmp: (variant === "snow" ? 15 + Math.random() * 20 : 8 + Math.random() * 12) * sWindMul,
        rotPhase: Math.random() * Math.PI * 2,
        opacity: 0.4 + Math.random() * 0.6
      }));
      let raf = 0;
      let last = performance.now();
      const draw = (now) => {
        const dt = Math.min(0.05, (now - last) / 1e3);
        last = now;
        ctx.clearRect(0, 0, safeWidth, safeHeight);
        for (const p of particles) {
          p.wobblePhase += dt * p.wobbleSpeed;
          p.rotPhase += dt * 1.2;
          const wobbleX = Math.sin(p.wobblePhase) * p.wobbleAmp;
          p.x += (p.vx + wobbleX) * dt;
          p.y += p.vy * dt;
          if (p.y - p.r > safeHeight) {
            p.y = -Math.random() * safeHeight * 0.25;
            p.x = Math.random() * safeWidth;
          }
          if (p.x < -50) p.x = safeWidth + 50;
          if (p.x > safeWidth + 50) p.x = -50;
          const alpha = p.opacity * intensity * (variant === "snow" ? 0.85 : 0.6);
          if (variant === "snow") {
            const gradient = ctx.createRadialGradient(p.x, p.y, 0, p.x, p.y, p.r * 1.5);
            gradient.addColorStop(0, \`rgba(\${baseColor.r}, \${baseColor.g}, \${baseColor.b}, \${alpha})\`);
            gradient.addColorStop(0.5, \`rgba(\${Math.floor(baseColor.r * 0.86)}, \${Math.floor(baseColor.g * 0.92)}, \${baseColor.b}, \${alpha * 0.7})\`);
            gradient.addColorStop(1, \`rgba(\${Math.floor(baseColor.r * 0.78)}, \${Math.floor(baseColor.g * 0.86)}, \${baseColor.b}, 0)\`);
            ctx.fillStyle = gradient;
            ctx.beginPath();
            ctx.arc(p.x, p.y, p.r * 1.5, 0, Math.PI * 2);
            ctx.fill();
          } else {
            ctx.fillStyle = \`rgba(\${baseColor.r}, \${baseColor.g}, \${baseColor.b}, \${alpha})\`;
            ctx.save();
            ctx.translate(p.x, p.y);
            ctx.rotate(p.rotPhase);
            ctx.beginPath();
            ctx.ellipse(0, 0, p.r, p.r * 0.6, 0, 0, Math.PI * 2);
            ctx.fill();
            ctx.restore();
          }
        }
        raf = requestAnimationFrame(draw);
      };
      raf = requestAnimationFrame(draw);
      return () => cancelAnimationFrame(raf);
    }, [snowAsh == null ? void 0 : snowAsh.intensity, snowAsh == null ? void 0 : snowAsh.variant, snowAsh == null ? void 0 : snowAsh.color, (_g = snowAsh == null ? void 0 : snowAsh.params) == null ? void 0 : _g.particleSize, (_h = snowAsh == null ? void 0 : snowAsh.params) == null ? void 0 : _h.windStrength, (_i = snowAsh == null ? void 0 : snowAsh.params) == null ? void 0 : _i.speed, safeWidth, safeHeight]);
    React2.useEffect(() => {
      const intensity = clamp01((sunbeams == null ? void 0 : sunbeams.intensity) ?? 0);
      const canvas = sunbeamsCanvasRef.current;
      if (!canvas || intensity <= 0 || safeWidth <= 0 || safeHeight <= 0) return;
      const dpr = Math.max(1, Math.min(2, window.devicePixelRatio || 1));
      canvas.width = Math.floor(safeWidth * dpr);
      canvas.height = Math.floor(safeHeight * dpr);
      const ctx = canvas.getContext("2d");
      if (!ctx) return;
      ctx.setTransform(dpr, 0, 0, dpr, 0, 0);
      const baseColor = parseColor(sunbeams == null ? void 0 : sunbeams.color, { r: 255, g: 220, b: 140 });
      const sbp = sunbeams == null ? void 0 : sunbeams.params;
      const sbSpeedMul = 0.3 + ep(sbp, "speed") * 1.4;
      const sbSpreadMul = 0.4 + ep(sbp, "spread") * 1.2;
      const noises = Array.from({ length: 4 }, () => createNoise());
      let raf = 0;
      let time = 0;
      let last = performance.now();
      const draw = (now) => {
        const dt = Math.min(0.05, (now - last) / 1e3);
        last = now;
        time += dt * sbSpeedMul;
        ctx.clearRect(0, 0, safeWidth, safeHeight);
        const centerX = safeWidth * 0.3;
        const centerY = -safeHeight * 0.1;
        ctx.globalCompositeOperation = "lighter";
        const baseGlow = ctx.createRadialGradient(centerX, centerY, 0, centerX, centerY, safeWidth * 1.2);
        baseGlow.addColorStop(0, \`rgba(\${baseColor.r}, \${baseColor.g}, \${baseColor.b}, \${intensity * 0.25})\`);
        baseGlow.addColorStop(0.2, \`rgba(\${baseColor.r}, \${baseColor.g}, \${baseColor.b}, \${intensity * 0.15})\`);
        baseGlow.addColorStop(0.5, \`rgba(\${baseColor.r}, \${Math.floor(baseColor.g * 0.9)}, \${Math.floor(baseColor.b * 0.8)}, \${intensity * 0.06})\`);
        baseGlow.addColorStop(1, \`rgba(\${baseColor.r}, \${Math.floor(baseColor.g * 0.8)}, \${Math.floor(baseColor.b * 0.6)}, 0)\`);
        ctx.fillStyle = baseGlow;
        ctx.fillRect(0, 0, safeWidth, safeHeight);
        const waveCount = 6;
        for (let w = 0; w < waveCount; w++) {
          const wavePhase = w / waveCount * Math.PI * 2;
          const n1 = noises[0](time * 0.08 + w * 10) * 0.5 + 0.5;
          const n2 = noises[1](time * 0.12 + w * 7) * 0.5 + 0.5;
          const n3 = noises[2](time * 0.06 + w * 13) * 0.5 + 0.5;
          const waveAngle = wavePhase + Math.sin(time * 0.1 + w) * 0.3 + n1 * 0.4;
          const waveWidth = (0.8 + n2 * 0.6) * sbSpreadMul;
          const waveBrightness = (0.3 + n3 * 0.4) * intensity * 0.15;
          const maxRadius = Math.max(safeWidth, safeHeight) * 2;
          for (let sub = 0; sub < 3; sub++) {
            const subOffset = (sub - 1) * 0.15;
            const subAngle = waveAngle + subOffset;
            const subBrightness = waveBrightness * (1 - Math.abs(sub - 1) * 0.3);
            const gradient = ctx.createRadialGradient(centerX, centerY, 0, centerX, centerY, maxRadius);
            gradient.addColorStop(0, \`rgba(\${baseColor.r}, \${baseColor.g}, \${baseColor.b}, \${subBrightness * 0.6})\`);
            gradient.addColorStop(0.1, \`rgba(\${baseColor.r}, \${baseColor.g}, \${baseColor.b}, \${subBrightness * 0.4})\`);
            gradient.addColorStop(0.3, \`rgba(\${baseColor.r}, \${Math.floor(baseColor.g * 0.95)}, \${Math.floor(baseColor.b * 0.85)}, \${subBrightness * 0.2})\`);
            gradient.addColorStop(0.6, \`rgba(\${baseColor.r}, \${Math.floor(baseColor.g * 0.9)}, \${Math.floor(baseColor.b * 0.7)}, \${subBrightness * 0.05})\`);
            gradient.addColorStop(1, \`rgba(\${baseColor.r}, \${Math.floor(baseColor.g * 0.85)}, \${Math.floor(baseColor.b * 0.6)}, 0)\`);
            ctx.fillStyle = gradient;
            ctx.beginPath();
            ctx.moveTo(centerX, centerY);
            ctx.arc(centerX, centerY, maxRadius, subAngle - waveWidth, subAngle + waveWidth);
            ctx.closePath();
            ctx.fill();
          }
        }
        const pulseIntensity = 0.85 + Math.sin(time * 0.5) * 0.1 + noises[3](time * 0.15) * 0.05;
        const hazeGradient = ctx.createRadialGradient(centerX, centerY, 0, centerX, centerY, safeWidth);
        hazeGradient.addColorStop(0, \`rgba(\${baseColor.r}, \${baseColor.g}, \${baseColor.b}, \${intensity * pulseIntensity * 0.12})\`);
        hazeGradient.addColorStop(0.4, \`rgba(\${baseColor.r}, \${Math.floor(baseColor.g * 0.95)}, \${Math.floor(baseColor.b * 0.9)}, \${intensity * pulseIntensity * 0.05})\`);
        hazeGradient.addColorStop(1, \`rgba(\${baseColor.r}, \${Math.floor(baseColor.g * 0.9)}, \${Math.floor(baseColor.b * 0.8)}, 0)\`);
        ctx.fillStyle = hazeGradient;
        ctx.fillRect(0, 0, safeWidth, safeHeight);
        raf = requestAnimationFrame(draw);
      };
      raf = requestAnimationFrame(draw);
      return () => cancelAnimationFrame(raf);
    }, [sunbeams == null ? void 0 : sunbeams.intensity, sunbeams == null ? void 0 : sunbeams.color, (_j = sunbeams == null ? void 0 : sunbeams.params) == null ? void 0 : _j.speed, (_k = sunbeams == null ? void 0 : sunbeams.params) == null ? void 0 : _k.spread, safeWidth, safeHeight]);
    React2.useEffect(() => {
      const intensity = clamp01((shimmer == null ? void 0 : shimmer.intensity) ?? 0);
      const canvas = shimmerCanvasRef.current;
      if (!canvas || intensity <= 0 || safeWidth <= 0 || safeHeight <= 0) return;
      const dpr = Math.max(1, Math.min(2, window.devicePixelRatio || 1));
      canvas.width = Math.floor(safeWidth * dpr);
      canvas.height = Math.floor(safeHeight * dpr);
      const ctx = canvas.getContext("2d");
      if (!ctx) return;
      ctx.setTransform(dpr, 0, 0, dpr, 0, 0);
      const baseColor = parseColor(shimmer == null ? void 0 : shimmer.color, { r: 255, g: 255, b: 255 });
      const shp = shimmer == null ? void 0 : shimmer.params;
      const shSpeedMul = 0.3 + ep(shp, "speed") * 1.4;
      const shDensityMul = 0.3 + ep(shp, "particleDensity") * 1.4;
      const shimmerSide = (shp == null ? void 0 : shp.shimmerSide) || "full";
      const shimmerDir = (shp == null ? void 0 : shp.shimmerDirection) || "up";
      const particlesOnly = !!(shp == null ? void 0 : shp.shimmerParticlesOnly);
      const noise = createNoise();
      let waveMinX = 0;
      let waveMaxX = safeWidth;
      if (shimmerSide === "left") {
        waveMaxX = safeWidth * 0.45;
      } else if (shimmerSide === "right") {
        waveMinX = safeWidth * 0.55;
      }
      const waveRegionW = waveMaxX - waveMinX;
      const waves = particlesOnly ? [] : Array.from({ length: 5 }).map((_, i) => ({
        phase: Math.random() * Math.PI * 2,
        speed: (0.4 + Math.random() * 0.6) * shSpeedMul,
        amplitude: 0.15 + Math.random() * 0.2,
        frequency: 0.5 + Math.random() * 1.5,
        yOffset: i / 5 * safeHeight,
        noiseOffset: Math.random() * 1e3,
        width: waveRegionW * (0.3 + Math.random() * 0.4)
      }));
      const pCount = Math.floor((20 + intensity * 30) * shDensityMul);
      const dirSign = shimmerDir === "down" ? 1 : -1;
      const particles = Array.from({ length: pCount }).map(() => ({
        x: Math.random() * safeWidth,
        y: Math.random() * safeHeight,
        vx: (-15 + Math.random() * 30) * shSpeedMul,
        vy: (5 + Math.random() * 15) * shSpeedMul * dirSign,
        size: 2 + Math.random() * 6,
        brightness: 0.3 + Math.random() * 0.7,
        pulsePhase: Math.random() * Math.PI * 2,
        pulseSpeed: 1 + Math.random() * 2
      }));
      let raf = 0;
      let time = 0;
      let last = performance.now();
      const draw = (now) => {
        const dt = Math.min(0.05, (now - last) / 1e3);
        last = now;
        time += dt;
        ctx.clearRect(0, 0, safeWidth, safeHeight);
        ctx.globalCompositeOperation = "lighter";
        for (const wave of waves) {
          wave.phase += dt * wave.speed;
          const noiseVal = noise(time * 0.2 + wave.noiseOffset);
          const xCenter = waveMinX + waveRegionW * 0.5;
          const xOffset = xCenter + (Math.sin(wave.phase) + noiseVal * 0.5) * waveRegionW * wave.amplitude;
          const yPos = wave.yOffset + Math.sin(time * 0.3 + wave.noiseOffset) * 50;
          const gradient = ctx.createLinearGradient(
            xOffset - wave.width / 2,
            yPos,
            xOffset + wave.width / 2,
            yPos
          );
          gradient.addColorStop(0, \`rgba(\${baseColor.r}, \${baseColor.g}, \${baseColor.b}, 0)\`);
          gradient.addColorStop(0.3, \`rgba(\${baseColor.r}, \${baseColor.g}, \${baseColor.b}, \${intensity * 0.08})\`);
          gradient.addColorStop(0.5, \`rgba(\${baseColor.r}, \${baseColor.g}, \${baseColor.b}, \${intensity * 0.15})\`);
          gradient.addColorStop(0.7, \`rgba(\${baseColor.r}, \${baseColor.g}, \${baseColor.b}, \${intensity * 0.08})\`);
          gradient.addColorStop(1, \`rgba(\${baseColor.r}, \${baseColor.g}, \${baseColor.b}, 0)\`);
          ctx.fillStyle = gradient;
          ctx.fillRect(xOffset - wave.width / 2, 0, wave.width, safeHeight);
        }
        for (const p of particles) {
          p.pulsePhase += dt * p.pulseSpeed;
          p.x += p.vx * dt;
          p.y += p.vy * dt;
          if (p.x < -20) p.x = safeWidth + 20;
          if (p.x > safeWidth + 20) p.x = -20;
          if (p.y < -20) p.y = safeHeight + 20;
          if (p.y > safeHeight + 20) p.y = -20;
          const pulse = 0.5 + Math.sin(p.pulsePhase) * 0.5;
          const alpha = p.brightness * pulse * intensity * 0.4;
          const gradient = ctx.createRadialGradient(p.x, p.y, 0, p.x, p.y, p.size * 2);
          gradient.addColorStop(0, \`rgba(\${baseColor.r}, \${baseColor.g}, \${baseColor.b}, \${alpha})\`);
          gradient.addColorStop(0.4, \`rgba(\${Math.floor(baseColor.r * 0.9)}, \${Math.floor(baseColor.g * 0.95)}, \${baseColor.b}, \${alpha * 0.6})\`);
          gradient.addColorStop(1, \`rgba(\${Math.floor(baseColor.r * 0.8)}, \${Math.floor(baseColor.g * 0.9)}, \${baseColor.b}, 0)\`);
          ctx.fillStyle = gradient;
          ctx.beginPath();
          ctx.arc(p.x, p.y, p.size * 2, 0, Math.PI * 2);
          ctx.fill();
        }
        raf = requestAnimationFrame(draw);
      };
      raf = requestAnimationFrame(draw);
      return () => cancelAnimationFrame(raf);
    }, [shimmer == null ? void 0 : shimmer.intensity, shimmer == null ? void 0 : shimmer.color, (_l = shimmer == null ? void 0 : shimmer.params) == null ? void 0 : _l.speed, (_m = shimmer == null ? void 0 : shimmer.params) == null ? void 0 : _m.particleDensity, (_n = shimmer == null ? void 0 : shimmer.params) == null ? void 0 : _n.shimmerSide, (_o = shimmer == null ? void 0 : shimmer.params) == null ? void 0 : _o.shimmerDirection, (_p = shimmer == null ? void 0 : shimmer.params) == null ? void 0 : _p.shimmerParticlesOnly, safeWidth, safeHeight]);
    React2.useEffect(() => {
      const canvas = fireworksCanvasRef.current;
      if (!canvas || !fireworks || clamp01(fireworks.intensity) <= 0 || safeWidth <= 0 || safeHeight <= 0) return;
      return runFireworksSim(canvas, safeWidth, safeHeight, {
        colors: fireworks.color ? [fireworks.color] : [],
        intensity: clamp01(fireworks.intensity),
        speedMul: 0.5 + ep(fireworks.params, "speed", 0.5) * 1.6,
        continuous: true
      });
    }, [fireworks == null ? void 0 : fireworks.intensity, fireworks == null ? void 0 : fireworks.color, (_q = fireworks == null ? void 0 : fireworks.params) == null ? void 0 : _q.speed, safeWidth, safeHeight]);
    const scanlinesOpacity = clamp01((scanlines == null ? void 0 : scanlines.intensity) ?? 0) * 0.65;
    const chromaOpacity = clamp01((chroma == null ? void 0 : chroma.intensity) ?? 0);
    const slLineSpacing = 2 + ep(scanlines == null ? void 0 : scanlines.params, "lineSpacing") * 6;
    const slSpeed = 2 + (1 - ep(scanlines == null ? void 0 : scanlines.params, "speed")) * 10;
    const cgSpread = 2 + ep(chroma == null ? void 0 : chroma.params, "chromaticSpread") * 8;
    const cgSpeed = 0.3 + (1 - ep(chroma == null ? void 0 : chroma.params, "speed")) * 1.4;
    const sunbeamsBlend = ((_r = sunbeams == null ? void 0 : sunbeams.params) == null ? void 0 : _r.blendMode) || "screen";
    const shimmerBlend = ((_s = shimmer == null ? void 0 : shimmer.params) == null ? void 0 : _s.blendMode) || "overlay";
    return /* @__PURE__ */ jsxRuntime2.jsxs("div", { className, children: [
      scanlinesOpacity > 0 && /* @__PURE__ */ jsxRuntime2.jsx(
        "div",
        {
          className: "vnfx-scanlines",
          style: {
            opacity: scanlinesOpacity,
            backgroundImage: \`repeating-linear-gradient(0deg, transparent, transparent \${slLineSpacing}px, rgba(0,0,0,0.4) \${slLineSpacing}px, rgba(0,0,0,0.4) \${slLineSpacing + 2}px)\`,
            animationDuration: \`\${slSpeed}s\`
          }
        }
      ),
      chromaOpacity > 0 && /* @__PURE__ */ jsxRuntime2.jsx(
        "div",
        {
          className: "vnfx-chromatic",
          style: {
            opacity: chromaOpacity,
            animationDuration: \`\${cgSpeed}s\`,
            backgroundImage: \`radial-gradient(ellipse at 20% 50%, rgba(255,0,0,0.15) 0%, transparent \${cgSpread * 4}%), radial-gradient(ellipse at 80% 50%, rgba(0,255,255,0.12) 0%, transparent \${cgSpread * 4}%), repeating-linear-gradient(0deg, transparent 0px, transparent \${cgSpread}px, rgba(255,255,255,0.03) \${cgSpread}px, rgba(255,255,255,0.03) \${cgSpread + 1}px)\`
          }
        }
      ),
      sunbeams && clamp01(sunbeams.intensity) > 0 && /* @__PURE__ */ jsxRuntime2.jsx(
        "canvas",
        {
          ref: sunbeamsCanvasRef,
          className: "vnfx-canvas",
          style: { mixBlendMode: sunbeamsBlend },
          "aria-hidden": true
        }
      ),
      shimmer && clamp01(shimmer.intensity) > 0 && /* @__PURE__ */ jsxRuntime2.jsx(
        "canvas",
        {
          ref: shimmerCanvasRef,
          className: "vnfx-canvas",
          style: { mixBlendMode: shimmerBlend },
          "aria-hidden": true
        }
      ),
      rain && clamp01(rain.intensity) > 0 && /* @__PURE__ */ jsxRuntime2.jsx(
        "canvas",
        {
          ref: rainCanvasRef,
          className: "vnfx-canvas",
          "aria-hidden": true
        }
      ),
      snowAsh && clamp01(snowAsh.intensity) > 0 && /* @__PURE__ */ jsxRuntime2.jsx(
        "canvas",
        {
          ref: snowCanvasRef,
          className: "vnfx-canvas",
          "aria-hidden": true
        }
      ),
      haze && clamp01(haze.intensity) > 0 && /* @__PURE__ */ jsxRuntime2.jsx("canvas", { ref: hazeCanvasRef, className: "vnfx-canvas", "aria-hidden": true }),
      fog && clamp01(fog.intensity) > 0 && /* @__PURE__ */ jsxRuntime2.jsx("canvas", { ref: fogCanvasRef, className: "vnfx-canvas", "aria-hidden": true }),
      smoke && clamp01(smoke.intensity) > 0 && /* @__PURE__ */ jsxRuntime2.jsx("canvas", { ref: smokeCanvasRef, className: "vnfx-canvas", "aria-hidden": true }),
      fireworks && clamp01(fireworks.intensity) > 0 && /* @__PURE__ */ jsxRuntime2.jsx("canvas", { ref: fireworksCanvasRef, className: "vnfx-canvas", style: { mixBlendMode: "screen" }, "aria-hidden": true }),
      pluginEffects.map(({ effect, def }) => /* @__PURE__ */ jsxRuntime2.jsx(
        PluginEffectCanvas,
        {
          def,
          effect,
          width: safeWidth,
          height: safeHeight
        },
        effect.id || def.type
      ))
    ] });
  };
  const PARTICLE_PRESETS = {
    fireflies: {
      shape: "circle",
      colors: ["#FFFF66", "#CCFF33", "#FFCC00"],
      emitRate: 8,
      lifetime: 4,
      speedMin: 5,
      speedMax: 20,
      sizeMin: 2,
      sizeMax: 5,
      gravity: -2,
      wind: 0,
      directionMin: 0,
      directionMax: 360,
      emitterX: 50,
      emitterY: 50,
      emitterWidth: 100,
      emitterHeight: 100,
      fadeOut: true,
      shrink: false,
      rotationSpeed: 0,
      opacity: 0.9,
      blendMode: "lighter"
    },
    sparks: {
      shape: "circle",
      colors: ["#FF6600", "#FFAA00", "#FFCC44", "#FF4400"],
      emitRate: 30,
      lifetime: 1.5,
      speedMin: 50,
      speedMax: 150,
      sizeMin: 1,
      sizeMax: 4,
      gravity: 40,
      wind: 0,
      directionMin: 45,
      directionMax: 135,
      emitterX: 50,
      emitterY: 70,
      emitterWidth: 60,
      emitterHeight: 5,
      fadeOut: true,
      shrink: true,
      rotationSpeed: 0,
      opacity: 1,
      blendMode: "lighter"
    },
    bubbles: {
      shape: "circle",
      colors: ["rgba(150,220,255,0.4)", "rgba(200,240,255,0.3)", "rgba(180,230,255,0.5)"],
      emitRate: 10,
      lifetime: 5,
      speedMin: 15,
      speedMax: 40,
      sizeMin: 4,
      sizeMax: 15,
      gravity: -15,
      wind: 3,
      directionMin: 70,
      directionMax: 110,
      emitterX: 50,
      emitterY: 100,
      emitterWidth: 80,
      emitterHeight: 5,
      fadeOut: true,
      shrink: false,
      rotationSpeed: 0,
      opacity: 0.6,
      blendMode: "source-over"
    },
    confetti: {
      shape: "square",
      colors: ["#FF3366", "#33CCFF", "#FFCC00", "#66FF66", "#CC66FF", "#FF6633"],
      emitRate: 40,
      lifetime: 4,
      speedMin: 30,
      speedMax: 100,
      sizeMin: 4,
      sizeMax: 10,
      gravity: 30,
      wind: 5,
      directionMin: 45,
      directionMax: 135,
      emitterX: 50,
      emitterY: 0,
      emitterWidth: 100,
      emitterHeight: 5,
      fadeOut: false,
      shrink: false,
      rotationSpeed: 180,
      opacity: 0.9,
      blendMode: "source-over"
    },
    embers: {
      shape: "circle",
      colors: ["#FF4400", "#FF6600", "#FF8800", "#FFAA00"],
      emitRate: 15,
      lifetime: 3,
      speedMin: 10,
      speedMax: 40,
      sizeMin: 1,
      sizeMax: 4,
      gravity: -20,
      wind: 8,
      directionMin: 60,
      directionMax: 120,
      emitterX: 50,
      emitterY: 100,
      emitterWidth: 80,
      emitterHeight: 10,
      fadeOut: true,
      shrink: true,
      rotationSpeed: 0,
      opacity: 0.8,
      blendMode: "lighter"
    },
    dust: {
      shape: "circle",
      colors: ["rgba(200,180,150,0.4)", "rgba(180,160,130,0.3)", "rgba(220,200,170,0.5)"],
      emitRate: 12,
      lifetime: 6,
      speedMin: 3,
      speedMax: 12,
      sizeMin: 1,
      sizeMax: 4,
      gravity: 2,
      wind: 5,
      directionMin: 0,
      directionMax: 360,
      emitterX: 50,
      emitterY: 50,
      emitterWidth: 100,
      emitterHeight: 100,
      fadeOut: true,
      shrink: false,
      rotationSpeed: 0,
      opacity: 0.5,
      blendMode: "source-over"
    },
    petals: {
      shape: "heart",
      colors: ["#FFAABB", "#FF88AA", "#FFCCDD", "#FF99BB"],
      emitRate: 8,
      lifetime: 6,
      speedMin: 10,
      speedMax: 30,
      sizeMin: 5,
      sizeMax: 12,
      gravity: 10,
      wind: 15,
      directionMin: 200,
      directionMax: 280,
      emitterX: 50,
      emitterY: 0,
      emitterWidth: 100,
      emitterHeight: 5,
      fadeOut: true,
      shrink: false,
      rotationSpeed: 90,
      opacity: 0.8,
      blendMode: "source-over"
    },
    magic: {
      shape: "sparkle",
      colors: ["#AA66FF", "#6699FF", "#FF66CC", "#66FFCC", "#FFFFFF"],
      emitRate: 20,
      lifetime: 2,
      speedMin: 20,
      speedMax: 60,
      sizeMin: 2,
      sizeMax: 8,
      gravity: -5,
      wind: 0,
      directionMin: 0,
      directionMax: 360,
      emitterX: 50,
      emitterY: 50,
      emitterWidth: 60,
      emitterHeight: 60,
      fadeOut: true,
      shrink: true,
      rotationSpeed: 120,
      opacity: 0.9,
      blendMode: "lighter"
    },
    stars: {
      shape: "star",
      colors: ["#FFFFFF", "#FFFFCC", "#CCCCFF", "#FFCCFF"],
      emitRate: 5,
      lifetime: 3,
      speedMin: 5,
      speedMax: 15,
      sizeMin: 3,
      sizeMax: 8,
      gravity: 0,
      wind: 0,
      directionMin: 0,
      directionMax: 360,
      emitterX: 50,
      emitterY: 50,
      emitterWidth: 100,
      emitterHeight: 100,
      fadeOut: true,
      shrink: false,
      rotationSpeed: 30,
      opacity: 0.8,
      blendMode: "lighter"
    }
  };
  function resolveConfig(config) {
    let resolved = config;
    if (config.preset && config.preset !== "none" && PARTICLE_PRESETS[config.preset]) {
      resolved = { ...PARTICLE_PRESETS[config.preset], ...config };
    }
    return {
      ...resolved,
      emitRate: resolved.emitRate ?? 10,
      lifetime: resolved.lifetime ?? 3,
      speedMin: resolved.speedMin ?? 5,
      speedMax: resolved.speedMax ?? 30,
      sizeMin: resolved.sizeMin ?? 2,
      sizeMax: resolved.sizeMax ?? 8,
      gravity: resolved.gravity ?? 0,
      wind: resolved.wind ?? 0,
      directionMin: resolved.directionMin ?? 0,
      directionMax: resolved.directionMax ?? 360,
      emitterX: resolved.emitterX ?? 50,
      emitterY: resolved.emitterY ?? 50,
      emitterWidth: resolved.emitterWidth ?? 100,
      emitterHeight: resolved.emitterHeight ?? 100,
      opacity: resolved.opacity ?? 1,
      rotationSpeed: resolved.rotationSpeed ?? 0,
      fadeOut: resolved.fadeOut ?? true,
      shrink: resolved.shrink ?? false,
      shape: resolved.shape || "circle",
      colors: resolved.colors && resolved.colors.length > 0 ? resolved.colors : ["#FFFFFF"]
    };
  }
  function drawShape(ctx, shape, x, y, size, rotation, color, opacity) {
    ctx.save();
    ctx.translate(x, y);
    ctx.rotate(rotation * Math.PI / 180);
    ctx.globalAlpha = opacity;
    ctx.fillStyle = color;
    switch (shape) {
      case "circle":
        ctx.beginPath();
        ctx.arc(0, 0, size / 2, 0, Math.PI * 2);
        ctx.fill();
        break;
      case "square":
        ctx.fillRect(-size / 2, -size / 2, size, size);
        break;
      case "star": {
        const spikes = 5;
        const outerRadius = size / 2;
        const innerRadius = size / 4;
        ctx.beginPath();
        for (let i = 0; i < spikes * 2; i++) {
          const r = i % 2 === 0 ? outerRadius : innerRadius;
          const angle = i * Math.PI / spikes - Math.PI / 2;
          if (i === 0) ctx.moveTo(Math.cos(angle) * r, Math.sin(angle) * r);
          else ctx.lineTo(Math.cos(angle) * r, Math.sin(angle) * r);
        }
        ctx.closePath();
        ctx.fill();
        break;
      }
      case "heart": {
        const s = size / 2;
        ctx.beginPath();
        ctx.moveTo(0, s * 0.4);
        ctx.bezierCurveTo(-s, -s * 0.3, -s * 0.5, -s, 0, -s * 0.4);
        ctx.bezierCurveTo(s * 0.5, -s, s, -s * 0.3, 0, s * 0.4);
        ctx.fill();
        break;
      }
      case "sparkle": {
        const r = size / 2;
        ctx.beginPath();
        for (let i = 0; i < 4; i++) {
          const angle = i * Math.PI / 2;
          ctx.moveTo(0, 0);
          ctx.lineTo(Math.cos(angle) * r, Math.sin(angle) * r);
          ctx.lineTo(Math.cos(angle + Math.PI / 4) * r * 0.3, Math.sin(angle + Math.PI / 4) * r * 0.3);
        }
        ctx.closePath();
        ctx.fill();
        break;
      }
      default:
        ctx.beginPath();
        ctx.arc(0, 0, size / 2, 0, Math.PI * 2);
        ctx.fill();
    }
    ctx.restore();
  }
  const ParticleSystem = ({
    effects,
    width,
    height,
    className
  }) => {
    const canvasRef = React2.useRef(null);
    const emittersRef = React2.useRef(/* @__PURE__ */ new Map());
    const animFrameRef = React2.useRef(0);
    const debugLoggedRef = React2.useRef(false);
    const safeWidth = Math.max(0, Math.min(width, 4096));
    const safeHeight = Math.max(0, Math.min(height, 4096));
    const effectKeys = React2.useMemo(() => Object.keys(effects).sort().join(","), [effects]);
    if (!debugLoggedRef.current && safeWidth > 0 && safeHeight > 0) {
      console.log("[ParticleSystem] Mounted with", Object.keys(effects).length, "effects, dimensions:", safeWidth, "x", safeHeight);
      debugLoggedRef.current = true;
    }
    React2.useEffect(() => {
      var _a;
      const currentTags = new Set(Object.keys(effects));
      const emitters = emittersRef.current;
      for (const tag of emitters.keys()) {
        if (!currentTags.has(tag)) {
          emitters.delete(tag);
        }
      }
      for (const [tag, effect] of Object.entries(effects)) {
        const existing = emitters.get(tag);
        if (existing) {
          existing.config = resolveConfig(effect.config);
          existing.fadingOut = !!effect.fadingOut;
          existing.fadeOutDuration = effect.fadeOutDuration || 1;
        } else {
          emitters.set(tag, {
            tag,
            config: resolveConfig(effect.config),
            particles: [],
            accumulatedEmit: 0,
            fadingOut: !!effect.fadingOut,
            fadeOpacity: 1,
            fadeOutDuration: effect.fadeOutDuration || 1
          });
          console.log("[ParticleSystem] Added emitter:", tag, "preset:", (_a = effect.config) == null ? void 0 : _a.preset, "emitRate:", resolveConfig(effect.config).emitRate);
        }
      }
    }, [effectKeys, effects]);
    React2.useEffect(() => {
      const canvas = canvasRef.current;
      if (!canvas || safeWidth <= 0 || safeHeight <= 0) {
        console.log("[ParticleSystem] Animation loop skipped: canvas=", !!canvas, "dimensions:", safeWidth, "x", safeHeight);
        return;
      }
      const dpr = Math.max(1, Math.min(2, window.devicePixelRatio || 1));
      canvas.width = Math.floor(safeWidth * dpr);
      canvas.height = Math.floor(safeHeight * dpr);
      const ctx = canvas.getContext("2d");
      if (!ctx) {
        console.error("[ParticleSystem] Failed to get 2D context");
        return;
      }
      ctx.setTransform(dpr, 0, 0, dpr, 0, 0);
      let lastTime = performance.now();
      let frameCount = 0;
      console.log("[ParticleSystem] Animation loop started. Canvas:", canvas.width, "x", canvas.height, "DPR:", dpr);
      const loop = (now) => {
        try {
          const dt = Math.min((now - lastTime) / 1e3, 0.1);
          lastTime = now;
          frameCount++;
          ctx.clearRect(0, 0, safeWidth, safeHeight);
          const emitters = emittersRef.current;
          if (frameCount % 60 === 1) {
            let totalParticles = 0;
            for (const em of emitters.values()) {
              totalParticles += em.particles.length;
            }
            if (emitters.size > 0 || totalParticles > 0) {
              console.log("[ParticleSystem] Frame", frameCount, "| Emitters:", emitters.size, "| Total particles:", totalParticles);
            }
          }
          for (const emitter of emitters.values()) {
            const config = emitter.config;
            const blendMode = config.blendMode || "source-over";
            if (emitter.fadingOut) {
              emitter.fadeOpacity = Math.max(0, emitter.fadeOpacity - dt / emitter.fadeOutDuration);
            }
            if (!emitter.fadingOut) {
              emitter.accumulatedEmit += (config.emitRate || 10) * dt;
              while (emitter.accumulatedEmit >= 1) {
                emitter.accumulatedEmit -= 1;
                const angle = config.directionMin + Math.random() * (config.directionMax - config.directionMin);
                const rad = angle * Math.PI / 180;
                const speed = config.speedMin + Math.random() * (config.speedMax - config.speedMin);
                const emitX = (config.emitterX - config.emitterWidth / 2 + Math.random() * config.emitterWidth) / 100 * safeWidth;
                const emitY = (config.emitterY - config.emitterHeight / 2 + Math.random() * config.emitterHeight) / 100 * safeHeight;
                emitter.particles.push({
                  x: emitX,
                  y: emitY,
                  vx: Math.cos(rad) * speed,
                  vy: -Math.sin(rad) * speed,
                  size: config.sizeMin + Math.random() * (config.sizeMax - config.sizeMin),
                  color: config.colors[Math.floor(Math.random() * config.colors.length)] || "#FFFFFF",
                  life: config.lifetime,
                  maxLife: config.lifetime,
                  rotation: Math.random() * 360,
                  rotationSpeed: config.rotationSpeed * (Math.random() > 0.5 ? 1 : -1),
                  shape: config.shape || "circle",
                  opacity: config.opacity ?? 1
                });
              }
            }
            ctx.globalCompositeOperation = blendMode;
            emitter.particles = emitter.particles.filter((p) => {
              p.life -= dt;
              if (p.life <= 0) return false;
              p.vy += (config.gravity || 0) * dt;
              p.vx += (config.wind || 0) * dt;
              p.x += p.vx * dt;
              p.y += p.vy * dt;
              p.rotation += p.rotationSpeed * dt;
              const lifeRatio = p.life / p.maxLife;
              let opacity = p.opacity * emitter.fadeOpacity;
              let size = p.size;
              if (config.fadeOut) {
                opacity *= lifeRatio;
              }
              if (config.shrink) {
                size *= lifeRatio;
              }
              if (opacity > 0.01 && size > 0.1) {
                drawShape(ctx, p.shape, p.x, p.y, size, p.rotation, p.color, opacity);
              }
              return true;
            });
            ctx.globalCompositeOperation = "source-over";
          }
        } catch (err) {
          console.error("[ParticleSystem] Error in animation loop:", err);
        }
        animFrameRef.current = requestAnimationFrame(loop);
      };
      animFrameRef.current = requestAnimationFrame(loop);
      return () => {
        if (animFrameRef.current) cancelAnimationFrame(animFrameRef.current);
      };
    }, [safeWidth, safeHeight]);
    if (safeWidth <= 0 || safeHeight <= 0 || Object.keys(effects).length === 0) return null;
    return /* @__PURE__ */ jsxRuntime2.jsx(
      "canvas",
      {
        ref: canvasRef,
        className,
        style: {
          position: "absolute",
          inset: 0,
          width: safeWidth,
          height: safeHeight,
          pointerEvents: "none",
          zIndex: 6
          // above characters (z-5), below dialogue (z-20)
        }
      }
    );
  };
  const targets = /* @__PURE__ */ new Map();
  let seq = 1;
  function registerDropTarget(target) {
    const order = target.order ?? seq++;
    const entry = { ...target, order };
    targets.set(entry.id, entry);
    return () => {
      if (targets.get(entry.id) === entry) targets.delete(entry.id);
    };
  }
  function hitTestDropTarget(point, draggedId, draggedTag) {
    let best = null;
    for (const t of targets.values()) {
      if (t.acceptedElementIds && t.acceptedElementIds.length > 0 && !t.acceptedElementIds.includes(draggedId)) continue;
      if (t.acceptTag && t.acceptTag !== draggedTag) continue;
      const { x, y, width, height } = t.rectPct;
      if (point.x >= x && point.x <= x + width && point.y >= y && point.y <= y + height) {
        if (!best || t.order > best.order) best = t;
      }
    }
    return best;
  }
  function revealHighlightStyle(hl) {
    switch (hl.style) {
      case "glow":
        return { textShadow: \`0 0 8px \${hl.color}, 0 0 14px \${hl.color}\` };
      case "underline":
        return { textDecoration: "underline", textDecorationColor: hl.color, textUnderlineOffset: "3px", textDecorationThickness: "2px" };
      case "color":
      default:
        return { color: hl.color, WebkitTextFillColor: hl.color };
    }
  }
  const TEXT_EFFECT_KEYFRAMES = \`
@keyframes vnTextShake {
    0%, 100% { transform: translate(0, 0); }
    10% { transform: translate(-1px, -1px); }
    20% { transform: translate(1px, 0); }
    30% { transform: translate(-1px, 1px); }
    40% { transform: translate(1px, -1px); }
    50% { transform: translate(-1px, 0px); }
    60% { transform: translate(1px, 1px); }
    70% { transform: translate(0, -1px); }
    80% { transform: translate(-1px, 1px); }
    90% { transform: translate(1px, 0); }
}

@keyframes vnTextWave {
    0%, 100% { transform: translateY(0); }
    50% { transform: translateY(var(--wave-amplitude, -4px)); }
}

@keyframes vnTextPulse {
    0%, 100% { transform: scale(1); opacity: 1; }
    50% { transform: scale(var(--pulse-scale, 1.15)); opacity: 0.85; }
}

@keyframes vnTextBounce {
    0%, 100% { transform: translateY(0); }
    30% { transform: translateY(var(--bounce-height, -6px)); }
    50% { transform: translateY(0); }
    70% { transform: translateY(var(--bounce-height-small, -3px)); }
}

@keyframes vnTextFadeIn {
    0% { opacity: 0; transform: translateY(4px); }
    100% { opacity: 1; transform: translateY(0); }
}

@keyframes vnTextGlitch {
    0%, 100% { transform: translate(0, 0) skew(0deg); opacity: 1; }
    7% { transform: translate(-2px, -1px) skew(-2deg); }
    10% { transform: translate(2px, 1px) skew(2deg); opacity: 0.8; }
    15% { transform: translate(-1px, 2px) skew(-1deg); }
    20% { transform: translate(0, 0) skew(0deg); opacity: 1; }
}

@keyframes vnTextTypewriterBounce {
    0% { transform: scale(0.5) translateY(8px); opacity: 0; }
    60% { transform: scale(1.1) translateY(-2px); opacity: 1; }
    80% { transform: scale(0.95) translateY(1px); }
    100% { transform: scale(1) translateY(0); opacity: 1; }
}
\`;
  let stylesInjected = false;
  function injectTextEffectStyles() {
    if (stylesInjected) return;
    stylesInjected = true;
    const style = document.createElement("style");
    style.setAttribute("data-vn-text-effects", "true");
    style.textContent = TEXT_EFFECT_KEYFRAMES;
    document.head.appendChild(style);
  }
  function getRainbowColor(index, speed) {
    const hue = (index * 30 + Date.now() * speed * 0.05) % 360;
    return \`hsl(\${hue}, 85%, 65%)\`;
  }
  function getCharacterStyle(effect, charIndex, totalChars) {
    const speed = effect.speed ?? 1;
    const intensity = effect.intensity ?? 1;
    const delay = charIndex * 0.05 / speed;
    switch (effect.type) {
      case "shake":
        return {
          display: "inline-block",
          animation: \`vnTextShake \${0.3 / speed}s ease-in-out infinite\`,
          animationDelay: \`\${delay}s\`,
          "--shake-intensity": \`\${intensity}px\`
        };
      case "wave":
        return {
          display: "inline-block",
          animation: \`vnTextWave \${0.8 / speed}s ease-in-out infinite\`,
          animationDelay: \`\${delay}s\`,
          "--wave-amplitude": \`\${-4 * intensity}px\`
        };
      case "rainbow":
        return {
          display: "inline-block",
          color: getRainbowColor(charIndex, speed),
          transition: "color 0.1s"
        };
      case "glitch":
        return {
          display: "inline-block",
          animation: \`vnTextGlitch \${0.5 / speed}s ease-in-out infinite\`,
          animationDelay: \`\${charIndex * 0.1}s\`
        };
      case "pulse":
        return {
          display: "inline-block",
          animation: \`vnTextPulse \${1 / speed}s ease-in-out infinite\`,
          animationDelay: \`\${delay}s\`,
          "--pulse-scale": \`\${1 + 0.15 * intensity}\`
        };
      case "fade-in":
        return {
          display: "inline-block",
          animation: \`vnTextFadeIn \${0.4 / speed}s ease-out forwards\`,
          animationDelay: \`\${charIndex * 0.03 / speed}s\`,
          opacity: 0
        };
      case "bounce":
        return {
          display: "inline-block",
          animation: \`vnTextBounce \${0.6 / speed}s ease-in-out infinite\`,
          animationDelay: \`\${delay}s\`,
          "--bounce-height": \`\${-6 * intensity}px\`,
          "--bounce-height-small": \`\${-3 * intensity}px\`
        };
      case "typewriter-bounce":
        return {
          display: "inline-block",
          animation: \`vnTextTypewriterBounce \${0.3 / speed}s ease-out forwards\`,
          animationDelay: \`0s\`
        };
      default:
        return {};
    }
  }
  const AnimatedDialogueText = ({
    displayText,
    textEffect,
    textStyle,
    gradientStyle,
    revealHighlight
  }) => {
    React2.useMemo(() => {
      injectTextEffectStyles();
    }, []);
    if (!textEffect || textEffect.type === "none") {
      if (revealHighlight && revealHighlight.start < displayText.length) {
        const hlEnd = Math.min(revealHighlight.end ?? displayText.length, displayText.length);
        const before = displayText.slice(0, revealHighlight.start);
        const word = displayText.slice(revealHighlight.start, hlEnd);
        const after = displayText.slice(hlEnd);
        return /* @__PURE__ */ jsxRuntime2.jsxs("span", { style: gradientStyle || void 0, children: [
          before,
          /* @__PURE__ */ jsxRuntime2.jsx("span", { style: revealHighlightStyle(revealHighlight), children: word }),
          after
        ] });
      }
      return /* @__PURE__ */ jsxRuntime2.jsx("span", { style: gradientStyle || void 0, children: displayText });
    }
    const tokens = displayText.split(/(\\s+)/);
    displayText.length;
    let charIndex = 0;
    return /* @__PURE__ */ jsxRuntime2.jsx("span", { style: gradientStyle || void 0, children: tokens.map((token, ti) => {
      if (token === "") return null;
      if (/^\\s+\$/.test(token)) {
        charIndex += token.length;
        return /* @__PURE__ */ jsxRuntime2.jsx("span", { children: token }, ti);
      }
      const wordStart = charIndex;
      charIndex += token.length;
      return /* @__PURE__ */ jsxRuntime2.jsx("span", { style: { display: "inline-block", whiteSpace: "nowrap" }, children: token.split("").map((char, ci) => {
        const charStyle = getCharacterStyle(textEffect, wordStart + ci);
        const gi = wordStart + ci;
        const hlStyle = revealHighlight && gi >= revealHighlight.start && gi < (revealHighlight.end ?? displayText.length) ? revealHighlightStyle(revealHighlight) : void 0;
        return /* @__PURE__ */ jsxRuntime2.jsx(
          "span",
          {
            style: {
              ...charStyle,
              // Preserve any gradient styling
              ...gradientStyle ? {
                WebkitBackgroundClip: void 0,
                backgroundClip: void 0,
                WebkitTextFillColor: void 0
              } : {},
              ...hlStyle || {}
            },
            children: char
          },
          ci
        );
      }) }, ti);
    }) });
  };
  const MAX_ANALYZE_BYTES = 6 * 1024 * 1024;
  let sharedCtx = null;
  const getCtx = () => {
    try {
      if (!sharedCtx) sharedCtx = new (window.AudioContext || window.webkitAudioContext)();
      return sharedCtx;
    } catch {
      return null;
    }
  };
  const HOP_MS = 10;
  const BRIDGE_MS = 140;
  const MIN_SEG_MS = 60;
  function findSpeechSegments(env) {
    let peak = 0;
    for (const v of env) peak = Math.max(peak, v);
    if (peak <= 0) return [];
    const thr = Math.max(peak * 0.12, 8e-3);
    const segs = [];
    let segStart = -1;
    for (let i = 0; i <= env.length; i++) {
      const loud = i < env.length && env[i] >= thr;
      if (loud && segStart < 0) segStart = i * HOP_MS;
      if (!loud && segStart >= 0) {
        segs.push({ start: segStart, end: i * HOP_MS });
        segStart = -1;
      }
    }
    const merged = [];
    for (const s of segs) {
      const last = merged[merged.length - 1];
      if (last && s.start - last.end <= BRIDGE_MS) last.end = s.end;
      else merged.push({ ...s });
    }
    return merged.filter((s) => s.end - s.start >= MIN_SEG_MS);
  }
  function wavEnvelope(buf) {
    try {
      const dv = new DataView(buf);
      if (buf.byteLength < 44) return null;
      if (dv.getUint32(0, false) !== 1380533830 || dv.getUint32(8, false) !== 1463899717) return null;
      let pos = 12;
      let fmt = null;
      let dataStart = -1, dataLen = 0;
      while (pos + 8 <= buf.byteLength) {
        const id = dv.getUint32(pos, false);
        const size = dv.getUint32(pos + 4, true);
        if (id === 1718449184 && size >= 16) {
          fmt = {
            codec: dv.getUint16(pos + 8, true),
            channels: Math.max(1, dv.getUint16(pos + 10, true)),
            rate: dv.getUint32(pos + 12, true),
            bits: dv.getUint16(pos + 22, true)
          };
          if (fmt.codec === 65534 && size >= 40) fmt.codec = dv.getUint16(pos + 32, true);
        } else if (id === 1684108385) {
          dataStart = pos + 8;
          dataLen = Math.min(size, buf.byteLength - dataStart);
        }
        pos += 8 + size + (size & 1);
      }
      if (!fmt || dataStart < 0 || dataLen <= 0 || fmt.rate < 4e3 || fmt.rate > 384e3) return null;
      const pcm16 = fmt.codec === 1 && fmt.bits === 16;
      const pcm8 = fmt.codec === 1 && fmt.bits === 8;
      const f32 = fmt.codec === 3 && fmt.bits === 32;
      if (!pcm16 && !pcm8 && !f32) return null;
      const bytesPer = fmt.bits / 8;
      const frameBytes = bytesPer * fmt.channels;
      const frames = Math.floor(dataLen / frameBytes);
      const hopFrames = Math.max(1, Math.round(fmt.rate * HOP_MS / 1e3));
      const env = new Float32Array(Math.ceil(frames / hopFrames));
      for (let e = 0; e < env.length; e++) {
        let sum = 0;
        const from = e * hopFrames, to = Math.min(frames, from + hopFrames);
        for (let f = from; f < to; f++) {
          const off = dataStart + f * frameBytes;
          const v = pcm16 ? dv.getInt16(off, true) / 32768 : pcm8 ? (dv.getUint8(off) - 128) / 128 : dv.getFloat32(off, true);
          sum += v * v;
        }
        env[e] = Math.sqrt(sum / Math.max(1, to - from));
      }
      return env;
    } catch {
      return null;
    }
  }
  let decodeChain = Promise.resolve();
  const clipCache = /* @__PURE__ */ new Map();
  function getClipSpeech(url) {
    const hit = clipCache.get(url);
    if (hit) return hit;
    const job = decodeChain.then(async () => {
      try {
        const res = await fetch(url);
        const buf = await res.arrayBuffer();
        if (buf.byteLength > MAX_ANALYZE_BYTES) return null;
        let env = wavEnvelope(buf);
        const isRiff = buf.byteLength >= 4 && new DataView(buf).getUint32(0, false) === 1380533830;
        if (!env && isRiff) return null;
        if (!env) {
          const ctx = getCtx();
          if (!ctx) return null;
          const audio = await ctx.decodeAudioData(buf);
          const ch = audio.getChannelData(0);
          const hop = Math.max(1, Math.round(audio.sampleRate * HOP_MS / 1e3));
          env = new Float32Array(Math.ceil(ch.length / hop));
          for (let i = 0; i < env.length; i++) {
            let sum = 0;
            const from = i * hop, to = Math.min(ch.length, from + hop);
            for (let j = from; j < to; j++) sum += ch[j] * ch[j];
            env[i] = Math.sqrt(sum / Math.max(1, to - from));
          }
        }
        const segs = findSpeechSegments(env);
        if (!segs.length) return null;
        return { segs, totalSpeech: segs.reduce((a, s) => a + (s.end - s.start), 0) };
      } catch {
        return null;
      }
    });
    decodeChain = job.catch(() => null);
    clipCache.set(url, job);
    return job;
  }
  function analyzeVoiceWordStarts(url, weights) {
    if (!url || !weights.length) return Promise.resolve(null);
    return getClipSpeech(url).then((clip) => {
      if (!clip) return null;
      const { segs, totalSpeech } = clip;
      const totalWeight = weights.reduce((a, w) => a + w, 0) || 1;
      const starts = [];
      let segIdx = 0;
      let posInSeg = 0;
      for (const w of weights) {
        const seg = segs[Math.min(segIdx, segs.length - 1)];
        starts.push(seg.start + posInSeg);
        let need = w / totalWeight * totalSpeech;
        while (need > 0 && segIdx < segs.length) {
          const s = segs[segIdx];
          const room = s.end - s.start - posInSeg;
          if (need < room) {
            posInSeg += need;
            need = 0;
          } else {
            need -= room;
            segIdx++;
            posInSeg = 0;
          }
        }
      }
      return starts;
    });
  }
  const normalizeSetVariableOperator = (variableType, variableName, operator) => {
    if ((operator === "add" || operator === "subtract") && variableType !== "number") {
      console.warn(
        \`[SetVariable] Operator "\${operator}" is not valid for \${variableType} variable "\${variableName}". Forcing operator to "set".\`
      );
      return "set";
    }
    if (operator === "random" && variableType !== "number") {
      console.warn(
        \`[SetVariable] Operator "\${operator}" is not valid for \${variableType} variable "\${variableName}". Forcing operator to "set".\`
      );
      return "set";
    }
    return operator;
  };
  const normalizeSetVariableOperatorByType = (variableType, variableName, operator) => {
    const effectiveOperator = normalizeSetVariableOperator(variableType, variableName, operator);
    return {
      effectiveOperator,
      wasCoerced: effectiveOperator !== operator
    };
  };
  const toNumeric = (value) => {
    if (typeof value === "number" && Number.isFinite(value)) {
      return value;
    }
    const parsed = Number(value);
    return Number.isFinite(parsed) ? parsed : 0;
  };
  const coerceValueToType = (value, variableType, currentValue) => {
    const changeValStr = String(value);
    switch (variableType) {
      case "number":
        return toNumeric(changeValStr);
      case "boolean":
        if (typeof value === "boolean") {
          return value;
        }
        const normalized = changeValStr.trim().toLowerCase();
        if (normalized === "" || normalized === "undefined" || normalized === "null") {
          return true;
        }
        if (normalized === "true" || normalized === "1" || normalized === "yes") {
          return true;
        }
        if (normalized === "false" || normalized === "0" || normalized === "no") {
          return false;
        }
        return !!value;
      case "string":
      default:
        return changeValStr;
    }
  };
  const clampNumberToBounds = (value, min, max) => {
    let v = value;
    if (typeof min === "number" && Number.isFinite(min) && v < min) v = min;
    if (typeof max === "number" && Number.isFinite(max) && v > max) v = max;
    return v;
  };
  const calculateVariableValue = (operator, variableType, currentValue, changeValue, randomMin, randomMax, originalOperator, boundMin, boundMax) => {
    const changeValStr = String(changeValue);
    const finish = (result) => typeof result === "number" ? clampNumberToBounds(result, boundMin, boundMax) : result;
    switch (operator) {
      case "add":
        return finish(toNumeric(currentValue) + toNumeric(changeValStr));
      case "subtract":
        return finish(toNumeric(currentValue) - toNumeric(changeValStr));
      case "random": {
        const min = randomMin ?? 0;
        const max = randomMax ?? 100;
        return finish(Math.floor(Math.random() * (max - min + 1)) + min);
      }
      case "set":
      default:
        if (variableType === "boolean" && originalOperator) {
          if (originalOperator === "add") {
            console.log("[Boolean Promotion] Normalized add -> set TRUE");
            return true;
          }
          if (originalOperator === "subtract") {
            console.log("[Boolean Promotion] Normalized subtract -> set FALSE");
            return false;
          }
          if (originalOperator === "random") {
            const randomVal = Math.random() >= 0.5;
            console.log("[Boolean Promotion] Normalized random -> set", randomVal);
            return randomVal;
          }
        }
        return finish(coerceValueToType(changeValue, variableType));
    }
  };
  const PRESET_X = {
    "left": 25,
    "center": 50,
    "right": 75,
    "off-left": -25,
    "off-right": 125
  };
  const SPREAD = 15;
  const MIN_X = 5;
  const MAX_X = 95;
  function computeArrangedPositions(characters) {
    const groups = /* @__PURE__ */ new Map();
    for (const char of characters) {
      if (typeof char.position === "string") {
        const preset = char.position;
        if (preset === "off-left" || preset === "off-right") continue;
        let arr = groups.get(preset);
        if (!arr) {
          arr = [];
          groups.set(preset, arr);
        }
        arr.push(char.id);
      }
    }
    const result = /* @__PURE__ */ new Map();
    for (const [preset, ids] of groups) {
      if (ids.length <= 1) continue;
      const baseX = PRESET_X[preset];
      const count = ids.length;
      const totalWidth = (count - 1) * SPREAD;
      const startX = baseX - totalWidth / 2;
      for (let i = 0; i < count; i++) {
        const rawX = startX + i * SPREAD;
        const clampedX = Math.max(MIN_X, Math.min(MAX_X, rawX));
        result.set(ids[i], clampedX);
      }
    }
    return result;
  }
  const handleDialogue = (command, context) => {
    const { project } = context;
    const resolvedCharacterId = resolveCommandCharacterId(command, project, context.playerState.variables) || command.characterId;
    const char = resolvedCharacterId ? project.characters[resolvedCharacterId] : null;
    const playerName = command.characterSource === "player" ? resolvePlayerCharacterName(project, context.playerState.variables) : null;
    const voiceAudioId = command.voiceAudioId || (char == null ? void 0 : char.defaultVoiceId) || null;
    const textEffect = command.textEffect || (char == null ? void 0 : char.textEffect) || void 0;
    if (voiceAudioId) {
      const voiceVol = context.settings.voiceVolume ?? 1;
      if (context.playVoice) context.playVoice(voiceAudioId, voiceVol);
      else context.playSound(voiceAudioId, voiceVol);
    }
    return {
      advance: false,
      // Wait for user to click
      updates: {
        uiState: {
          isWaitingForInput: true,
          dialogue: {
            text: command.text,
            characterName: playerName || (char == null ? void 0 : char.name) || "Narrator",
            characterColor: (char == null ? void 0 : char.color) || "#FFFFFF",
            characterId: resolvedCharacterId || null,
            voiceAudioId,
            textEffect,
            textboxThemeId: command.textboxThemeId ?? null,
            textSpeed: command.textSpeed,
            // Per-line auto-advance timer (counts from typewriter completion).
            timeLimit: command.timeLimit,
            timeLimitLocked: command.timeLimitLocked,
            showTimer: command.showTimer
          }
        }
      }
    };
  };
  const handleSetVariable = (command, context) => {
    const { project, playerState } = context;
    console.log("[DEBUG SetVariable] Executing - Variable:", command.variableId, "Operator:", command.operator, "Value:", command.value);
    const variable = project.variables[command.variableId];
    if (!variable) {
      console.warn(\`SetVariable command failed: Variable with ID \${command.variableId} not found.\`);
      return {
        advance: true,
        updates: {}
      };
    }
    const currentVal = playerState.variables[command.variableId];
    const originalOperator = command.operator;
    const { effectiveOperator, wasCoerced } = normalizeSetVariableOperatorByType(
      variable.type,
      variable.name,
      originalOperator
    );
    const newVal = calculateVariableValue(
      effectiveOperator,
      variable.type,
      currentVal,
      command.value,
      command.randomMin,
      command.randomMax,
      wasCoerced ? originalOperator : void 0,
      variable.min,
      variable.max
    );
    console.log("[DEBUG SetVariable] New value:", newVal, "| operator:", \`\${command.operator} => \${effectiveOperator}\`);
    return {
      advance: true,
      // Auto-advance for variable commands
      updates: {
        variables: {
          ...playerState.variables,
          [command.variableId]: newVal
        }
      }
    };
  };
  const evaluateConditions = (conditions, variables) => {
    if (!conditions || conditions.length === 0) return true;
    return combineConditions(conditions, (condition) => {
      const varValue = variables[condition.variableId];
      if (varValue === void 0) return false;
      switch (condition.operator) {
        case "is true":
          return !!varValue;
        case "is false":
          return !varValue;
        case "==":
          return String(varValue).toLowerCase() === String(condition.value).toLowerCase();
        case "!=":
          return String(varValue).toLowerCase() !== String(condition.value).toLowerCase();
        case ">":
          return Number(varValue) > Number(condition.value);
        case "<":
          return Number(varValue) < Number(condition.value);
        case ">=":
          return Number(varValue) >= Number(condition.value);
        case "<=":
          return Number(varValue) <= Number(condition.value);
        case "contains":
          return String(varValue).toLowerCase().includes(String(condition.value).toLowerCase());
        case "startsWith":
          return String(varValue).toLowerCase().startsWith(String(condition.value).toLowerCase());
        default:
          return false;
      }
    });
  };
  function handleChoice(command, context) {
    const { playerState } = context;
    const availableChoices = command.options.filter(
      (opt) => evaluateConditions(opt.conditions, playerState.variables)
    );
    const timeLimit = command.timeLimit && command.timeLimit > 0 ? command.timeLimit : void 0;
    return {
      advance: false,
      // Wait for player to select a choice
      updates: {
        uiState: {
          ...playerState.uiState,
          choices: availableChoices,
          choiceLayout: command.layout,
          // Time-limited choice config (undefined when no limit → classic behavior unchanged).
          choiceTimeLimit: timeLimit,
          choiceShowTimer: timeLimit ? command.showTimer !== false : void 0,
          choiceTimeoutBehavior: timeLimit ? command.timeoutBehavior || "option" : void 0,
          choiceTimeoutOptionId: timeLimit ? command.timeoutOptionId : void 0,
          choiceTimeoutActions: timeLimit ? command.timeoutActions : void 0
        }
      }
    };
  }
  const easingFunctions = {
    linear: (t) => t,
    // Quadratic
    easeInQuad: (t) => t * t,
    easeOutQuad: (t) => t * (2 - t),
    easeInOutQuad: (t) => t < 0.5 ? 2 * t * t : -1 + (4 - 2 * t) * t,
    // Cubic
    easeInCubic: (t) => t * t * t,
    easeOutCubic: (t) => {
      const t1 = t - 1;
      return t1 * t1 * t1 + 1;
    },
    easeInOutCubic: (t) => t < 0.5 ? 4 * t * t * t : (t - 1) * (2 * t - 2) * (2 * t - 2) + 1,
    // Quartic
    easeInQuart: (t) => t * t * t * t,
    easeOutQuart: (t) => {
      const t1 = t - 1;
      return 1 - t1 * t1 * t1 * t1;
    },
    easeInOutQuart: (t) => t < 0.5 ? 8 * t * t * t * t : 1 - 8 * --t * t * t * t,
    // Quintic
    easeInQuint: (t) => t * t * t * t * t,
    easeOutQuint: (t) => {
      const t1 = t - 1;
      return 1 + t1 * t1 * t1 * t1 * t1;
    },
    easeInOutQuint: (t) => t < 0.5 ? 16 * t * t * t * t * t : 1 + 16 * --t * t * t * t * t,
    // Sinusoidal
    easeInSine: (t) => 1 - Math.cos(t * Math.PI / 2),
    easeOutSine: (t) => Math.sin(t * Math.PI / 2),
    easeInOutSine: (t) => -(Math.cos(Math.PI * t) - 1) / 2,
    // Exponential
    easeInExpo: (t) => t === 0 ? 0 : Math.pow(2, 10 * t - 10),
    easeOutExpo: (t) => t === 1 ? 1 : 1 - Math.pow(2, -10 * t),
    easeInOutExpo: (t) => {
      if (t === 0) return 0;
      if (t === 1) return 1;
      return t < 0.5 ? Math.pow(2, 20 * t - 10) / 2 : (2 - Math.pow(2, -20 * t + 10)) / 2;
    },
    // Circular
    easeInCirc: (t) => 1 - Math.sqrt(1 - t * t),
    easeOutCirc: (t) => Math.sqrt(1 - (t - 1) * (t - 1)),
    easeInOutCirc: (t) => t < 0.5 ? (1 - Math.sqrt(1 - 4 * t * t)) / 2 : (Math.sqrt(1 - (-2 * t + 2) * (-2 * t + 2)) + 1) / 2,
    // Back (overshoot)
    easeInBack: (t) => {
      const c1 = 1.70158;
      const c3 = c1 + 1;
      return c3 * t * t * t - c1 * t * t;
    },
    easeOutBack: (t) => {
      const c1 = 1.70158;
      const c3 = c1 + 1;
      const t1 = t - 1;
      return 1 + c3 * t1 * t1 * t1 + c1 * t1 * t1;
    },
    easeInOutBack: (t) => {
      const c1 = 1.70158;
      const c2 = c1 * 1.525;
      return t < 0.5 ? 2 * t * (2 * t) * ((c2 + 1) * 2 * t - c2) / 2 : ((2 * t - 2) * (2 * t - 2) * ((c2 + 1) * (t * 2 - 2) + c2) + 2) / 2;
    },
    // Elastic
    easeInElastic: (t) => {
      if (t === 0) return 0;
      if (t === 1) return 1;
      return -Math.pow(2, 10 * t - 10) * Math.sin((t * 10 - 10.75) * (2 * Math.PI / 3));
    },
    easeOutElastic: (t) => {
      if (t === 0) return 0;
      if (t === 1) return 1;
      return Math.pow(2, -10 * t) * Math.sin((t * 10 - 0.75) * (2 * Math.PI / 3)) + 1;
    },
    easeInOutElastic: (t) => {
      if (t === 0) return 0;
      if (t === 1) return 1;
      const c5 = 2 * Math.PI / 4.5;
      return t < 0.5 ? -(Math.pow(2, 20 * t - 10) * Math.sin((20 * t - 11.125) * c5)) / 2 : Math.pow(2, -20 * t + 10) * Math.sin((20 * t - 11.125) * c5) / 2 + 1;
    },
    // Bounce
    easeOutBounce: (t) => {
      const n1 = 7.5625;
      const d1 = 2.75;
      if (t < 1 / d1) return n1 * t * t;
      if (t < 2 / d1) return n1 * (t -= 1.5 / d1) * t + 0.75;
      if (t < 2.5 / d1) return n1 * (t -= 2.25 / d1) * t + 0.9375;
      return n1 * (t -= 2.625 / d1) * t + 0.984375;
    },
    easeInBounce: (t) => 1 - easingFunctions.easeOutBounce(1 - t),
    easeInOutBounce: (t) => t < 0.5 ? (1 - easingFunctions.easeOutBounce(1 - 2 * t)) / 2 : (1 + easingFunctions.easeOutBounce(2 * t - 1)) / 2
  };
  function applyEasing(t, easing = "linear") {
    const fn = easingFunctions[easing] || easingFunctions.linear;
    return fn(Math.max(0, Math.min(1, t)));
  }
  function lerp(from, to, t) {
    return from + (to - from) * t;
  }
  function lerpColor(from, to, t) {
    const fromRgb = hexToRgb\$1(from);
    const toRgb = hexToRgb\$1(to);
    if (!fromRgb || !toRgb) return to;
    const r = Math.round(lerp(fromRgb.r, toRgb.r, t));
    const g = Math.round(lerp(fromRgb.g, toRgb.g, t));
    const b = Math.round(lerp(fromRgb.b, toRgb.b, t));
    return \`#\${((1 << 24) + (r << 16) + (g << 8) + b).toString(16).slice(1)}\`;
  }
  function hexToRgb\$1(hex) {
    const match = hex.replace("#", "").match(/^([a-f\\d]{2})([a-f\\d]{2})([a-f\\d]{2})\$/i);
    if (!match) return null;
    return { r: parseInt(match[1], 16), g: parseInt(match[2], 16), b: parseInt(match[3], 16) };
  }
  let nextTweenId = 0;
  class TweenManagerImpl {
    constructor() {
      this.tweens = /* @__PURE__ */ new Map();
      this.restingValues = /* @__PURE__ */ new Map();
      this.rafId = null;
      this.listeners = /* @__PURE__ */ new Set();
      this.lastFrameTime = 0;
      this.tick = () => {
        var _a;
        this.rafId = requestAnimationFrame(this.tick);
        const now = performance.now();
        this.lastFrameTime = now;
        let hasChanges = false;
        const completed = [];
        for (const [id, tween] of this.tweens) {
          const elapsed = now - tween.startTime;
          const rawProgress = Math.min(elapsed / tween.duration, 1);
          const easedProgress = applyEasing(rawProgress, tween.easing);
          const newCurrent = {};
          for (const key of Object.keys(tween.to)) {
            const fromVal = tween.from[key];
            const toVal = tween.to[key];
            if (fromVal === void 0 || toVal === void 0) continue;
            if (typeof fromVal === "number" && typeof toVal === "number") {
              newCurrent[key] = lerp(fromVal, toVal, easedProgress);
            } else if (typeof fromVal === "string" && typeof toVal === "string") {
              newCurrent[key] = lerpColor(fromVal, toVal, easedProgress);
            }
          }
          tween.current = newCurrent;
          hasChanges = true;
          if (rawProgress >= 1) {
            tween.finished = true;
            this.restingValues.set(
              TweenManagerImpl.restingKey(tween.targetId, tween.targetType),
              { ...tween.to }
            );
            completed.push(tween);
            this.tweens.delete(id);
          }
        }
        if (hasChanges) {
          this.notifyListeners();
        }
        for (const tween of completed) {
          (_a = tween.onComplete) == null ? void 0 : _a.call(tween);
        }
        if (this.tweens.size === 0) {
          this.stop();
        }
      };
    }
    static restingKey(targetId, targetType) {
      return \`\${targetType}:\${targetId}\`;
    }
    /**
     * Start a new tween. Replaces any existing tween on the same target+property set.
     */
    start(config) {
      const id = \`tween_\${++nextTweenId}\`;
      const rkey = TweenManagerImpl.restingKey(config.targetId, config.targetType);
      for (const [existingId, existing] of this.tweens) {
        if (existing.targetId === config.targetId && existing.targetType === config.targetType) {
          existing.finished = true;
          this.tweens.delete(existingId);
        }
      }
      this.restingValues.delete(rkey);
      const tween = {
        id,
        targetId: config.targetId,
        targetType: config.targetType,
        from: { ...config.from },
        to: { ...config.to },
        easing: config.easing || "easeInOutCubic",
        duration: config.duration * 1e3,
        startTime: performance.now(),
        current: { ...config.from },
        onComplete: config.onComplete,
        finished: false
      };
      this.tweens.set(id, tween);
      this.ensureRunning();
      return id;
    }
    /**
     * Cancel a tween by ID.
     */
    cancel(tweenId) {
      this.tweens.delete(tweenId);
      if (this.tweens.size === 0) this.stop();
    }
    /**
     * Cancel all tweens for a specific target.
     */
    cancelForTarget(targetId, targetType) {
      for (const [id, tween] of this.tweens) {
        if (tween.targetId === targetId && (!targetType || tween.targetType === targetType)) {
          this.tweens.delete(id);
        }
      }
      if (targetType) {
        this.restingValues.delete(TweenManagerImpl.restingKey(targetId, targetType));
      } else {
        for (const key of this.restingValues.keys()) {
          if (key.endsWith(\`:\${targetId}\`)) this.restingValues.delete(key);
        }
      }
      if (this.tweens.size === 0) this.stop();
    }
    /**
     * Cancel all active tweens.
     */
    cancelAll() {
      this.tweens.clear();
      this.restingValues.clear();
      this.stop();
    }
    /**
     * Get the current interpolated values for a target, or null if no tween is active.
     */
    getCurrentValues(targetId, targetType) {
      for (const tween of this.tweens.values()) {
        if (tween.targetId === targetId && tween.targetType === targetType && !tween.finished) {
          return tween.current;
        }
      }
      return this.restingValues.get(TweenManagerImpl.restingKey(targetId, targetType)) ?? null;
    }
    /**
     * Get resting (post-tween) values for a target, ignoring active tweens.
     * Used by captureCurrentValues to chain tweens correctly.
     */
    getRestingValues(targetId, targetType) {
      return this.restingValues.get(TweenManagerImpl.restingKey(targetId, targetType)) ?? null;
    }
    /**
     * Check if a target has an active tween.
     */
    hasTween(targetId, targetType) {
      for (const tween of this.tweens.values()) {
        if (tween.targetId === targetId && (!targetType || tween.targetType === targetType) && !tween.finished) {
          return true;
        }
      }
      return false;
    }
    /**
     * Subscribe to tween updates (called every frame while tweens are active).
     */
    subscribe(callback) {
      this.listeners.add(callback);
      return () => this.listeners.delete(callback);
    }
    /**
     * Get count of active tweens.
     */
    get activeCount() {
      return this.tweens.size;
    }
    ensureRunning() {
      if (this.rafId !== null) return;
      this.lastFrameTime = performance.now();
      this.tick();
    }
    stop() {
      if (this.rafId !== null) {
        cancelAnimationFrame(this.rafId);
        this.rafId = null;
      }
    }
    notifyListeners() {
      for (const listener of this.listeners) {
        listener(this.tweens);
      }
    }
  }
  const TweenManager = new TweenManagerImpl();
  function buildCharacterMedia(charData, layerSelections, wrap) {
    const imageUrls = [];
    const videoUrls = [];
    const videoTrims = [];
    let hasVideo = false;
    let videoLoop = false;
    if (charData.baseVideoUrl) {
      videoUrls.push(wrap(charData.baseVideoUrl));
      videoTrims.push({ start: charData.baseVideoTrimStart, end: charData.baseVideoTrimEnd });
      hasVideo = true;
      videoLoop = !!charData.baseVideoLoop;
    } else if (charData.baseImageUrl) {
      imageUrls.push(wrap(charData.baseImageUrl));
    }
    Object.values(charData.layers).forEach((layer) => {
      const assetId = layerSelections[layer.id];
      const asset = assetId ? layer.assets[assetId] : null;
      if (asset == null ? void 0 : asset.videoUrl) {
        videoUrls.push(wrap(asset.videoUrl));
        videoTrims.push({});
        hasVideo = true;
        videoLoop = videoLoop || !!asset.loop;
      } else if (asset == null ? void 0 : asset.imageUrl) {
        imageUrls.push(wrap(asset.imageUrl));
      }
    });
    return { imageUrls, videoUrls, videoTrims, hasVideo, videoLoop };
  }
  function handleShowCharacter(command, context) {
    const { project, playerState, activeEffectTimeoutsRef, advance, setPlayerState } = context;
    const characterId = resolveCommandCharacterId(command, project, playerState.variables) || command.characterId;
    const charData = characterId ? project.characters[characterId] : void 0;
    const exprData = charData ? charData.expressions[command.expressionId] || Object.values(charData.expressions)[0] : void 0;
    if (!characterId || !charData || !exprData) {
      return { advance: true };
    }
    TweenManager.cancelForTarget(characterId, "character");
    const wrap = (u) => resolveFieldUrl(project.id, u) || u;
    const finalBindings = {};
    const existingChar = playerState == null ? void 0 : playerState.stageState.characters[characterId];
    Object.values(charData.layers).forEach((layer) => {
      let boundVarId = null;
      const existingVarId = existingChar == null ? void 0 : existingChar.layerVariableBindings[layer.id];
      if (existingVarId) {
        const existingValue = String(playerState.variables[existingVarId] || "");
        if (existingValue && existingValue in layer.assets) {
          boundVarId = existingVarId;
          console.log(\`ShowCharacter: Keeping existing binding for layer "\${layer.name}" to variable \${existingVarId}\`);
        }
      }
      if (!boundVarId) {
        const matchingVars = Object.entries(project.variables).filter(([varId, v]) => {
          if (v.type !== "string") return false;
          const varValue = String(playerState.variables[varId] || "");
          if (!varValue) return false;
          return varValue in layer.assets;
        });
        const matchingVar = matchingVars[matchingVars.length - 1];
        if (matchingVar) {
          const [varId, varData] = matchingVar;
          boundVarId = varId;
          console.log(\`ShowCharacter: Auto-bound layer "\${layer.name}" to variable "\${varData.name}" (contains asset ID from this layer)\`);
        }
      }
      if (boundVarId) {
        finalBindings[layer.id] = boundVarId;
      }
    });
    const layerSelections = {};
    Object.values(charData.layers).forEach((layer) => {
      var _a;
      if (command.layerOverrides && Object.prototype.hasOwnProperty.call(command.layerOverrides, layer.id)) {
        layerSelections[layer.id] = command.layerOverrides[layer.id] || null;
        return;
      }
      const variableId = finalBindings[layer.id];
      if (variableId && playerState.variables[variableId] !== void 0) {
        const varValue = playerState.variables[variableId];
        const variable = project.variables[variableId];
        if ((variable == null ? void 0 : variable.type) === "number") {
          const index = Number(varValue) || 0;
          layerSelections[layer.id] = ((_a = Object.values(layer.assets)[index]) == null ? void 0 : _a.id) ?? null;
        } else {
          const assetId = String(varValue);
          layerSelections[layer.id] = assetId && layer.assets[assetId] ? assetId : null;
        }
      } else if (Object.prototype.hasOwnProperty.call(exprData.layerConfiguration, layer.id)) {
        layerSelections[layer.id] = exprData.layerConfiguration[layer.id] ?? null;
      } else if ((existingChar == null ? void 0 : existingChar.layerSelections) && Object.prototype.hasOwnProperty.call(existingChar.layerSelections, layer.id)) {
        layerSelections[layer.id] = existingChar.layerSelections[layer.id];
      } else {
        layerSelections[layer.id] = null;
      }
    });
    const { imageUrls, videoUrls, videoTrims, hasVideo, videoLoop } = buildCharacterMedia(charData, layerSelections, wrap);
    let finalPosition = command.endPosition || command.position;
    const startPosition = command.startPosition;
    const requestedTransition = command.transition;
    const currentCharacters = playerState.stageState.characters;
    const hasShowTransitionFlag = requestedTransition && requestedTransition !== "instant";
    const existingSameChar = currentCharacters[characterId];
    const isPoseChange = !!existingSameChar && !!hasShowTransitionFlag && (existingSameChar.imageUrls.join(",") !== imageUrls.join(",") || existingSameChar.expressionId !== command.expressionId);
    if (command.keepPosition && existingSameChar) {
      finalPosition = existingSameChar.position;
    }
    const ghostKey = isPoseChange ? \`__ghost_\${characterId}_\${Date.now()}\` : null;
    const ghostEntry = isPoseChange ? {
      charId: ghostKey,
      position: existingSameChar.position,
      imageUrls: existingSameChar.imageUrls,
      videoUrls: existingSameChar.videoUrls,
      videoTrims: existingSameChar.videoTrims,
      isVideo: existingSameChar.isVideo,
      videoLoop: existingSameChar.videoLoop,
      expressionId: existingSameChar.expressionId,
      layerVariableBindings: existingSameChar.layerVariableBindings,
      sourceCommandId: void 0,
      scale: existingSameChar.scale,
      inverted: existingSameChar.inverted,
      visualEffects: void 0,
      transition: {
        type: command.transition ?? "fade",
        duration: command.duration ?? 0.5,
        startPosition: void 0,
        endPosition: void 0,
        action: "hide"
      }
    } : null;
    console.log(
      \`ShowCharacter: \${charData.name}, expression: \${exprData.name}, bindings:\`,
      finalBindings,
      "variables:",
      playerState.variables
    );
    const characterState = {
      charId: characterId,
      layer: command.layer,
      parallaxDepth: command.parallaxDepth,
      position: finalPosition,
      imageUrls,
      videoUrls,
      videoTrims,
      isVideo: hasVideo,
      videoLoop,
      expressionId: command.expressionId,
      layerVariableBindings: finalBindings,
      layerSelections,
      sourceCommandId: command.id,
      scale: command.scale,
      inverted: command.inverted,
      rotation: command.rotation,
      flipY: command.flipY,
      visualEffects: (() => {
        const effects = [];
        if (command.visualEffects && command.visualEffects.length > 0) {
          effects.push(...command.visualEffects.filter((e) => e.type !== "none"));
        } else if (command.visualEffect && command.visualEffect.type !== "none") {
          effects.push(command.visualEffect);
        }
        return effects.length > 0 ? effects : void 0;
      })(),
      transition: requestedTransition && requestedTransition !== "instant" ? {
        type: requestedTransition,
        duration: command.duration ?? 0.5,
        startPosition,
        action: "show"
      } : null,
      ...command.liveConditions ? { conditions: command.conditions, live: true } : {}
    };
    if (command.transition && command.transition !== "instant") {
      const duration = (command.duration ?? 0.5) * 1e3 + 100;
      return {
        advance: false,
        // Functional patch so stacked/parallel Show Character commands compose (add against latest).
        stagePatch: (prev) => ({
          characters: {
            ...prev.characters,
            // Old pose ghost (fades out) when changing pose of the same character
            ...ghostEntry && ghostKey ? { [ghostKey]: ghostEntry } : {},
            [characterId]: characterState
          }
        }),
        delay: duration,
        callback: () => {
          if (ghostKey) {
            setPlayerState((p) => {
              if (!p) return null;
              const { [ghostKey]: _removed, ...remaining } = p.stageState.characters;
              return { ...p, stageState: { ...p.stageState, characters: remaining } };
            });
          }
          advance();
        }
      };
    }
    return {
      advance: true,
      stagePatch: (prev) => ({
        characters: { ...prev.characters, [characterId]: characterState }
      })
    };
  }
  function handleHideCharacter(command, context) {
    const { project, playerState, setPlayerState, advance } = context;
    const hideTransitionType = command.transition;
    const characterId = resolveCommandCharacterId(command, project, playerState.variables) || command.characterId;
    const existingChar = characterId ? playerState.stageState.characters[characterId] : void 0;
    if (!characterId || !existingChar) {
      return { advance: true };
    }
    TweenManager.cancelForTarget(characterId, "character");
    if (hideTransitionType && hideTransitionType !== "instant") {
      const finalPosition = existingChar.position;
      const startPosition = void 0;
      const characterWithTransition = {
        ...existingChar,
        position: finalPosition,
        transition: {
          type: hideTransitionType,
          duration: command.duration ?? 0.5,
          startPosition,
          endPosition: command.endPosition,
          action: "hide"
        }
      };
      const duration = (command.duration ?? 0.5) * 1e3 + 100;
      return {
        advance: false,
        // Functional patch (composes with other stacked/parallel character commands).
        stagePatch: (prev) => ({
          characters: { ...prev.characters, [characterId]: characterWithTransition }
        }),
        delay: duration,
        callback: () => {
          setPlayerState((p) => {
            if (!p) return null;
            const { [characterId]: _, ...remaining } = p.stageState.characters;
            return {
              ...p,
              stageState: { ...p.stageState, characters: remaining }
            };
          });
          advance();
        }
      };
    } else {
      return {
        advance: true,
        stagePatch: (prev) => {
          const { [characterId]: _, ...remaining } = prev.characters;
          return { characters: remaining };
        }
      };
    }
  }
  function handleSetCharacterLayer(command, context) {
    const { project, playerState } = context;
    const charData = project.characters[command.characterId];
    const onStage = playerState.stageState.characters[command.characterId];
    if (!charData || !onStage) {
      return { advance: true };
    }
    const wrap = (u) => resolveFieldUrl(project.id, u) || u;
    const useTransition = !!command.transition && command.transition !== "instant";
    return {
      advance: true,
      // Functional patch so it composes with other stacked character commands against the latest stage.
      stagePatch: (prev) => {
        const cur = prev.characters[command.characterId];
        if (!cur) return {};
        const selections = { ...cur.layerSelections || {} };
        (command.layers || []).forEach(({ layerId, assetId }) => {
          selections[layerId] = assetId || null;
        });
        const { imageUrls, videoUrls, videoTrims, hasVideo, videoLoop } = buildCharacterMedia(charData, selections, wrap);
        return {
          characters: {
            ...prev.characters,
            [command.characterId]: {
              ...cur,
              imageUrls,
              videoUrls,
              videoTrims,
              isVideo: hasVideo,
              videoLoop,
              layerSelections: selections,
              // Optional crossfade of the character to the new look; otherwise an instant swap.
              transition: useTransition ? { type: command.transition, duration: command.duration ?? 0.3, action: "show" } : null
            }
          }
        };
      }
    };
  }
  async function handleSetBackground(command, context) {
    var _a, _b, _c;
    const { assetResolver, getAssetMetadata, setPlayerState, playerState, advance, project } = context;
    const bgAssetRec = command.backgroundId ? ((_a = project.backgrounds) == null ? void 0 : _a[command.backgroundId]) || ((_b = project.images) == null ? void 0 : _b[command.backgroundId]) || ((_c = project.videos) == null ? void 0 : _c[command.backgroundId]) : void 0;
    const bgTrim = resolveVideoTrim(command, bgAssetRec);
    const bgFx = { backgroundParallaxDepth: command.parallaxDepth, backgroundLayer: command.layer, backgroundTrimStart: bgTrim.start, backgroundTrimEnd: bgTrim.end };
    if (command.liveConditions) {
      const url = command.backgroundColor ? null : assetResolver(command.backgroundId, "image");
      const meta = command.backgroundColor ? { isVideo: false, loop: false } : getAssetMetadata(command.backgroundId, "image");
      const layer = {
        commandId: command.id,
        conditions: command.conditions,
        url: url || null,
        color: command.backgroundColor,
        isVideo: meta.isVideo,
        loop: meta.loop,
        trimStart: bgTrim.start,
        trimEnd: bgTrim.end,
        parallaxDepth: command.parallaxDepth,
        layer: command.layer
      };
      const existing = playerState.stageState.backgroundLayers || [];
      return {
        advance: true,
        updates: {
          stageState: {
            ...playerState.stageState,
            backgroundLayers: [...existing.filter((l) => l.commandId !== command.id), layer]
          }
        }
      };
    }
    if (command.stack) {
      const url = command.backgroundColor ? null : assetResolver(command.backgroundId, "image");
      const meta = command.backgroundColor ? { isVideo: false, loop: false } : getAssetMetadata(command.backgroundId, "image");
      const plane = {
        commandId: command.id,
        url: url || null,
        color: command.backgroundColor,
        isVideo: meta.isVideo,
        loop: meta.loop,
        trimStart: bgTrim.start,
        trimEnd: bgTrim.end,
        parallaxDepth: command.parallaxDepth,
        layer: command.layer,
        transition: command.transition,
        duration: command.duration
      };
      const existing = playerState.stageState.backgroundStack || [];
      return {
        advance: true,
        updates: {
          stageState: {
            ...playerState.stageState,
            backgroundStack: [...existing.filter((p) => p.commandId !== command.id), plane]
          }
        }
      };
    }
    if (command.backgroundColor) {
      const duration2 = command.duration ?? 1;
      if (command.transition === "instant" || !command.transition) {
        return {
          advance: true,
          updates: {
            stageState: {
              ...playerState.stageState,
              ...bgFx,
              backgroundUrl: null,
              backgroundIsVideo: false,
              backgroundColor: command.backgroundColor
            }
          }
        };
      }
      setPlayerState((p) => {
        if (!p) return null;
        return { ...p, uiState: { ...p.uiState, isTransitioning: true } };
      });
      if (command.transition === "fade") {
        const phase1 = /* @__PURE__ */ jsxRuntime2.jsx(
          "div",
          {
            className: "absolute inset-0 z-0 bg-black",
            style: { animation: \`dissolve-in \${duration2 / 2}s forwards\` }
          },
          Date.now()
        );
        setPlayerState((p) => p ? { ...p, uiState: { ...p.uiState, transitionElement: phase1 } } : null);
        setTimeout(() => {
          setPlayerState((p) => {
            if (!p) return null;
            const phase2 = /* @__PURE__ */ jsxRuntime2.jsx(
              "div",
              {
                className: "absolute inset-0 z-0 bg-black",
                style: { animation: \`fade-out \${duration2 / 2}s forwards\` }
              },
              Date.now() + 1
            );
            return {
              ...p,
              stageState: {
                ...p.stageState,
                ...bgFx,
                backgroundUrl: null,
                backgroundIsVideo: false,
                backgroundColor: command.backgroundColor
              },
              uiState: { ...p.uiState, transitionElement: phase2 }
            };
          });
        }, duration2 * 500);
        await new Promise((resolve) => {
          setTimeout(() => {
            setPlayerState(
              (p) => p ? { ...p, uiState: { ...p.uiState, isTransitioning: false, transitionElement: null } } : null
            );
            resolve();
          }, duration2 * 1e3 + 100);
        });
        advance();
        return { advance: false };
      }
      let transitionElement2 = null;
      if (command.transition === "cross-fade") {
        transitionElement2 = /* @__PURE__ */ jsxRuntime2.jsx(
          "div",
          {
            className: "absolute inset-0 z-0",
            style: {
              backgroundColor: command.backgroundColor,
              animation: \`dissolve-in \${duration2}s ease-in-out forwards\`
            }
          },
          Date.now()
        );
      } else if (command.transition === "dissolve") {
        transitionElement2 = /* @__PURE__ */ jsxRuntime2.jsx(
          "div",
          {
            className: "absolute inset-0 z-0",
            style: {
              backgroundColor: command.backgroundColor,
              animation: \`dissolve-in \${duration2}s forwards\`
            }
          },
          Date.now()
        );
      } else if (command.transition === "slide") {
        transitionElement2 = /* @__PURE__ */ jsxRuntime2.jsx(
          "div",
          {
            className: "absolute inset-0 z-0",
            style: {
              backgroundColor: command.backgroundColor,
              animation: \`slide-in-right \${duration2}s forwards\`
            }
          },
          Date.now()
        );
      } else if (command.transition === "iris-in") {
        transitionElement2 = /* @__PURE__ */ jsxRuntime2.jsx(
          "div",
          {
            className: "absolute inset-0 z-0",
            style: {
              backgroundColor: command.backgroundColor,
              animation: \`iris-in \${duration2}s forwards\`
            }
          },
          Date.now()
        );
      } else if (command.transition === "wipe-right") {
        transitionElement2 = /* @__PURE__ */ jsxRuntime2.jsx(
          "div",
          {
            className: "absolute inset-0 z-0",
            style: {
              backgroundColor: command.backgroundColor,
              animation: \`wipe-right \${duration2}s forwards\`
            }
          },
          Date.now()
        );
      } else {
        transitionElement2 = /* @__PURE__ */ jsxRuntime2.jsx(
          "div",
          {
            className: "absolute inset-0 z-0 bg-black",
            style: { animation: \`dissolve-in \${duration2 / 2}s forwards\` }
          },
          Date.now()
        );
      }
      setPlayerState((p) => {
        if (!p) return null;
        return {
          ...p,
          uiState: { ...p.uiState, transitionElement: transitionElement2 }
        };
      });
      await new Promise((resolve) => {
        setTimeout(() => {
          setPlayerState((p) => {
            if (!p) return null;
            return {
              ...p,
              stageState: {
                ...p.stageState,
                ...bgFx,
                backgroundUrl: null,
                backgroundIsVideo: false,
                backgroundColor: command.backgroundColor
              },
              uiState: { ...p.uiState, isTransitioning: false, transitionElement: null }
            };
          });
          resolve();
        }, duration2 * 1e3 + 100);
      });
      advance();
      return { advance: false };
    }
    const newUrl = assetResolver(command.backgroundId, "image");
    const { isVideo, loop } = getAssetMetadata(command.backgroundId, "image");
    const duration = command.duration ?? 1;
    if (!newUrl) {
      console.warn(\`Background not found: \${command.backgroundId}\`);
      return { advance: true };
    }
    if (command.transition === "instant" || !command.transition) {
      return {
        advance: true,
        updates: {
          stageState: {
            ...playerState.stageState,
            ...bgFx,
            backgroundUrl: newUrl,
            backgroundIsVideo: isVideo,
            backgroundLoop: command.loop ?? loop
          }
        }
      };
    }
    const preloadMedia = () => new Promise((resolve, reject) => {
      if (isVideo) {
        const video = document.createElement("video");
        video.src = newUrl;
        video.preload = "auto";
        video.onerror = () => reject(new Error(\`Failed to load background video: \${newUrl}\`));
        video.onloadeddata = () => resolve();
      } else {
        const img = new Image();
        img.src = newUrl;
        img.onerror = () => reject(new Error(\`Failed to load background image: \${newUrl}\`));
        img.onload = () => resolve();
      }
    });
    try {
      await preloadMedia();
    } catch (error) {
      console.error(error);
      return { advance: true };
    }
    let transitionElement = null;
    const MediaElement = isVideo ? "video" : "img";
    const mediaProps = isVideo ? { autoPlay: true, muted: true, loop, playsInline: true } : { alt: "" };
    if (command.transition === "cross-fade") {
      transitionElement = /* @__PURE__ */ jsxRuntime2.jsx(
        MediaElement,
        {
          src: newUrl,
          ...mediaProps,
          className: "absolute inset-0 w-full h-full object-cover z-0",
          style: { opacity: 0, transition: \`opacity \${duration}s ease-in-out\` }
        },
        Date.now()
      );
      setTimeout(() => {
        setPlayerState((p) => {
          if (!p) return null;
          const el = /* @__PURE__ */ jsxRuntime2.jsx(
            MediaElement,
            {
              src: newUrl,
              ...mediaProps,
              className: "absolute inset-0 w-full h-full object-cover z-0",
              style: { opacity: 1, transition: \`opacity \${duration}s ease-in-out\` }
            },
            Date.now()
          );
          return { ...p, uiState: { ...p.uiState, transitionElement: el } };
        });
      }, 50);
    } else if (command.transition === "fade") {
      transitionElement = /* @__PURE__ */ jsxRuntime2.jsx(
        "div",
        {
          className: "absolute inset-0 z-0 bg-black",
          style: { animation: \`dissolve-in \${duration / 2}s forwards\` }
        },
        Date.now()
      );
      setTimeout(() => {
        setPlayerState((p) => {
          if (!p) return null;
          const el = /* @__PURE__ */ jsxRuntime2.jsx(
            "div",
            {
              className: "absolute inset-0 z-0 bg-black",
              style: { animation: \`fade-out \${duration / 2}s forwards\` }
            },
            Date.now() + 1
          );
          return {
            ...p,
            stageState: {
              ...p.stageState,
              ...bgFx,
              backgroundUrl: newUrl,
              backgroundIsVideo: isVideo,
              backgroundLoop: command.loop ?? loop
            },
            uiState: { ...p.uiState, transitionElement: el }
          };
        });
      }, duration * 500);
    } else {
      let transitionClass = "";
      switch (command.transition) {
        case "dissolve":
          transitionClass = "transition-dissolve";
          break;
        case "slide":
          transitionClass = "transition-slide-in-right";
          break;
        case "iris-in":
          transitionClass = "transition-iris-in";
          break;
        case "wipe-right":
          transitionClass = "transition-wipe-right";
          break;
      }
      transitionElement = /* @__PURE__ */ jsxRuntime2.jsx(
        MediaElement,
        {
          src: newUrl,
          ...mediaProps,
          className: \`absolute inset-0 w-full h-full object-cover z-0 transition-base \${transitionClass}\`,
          style: { animationDuration: \`\${duration}s\` }
        },
        Date.now()
      );
    }
    setPlayerState(
      (p) => p ? { ...p, uiState: { ...p.uiState, isTransitioning: true, transitionElement } } : null
    );
    await new Promise((resolve) => {
      setTimeout(() => {
        setPlayerState((p) => {
          if (!p) return null;
          return {
            ...p,
            stageState: {
              ...p.stageState,
              ...bgFx,
              backgroundUrl: newUrl,
              backgroundIsVideo: isVideo,
              backgroundLoop: command.loop ?? loop
            },
            uiState: { ...p.uiState, isTransitioning: false, transitionElement: null }
          };
        });
        resolve();
      }, duration * 1e3 + 100);
    });
    advance();
    return { advance: false };
  }
  function handlePlayMusic(command, context) {
    const { assetResolver, musicAudioRef, fadeAudio, settings, playerState, setPlayerState } = context;
    console.log("[PlayMusic] Starting music command", { audioId: command.audioId, loop: command.loop });
    const url = assetResolver(command.audioId, "audio");
    if (!url) {
      console.warn(\`No audio URL found for audioId: \${command.audioId}\`);
      return { advance: true };
    }
    const audio = musicAudioRef.current;
    if (!audio) {
      console.warn("[PlayMusic] Audio element not available");
      return { advance: true };
    }
    const currentSrcPath = audio.src ? new URL(audio.src, window.location.href).pathname : null;
    const newSrcPath = url ? new URL(url, window.location.href).pathname : null;
    const isNewTrack = currentSrcPath !== newSrcPath;
    console.log("[PlayMusic] Audio setup", { isNewTrack, currentSrc: audio.src, newUrl: url, paused: audio.paused });
    if (!isNewTrack && !audio.paused) {
      console.log("[PlayMusic] Same track already playing, updating state only");
      return {
        advance: true,
        updates: {
          musicState: {
            ...playerState.musicState,
            audioId: command.audioId,
            loop: command.loop,
            isPlaying: true
          }
        }
      };
    }
    const musicState = {
      audioId: command.audioId,
      loop: command.loop,
      currentTime: 0,
      isPlaying: true
    };
    const startPlayback = () => {
      console.log("[PlayMusic] Starting playback");
      audio.loop = command.loop;
      audio.volume = 0;
      audio.play().then(() => {
        console.log("[PlayMusic] Audio playing, starting fade-in");
        const target = typeof command.volume === "number" ? command.volume : settings.musicVolume;
        fadeAudio(audio, target, command.fadeDuration);
      }).catch((e) => {
        console.error("[PlayMusic] Music play failed:", e);
      });
    };
    if (isNewTrack) {
      audio.src = url;
      audio.load();
      audio.addEventListener("canplaythrough", startPlayback, { once: true });
      audio.addEventListener("error", (e) => {
        console.error("[PlayMusic] Music load failed:", e);
      }, { once: true });
    } else {
      startPlayback();
    }
    console.log("[PlayMusic] Command complete, advancing");
    return {
      advance: true,
      updates: {
        musicState
      }
    };
  }
  function handleStopMusic(command, context) {
    const { musicAudioRef, fadeAudio, playerState } = context;
    if (musicAudioRef.current) {
      fadeAudio(musicAudioRef.current, 0, command.fadeDuration, () => {
        var _a;
        (_a = musicAudioRef.current) == null ? void 0 : _a.pause();
      });
    }
    return {
      advance: true,
      updates: {
        musicState: {
          audioId: null,
          loop: false,
          currentTime: 0,
          isPlaying: false
        }
      }
    };
  }
  function handlePlaySoundEffect(command, context) {
    const { playSound } = context;
    try {
      playSound(command.audioId, command.volume, command.loop);
    } catch (e) {
      console.error("Failed to play sound effect:", e);
    }
    return { advance: true };
  }
  function handleStopSoundEffect(command, context) {
    const { stopSfx } = context;
    console.log("[StopSoundEffect] Stopping sound effects", { audioId: command.audioId || "(all)", fadeDuration: command.fadeDuration || 0 });
    stopSfx(command.audioId || null, command.fadeDuration);
    return { advance: true };
  }
  function handleShowText(command, context) {
    const { playerState, project } = context;
    TweenManager.cancelForTarget(command.id, "text");
    const interpolatedText = interpolateVariables(command.text, playerState.variables, project);
    const overlay = {
      id: command.id,
      layer: command.layer,
      parallaxDepth: command.parallaxDepth,
      text: interpolatedText,
      rawText: command.text,
      x: command.x,
      y: command.y,
      fontSize: command.fontSize,
      fontFamily: command.fontFamily,
      color: command.color,
      width: command.width,
      height: command.height,
      fontWeight: command.fontWeight,
      fontStyle: command.fontStyle,
      letterSpacing: command.letterSpacing,
      textShadow: command.textShadow,
      textGradient: command.textGradient,
      textBorder: command.textBorder,
      textAlign: command.textAlign,
      verticalAlign: command.verticalAlign,
      transition: command.transition !== "instant" ? command.transition : void 0,
      duration: command.duration,
      action: "show",
      rotation: command.rotation,
      flipX: command.flipX,
      flipY: command.flipY,
      // \`live\` drives per-render re-evaluation: live conditions (visibility) AND/OR live text
      // (re-interpolating {variable} tokens). Conditions are only attached for liveConditions.
      ...command.liveConditions || command.liveText ? { live: true } : {},
      ...command.liveConditions ? { conditions: command.conditions } : {}
    };
    const hasTransition = command.transition && command.transition !== "instant";
    const delay = hasTransition ? (command.duration ?? 0.5) * 1e3 + 100 : 0;
    return {
      advance: !hasTransition,
      // Functional patch so stacked/runAsync Show Text commands compose (append vs latest).
      stagePatch: (prev) => ({ textOverlays: [...prev.textOverlays, overlay] }),
      delay,
      callback: hasTransition ? context.advance : void 0
    };
  }
  function handleHideText(command, context) {
    const { playerState, setPlayerState, advance } = context;
    const overlays = playerState.stageState.textOverlays;
    const target = overlays.find((o) => o.id === command.targetCommandId);
    if (!target) {
      return { advance: true };
    }
    TweenManager.cancelForTarget(command.targetCommandId, "text");
    if (command.transition && command.transition !== "instant") {
      const updated = overlays.map(
        (o) => o.id === command.targetCommandId ? { ...o, transition: command.transition, duration: command.duration, action: "hide" } : o
      );
      const duration = (command.duration ?? 0.5) * 1e3 + 100;
      return {
        advance: false,
        updates: {
          stageState: {
            ...playerState.stageState,
            textOverlays: updated
          }
        },
        delay: duration,
        callback: () => {
          setPlayerState(
            (inner) => inner ? {
              ...inner,
              stageState: {
                ...inner.stageState,
                textOverlays: inner.stageState.textOverlays.filter(
                  (o) => o.id !== command.targetCommandId
                )
              }
            } : null
          );
          advance();
        }
      };
    } else {
      return {
        advance: true,
        stagePatch: (prev) => ({ textOverlays: prev.textOverlays.filter((o) => o.id !== command.targetCommandId) })
      };
    }
  }
  function handleShowImage(command, context) {
    var _a, _b, _c;
    const { assetResolver, getAssetMetadata, playerState, project } = context;
    const imageUrl = assetResolver(command.imageId, "image");
    const { isVideo, loop } = getAssetMetadata(command.imageId, "image");
    if (!imageUrl) {
      console.warn(\`Image not found: \${command.imageId}\`);
      return { advance: true };
    }
    TweenManager.cancelForTarget(command.id, "image");
    const imgAssetRec = command.imageId ? ((_a = project.images) == null ? void 0 : _a[command.imageId]) || ((_b = project.backgrounds) == null ? void 0 : _b[command.imageId]) || ((_c = project.videos) == null ? void 0 : _c[command.imageId]) : void 0;
    const imgTrim = resolveVideoTrim(command, imgAssetRec);
    const overlay = {
      id: command.id,
      layer: command.layer,
      parallaxDepth: command.parallaxDepth,
      imageUrl: !isVideo ? imageUrl : void 0,
      videoUrl: isVideo ? imageUrl : void 0,
      isVideo,
      videoLoop: loop,
      videoTrimStart: imgTrim.start,
      videoTrimEnd: imgTrim.end,
      x: command.x,
      y: command.y,
      width: command.width,
      height: command.height,
      rotation: command.rotation,
      opacity: command.opacity,
      scaleX: command.scaleX ?? 1,
      scaleY: command.scaleY ?? 1,
      flipX: command.flipX,
      flipY: command.flipY,
      transition: command.transition !== "instant" ? command.transition : void 0,
      duration: command.duration,
      fitToContent: command.fitToContent,
      action: "show",
      ...command.liveConditions ? { conditions: command.conditions, live: true } : {}
    };
    const hasTransition = command.transition && command.transition !== "instant";
    const delay = hasTransition ? (command.duration ?? 0.5) * 1e3 + 100 : 0;
    return {
      advance: !hasTransition,
      // Functional patch so stacked/runAsync Show Image commands compose (append vs latest).
      stagePatch: (prev) => ({ imageOverlays: [...prev.imageOverlays, overlay] }),
      delay,
      callback: hasTransition ? context.advance : void 0
    };
  }
  function handleHideImage(command, context) {
    const { playerState, setPlayerState, advance } = context;
    const overlays = playerState.stageState.imageOverlays;
    const target = overlays.find((o) => o.id === command.targetCommandId);
    if (!target) {
      return { advance: true };
    }
    TweenManager.cancelForTarget(command.targetCommandId, "image");
    if (command.transition && command.transition !== "instant") {
      const updated = overlays.map(
        (o) => o.id === command.targetCommandId ? { ...o, transition: command.transition, duration: command.duration, action: "hide" } : o
      );
      const duration = (command.duration ?? 0.5) * 1e3 + 100;
      return {
        advance: false,
        updates: {
          stageState: {
            ...playerState.stageState,
            imageOverlays: updated
          }
        },
        delay: duration,
        callback: () => {
          setPlayerState(
            (inner) => inner ? {
              ...inner,
              stageState: {
                ...inner.stageState,
                imageOverlays: inner.stageState.imageOverlays.filter(
                  (o) => o.id !== command.targetCommandId
                )
              }
            } : null
          );
          advance();
        }
      };
    } else {
      return {
        advance: true,
        // Functional patch so stacked Hide Image commands compose.
        stagePatch: (prev) => ({ imageOverlays: prev.imageOverlays.filter((o) => o.id !== command.targetCommandId) })
      };
    }
  }
  function handleShowButton(command, context) {
    const { playerState, assetResolver, setPlayerState, evaluateConditions: evaluateConditions2 } = context;
    TweenManager.cancelForTarget(command.id, "button");
    if (!command.liveConditions && command.showConditions && command.showConditions.length > 0) {
      const conditionsMet = evaluateConditions2(command.showConditions, playerState.variables);
      if (!conditionsMet) {
        return { advance: true };
      }
    }
    const buttonOverlay = {
      id: command.id,
      layer: command.layer,
      parallaxDepth: command.parallaxDepth,
      text: command.text,
      x: command.x,
      y: command.y,
      width: command.width || 20,
      height: command.height || 8,
      anchorX: command.anchorX || 0.5,
      anchorY: command.anchorY || 0.5,
      backgroundColor: command.backgroundColor || "#6366f1",
      textColor: command.textColor || "#ffffff",
      fontSize: command.fontSize || 18,
      fontWeight: command.fontWeight || "normal",
      textAlign: command.textAlign || "center",
      paddingX: command.paddingX ?? 0,
      borderRadius: command.borderRadius || 8,
      opacity: command.opacity ?? 1,
      imageUrl: command.image ? assetResolver(command.image.id, command.image.type) : null,
      hoverImageUrl: command.hoverImage ? assetResolver(command.hoverImage.id, command.hoverImage.type) : null,
      onClick: command.onClick,
      actions: command.actions,
      // Multiple actions support
      clickSound: command.clickSound,
      waitForClick: command.waitForClick,
      quickMenuMode: command.quickMenuMode,
      contentBox: command.contentBox,
      rotation: command.rotation,
      flipX: command.flipX,
      flipY: command.flipY,
      transition: command.transition !== "instant" ? command.transition : void 0,
      duration: command.duration || 0.3,
      action: "show",
      ...command.liveConditions ? { conditions: command.conditions, live: true } : {}
    };
    const hasTransition = command.transition && command.transition !== "instant";
    const waitForClick = command.waitForClick;
    let shouldAdvance = true;
    let delay = 0;
    let callback;
    if (hasTransition && waitForClick) {
      shouldAdvance = false;
      delay = 0;
      callback = () => {
        setPlayerState(
          (p) => p ? { ...p, uiState: { ...p.uiState, isWaitingForInput: true } } : null
        );
      };
    } else if (hasTransition) {
      shouldAdvance = false;
      delay = (command.duration ?? 0.3) * 1e3 + 100;
      callback = context.advance;
    } else if (waitForClick) {
      shouldAdvance = false;
      callback = () => {
        setPlayerState(
          (p) => p ? { ...p, uiState: { ...p.uiState, isWaitingForInput: true } } : null
        );
      };
    }
    return {
      advance: shouldAdvance,
      // Functional patch (appends against the LATEST overlays) so stacked/runAsync Show Button
      // commands compose instead of clobbering each other — e.g. an Exit Game button + a Quit-to-
      // Title button stacked together: the snapshot path made the second overwrite the first, so
      // only one rendered ("two buttons, only one works"; the missing one looked like a dead click).
      stagePatch: (prev) => ({ buttonOverlays: [...prev.buttonOverlays, buttonOverlay] }),
      delay,
      callback
    };
  }
  function handleShowItem(command, context) {
    var _a;
    const { playerState, project, assetResolver, evaluateConditions: evaluateConditions2 } = context;
    TweenManager.cancelForTarget(command.id, "button");
    if (command.pickUpOnce !== false && (playerState.pickedUpItems || []).includes(command.id)) {
      return { advance: true };
    }
    if (!command.liveConditions && command.showConditions && command.showConditions.length > 0) {
      if (!evaluateConditions2(command.showConditions, playerState.variables)) {
        return { advance: true };
      }
    }
    const item = (_a = project.items) == null ? void 0 : _a[command.itemId];
    const visual = command.image || (item == null ? void 0 : item.icon) || null;
    const give = command.giveOnClick !== false;
    const extra = command.actions ?? [];
    const overlay = {
      id: command.id,
      layer: command.layer,
      parallaxDepth: command.parallaxDepth,
      text: "",
      x: command.x,
      y: command.y,
      width: command.width || 10,
      height: command.height || 10,
      anchorX: command.anchorX ?? 0.5,
      anchorY: command.anchorY ?? 0.5,
      backgroundColor: "transparent",
      textColor: "#ffffff",
      fontSize: 0,
      fontWeight: "normal",
      borderRadius: 0,
      opacity: command.opacity ?? 1,
      imageUrl: visual ? assetResolver(visual.id, visual.type) : null,
      hoverImageUrl: command.hoverImage ? assetResolver(command.hoverImage.id, command.hoverImage.type) : null,
      // The give is applied directly on click (see giveItemId below) — reliable, atomic with the
      // overlay removal — so it is NOT a click action. Click actions are only the author's extras.
      onClick: extra[0] ?? { type: UIActionType.None },
      actions: extra.slice(1),
      clickSound: command.clickSound ?? null,
      rotation: command.rotation,
      flipX: command.flipX,
      flipY: command.flipY,
      transition: command.transition !== "instant" ? command.transition : void 0,
      duration: command.duration || 0.3,
      action: "show",
      removeAfterClick: command.removeAfterPickup !== false,
      pickUpOnceId: command.pickUpOnce !== false ? command.id : null,
      giveItemId: give ? command.itemId : null,
      giveQuantity: command.quantity ?? 1,
      draggable: command.draggable,
      dragItemId: command.itemId,
      ...command.liveConditions ? { conditions: command.conditions, live: true } : {}
    };
    const hasTransition = command.transition && command.transition !== "instant";
    return {
      advance: !hasTransition,
      // Functional patch so a stacked Show Item composes with other overlay commands.
      stagePatch: (prev) => ({ buttonOverlays: [...prev.buttonOverlays, overlay] }),
      delay: hasTransition ? (command.duration ?? 0.3) * 1e3 + 100 : 0,
      callback: hasTransition ? context.advance : void 0
    };
  }
  function handleHideButton(command, context) {
    const { playerState, setPlayerState, advance } = context;
    const overlays = playerState.stageState.buttonOverlays;
    const target = overlays.find((o) => o.id === command.targetCommandId);
    if (!target) {
      return { advance: true };
    }
    TweenManager.cancelForTarget(command.targetCommandId, "button");
    if (command.transition && command.transition !== "instant") {
      const updated = overlays.map(
        (o) => o.id === command.targetCommandId ? {
          ...o,
          transition: command.transition,
          duration: command.duration || 0.3,
          action: "hide"
        } : o
      );
      const duration = (command.duration ?? 0.3) * 1e3 + 100;
      return {
        advance: false,
        updates: {
          stageState: {
            ...playerState.stageState,
            buttonOverlays: updated
          }
        },
        delay: duration,
        callback: () => {
          setPlayerState(
            (inner) => inner ? {
              ...inner,
              stageState: {
                ...inner.stageState,
                buttonOverlays: inner.stageState.buttonOverlays.filter(
                  (o) => o.id !== command.targetCommandId
                )
              }
            } : null
          );
          advance();
        }
      };
    } else {
      return {
        advance: true,
        // Functional patch so stacked Hide Button commands compose.
        stagePatch: (prev) => ({ buttonOverlays: prev.buttonOverlays.filter((o) => o.id !== command.targetCommandId) })
      };
    }
  }
  function handleJump(command, context) {
    const { project, playerState } = context;
    const actualSceneId = command.targetSceneId;
    const newScene = project.scenes[actualSceneId];
    if (!newScene) {
      console.error(\`Scene not found: \${actualSceneId}\`);
      return { advance: true };
    }
    return {
      advance: false,
      // don't auto-advance after a jump
      updates: {
        currentSceneId: actualSceneId,
        currentCommands: newScene.commands,
        currentIndex: 0,
        commandStack: [],
        // Clear stage state for new scene
        stageState: {
          backgroundUrl: null,
          characters: {},
          textOverlays: [],
          imageOverlays: [],
          buttonOverlays: [],
          movieOverlays: [],
          screen: {
            shake: { active: false, intensity: 0 },
            tint: "transparent",
            zoom: 1,
            panX: 0,
            panY: 0,
            transitionDuration: 0.5,
            overlayEffects: []
          }
        },
        // Clear UI state
        uiState: {
          dialogue: null,
          choices: null,
          textInput: null,
          movieUrl: null,
          isWaitingForInput: false,
          isTransitioning: false,
          transitionElement: null,
          flash: null,
          showHistory: false,
          screenSceneId: null
        }
      }
    };
  }
  function handleJumpToLabel(command, context) {
    const { project, playerState } = context;
    const matchesLabel = (c) => c.type === CommandType.Label && c.labelId === command.labelId;
    const localIndex = playerState.currentCommands.findIndex(matchesLabel);
    if (localIndex !== -1) {
      return { advance: false, updates: { currentIndex: localIndex } };
    }
    for (const [sceneId, scene] of Object.entries(project.scenes || {})) {
      if (sceneId === playerState.currentSceneId) continue;
      const idx = scene.commands.findIndex(matchesLabel);
      if (idx === -1) continue;
      return {
        advance: false,
        updates: {
          currentSceneId: sceneId,
          currentCommands: scene.commands,
          currentIndex: idx,
          commandStack: [],
          // Clear stage state for the new scene (mirrors handleJump).
          stageState: {
            backgroundUrl: null,
            characters: {},
            textOverlays: [],
            imageOverlays: [],
            buttonOverlays: [],
            movieOverlays: [],
            screen: {
              shake: { active: false, intensity: 0 },
              tint: "transparent",
              zoom: 1,
              panX: 0,
              panY: 0,
              transitionDuration: 0.5,
              overlayEffects: []
            }
          },
          uiState: {
            dialogue: null,
            choices: null,
            textInput: null,
            movieUrl: null,
            isWaitingForInput: false,
            isTransitioning: false,
            transitionElement: null,
            flash: null,
            showHistory: false,
            screenSceneId: null
          }
        }
      };
    }
    console.warn(\`Label not found: \${command.labelId}\`);
    return { advance: true };
  }
  function handleLabel(command, context) {
    return { advance: true };
  }
  function handleBranchStart() {
    return { advance: true };
  }
  function handleBranchEnd() {
    return { advance: true };
  }
  function jumpPastBranchEnd(branchId, context) {
    const { playerState } = context;
    const endIdx = playerState.currentCommands.findIndex(
      (c, i) => i > playerState.currentIndex && c.type === CommandType.BranchEnd && c.branchId === branchId
    );
    if (endIdx === -1) {
      return { advance: true };
    }
    return { advance: false, updates: { currentIndex: endIdx + 1 } };
  }
  function handleBranchElseIf(command, context) {
    return jumpPastBranchEnd(command.branchId, context);
  }
  function handleBranchElse(command, context) {
    return jumpPastBranchEnd(command.branchId, context);
  }
  function handleGroup() {
    return { advance: true };
  }
  function handleTextInput(command, context) {
    const { playerState } = context;
    return {
      advance: false,
      // Wait for user input
      updates: {
        uiState: {
          ...playerState.uiState,
          isWaitingForInput: true,
          textInput: {
            variableId: command.variableId,
            prompt: command.prompt,
            placeholder: command.placeholder || "",
            maxLength: command.maxLength || 50
          }
        }
      }
    };
  }
  function handleCreditRoll(command, _ctx) {
    return { advance: false };
  }
  async function executeScript(script, context) {
    var _a;
    const startTime = performance.now();
    const variableChanges = {};
    let navigationRequest = void 0;
    const varNameToId = {};
    const varIdToName = {};
    for (const [id, variable] of Object.entries(context.project.variables || {})) {
      varNameToId[variable.name.toLowerCase()] = id;
      varIdToName[id] = variable.name;
    }
    const resolveVarId = (nameOrId) => {
      var _a2;
      if ((_a2 = context.project.variables) == null ? void 0 : _a2[nameOrId]) return nameOrId;
      return varNameToId[nameOrId.toLowerCase()];
    };
    const resolveCharacterId = (nameOrId) => {
      const chars = context.project.characters || {};
      if (chars[nameOrId]) return nameOrId;
      const lower = nameOrId.toLowerCase();
      const found = Object.entries(chars).find(([, c]) => {
        var _a2;
        return ((_a2 = c == null ? void 0 : c.name) == null ? void 0 : _a2.toLowerCase()) === lower;
      });
      return found == null ? void 0 : found[0];
    };
    const resolveExpressionId = (charId, nameOrId) => {
      var _a2;
      const exprs = ((_a2 = (context.project.characters || {})[charId]) == null ? void 0 : _a2.expressions) || {};
      if (exprs[nameOrId]) return nameOrId;
      const lower = nameOrId.toLowerCase();
      const found = Object.entries(exprs).find(([, e]) => {
        var _a3;
        return ((_a3 = e == null ? void 0 : e.name) == null ? void 0 : _a3.toLowerCase()) === lower;
      });
      return found == null ? void 0 : found[0];
    };
    const resolveAssetId = (collection, nameOrId) => {
      const c = collection || {};
      if (c[nameOrId]) return nameOrId;
      const lower = nameOrId.toLowerCase();
      const found = Object.entries(c).find(([, a]) => {
        var _a2;
        return ((_a2 = a == null ? void 0 : a.name) == null ? void 0 : _a2.toLowerCase()) === lower;
      });
      return found == null ? void 0 : found[0];
    };
    const resolveItem = (nameOrId) => {
      const items = context.project.items || {};
      if (items[nameOrId]) return items[nameOrId];
      const lower = nameOrId.toLowerCase();
      return Object.values(items).find((it) => it.name.toLowerCase() === lower);
    };
    const resolveScreenId = (nameOrId) => {
      const screens = context.project.uiScreens || {};
      if (screens[nameOrId]) return nameOrId;
      const lower = String(nameOrId).toLowerCase();
      const found = Object.values(screens).find((s) => ((s == null ? void 0 : s.name) || "").toLowerCase() === lower);
      return found ? found.id : nameOrId;
    };
    const warnMissing = (message) => {
      var _a2;
      console.warn(\`[Script] \${message}\`);
      if (!context.isStandalone) (_a2 = context.onNotify) == null ? void 0 : _a2.call(context, \`Script: \${message}\`, "warning");
    };
    const gameAPI = {
      getVariable: (nameOrId) => {
        const id = resolveVarId(nameOrId);
        if (!id) return void 0;
        return context.variables[id];
      },
      setVariable: (nameOrId, value) => {
        var _a2;
        const id = resolveVarId(nameOrId);
        if (!id) {
          warnMissing(\`variable "\${nameOrId}" doesn't exist — create it in the Variables panel first.\`);
          return;
        }
        let finalValue = value;
        const def = (_a2 = context.project.variables) == null ? void 0 : _a2[id];
        if ((def == null ? void 0 : def.type) === "number" && typeof finalValue === "number") {
          if (typeof def.min === "number" && finalValue < def.min) finalValue = def.min;
          if (typeof def.max === "number" && finalValue > def.max) finalValue = def.max;
        }
        variableChanges[id] = finalValue;
        context.onSetVariable(nameOrId, finalValue);
      },
      getAllVariables: () => {
        const result = {};
        for (const [id, val] of Object.entries(context.variables)) {
          const name = varIdToName[id] || id;
          result[name] = val;
        }
        return result;
      },
      // Documented alias of getAllVariables() (name → value map).
      getVariables: () => {
        const result = {};
        for (const [id, val] of Object.entries(context.variables)) {
          const name = varIdToName[id] || id;
          result[name] = val;
        }
        return result;
      },
      getScenes: () => {
        return Object.values(context.project.scenes || {}).map((s) => s.name);
      },
      getCharacters: () => {
        return Object.entries(context.project.characters || {}).map(([id, c]) => ({ id, name: c.name }));
      },
      // ── Inventory (items are sugar over a number "count" variable) ──
      getItemCount: (nameOrId) => {
        const item = resolveItem(nameOrId);
        if (!item) return 0;
        const v = context.variables[item.countVariableId];
        return typeof v === "number" ? v : Number(v) || 0;
      },
      hasItem: (nameOrId) => {
        const item = resolveItem(nameOrId);
        if (!item) return false;
        const v = context.variables[item.countVariableId];
        return (typeof v === "number" ? v : Number(v) || 0) > 0;
      },
      addItem: (nameOrId, amount = 1) => {
        const item = resolveItem(nameOrId);
        if (!item) {
          warnMissing(\`addItem: no item named "\${nameOrId}".\`);
          return;
        }
        const cur = Number(context.variables[item.countVariableId]) || 0;
        let next = cur + amount;
        if (item.unique) next = Math.min(1, Math.max(0, next));
        context.variables[item.countVariableId] = next;
        context.onSetVariable(item.countVariableId, next);
      },
      removeItem: (nameOrId, amount = 1) => {
        const item = resolveItem(nameOrId);
        if (!item) {
          warnMissing(\`removeItem: no item named "\${nameOrId}".\`);
          return;
        }
        const cur = Number(context.variables[item.countVariableId]) || 0;
        const next = Math.max(0, cur - amount);
        context.variables[item.countVariableId] = next;
        context.onSetVariable(item.countVariableId, next);
      },
      getItems: () => {
        return Object.values(context.project.items || {}).map((it) => ({
          id: it.id,
          name: it.name,
          count: Number(context.variables[it.countVariableId]) || 0
        }));
      },
      showDialogue: (characterName, text) => {
        context.onShowDialogue(characterName, text);
      },
      jumpToScene: (nameOrId) => {
        navigationRequest = { type: "scene", target: nameOrId };
        context.onJumpToScene(nameOrId);
      },
      jumpToLabel: (labelId) => {
        navigationRequest = { type: "label", target: labelId };
        context.onJumpToLabel(labelId);
      },
      // Returns the callback's result so a script may \`await game.runScript(...)\` for ordering.
      runScript: (nameOrId, args) => {
        var _a2;
        return (_a2 = context.onRunScript) == null ? void 0 : _a2.call(context, nameOrId, args);
      },
      callCommonEvent: (nameOrId, args) => {
        var _a2;
        return (_a2 = context.onCallCommonEvent) == null ? void 0 : _a2.call(context, nameOrId, args);
      },
      // ── Presentation: run real visual commands imperatively (reuses the engine handlers) ──
      showCharacter: async (idOrName, opts = {}) => {
        if (!context.onRunCommand) return;
        const characterId = resolveCharacterId(idOrName);
        if (!characterId) {
          console.warn(\`[Script] showCharacter: unknown character "\${idOrName}"\`);
          return;
        }
        const params = { characterId };
        if (opts.expression) {
          const e = resolveExpressionId(characterId, opts.expression);
          if (e) params.expressionId = e;
        }
        if (opts.position !== void 0) params.position = opts.position;
        if (opts.transition) params.transition = opts.transition;
        if (opts.duration !== void 0) params.duration = opts.duration;
        await context.onRunCommand("ShowCharacter", params);
      },
      hideCharacter: async (idOrName, opts = {}) => {
        if (!context.onRunCommand) return;
        const characterId = resolveCharacterId(idOrName);
        if (!characterId) {
          console.warn(\`[Script] hideCharacter: unknown character "\${idOrName}"\`);
          return;
        }
        const params = { characterId };
        if (opts.transition) params.transition = opts.transition;
        if (opts.duration !== void 0) params.duration = opts.duration;
        await context.onRunCommand("HideCharacter", params);
      },
      setBackground: async (idOrName, opts = {}) => {
        if (!context.onRunCommand) return;
        const backgroundId = resolveAssetId(context.project.backgrounds, idOrName) || resolveAssetId(context.project.images, idOrName);
        if (!backgroundId) {
          console.warn(\`[Script] setBackground: unknown background "\${idOrName}"\`);
          return;
        }
        const params = { backgroundId };
        if (opts.transition) params.transition = opts.transition;
        if (opts.duration !== void 0) params.duration = opts.duration;
        await context.onRunCommand("SetBackground", params);
      },
      showImage: async (idOrName, opts = {}) => {
        if (!context.onRunCommand) return;
        const imageId = resolveAssetId(context.project.images, idOrName);
        if (!imageId) {
          console.warn(\`[Script] showImage: unknown image "\${idOrName}"\`);
          return;
        }
        const params = { imageId };
        ["x", "y", "width", "height", "transition", "duration"].forEach((k) => {
          if (opts[k] !== void 0) params[k] = opts[k];
        });
        await context.onRunCommand("ShowImage", params);
      },
      // ── Screen effects (run the real effect commands) ──
      shakeScreen: async (opts = {}) => {
        if (!context.onRunCommand) return;
        const params = {};
        if (opts.intensity !== void 0) params.intensity = opts.intensity;
        if (opts.duration !== void 0) params.duration = opts.duration;
        await context.onRunCommand("ShakeScreen", params);
      },
      flashScreen: async (opts = {}) => {
        if (!context.onRunCommand) return;
        const params = {};
        if (opts.color !== void 0) params.color = opts.color;
        if (opts.duration !== void 0) params.duration = opts.duration;
        await context.onRunCommand("FlashScreen", params);
      },
      tintScreen: async (color, opts = {}) => {
        if (!context.onRunCommand) return;
        const params = { color };
        if (opts.duration !== void 0) params.duration = opts.duration;
        await context.onRunCommand("TintScreen", params);
      },
      panZoom: async (opts = {}) => {
        if (!context.onRunCommand) return;
        const params = {};
        ["zoom", "panX", "panY", "duration"].forEach((k) => {
          if (opts[k] !== void 0) params[k] = opts[k];
        });
        await context.onRunCommand("PanZoomScreen", params);
      },
      resetScreenEffects: async (opts = {}) => {
        if (!context.onRunCommand) return;
        const params = {};
        if (opts.duration !== void 0) params.duration = opts.duration;
        await context.onRunCommand("ResetScreenEffects", params);
      },
      // Particles + element tweens (power-user passthrough: opts map to the command's fields).
      spawnParticles: async (opts = {}) => {
        if (!context.onRunCommand) return;
        await context.onRunCommand("SpawnParticles", { ...opts });
      },
      stopParticles: async (opts = {}) => {
        if (!context.onRunCommand) return;
        await context.onRunCommand("StopParticles", { ...opts });
      },
      tween: async (opts = {}) => {
        if (!context.onRunCommand) return;
        await context.onRunCommand("TweenElement", { ...opts });
      },
      // ── Blocking: show UI and AWAIT the player's response (dialogue advance / choice / text). ──
      dialogue: async (speaker, text) => {
        if (!context.onRunCommand) {
          context.onShowDialogue(speaker, text);
          return;
        }
        const charId = speaker ? resolveCharacterId(speaker) : null;
        await context.onRunCommand("Dialogue", { characterId: charId || null, text: text ?? "" });
      },
      choice: async (options) => {
        if (!context.onRunCommand) return 0;
        const opts = (options || []).map((o) => ({
          id: \`script-opt-\${Math.random().toString(36).slice(2, 7)}\`,
          text: typeof o === "string" ? o : (o == null ? void 0 : o.text) ?? "",
          actions: []
        }));
        const idx = await context.onRunCommand("Choice", { options: opts });
        return typeof idx === "number" ? idx : 0;
      },
      textInput: async (prompt, opts = {}) => {
        if (!context.onRunCommand) return "";
        const params = { prompt: prompt ?? "" };
        if (opts.placeholder !== void 0) params.placeholder = opts.placeholder;
        if (opts.variable) {
          const vid = resolveVarId(opts.variable);
          if (vid) params.variableId = vid;
        }
        const val = await context.onRunCommand("TextInput", params);
        return typeof val === "string" ? val : "";
      },
      // ── Generic power-user escape hatches ──────────────────────────────────────────────
      // \`game.runCommand\` runs ANY scene command by type with raw params (the same handler the
      // editor uses); \`game.ui\` fires ANY UI action by type. These give full parity with the
      // editor — every command/action in the docs is reachable even without a named helper below.
      runCommand: async (type, params = {}) => {
        if (!context.onRunCommand) return;
        return await context.onRunCommand(type, params || {});
      },
      ui: (actionType, params = {}) => {
        var _a2;
        (_a2 = context.onRunUIAction) == null ? void 0 : _a2.call(context, actionType, params || {});
      },
      // ── Named UI helpers (sugar over game.ui) ──────────────────────────────────────────
      goToScreen: (screenIdOrName) => {
        var _a2;
        return (_a2 = context.onRunUIAction) == null ? void 0 : _a2.call(context, "GoToScreen", { targetScreenId: resolveScreenId(screenIdOrName) });
      },
      toggleScreen: (screenIdOrName) => {
        var _a2;
        return (_a2 = context.onRunUIAction) == null ? void 0 : _a2.call(context, "ToggleScreen", { targetScreenId: resolveScreenId(screenIdOrName) });
      },
      returnToGame: () => {
        var _a2;
        return (_a2 = context.onRunUIAction) == null ? void 0 : _a2.call(context, "ReturnToGame", {});
      },
      showElement: (elementId) => {
        var _a2;
        return (_a2 = context.onRunUIAction) == null ? void 0 : _a2.call(context, "ShowElement", { targetElementId: elementId });
      },
      hideElement: (elementId) => {
        var _a2;
        return (_a2 = context.onRunUIAction) == null ? void 0 : _a2.call(context, "HideElement", { targetElementId: elementId });
      },
      changeImage: (elementId, imageIdOrName) => {
        var _a2;
        return (_a2 = context.onRunUIAction) == null ? void 0 : _a2.call(context, "ChangeImage", { targetElementId: elementId, newImageId: resolveAssetId(context.project.images, imageIdOrName) || imageIdOrName });
      },
      playAnimation: (elementId, animation, duration) => {
        var _a2;
        return (_a2 = context.onRunUIAction) == null ? void 0 : _a2.call(context, "PlayAnimation", { targetElementId: elementId, animation, ...duration != null ? { duration } : {} });
      },
      saveGame: (slot = 0) => {
        var _a2;
        return (_a2 = context.onRunUIAction) == null ? void 0 : _a2.call(context, "SaveGame", { slotNumber: slot });
      },
      loadGame: (slot = 0) => {
        var _a2;
        return (_a2 = context.onRunUIAction) == null ? void 0 : _a2.call(context, "LoadGame", { slotNumber: slot });
      },
      quitToTitle: () => {
        var _a2;
        return (_a2 = context.onRunUIAction) == null ? void 0 : _a2.call(context, "QuitToTitle", {});
      },
      exitGame: () => {
        var _a2;
        return (_a2 = context.onRunUIAction) == null ? void 0 : _a2.call(context, "ExitGame", {});
      },
      openURL: (url, newTab = true) => {
        var _a2;
        return (_a2 = context.onRunUIAction) == null ? void 0 : _a2.call(context, "OpenURL", { url, newTab });
      },
      // ── Named command helpers (sugar over game.runCommand) ─────────────────────────────
      setCharacterLayer: async (idOrName, opts = {}) => {
        if (!context.onRunCommand) return;
        const characterId = resolveCharacterId(idOrName);
        if (!characterId) {
          console.warn(\`[Script] setCharacterLayer: unknown character "\${idOrName}"\`);
          return;
        }
        await context.onRunCommand("SetCharacterLayer", { characterId, ...opts });
      },
      showText: async (text, opts = {}) => {
        if (!context.onRunCommand) return;
        await context.onRunCommand("ShowText", { text, ...opts });
      },
      hideText: async (opts = {}) => {
        if (!context.onRunCommand) return;
        await context.onRunCommand("HideText", { ...opts });
      },
      hideImage: async (idOrName) => {
        if (!context.onRunCommand) return;
        await context.onRunCommand("HideImage", { imageId: resolveAssetId(context.project.images, idOrName) || idOrName });
      },
      stopSFX: async (idOrName, fadeDuration) => {
        if (!context.onRunCommand) return;
        await context.onRunCommand("StopSoundEffect", { audioId: resolveAssetId(context.project.audio, idOrName) || idOrName, ...fadeDuration != null ? { fadeDuration } : {} });
      },
      lightning: async (opts = {}) => {
        if (!context.onRunCommand) return;
        await context.onRunCommand("Lightning", { ...opts });
      },
      fireworks: async (opts = {}) => {
        if (!context.onRunCommand) return;
        await context.onRunCommand("Fireworks", { ...opts });
      },
      flashlight: async (opts = {}) => {
        if (!context.onRunCommand) return;
        await context.onRunCommand("Flashlight", { enabled: true, ...opts });
      },
      flashlightOff: async () => {
        if (!context.onRunCommand) return;
        await context.onRunCommand("Flashlight", { enabled: false });
      },
      spotlight: async (opts = {}) => {
        if (!context.onRunCommand) return;
        await context.onRunCommand("Spotlight", { enabled: true, ...opts });
      },
      spotlightOff: async () => {
        if (!context.onRunCommand) return;
        await context.onRunCommand("Spotlight", { enabled: false });
      },
      screenOverlay: async (effectType, opts = {}) => {
        if (!context.onRunCommand) return;
        await context.onRunCommand("SetScreenOverlayEffect", { effectType, ...opts });
      },
      creditRoll: async (opts = {}) => {
        if (!context.onRunCommand) return;
        await context.onRunCommand("CreditRoll", { ...opts });
      },
      playMovie: async (idOrName, opts = {}) => {
        if (!context.onRunCommand) return;
        const videoId = resolveAssetId(context.project.videos, idOrName) || resolveAssetId(context.project.backgrounds, idOrName) || resolveAssetId(context.project.images, idOrName) || idOrName;
        await context.onRunCommand("PlayMovie", { videoId, ...opts });
      },
      stopMovie: async () => {
        if (!context.onRunCommand) return;
        await context.onRunCommand("StopMovie", {});
      },
      playSFX: (nameOrId, volume) => {
        context.onPlaySFX(nameOrId, volume);
      },
      playMusic: (nameOrId, loop, volume) => {
        context.onPlayMusic(nameOrId, loop, volume);
      },
      stopMusic: (fadeDuration) => {
        context.onStopMusic(fadeDuration);
      },
      log: (...args) => {
        console.log("[Script]", ...args);
      },
      notify: (message, type) => {
        context.onNotify(message, type);
      },
      currentScene: ((_a = context.project.scenes[context.currentSceneId]) == null ? void 0 : _a.name) || "",
      currentSceneId: context.currentSceneId,
      args: context.args || {},
      wait: (seconds) => {
        return new Promise((resolve) => setTimeout(resolve, seconds * 1e3));
      },
      random: (min, max) => {
        return Math.floor(Math.random() * (max - min + 1)) + min;
      },
      // Top-level convenience aliases (the doc referenced game.clamp / game.lerp).
      clamp: (value, min, max) => Math.min(Math.max(value, min), max),
      lerp: (start, end, t) => start + (end - start) * t,
      math: {
        clamp: (value, min, max) => Math.min(Math.max(value, min), max),
        lerp: (start, end, t) => start + (end - start) * t,
        randomFloat: (min, max) => Math.random() * (max - min) + min
      }
    };
    try {
      if (/\\.\\s*constructor\\b/.test(script.code) || /\\[\\s*['"]constructor['"]\\s*\\]/.test(script.code)) {
        return {
          success: false,
          error: 'Access to ".constructor" is blocked in the script sandbox.',
          duration: performance.now() - startTime
        };
      }
      if (/\\beval\\s*\\(/.test(script.code)) {
        return {
          success: false,
          error: "eval() is blocked in the script sandbox.",
          duration: performance.now() - startTime
        };
      }
      const blockedGlobals = [
        "document",
        "window",
        "globalThis",
        "self",
        "fetch",
        "XMLHttpRequest",
        "WebSocket",
        "localStorage",
        "sessionStorage",
        "indexedDB",
        "Function",
        "setTimeout",
        "setInterval",
        "clearTimeout",
        "clearInterval",
        "requestAnimationFrame",
        "queueMicrotask"
      ];
      const blockStatements = blockedGlobals.map((g) => \`var \${g} = undefined;\`).join("\\n");
      const wrappedCode = \`
            "use strict";
            \${blockStatements}
            \${script.code}
        \`;
      const AsyncFunction = Object.getPrototypeOf(async function() {
      }).constructor;
      const scriptFn = new AsyncFunction("game", wrappedCode);
      const returnValue = await scriptFn(gameAPI);
      return {
        success: true,
        returnValue,
        variableChanges: Object.keys(variableChanges).length > 0 ? variableChanges : void 0,
        navigationRequest,
        duration: performance.now() - startTime
      };
    } catch (err) {
      return {
        success: false,
        error: err.message || String(err),
        stack: err.stack,
        variableChanges: Object.keys(variableChanges).length > 0 ? variableChanges : void 0,
        navigationRequest,
        duration: performance.now() - startTime
      };
    }
  }
  const MAX_CALL_DEPTH = 32;
  function handleCallCommonEvent(command, context) {
    var _a, _b, _c;
    const { project, playerState } = context;
    const commonEvents = project.commonEvents || {};
    const commonEvent = commonEvents[command.commonEventId];
    if (!commonEvent) {
      console.warn(\`[CallCommonEvent] Common event not found: \${command.commonEventId}\`);
      return { advance: true };
    }
    if (!commonEvent.enabled) {
      console.warn(\`[CallCommonEvent] Common event is disabled: \${commonEvent.name}\`);
      return { advance: true };
    }
    if (!commonEvent.commands || commonEvent.commands.length === 0) {
      console.warn(\`[CallCommonEvent] Common event has no commands: \${commonEvent.name}\`);
      return { advance: true };
    }
    if (playerState.commandStack.length >= MAX_CALL_DEPTH) {
      console.error(\`[CallCommonEvent] Max call depth (\${MAX_CALL_DEPTH}) reached calling "\${commonEvent.name}"\`);
      (_a = context.notify) == null ? void 0 : _a.call(context, \`Common Event call depth limit reached ("\${commonEvent.name}")\`, "error");
      return { advance: true };
    }
    if (playerState.commandStack.some((frame) => frame.commonEventId === commonEvent.id)) {
      console.error(\`[CallCommonEvent] Cycle detected — "\${commonEvent.name}" is already on the call stack\`);
      (_b = context.notify) == null ? void 0 : _b.call(context, \`Common Event cycle blocked ("\${commonEvent.name}")\`, "error");
      return { advance: true };
    }
    const variableOverrides = {};
    const savedVariables = {};
    const clearedVariables = [];
    if (commonEvent.parameters && commonEvent.parameters.length > 0) {
      for (const param of commonEvent.parameters) {
        const raw = (_c = command.arguments) == null ? void 0 : _c[param.id];
        const value = raw !== void 0 ? coerceParam(raw, param.type) : param.defaultValue;
        variableOverrides[param.id] = value;
        if (Object.prototype.hasOwnProperty.call(playerState.variables, param.id)) {
          savedVariables[param.id] = playerState.variables[param.id];
        } else {
          clearedVariables.push(param.id);
        }
      }
    }
    const newStack = [
      ...playerState.commandStack,
      {
        sceneId: playerState.currentSceneId,
        commands: playerState.currentCommands,
        index: playerState.currentIndex + 1,
        commonEventId: commonEvent.id,
        ...Object.keys(savedVariables).length > 0 ? { savedVariables } : {},
        ...clearedVariables.length > 0 ? { clearedVariables } : {}
      }
    ];
    return {
      advance: false,
      // We handle navigation ourselves
      updates: {
        currentCommands: commonEvent.commands,
        currentIndex: 0,
        commandStack: newStack,
        // Merge parameter arguments into variables
        ...Object.keys(variableOverrides).length > 0 ? { variables: { ...playerState.variables, ...variableOverrides } } : {}
      }
    };
  }
  function coerceParam(value, type) {
    if (type === "number") {
      const n = typeof value === "number" ? value : parseFloat(String(value));
      return isNaN(n) ? 0 : n;
    }
    if (type === "boolean") {
      if (typeof value === "boolean") return value;
      return value === "true" || value === 1 || value === "1";
    }
    return String(value);
  }
  const MAX_SCRIPT_DEPTH = 16;
  function findScript(project, nameOrId) {
    const scripts = project.scripts || {};
    if (scripts[nameOrId]) return scripts[nameOrId];
    const lower = nameOrId.toLowerCase();
    return Object.values(scripts).find((s) => s.name.toLowerCase() === lower);
  }
  function coerceValue(value, type) {
    if (type === "number") {
      const n = typeof value === "number" ? value : parseFloat(String(value));
      return isNaN(n) ? 0 : n;
    }
    if (type === "boolean") {
      if (typeof value === "boolean") return value;
      return value === "true" || value === 1 || value === "1";
    }
    return String(value);
  }
  function resolveArgs(script, supplied) {
    const out = {};
    for (const p of script.params || []) {
      let v;
      if (supplied) {
        if (p.id in supplied) v = supplied[p.id];
        else if (p.name in supplied) v = supplied[p.name];
      }
      out[p.name] = v !== void 0 ? coerceValue(v, p.type) : p.defaultValue;
    }
    return out;
  }
  const handleRunScript = async (command, context) => {
    var _a;
    const { project, playerState } = context;
    const script = (project.scripts || {})[command.scriptId];
    if (!script) {
      console.warn(\`[RunScript] Script not found: \${command.scriptId}\`);
      return { advance: true };
    }
    if (!script.enabled) {
      console.log(\`[RunScript] Script "\${script.name}" is disabled, skipping.\`);
      return { advance: true };
    }
    const sceneNameToId = {};
    for (const [id, scene] of Object.entries(project.scenes)) {
      sceneNameToId[scene.name.toLowerCase()] = id;
    }
    const resolveAudioId = (nameOrId) => {
      if (project.audio[nameOrId]) return nameOrId;
      const lower = nameOrId.toLowerCase();
      for (const [id, audio] of Object.entries(project.audio)) {
        if (audio.name.toLowerCase() === lower) return id;
      }
      return nameOrId;
    };
    const resolveVarId = (nameOrId) => {
      if (project.variables[nameOrId]) return nameOrId;
      const lower = nameOrId.toLowerCase();
      for (const [id, variable] of Object.entries(project.variables)) {
        if (variable.name.toLowerCase() === lower) return id;
      }
      return nameOrId;
    };
    const variableUpdates = { ...playerState.variables };
    let dialogueUpdate;
    let musicStateUpdate;
    let navigationRequest;
    let pendingCommonEvent;
    const playMusicImperative = (nameOrId, loop, volume) => {
      const audioId = resolveAudioId(nameOrId);
      const url = context.assetResolver(audioId, "audio");
      const audio = context.musicAudioRef.current;
      if (!url || !audio) {
        console.warn(\`[Script] playMusic: no audio for "\${nameOrId}"\`);
        return;
      }
      const currentSrcPath = audio.src ? new URL(audio.src, window.location.href).pathname : null;
      const newSrcPath = new URL(url, window.location.href).pathname;
      const isNewTrack = currentSrcPath !== newSrcPath;
      const target = typeof volume === "number" ? volume : context.settings.musicVolume;
      const startPlayback = () => {
        audio.loop = !!loop;
        audio.volume = 0;
        audio.play().then(() => context.fadeAudio(audio, target, 1)).catch((e) => console.error("[Script] playMusic failed:", e));
      };
      if (isNewTrack) {
        audio.src = url;
        audio.load();
        audio.addEventListener("canplaythrough", startPlayback, { once: true });
      } else if (audio.paused) {
        startPlayback();
      }
      musicStateUpdate = { audioId, loop: !!loop, currentTime: 0, isPlaying: true };
    };
    const runScriptInternal = async (scr, args, depth) => {
      var _a2, _b;
      if (depth > MAX_SCRIPT_DEPTH) {
        console.error(\`[RunScript] Recursion limit (\${MAX_SCRIPT_DEPTH}) reached at "\${scr.name}"\`);
        if (!context.isStandalone) (_a2 = context.notify) == null ? void 0 : _a2.call(context, \`Script recursion limit reached ("\${scr.name}")\`, "error");
        return;
      }
      const runtimeContext = {
        project,
        variables: variableUpdates,
        // reads see prior accumulated writes
        currentSceneId: playerState.currentSceneId || project.startSceneId,
        args,
        onSetVariable: (nameOrId, value) => {
          variableUpdates[resolveVarId(nameOrId)] = value;
        },
        onJumpToScene: () => {
        },
        onJumpToLabel: () => {
        },
        onShowDialogue: (characterName, text) => {
          const match = Object.values(project.characters).find((c) => c.name.toLowerCase() === characterName.toLowerCase());
          dialogueUpdate = {
            characterName: characterName || "Narrator",
            characterColor: (match == null ? void 0 : match.color) || "#FFFFFF",
            characterId: (match == null ? void 0 : match.id) || null,
            text
          };
        },
        onPlaySFX: (nameOrId, volume) => {
          context.playSound(resolveAudioId(nameOrId), volume);
        },
        onPlayMusic: (nameOrId, loop, volume) => {
          playMusicImperative(nameOrId, loop, volume);
        },
        onStopMusic: (fadeDuration) => {
          if (context.musicAudioRef.current) {
            context.fadeAudio(context.musicAudioRef.current, 0, fadeDuration || 1);
          }
          musicStateUpdate = { audioId: null, loop: false, currentTime: 0, isPlaying: false };
        },
        onNotify: (message, type) => {
          var _a3;
          (_a3 = context.notify) == null ? void 0 : _a3.call(context, message, type);
        },
        onRunScript: (nameOrId, a) => {
          const target = findScript(project, nameOrId);
          if (!target) {
            console.warn(\`[Script] runScript: script not found "\${nameOrId}"\`);
            return void 0;
          }
          if (!target.enabled) return void 0;
          return runScriptInternal(target, resolveArgs(target, a), depth + 1);
        },
        onCallCommonEvent: (nameOrId, a) => {
          const events = project.commonEvents || {};
          let ce = events[nameOrId];
          if (!ce) {
            const lower = nameOrId.toLowerCase();
            ce = Object.values(events).find((e) => e.name.toLowerCase() === lower);
          }
          if (!ce || !ce.enabled) {
            console.warn(\`[Script] callCommonEvent: not found/disabled "\${nameOrId}"\`);
            return;
          }
          const overrides = {};
          for (const param of ce.parameters || []) {
            let v;
            if (a) {
              if (param.id in a) v = a[param.id];
              else if (param.name in a) v = a[param.name];
            }
            overrides[param.id] = v !== void 0 ? v : param.defaultValue;
          }
          pendingCommonEvent = { commonEventId: ce.id, variableOverrides: overrides };
        },
        // Presentation parity: run a real engine command imperatively, reusing the live runtime's
        // command handlers. No-op if the runtime didn't provide the bridge (e.g. ScriptEditor test run).
        onRunCommand: context.runCommand ? (type, params) => context.runCommand(type, params) : void 0,
        // UI parity: fire a real UI action (go to screen, save/load, show/hide element…).
        onRunUIAction: context.runUIAction ? (actionType, params) => context.runUIAction(actionType, params) : void 0,
        isStandalone: context.isStandalone
      };
      const result = await executeScript(scr, runtimeContext);
      if (!result.success) {
        console.error(\`[RunScript] Script "\${scr.name}" failed:\`, result.error);
        if (result.stack) console.error(result.stack);
        if (!context.isStandalone) (_b = context.notify) == null ? void 0 : _b.call(context, \`Script "\${scr.name}" error: \${result.error}\`, "error");
      } else {
        console.log(\`[RunScript] Script "\${scr.name}" completed in \${result.duration.toFixed(1)}ms\`);
      }
      if (result.navigationRequest) navigationRequest = result.navigationRequest;
    };
    await runScriptInternal(script, resolveArgs(script, command.arguments), 0);
    const updates = {};
    if (Object.keys(variableUpdates).length > 0) updates.variables = variableUpdates;
    if (dialogueUpdate) updates.uiState = { ...updates.uiState || {}, dialogue: dialogueUpdate };
    if (musicStateUpdate) updates.musicState = musicStateUpdate;
    if (pendingCommonEvent) {
      const ce = (project.commonEvents || {})[pendingCommonEvent.commonEventId];
      const onStack = playerState.commandStack.some((f) => f.commonEventId === pendingCommonEvent.commonEventId);
      if (playerState.commandStack.length >= MAX_CALL_DEPTH || onStack) {
        console.error(\`[Script] callCommonEvent blocked (depth/cycle): "\${(ce == null ? void 0 : ce.name) || pendingCommonEvent.commonEventId}"\`);
        if (!context.isStandalone) (_a = context.notify) == null ? void 0 : _a.call(context, \`Common Event call blocked (depth/cycle): "\${(ce == null ? void 0 : ce.name) || ""}"\`, "error");
      } else if (ce && ce.commands && ce.commands.length > 0) {
        const savedVariables = {};
        const clearedVariables = [];
        for (const k of Object.keys(pendingCommonEvent.variableOverrides)) {
          if (Object.prototype.hasOwnProperty.call(variableUpdates, k)) savedVariables[k] = variableUpdates[k];
          else clearedVariables.push(k);
        }
        const newStack = [
          ...playerState.commandStack,
          {
            sceneId: playerState.currentSceneId,
            commands: playerState.currentCommands,
            index: playerState.currentIndex + 1,
            commonEventId: ce.id,
            ...Object.keys(savedVariables).length > 0 ? { savedVariables } : {},
            ...clearedVariables.length > 0 ? { clearedVariables } : {}
          }
        ];
        return {
          advance: false,
          updates: {
            ...updates,
            currentCommands: ce.commands,
            currentIndex: 0,
            commandStack: newStack,
            variables: { ...variableUpdates, ...pendingCommonEvent.variableOverrides }
          }
        };
      }
    }
    if (navigationRequest) {
      if (navigationRequest.type === "scene") {
        const target = navigationRequest.target;
        let sceneId = target;
        if (!project.scenes[target]) sceneId = sceneNameToId[target.toLowerCase()] || target;
        if (project.scenes[sceneId]) {
          updates.currentSceneId = sceneId;
          updates.currentCommands = project.scenes[sceneId].commands;
          updates.currentIndex = 0;
        }
      } else if (navigationRequest.type === "label") {
        const currentSceneId = playerState.currentSceneId || project.startSceneId;
        const scene = project.scenes[currentSceneId];
        if (scene) {
          const labelIndex = scene.commands.findIndex(
            (cmd) => cmd.type === "Label" && cmd.labelId === navigationRequest.target
          );
          if (labelIndex >= 0) updates.currentIndex = labelIndex + 1;
        }
      }
    }
    return { advance: true, updates };
  };
  function handleSpawnParticles(command, context) {
    var _a, _b;
    const { playerState, activeEffectTimeoutsRef, advance, setPlayerState } = context;
    const tag = command.particleTag || \`particles_\${command.id}\`;
    const hasBindings = !!(command.emitRateVariableId || command.windVariableId || command.gravityVariableId || command.opacityVariableId);
    const particleEntry = {
      tag,
      config: command.config,
      startTime: Date.now(),
      duration: command.duration || 0,
      // LIVE variable bindings ride the entry; the stage render resolves them into the
      // config each frame-render so the emitter reacts while running.
      ...hasBindings ? {
        varBindings: {
          emitRate: command.emitRateVariableId ?? null,
          wind: command.windVariableId ?? null,
          gravity: command.gravityVariableId ?? null,
          opacity: command.opacityVariableId ?? null
        }
      } : {}
    };
    console.log("[ParticleHandler] SpawnParticles:", { tag, preset: (_a = command.config) == null ? void 0 : _a.preset, emitRate: (_b = command.config) == null ? void 0 : _b.emitRate, duration: command.duration, configKeys: Object.keys(command.config || {}) });
    if (command.duration > 0) {
      const timeout = window.setTimeout(() => {
        setPlayerState((p) => {
          if (!p) return null;
          const { [tag]: _, ...remaining } = p.stageState.particleEffects || {};
          return {
            ...p,
            stageState: {
              ...p.stageState,
              particleEffects: remaining
            }
          };
        });
      }, command.duration * 1e3);
      activeEffectTimeoutsRef.current.push(timeout);
    }
    return {
      advance: true,
      updates: {
        stageState: {
          ...playerState.stageState,
          particleEffects: {
            ...playerState.stageState.particleEffects || {},
            [tag]: particleEntry
          }
        }
      }
    };
  }
  function handleStopParticles(command, context) {
    const { playerState, setPlayerState, advance: advanceFn } = context;
    if (command.fadeDuration > 0) {
      const tag = command.particleTag;
      const currentEffects2 = { ...playerState.stageState.particleEffects || {} };
      if (tag) {
        if (currentEffects2[tag]) {
          currentEffects2[tag] = {
            ...currentEffects2[tag],
            fadingOut: true,
            fadeOutDuration: command.fadeDuration
          };
        }
      } else {
        for (const key of Object.keys(currentEffects2)) {
          currentEffects2[key] = {
            ...currentEffects2[key],
            fadingOut: true,
            fadeOutDuration: command.fadeDuration
          };
        }
      }
      const timeout = window.setTimeout(() => {
        setPlayerState((p) => {
          if (!p) return null;
          const effects = { ...p.stageState.particleEffects || {} };
          if (tag) {
            delete effects[tag];
          } else {
            for (const key of Object.keys(effects)) {
              delete effects[key];
            }
          }
          return {
            ...p,
            stageState: { ...p.stageState, particleEffects: effects }
          };
        });
      }, command.fadeDuration * 1e3);
      context.activeEffectTimeoutsRef.current.push(timeout);
      return {
        advance: true,
        updates: {
          stageState: {
            ...playerState.stageState,
            particleEffects: currentEffects2
          }
        }
      };
    }
    const currentEffects = { ...playerState.stageState.particleEffects || {} };
    if (command.particleTag) {
      delete currentEffects[command.particleTag];
    } else {
      for (const key of Object.keys(currentEffects)) {
        delete currentEffects[key];
      }
    }
    return {
      advance: true,
      updates: {
        stageState: {
          ...playerState.stageState,
          particleEffects: currentEffects
        }
      }
    };
  }
  const PRESET_COORDS = {
    "left": { x: 25, y: 10 },
    "center": { x: 50, y: 10 },
    "right": { x: 75, y: 10 },
    "off-left": { x: -25, y: 10 },
    "off-right": { x: 125, y: 10 }
  };
  function captureCurrentValues(command, context) {
    const effectiveId = command.targetType === "screen" ? "__screen__" : command.targetId;
    const resting = TweenManager.getRestingValues(effectiveId, command.targetType);
    const state = context.playerState.stageState;
    const current = {};
    switch (command.targetType) {
      case "character": {
        const char = state.characters[command.targetId];
        if (!char) break;
        const pos = typeof char.position === "object" ? char.position : PRESET_COORDS[char.position] ?? { x: 50, y: 10 };
        if (command.x !== void 0) current.x = (resting == null ? void 0 : resting.x) ?? pos.x;
        if (command.y !== void 0) current.y = (resting == null ? void 0 : resting.y) ?? pos.y;
        if (command.scale !== void 0) current.scale = (resting == null ? void 0 : resting.scale) ?? char.scale ?? 1;
        if (command.opacity !== void 0) current.opacity = (resting == null ? void 0 : resting.opacity) ?? 1;
        break;
      }
      case "image": {
        const img = state.imageOverlays.find((o) => o.id === command.targetId);
        if (!img) break;
        if (command.x !== void 0) current.x = (resting == null ? void 0 : resting.x) ?? img.x;
        if (command.y !== void 0) current.y = (resting == null ? void 0 : resting.y) ?? img.y;
        if (command.width !== void 0) current.width = (resting == null ? void 0 : resting.width) ?? img.width;
        if (command.height !== void 0) current.height = (resting == null ? void 0 : resting.height) ?? img.height;
        if (command.opacity !== void 0) current.opacity = (resting == null ? void 0 : resting.opacity) ?? img.opacity;
        if (command.rotation !== void 0) current.rotation = (resting == null ? void 0 : resting.rotation) ?? img.rotation;
        if (command.scaleX !== void 0) current.scaleX = (resting == null ? void 0 : resting.scaleX) ?? img.scaleX;
        if (command.scaleY !== void 0) current.scaleY = (resting == null ? void 0 : resting.scaleY) ?? img.scaleY;
        break;
      }
      case "text": {
        const txt = state.textOverlays.find((o) => o.id === command.targetId);
        if (!txt) break;
        if (command.x !== void 0) current.x = (resting == null ? void 0 : resting.x) ?? txt.x;
        if (command.y !== void 0) current.y = (resting == null ? void 0 : resting.y) ?? txt.y;
        if (command.fontSize !== void 0) current.fontSize = (resting == null ? void 0 : resting.fontSize) ?? txt.fontSize;
        if (command.width !== void 0) current.width = (resting == null ? void 0 : resting.width) ?? txt.width ?? 0;
        if (command.height !== void 0) current.height = (resting == null ? void 0 : resting.height) ?? txt.height ?? 0;
        if (command.color !== void 0) current.color = (resting == null ? void 0 : resting.color) ?? txt.color;
        break;
      }
      case "button": {
        const btn = state.buttonOverlays.find((o) => o.id === command.targetId);
        if (!btn) break;
        if (command.x !== void 0) current.x = (resting == null ? void 0 : resting.x) ?? btn.x;
        if (command.y !== void 0) current.y = (resting == null ? void 0 : resting.y) ?? btn.y;
        if (command.width !== void 0) current.width = (resting == null ? void 0 : resting.width) ?? btn.width;
        if (command.height !== void 0) current.height = (resting == null ? void 0 : resting.height) ?? btn.height;
        if (command.opacity !== void 0) current.opacity = (resting == null ? void 0 : resting.opacity) ?? btn.opacity;
        if (command.fontSize !== void 0) current.fontSize = (resting == null ? void 0 : resting.fontSize) ?? btn.fontSize;
        if (command.borderRadius !== void 0) current.borderRadius = (resting == null ? void 0 : resting.borderRadius) ?? btn.borderRadius;
        if (command.backgroundColor !== void 0) current.backgroundColor = (resting == null ? void 0 : resting.backgroundColor) ?? btn.backgroundColor;
        break;
      }
      case "movie": {
        let base = (state.movieOverlays || []).find((o) => o.commandId === command.targetId);
        if (!base) {
          for (const scene of Object.values(context.project.scenes)) {
            const c = (scene.commands || []).find((cc) => cc.id === command.targetId);
            if (c) {
              base = c;
              break;
            }
          }
        }
        if (!base) break;
        if (command.x !== void 0) current.x = (resting == null ? void 0 : resting.x) ?? base.x ?? 0;
        if (command.y !== void 0) current.y = (resting == null ? void 0 : resting.y) ?? base.y ?? 0;
        if (command.width !== void 0) current.width = (resting == null ? void 0 : resting.width) ?? base.width ?? 100;
        if (command.height !== void 0) current.height = (resting == null ? void 0 : resting.height) ?? base.height ?? 100;
        if (command.opacity !== void 0) current.opacity = (resting == null ? void 0 : resting.opacity) ?? base.opacity ?? 1;
        if (command.rotation !== void 0) current.rotation = (resting == null ? void 0 : resting.rotation) ?? 0;
        if (command.scaleX !== void 0) current.scaleX = (resting == null ? void 0 : resting.scaleX) ?? 1;
        if (command.scaleY !== void 0) current.scaleY = (resting == null ? void 0 : resting.scaleY) ?? 1;
        break;
      }
      case "screen": {
        if (command.zoom !== void 0) current.scaleX = (resting == null ? void 0 : resting.scaleX) ?? state.screen.zoom;
        if (command.panX !== void 0) current.x = (resting == null ? void 0 : resting.x) ?? state.screen.panX;
        if (command.panY !== void 0) current.y = (resting == null ? void 0 : resting.y) ?? state.screen.panY;
        break;
      }
    }
    return current;
  }
  function buildTargetValues(command) {
    const to = {};
    if (command.x !== void 0) to.x = command.x;
    if (command.y !== void 0) to.y = command.y;
    if (command.width !== void 0) to.width = command.width;
    if (command.height !== void 0) to.height = command.height;
    if (command.opacity !== void 0) to.opacity = command.opacity;
    if (command.rotation !== void 0) to.rotation = command.rotation;
    if (command.scaleX !== void 0) to.scaleX = command.scaleX;
    if (command.scaleY !== void 0) to.scaleY = command.scaleY;
    if (command.scale !== void 0) to.scale = command.scale;
    if (command.fontSize !== void 0) to.fontSize = command.fontSize;
    if (command.borderRadius !== void 0) to.borderRadius = command.borderRadius;
    if (command.color !== void 0) to.color = command.color;
    if (command.backgroundColor !== void 0) to.backgroundColor = command.backgroundColor;
    if (command.zoom !== void 0) to.scaleX = command.zoom;
    if (command.panX !== void 0 && command.targetType === "screen") to.x = command.panX;
    if (command.panY !== void 0 && command.targetType === "screen") to.y = command.panY;
    return to;
  }
  function handleTweenElement(command, context) {
    const { advance } = context;
    const effectiveId = command.targetType === "screen" ? "__screen__" : command.targetId;
    const from = captureCurrentValues(command, context);
    const to = buildTargetValues(command);
    if (Object.keys(to).length === 0) {
      return { advance: true };
    }
    const waitForCompletion = command.waitForCompletion !== false;
    TweenManager.start({
      targetId: effectiveId,
      targetType: command.targetType,
      from,
      to,
      easing: command.easing || "easeInOutCubic",
      duration: command.duration,
      onComplete: waitForCompletion ? () => advance() : void 0
    });
    return {
      advance: !waitForCompletion
    };
  }
  function posToCoords(p) {
    if (typeof p === "string") return PRESET_COORDS[p] ?? { x: 50, y: 10 };
    if (p && typeof p === "object") return p;
    return { x: 50, y: 10 };
  }
  function handleMoveCharacter(command, context) {
    const { advance } = context;
    const characterId = resolveCommandCharacterId(command, context.project, context.playerState.variables) || command.characterId;
    const char = characterId ? context.playerState.stageState.characters[characterId] : void 0;
    if (!characterId || !char) return { advance: true };
    const resting = TweenManager.getRestingValues(characterId, "character");
    const curPos = typeof char.position === "object" ? char.position : PRESET_COORDS[char.position] ?? { x: 50, y: 10 };
    const fromPos = command.fromPosition ? posToCoords(command.fromPosition) : { x: (resting == null ? void 0 : resting.x) ?? curPos.x, y: (resting == null ? void 0 : resting.y) ?? curPos.y };
    const toPos = posToCoords(command.toPosition);
    const from = { x: fromPos.x, y: fromPos.y };
    const to = { x: toPos.x, y: toPos.y };
    if (command.scale !== void 0) {
      from.scale = (resting == null ? void 0 : resting.scale) ?? char.scale ?? 1;
      to.scale = command.scale;
    }
    if (command.opacity !== void 0) {
      from.opacity = (resting == null ? void 0 : resting.opacity) ?? 1;
      to.opacity = command.opacity;
    }
    if (command.rotation !== void 0) {
      from.rotation = (resting == null ? void 0 : resting.rotation) ?? char.rotation ?? 0;
      to.rotation = command.rotation;
    }
    const waitForCompletion = command.waitForCompletion !== false;
    TweenManager.start({
      targetId: characterId,
      targetType: "character",
      from,
      to,
      easing: command.easing || "easeInOutCubic",
      duration: command.duration,
      onComplete: waitForCompletion ? () => advance() : void 0
    });
    const stagePatch = (prev) => {
      const existing = prev.characters[characterId];
      if (!existing) return {};
      const updated = { ...existing, position: command.toPosition };
      if (command.scale !== void 0) updated.scale = command.scale;
      if (command.rotation !== void 0) updated.rotation = command.rotation;
      return { characters: { ...prev.characters, [characterId]: updated } };
    };
    return { advance: !waitForCompletion, stagePatch };
  }
  const computeCollectionRestock = (collection, variables) => {
    var _a;
    const out = {};
    const mode = ((_a = collection.restock) == null ? void 0 : _a.amount) || "reset";
    for (const e of collection.entries) {
      const target = Math.floor(e.restockTo ?? e.startQty ?? 0);
      let val;
      if (mode === "randomRange") {
        const lo = Math.max(0, Math.floor(e.restockMin ?? 0));
        const hi = Math.max(lo, target);
        val = lo + Math.floor(Math.random() * (hi - lo + 1));
      } else {
        val = Math.max(0, target);
      }
      const v = variables[e.countVariableId];
      if (v) {
        if (typeof v.min === "number") val = Math.max(v.min, val);
        if (typeof v.max === "number") val = Math.min(v.max, val);
      }
      out[e.countVariableId] = val;
    }
    return out;
  };
  const num = (v) => Number(v ?? 0);
  const clampVar = (project, varId, value) => {
    const v = project.variables[varId];
    return clampNumberToBounds(value, v == null ? void 0 : v.min, v == null ? void 0 : v.max);
  };
  const tradePrice = (item, entry) => Math.max(0, Math.floor((entry == null ? void 0 : entry.price) ?? (item == null ? void 0 : item.price) ?? 0));
  const computeBuy = (itemId, collection, project, vars) => {
    var _a;
    const item = (_a = project.items) == null ? void 0 : _a[itemId];
    const entry = collection.entries.find((e) => e.itemId === itemId);
    if (!item || !entry) return { blocked: "invalid" };
    const price = tradePrice(item, entry);
    const currencyId = collection.currencyVariableId;
    if (currencyId && num(vars[currencyId]) < price) return { blocked: "funds" };
    if (!entry.infiniteStock && num(vars[entry.countVariableId]) < 1) return { blocked: "stock" };
    const updates = {};
    if (currencyId) updates[currencyId] = clampVar(project, currencyId, num(vars[currencyId]) - price);
    if (!entry.infiniteStock) updates[entry.countVariableId] = clampVar(project, entry.countVariableId, num(vars[entry.countVariableId]) - 1);
    updates[item.countVariableId] = item.unique ? 1 : clampVar(project, item.countVariableId, num(vars[item.countVariableId]) + 1);
    return { updates };
  };
  const computeSell = (itemId, collection, project, vars) => {
    var _a;
    const item = (_a = project.items) == null ? void 0 : _a[itemId];
    if (!item) return { blocked: "invalid" };
    if (num(vars[item.countVariableId]) < 1) return { blocked: "owned" };
    const entry = collection.entries.find((e) => e.itemId === itemId);
    const price = tradePrice(item, entry);
    const gain = Math.floor(price * (collection.sellMultiplier ?? 0.5));
    const currencyId = collection.currencyVariableId;
    const updates = {};
    updates[item.countVariableId] = clampVar(project, item.countVariableId, num(vars[item.countVariableId]) - 1);
    if (currencyId) updates[currencyId] = clampVar(project, currencyId, num(vars[currencyId]) + gain);
    if (collection.sellRestocksShop && entry) updates[entry.countVariableId] = clampVar(project, entry.countVariableId, num(vars[entry.countVariableId]) + 1);
    return { updates };
  };
  const handleItemCommand = (command, context) => {
    var _a;
    const { project, playerState } = context;
    const item = (_a = project.items) == null ? void 0 : _a[command.itemId];
    if (!item) return { advance: true, updates: {} };
    const vars = { ...playerState.variables };
    const countVar = project.variables[item.countVariableId];
    const clamp = (n) => {
      const min = (countVar == null ? void 0 : countVar.min) ?? 0;
      const max = countVar == null ? void 0 : countVar.max;
      let v = Math.max(min, n);
      if (max !== void 0) v = Math.min(max, v);
      return v;
    };
    const cur = Number(vars[item.countVariableId] ?? 0);
    if (command.type === CommandType.GiveItem) {
      vars[item.countVariableId] = item.unique ? 1 : clamp(cur + (command.quantity ?? 1));
    } else if (command.type === CommandType.DestroyItem) {
      const c = command;
      vars[item.countVariableId] = c.all ? clamp(0) : clamp(cur - (c.quantity ?? 1));
    } else {
      if (item.consumeOnUse !== false) vars[item.countVariableId] = clamp(cur - 1);
      for (const eff of item.useEffect || []) {
        if (eff.type === UIActionType.SetVariable) {
          const ea = eff;
          const ev = project.variables[ea.variableId];
          if (!ev) continue;
          const { effectiveOperator } = normalizeSetVariableOperatorByType(ev.type, ev.name, ea.operator);
          vars[ea.variableId] = calculateVariableValue(effectiveOperator, ev.type, vars[ea.variableId], ea.value, ea.randomMin, ea.randomMax, void 0, ev.min, ev.max);
        } else if (eff.type === UIActionType.ResetVariable) {
          const ev = project.variables[eff.variableId];
          if (ev) vars[eff.variableId] = ev.defaultValue;
        }
      }
    }
    return { advance: true, updates: { variables: vars } };
  };
  const handleRestockCollectionCommand = (command, context) => {
    var _a;
    const { project, playerState } = context;
    const collection = (_a = project.itemCollections) == null ? void 0 : _a[command.collectionId];
    if (!collection) return { advance: true, updates: {} };
    const restocked = computeCollectionRestock(collection, project.variables);
    return { advance: true, updates: { variables: { ...playerState.variables, ...restocked } } };
  };
  const handleBuyItemCommand = (command, context) => {
    var _a;
    const { project, playerState } = context;
    const collection = (_a = project.itemCollections) == null ? void 0 : _a[command.collectionId];
    if (!collection) return { advance: true, updates: {} };
    const res = computeBuy(command.itemId, collection, project, playerState.variables);
    if ("blocked" in res) return { advance: true, updates: {} };
    return { advance: true, updates: { variables: { ...playerState.variables, ...res.updates } } };
  };
  const handleSellItemCommand = (command, context) => {
    var _a;
    const { project, playerState } = context;
    const collection = (_a = project.itemCollections) == null ? void 0 : _a[command.collectionId];
    if (!collection) return { advance: true, updates: {} };
    const res = computeSell(command.itemId, collection, project, playerState.variables);
    if ("blocked" in res) return { advance: true, updates: {} };
    return { advance: true, updates: { variables: { ...playerState.variables, ...res.updates } } };
  };
  class CommandScheduler {
    constructor() {
      this.lastProcessed = null;
    }
    /**
     * Determines whether the supplied command should execute. Returns false if
     * the scheduler has already advanced past the same signature.
     */
    shouldProcess(signature) {
      if (!this.lastProcessed) {
        return true;
      }
      return !(this.lastProcessed.sceneId === signature.sceneId && this.lastProcessed.commandId === signature.commandId && this.lastProcessed.index === signature.index);
    }
    /**
     * Records the most recently processed command signature.
     */
    markProcessed(signature) {
      this.lastProcessed = signature;
    }
    /**
     * Resets processed state, typically when scenes change or the command loop
     * rewinds (e.g., user restarts a scene).
     */
    reset() {
      this.lastProcessed = null;
    }
    /**
     * Utility guard that replicates the \`lastProcessedCommandRef\` early exit in
     * the legacy runtime. Useful when a command attempts to advance after the
     * scheduler already moved ahead.
     */
    alreadyAdvancedPast(currentIndex) {
      if (!this.lastProcessed) {
        return false;
      }
      return this.lastProcessed.index > currentIndex;
    }
    getLastProcessed() {
      return this.lastProcessed;
    }
  }
  class RuntimeVariableStore {
    constructor(initial) {
      this.lastWriteOrder = [];
      this.globals = { ...(initial == null ? void 0 : initial.globals) ?? {} };
      this.scene = { ...(initial == null ? void 0 : initial.scene) ?? {} };
      this.transient = { ...(initial == null ? void 0 : initial.transient) ?? {} };
      this.lastWriteOrder = [...(initial == null ? void 0 : initial.lastWriteOrder) ?? []];
    }
    applyWrites(writes) {
      for (const write of writes) {
        const { variableId, value, scope, sourceCommandId } = write;
        switch (scope) {
          case "global":
            this.globals[variableId] = value;
            break;
          case "scene":
            this.scene[variableId] = value;
            break;
          case "transient":
            this.transient[variableId] = value;
            break;
          default:
            throw new Error(\`Unsupported variable scope: \${scope}\`);
        }
        if (sourceCommandId) {
          this.lastWriteOrder.push(sourceCommandId);
        }
      }
    }
    get(variableId) {
      if (variableId in this.transient) {
        return this.transient[variableId];
      }
      if (variableId in this.scene) {
        return this.scene[variableId];
      }
      return this.globals[variableId];
    }
    resetScene(sceneDefaults) {
      this.scene = { ...sceneDefaults ?? {} };
      this.transient = {};
    }
    resetTransient() {
      this.transient = {};
    }
    snapshot() {
      return {
        globals: { ...this.globals },
        scene: { ...this.scene },
        transient: { ...this.transient },
        lastWriteOrder: [...this.lastWriteOrder]
      };
    }
  }
  class RuntimeDiagnostics {
    constructor() {
      this.events = [];
      this.subscribers = /* @__PURE__ */ new Set();
    }
    emit(type, payload) {
      const event = {
        id: \`\${type}-\${Date.now()}-\${Math.random().toString(36).slice(2)}\`,
        timestamp: Date.now(),
        type,
        payload
      };
      this.events.push(event);
      for (const subscriber of this.subscribers) {
        subscriber(event);
      }
      return event;
    }
    getEvents() {
      return [...this.events];
    }
    clear() {
      this.events = [];
    }
    subscribe(subscriber) {
      this.subscribers.add(subscriber);
      return () => {
        this.subscribers.delete(subscriber);
      };
    }
  }
  const getOverlayTransitionClass = (transition, isHide) => {
    switch (transition) {
      case "fade":
        return isHide ? "transition-fade-out" : "transition-dissolve";
      case "dissolve":
        return isHide ? "transition-dissolve-out" : "transition-dissolve";
      case "slide":
        return "transition-slide";
      case "iris-in":
        return isHide ? "transition-iris-out" : "transition-iris-in";
      case "wipe-right":
        return isHide ? "transition-wipe-out-right" : "transition-wipe-right";
      default:
        return "transition-dissolve";
    }
  };
  const r3 = (n) => Math.round(n * 1e3) / 1e3;
  function hexToRgb(hex) {
    const m = /^#?([a-f\\d]{2})([a-f\\d]{2})([a-f\\d]{2})\$/i.exec((hex || "#ffffff").trim());
    return m ? { r: parseInt(m[1], 16), g: parseInt(m[2], 16), b: parseInt(m[3], 16) } : { r: 255, g: 255, b: 255 };
  }
  function hexToRgba\$1(hex, alpha) {
    const { r, g, b } = hexToRgb(hex);
    return \`rgba(\${r}, \${g}, \${b}, \${r3(Math.max(0, Math.min(1, alpha)))})\`;
  }
  function effOpacity(l) {
    return l.enabled ? l.tintOpacity ?? 0 : 0;
  }
  function effBrightness(l) {
    return l.enabled ? l.brightness ?? 1 : 1;
  }
  function effSaturation(l) {
    return l.enabled ? l.saturation ?? 1 : 1;
  }
  function lerpLayer(a, b, t) {
    return {
      enabled: a.enabled || b.enabled,
      tint: lerpColor(a.tint || "#ffffff", b.tint || "#ffffff", t),
      tintOpacity: r3(lerp(effOpacity(a), effOpacity(b), t)),
      brightness: r3(lerp(effBrightness(a), effBrightness(b), t)),
      saturation: r3(lerp(effSaturation(a), effSaturation(b), t))
    };
  }
  function computeGrade(hour, phases) {
    if (!phases || phases.length === 0) return null;
    const h = (hour % 24 + 24) % 24;
    const sorted = [...phases].sort((p, q) => p.atHour - q.atHour);
    if (sorted.length === 1) return { background: lerpLayer(sorted[0].background, sorted[0].background, 0), sprites: lerpLayer(sorted[0].sprites, sorted[0].sprites, 0) };
    let lo = sorted[sorted.length - 1], hi = sorted[0];
    for (let i = 0; i < sorted.length; i++) {
      if (sorted[i].atHour <= h && (i === sorted.length - 1 || sorted[i + 1].atHour > h)) {
        lo = sorted[i];
        hi = sorted[(i + 1) % sorted.length];
        break;
      }
    }
    let span = hi.atHour - lo.atHour;
    if (span <= 0) span += 24;
    let pos = h - lo.atHour;
    if (pos < 0) pos += 24;
    const t = span > 0 ? Math.min(1, Math.max(0, pos / span)) : 0;
    return { background: lerpLayer(lo.background, hi.background, t), sprites: lerpLayer(lo.sprites, hi.sprites, t) };
  }
  function gradeToBackgroundStyle(g) {
    return {
      overlayColor: g.tintOpacity > 1e-3 ? hexToRgba\$1(g.tint, g.tintOpacity) : "transparent",
      filter: \`brightness(\${g.brightness}) saturate(\${g.saturation})\`
    };
  }
  function gradeToCharacterFilter(g) {
    if (!g.enabled) return "";
    const parts = [];
    if (g.brightness !== 1) parts.push(\`brightness(\${g.brightness})\`);
    if (g.saturation !== 1) parts.push(\`saturate(\${g.saturation})\`);
    return parts.join(" ");
  }
  function gradeToSpriteTint(g) {
    if (!g.enabled || g.tintOpacity <= 1e-3) return null;
    return { color: g.tint, opacity: Math.min(1, g.tintOpacity) };
  }
  function useTween(targetId, targetType) {
    const [values, setValues] = React2.useState(
      () => TweenManager.getCurrentValues(targetId, targetType)
    );
    const targetIdRef = React2.useRef(targetId);
    const targetTypeRef = React2.useRef(targetType);
    targetIdRef.current = targetId;
    targetTypeRef.current = targetType;
    React2.useEffect(() => {
      const unsubscribe = TweenManager.subscribe((tweens) => {
        let found = null;
        for (const tween of tweens.values()) {
          if (tween.targetId === targetIdRef.current && tween.targetType === targetTypeRef.current && !tween.finished) {
            found = tween.current;
            break;
          }
        }
        if (found === null) {
          found = TweenManager.getCurrentValues(targetIdRef.current, targetTypeRef.current);
        }
        setValues((prev) => {
          if (found === null && prev === null) return prev;
          return found;
        });
      });
      return unsubscribe;
    }, []);
    React2.useEffect(() => {
      setValues(TweenManager.getCurrentValues(targetId, targetType));
    }, [targetId, targetType]);
    return values;
  }
  function getHueFromHex(hex) {
    const match = hex.match(/^#?([a-f\\d]{2})([a-f\\d]{2})([a-f\\d]{2})\$/i);
    if (!match) return 0;
    const r = parseInt(match[1], 16) / 255;
    const g = parseInt(match[2], 16) / 255;
    const b = parseInt(match[3], 16) / 255;
    const max = Math.max(r, g, b), min = Math.min(r, g, b);
    let h = 0;
    if (max !== min) {
      const d = max - min;
      if (max === r) h = ((g - b) / d + (g < b ? 6 : 0)) / 6;
      else if (max === g) h = ((b - r) / d + 2) / 6;
      else h = ((r - g) / d + 4) / 6;
    }
    return Math.round(h * 360);
  }
  const FireworksBurst = ({ colors, intensity, bursts, duration, heightFrac, width, height, onExplode, onDone }) => {
    const canvasRef = React2.useRef(null);
    const doneRef = React2.useRef(onDone);
    doneRef.current = onDone;
    const explodeRef = React2.useRef(onExplode);
    explodeRef.current = onExplode;
    React2.useEffect(() => {
      const canvas = canvasRef.current;
      if (!canvas || width <= 0 || height <= 0) return;
      const speedMul = Math.max(0.25, 430 * bursts / (Math.max(0.5, duration) * 1e3));
      const stop = runFireworksSim(canvas, width, height, {
        colors,
        intensity,
        speedMul,
        continuous: false,
        maxBursts: bursts,
        heightFrac,
        onExplode: () => {
          var _a;
          return (_a = explodeRef.current) == null ? void 0 : _a.call(explodeRef);
        },
        onIdle: () => doneRef.current()
      });
      const safety = window.setTimeout(() => doneRef.current(), duration * 1e3 + 4500);
      return () => {
        stop();
        clearTimeout(safety);
      };
    }, [width, height]);
    return /* @__PURE__ */ jsxRuntime2.jsx("canvas", { ref: canvasRef, className: "absolute inset-0 w-full h-full pointer-events-none", style: { mixBlendMode: "screen" }, "aria-hidden": true });
  };
  const hexToRgbStr = (hex) => {
    const m = (hex || "").match(/^#?([a-f\\d]{2})([a-f\\d]{2})([a-f\\d]{2})\$/i);
    if (!m) return "255, 255, 255";
    return \`\${parseInt(m[1], 16)}, \${parseInt(m[2], 16)}, \${parseInt(m[3], 16)}\`;
  };
  const LightsLayer = ({ lights, stageW, stageH }) => {
    const base = Math.min(stageW || 800, stageH || 600);
    return /* @__PURE__ */ jsxRuntime2.jsx(jsxRuntime2.Fragment, { children: lights.map((l, i) => {
      const sizePx = Math.max(6, base * 0.05 * (l.size ?? 1));
      const bright = Math.max(0, Math.min(1, l.brightness ?? 1));
      const spd = l.twinkleSpeed && l.twinkleSpeed > 0 ? l.twinkleSpeed : 1;
      let background = "";
      let animation;
      let delay = \`\${i % 7 * 0.13}s\`;
      const wPx = sizePx * 1.8;
      const hPx = sizePx * 1.8;
      if (l.type === "candle") {
        background = \`radial-gradient(circle at 50% 45%, rgba(255,250,220,\${0.97 * bright}) 0%, rgba(255,185,75,\${0.8 * bright}) 9%, rgba(255,135,45,\${0.4 * bright}) 24%, rgba(255,105,25,\${0.14 * bright}) 46%, rgba(255,95,15,\${0.04 * bright}) 70%, rgba(255,95,15,0) 100%)\`;
        animation = \`vnfx-candle \${(1.1 / spd).toFixed(2)}s ease-in-out infinite\`;
      } else if (l.type === "star") {
        const rgb = hexToRgbStr(l.color || "#ffffff");
        background = \`radial-gradient(circle, rgba(255,255,255,\${0.98 * bright}) 0%, rgba(\${rgb},\${0.85 * bright}) 8%, rgba(\${rgb},\${0.4 * bright}) 22%, rgba(\${rgb},\${0.14 * bright}) 44%, rgba(\${rgb},\${0.04 * bright}) 68%, rgba(\${rgb},0) 100%)\`;
        animation = \`vnfx-star \${(2.2 / spd).toFixed(2)}s ease-in-out infinite\`;
      } else {
        const rgb = hexToRgbStr(l.color || "#ff3b3b");
        background = \`radial-gradient(circle, rgba(255,255,255,\${0.98 * bright}) 0%, rgba(\${rgb},\${0.95 * bright}) 5%, rgba(\${rgb},\${0.5 * bright}) 13%, rgba(\${rgb},\${0.26 * bright}) 26%, rgba(\${rgb},\${0.1 * bright}) 44%, rgba(\${rgb},\${0.03 * bright}) 66%, rgba(\${rgb},0) 100%)\`;
        const tw = l.twinkle ?? "fade";
        if (tw === "fade") animation = \`vnfx-bulb-fade \${(1.6 / spd).toFixed(2)}s ease-in-out infinite\`;
        else if (tw === "blink") animation = \`vnfx-bulb-blink \${(1 / spd).toFixed(2)}s steps(1, end) infinite\`;
        else if (tw === "chase") {
          animation = \`vnfx-bulb-fade \${(1.6 / spd).toFixed(2)}s ease-in-out infinite\`;
          delay = \`\${i % 5 * (0.32 / spd)}s\`;
        }
      }
      return /* @__PURE__ */ jsxRuntime2.jsx("div", { "data-vnlight": l.type, className: "absolute pointer-events-none", style: {
        left: \`\${l.x}%\`,
        top: \`\${l.y}%\`,
        width: wPx,
        height: hPx,
        transform: "translate(-50%, -50%)",
        borderRadius: "50%",
        background,
        animation,
        animationDelay: animation ? delay : void 0,
        mixBlendMode: "screen"
      } }, l.id);
    }) });
  };
  function isRuntimeDebugEnabled() {
    try {
      return window.localStorage.getItem("flourish:runtimeDebug") === "1";
    } catch {
      return false;
    }
  }
  function runtimeDebugLog(...args) {
    if (!isRuntimeDebugEnabled()) return;
    console.log(...args);
  }
  function runtimeDebugWarn(...args) {
    if (!isRuntimeDebugEnabled()) return;
    console.warn(...args);
  }
  function getPersistentVarsKey(projectId) {
    return \`vn-persistent-vars-\${projectId}\`;
  }
  function loadPersistentVariables(projectId) {
    var _a;
    try {
      if (typeof window !== "undefined" && ((_a = window.electronAPI) == null ? void 0 : _a.storage)) {
      }
      const raw = localStorage.getItem(getPersistentVarsKey(projectId));
      return raw ? JSON.parse(raw) : {};
    } catch {
      return {};
    }
  }
  function savePersistentVariables(projectId, vars) {
    var _a;
    try {
      if (typeof window !== "undefined" && ((_a = window.electronAPI) == null ? void 0 : _a.storage)) {
        window.electronAPI.storage.setItem(getPersistentVarsKey(projectId), vars);
      }
      localStorage.setItem(getPersistentVarsKey(projectId), JSON.stringify(vars));
    } catch (e) {
      console.error("Failed to save persistent variables:", e);
    }
  }
  function getInitialVariablesWithPersistent(projectVariables, projectId) {
    const vars = {};
    Object.values(projectVariables).forEach((v) => {
      vars[v.id] = v.defaultValue;
    });
    const persistentVars = loadPersistentVariables(projectId);
    Object.values(projectVariables).forEach((v) => {
      if ((v.scope || "global") === "persistent" && persistentVars[v.id] !== void 0) {
        vars[v.id] = persistentVars[v.id];
      }
    });
    return vars;
  }
  function getLocalVariableDefaults(projectVariables) {
    const defaults = {};
    Object.values(projectVariables).forEach((v) => {
      if ((v.scope || "global") === "local") {
        defaults[v.id] = v.defaultValue;
      }
    });
    return defaults;
  }
  const defaultSettings = {
    textSpeed: 50,
    musicVolume: 0.8,
    sfxVolume: 0.8,
    voiceVolume: 0.8,
    ambientVolume: 0.8,
    enableSkip: true,
    autoAdvance: false,
    autoAdvanceDelay: 3
  };
  const PARALLAX_MAX_PX = 40;
  const movieEntryAnim = (transition, durationSec) => {
    const d = durationSec ?? 0.5;
    switch (transition) {
      case void 0:
      case "":
      case "instant":
        return void 0;
      case "slide":
        return \`slide-in-right \${d}s ease-out forwards\`;
      case "iris-in":
        return \`iris-in \${d}s ease-out forwards\`;
      case "wipe-right":
        return \`wipe-right \${d}s ease-out forwards\`;
      default:
        return \`dissolve-in \${d}s ease-out forwards\`;
    }
  };
  const CAMERA_PAN_REF = 30;
  const parallaxTransform = (depth) => {
    if (!depth) return "";
    const d = depth * PARALLAX_MAX_PX;
    return \` translate(calc(var(--ppx, 0) * \${d}px), calc(var(--ppy, 0) * \${d}px))\`;
  };
  const buildSlideStyle = (x, _y, action, stageSize) => {
    const horizontalBias = x <= 50 ? -60 : 60;
    const startPercent = action === "show" ? horizontalBias : 0;
    const endPercent = action === "hide" ? horizontalBias : 0;
    const style = {
      "--slide-start-x": \`\${startPercent}%\`,
      "--slide-start-y": \`0%\`,
      "--slide-end-x": \`\${endPercent}%\`,
      "--slide-end-y": \`0%\`
    };
    if (stageSize.width > 0 && stageSize.height > 0) {
      style["--slide-start-px"] = \`\${startPercent / 100 * stageSize.width}px\`;
      style["--slide-end-px"] = \`\${endPercent / 100 * stageSize.width}px\`;
      style["--slide-start-py"] = \`0px\`;
      style["--slide-end-py"] = \`0px\`;
    }
    return style;
  };
  const TextOverlayElement = ({ overlay, stageSize }) => {
    var _a, _b, _c;
    const tweenValues = useTween(overlay.id, "text");
    const hasTransition = overlay.transition && overlay.transition !== "instant";
    const [playTransition, setPlayTransition] = React2.useState(overlay.action === "hide" && !!hasTransition);
    const timeoutRef = React2.useRef(null);
    React2.useEffect(() => {
      if (timeoutRef.current !== null) {
        clearTimeout(timeoutRef.current);
        timeoutRef.current = null;
      }
      if (!overlay.transition || overlay.transition === "instant") {
        setPlayTransition(false);
        return;
      }
      if (overlay.action === "show") {
        setPlayTransition(false);
        timeoutRef.current = window.setTimeout(() => {
          setPlayTransition(true);
          timeoutRef.current = null;
        }, 0);
        return () => {
          if (timeoutRef.current !== null) {
            clearTimeout(timeoutRef.current);
            timeoutRef.current = null;
          }
        };
      }
      setPlayTransition(true);
      return () => {
        if (timeoutRef.current !== null) {
          clearTimeout(timeoutRef.current);
          timeoutRef.current = null;
        }
      };
    }, [overlay.id, overlay.transition, overlay.action]);
    const applyTransition = playTransition && !!hasTransition;
    const transitionClass = applyTransition && overlay.transition ? getOverlayTransitionClass(overlay.transition, overlay.action === "hide") : "";
    const animDuration = \`\${overlay.duration ?? 0.5}s\`;
    const isSlideTransition = overlay.transition === "slide";
    const slideStyle = isSlideTransition ? buildSlideStyle(overlay.x, overlay.y ?? 0, overlay.action, stageSize) : {};
    const tx = (tweenValues == null ? void 0 : tweenValues.x) ?? overlay.x;
    const ty = (tweenValues == null ? void 0 : tweenValues.y) ?? overlay.y;
    const tFontSize = (tweenValues == null ? void 0 : tweenValues.fontSize) ?? overlay.fontSize;
    const tColor = (tweenValues == null ? void 0 : tweenValues.color) ?? overlay.color;
    const tWidth = (tweenValues == null ? void 0 : tweenValues.width) ?? overlay.width;
    const tHeight = (tweenValues == null ? void 0 : tweenValues.height) ?? overlay.height;
    const _orient = \`\${buildOrientationTransform({ rotation: overlay.rotation, flipX: overlay.flipX, flipY: overlay.flipY })}\${parallaxTransform(overlay.parallaxDepth)}\`.trim();
    const ovScale = (stageSize == null ? void 0 : stageSize.width) ? stageSize.width / 1280 : 1;
    const baseStyle = {
      left: \`\${tx}%\`,
      top: \`\${ty}%\`,
      ...isSlideTransition ? _orient ? { transform: _orient } : {} : { transform: \`translate(-50%, -50%) \${_orient}\`.trim() },
      fontSize: \`\${tFontSize * ovScale}px\`,
      fontFamily: overlay.fontFamily,
      color: tColor,
      fontWeight: overlay.fontWeight || "normal",
      fontStyle: overlay.fontStyle || "normal",
      letterSpacing: overlay.letterSpacing ? \`\${overlay.letterSpacing}px\` : void 0,
      width: tWidth ? \`\${tWidth / 1280 * 100}%\` : "auto",
      height: tHeight ? \`\${tHeight / 720 * 100}%\` : "auto",
      textAlign: overlay.textAlign || "left",
      display: "flex",
      alignItems: overlay.verticalAlign === "top" ? "flex-start" : overlay.verticalAlign === "bottom" ? "flex-end" : "center",
      justifyContent: overlay.textAlign === "left" ? "flex-start" : overlay.textAlign === "right" ? "flex-end" : "center",
      whiteSpace: overlay.width ? "pre-wrap" : "nowrap",
      overflow: "hidden",
      // Author stacking: text band (1) + layer. Default 0 → below characters (band 5), as today.
      zIndex: 1 + (overlay.layer ?? 0) * 100
    };
    const { containerStyle: effectsContainerStyle, gradientSpanStyle } = buildTextEffectStyles({
      textShadow: overlay.textShadow,
      textGradient: overlay.textGradient,
      textBorder: overlay.textBorder
    });
    Object.assign(baseStyle, effectsContainerStyle);
    if (overlay.action === "show" && hasTransition && !playTransition) {
      baseStyle.opacity = 0;
    }
    const className = \`absolute\${applyTransition ? \` \${transitionClass} transition-base\` : ""}\`;
    const style = {
      ...baseStyle,
      ...applyTransition ? { animationDuration: animDuration } : {},
      ...isSlideTransition ? slideStyle : {}
    };
    return /* @__PURE__ */ jsxRuntime2.jsx("div", { className, style, children: gradientSpanStyle ? /* @__PURE__ */ jsxRuntime2.jsx("span", { style: gradientSpanStyle, children: overlay.text }, \`grad-\${(_a = overlay.textGradient) == null ? void 0 : _a.type}-\${(_b = overlay.textGradient) == null ? void 0 : _b.angle}-\${(((_c = overlay.textGradient) == null ? void 0 : _c.colors) || []).join(",")}\`) : overlay.text });
  };
  const FF_VISUAL_TYPES = /* @__PURE__ */ new Set([
    CommandType.SetBackground,
    CommandType.ShowCharacter,
    CommandType.HideCharacter,
    CommandType.SetCharacterLayer,
    CommandType.ShowImage,
    CommandType.HideImage,
    CommandType.ShowText,
    CommandType.HideText,
    CommandType.PlaceLights,
    CommandType.ClearLights,
    CommandType.SetScreenOverlayEffect,
    CommandType.PlayMusic
  ]);
  const ButtonOverlayElement = ({ overlay, onAction, playSound, onAdvance, onCommitVariables, onPickup, onItemDrop, onItemDragMove, onItemDragEnd }) => {
    const tweenValues = useTween(overlay.id, "button");
    const [isHovered, setIsHovered] = React2.useState(false);
    const [dragging, setDragging] = React2.useState(false);
    const startDrag = (e) => {
      e.preventDefault();
      e.stopPropagation();
      const sx = e.clientX, sy = e.clientY;
      const rect = e.currentTarget.getBoundingClientRect();
      const w = rect.width, h = rect.height;
      let moved = false;
      const onMove = (ev) => {
        if (!moved && Math.abs(ev.clientX - sx) + Math.abs(ev.clientY - sy) > 4) {
          moved = true;
          setDragging(true);
        }
        if (moved) onItemDragMove == null ? void 0 : onItemDragMove(displayImage, ev.clientX, ev.clientY, w, h);
      };
      const onUp = (ev) => {
        window.removeEventListener("mousemove", onMove);
        window.removeEventListener("mouseup", onUp);
        setDragging(false);
        onItemDragEnd == null ? void 0 : onItemDragEnd();
        if (moved) onItemDrop == null ? void 0 : onItemDrop(overlay, ev.clientX, ev.clientY);
        else handleClick();
      };
      window.addEventListener("mousemove", onMove);
      window.addEventListener("mouseup", onUp);
    };
    const hasTransition = overlay.transition && overlay.transition !== "instant";
    const [playTransition, setPlayTransition] = React2.useState(overlay.action === "hide" && !!hasTransition);
    const timeoutRef = React2.useRef(null);
    React2.useEffect(() => {
      if (timeoutRef.current !== null) {
        clearTimeout(timeoutRef.current);
        timeoutRef.current = null;
      }
      if (!overlay.transition || overlay.transition === "instant") {
        setPlayTransition(false);
        return;
      }
      if (overlay.action === "show") {
        setPlayTransition(false);
        timeoutRef.current = window.setTimeout(() => {
          setPlayTransition(true);
          timeoutRef.current = null;
        }, 0);
        return () => {
          if (timeoutRef.current !== null) {
            clearTimeout(timeoutRef.current);
            timeoutRef.current = null;
          }
        };
      }
      setPlayTransition(true);
      return () => {
        if (timeoutRef.current !== null) {
          clearTimeout(timeoutRef.current);
          timeoutRef.current = null;
        }
      };
    }, [overlay.id, overlay.transition, overlay.action]);
    const handleClick = (e) => {
      var _a;
      runtimeDebugLog("Button clicked:", overlay.text, "Primary Action:", overlay.onClick, "Additional Actions:", ((_a = overlay.actions) == null ? void 0 : _a.length) || 0, "quickMenuMode:", overlay.quickMenuMode);
      if (e) {
        e.stopPropagation();
      }
      if (overlay.clickSound) {
        try {
          playSound(overlay.clickSound);
        } catch (e2) {
          console.error("Error playing button click sound:", e2);
        }
      }
      const allActions = [overlay.onClick, ...overlay.actions || []];
      const isVarMutation = (a) => a.type === UIActionType.SetVariable || a.type === UIActionType.ResetVariable || a.type === UIActionType.GiveItem || a.type === UIActionType.UseItem || a.type === UIActionType.DestroyItem || a.type === UIActionType.UseSelectedItem || a.type === UIActionType.RestockCollection || a.type === UIActionType.BuyItem || a.type === UIActionType.SellItem || a.type === UIActionType.BuySelectedItem || a.type === UIActionType.SellSelectedItem;
      const setVarActions = allActions.filter(isVarMutation);
      const otherActions = allActions.filter((a) => !isVarMutation(a));
      setVarActions.forEach((action) => onAction(action));
      if (setVarActions.length > 0 && onCommitVariables) {
        runtimeDebugLog("[Button] Committing", setVarActions.length, "variable changes before navigation");
        onCommitVariables();
      }
      otherActions.forEach((action) => onAction(action));
      const calledCommonEvent = allActions.some((a) => a.type === UIActionType.CallCommonEvent);
      if (overlay.waitForClick && onAdvance && !overlay.quickMenuMode && overlay.onClick.type !== UIActionType.JumpToScene && !calledCommonEvent) {
        onAdvance();
      }
      if ((overlay.giveItemId || overlay.removeAfterClick || overlay.pickUpOnceId) && onPickup) {
        onPickup(overlay);
      }
    };
    const applyTransition = playTransition && overlay.transition && overlay.transition !== "instant";
    const transitionClass = applyTransition && overlay.transition ? getOverlayTransitionClass(overlay.transition, overlay.action === "hide") : "";
    const animDuration = \`\${overlay.duration ?? 0.3}s\`;
    const bx = (tweenValues == null ? void 0 : tweenValues.x) ?? overlay.x;
    const by = (tweenValues == null ? void 0 : tweenValues.y) ?? overlay.y;
    const bw = (tweenValues == null ? void 0 : tweenValues.width) ?? overlay.width;
    const bh = (tweenValues == null ? void 0 : tweenValues.height) ?? overlay.height;
    const bOpacity = (tweenValues == null ? void 0 : tweenValues.opacity) ?? overlay.opacity;
    const bFontSize = (tweenValues == null ? void 0 : tweenValues.fontSize) ?? overlay.fontSize;
    const bBorderRadius = (tweenValues == null ? void 0 : tweenValues.borderRadius) ?? overlay.borderRadius;
    const bBgColor = (tweenValues == null ? void 0 : tweenValues.backgroundColor) ?? overlay.backgroundColor;
    const displayImage = isHovered && overlay.hoverImageUrl ? overlay.hoverImageUrl : overlay.imageUrl;
    const btnAlign = overlay.textAlign || "center";
    const btnJustify = { left: "flex-start", center: "center", right: "flex-end" }[btnAlign];
    const btnPadX = \`\${overlay.paddingX ?? 0}%\`;
    const containerStyle = {
      position: "absolute",
      left: \`\${bx}%\`,
      top: \`\${by}%\`,
      width: \`\${bw}%\`,
      // When an image is the button, let the height follow the image's aspect ratio so the
      // box conforms to the art (no cropping, no distortion). Otherwise use the set height.
      height: displayImage ? "auto" : \`\${bh}%\`,
      transform: \`translate(-\${overlay.anchorX * 100}%, -\${overlay.anchorY * 100}%) \${buildOrientationTransform({ rotation: overlay.rotation, flipX: overlay.flipX, flipY: overlay.flipY })}\${parallaxTransform(overlay.parallaxDepth)}\`.trim(),
      pointerEvents: "auto",
      // Author stacking: button band (1) + layer. Default 0 → below characters (band 5), as today.
      zIndex: 1 + (overlay.layer ?? 0) * 100,
      // While dragging, dim the original in place; the parent draws the moving ghost at the cursor.
      ...dragging ? { opacity: 0.3 } : {}
    };
    if (overlay.action === "show" && hasTransition && !playTransition) {
      containerStyle.opacity = 0;
    }
    const buttonStyle = displayImage ? {
      // Image button: box conforms to the image; text (if any) layers on top.
      position: "relative",
      width: "100%",
      height: "auto",
      display: "block",
      padding: 0,
      border: "none",
      background: "transparent",
      cursor: "pointer",
      lineHeight: 0,
      color: overlay.textColor,
      fontSize: \`calc(var(--ovl-scale, 1) * \${bFontSize}px)\`,
      fontWeight: overlay.fontWeight,
      transition: "transform 0.1s",
      transform: isHovered ? "translateY(-2px)" : "none",
      opacity: bOpacity ?? 1
    } : {
      position: "relative",
      width: "100%",
      height: "100%",
      backgroundColor: bBgColor,
      color: overlay.textColor,
      fontSize: \`calc(var(--ovl-scale, 1) * \${bFontSize}px)\`,
      fontWeight: overlay.fontWeight,
      borderRadius: \`calc(var(--ovl-scale, 1) * \${bBorderRadius}px)\`,
      border: "none",
      cursor: "pointer",
      padding: 0,
      paddingLeft: btnPadX,
      paddingRight: btnPadX,
      boxSizing: "border-box",
      textAlign: btnAlign,
      display: "flex",
      alignItems: "center",
      justifyContent: btnJustify,
      transition: "transform 0.1s, box-shadow 0.1s",
      boxShadow: isHovered ? "0 4px 12px rgba(0,0,0,0.3)" : "0 2px 4px rgba(0,0,0,0.2)",
      transform: isHovered ? "translateY(-2px)" : "none",
      opacity: bOpacity ?? 1
    };
    const cb = overlay.contentBox;
    const hasCb = !!cb && ((cb.left || 0) > 5e-3 || (cb.top || 0) > 5e-3 || (cb.right || 0) > 5e-3 || (cb.bottom || 0) > 5e-3);
    const interactiveProps = {
      onClick: overlay.draggable ? void 0 : handleClick,
      onMouseDown: overlay.draggable ? startDrag : void 0,
      onMouseEnter: () => setIsHovered(true),
      onMouseLeave: () => setIsHovered(false)
    };
    return /* @__PURE__ */ jsxRuntime2.jsxs(
      "div",
      {
        style: { ...containerStyle, ...hasTransition ? { animationDuration: animDuration } : {} },
        className: \`\${transitionClass}\`,
        ...overlay.quickMenuMode ? { "data-vn-no-advance": "true" } : {},
        children: [
          /* @__PURE__ */ jsxRuntime2.jsxs(
            "button",
            {
              ...hasCb ? {} : interactiveProps,
              style: { ...buttonStyle, pointerEvents: hasCb ? "none" : buttonStyle.pointerEvents, cursor: overlay.draggable ? dragging ? "grabbing" : "grab" : buttonStyle.cursor },
              children: [
                displayImage && /* @__PURE__ */ jsxRuntime2.jsx(
                  "img",
                  {
                    src: displayImage,
                    alt: overlay.text,
                    draggable: false,
                    style: { display: "block", width: "100%", height: "auto", objectFit: "contain", borderRadius: \`\${overlay.borderRadius}px\` }
                  }
                ),
                overlay.text && /* @__PURE__ */ jsxRuntime2.jsx("span", { style: displayImage ? { position: "absolute", inset: 0, display: "flex", alignItems: "center", justifyContent: btnJustify, lineHeight: "normal", zIndex: 1, paddingLeft: btnPadX, paddingRight: btnPadX, boxSizing: "border-box", textAlign: btnAlign } : { position: "relative", zIndex: 1 }, children: overlay.text })
              ]
            }
          ),
          hasCb && cb && /* @__PURE__ */ jsxRuntime2.jsx(
            "div",
            {
              ...interactiveProps,
              style: {
                position: "absolute",
                left: \`\${cb.left * 100}%\`,
                top: \`\${cb.top * 100}%\`,
                right: \`\${cb.right * 100}%\`,
                bottom: \`\${cb.bottom * 100}%\`,
                pointerEvents: "auto",
                zIndex: 2,
                cursor: overlay.draggable ? dragging ? "grabbing" : "grab" : "pointer"
              }
            }
          )
        ]
      },
      overlay.id
    );
  };
  const QuickMenuButtonEl = ({ label, title, icon, onClick, disabled, config, assetResolver, artImgStyle, artButtonStyle, pillClassName, pillStyle }) => {
    const [isHovered, setIsHovered] = React2.useState(false);
    const img = (config == null ? void 0 : config.image) ? assetResolver(config.image.id, config.image.type) : null;
    const hoverImg = (config == null ? void 0 : config.hoverImage) ? assetResolver(config.hoverImage.id, config.hoverImage.type) : null;
    const displayImg = isHovered && hoverImg ? hoverImg : img;
    if (displayImg) {
      return /* @__PURE__ */ jsxRuntime2.jsx(
        "button",
        {
          onClick,
          disabled,
          title,
          onMouseEnter: () => setIsHovered(true),
          onMouseLeave: () => setIsHovered(false),
          style: {
            padding: 0,
            margin: 0,
            border: "none",
            background: "transparent",
            lineHeight: 0,
            cursor: disabled ? "default" : "pointer",
            opacity: disabled ? 0.4 : 1,
            display: "block",
            ...artButtonStyle
          },
          children: /* @__PURE__ */ jsxRuntime2.jsx("img", { src: displayImg, alt: label, draggable: false, style: artImgStyle })
        }
      );
    }
    return /* @__PURE__ */ jsxRuntime2.jsxs(
      "button",
      {
        onClick,
        disabled,
        title,
        onMouseEnter: () => setIsHovered(true),
        onMouseLeave: () => setIsHovered(false),
        className: pillClassName,
        style: pillStyle,
        children: [
          icon,
          label
        ]
      }
    );
  };
  const ImageOverlayElement = ({ overlay, stageSize }) => {
    const tweenValues = useTween(overlay.id, "image");
    const hasTransition = overlay.transition && overlay.transition !== "instant";
    const [playTransition, setPlayTransition] = React2.useState(overlay.action === "hide" && !!hasTransition);
    const timeoutRef = React2.useRef(null);
    React2.useEffect(() => {
      if (timeoutRef.current !== null) {
        clearTimeout(timeoutRef.current);
        timeoutRef.current = null;
      }
      if (!overlay.transition || overlay.transition === "instant") {
        setPlayTransition(false);
        return;
      }
      if (overlay.action === "show") {
        setPlayTransition(false);
        timeoutRef.current = window.setTimeout(() => {
          setPlayTransition(true);
          timeoutRef.current = null;
        }, 0);
        return () => {
          if (timeoutRef.current !== null) {
            clearTimeout(timeoutRef.current);
            timeoutRef.current = null;
          }
        };
      }
      setPlayTransition(true);
      return () => {
        if (timeoutRef.current !== null) {
          clearTimeout(timeoutRef.current);
          timeoutRef.current = null;
        }
      };
    }, [overlay.id, overlay.transition, overlay.action]);
    const applyTransition = playTransition && overlay.transition && overlay.transition !== "instant";
    const transitionClass = applyTransition && overlay.transition ? getOverlayTransitionClass(overlay.transition, overlay.action === "hide") : "";
    const animDuration = \`\${overlay.duration ?? 0.5}s\`;
    const isSlideTransition = overlay.transition === "slide";
    const slideStyle = isSlideTransition ? buildSlideStyle(overlay.x, overlay.y ?? 0, overlay.action, stageSize) : {};
    const ix = (tweenValues == null ? void 0 : tweenValues.x) ?? overlay.x;
    const iy = (tweenValues == null ? void 0 : tweenValues.y) ?? overlay.y;
    const iw = (tweenValues == null ? void 0 : tweenValues.width) ?? overlay.width;
    const ih = (tweenValues == null ? void 0 : tweenValues.height) ?? overlay.height;
    const iOpacity = (tweenValues == null ? void 0 : tweenValues.opacity) ?? overlay.opacity;
    const iRotation = (tweenValues == null ? void 0 : tweenValues.rotation) ?? overlay.rotation;
    const iScaleX = (tweenValues == null ? void 0 : tweenValues.scaleX) ?? overlay.scaleX;
    const iScaleY = (tweenValues == null ? void 0 : tweenValues.scaleY) ?? overlay.scaleY;
    const fit = !!overlay.fitToContent;
    const refPctW = (px) => \`\${px / 1280 * 100}%\`;
    const refPctH = (px) => \`\${px / 720 * 100}%\`;
    const containerStyle = {
      left: \`\${ix}%\`,
      top: \`\${iy}%\`,
      ...fit ? { width: "auto", height: "auto", maxWidth: refPctW(iw), maxHeight: refPctH(ih) } : { width: refPctW(iw), height: refPctH(ih) },
      transform: \`\${isSlideTransition ? "" : "translate(-50%, -50%)"}\${parallaxTransform(overlay.parallaxDepth)}\`.trim() || void 0,
      // Author stacking: image band (1) + layer. Default 0 → below characters (band 5), as today.
      zIndex: 1 + (overlay.layer ?? 0) * 100
    };
    if (overlay.action === "show" && hasTransition && !playTransition) {
      containerStyle.opacity = 0;
    }
    const iFlipX = overlay.flipX ? -1 : 1;
    const iFlipY = overlay.flipY ? -1 : 1;
    const imageStyle = {
      ...fit ? { display: "block", width: "auto", height: "auto", maxWidth: "100%", maxHeight: "100%" } : { width: "100%", height: "100%" },
      transform: \`rotate(\${iRotation}deg) scale(\${iScaleX * iFlipX}, \${iScaleY * iFlipY})\`,
      transformOrigin: "center center",
      opacity: iOpacity
    };
    const mediaCls = \`\${fit ? "" : "absolute inset-0 w-full h-full "}object-contain pointer-events-none\`;
    const className = \`absolute\${applyTransition ? \` \${transitionClass} transition-base\` : ""}\`;
    const style = {
      ...containerStyle,
      ...applyTransition ? { animationDuration: animDuration } : {},
      ...isSlideTransition ? slideStyle : {}
    };
    return /* @__PURE__ */ jsxRuntime2.jsx("div", { className, style, children: overlay.isVideo && overlay.videoUrl ? /* @__PURE__ */ jsxRuntime2.jsx(
      TrimmedVideo,
      {
        src: overlay.videoUrl,
        autoPlay: true,
        muted: true,
        loop: overlay.videoLoop,
        trimStart: overlay.videoTrimStart,
        trimEnd: overlay.videoTrimEnd,
        playsInline: true,
        className: mediaCls,
        style: imageStyle
      }
    ) : /* @__PURE__ */ jsxRuntime2.jsx(
      "img",
      {
        src: overlay.imageUrl,
        alt: "",
        className: mediaCls,
        style: imageStyle
      }
    ) });
  };
  const HotSpotOverlayElement = ({ overlay, onAction, onAdvance, evaluateConditions: evaluateConditions2, variables }) => {
    const active = !overlay.conditions || overlay.conditions.length === 0 || evaluateConditions2(overlay.conditions, variables);
    React2.useEffect(() => {
      if (!active || overlay.trigger !== "drag-drop") return;
      return registerDropTarget({
        id: \`scene-\${overlay.commandId}\`,
        rectPct: { x: overlay.x, y: overlay.y, width: overlay.width, height: overlay.height },
        acceptTag: overlay.acceptedTag || void 0,
        onDrop: () => {
          (overlay.actions || []).forEach((a) => onAction(a));
        }
      });
    }, [active, overlay, onAction]);
    if (!active) return null;
    const fire = (e) => {
      e.stopPropagation();
      (overlay.actions || []).forEach((a) => onAction(a));
      if (overlay.advanceOnTrigger && onAdvance) onAdvance();
    };
    const style = {
      position: "absolute",
      left: \`\${overlay.x}%\`,
      top: \`\${overlay.y}%\`,
      width: \`\${overlay.width}%\`,
      height: \`\${overlay.height}%\`,
      borderRadius: overlay.shape === "circle" ? "50%" : 6,
      // Honor a per-spot layer so items/images can sit above a hot spot (1 + layer*100, the shared
      // overlay band). Without a layer set, keep the legacy fixed z (above characters z-5, below dialogue z-20).
      zIndex: overlay.layer != null ? 1 + overlay.layer * 100 : 8,
      // drag-drop spots are pure drop zones (coordinate hit-test) — don't capture clicks,
      // so empty/drag clicks still reach the stage. click/hover spots capture.
      pointerEvents: overlay.trigger === "drag-drop" ? "none" : "auto",
      cursor: (overlay.trigger || "click") === "click" ? "pointer" : "default",
      // Honor "Draw the spot during play" (overlay.visible) ONLY — an invisible spot is fully
      // invisible even in test-play. (Authors still see/position it on the scene editor canvas,
      // which always draws hot spots with a label.)
      background: overlay.visible ? overlay.highlightColor || "rgba(99,102,241,0.35)" : "transparent",
      border: overlay.visible ? \`1px solid \${overlay.highlightColor || "rgba(99,102,241,0.6)"}\` : void 0
    };
    return /* @__PURE__ */ jsxRuntime2.jsx(
      "div",
      {
        style,
        onClick: (overlay.trigger || "click") === "click" ? fire : void 0,
        onMouseEnter: overlay.trigger === "hover" ? () => (overlay.actions || []).forEach((a) => onAction(a)) : void 0
      }
    );
  };
  const useTypewriter = (text, speed, msPerCharOverride) => {
    const [displayText, setDisplayText] = React2.useState("");
    const hasFinished = displayText.length === text.length;
    React2.useEffect(() => {
      setDisplayText("");
      if (!text) return;
      const interval = setInterval(() => {
        setDisplayText((prev) => {
          if (prev.length < text.length) {
            return text.substring(0, prev.length + 1);
          } else {
            clearInterval(interval);
            return prev;
          }
        });
      }, msPerCharOverride ?? 1e3 / speed);
      return () => clearInterval(interval);
    }, [text, speed, msPerCharOverride]);
    const skip = () => setDisplayText(text);
    return { displayText, skip, hasFinished };
  };
  const useStageSize = (ref) => {
    const [size, setSize] = React2.useState({ width: 0, height: 0 });
    const [element, setElement] = React2.useState(null);
    React2.useEffect(() => {
      if (ref.current) {
        setElement(ref.current);
        return;
      }
      const interval = setInterval(() => {
        if (ref.current) {
          setElement(ref.current);
          clearInterval(interval);
        }
      }, 100);
      return () => clearInterval(interval);
    }, [ref]);
    React2.useEffect(() => {
      if (!element) return;
      let rafId = null;
      const obs = new ResizeObserver(() => {
        if (rafId !== null) {
          cancelAnimationFrame(rafId);
        }
        rafId = requestAnimationFrame(() => {
          const r2 = element.getBoundingClientRect();
          setSize((prev) => {
            if (prev.width === r2.width && prev.height === r2.height) {
              return prev;
            }
            return { width: r2.width, height: r2.height };
          });
        });
      });
      obs.observe(element);
      const r = element.getBoundingClientRect();
      setSize({ width: r.width, height: r.height });
      return () => {
        obs.disconnect();
        if (rafId !== null) {
          cancelAnimationFrame(rafId);
        }
      };
    }, [element]);
    React2.useEffect(() => {
      if (ref.current && ref.current !== element) {
        setElement(ref.current);
      }
    });
    return size;
  };
  function hexToRgba(hex, opacity) {
    const r = parseInt(hex.slice(1, 3), 16);
    const g = parseInt(hex.slice(3, 5), 16);
    const b = parseInt(hex.slice(5, 7), 16);
    return \`rgba(\${r}, \${g}, \${b}, \${opacity / 100})\`;
  }
  function buildImageBackgroundStyle(url, sizeMode, slicePx) {
    switch (sizeMode) {
      case "nine-slice": {
        const s = slicePx ?? 30;
        return {
          borderImageSource: \`url(\${url})\`,
          borderImageSlice: \`\${s} fill\`,
          borderImageWidth: \`calc(var(--font-scale,1) * \${s}px)\`,
          borderImageRepeat: "stretch",
          borderStyle: "solid",
          borderColor: "transparent",
          borderWidth: \`calc(var(--font-scale,1) * \${s}px)\`
        };
      }
      case "contain":
        return { backgroundImage: \`url(\${url})\`, backgroundSize: "contain", backgroundRepeat: "no-repeat", backgroundPosition: "center" };
      case "cover":
        return { backgroundImage: \`url(\${url})\`, backgroundSize: "cover", backgroundRepeat: "no-repeat", backgroundPosition: "center" };
      case "tile":
        return { backgroundImage: \`url(\${url})\`, backgroundSize: "auto", backgroundRepeat: "repeat" };
      case "stretch":
      default:
        return { backgroundImage: \`url(\${url})\`, backgroundSize: "100% 100%", backgroundRepeat: "no-repeat", backgroundPosition: "center" };
    }
  }
  const scalePx = (n) => \`calc(var(--font-scale,1) * \${n}px)\`;
  const CrossfadeImage = ({ src, alt, style, transitionMs }) => {
    const keyRef = React2.useRef(0);
    const prev = React2.useRef(src);
    const [layers, setLayers] = React2.useState([{ src, k: 0 }]);
    React2.useEffect(() => {
      if (src === prev.current) return;
      prev.current = src;
      keyRef.current += 1;
      const k = keyRef.current;
      if (!transitionMs) {
        setLayers([{ src, k }]);
        return;
      }
      setLayers((ls) => [...ls, { src, k }]);
      const t = window.setTimeout(() => setLayers([{ src, k }]), transitionMs + 60);
      return () => window.clearTimeout(t);
    }, [src, transitionMs]);
    return /* @__PURE__ */ jsxRuntime2.jsx(jsxRuntime2.Fragment, { children: layers.map((layer, i) => /* @__PURE__ */ jsxRuntime2.jsx(
      "img",
      {
        src: layer.src,
        alt,
        style: {
          ...style,
          ...i === 0 ? {} : { position: "absolute", inset: 0 },
          ...i > 0 && transitionMs ? { animation: \`vnImgCrossfade \${transitionMs}ms ease forwards\` } : {}
        }
      },
      layer.k
    )) });
  };
  const resolveEffectiveTextbox = (project, character, lineThemeId) => {
    const themes = project.textboxThemes;
    if (lineThemeId && (themes == null ? void 0 : themes[lineThemeId])) return themes[lineThemeId];
    const base = (character == null ? void 0 : character.textboxThemeId) ? themes == null ? void 0 : themes[character.textboxThemeId] : void 0;
    const inline = character == null ? void 0 : character.textbox;
    if (base || inline) return { ...base || {}, ...inline || {} };
    return void 0;
  };
  const pickActiveAppearanceState = (element, variables, evalConditions) => {
    const states = element.appearanceStates;
    if (!states || states.length === 0) return null;
    for (const st of states) {
      if (st.conditions && st.conditions.length > 0 && evalConditions(st.conditions, variables)) return st;
    }
    return null;
  };
  const mergeAppearanceStatePrimary = (element, state) => {
    if (!state.primaryColor && !state.image) return element;
    const el = { ...element };
    if (state.primaryColor) {
      if (el.type === UIElementType.Meter) {
        el.fillColor = state.primaryColor;
        el.fillColorEnd = void 0;
      } else if (el.type === UIElementType.Text) {
        el.font = { ...el.font || {}, color: state.primaryColor };
      } else if (el.type === UIElementType.Button) {
        el.backgroundColor = state.primaryColor;
      }
    }
    if (state.image) {
      if (el.type === UIElementType.Image) {
        el.background = { type: "image", assetId: state.image.id };
        el.image = state.image;
      } else if (el.type === UIElementType.Button) {
        el.image = state.image;
      }
    }
    return el;
  };
  const flashlightBg = (mx, my, radiusPx, softness, darkRgba) => {
    const inner = Math.round(Math.max(0, Math.min(1, 1 - softness)) * 100);
    return \`radial-gradient(circle \${Math.max(20, radiusPx)}px at \${mx}px \${my}px, transparent 0%, transparent \${inner}%, \${darkRgba} 100%)\`;
  };
  const pickReactiveTextboxState = (states, variables, evalConditions) => {
    if (!states || states.length === 0) return null;
    for (const st of states) {
      if (st.conditions && st.conditions.length > 0 && evalConditions(st.conditions, variables)) return st;
    }
    return null;
  };
  const DialogueBox = ({ dialogue, settings, projectUI, onFinished, variables, project, reactiveState, timerPaused, uiPalette, voiceRef }) => {
    var _a, _b, _c, _d, _e, _f, _g, _h, _i, _j, _k, _l;
    if (!dialogue) return null;
    const interpolatedText = interpolateVariables(dialogue.text, variables, project);
    const effectiveTextSpeed = dialogue.textSpeed != null && dialogue.textSpeed > 0 ? dialogue.textSpeed : settings.textSpeed;
    const wantVoicePacing = !!projectUI.voicePacedText && !!dialogue.voiceAudioId && !(dialogue.textSpeed != null && dialogue.textSpeed > 0);
    const [voiceDurationMs, setVoiceDurationMs] = React2.useState(null);
    React2.useEffect(() => {
      setVoiceDurationMs(null);
      if (!wantVoicePacing || !voiceRef) return;
      let tries = 0;
      const id = window.setInterval(() => {
        var _a2;
        const d = (_a2 = voiceRef.current) == null ? void 0 : _a2.duration;
        if (d && isFinite(d) && d > 0) {
          setVoiceDurationMs(d * 1e3);
          clearInterval(id);
        } else if (++tries > 25) clearInterval(id);
      }, 100);
      return () => clearInterval(id);
    }, [dialogue.text, dialogue.voiceAudioId, wantVoicePacing]);
    const pacedMsPerChar = wantVoicePacing && voiceDurationMs && interpolatedText.length > 0 ? Math.max(8, Math.min(200, voiceDurationMs * 0.92 / interpolatedText.length)) : null;
    const { displayText, skip, hasFinished } = useTypewriter(interpolatedText, effectiveTextSpeed, pacedMsPerChar);
    const timeLimit = dialogue.timeLimit && dialogue.timeLimit > 0 ? dialogue.timeLimit : 0;
    const timerLocked = !!(timeLimit && dialogue.timeLimitLocked);
    const [timerRemaining, setTimerRemaining] = React2.useState(0);
    React2.useEffect(() => {
      if (!timeLimit || !hasFinished || timerPaused) return;
      let raf = 0;
      let fired = false;
      const start = performance.now();
      const tick = () => {
        const left = timeLimit - (performance.now() - start) / 1e3;
        setTimerRemaining(Math.max(0, left));
        if (left <= 0) {
          if (!fired) {
            fired = true;
            onFinished();
          }
          return;
        }
        raf = requestAnimationFrame(tick);
      };
      raf = requestAnimationFrame(tick);
      return () => cancelAnimationFrame(raf);
    }, [timeLimit, hasFinished, timerPaused, dialogue.text]);
    const character = dialogue.characterId ? project.characters[dialogue.characterId] : null;
    let charTb = resolveEffectiveTextbox(project, character, dialogue.textboxThemeId);
    if (reactiveState) {
      const merged = { ...charTb || {} };
      for (const [k, v] of Object.entries(reactiveState)) {
        if (v !== void 0 && k !== "id" && k !== "name" && k !== "conditions" && k !== "hideNamebox" && k !== "transitionMs") merged[k] = v;
      }
      charTb = merged;
    }
    const reactiveMs = (projectUI.dialogueReactiveStates || []).reduce(
      (m, s) => s.transitionMs ? Math.max(m, s.transitionMs) : m,
      0
    );
    const reactiveTransition = reactiveMs > 0 ? \`background-color \${reactiveMs}ms ease, background \${reactiveMs}ms ease, opacity \${reactiveMs}ms ease, border-radius \${reactiveMs}ms ease\` : void 0;
    const handleClick = () => {
      if (timerLocked) {
        if (!hasFinished && settings.enableSkip) skip();
        return;
      }
      if (hasFinished) {
        onFinished();
      } else if (settings.enableSkip) {
        skip();
      }
    };
    const boxImage = (charTb == null ? void 0 : charTb.dialogueBoxImage) ?? projectUI.dialogueBoxImage;
    const dialogueBoxUrl = boxImage ? resolveFieldUrl(
      project.id,
      boxImage.type === "video" ? ((_a = project.videos[boxImage.id]) == null ? void 0 : _a.videoUrl) || ((_b = project.images[boxImage.id]) == null ? void 0 : _b.videoUrl) || ((_c = project.backgrounds[boxImage.id]) == null ? void 0 : _c.videoUrl) : ((_d = project.images[boxImage.id]) == null ? void 0 : _d.imageUrl) || ((_e = project.backgrounds[boxImage.id]) == null ? void 0 : _e.imageUrl)
    ) : null;
    const isDialogueBoxVideo = (boxImage == null ? void 0 : boxImage.type) === "video";
    const dialogueBoxTrim = resolveVideoTrim(boxImage, (boxImage == null ? void 0 : boxImage.id) ? project.videos[boxImage.id] || project.images[boxImage.id] || project.backgrounds[boxImage.id] : void 0);
    const borderImage = (charTb == null ? void 0 : charTb.dialogueBoxBorderImage) ?? projectUI.dialogueBoxBorderImage;
    const dialogueBorderUrl = borderImage ? resolveFieldUrl(project.id, ((_f = project.images[borderImage.id]) == null ? void 0 : _f.imageUrl) || ((_g = project.backgrounds[borderImage.id]) == null ? void 0 : _g.imageUrl)) : null;
    const dialogueBorderPadding = (charTb == null ? void 0 : charTb.dialogueBorderPadding) ?? projectUI.dialogueBorderPadding ?? 12;
    const dialogueBoxWidth = projectUI.dialogueBoxWidth ?? 100;
    const dialogueBoxHeight = projectUI.dialogueBoxHeight || 0;
    const dialogueBoxBottomMargin = projectUI.dialogueBoxBottomMargin ?? 20;
    const dialogueBoxPadding = projectUI.dialogueBoxPadding ?? 20;
    const dialogueSizeMode = (charTb == null ? void 0 : charTb.dialogueBoxSizeMode) ?? projectUI.dialogueBoxSizeMode ?? "stretch";
    const dialogueSlice = (charTb == null ? void 0 : charTb.dialogueBoxSlice) ?? projectUI.dialogueBoxSlice ?? 30;
    const dialogueColor = (uiPalette == null ? void 0 : uiPalette.dialogueBg) ?? (charTb == null ? void 0 : charTb.dialogueBoxColor) ?? projectUI.dialogueBoxColor ?? "#0f172a";
    const dialogueOpacity = (charTb == null ? void 0 : charTb.dialogueBoxOpacity) ?? projectUI.dialogueBoxOpacity ?? 90;
    const dialogueBorderRadius = (charTb == null ? void 0 : charTb.dialogueBoxBorderRadius) ?? projectUI.dialogueBoxBorderRadius ?? 8;
    const nameboxImage = (charTb == null ? void 0 : charTb.nameboxImage) ?? projectUI.nameboxImage;
    const nameboxImageUrl = nameboxImage ? resolveFieldUrl(project.id, ((_h = project.images[nameboxImage.id]) == null ? void 0 : _h.imageUrl) || ((_i = project.backgrounds[nameboxImage.id]) == null ? void 0 : _i.imageUrl)) : null;
    const nameboxColor = (uiPalette == null ? void 0 : uiPalette.dialogueName) ?? (charTb == null ? void 0 : charTb.nameboxColor) ?? projectUI.nameboxColor ?? "#0f172a";
    const nameboxOpacity = (charTb == null ? void 0 : charTb.nameboxOpacity) ?? projectUI.nameboxOpacity ?? 92;
    const nameboxPadding = (charTb == null ? void 0 : charTb.nameboxPadding) ?? projectUI.nameboxPadding ?? 8;
    const nameboxHPadding = (charTb == null ? void 0 : charTb.nameboxHorizontalPadding) ?? projectUI.nameboxHorizontalPadding ?? 14;
    const nameboxBorderRadius = (charTb == null ? void 0 : charTb.nameboxBorderRadius) ?? projectUI.nameboxBorderRadius ?? 6;
    const nameboxOffsetX = projectUI.nameboxOffsetX ?? 20;
    const nameboxOffsetY = projectUI.nameboxOffsetY ?? 0;
    const nameboxSizeMode = (charTb == null ? void 0 : charTb.nameboxSizeMode) ?? projectUI.nameboxSizeMode ?? "stretch";
    const characterFont = character == null ? void 0 : character.fontFamily;
    const characterFontSize = character == null ? void 0 : character.fontSize;
    const characterFontWeight = character == null ? void 0 : character.fontWeight;
    const characterFontItalic = character == null ? void 0 : character.fontItalic;
    const dialogueTextStyle = {
      ...fontSettingsToStyle((charTb == null ? void 0 : charTb.dialogueTextFont) ?? projectUI.dialogueTextFont),
      ...characterFont ? { fontFamily: characterFont } : {},
      ...characterFontSize ? { fontSize: \`calc(var(--font-scale, 1) * \${characterFontSize}px)\` } : {},
      ...characterFontWeight ? { fontWeight: characterFontWeight } : {},
      ...characterFontItalic ? { fontStyle: "italic" } : {},
      // Speaker-tinted lines: this character's dialogue renders in their name color (or a
      // custom one). Wins over theme/global text color; a palette→UI restyle still wins.
      ...(character == null ? void 0 : character.dialogueTextColorMode) === "character" && dialogue.characterColor ? { color: dialogue.characterColor } : (character == null ? void 0 : character.dialogueTextColorMode) === "custom" && character.dialogueTextColor ? { color: character.dialogueTextColor } : {},
      ...(uiPalette == null ? void 0 : uiPalette.dialogueText) ? { color: uiPalette.dialogueText } : {}
    };
    const rhCfg = projectUI.dialogueRevealHighlight;
    const wordRanges = React2.useMemo(() => {
      const out = [];
      const re = /\\S+/g;
      let m;
      while (m = re.exec(interpolatedText)) out.push({ start: m.index, end: m.index + m[0].length });
      return out;
    }, [interpolatedText]);
    const wantVoiceSync = !!((rhCfg == null ? void 0 : rhCfg.enabled) && rhCfg.syncToVoice && dialogue.voiceAudioId && voiceRef);
    const [voiceWordStarts, setVoiceWordStarts] = React2.useState(null);
    const [syncWordIdx, setSyncWordIdx] = React2.useState(-1);
    React2.useEffect(() => {
      var _a2;
      setVoiceWordStarts(null);
      setSyncWordIdx(-1);
      if (!wantVoiceSync || !wordRanges.length) return;
      let alive = true;
      const asset = (_a2 = project.audio) == null ? void 0 : _a2[dialogue.voiceAudioId];
      const url = asset ? resolveFieldUrl(project.id, asset.audioUrl) : null;
      if (!url) return;
      analyzeVoiceWordStarts(url, wordRanges.map((w) => w.end - w.start + 1)).then((starts) => {
        if (alive && starts) setVoiceWordStarts(starts);
      }).catch(() => {
      });
      return () => {
        alive = false;
      };
    }, [dialogue.text, dialogue.voiceAudioId, wantVoiceSync]);
    React2.useEffect(() => {
      if (!voiceWordStarts || !voiceRef) return;
      let raf = 0;
      const tick = () => {
        const a = voiceRef.current;
        if (!a || a.ended || a.paused && a.currentTime === 0) {
          setSyncWordIdx(-1);
          raf = requestAnimationFrame(tick);
          return;
        }
        const t = a.currentTime * 1e3;
        let idx = -1;
        for (let i = 0; i < voiceWordStarts.length; i++) {
          if (t >= voiceWordStarts[i]) idx = i;
          else break;
        }
        if (a.ended) idx = -1;
        setSyncWordIdx((prev) => prev === idx ? prev : idx);
        raf = requestAnimationFrame(tick);
      };
      raf = requestAnimationFrame(tick);
      return () => cancelAnimationFrame(raf);
    }, [voiceWordStarts, voiceRef]);
    let revealHighlight = null;
    if ((rhCfg == null ? void 0 : rhCfg.enabled) && displayText.length > 0) {
      const hlColor = rhCfg.useSpeakerColor && dialogue.characterColor && dialogue.characterColor !== "#FFFFFF" ? dialogue.characterColor : rhCfg.color || "#facc15";
      const style = rhCfg.style || "color";
      const syncRange = wantVoiceSync && voiceWordStarts && syncWordIdx >= 0 && syncWordIdx < wordRanges.length ? wordRanges[syncWordIdx] : null;
      if (syncRange && syncRange.start < displayText.length) {
        revealHighlight = { start: syncRange.start, end: Math.min(syncRange.end, displayText.length), color: hlColor, style };
      } else if (!syncRange && !hasFinished) {
        const m = displayText.match(/(\\S+)\$/);
        if (m && m.index != null) revealHighlight = { start: m.index, color: hlColor, style };
      }
    }
    const hasCustomImage = dialogueBoxUrl || dialogueBorderUrl;
    const showNamebox = dialogue.characterName !== "Narrator" && !(reactiveState == null ? void 0 : reactiveState.hideNamebox);
    const nameFont = (charTb == null ? void 0 : charTb.dialogueNameFont) ?? projectUI.dialogueNameFont;
    const nameStyle = {
      ...fontSettingsToStyle(nameFont),
      ...dialogue.characterColor && dialogue.characterColor !== "#FFFFFF" ? { color: dialogue.characterColor } : {}
    };
    const nameboxBgStyle = nameboxImageUrl ? { ...buildImageBackgroundStyle(nameboxImageUrl, nameboxSizeMode), borderRadius: scalePx(nameboxBorderRadius), transition: reactiveTransition } : { backgroundColor: hexToRgba(nameboxColor, nameboxOpacity), borderRadius: scalePx(nameboxBorderRadius), transition: reactiveTransition };
    const dialogueBgColor = hexToRgba(dialogueColor, dialogueOpacity);
    const dialogueImageStyle = dialogueBoxUrl && !isDialogueBoxVideo ? buildImageBackgroundStyle(dialogueBoxUrl, dialogueSizeMode, dialogueSlice) : {};
    const gameW = ((_j = project.gameResolution) == null ? void 0 : _j.width) || 1920;
    const gameH = ((_k = project.gameResolution) == null ? void 0 : _k.height) || 1080;
    const dialogueHPct = dialogueBoxHeight ? dialogueBoxHeight * 100 / gameH : 20;
    const dialogueXPct = projectUI.dialogueBoxX ?? (100 - dialogueBoxWidth) / 2;
    const bmPct = dialogueBoxBottomMargin * 100 / gameH;
    const _qmPosForReserve = projectUI.quickMenuPosition ?? "above-dialogue";
    const _isBottomQmPreset = _qmPosForReserve === "bottom-right" || _qmPosForReserve === "bottom-left";
    const _shouldFloatQm = projectUI.quickMenuFloatOverDialogue ?? false;
    const _qmBottomReservePct = !_shouldFloatQm && _isBottomQmPreset && projectUI.quickMenuY === void 0 ? (projectUI.quickMenuHeight ?? 4) + 2 : 0;
    const dialogueYPct = projectUI.dialogueBoxY ?? 100 - dialogueHPct - bmPct - _qmBottomReservePct;
    const nameWPct = projectUI.nameboxWidth ?? 15;
    const nameHPct = projectUI.nameboxHeight ?? 5;
    const nameXPct = projectUI.nameboxX ?? dialogueXPct + nameboxOffsetX * 100 / gameW;
    const nameYPct = projectUI.nameboxY ?? dialogueYPct - nameHPct - nameboxOffsetY * 100 / gameH;
    const textPadTop = projectUI.dialogueTextPaddingTop ?? 0;
    const textPadBot = projectUI.dialogueTextPaddingBottom ?? 0;
    const textPadLeft = projectUI.dialogueTextPaddingLeft ?? 0;
    const textPadRight = projectUI.dialogueTextPaddingRight ?? 0;
    return /* @__PURE__ */ jsxRuntime2.jsxs(jsxRuntime2.Fragment, { children: [
      showNamebox && /* @__PURE__ */ jsxRuntime2.jsx(
        "div",
        {
          className: "absolute z-[21] cursor-pointer",
          style: {
            left: \`\${nameXPct}%\`,
            top: \`\${nameYPct}%\`,
            width: \`\${nameWPct}%\`,
            height: \`\${nameHPct}%\`,
            animation: "vnDialogueIn 0.25s ease-out"
          },
          onClick: handleClick,
          children: /* @__PURE__ */ jsxRuntime2.jsx("div", { style: {
            width: "100%",
            height: "100%",
            display: "flex",
            alignItems: "center",
            justifyContent: (nameFont == null ? void 0 : nameFont.align) === "center" ? "center" : (nameFont == null ? void 0 : nameFont.align) === "right" ? "flex-end" : "flex-start",
            ...nameboxBgStyle,
            padding: \`\${scalePx(nameboxPadding)} \${scalePx(nameboxHPadding)}\`,
            ...hasCustomImage || nameboxImageUrl ? {} : {
              border: "1px solid rgba(148,163,184,0.35)",
              backdropFilter: "blur(6px)",
              WebkitBackdropFilter: "blur(6px)"
            }
          }, children: /* @__PURE__ */ jsxRuntime2.jsx("span", { style: { ...nameStyle, lineHeight: 1.3 }, children: /* @__PURE__ */ jsxRuntime2.jsx("span", { style: extractTextGradientStyle(nameFont) || void 0, children: dialogue.characterName }) }) })
        }
      ),
      /* @__PURE__ */ jsxRuntime2.jsxs(
        "div",
        {
          className: "absolute z-20 cursor-pointer",
          style: {
            left: \`\${dialogueXPct}%\`,
            top: \`\${dialogueYPct}%\`,
            width: \`\${dialogueBoxWidth}%\`,
            height: \`\${dialogueHPct}%\`,
            animation: "vnDialogueIn 0.25s ease-out",
            ...dialogueBorderUrl ? { ...buildImageBackgroundStyle(dialogueBorderUrl, dialogueSizeMode, dialogueSlice), padding: scalePx(dialogueBorderPadding), borderRadius: scalePx(dialogueBorderRadius) } : {}
          },
          onClick: handleClick,
          children: [
            /* @__PURE__ */ jsxRuntime2.jsxs(
              "div",
              {
                className: "relative",
                style: {
                  borderRadius: scalePx(dialogueBorderRadius),
                  overflow: "hidden",
                  width: "100%",
                  height: "100%",
                  transition: reactiveTransition,
                  ...hasCustomImage ? {} : {
                    backgroundColor: dialogueBgColor,
                    // dialogueBorder palette slot restyles the default box edge (image-
                    // skinned boxes keep their border art — that's authored pixels).
                    border: (uiPalette == null ? void 0 : uiPalette.dialogueBorder) ? \`2px solid \${uiPalette.dialogueBorder}\` : "1px solid rgba(148,163,184,0.25)",
                    boxShadow: "0 4px 24px rgba(0,0,0,0.4), inset 0 1px 0 rgba(255,255,255,0.05)",
                    backdropFilter: "blur(8px)",
                    WebkitBackdropFilter: "blur(8px)"
                  },
                  ...dialogueBoxUrl && !isDialogueBoxVideo ? {
                    ...dialogueImageStyle,
                    backgroundColor: dialogueBgColor,
                    ...dialogueSizeMode !== "nine-slice" ? { padding: scalePx(dialogueBoxPadding) } : {}
                  } : { padding: scalePx(dialogueBoxPadding) }
                },
                children: [
                  isDialogueBoxVideo && dialogueBoxUrl && /* @__PURE__ */ jsxRuntime2.jsx(
                    TrimmedVideo,
                    {
                      src: dialogueBoxUrl,
                      autoPlay: true,
                      loop: true,
                      muted: true,
                      trimStart: dialogueBoxTrim.start,
                      trimEnd: dialogueBoxTrim.end,
                      className: "absolute inset-0 w-full h-full -z-10",
                      style: { pointerEvents: "none", objectFit: "fill", borderRadius: scalePx(dialogueBorderRadius) }
                    }
                  ),
                  /* @__PURE__ */ jsxRuntime2.jsxs("div", { style: {
                    position: "relative",
                    zIndex: 1,
                    padding: dialogueSizeMode === "nine-slice" && dialogueBoxUrl ? scalePx(dialogueBoxPadding) : void 0,
                    paddingTop: textPadTop ? scalePx(textPadTop) : void 0,
                    paddingBottom: textPadBot ? scalePx(textPadBot) : void 0,
                    paddingLeft: textPadLeft ? scalePx(textPadLeft) : void 0,
                    paddingRight: textPadRight ? scalePx(textPadRight) : void 0
                  }, children: [
                    /* @__PURE__ */ jsxRuntime2.jsxs("p", { className: "leading-relaxed", style: { ...dialogueTextStyle, wordBreak: "normal", overflowWrap: "break-word" }, children: [
                      /* @__PURE__ */ jsxRuntime2.jsx(
                        AnimatedDialogueText,
                        {
                          displayText,
                          textEffect: dialogue.textEffect,
                          gradientStyle: extractTextGradientStyle(projectUI.dialogueTextFont) || void 0,
                          revealHighlight
                        }
                      ),
                      !hasFinished && /* @__PURE__ */ jsxRuntime2.jsx("span", { style: {
                        display: "inline-block",
                        width: "0.5em",
                        height: "1em",
                        marginLeft: "2px",
                        verticalAlign: "text-bottom",
                        backgroundColor: dialogueTextStyle.color || ((_l = projectUI.dialogueTextFont) == null ? void 0 : _l.color) || "#FFFFFF",
                        animation: "vnCursorBlink 0.8s step-end infinite",
                        opacity: 0.85
                      } })
                    ] }),
                    hasFinished && !timerLocked && /* @__PURE__ */ jsxRuntime2.jsx("div", { style: {
                      position: "absolute",
                      bottom: "8px",
                      right: "12px",
                      animation: "vnAdvanceBounce 1.2s ease-in-out infinite",
                      opacity: 0.6,
                      fontSize: "calc(var(--font-scale, 1) * 12px)",
                      color: "#94a3b8"
                    }, children: "▼" })
                  ] }),
                  timeLimit > 0 && dialogue.showTimer && hasFinished && /* @__PURE__ */ jsxRuntime2.jsx("div", { style: {
                    position: "absolute",
                    top: 0,
                    left: 0,
                    height: "3px",
                    zIndex: 2,
                    width: \`\${Math.max(0, Math.min(100, timerRemaining / timeLimit * 100))}%\`,
                    background: projectUI.choiceHoverColor || "#6B4C9A",
                    transition: "width 0.12s linear"
                  } })
                ]
              }
            ),
            /* @__PURE__ */ jsxRuntime2.jsx("style", { children: \`
                    @keyframes vnDialogueIn {
                        from { opacity: 0; transform: translateY(12px); }
                        to   { opacity: 1; transform: translateY(0); }
                    }
                    @keyframes vnCursorBlink {
                        0%, 100% { opacity: 0.85; }
                        50% { opacity: 0; }
                    }
                    @keyframes vnAdvanceBounce {
                        0%, 100% { transform: translateY(0); }
                        50% { transform: translateY(4px); }
                    }
                \` })
          ]
        }
      )
    ] });
  };
  const ChoiceMenu = ({ choices, projectUI, onSelect, variables, project, layout, timeLimit, showTimer, onTimeout, uiPalette }) => {
    var _a, _b, _c, _d, _e, _f, _g, _h, _i, _j, _k, _l;
    const [hoveredIndex, setHoveredIndex] = React2.useState(null);
    const [remaining, setRemaining] = React2.useState(timeLimit ?? 0);
    React2.useEffect(() => {
      if (!timeLimit || timeLimit <= 0) return;
      setRemaining(timeLimit);
      const start = performance.now();
      let raf = 0;
      let fired = false;
      const tick = () => {
        const left = Math.max(0, timeLimit - (performance.now() - start) / 1e3);
        setRemaining(left);
        if (left <= 0) {
          if (!fired) {
            fired = true;
            onTimeout == null ? void 0 : onTimeout();
          }
          return;
        }
        raf = requestAnimationFrame(tick);
      };
      raf = requestAnimationFrame(tick);
      return () => cancelAnimationFrame(raf);
    }, [timeLimit, choices]);
    const showCountdown = !!(timeLimit && timeLimit > 0 && showTimer);
    const countdownFrac = timeLimit && timeLimit > 0 ? Math.max(0, Math.min(1, remaining / timeLimit)) : 0;
    const countdownOverlay = showCountdown ? /* @__PURE__ */ jsxRuntime2.jsx("div", { className: "absolute inset-0 z-40 pointer-events-none", children: /* @__PURE__ */ jsxRuntime2.jsx("div", { className: "absolute left-1/2 -translate-x-1/2", style: { top: "3%", width: "40%", height: scalePx(8), background: "rgba(0,0,0,0.45)", borderRadius: 999, overflow: "hidden", boxShadow: "0 1px 4px rgba(0,0,0,0.4)" }, children: /* @__PURE__ */ jsxRuntime2.jsx("div", { style: { height: "100%", width: \`\${countdownFrac * 100}%\`, background: projectUI.choiceHoverColor || "#6B4C9A", transition: "width 0.12s linear" } }) }) }) : null;
    const choiceButtonUrl = projectUI.choiceButtonImage ? resolveFieldUrl(
      project.id,
      projectUI.choiceButtonImage.type === "video" ? ((_a = project.videos[projectUI.choiceButtonImage.id]) == null ? void 0 : _a.videoUrl) || ((_b = project.images[projectUI.choiceButtonImage.id]) == null ? void 0 : _b.videoUrl) || ((_c = project.backgrounds[projectUI.choiceButtonImage.id]) == null ? void 0 : _c.videoUrl) : ((_d = project.images[projectUI.choiceButtonImage.id]) == null ? void 0 : _d.imageUrl) || ((_e = project.backgrounds[projectUI.choiceButtonImage.id]) == null ? void 0 : _e.imageUrl)
    ) : null;
    const isChoiceButtonVideo = ((_f = projectUI.choiceButtonImage) == null ? void 0 : _f.type) === "video";
    const choiceBorderUrl = projectUI.choiceButtonBorderImage ? resolveFieldUrl(project.id, ((_g = project.images[projectUI.choiceButtonBorderImage.id]) == null ? void 0 : _g.imageUrl) || ((_h = project.backgrounds[projectUI.choiceButtonBorderImage.id]) == null ? void 0 : _h.imageUrl)) : null;
    const choiceBorderPadding = projectUI.choiceBorderPadding ?? 8;
    const choiceWidth = projectUI.choiceButtonWidth || 0;
    const choiceHeight = projectUI.choiceButtonHeight || 0;
    const choicePadding = projectUI.choiceButtonPadding ?? 16;
    const choiceSizeMode = projectUI.choiceButtonSizeMode ?? "stretch";
    const choiceSlice = projectUI.choiceButtonSlice ?? 15;
    const choiceColor = (uiPalette == null ? void 0 : uiPalette.choiceBg) ?? projectUI.choiceButtonColor ?? "#1e293b";
    const choiceOpacity = projectUI.choiceButtonOpacity ?? 90;
    const choiceBorderRadius = projectUI.choiceButtonBorderRadius ?? 8;
    const choiceHoverColor = projectUI.choiceHoverColor ?? "#334155";
    const choiceHoverUrl = projectUI.choiceHoverImage ? resolveFieldUrl(project.id, ((_i = project.images[projectUI.choiceHoverImage.id]) == null ? void 0 : _i.imageUrl) || ((_j = project.backgrounds[projectUI.choiceHoverImage.id]) == null ? void 0 : _j.imageUrl)) : null;
    const choiceBgColor = hexToRgba(choiceColor, choiceOpacity);
    const choiceHoverBgColor = hexToRgba(choiceHoverColor, choiceOpacity);
    const gameW = ((_k = project.gameResolution) == null ? void 0 : _k.width) || 1920;
    const gameH = ((_l = project.gameResolution) == null ? void 0 : _l.height) || 1080;
    const choiceWPct = choiceWidth ? choiceWidth * 100 / gameW : 30;
    const choiceHPct = choiceHeight ? choiceHeight * 100 / gameH : 25;
    const choiceXPct = projectUI.choiceButtonX ?? 50 - choiceWPct / 2;
    const choiceYPct = projectUI.choiceButtonY ?? 35;
    const resolveChoiceImg = (a) => {
      var _a2, _b2, _c2, _d2, _e2;
      if (!a) return null;
      return resolveFieldUrl(project.id, a.type === "video" ? ((_a2 = project.videos[a.id]) == null ? void 0 : _a2.videoUrl) || ((_b2 = project.backgrounds[a.id]) == null ? void 0 : _b2.videoUrl) || ((_c2 = project.images[a.id]) == null ? void 0 : _c2.videoUrl) || null : ((_d2 = project.images[a.id]) == null ? void 0 : _d2.imageUrl) || ((_e2 = project.backgrounds[a.id]) == null ? void 0 : _e2.imageUrl) || null);
    };
    const renderButton = (choice, index, fill) => {
      var _a2, _b2;
      const interpolatedText = interpolateVariables(choice.text, variables, project);
      const isHovered = hoveredIndex === index;
      const optImg = resolveChoiceImg(choice.image);
      const optHoverImg = resolveChoiceImg(choice.hoverImage);
      const baseImg = optImg ?? choiceButtonUrl;
      const baseIsVideo = optImg ? ((_a2 = choice.image) == null ? void 0 : _a2.type) === "video" : isChoiceButtonVideo;
      const baseTrimRef = optImg ? choice.image : projectUI.choiceButtonImage;
      const baseTrimAsset = (baseTrimRef == null ? void 0 : baseTrimRef.id) ? project.videos[baseTrimRef.id] || project.images[baseTrimRef.id] || project.backgrounds[baseTrimRef.id] : void 0;
      const baseTrim = resolveVideoTrim(baseTrimRef, baseTrimAsset);
      const hoverImg = optHoverImg ?? choiceHoverUrl;
      const activeButtonUrl = isHovered && hoverImg ? hoverImg : baseImg;
      const optBg = choice.backgroundColor ? hexToRgba(choice.backgroundColor, choiceOpacity) : choiceBgColor;
      const optHoverBg = choice.hoverBackgroundColor ? hexToRgba(choice.hoverBackgroundColor, choiceOpacity) : choiceHoverBgColor;
      const optRadius = choice.borderRadius ?? choiceBorderRadius;
      const hasImg = !!(baseImg || choiceBorderUrl);
      return /* @__PURE__ */ jsxRuntime2.jsxs(
        "button",
        {
          onClick: () => onSelect(choice),
          onMouseEnter: () => setHoveredIndex(index),
          onMouseLeave: () => setHoveredIndex(null),
          className: "relative overflow-hidden w-full transition-all duration-200 hover:scale-[1.03]",
          style: {
            borderRadius: scalePx(optRadius),
            ...fill ? { height: "100%" } : {},
            ...activeButtonUrl && !baseIsVideo ? {
              ...buildImageBackgroundStyle(activeButtonUrl, choiceSizeMode, choiceSlice),
              backgroundColor: isHovered ? optHoverBg : optBg
            } : !hasImg ? {
              backgroundColor: isHovered ? optHoverBg : optBg,
              border: (uiPalette == null ? void 0 : uiPalette.choiceBorder) ? \`2px solid \${uiPalette.choiceBorder}\` : "1px solid rgba(148,163,184,0.3)",
              boxShadow: "0 2px 12px rgba(0,0,0,0.3), inset 0 1px 0 rgba(255,255,255,0.06)",
              backdropFilter: "blur(6px)",
              WebkitBackdropFilter: "blur(6px)"
            } : { backgroundColor: isHovered ? optHoverBg : "transparent" },
            padding: \`\${scalePx(choicePadding)} \${scalePx(choicePadding * 2)}\`,
            ...!fill && choiceHeight ? { height: scalePx(choiceHeight) } : {},
            ...fontSettingsToStyle(projectUI.choiceTextFont),
            ...(uiPalette == null ? void 0 : uiPalette.choiceText) ? { color: uiPalette.choiceText } : {},
            ...choice.fontSize ? { fontSize: scalePx(choice.fontSize) } : {},
            ...choice.textColor ? { color: choice.textColor } : {},
            textAlign: ((_b2 = projectUI.choiceTextFont) == null ? void 0 : _b2.align) || "center",
            wordBreak: "normal",
            overflowWrap: "break-word",
            cursor: "pointer"
          },
          children: [
            baseIsVideo && baseImg && /* @__PURE__ */ jsxRuntime2.jsx(TrimmedVideo, { src: baseImg, autoPlay: true, loop: true, muted: true, trimStart: baseTrim.start, trimEnd: baseTrim.end, className: "absolute inset-0 w-full h-full -z-10", style: { pointerEvents: "none", objectFit: "fill", borderRadius: scalePx(optRadius) } }),
            /* @__PURE__ */ jsxRuntime2.jsx("span", { className: "relative z-10", style: { ...extractTextGradientStyle(projectUI.choiceTextFont) || {}, ...(uiPalette == null ? void 0 : uiPalette.choiceText) && !choice.textColor ? { color: uiPalette.choiceText, background: "none", WebkitTextFillColor: uiPalette.choiceText } : {}, ...choice.textColor ? { color: choice.textColor } : {} }, children: interpolatedText })
          ]
        }
      );
    };
    const choiceKeyframes = /* @__PURE__ */ jsxRuntime2.jsx("style", { children: \`
            @keyframes vnChoiceOverlayIn { from { opacity: 0; } to { opacity: 1; } }
            @keyframes vnChoiceSlideIn { from { opacity: 0; transform: translateY(16px); } to { opacity: 1; transform: translateY(0); } }
        \` });
    if (layout === "free") {
      return /* @__PURE__ */ jsxRuntime2.jsxs(jsxRuntime2.Fragment, { children: [
        countdownOverlay,
        /* @__PURE__ */ jsxRuntime2.jsxs("div", { className: "absolute inset-0 z-30", style: { pointerEvents: "none", animation: "vnChoiceOverlayIn 0.3s ease-out" }, children: [
          choices.map((choice, index) => {
            const bx = choice.x ?? 34 + index * 2;
            const by = choice.y ?? 40 + index * 12;
            const bw = choice.width ?? 25;
            const bh = choice.height ?? 9;
            return /* @__PURE__ */ jsxRuntime2.jsx("div", { style: {
              position: "absolute",
              left: \`\${bx}%\`,
              top: \`\${by}%\`,
              width: \`\${bw}%\`,
              height: \`\${bh}%\`,
              pointerEvents: "auto",
              animation: \`vnChoiceSlideIn 0.35s ease-out \${index * 0.08}s both\`,
              ...choiceBorderUrl ? { ...buildImageBackgroundStyle(choiceBorderUrl, choiceSizeMode, choiceSlice), padding: scalePx(choiceBorderPadding), borderRadius: scalePx(choiceBorderRadius) } : {}
            }, children: renderButton(choice, index, true) }, index);
          }),
          choiceKeyframes
        ] })
      ] });
    }
    const horizontal = layout === "horizontal";
    return /* @__PURE__ */ jsxRuntime2.jsxs(jsxRuntime2.Fragment, { children: [
      countdownOverlay,
      /* @__PURE__ */ jsxRuntime2.jsxs(
        "div",
        {
          className: \`absolute z-30 flex \${horizontal ? "flex-row flex-wrap gap-3" : "flex-col"} items-center justify-center\`,
          style: {
            left: \`\${choiceXPct}%\`,
            top: \`\${choiceYPct}%\`,
            width: \`\${choiceWPct}%\`,
            height: \`\${choiceHPct}%\`,
            animation: "vnChoiceOverlayIn 0.3s ease-out"
          },
          children: [
            choices.map((choice, index) => /* @__PURE__ */ jsxRuntime2.jsx(
              "div",
              {
                className: horizontal ? "" : "mb-3",
                style: {
                  animation: \`vnChoiceSlideIn 0.35s ease-out \${index * 0.08}s both\`,
                  ...horizontal ? {} : { width: "100%" },
                  ...choiceBorderUrl ? { ...buildImageBackgroundStyle(choiceBorderUrl, choiceSizeMode, choiceSlice), padding: scalePx(choiceBorderPadding), borderRadius: scalePx(choiceBorderRadius) } : {}
                },
                children: renderButton(choice, index, false)
              },
              index
            )),
            choiceKeyframes
          ]
        }
      )
    ] });
  };
  const TextInputForm = ({ textInput, onSubmit, variables, project, projectUI }) => {
    var _a, _b, _c, _d, _e, _f, _g, _h, _i, _j, _k, _l, _m;
    const [inputValue, setInputValue] = React2.useState("");
    const handleSubmit = (e) => {
      e.preventDefault();
      onSubmit(inputValue);
    };
    const interpolatedPrompt = interpolateVariables(textInput.prompt, variables, project);
    const inputBoxUrl = (projectUI == null ? void 0 : projectUI.inputBoxImage) ? resolveFieldUrl(
      project.id,
      projectUI.inputBoxImage.type === "video" ? ((_a = project.videos[projectUI.inputBoxImage.id]) == null ? void 0 : _a.videoUrl) || ((_b = project.images[projectUI.inputBoxImage.id]) == null ? void 0 : _b.videoUrl) || ((_c = project.backgrounds[projectUI.inputBoxImage.id]) == null ? void 0 : _c.videoUrl) : ((_d = project.images[projectUI.inputBoxImage.id]) == null ? void 0 : _d.imageUrl) || ((_e = project.backgrounds[projectUI.inputBoxImage.id]) == null ? void 0 : _e.imageUrl)
    ) : null;
    const isInputBoxVideo = ((_f = projectUI == null ? void 0 : projectUI.inputBoxImage) == null ? void 0 : _f.type) === "video";
    const inputBoxTrim = resolveVideoTrim(projectUI == null ? void 0 : projectUI.inputBoxImage, ((_g = projectUI == null ? void 0 : projectUI.inputBoxImage) == null ? void 0 : _g.id) ? project.videos[projectUI.inputBoxImage.id] || project.images[projectUI.inputBoxImage.id] || project.backgrounds[projectUI.inputBoxImage.id] : void 0);
    const inputBorderUrl = (projectUI == null ? void 0 : projectUI.inputBoxBorderImage) ? resolveFieldUrl(project.id, ((_h = project.images[projectUI.inputBoxBorderImage.id]) == null ? void 0 : _h.imageUrl) || ((_i = project.backgrounds[projectUI.inputBoxBorderImage.id]) == null ? void 0 : _i.imageUrl)) : null;
    const inputBorderPadding = (projectUI == null ? void 0 : projectUI.inputBorderPadding) ?? 8;
    const inputBoxWidth = (projectUI == null ? void 0 : projectUI.inputBoxWidth) || 0;
    const inputBoxPadding = (projectUI == null ? void 0 : projectUI.inputBoxPadding) ?? 24;
    const inputSizeMode = (projectUI == null ? void 0 : projectUI.inputBoxSizeMode) ?? "stretch";
    const inputSlice = (projectUI == null ? void 0 : projectUI.inputBoxSlice) ?? 20;
    const inputColor = (projectUI == null ? void 0 : projectUI.inputBoxColor) ?? "#0f172a";
    const inputOpacity = (projectUI == null ? void 0 : projectUI.inputBoxOpacity) ?? 92;
    const inputBorderRadius = (projectUI == null ? void 0 : projectUI.inputBoxBorderRadius) ?? 8;
    const inputBgColor = hexToRgba(inputColor, inputOpacity);
    const hasCustomImage = inputBoxUrl || inputBorderUrl;
    const promptStyle = (projectUI == null ? void 0 : projectUI.inputPromptFont) ? { ...fontSettingsToStyle(projectUI.inputPromptFont), textAlign: projectUI.inputPromptFont.align || "center" } : { color: "#FFFFFF", textAlign: "center" };
    const fieldStyle = (projectUI == null ? void 0 : projectUI.inputFieldFont) ? fontSettingsToStyle(projectUI.inputFieldFont) : { color: "#FFFFFF" };
    const submitStyle = (projectUI == null ? void 0 : projectUI.inputSubmitFont) ? fontSettingsToStyle(projectUI.inputSubmitFont) : { color: "#FFFFFF" };
    const inputSubmitUrl = (projectUI == null ? void 0 : projectUI.inputSubmitImage) ? resolveFieldUrl(project.id, ((_j = project.images[projectUI.inputSubmitImage.id]) == null ? void 0 : _j.imageUrl) || ((_k = project.backgrounds[projectUI.inputSubmitImage.id]) == null ? void 0 : _k.imageUrl)) : null;
    const submitRadius = (projectUI == null ? void 0 : projectUI.inputSubmitBorderRadius) ?? Math.max(4, inputBorderRadius - 4);
    const submitLabel = (projectUI == null ? void 0 : projectUI.inputSubmitLabel) || "Submit";
    const gameW = ((_l = project.gameResolution) == null ? void 0 : _l.width) || 1920;
    const gameH = ((_m = project.gameResolution) == null ? void 0 : _m.height) || 1080;
    const inputWPct = inputBoxWidth ? inputBoxWidth * 100 / gameW : 30;
    const inputHPct = (projectUI == null ? void 0 : projectUI.inputBoxHeight) ? projectUI.inputBoxHeight * 100 / gameH : 20;
    const inputXPct = (projectUI == null ? void 0 : projectUI.inputBoxX) ?? 50 - inputWPct / 2;
    const inputYPct = (projectUI == null ? void 0 : projectUI.inputBoxY) ?? 40;
    return /* @__PURE__ */ jsxRuntime2.jsx(
      "div",
      {
        className: "absolute z-30 flex flex-col items-center justify-center",
        style: {
          left: \`\${inputXPct}%\`,
          top: \`\${inputYPct}%\`,
          width: \`\${inputWPct}%\`,
          height: \`\${inputHPct}%\`
        },
        children: /* @__PURE__ */ jsxRuntime2.jsx(
          "div",
          {
            className: "relative",
            style: {
              borderRadius: scalePx(inputBorderRadius),
              width: "100%",
              ...inputBorderUrl ? { ...buildImageBackgroundStyle(inputBorderUrl, inputSizeMode, inputSlice), padding: scalePx(inputBorderPadding) } : {},
              animation: "vnDialogueIn 0.25s ease-out"
            },
            children: /* @__PURE__ */ jsxRuntime2.jsxs(
              "div",
              {
                className: "relative",
                style: {
                  borderRadius: scalePx(inputBorderRadius),
                  overflow: "hidden",
                  ...hasCustomImage ? {} : {
                    backgroundColor: inputBgColor,
                    border: "1px solid rgba(148,163,184,0.3)",
                    boxShadow: "0 4px 24px rgba(0,0,0,0.4), inset 0 1px 0 rgba(255,255,255,0.05)",
                    backdropFilter: "blur(8px)",
                    WebkitBackdropFilter: "blur(8px)"
                  },
                  ...inputBoxUrl && !isInputBoxVideo ? { ...buildImageBackgroundStyle(inputBoxUrl, inputSizeMode, inputSlice), backgroundColor: inputBgColor, ...inputSizeMode !== "nine-slice" ? { padding: scalePx(inputBoxPadding) } : {} } : { padding: scalePx(inputBoxPadding) }
                },
                children: [
                  isInputBoxVideo && inputBoxUrl && /* @__PURE__ */ jsxRuntime2.jsx(
                    TrimmedVideo,
                    {
                      src: inputBoxUrl,
                      autoPlay: true,
                      loop: true,
                      muted: true,
                      trimStart: inputBoxTrim.start,
                      trimEnd: inputBoxTrim.end,
                      className: "absolute inset-0 w-full h-full -z-10",
                      style: { pointerEvents: "none", objectFit: "fill", borderRadius: scalePx(inputBorderRadius) }
                    }
                  ),
                  /* @__PURE__ */ jsxRuntime2.jsxs("div", { style: { position: "relative", zIndex: 1, padding: inputSizeMode === "nine-slice" && inputBoxUrl ? scalePx(inputBoxPadding) : void 0 }, children: [
                    /* @__PURE__ */ jsxRuntime2.jsx("p", { className: "mb-4", style: promptStyle, children: /* @__PURE__ */ jsxRuntime2.jsx("span", { style: extractTextGradientStyle(projectUI == null ? void 0 : projectUI.inputPromptFont) || void 0, children: interpolatedPrompt }) }),
                    /* @__PURE__ */ jsxRuntime2.jsxs("form", { onSubmit: handleSubmit, children: [
                      /* @__PURE__ */ jsxRuntime2.jsx(
                        "input",
                        {
                          type: "text",
                          value: inputValue,
                          onChange: (e) => setInputValue(e.target.value),
                          placeholder: textInput.placeholder,
                          maxLength: textInput.maxLength,
                          className: "w-full px-3 py-2 focus:outline-none transition-colors",
                          style: {
                            ...fieldStyle,
                            backgroundColor: "rgba(15,23,42,0.6)",
                            border: "1px solid rgba(148,163,184,0.3)",
                            borderRadius: scalePx(Math.max(4, inputBorderRadius - 4))
                          },
                          autoFocus: true
                        }
                      ),
                      /* @__PURE__ */ jsxRuntime2.jsx(
                        "button",
                        {
                          type: "submit",
                          className: "w-full mt-4 px-4 py-2 transition-colors hover:brightness-110",
                          style: {
                            ...submitStyle,
                            ...inputSubmitUrl ? { backgroundImage: \`url(\${inputSubmitUrl})\`, backgroundSize: "100% 100%", backgroundRepeat: "no-repeat", backgroundColor: "transparent", border: "none" } : { backgroundColor: (projectUI == null ? void 0 : projectUI.inputSubmitColor) ?? (hasCustomImage ? "rgba(255,255,255,0.1)" : "rgba(51,65,85,0.8)"), border: "1px solid rgba(148,163,184,0.2)" },
                            borderRadius: scalePx(submitRadius)
                          },
                          children: /* @__PURE__ */ jsxRuntime2.jsx("span", { style: extractTextGradientStyle(projectUI == null ? void 0 : projectUI.inputSubmitFont) || void 0, children: submitLabel })
                        }
                      )
                    ] })
                  ] })
                ]
              }
            )
          }
        )
      }
    );
  };
  const SLOTS_PER_PAGE = 4;
  const SaveSlotGridComponent = ({ element, style, isSaveMode, gameSaves, onAction }) => {
    const [currentPage, setCurrentPage] = React2.useState(0);
    const el = element;
    const totalSlots = el.slotCount;
    const totalPages = Math.max(1, Math.ceil(totalSlots / SLOTS_PER_PAGE));
    const startIndex = currentPage * SLOTS_PER_PAGE;
    const pageSlots = Array.from({ length: SLOTS_PER_PAGE }, (_, k) => startIndex + k).filter((i) => i < totalSlots);
    const baseFont = fontSettingsToStyle(el.font);
    const slotBgColor = el.slotBackgroundColor || "#1e293b";
    const slotBorderColor = el.slotBorderColor || "#475569";
    const slotHoverBorderColor = el.slotHoverBorderColor || "#38bdf8";
    const slotHeaderColor = el.slotHeaderColor || "#7dd3fc";
    const slotTextColor = el.slotTextColor || "#e2e8f0";
    const emptySlotStyle = el.emptySlotFont ? { ...fontSettingsToStyle(el.emptySlotFont), textAlign: void 0 } : { color: el.emptySlotTextColor || "#a0aec0", fontSize: baseFont.fontSize, fontFamily: baseFont.fontFamily };
    const navBtnStyle = el.navButtonFont ? { ...fontSettingsToStyle(el.navButtonFont), textAlign: void 0, backgroundColor: "rgba(255,255,255,0.1)", borderRadius: "4px", padding: "2px 10px" } : { color: slotHeaderColor, fontFamily: baseFont.fontFamily, fontSize: baseFont.fontSize, fontWeight: "bold", backgroundColor: "rgba(255,255,255,0.1)", borderRadius: "4px", padding: "2px 10px" };
    const pageIndicatorStyle = el.pageIndicatorFont ? { ...fontSettingsToStyle(el.pageIndicatorFont), textAlign: void 0 } : { color: slotHeaderColor, fontFamily: baseFont.fontFamily, fontSize: baseFont.fontSize };
    const prevLabel = el.prevButtonText ?? "◀ Prev";
    const nextLabel = el.nextButtonText ?? "Next ▶";
    const renderSlot = (i) => {
      const slotData = gameSaves[i + 1];
      const action = isSaveMode ? { type: UIActionType.SaveGame, slotNumber: i + 1 } : { type: UIActionType.LoadGame, slotNumber: i + 1 };
      return /* @__PURE__ */ jsxRuntime2.jsxs(
        "button",
        {
          onClick: () => {
            if (!isSaveMode && !slotData) return;
            onAction(action);
          },
          disabled: !isSaveMode && !slotData,
          className: "rounded-lg border-2 overflow-hidden flex flex-col w-full h-full",
          style: {
            backgroundColor: slotBgColor,
            borderColor: slotBorderColor,
            transition: "border-color 0.15s",
            cursor: !isSaveMode && !slotData ? "default" : "pointer"
          },
          onMouseEnter: (e) => {
            if (!e.currentTarget.disabled) {
              e.currentTarget.style.borderColor = slotHoverBorderColor;
            }
          },
          onMouseLeave: (e) => {
            e.currentTarget.style.borderColor = slotBorderColor;
          },
          children: [
            /* @__PURE__ */ jsxRuntime2.jsxs("div", { className: "relative w-full overflow-hidden", style: { flex: "1 1 0", minHeight: 0 }, children: [
              (slotData == null ? void 0 : slotData.screenshot) ? /* @__PURE__ */ jsxRuntime2.jsx(
                "img",
                {
                  src: slotData.screenshot,
                  alt: \`Save slot \${i + 1}\`,
                  className: "absolute inset-0 w-full h-full object-cover"
                }
              ) : /* @__PURE__ */ jsxRuntime2.jsx("div", { className: "absolute inset-0 flex flex-col items-center justify-center", style: { backgroundColor: "rgba(0,0,0,0.4)", padding: "0 8%" }, children: /* @__PURE__ */ jsxRuntime2.jsx("span", { style: { ...emptySlotStyle, textShadow: "0 2px 4px rgba(0,0,0,0.5)" }, children: el.emptySlotText }) }),
              !el.hideSlotLabel && /* @__PURE__ */ jsxRuntime2.jsxs("div", { style: {
                position: "absolute",
                top: "4px",
                left: "4px",
                color: slotHeaderColor,
                fontWeight: "bold",
                fontSize: baseFont.fontSize,
                fontFamily: baseFont.fontFamily,
                textShadow: "0 2px 4px rgba(0,0,0,0.7)",
                zIndex: 10
              }, children: [
                "Slot ",
                i + 1
              ] }),
              slotData && !el.hideEraseButtons && /* @__PURE__ */ jsxRuntime2.jsx(
                "div",
                {
                  role: "button",
                  "aria-label": "Erase this save",
                  title: "Erase this save",
                  onClick: (e) => {
                    e.stopPropagation();
                    onAction({ type: UIActionType.DeleteSave, slotNumber: i + 1 });
                  },
                  style: { position: "absolute", top: "4px", right: "4px", zIndex: 11, width: "1.5em", height: "1.5em", display: "flex", alignItems: "center", justifyContent: "center", borderRadius: "9999px", background: "rgba(0,0,0,0.55)", color: "#fff", fontSize: baseFont.fontSize, lineHeight: 1, cursor: "pointer", textShadow: "0 1px 2px rgba(0,0,0,0.8)" },
                  onMouseEnter: (e) => {
                    e.currentTarget.style.background = "rgba(220,38,38,0.92)";
                  },
                  onMouseLeave: (e) => {
                    e.currentTarget.style.background = "rgba(0,0,0,0.55)";
                  },
                  children: "✕"
                }
              )
            ] }),
            !el.hideInfoBar && slotData && /* @__PURE__ */ jsxRuntime2.jsxs("div", { style: { flex: "0 0 auto", padding: "2px 4px", backgroundColor: "rgba(0,0,0,0.5)" }, children: [
              /* @__PURE__ */ jsxRuntime2.jsx("div", { style: { color: slotTextColor, margin: 0, overflow: "hidden", textOverflow: "ellipsis", whiteSpace: "nowrap", fontSize: \`calc(0.8 * \${baseFont.fontSize})\` }, children: slotData.sceneName }),
              /* @__PURE__ */ jsxRuntime2.jsx("div", { style: { color: slotTextColor, opacity: 0.6, margin: 0, fontSize: \`calc(0.65 * \${baseFont.fontSize})\` }, children: new Date(slotData.timestamp).toLocaleString() })
            ] })
          ]
        },
        i
      );
    };
    if (el.slotLayout === "free" && el.slotRects && el.slotRects.length > 0) {
      const rects = el.slotRects;
      return /* @__PURE__ */ jsxRuntime2.jsx("div", { style: { position: "absolute", left: 0, top: 0, width: "100%", height: "100%", zIndex: style.zIndex, opacity: style.opacity, pointerEvents: "none" }, children: Array.from({ length: totalSlots }, (_, i) => {
        const rect = rects[i];
        if (!rect) return null;
        return /* @__PURE__ */ jsxRuntime2.jsx(
          "div",
          {
            style: { position: "absolute", left: \`\${rect.x}%\`, top: \`\${rect.y}%\`, width: \`\${rect.width}%\`, height: \`\${rect.height}%\`, pointerEvents: "auto" },
            children: renderSlot(i)
          },
          i
        );
      }) });
    }
    return /* @__PURE__ */ jsxRuntime2.jsxs("div", { style, className: "flex flex-col h-full", children: [
      /* @__PURE__ */ jsxRuntime2.jsx("div", { className: "grid grid-cols-2 gap-[3%] flex-1 min-h-0 p-[2%]", children: pageSlots.map(renderSlot) }),
      totalPages > 1 && /* @__PURE__ */ jsxRuntime2.jsxs("div", { className: "flex items-center justify-center gap-4 py-2 flex-shrink-0", children: [
        /* @__PURE__ */ jsxRuntime2.jsx(
          "button",
          {
            onClick: (e) => {
              e.stopPropagation();
              setCurrentPage((p) => Math.max(0, p - 1));
            },
            disabled: currentPage === 0,
            className: "disabled:opacity-30",
            style: navBtnStyle,
            children: prevLabel
          }
        ),
        /* @__PURE__ */ jsxRuntime2.jsxs("span", { style: pageIndicatorStyle, children: [
          "Page ",
          currentPage + 1,
          " / ",
          totalPages
        ] }),
        /* @__PURE__ */ jsxRuntime2.jsx(
          "button",
          {
            onClick: (e) => {
              e.stopPropagation();
              setCurrentPage((p) => Math.min(totalPages - 1, p + 1));
            },
            disabled: currentPage >= totalPages - 1,
            className: "disabled:opacity-30",
            style: navBtnStyle,
            children: nextLabel
          }
        )
      ] })
    ] });
  };
  const makeSpotlightState = (c) => ({
    sourceX: c.sourceX ?? 50,
    sourceY: c.sourceY ?? 0,
    aimAngle: c.aimAngle ?? 0,
    intensity: c.intensity ?? 0.85,
    beamWidth: c.beamWidth ?? 45,
    sourceWidth: c.sourceWidth ?? 8,
    height: c.height ?? 100,
    falloff: c.falloff ?? 0.5,
    color: c.color || "#fff3d6",
    followMouse: c.followMouse !== false,
    swivelMax: c.swivelMax ?? 30,
    toggleKey: c.toggleKey,
    affectsDialogue: c.affectsDialogue !== false,
    on: true
  });
  const TIMER_VAR_MUTATION_TYPES = /* @__PURE__ */ new Set([
    UIActionType.SetVariable,
    UIActionType.ResetVariable,
    UIActionType.GiveItem,
    UIActionType.UseItem,
    UIActionType.DestroyItem,
    UIActionType.UseSelectedItem,
    UIActionType.RestockCollection,
    UIActionType.BuyItem,
    UIActionType.SellItem,
    UIActionType.BuySelectedItem,
    UIActionType.SellSelectedItem
  ]);
  const TimerElement = ({ element, style, onAction, onCommitVariables }) => {
    const [remaining, setRemaining] = React2.useState(Math.max(0, element.durationSeconds || 0));
    const onActionRef = React2.useRef(onAction);
    onActionRef.current = onAction;
    const onCommitRef = React2.useRef(onCommitVariables);
    onCommitRef.current = onCommitVariables;
    const actionsRef = React2.useRef(element.actions);
    actionsRef.current = element.actions;
    React2.useEffect(() => {
      let cancelled = false;
      const total = Math.max(0, element.durationSeconds || 0);
      const fire = () => {
        if (cancelled) return;
        const acts = actionsRef.current || [];
        const vars = acts.filter((a) => TIMER_VAR_MUTATION_TYPES.has(a.type));
        const others = acts.filter((a) => !TIMER_VAR_MUTATION_TYPES.has(a.type));
        vars.forEach((a) => onActionRef.current(a));
        if (vars.length && onCommitRef.current) onCommitRef.current();
        others.forEach((a) => onActionRef.current(a));
      };
      setRemaining(total);
      let elapsedMs = 0;
      const TICK = 250;
      const iv = window.setInterval(() => {
        if (cancelled) return;
        elapsedMs += TICK;
        setRemaining(Math.max(0, total - elapsedMs / 1e3));
        if (elapsedMs >= total * 1e3) {
          fire();
          if (element.loop) {
            elapsedMs = 0;
            setRemaining(total);
          } else {
            window.clearInterval(iv);
          }
        }
      }, TICK);
      return () => {
        cancelled = true;
        window.clearInterval(iv);
      };
    }, [element.id, element.durationSeconds, element.loop]);
    if (!element.showCountdown) return /* @__PURE__ */ jsxRuntime2.jsx("div", { style: { ...style, pointerEvents: "none" } });
    return /* @__PURE__ */ jsxRuntime2.jsx("div", { style: { ...style, pointerEvents: "none" }, className: "flex items-center justify-center text-white text-sm font-semibold", children: /* @__PURE__ */ jsxRuntime2.jsxs("span", { style: { textShadow: "0 1px 3px rgba(0,0,0,0.85)" }, children: [
      "⏱ ",
      Math.ceil(remaining)
    ] }) });
  };
  const ButtonElement = ({ element, style, playSound, onAction, getElementAssetUrl, variables = {}, project, onCommitVariables }) => {
    var _a;
    const [isHovered, setIsHovered] = React2.useState(false);
    const bgUrl = getElementAssetUrl(element.image);
    const hoverUrl = getElementAssetUrl(element.hoverImage);
    const displayUrl = isHovered && hoverUrl ? hoverUrl : bgUrl;
    const textStyle = fontSettingsToStyle(element.font);
    const interpolatedText = project ? interpolateVariables(element.text, variables, project) : element.text;
    const handleClick = () => {
      try {
        playSound(element.clickSoundId);
      } catch (e) {
      }
      const allActions = [];
      if (element.action) {
        allActions.push(element.action);
      }
      if (element.actions && element.actions.length > 0) {
        allActions.push(...element.actions);
      }
      const isVarMutation = (a) => a.type === UIActionType.SetVariable || a.type === UIActionType.ResetVariable || a.type === UIActionType.GiveItem || a.type === UIActionType.UseItem || a.type === UIActionType.DestroyItem || a.type === UIActionType.UseSelectedItem || a.type === UIActionType.RestockCollection || a.type === UIActionType.BuyItem || a.type === UIActionType.SellItem || a.type === UIActionType.BuySelectedItem || a.type === UIActionType.SellSelectedItem;
      const setVarActions = allActions.filter(isVarMutation);
      const otherActions = allActions.filter((a) => !isVarMutation(a));
      setVarActions.forEach((action) => onAction(action));
      if (setVarActions.length > 0 && onCommitVariables) {
        runtimeDebugLog("[UI Button] Committing", setVarActions.length, "variable changes before navigation");
        onCommitVariables();
      }
      otherActions.forEach((action) => onAction(action));
    };
    const buttonBg = element.backgroundColor || "#4D3273";
    const hoverBg = element.hoverBackgroundColor || (element.backgroundColor ? void 0 : "#6B4C9A");
    const btnRadius = element.borderRadius != null ? \`calc(var(--font-scale, 1) * \${element.borderRadius}px)\` : void 0;
    const { transform, overflow, ...wrapperStyle } = style;
    const fit = !!element.fitToContent && !!displayUrl;
    const interactive = wrapperStyle.pointerEvents !== "none";
    if (fit) {
      return /* @__PURE__ */ jsxRuntime2.jsx("div", { style: { ...wrapperStyle, transform, overflow: "hidden", display: "flex", alignItems: "center", justifyContent: "center", pointerEvents: "none" }, children: /* @__PURE__ */ jsxRuntime2.jsxs(
        "button",
        {
          style: { maxWidth: "100%", maxHeight: "100%", width: "auto", height: "auto", padding: 0, border: "none", background: "transparent", position: "relative", display: "block", lineHeight: 0, cursor: interactive ? "pointer" : "default", pointerEvents: interactive ? "auto" : "none" },
          className: "transition-transform transform hover:scale-105",
          onMouseEnter: () => {
            try {
              playSound(element.hoverSoundId);
            } catch (e) {
            }
            setIsHovered(true);
          },
          onMouseLeave: () => setIsHovered(false),
          onClick: handleClick,
          children: [
            /* @__PURE__ */ jsxRuntime2.jsx("img", { src: displayUrl, alt: element.text, draggable: false, style: { display: "block", maxWidth: "100%", maxHeight: "100%", width: "auto", height: "auto", objectFit: "contain", borderRadius: btnRadius } }),
            interpolatedText && /* @__PURE__ */ jsxRuntime2.jsx("span", { className: "absolute inset-0 flex items-center justify-center z-10", style: { ...textStyle, ...extractTextGradientStyle(element.font) || {}, pointerEvents: "none" }, children: interpolatedText })
          ]
        }
      ) }, element.id);
    }
    const cb = element.contentBox;
    const hasCb = !!cb && ((cb.left || 0) > 5e-3 || (cb.top || 0) > 5e-3 || (cb.right || 0) > 5e-3 || (cb.bottom || 0) > 5e-3);
    const hoverEnter = () => {
      try {
        playSound(element.hoverSoundId);
      } catch (e) {
      }
      setIsHovered(true);
    };
    return /* @__PURE__ */ jsxRuntime2.jsxs("div", { style: { ...wrapperStyle, transform }, children: [
      /* @__PURE__ */ jsxRuntime2.jsxs(
        "button",
        {
          style: { width: "100%", height: "100%", position: "relative", overflow, fontFamily: "inherit", fontSize: "inherit", lineHeight: "inherit", paddingLeft: \`\${element.paddingX ?? 0}%\`, paddingRight: \`\${element.paddingX ?? 0}%\`, boxSizing: "border-box", ...hasCb ? { pointerEvents: "none" } : {} },
          className: \`transition-transform transform hover:scale-105 flex items-center \${{ left: "justify-start", center: "justify-center", right: "justify-end" }[((_a = element.font) == null ? void 0 : _a.align) || "center"]}\`,
          onMouseEnter: hasCb ? void 0 : hoverEnter,
          onMouseLeave: hasCb ? void 0 : () => setIsHovered(false),
          onClick: hasCb ? void 0 : handleClick,
          children: [
            displayUrl ? /* @__PURE__ */ jsxRuntime2.jsx("img", { src: displayUrl, alt: element.text, className: "absolute inset-0 w-full h-full object-fill", style: { borderRadius: btnRadius } }) : /* @__PURE__ */ jsxRuntime2.jsx(
              "div",
              {
                className: "absolute inset-0 w-full h-full rounded",
                style: { backgroundColor: isHovered && hoverBg ? hoverBg : buttonBg, borderRadius: btnRadius }
              }
            ),
            /* @__PURE__ */ jsxRuntime2.jsx("span", { className: "relative z-10", style: { ...textStyle, ...extractTextGradientStyle(element.font) || {}, display: "inline-block", pointerEvents: "none" }, children: interpolatedText })
          ]
        }
      ),
      hasCb && interactive && cb && /* @__PURE__ */ jsxRuntime2.jsx(
        "div",
        {
          onMouseEnter: hoverEnter,
          onMouseLeave: () => setIsHovered(false),
          onClick: handleClick,
          style: { position: "absolute", left: \`\${cb.left * 100}%\`, top: \`\${cb.top * 100}%\`, right: \`\${cb.right * 100}%\`, bottom: \`\${cb.bottom * 100}%\`, cursor: "pointer", zIndex: 11 }
        }
      )
    ] }, element.id);
  };
  const AssetCyclerElement = ({ element, style, variables, onVariableChange, project }) => {
    var _a, _b, _c, _d, _e, _f, _g, _h, _i, _j;
    const el = element;
    const character = project.characters[el.characterId];
    const layer = character == null ? void 0 : character.layers[el.layerId];
    let currentAssetId = String(variables[el.variableId] || "");
    runtimeDebugLog(\`[AssetCycler] Rendering cycler for variable \${el.variableId}, current value:\`, currentAssetId);
    let filteredAssetIds = el.assetIds;
    if (el.assetConditions && el.assetConditions.length > 0) {
      runtimeDebugLog(\`[AssetCycler] Using assetConditions filtering for \${el.variableId}\`);
      filteredAssetIds = el.assetConditions.filter((condition) => {
        const allConditionsMet = condition.conditions.every((cond) => {
          const currentVarValue = String(variables[cond.variableId] || "");
          const conditionMet = currentVarValue === cond.value;
          runtimeDebugLog(\`[AssetCycler] Condition check: var \${cond.variableId} = "\${currentVarValue}" === "\${cond.value}" ? \${conditionMet}\`);
          return conditionMet;
        });
        if (allConditionsMet) {
          runtimeDebugLog(\`[AssetCycler] ✓ All conditions met for asset \${condition.assetId}\`);
        }
        return allConditionsMet;
      }).map((condition) => condition.assetId);
      if (filteredAssetIds.length === 0) {
        const conditionVars = new Set(el.assetConditions.flatMap((c) => c.conditions.map((cond) => cond.variableId)));
        const anyVarsSet = Array.from(conditionVars).some((varId) => variables[varId]);
        if (!anyVarsSet) {
          filteredAssetIds = el.assetIds;
          runtimeDebugLog(\`[AssetCycler] No condition variables set yet, showing all \${filteredAssetIds.length} assets\`);
        } else {
          runtimeDebugLog(\`[AssetCycler] Conditions set but no matches, filtered to 0 assets\`);
        }
      }
      runtimeDebugLog(\`[AssetCycler] Condition-filtered assets (\${filteredAssetIds.length}):\`, filteredAssetIds);
    } else if (el.filterPattern) {
      const filterVarIds = el.filterVariableIds || (el.filterVariableId ? [el.filterVariableId] : []);
      if (filterVarIds.length > 0) {
        runtimeDebugLog(\`[AssetCycler] Filter variables for \${el.variableId}:\`, filterVarIds);
        const filterValues = {};
        let allFiltersHaveValues = true;
        for (const varId of filterVarIds) {
          const assetId = String(variables[varId] || "");
          if (!assetId) {
            allFiltersHaveValues = false;
            break;
          }
          const asset = layer == null ? void 0 : layer.assets[assetId];
          const assetName = (asset == null ? void 0 : asset.name) || assetId;
          runtimeDebugLog(\`[AssetCycler] Filter var \${varId}: assetId=\${assetId}, assetName=\${assetName}\`);
          filterValues[varId] = assetName;
        }
        if (allFiltersHaveValues) {
          let pattern = el.filterPattern;
          const extractPart = (assetName, index) => {
            const parts = assetName.split("_");
            if (index >= 0 && index < parts.length) {
              return parts[index];
            }
            return assetName;
          };
          for (const varId of filterVarIds) {
            const assetName = filterValues[varId];
            const indexedRegex = new RegExp(\`\\\\{\${varId}\\\\[(\\\\d+)\\\\]\\\\}\`, "g");
            pattern = pattern.replace(indexedRegex, (match, indexStr) => {
              const index = parseInt(indexStr, 10);
              const part = extractPart(assetName, index);
              runtimeDebugLog(\`[AssetCycler] Extracting part \${index} from \${assetName}: \${part}\`);
              return part;
            });
            const specificRegex = new RegExp(\`\\\\{\${varId}\\\\}\`, "g");
            pattern = pattern.replace(specificRegex, assetName);
          }
          const remainingPlaceholders = pattern.match(/\\{[^}]*\\}/g);
          if (remainingPlaceholders) {
            for (let i = 0; i < Math.min(remainingPlaceholders.length, filterVarIds.length); i++) {
              const varId = filterVarIds[i];
              const assetName = filterValues[varId];
              const indexMatch = remainingPlaceholders[i].match(/\\[(\\d+)\\]/);
              if (indexMatch) {
                const index = parseInt(indexMatch[1], 10);
                const part = extractPart(assetName, index);
                runtimeDebugLog(\`[AssetCycler] Generic placeholder [\${index}] extracting from \${assetName}: \${part}\`);
                pattern = pattern.replace(/\\{[^}]*\\}/, part);
              } else {
                pattern = pattern.replace(/\\{[^}]*\\}/, assetName);
              }
            }
          }
          runtimeDebugLog(\`[AssetCycler] Filtering with resolved pattern: \${pattern}\`);
          filteredAssetIds = el.assetIds.filter((assetId) => {
            const asset = layer == null ? void 0 : layer.assets[assetId];
            if (!asset) return false;
            const matches = asset.name.toLowerCase().includes(pattern.toLowerCase());
            if (matches) {
              runtimeDebugLog(\`[AssetCycler] ✓ Match: \${asset.name} contains \${pattern}\`);
            }
            return matches;
          });
          runtimeDebugLog(\`[AssetCycler] Filtered assets (\${filteredAssetIds.length}):\`, filteredAssetIds);
        } else {
          runtimeDebugLog(\`[AssetCycler] Not all filter variables have values yet, showing all \${filteredAssetIds.length} assets\`);
        }
      }
    }
    React2.useEffect(() => {
      if (!currentAssetId && filteredAssetIds.length > 0 && onVariableChange) {
        const firstAsset = filteredAssetIds[0];
        runtimeDebugLog(\`[AssetCycler] Initializing variable \${el.variableId} to:\`, firstAsset);
        onVariableChange(el.variableId, firstAsset);
      }
    }, [currentAssetId, filteredAssetIds.length > 0 ? filteredAssetIds[0] : null, el.variableId, onVariableChange]);
    React2.useEffect(() => {
      const hasConditionFiltering = el.assetConditions && el.assetConditions.length > 0;
      const hasPatternFiltering = el.filterVariableIds && el.filterVariableIds.length > 0;
      if ((hasConditionFiltering || hasPatternFiltering) && filteredAssetIds.length > 0 && onVariableChange) {
        if (!filteredAssetIds.includes(currentAssetId)) {
          const firstFiltered = filteredAssetIds[0];
          runtimeDebugLog(\`[AssetCycler] Filter changed - updating variable \${el.variableId} to first match:\`, firstFiltered);
          onVariableChange(el.variableId, firstFiltered);
        }
      }
    }, [filteredAssetIds.join(","), el.assetConditions, el.filterVariableIds, el.variableId, currentAssetId, onVariableChange]);
    const currentIndex = filteredAssetIds.indexOf(currentAssetId);
    const currentAsset = currentAssetId && layer ? layer.assets[currentAssetId] : null;
    const handlePrevious = () => {
      if (filteredAssetIds.length === 0) return;
      const newIndex = currentIndex <= 0 ? filteredAssetIds.length - 1 : currentIndex - 1;
      runtimeDebugLog(\`[AssetCycler] Previous: setting variable \${el.variableId} to:\`, filteredAssetIds[newIndex]);
      onVariableChange == null ? void 0 : onVariableChange(el.variableId, filteredAssetIds[newIndex]);
    };
    const handleNext = () => {
      if (filteredAssetIds.length === 0) return;
      const newIndex = currentIndex >= filteredAssetIds.length - 1 ? 0 : currentIndex + 1;
      runtimeDebugLog(\`[AssetCycler] Next: setting variable \${el.variableId} to:\`, filteredAssetIds[newIndex]);
      onVariableChange == null ? void 0 : onVariableChange(el.variableId, filteredAssetIds[newIndex]);
    };
    return /* @__PURE__ */ jsxRuntime2.jsxs(
      "div",
      {
        style: {
          ...style,
          backgroundColor: el.backgroundColor || "rgba(30, 41, 59, 0.8)",
          borderRadius: "8px",
          padding: "8px",
          display: "flex",
          flexDirection: "column",
          gap: "4px",
          alignItems: "center",
          justifyContent: "center",
          opacity: el.visible === false ? 0 : 1,
          pointerEvents: el.visible === false ? "none" : "auto"
        },
        children: [
          el.label && /* @__PURE__ */ jsxRuntime2.jsx(
            "div",
            {
              style: {
                fontSize: \`calc(var(--font-scale, 1) * \${(((_a = el.font) == null ? void 0 : _a.size) || 16) * 0.8}px)\`,
                fontFamily: ((_b = el.font) == null ? void 0 : _b.family) || "Inter, system-ui, sans-serif",
                fontWeight: ((_c = el.font) == null ? void 0 : _c.weight) || "normal",
                fontStyle: ((_d = el.font) == null ? void 0 : _d.italic) ? "italic" : "normal",
                color: ((_e = el.font) == null ? void 0 : _e.color) || "#f1f5f9",
                opacity: 0.8,
                textAlign: "center"
              },
              children: el.label
            }
          ),
          /* @__PURE__ */ jsxRuntime2.jsxs("div", { style: { display: "flex", alignItems: "center", gap: "12px", width: "100%" }, children: [
            /* @__PURE__ */ jsxRuntime2.jsx(
              "button",
              {
                onClick: handlePrevious,
                style: {
                  background: "none",
                  border: "none",
                  color: el.arrowColor || "#a855f7",
                  fontSize: \`calc(var(--font-scale, 1) * \${el.arrowSize || 24}px)\`,
                  cursor: "pointer",
                  padding: "4px",
                  lineHeight: 1,
                  opacity: filteredAssetIds.length > 0 ? 1 : 0.3,
                  transition: "opacity 0.2s"
                },
                disabled: filteredAssetIds.length === 0,
                children: "◀"
              }
            ),
            /* @__PURE__ */ jsxRuntime2.jsx(
              "div",
              {
                style: {
                  flex: 1,
                  fontSize: \`calc(var(--font-scale, 1) * \${((_f = el.font) == null ? void 0 : _f.size) || 16}px)\`,
                  fontFamily: ((_g = el.font) == null ? void 0 : _g.family) || "Inter, system-ui, sans-serif",
                  fontWeight: ((_h = el.font) == null ? void 0 : _h.weight) || "normal",
                  fontStyle: ((_i = el.font) == null ? void 0 : _i.italic) ? "italic" : "normal",
                  color: ((_j = el.font) == null ? void 0 : _j.color) || "#f1f5f9",
                  textAlign: "center",
                  overflow: "hidden",
                  textOverflow: "ellipsis",
                  whiteSpace: "nowrap"
                },
                children: el.showAssetName && currentAsset ? currentAsset.name : currentIndex >= 0 ? \`\${currentIndex + 1} / \${filteredAssetIds.length}\` : "–"
              }
            ),
            /* @__PURE__ */ jsxRuntime2.jsx(
              "button",
              {
                onClick: handleNext,
                style: {
                  background: "none",
                  border: "none",
                  color: el.arrowColor || "#a855f7",
                  fontSize: \`calc(var(--font-scale, 1) * \${el.arrowSize || 24}px)\`,
                  cursor: "pointer",
                  padding: "4px",
                  lineHeight: 1,
                  opacity: filteredAssetIds.length > 0 ? 1 : 0.3,
                  transition: "opacity 0.2s"
                },
                disabled: filteredAssetIds.length === 0,
                children: "▶"
              }
            )
          ] })
        ]
      },
      el.id
    );
  };
  const CGGalleryGridElement = ({ element, entries, variables, project, assetResolver, viewerPortalRef }) => {
    var _a;
    const [viewingEntry, setViewingEntry] = React2.useState(null);
    const [viewerIndex, setViewerIndex] = React2.useState(0);
    const isEntryUnlocked = (entry) => {
      if (!entry.unlockable) return true;
      if (!entry.unlockVariableId) return true;
      const val = variables[entry.unlockVariableId];
      return val === true || val === "true" || val === 1;
    };
    const unlockedEntries = entries.filter((e) => isEntryUnlocked(e));
    const handleThumbnailClick = (entry, index) => {
      if (!isEntryUnlocked(entry)) return;
      setViewingEntry(entry);
      setViewerIndex(unlockedEntries.indexOf(entry));
    };
    const navigateViewer = (dir) => {
      const newIndex = (viewerIndex + dir + unlockedEntries.length) % unlockedEntries.length;
      setViewerIndex(newIndex);
      setViewingEntry(unlockedEntries[newIndex]);
    };
    if (viewingEntry) {
      const viewUrl = assetResolver(viewingEntry.assetId, "image");
      const viewer = /* @__PURE__ */ jsxRuntime2.jsxs(
        "div",
        {
          className: "absolute inset-0 z-[9999] flex items-center justify-center",
          style: { backgroundColor: element.backgroundColor || "rgba(0,0,0,0.95)", pointerEvents: "auto" },
          onClick: () => setViewingEntry(null),
          children: [
            viewUrl && /* @__PURE__ */ jsxRuntime2.jsx(
              "img",
              {
                src: viewUrl,
                alt: viewingEntry.name,
                className: "max-w-[90%] max-h-[85%] object-contain",
                onClick: (e) => e.stopPropagation()
              }
            ),
            /* @__PURE__ */ jsxRuntime2.jsx(
              "button",
              {
                "aria-label": "Close",
                className: "absolute top-16 right-4 w-10 h-10 flex items-center justify-center rounded-full bg-black/55 hover:bg-black/85 text-white text-xl leading-none",
                onClick: (e) => {
                  e.stopPropagation();
                  setViewingEntry(null);
                },
                children: "✕"
              }
            ),
            unlockedEntries.length > 1 && /* @__PURE__ */ jsxRuntime2.jsxs(jsxRuntime2.Fragment, { children: [
              /* @__PURE__ */ jsxRuntime2.jsx(
                "button",
                {
                  className: "absolute left-4 top-1/2 -translate-y-1/2 text-white/60 hover:text-white text-4xl p-2",
                  onClick: (e) => {
                    e.stopPropagation();
                    navigateViewer(-1);
                  },
                  children: "◀"
                }
              ),
              /* @__PURE__ */ jsxRuntime2.jsx(
                "button",
                {
                  className: "absolute right-4 top-1/2 -translate-y-1/2 text-white/60 hover:text-white text-4xl p-2",
                  onClick: (e) => {
                    e.stopPropagation();
                    navigateViewer(1);
                  },
                  children: "▶"
                }
              )
            ] }),
            /* @__PURE__ */ jsxRuntime2.jsxs("div", { className: "absolute bottom-4 left-0 right-0 text-center", children: [
              element.showNames !== false && /* @__PURE__ */ jsxRuntime2.jsx("div", { className: "text-white text-sm mb-1", children: viewingEntry.name }),
              /* @__PURE__ */ jsxRuntime2.jsx("div", { className: "text-white/40 text-xs", children: "Click anywhere (or ✕) to close" })
            ] }),
            /* @__PURE__ */ jsxRuntime2.jsxs("div", { className: "absolute top-16 left-4 text-white/50 text-sm", children: [
              viewerIndex + 1,
              " / ",
              unlockedEntries.length
            ] })
          ]
        }
      );
      const portalTarget = viewerPortalRef == null ? void 0 : viewerPortalRef.current;
      return portalTarget ? reactDom.createPortal(viewer, portalTarget) : viewer;
    }
    const lockedPlaceholderUrl = ((_a = project.cgGallery) == null ? void 0 : _a.lockedPlaceholderAssetId) ? assetResolver(project.cgGallery.lockedPlaceholderAssetId, "image") : null;
    const renderThumb = (entry, idx, free) => {
      const unlocked = isEntryUnlocked(entry);
      const thumbAssetId = entry.thumbnailAssetId || entry.assetId;
      const thumbUrl = unlocked ? assetResolver(thumbAssetId, "image") : lockedPlaceholderUrl;
      return /* @__PURE__ */ jsxRuntime2.jsxs(
        "div",
        {
          className: "relative overflow-hidden flex items-center justify-center",
          style: {
            ...free ? { width: "100%", height: "100%" } : { aspectRatio: "16/9" },
            borderRadius: \`\${element.thumbnailBorderRadius || 8}px\`,
            border: \`2px solid \${element.thumbnailBorderColor || "#4D3273"}\`,
            backgroundColor: unlocked ? "#334155" : element.lockedColor || "#1e293b",
            cursor: unlocked ? "pointer" : "default",
            transition: "transform 0.15s ease, border-color 0.15s ease"
          },
          onClick: () => handleThumbnailClick(entry),
          onMouseEnter: (e) => {
            if (unlocked) {
              e.currentTarget.style.transform = "scale(1.05)";
              e.currentTarget.style.borderColor = "#8b5cf6";
            }
          },
          onMouseLeave: (e) => {
            e.currentTarget.style.transform = "scale(1)";
            e.currentTarget.style.borderColor = element.thumbnailBorderColor || "#4D3273";
          },
          children: [
            unlocked && thumbUrl ? /* @__PURE__ */ jsxRuntime2.jsx("img", { src: thumbUrl, alt: entry.name, className: "w-full h-full object-cover" }) : !unlocked ? /* @__PURE__ */ jsxRuntime2.jsx("span", { className: "text-2xl", children: element.lockedText || "🔒" }) : /* @__PURE__ */ jsxRuntime2.jsx("span", { className: "text-xs text-slate-500", children: entry.name }),
            element.showNames !== false && unlocked && /* @__PURE__ */ jsxRuntime2.jsx("div", { className: "absolute bottom-0 left-0 right-0 bg-black/60 text-white text-[10px] text-center py-0.5 truncate px-1", children: entry.name })
          ]
        },
        entry.id
      );
    };
    if (element.slotLayout === "free" && element.slotRects && element.slotRects.length > 0) {
      const rects = element.slotRects;
      return /* @__PURE__ */ jsxRuntime2.jsx("div", { style: { position: "absolute", left: 0, top: 0, width: "100%", height: "100%", pointerEvents: "none" }, children: entries.map((entry, idx) => {
        const rect = rects[idx];
        if (!rect) return null;
        return /* @__PURE__ */ jsxRuntime2.jsx(
          "div",
          {
            style: { position: "absolute", left: \`\${rect.x}%\`, top: \`\${rect.y}%\`, width: \`\${rect.width}%\`, height: \`\${rect.height}%\`, pointerEvents: "auto" },
            children: renderThumb(entry, idx, true)
          },
          entry.id
        );
      }) });
    }
    return /* @__PURE__ */ jsxRuntime2.jsx(
      "div",
      {
        className: "w-full h-full overflow-y-auto p-2 rounded",
        style: { backgroundColor: element.hideBackgroundPanel ? "transparent" : element.backgroundColor || "rgba(15, 23, 42, 0.9)" },
        children: /* @__PURE__ */ jsxRuntime2.jsx(
          "div",
          {
            className: "grid",
            style: {
              gridTemplateColumns: \`repeat(\${element.columns || 4}, 1fr)\`,
              gap: \`\${element.gap || 8}px\`
            },
            children: entries.map((entry, idx) => renderThumb(entry, idx, false))
          }
        )
      }
    );
  };
  const InventoryGridElement = ({ element, items, variables, project, assetResolver, onAction, onCommitVariables, inventorySlots, onReorderSlots, selectedItemId, selectedElementId, onSelectItem, groupByCategory, categoryOrder, autoSort, style }) => {
    var _a;
    const count = (it) => Number(variables[it.countVariableId] ?? 0);
    const filteredAll = element.categoryFilter ? items.filter((it) => it.category === element.categoryFilter) : items;
    const shown = filteredAll.filter((it) => count(it) >= 1 || element.hideUnowned === false && !it.hideWhenEmpty);
    const itemById = new Map(shown.map((it) => [it.id, it]));
    const cols = element.columns || 4;
    const colGap = element.columnGap ?? element.gap ?? 8;
    const rowGap = element.rowGap ?? element.gap ?? 8;
    const gridContainerRef = React2.useRef(null);
    const [autoRows, setAutoRows] = React2.useState(0);
    React2.useEffect(() => {
      if (element.rows && element.rows > 0) {
        setAutoRows(0);
        return;
      }
      const node = gridContainerRef.current;
      if (!node) return;
      const compute = () => {
        const cs = window.getComputedStyle(node);
        const availW = node.clientWidth - parseFloat(cs.paddingLeft || "0") - parseFloat(cs.paddingRight || "0");
        const availH = node.clientHeight - parseFloat(cs.paddingTop || "0") - parseFloat(cs.paddingBottom || "0");
        if (availW <= 0 || availH <= 0) return;
        const slotW = (availW - colGap * (cols - 1)) / cols;
        if (slotW <= 0) return;
        setAutoRows(Math.max(1, Math.floor((availH + rowGap) / (slotW + rowGap))));
      };
      compute();
      const ro = new ResizeObserver(compute);
      ro.observe(node);
      return () => ro.disconnect();
    }, [element.rows, cols, colGap, rowGap]);
    const minSlots = element.rows && element.rows > 0 ? cols * element.rows : cols * autoRows;
    const totalSlots = Math.max(shown.length, minSlots);
    const slots = [];
    if (autoSort) {
      for (let i = 0; i < shown.length; i++) slots.push(shown[i].id);
      for (let i = shown.length; i < totalSlots; i++) slots.push(null);
    } else {
      const placed = /* @__PURE__ */ new Set();
      for (let i = 0; i < totalSlots; i++) {
        const saved = (inventorySlots == null ? void 0 : inventorySlots[i]) ?? null;
        if (saved && itemById.has(saved) && !placed.has(saved)) {
          slots.push(saved);
          placed.add(saved);
        } else slots.push(null);
      }
      const unplaced = shown.filter((it) => !placed.has(it.id));
      let u = 0;
      for (let i = 0; i < slots.length && u < unplaced.length; i++) {
        if (!slots[i]) {
          slots[i] = unplaced[u].id;
          placed.add(unplaced[u].id);
          u++;
        }
      }
      while (u < unplaced.length) {
        slots.push(unplaced[u].id);
        placed.add(unplaced[u].id);
        u++;
      }
    }
    const reorderEnabled = element.allowReorder !== false && !!onReorderSlots && !autoSort;
    const selectEnabled = !!onSelectItem;
    const [dragSlot, setDragSlot] = React2.useState(null);
    const [hoverUseId, setHoverUseId] = React2.useState(null);
    const suppressClickRef = React2.useRef(false);
    const swap = (a, b) => {
      if (!onReorderSlots || a == null || a === b) {
        setDragSlot(null);
        return;
      }
      const next = [...slots];
      const tmp = next[a];
      next[a] = next[b];
      next[b] = tmp;
      onReorderSlots(next);
      suppressClickRef.current = true;
      setDragSlot(null);
    };
    const isVarMutation = (a) => a.type === UIActionType.SetVariable || a.type === UIActionType.ResetVariable || a.type === UIActionType.GiveItem || a.type === UIActionType.UseItem || a.type === UIActionType.DestroyItem || a.type === UIActionType.UseSelectedItem || a.type === UIActionType.RestockCollection || a.type === UIActionType.BuyItem || a.type === UIActionType.SellItem || a.type === UIActionType.BuySelectedItem || a.type === UIActionType.SellSelectedItem;
    const runActionList = (acts) => {
      const setVarActions = acts.filter(isVarMutation);
      const otherActions = acts.filter((a) => !isVarMutation(a));
      setVarActions.forEach((a) => onAction(a));
      if (setVarActions.length > 0 && onCommitVariables) onCommitVariables();
      otherActions.forEach((a) => onAction(a));
    };
    const slotButtonActionsFor = (it) => {
      var _a2;
      return ((_a2 = it.slotButtonActions) == null ? void 0 : _a2.length) ? it.slotButtonActions : element.slotButtonActions || [];
    };
    const useItem = (it) => {
      if (!it.usable) return;
      if (it.carryToUse) {
        onAction({ type: UIActionType.CarryItem, itemId: it.id });
        return;
      }
      runActionList([
        ...it.consumeOnUse === false ? [] : [{ type: UIActionType.SetVariable, variableId: it.countVariableId, operator: "subtract", value: 1 }],
        ...it.useEffect || [],
        ...slotButtonActionsFor(it)
      ]);
    };
    const slotButtonMode = element.slotButton ?? "none";
    const tradeCollectionId = slotButtonMode === "buy" ? element.collectionId : slotButtonMode === "sell" ? element.sellToCollectionId : void 0;
    const tradeCollection = tradeCollectionId ? (_a = project.itemCollections) == null ? void 0 : _a[tradeCollectionId] : void 0;
    const tradeItem = (it) => {
      if (!tradeCollectionId) return;
      onAction({ type: slotButtonMode === "buy" ? UIActionType.BuyItem : UIActionType.SellItem, itemId: it.id, collectionId: tradeCollectionId });
      if (onCommitVariables) onCommitVariables();
      const acts = slotButtonActionsFor(it);
      if (acts.length) runActionList(acts);
    };
    const tradeBlocked = (it) => {
      if (!tradeCollection) return true;
      const res = slotButtonMode === "buy" ? computeBuy(it.id, tradeCollection, project, variables) : computeSell(it.id, tradeCollection, project, variables);
      return "blocked" in res;
    };
    const hideBox = element.hideSlotBox === true;
    const slotStyle = { ...element.squareSlots === false ? { width: "100%", height: "100%" } : { aspectRatio: "1 / 1" }, borderRadius: \`\${element.slotBorderRadius ?? 8}px\`, border: hideBox ? "none" : \`2px solid \${element.slotBorderColor || "#4D3273"}\`, background: hideBox ? "transparent" : element.slotColor || "transparent" };
    const selectedRing = element.selectedBorderColor || "#38bdf8";
    const qtyCorner = { "top-left": "top-1 left-1", "top-right": "top-1 right-1", "bottom-left": "bottom-1 left-1", "bottom-right": "bottom-1 right-1" }[element.quantityPosition || "top-right"];
    const qtyStyle = { backgroundColor: element.quantityBgColor || "rgba(0,0,0,0.7)", color: element.quantityColor || "#ffffff", ...element.quantityFont ? fontSettingsToStyle(element.quantityFont) : {} };
    const gridStyle = { gridTemplateColumns: \`repeat(\${cols}, 1fr)\`, columnGap: \`\${colGap}px\`, rowGap: \`\${rowGap}px\` };
    const renderSlot = (slotId, i) => {
      var _a2;
      const it = slotId ? itemById.get(slotId) : void 0;
      if (!it) return /* @__PURE__ */ jsxRuntime2.jsx(
        "div",
        {
          style: { ...slotStyle, touchAction: reorderEnabled ? "none" : void 0 },
          onPointerUp: reorderEnabled ? (() => {
            if (dragSlot != null) swap(dragSlot, i);
            else setDragSlot(null);
          }) : void 0
        },
        \`slot-\${i}\`
      );
      const url = ((_a2 = it.icon) == null ? void 0 : _a2.id) ? assetResolver(it.icon.id, it.icon.type === "video" ? "video" : "image") : null;
      const qty = count(it);
      const selected = selectEnabled && selectedItemId === it.id && selectedElementId === element.id;
      return /* @__PURE__ */ jsxRuntime2.jsxs(
        "div",
        {
          className: "relative flex flex-col items-center justify-center p-1",
          style: { ...slotStyle, cursor: reorderEnabled ? "grab" : selectEnabled ? "pointer" : void 0, touchAction: reorderEnabled ? "none" : void 0, opacity: dragSlot === i ? 0.4 : 1, ...selected ? { boxShadow: \`0 0 0 2px \${selectedRing} inset\`, border: \`2px solid \${selectedRing}\` } : {} },
          onPointerDown: reorderEnabled ? (() => setDragSlot(i)) : void 0,
          onPointerUp: reorderEnabled ? (() => {
            if (dragSlot != null && dragSlot !== i) swap(dragSlot, i);
            else setDragSlot(null);
          }) : void 0,
          onClick: selectEnabled ? (() => {
            if (suppressClickRef.current) {
              suppressClickRef.current = false;
              return;
            }
            onSelectItem(selected ? null : it.id, element.id);
          }) : void 0,
          children: [
            url ? /* @__PURE__ */ jsxRuntime2.jsx("img", { src: url, alt: it.name, className: "w-full flex-1 min-h-0 object-contain", draggable: false }) : /* @__PURE__ */ jsxRuntime2.jsx("div", { className: "w-full flex-1 min-h-0" }),
            element.showQuantity !== false && qty > 1 && /* @__PURE__ */ jsxRuntime2.jsxs("span", { className: \`absolute \${qtyCorner} text-[10px] rounded px-1 leading-tight\`, style: qtyStyle, children: [
              "×",
              qty
            ] }),
            element.showNames !== false && /* @__PURE__ */ jsxRuntime2.jsx("span", { className: "text-[10px] text-white truncate w-full mt-0.5", style: { ...element.nameFont ? fontSettingsToStyle(element.nameFont) : {}, textAlign: "center" }, children: it.name }),
            (() => {
              var _a3, _b;
              const showBtn = slotButtonMode === "use" ? !!it.usable : slotButtonMode === "buy" || slotButtonMode === "sell" ? !!tradeCollection : false;
              if (!showBtn) return null;
              const blocked = (slotButtonMode === "buy" || slotButtonMode === "sell") && tradeBlocked(it);
              const hovered = hoverUseId === it.id && !blocked;
              const baseArt = ((_a3 = element.useButtonImage) == null ? void 0 : _a3.id) ? assetResolver(element.useButtonImage.id, element.useButtonImage.type === "video" ? "video" : "image") : null;
              const hoverArt = ((_b = element.useButtonHoverImage) == null ? void 0 : _b.id) ? assetResolver(element.useButtonHoverImage.id, element.useButtonHoverImage.type === "video" ? "video" : "image") : null;
              const art = hovered && hoverArt ? hoverArt : baseArt;
              const bg = art ? void 0 : hovered ? element.useButtonHoverColor || element.useButtonColor || "#0ea5e9" : element.useButtonColor || "#0ea5e9";
              const defaultLabel = slotButtonMode === "buy" ? "Buy" : slotButtonMode === "sell" ? "Sell" : "Use";
              let label = element.useButtonText || defaultLabel;
              if (!element.useButtonText && (slotButtonMode === "buy" || slotButtonMode === "sell") && tradeCollection) {
                const entry = tradeCollection.entries.find((e) => e.itemId === it.id);
                const price = tradePrice(it, entry);
                if (price > 0) label = \`\${defaultLabel} \${price}\`;
              }
              const onClick = (e) => {
                e.stopPropagation();
                if (blocked) return;
                slotButtonMode === "use" ? useItem(it) : tradeItem(it);
              };
              return /* @__PURE__ */ jsxRuntime2.jsx(
                "button",
                {
                  onClick,
                  disabled: blocked,
                  onMouseEnter: () => setHoverUseId(it.id),
                  onMouseLeave: () => setHoverUseId(null),
                  className: "mt-0.5 px-1.5 py-0.5 relative overflow-hidden leading-tight",
                  style: {
                    borderRadius: \`\${element.useButtonRadius ?? 6}px\`,
                    background: art ? \`center / cover no-repeat url(\${art})\` : bg,
                    color: element.useButtonTextColor || "#ffffff",
                    fontSize: "9px",
                    opacity: blocked ? 0.4 : 1,
                    cursor: blocked ? "not-allowed" : "pointer",
                    ...element.useButtonFont ? fontSettingsToStyle(element.useButtonFont) : {}
                  },
                  children: label
                }
              );
            })()
          ]
        },
        \`slot-\${i}\`
      );
    };
    const groupedSections = (() => {
      if (!groupByCategory) return null;
      const order = categoryOrder || [];
      const rank = (c) => {
        const idx = order.indexOf(c);
        return idx >= 0 ? idx : order.length;
      };
      const occ = slots.map((s, i) => ({ s, i })).filter((x) => !!x.s);
      const cats = Array.from(new Set(occ.map((o) => {
        var _a2;
        return ((_a2 = itemById.get(o.s)) == null ? void 0 : _a2.category) || "";
      })));
      const listed = cats.filter(Boolean).sort((a, b) => rank(a) - rank(b) || a.localeCompare(b));
      const ordered = [...listed, ...cats.includes("") ? [""] : []];
      return ordered.map((cat) => {
        const cells = occ.filter((o) => {
          var _a2;
          return (((_a2 = itemById.get(o.s)) == null ? void 0 : _a2.category) || "") === cat;
        });
        if (cells.length === 0) return null;
        return /* @__PURE__ */ jsxRuntime2.jsxs("div", { className: "mb-2", children: [
          /* @__PURE__ */ jsxRuntime2.jsx("div", { className: "text-[10px] font-semibold text-white opacity-70 mb-1 px-0.5", children: cat || "Other" }),
          /* @__PURE__ */ jsxRuntime2.jsx("div", { className: "grid", style: gridStyle, children: cells.map((c) => renderSlot(c.s, c.i)) })
        ] }, cat || "__uncat__");
      });
    })();
    if (element.slotLayout === "free" && element.slotRects && element.slotRects.length > 0) {
      const rects = element.slotRects;
      return /* @__PURE__ */ jsxRuntime2.jsx(
        "div",
        {
          style: { position: "absolute", left: 0, top: 0, width: "100%", height: "100%", zIndex: style == null ? void 0 : style.zIndex, opacity: style == null ? void 0 : style.opacity, pointerEvents: "none" },
          onPointerUp: reorderEnabled ? (() => setDragSlot(null)) : void 0,
          children: slots.map((slotId, i) => {
            const rect = rects[i];
            if (!rect) return null;
            return /* @__PURE__ */ jsxRuntime2.jsx("div", { style: { position: "absolute", left: \`\${rect.x}%\`, top: \`\${rect.y}%\`, width: \`\${rect.width}%\`, height: \`\${rect.height}%\`, pointerEvents: "auto" }, children: renderSlot(slotId, i) }, \`free-\${i}\`);
          })
        }
      );
    }
    return /* @__PURE__ */ jsxRuntime2.jsxs(
      "div",
      {
        ref: gridContainerRef,
        className: "w-full h-full overflow-y-auto p-2 rounded",
        style: { backgroundColor: element.hideBackgroundPanel ? "transparent" : element.backgroundColor || "rgba(15, 23, 42, 0.9)" },
        onPointerUp: reorderEnabled ? (() => setDragSlot(null)) : void 0,
        children: [
          totalSlots === 0 && /* @__PURE__ */ jsxRuntime2.jsx("div", { className: "w-full h-full flex items-center justify-center text-center text-xs text-white/50 px-2", children: element.emptyText || "" }),
          groupByCategory ? groupedSections : /* @__PURE__ */ jsxRuntime2.jsx("div", { className: "grid", style: gridStyle, children: slots.map((slotId, i) => renderSlot(slotId, i)) })
        ]
      }
    );
  };
  const getTransitionStyle = (transitionIn, duration, delay) => {
    const durationMs = duration || 300;
    const delayMs = delay || 0;
    if (!transitionIn || transitionIn === "none") return {};
    const transitionProp = \`opacity \${durationMs}ms ease-out \${delayMs}ms, filter \${durationMs}ms ease-out \${delayMs}ms\`;
    return {
      transition: transitionProp,
      animation: \`elementTransition\${transitionIn} \${durationMs}ms ease-out \${delayMs}ms\`
    };
  };
  const HotZoneTextInput = ({ element, variables, onVariableChange, playSound, font }) => {
    const vid = element.variableId;
    const currentVal = vid && variables[vid] != null ? String(variables[vid]) : "";
    const [localValue, setLocalValue] = React2.useState(currentVal);
    React2.useEffect(() => {
      setLocalValue(currentVal);
    }, [currentVal]);
    const commit = () => {
      if (vid && onVariableChange) onVariableChange(vid, localValue);
    };
    return /* @__PURE__ */ jsxRuntime2.jsxs("div", { className: "w-full h-full flex items-center gap-0", children: [
      /* @__PURE__ */ jsxRuntime2.jsx(
        "input",
        {
          type: "text",
          className: "flex-1 h-full rounded-l px-2 outline-none min-w-0",
          style: {
            backgroundColor: element.backgroundColor || "#1e293b",
            border: \`1px solid \${element.borderColor || "#475569"}\`,
            borderRight: "none",
            color: (font == null ? void 0 : font.color) || "#fff",
            fontFamily: font == null ? void 0 : font.fontFamily,
            fontSize: font == null ? void 0 : font.fontSize
          },
          placeholder: element.placeholder || "",
          maxLength: element.maxLength || void 0,
          value: localValue,
          onChange: (e) => setLocalValue(e.target.value),
          onKeyDown: (e) => {
            if (e.key === "Enter") {
              commit();
              try {
                playSound(element.clickSoundId || null);
              } catch (_) {
              }
            }
          },
          onClick: (e) => e.stopPropagation()
        }
      ),
      /* @__PURE__ */ jsxRuntime2.jsx(
        "button",
        {
          type: "button",
          className: "h-full px-2 rounded-r text-xs font-semibold flex items-center justify-center shrink-0",
          style: {
            backgroundColor: element.borderColor || "#475569",
            color: "#fff",
            border: \`1px solid \${element.borderColor || "#475569"}\`
          },
          onClick: (e) => {
            e.stopPropagation();
            commit();
            try {
              playSound(element.clickSoundId || null);
            } catch (_) {
            }
          },
          title: "Confirm",
          children: "✓"
        }
      )
    ] });
  };
  const HotZonedraggableImageElementRenderer = ({ el, imageUrl, assetResolver, evaluateConditions: evaluateConditions2, variables, playSound, handleLocalAction }) => {
    const [hoveredRegionId, setHoveredRegionId] = React2.useState(null);
    const hoverImageUrl = el.hoverImageId ? assetResolver(el.hoverImageId, "image") : null;
    const regions = el.draggableImageElementRegions || [];
    const hoveredRegion = hoveredRegionId ? regions.find((r) => r.id === hoveredRegionId) : null;
    let hoverClipPath;
    if (hoveredRegion) {
      if (hoveredRegion.shape === "rect" && hoveredRegion.coords.length >= 4) {
        const [x, y, w, h] = hoveredRegion.coords;
        hoverClipPath = \`inset(\${y}% \${100 - x - w}% \${100 - y - h}% \${x}%)\`;
      } else if (hoveredRegion.shape === "circle" && hoveredRegion.coords.length >= 3) {
        const [cx, cy, r] = hoveredRegion.coords;
        hoverClipPath = \`circle(\${r}% at \${cx}% \${cy}%)\`;
      } else if (hoveredRegion.shape === "poly" && hoveredRegion.coords.length >= 6) {
        const points = [];
        for (let i = 0; i < hoveredRegion.coords.length; i += 2) {
          points.push(\`\${hoveredRegion.coords[i]}% \${hoveredRegion.coords[i + 1]}%\`);
        }
        hoverClipPath = \`polygon(\${points.join(", ")})\`;
      }
    }
    return /* @__PURE__ */ jsxRuntime2.jsxs("div", { className: "w-full h-full relative pointer-events-none", children: [
      imageUrl && /* @__PURE__ */ jsxRuntime2.jsx("img", { src: imageUrl, alt: el.name, className: "w-full h-full object-contain", draggable: false }),
      hoverImageUrl && hoverClipPath && /* @__PURE__ */ jsxRuntime2.jsx(
        "img",
        {
          src: hoverImageUrl,
          alt: "",
          style: {
            position: "absolute",
            left: 0,
            top: 0,
            width: "100%",
            height: "100%",
            objectFit: "contain",
            pointerEvents: "none",
            clipPath: hoverClipPath,
            zIndex: 1
          },
          draggable: false
        }
      ),
      regions.map((region) => {
        if (region.conditions && region.conditions.length > 0 && !evaluateConditions2(region.conditions, variables)) return null;
        const regionStyle = {
          position: "absolute",
          cursor: region.cursor || "pointer",
          pointerEvents: "auto",
          zIndex: 2
        };
        if (region.shape === "rect") {
          regionStyle.left = \`\${region.coords[0]}%\`;
          regionStyle.top = \`\${region.coords[1]}%\`;
          regionStyle.width = \`\${region.coords[2]}%\`;
          regionStyle.height = \`\${region.coords[3]}%\`;
        } else if (region.shape === "circle") {
          const r = region.coords[2];
          regionStyle.left = \`\${region.coords[0] - r}%\`;
          regionStyle.top = \`\${region.coords[1] - r}%\`;
          regionStyle.width = \`\${r * 2}%\`;
          regionStyle.height = \`\${r * 2}%\`;
          regionStyle.borderRadius = "50%";
        }
        return /* @__PURE__ */ jsxRuntime2.jsx(
          "div",
          {
            style: regionStyle,
            title: region.tooltip || region.name,
            className: !hoverImageUrl ? "hover:bg-white/10 transition-colors" : "transition-colors",
            onClick: (e) => {
              e.stopPropagation();
              try {
                playSound(el.clickSoundId || null);
              } catch {
              }
              (region.actions || []).forEach((action) => handleLocalAction(action));
            },
            onMouseEnter: () => setHoveredRegionId(region.id),
            onMouseLeave: () => setHoveredRegionId(null)
          },
          region.id
        );
      })
    ] });
  };
  const InteractiveRuntime = ({ screen, onAction, variables, onVariableChange, evaluateConditions: evaluateConditions2, assetResolver, playSound }) => {
    const { project } = useProject();
    const hotSpots = React2.useMemo(() => deriveHotSpotsFromScreen(screen), [screen]);
    const interactiveElements = React2.useMemo(() => deriveInteractiveElementsFromScreen(screen), [screen]);
    const [elementPositions, setElementPositions] = React2.useState({});
    const [dragState, setDragState] = React2.useState(null);
    const [dragOffset, setDragOffset] = React2.useState(null);
    const containerRef = React2.useRef(null);
    const [containerSize, setContainerSize] = React2.useState({ width: 1, height: 1 });
    const [placedElements, setPlacedElements] = React2.useState({});
    const [imageOverrides, setImageOverrides] = React2.useState({});
    const [activeAnimations, setActiveAnimations] = React2.useState({});
    const [persistentEffects, setPersistentEffects] = React2.useState({});
    const handleLocalAction = React2.useCallback((action) => {
      if (action.type === UIActionType.ChangeImage) {
        const a = action;
        if (a.targetElementId && a.newImageId) {
          setImageOverrides((prev) => ({ ...prev, [a.targetElementId]: a.newImageId }));
        }
        return;
      }
      if (action.type === UIActionType.PlayAnimation) {
        const a = action;
        if (a.targetElementId) {
          const anim = a.animation || "shake";
          const dur = a.duration || 500;
          if (anim === "fadeIn") {
            setPersistentEffects((prev) => {
              if (!prev[a.targetElementId]) return prev;
              const next = { ...prev };
              delete next[a.targetElementId];
              return next;
            });
          }
          setActiveAnimations((prev) => ({ ...prev, [a.targetElementId]: { animation: anim, duration: dur } }));
          setTimeout(() => {
            setActiveAnimations((prev) => {
              const next = { ...prev };
              delete next[a.targetElementId];
              return next;
            });
            if (anim === "fadeOut") {
              setPersistentEffects((prev) => ({ ...prev, [a.targetElementId]: "fadedOut" }));
            }
          }, dur);
        }
        return;
      }
      onAction(action);
    }, [onAction]);
    React2.useEffect(() => {
      if (!containerRef.current) return;
      const observer = new ResizeObserver((entries) => {
        for (const entry of entries) {
          setContainerSize({ width: entry.contentRect.width, height: entry.contentRect.height });
        }
      });
      observer.observe(containerRef.current);
      return () => observer.disconnect();
    }, []);
    React2.useEffect(() => {
      if (!screen.winCondition) return;
      const wc = screen.winCondition;
      if (wc.type === "allPlaced") {
        const draggableElements = Object.values(interactiveElements).filter((el) => el.draggable);
        const allPlaced = draggableElements.length > 0 && draggableElements.every((el) => placedElements[el.id]);
        if (allPlaced) {
          wc.actions.forEach((action) => handleLocalAction(action));
        }
      } else if (wc.type === "variable" && wc.variableId && wc.operator && wc.value !== void 0) {
        const met = evaluateConditions2([{ variableId: wc.variableId, operator: wc.operator, value: wc.value }], variables);
        if (met) {
          wc.actions.forEach((action) => handleLocalAction(action));
        }
      }
    }, [placedElements, variables, screen.winCondition, interactiveElements, handleLocalAction, evaluateConditions2]);
    React2.useEffect(() => {
      const unregs = [];
      for (const spot of Object.values(hotSpots)) {
        if (spot.trigger !== "drag-drop") continue;
        if (spot.conditions && spot.conditions.length > 0 && !evaluateConditions2(spot.conditions, variables)) continue;
        unregs.push(registerDropTarget({
          id: \`screen-\${screen.id}-\${spot.id}\`,
          rectPct: { x: spot.x, y: spot.y, width: spot.width, height: spot.height },
          acceptedElementIds: spot.acceptedElementIds,
          acceptTag: spot.acceptTag || void 0,
          onDrop: () => {
            spot.actions.forEach((a) => handleLocalAction(a));
          }
        }));
      }
      return () => unregs.forEach((u) => u());
    }, [hotSpots, screen.id, variables, evaluateConditions2, handleLocalAction]);
    const handleElementMouseDown = React2.useCallback((e, el) => {
      if (!el.draggable) {
        try {
          playSound(el.clickSoundId || null);
        } catch (e2) {
        }
        if (el.actions) el.actions.forEach((a) => handleLocalAction(a));
        return;
      }
      e.preventDefault();
      e.stopPropagation();
      try {
        playSound(el.clickSoundId || null);
      } catch (e2) {
      }
      const pos = elementPositions[el.id] || { x: el.x, y: el.y };
      setDragState({
        elementId: el.id,
        startMouseX: e.clientX,
        startMouseY: e.clientY,
        startX: pos.x,
        startY: pos.y
      });
      setDragOffset(null);
      setPlacedElements((prev) => {
        const next = { ...prev };
        delete next[el.id];
        return next;
      });
    }, [elementPositions, handleLocalAction, playSound]);
    React2.useEffect(() => {
      if (!dragState) return;
      const sw = containerSize.width || 1;
      const sh = containerSize.height || 1;
      const onMove = (e) => {
        const dx = (e.clientX - dragState.startMouseX) / sw * 100;
        const dy = (e.clientY - dragState.startMouseY) / sh * 100;
        let nx = Math.round((dragState.startX + dx) * 10) / 10;
        let ny = Math.round((dragState.startY + dy) * 10) / 10;
        setDragOffset({ x: nx, y: ny });
      };
      const onUp = () => {
        var _a;
        if (!dragOffset) {
          setDragState(null);
          return;
        }
        const el = interactiveElements[dragState.elementId];
        if (!el) {
          setDragState(null);
          setDragOffset(null);
          return;
        }
        const cx = dragOffset.x + el.width / 2;
        const cy = dragOffset.y + el.height / 2;
        const boundItem = el.boundItemId ? (_a = project.items) == null ? void 0 : _a[el.boundItemId] : null;
        const effTag = el.dragTag || (boundItem == null ? void 0 : boundItem.dragTag) || void 0;
        const target = hitTestDropTarget({ x: cx, y: cy }, el.id, effTag);
        if (target) {
          const r = target.rectPct;
          const finalPos = el.snapToHotSpot ? { x: r.x + (r.width - el.width) / 2, y: r.y + (r.height - el.height) / 2 } : { x: dragOffset.x, y: dragOffset.y };
          setElementPositions((prev) => ({ ...prev, [el.id]: finalPos }));
          setPlacedElements((prev) => ({ ...prev, [el.id]: target.id }));
          target.onDrop(el.id);
          if (boundItem) {
            if (boundItem.consumeOnUse !== false) handleLocalAction({ type: UIActionType.SetVariable, variableId: boundItem.countVariableId, operator: "subtract", value: 1 });
            (boundItem.useEffect || []).forEach((eff) => handleLocalAction(eff));
          }
        } else if (el.snapBack) {
          setElementPositions((prev) => ({ ...prev, [el.id]: { x: el.x, y: el.y } }));
        } else {
          setElementPositions((prev) => ({ ...prev, [el.id]: { x: dragOffset.x, y: dragOffset.y } }));
        }
        setDragState(null);
        setDragOffset(null);
      };
      window.addEventListener("mousemove", onMove);
      window.addEventListener("mouseup", onUp);
      return () => {
        window.removeEventListener("mousemove", onMove);
        window.removeEventListener("mouseup", onUp);
      };
    }, [dragState, dragOffset, containerSize, interactiveElements, hotSpots, handleLocalAction, project]);
    const handleSpotClick = React2.useCallback((spot) => {
      if ((spot.trigger || "click") === "click") {
        (spot.actions || []).forEach((a) => handleLocalAction(a));
      }
    }, [handleLocalAction]);
    const handleSpotHover = React2.useCallback((spot) => {
      if (spot.trigger === "hover") {
        spot.actions.forEach((a) => handleLocalAction(a));
      }
    }, [handleLocalAction]);
    return (
      // pointer-events:none lets clicks pass through empty overlay areas to standard
      // elements (buttons, etc.) rendered underneath; interactive children below
      // re-enable pointer events individually with pointer-events:auto.
      /* @__PURE__ */ jsxRuntime2.jsxs("div", { ref: containerRef, className: "absolute inset-0 w-full h-full", style: { pointerEvents: "none" }, children: [
        Object.values(hotSpots).map((spot) => {
          if (spot.conditions && !evaluateConditions2(spot.conditions, variables)) return null;
          return /* @__PURE__ */ jsxRuntime2.jsx(
            "div",
            {
              className: "absolute",
              style: {
                left: \`\${spot.x}%\`,
                top: \`\${spot.y}%\`,
                width: \`\${spot.width}%\`,
                height: \`\${spot.height}%\`,
                borderRadius: spot.shape === "circle" ? "50%" : void 0,
                backgroundColor: spot.visible ? spot.highlightColor || "rgba(59, 130, 246, 0.2)" : "transparent",
                border: spot.visible ? \`2px dashed \${spot.highlightColor || "rgba(59, 130, 246, 0.5)"}\` : "none",
                pointerEvents: spot.trigger === "drag-drop" ? "none" : "auto",
                cursor: (spot.trigger || "click") === "click" ? "pointer" : void 0
              },
              onClick: () => handleSpotClick(spot),
              onMouseEnter: () => handleSpotHover(spot)
            },
            spot.id
          );
        }),
        Object.values(interactiveElements).map((el) => {
          if (el.conditions && !evaluateConditions2(el.conditions, variables)) return null;
          if (el.snapToHotSpot && el.hideOnDrop && placedElements[el.id]) return null;
          const isDragging = (dragState == null ? void 0 : dragState.elementId) === el.id;
          const pos = isDragging && dragOffset ? dragOffset : elementPositions[el.id] || { x: el.x, y: el.y };
          const effectiveImageId = imageOverrides[el.id] || el.imageId;
          const imageUrl = assetResolver(effectiveImageId, "image");
          const anim = activeAnimations[el.id];
          const animationKeyframes = {
            shake: "hz-shake",
            bounce: "hz-bounce",
            pulse: "hz-pulse",
            spin: "hz-spin",
            fadeIn: "hz-fadeIn",
            fadeOut: "hz-fadeOut",
            slideIn: "hz-slideIn",
            glow: "hz-glow"
          };
          const elType = el.elementType || "image";
          const elText = el.text || "";
          const elFont = el.font;
          const videoUrl = el.videoId ? assetResolver(el.videoId, "video") : null;
          const isFadedOut = !anim && persistentEffects[el.id] === "fadedOut";
          return /* @__PURE__ */ jsxRuntime2.jsx(
            "div",
            {
              className: "absolute",
              style: {
                left: \`\${pos.x}%\`,
                top: \`\${pos.y}%\`,
                width: \`\${el.width}%\`,
                height: \`\${el.height}%\`,
                cursor: el.draggable ? isDragging ? "grabbing" : "grab" : elType === "textInput" ? "text" : "pointer",
                zIndex: isDragging ? 50 : 10,
                pointerEvents: isFadedOut ? "none" : "auto",
                opacity: isFadedOut ? 0 : void 0,
                transition: isDragging ? "none" : "left 0.2s, top 0.2s",
                animation: anim ? \`\${animationKeyframes[anim.animation] || "hz-shake"} \${anim.duration}ms ease\` : void 0
              },
              onMouseDown: (e) => elType !== "textInput" && handleElementMouseDown(e, el),
              onMouseEnter: () => {
                try {
                  playSound(el.hoverSoundId || null);
                } catch (e) {
                }
              },
              children: elType === "text" ? /* @__PURE__ */ jsxRuntime2.jsx(
                "div",
                {
                  className: "w-full h-full flex items-center justify-center text-white pointer-events-none",
                  style: elFont ? { fontFamily: elFont.fontFamily, fontSize: elFont.fontSize, fontWeight: elFont.bold ? "bold" : "normal", fontStyle: elFont.italic ? "italic" : "normal", color: elFont.color || "#fff" } : {},
                  children: project ? interpolateVariables(elText, variables, project) : elText
                }
              ) : elType === "button" ? /* @__PURE__ */ jsxRuntime2.jsxs("div", { className: "w-full h-full relative flex items-center justify-center pointer-events-none", children: [
                imageUrl && /* @__PURE__ */ jsxRuntime2.jsx("img", { src: imageUrl, alt: el.name, className: "absolute inset-0 w-full h-full object-fill", draggable: false }),
                /* @__PURE__ */ jsxRuntime2.jsx(
                  "span",
                  {
                    className: "relative z-10 text-white text-sm font-semibold",
                    style: elFont ? { fontFamily: elFont.fontFamily, fontSize: elFont.fontSize, color: elFont.color || "#fff" } : {},
                    children: project ? interpolateVariables(elText, variables, project) : elText
                  }
                )
              ] }) : elType === "video" ? videoUrl ? /* @__PURE__ */ jsxRuntime2.jsx(
                TrimmedVideo,
                {
                  src: videoUrl,
                  className: "w-full h-full object-contain pointer-events-none",
                  autoPlay: true,
                  loop: el.videoLoop ?? true,
                  muted: el.videoMuted ?? true,
                  trimStart: el.videoTrimStart,
                  trimEnd: el.videoTrimEnd,
                  playsInline: true
                }
              ) : /* @__PURE__ */ jsxRuntime2.jsxs("div", { className: "w-full h-full bg-indigo-500/30 border border-indigo-400 rounded flex items-center justify-center text-xs text-white pointer-events-none", children: [
                el.name,
                " (no video)"
              ] }) : elType === "textInput" ? /* @__PURE__ */ jsxRuntime2.jsx(
                HotZoneTextInput,
                {
                  element: el,
                  variables,
                  onVariableChange,
                  playSound,
                  font: elFont
                }
              ) : elType === "draggableImageElement" ? /* @__PURE__ */ jsxRuntime2.jsx(
                HotZonedraggableImageElementRenderer,
                {
                  el,
                  imageUrl,
                  assetResolver,
                  evaluateConditions: evaluateConditions2,
                  variables,
                  playSound,
                  handleLocalAction
                }
              ) : imageUrl ? /* @__PURE__ */ jsxRuntime2.jsx("img", { src: imageUrl, alt: el.name, className: "w-full h-full object-contain pointer-events-none", draggable: false }) : /* @__PURE__ */ jsxRuntime2.jsx("div", { className: "w-full h-full bg-purple-500/30 border border-purple-400 rounded flex items-center justify-center text-xs text-white pointer-events-none", children: el.name })
            },
            el.id
          );
        })
      ] })
    );
  };
  const UIScreenRenderer = React2.memo(({ screenId, onAction, settings, onSettingsChange, assetResolver, gameSaves, playSound, variables = {}, onVariableChange, isClosing = false, evaluateConditions: evaluateConditions2, onCommitVariables, inventorySlots, onReorderSlots, selectedItemId, selectedElementId, onSelectItem, elementVisibility }) => {
    var _a, _b;
    const { project } = useProject();
    const screen = project.uiScreens[screenId];
    const backgroundVideoRef = React2.useRef(null);
    const screenRootRef = React2.useRef(null);
    const screenSize = useStageSize(screenRootRef);
    React2.useEffect(() => {
      return () => {
        if (backgroundVideoRef.current) {
          backgroundVideoRef.current.pause();
          backgroundVideoRef.current.src = "";
          backgroundVideoRef.current.load();
        }
      };
    }, []);
    React2.useEffect(() => {
      const root = screenRootRef.current;
      if (!root) return;
      const px = screen == null ? void 0 : screen.parallax;
      const active = (px == null ? void 0 : px.mode) === "mouse" || (px == null ? void 0 : px.mode) === "both";
      const reduce = typeof window !== "undefined" && window.matchMedia && window.matchMedia("(prefers-reduced-motion: reduce)").matches;
      if (!active || reduce) {
        root.style.setProperty("--ppx", "0");
        root.style.setProperty("--ppy", "0");
        return;
      }
      const intensity = (px == null ? void 0 : px.intensity) ?? 1;
      const target = { x: 0, y: 0 };
      const cur = { x: 0, y: 0 };
      let raf = 0;
      const onMove = (e) => {
        const r = root.getBoundingClientRect();
        if (!r.width || !r.height) return;
        target.x = Math.max(-1, Math.min(1, (e.clientX - r.left) / r.width * 2 - 1));
        target.y = Math.max(-1, Math.min(1, (e.clientY - r.top) / r.height * 2 - 1));
      };
      const onLeave = () => {
        target.x = 0;
        target.y = 0;
      };
      const tick = () => {
        cur.x += (target.x - cur.x) * 0.08;
        cur.y += (target.y - cur.y) * 0.08;
        root.style.setProperty("--ppx", (cur.x * intensity).toFixed(4));
        root.style.setProperty("--ppy", (cur.y * intensity).toFixed(4));
        raf = requestAnimationFrame(tick);
      };
      window.addEventListener("mousemove", onMove);
      root.addEventListener("mouseleave", onLeave);
      raf = requestAnimationFrame(tick);
      return () => {
        cancelAnimationFrame(raf);
        window.removeEventListener("mousemove", onMove);
        root.removeEventListener("mouseleave", onLeave);
      };
    }, [(_a = screen == null ? void 0 : screen.parallax) == null ? void 0 : _a.mode, (_b = screen == null ? void 0 : screen.parallax) == null ? void 0 : _b.intensity]);
    if (!screen) return /* @__PURE__ */ jsxRuntime2.jsxs("div", { className: "text-red-500", children: [
      "Error: Screen ",
      screenId,
      " not found."
    ] });
    const isPassThrough = screen.passThrough ?? (screenId === project.ui.gameHudScreenId || !!screen.hudNonBlocking);
    const screenBgScale = (depth) => {
      var _a2;
      if (!depth) return 1;
      const intensity = Math.max(1, ((_a2 = screen.parallax) == null ? void 0 : _a2.intensity) ?? 1);
      const maxShiftPx = depth * PARALLAX_MAX_PX * intensity * 1.1;
      const minDim = Math.min((screenSize == null ? void 0 : screenSize.width) || 1280, (screenSize == null ? void 0 : screenSize.height) || 720);
      return 1 + 2 * maxShiftPx / minDim;
    };
    const bgTransitionAnim = (transition, durationMs) => {
      const d = durationMs ?? 400;
      switch (transition) {
        case void 0:
        case "":
        case "none":
          return void 0;
        case "slide":
          return \`slide-in-right \${d}ms ease-out forwards\`;
        case "iris":
          return \`iris-in \${d}ms ease-out forwards\`;
        case "wipe":
          return \`wipe-right \${d}ms ease-out forwards\`;
        default:
          return \`dissolve-in \${d}ms ease-out forwards\`;
      }
    };
    const buildBgPlane = (bg, depth, layer, key, videoRef, transition, transitionDuration) => {
      var _a2;
      let node = null;
      if (bg.type === "color") {
        node = /* @__PURE__ */ jsxRuntime2.jsx("div", { className: "absolute inset-0", style: { backgroundColor: bg.value } });
      } else if (bg.assetId) {
        const asset = project.backgrounds[bg.assetId] || ((_a2 = project.images) == null ? void 0 : _a2[bg.assetId]) || project.videos[bg.assetId];
        const isVid = bg.type === "video" || !!(asset && (asset.isVideo || asset.videoUrl));
        const url = isVid ? assetResolver(bg.assetId, "video") : assetResolver(bg.assetId, "image");
        const bgTrim = resolveVideoTrim(bg, asset);
        if (url) {
          node = isVid ? /* @__PURE__ */ jsxRuntime2.jsx(TrimmedVideo, { ref: videoRef, src: url, autoPlay: true, loop: bg.loop ?? true, muted: true, trimStart: bgTrim.start, trimEnd: bgTrim.end, playsInline: true, className: "absolute inset-0 w-full h-full object-cover" }) : /* @__PURE__ */ jsxRuntime2.jsx("img", { src: url, alt: "", className: "absolute inset-0 w-full h-full object-cover" });
        }
      }
      if (!node) return null;
      const inner = depth ? /* @__PURE__ */ jsxRuntime2.jsx("div", { className: "absolute inset-0", style: { transform: \`scale(\${screenBgScale(depth)})\${parallaxTransform(depth)}\`, transformOrigin: "center" }, children: node }) : node;
      return /* @__PURE__ */ jsxRuntime2.jsx("div", { className: "absolute inset-0 overflow-hidden", style: { zIndex: layer, animation: bgTransitionAnim(transition, transitionDuration) }, children: inner }, key);
    };
    const getBackgroundElement = () => /* @__PURE__ */ jsxRuntime2.jsxs(jsxRuntime2.Fragment, { children: [
      buildBgPlane(screen.background, screen.backgroundParallaxDepth ?? 0, screen.backgroundLayer ?? 0, "main-bg", backgroundVideoRef, screen.backgroundTransition, screen.backgroundTransitionDuration),
      (screen.additionalBackgrounds || []).map((b) => buildBgPlane(b.background, b.parallaxDepth ?? 0, b.layer ?? 0, b.id, void 0, b.transition, b.transitionDuration))
    ] });
    const meterPrevRef = React2.useRef({});
    const renderElement = (element, variables2, project2, onCommitVariables2) => {
      var _a2, _b2, _c, _d, _e, _f, _g, _h, _i, _j, _k, _l, _m, _n, _o, _p, _q, _r, _s, _t, _u, _v, _w, _x, _y, _z, _A, _B;
      runtimeDebugLog("🎯 renderElement called:", element.type, element.name, element.id);
      if (element.conditions && element.conditions.length > 0) {
        const conditionsMet = evaluateConditions2(element.conditions, variables2);
        if (!conditionsMet) {
          runtimeDebugLog("🚫 Element conditions not met, skipping render:", element.name);
          return null;
        }
      }
      const activeState = pickActiveAppearanceState(element, variables2, evaluateConditions2);
      if (activeState) element = mergeAppearanceStatePrimary(element, activeState);
      let stateExtraTransform = "";
      let stateFilter;
      let stateOpacityMul = 1;
      let stateTransition;
      if (activeState) {
        if (activeState.scale != null && activeState.scale !== 1) stateExtraTransform += \` scale(\${activeState.scale})\`;
        if (activeState.rotation) stateExtraTransform += \` rotate(\${activeState.rotation}deg)\`;
        const noTypedPrimary = element.type !== UIElementType.Meter && element.type !== UIElementType.Text && element.type !== UIElementType.Button;
        const glow = activeState.glowColor || (activeState.primaryColor && noTypedPrimary ? activeState.primaryColor : void 0);
        if (glow) stateFilter = \`drop-shadow(0 0 \${activeState.glowSize ?? 8}px \${glow})\`;
        if (activeState.opacity != null) stateOpacityMul = activeState.opacity;
        if (activeState.transitionMs) stateTransition = \`transform \${activeState.transitionMs}ms ease, opacity \${activeState.transitionMs}ms ease, filter \${activeState.transitionMs}ms ease\`;
      }
      const transitionStyle = getTransitionStyle(element.transitionIn, element.transitionDuration, element.transitionDelay);
      const isDisabled = !!(element.disabledConditions && element.disabledConditions.length > 0 && evaluateConditions2(element.disabledConditions, variables2));
      const combinedFilter = [isDisabled ? "grayscale(0.6)" : "", stateFilter || ""].filter(Boolean).join(" ") || void 0;
      const style = {
        position: "absolute",
        left: \`\${element.x}%\`,
        top: \`\${element.y}%\`,
        width: \`\${element.width}%\`,
        height: \`\${element.height}%\`,
        // \`translateZ(0)\` promotes each element onto its own compositing layer. A <video>
        // background is ALWAYS GPU-composited and (once it has a parallax/scale transform)
        // will paint over non-composited siblings at the same z-index — which made buttons
        // vanish behind a parallaxed video bg. Promoting elements keeps normal z-order.
        // \`stateExtraTransform\` (appearance-state scale/rotation) composes on top.
        transform: \`translate(-\${element.anchorX * 100}%, -\${element.anchorY * 100}%)\${parallaxTransform(element.parallaxDepth)} translateZ(0)\${stateExtraTransform}\`,
        overflow: "hidden",
        // Prevent content overflow when using cover
        // Author-controlled stacking. Default 0 → insertion order (back-compat).
        zIndex: element.layer ?? 0,
        opacity: (element.opacity ?? 1) * (isDisabled ? 0.45 : 1) * stateOpacityMul,
        // On a pass-through screen the wrapper is pointer-events:none, so each visible
        // element must opt back in to remain clickable.
        ...isPassThrough && !isDisabled ? { pointerEvents: "auto" } : {},
        ...isDisabled ? { pointerEvents: "none", cursor: "not-allowed" } : {},
        ...combinedFilter ? { filter: combinedFilter } : {},
        ...stateTransition ? { transition: stateTransition } : {},
        ...transitionStyle
      };
      {
        const visOverride = elementVisibility == null ? void 0 : elementVisibility[element.id];
        if (element.startHidden || visOverride !== void 0) {
          const isHidden = visOverride !== void 0 ? !visOverride : !!element.startHidden;
          const fadeMs = element.transitionDuration ?? 300;
          style.transition = style.transition ? \`\${style.transition}, opacity \${fadeMs}ms ease\` : \`opacity \${fadeMs}ms ease\`;
          if (isHidden) {
            style.opacity = 0;
            style.pointerEvents = "none";
          }
        }
      }
      const getElementAssetUrl = (image) => {
        if (!image) return null;
        return assetResolver(image.id, image.type);
      };
      switch (element.type) {
        case UIElementType.Button: {
          const el = element;
          return /* @__PURE__ */ jsxRuntime2.jsx(ButtonElement, { element: el, style, playSound, onAction, getElementAssetUrl, variables: variables2, project: project2, onCommitVariables: onCommitVariables2 }, el.id);
        }
        case UIElementType.Timer: {
          const el = element;
          return /* @__PURE__ */ jsxRuntime2.jsx(TimerElement, { element: el, style, onAction, onCommitVariables: onCommitVariables2 }, el.id);
        }
        case UIElementType.Text: {
          const el = element;
          const effectiveAlign = el.textAlign || ((_a2 = el.font) == null ? void 0 : _a2.align) || "center";
          const hAlignClass = { left: "justify-start", center: "justify-center", right: "justify-end" }[effectiveAlign];
          const vAlignClass = { top: "items-start", middle: "items-center", bottom: "items-end" }[el.verticalAlign || "middle"];
          const interpolatedText = interpolateVariables(el.text, variables2, project2);
          const textStyle = {
            ...fontSettingsToStyle(el.font),
            ...(activeState == null ? void 0 : activeState.transitionMs) ? { transition: \`color \${activeState.transitionMs}ms ease\` } : {}
          };
          return /* @__PURE__ */ jsxRuntime2.jsx(
            "div",
            {
              style,
              className: \`flex \${hAlignClass} \${vAlignClass} p-1\`,
              children: /* @__PURE__ */ jsxRuntime2.jsx("div", { style: textStyle, children: /* @__PURE__ */ jsxRuntime2.jsx("span", { style: extractTextGradientStyle(el.font) || void 0, children: interpolatedText }) })
            },
            el.id
          );
        }
        case UIElementType.Image: {
          const el = element;
          const bgType = ((_b2 = el.background) == null ? void 0 : _b2.type) || "image";
          const bgValue = ((_c = el.background) == null ? void 0 : _c.type) === "color" ? el.background.value : ((_d = el.background) == null ? void 0 : _d.type) ? el.background.assetId : ((_e = el.image) == null ? void 0 : _e.id) || null;
          const fit = !!el.fitToContent;
          const containerStyle = {
            ...style,
            overflow: "hidden",
            ...fit ? { display: "flex", alignItems: "center", justifyContent: "center" } : {}
          };
          if (bgType === "color" && typeof bgValue === "string") {
            return /* @__PURE__ */ jsxRuntime2.jsx("div", { style: { ...style, overflow: "hidden", backgroundColor: bgValue } }, el.id);
          }
          const url = bgValue ? assetResolver(bgValue, bgType === "video" ? "video" : "image") : null;
          if (!url || url === "" || url === "http://localhost:3000/") {
            return /* @__PURE__ */ jsxRuntime2.jsx("div", { style: containerStyle, className: "bg-slate-800/50" }, el.id);
          }
          const isVideo = bgType === "video";
          const elVidAsset = typeof bgValue === "string" ? ((_f = project2.videos) == null ? void 0 : _f[bgValue]) || ((_g = project2.images) == null ? void 0 : _g[bgValue]) || ((_h = project2.backgrounds) == null ? void 0 : _h[bgValue]) : void 0;
          const elVidTrim = resolveVideoTrim(el.background, elVidAsset);
          const mediaStyle = fit ? { maxWidth: "100%", maxHeight: "100%", width: "auto", height: "auto", objectFit: el.objectFit || "contain", display: "block" } : { width: "100%", height: "100%", objectFit: el.objectFit || "contain", display: "block" };
          if (isVideo) {
            return /* @__PURE__ */ jsxRuntime2.jsx("div", { style: containerStyle, children: /* @__PURE__ */ jsxRuntime2.jsxs(
              TrimmedVideo,
              {
                ref: (videoEl) => {
                  if (videoEl && url) {
                    if (!videoEl.src || videoEl.src === "http://localhost:3000/" || videoEl.src === window.location.href) {
                      videoEl.src = url;
                    }
                    setTimeout(() => {
                      if (videoEl.readyState >= 2) {
                        videoEl.play().catch((error) => {
                          console.error("[Video Play Error]", el.name, error);
                        });
                      } else {
                        setTimeout(() => videoEl.play().catch(() => {
                        }), 500);
                      }
                    }, 100);
                  }
                },
                src: url,
                style: mediaStyle,
                autoPlay: true,
                loop: ((_i = el.background) == null ? void 0 : _i.loop) ?? true,
                muted: ((_j = el.background) == null ? void 0 : _j.muted) ?? false,
                trimStart: elVidTrim.start,
                trimEnd: elVidTrim.end,
                playsInline: true,
                children: [
                  /* @__PURE__ */ jsxRuntime2.jsx("source", { src: url, type: "video/webm" }),
                  /* @__PURE__ */ jsxRuntime2.jsx("source", { src: url, type: "video/mp4" }),
                  "Your browser doesn't support this video format."
                ]
              }
            ) }, el.id);
          } else {
            const imageCrossfadeMs = (el.appearanceStates || []).reduce(
              (m, s) => s.image && s.transitionMs ? Math.max(m, s.transitionMs) : m,
              0
            );
            return /* @__PURE__ */ jsxRuntime2.jsx("div", { style: containerStyle, children: /* @__PURE__ */ jsxRuntime2.jsx(CrossfadeImage, { src: url, alt: el.name, style: mediaStyle, transitionMs: imageCrossfadeMs || void 0 }) }, el.id);
          }
        }
        case UIElementType.SettingsSlider: {
          const el = element;
          let value;
          let min;
          let max;
          let step;
          if (el.variableId) {
            value = Number(variables2[el.variableId]) || (el.minValue ?? 0);
            min = el.minValue ?? 0;
            max = el.maxValue ?? 100;
            step = 1;
          } else {
            const settingKey = el.setting === "textSpeed" ? "textSpeed" : el.setting;
            value = settings[settingKey];
            min = el.setting === "textSpeed" ? 10 : 0;
            max = el.setting === "textSpeed" ? 100 : 1;
            step = el.setting === "textSpeed" ? 1 : 0.01;
          }
          const thumbUrl = el.thumbImage ? getElementAssetUrl(el.thumbImage) : null;
          const trackUrl = el.trackImage ? getElementAssetUrl(el.trackImage) : null;
          const thumbColor = el.thumbColor || "#8a2be2";
          const trackColor = el.trackColor || "#4D3273";
          const customSliderStyle = {
            // Custom thumb via CSS variable (if no image)
            ...!thumbUrl ? {
              ["--slider-thumb-color"]: thumbColor
            } : {},
            // Custom track via CSS variable (if no image)
            ...!trackUrl ? {
              ["--slider-track-color"]: trackColor
            } : {},
            // Thumb image as background
            ...thumbUrl ? {
              ["--slider-thumb-bg"]: \`url(\${thumbUrl})\`
            } : {},
            // Track image as background
            ...trackUrl ? {
              ["--slider-track-bg"]: \`url(\${trackUrl})\`
            } : {}
          };
          return /* @__PURE__ */ jsxRuntime2.jsx("div", { style, className: "flex items-center", children: /* @__PURE__ */ jsxRuntime2.jsx(
            "input",
            {
              type: "range",
              min,
              max,
              step,
              value,
              onChange: (e) => {
                const newValue = parseFloat(e.target.value);
                if (el.variableId) {
                  onVariableChange == null ? void 0 : onVariableChange(el.variableId, newValue);
                } else {
                  onSettingsChange(el.setting, newValue);
                }
                if (el.actions && el.actions.length > 0) {
                  el.actions.forEach((action) => onAction(action));
                }
              },
              style: customSliderStyle,
              className: thumbUrl || trackUrl ? "custom-slider" : ""
            }
          ) }, el.id);
        }
        case UIElementType.SettingsToggle: {
          const el = element;
          let isChecked;
          if (el.variableId) {
            const currentValue = variables2[el.variableId];
            if (el.checkedValue !== void 0 && el.uncheckedValue !== void 0) {
              isChecked = currentValue === el.checkedValue;
            } else {
              isChecked = Boolean(currentValue);
            }
          } else {
            isChecked = settings[el.setting];
          }
          const checkboxImage = isChecked ? el.checkedImage : el.uncheckedImage;
          const imageUrl = checkboxImage ? getElementAssetUrl(checkboxImage) : null;
          const handleToggle = () => {
            if (el.variableId) {
              if (el.checkedValue !== void 0 && el.uncheckedValue !== void 0) {
                const newValue = isChecked ? el.uncheckedValue : el.checkedValue;
                onVariableChange == null ? void 0 : onVariableChange(el.variableId, newValue);
              } else {
                onVariableChange == null ? void 0 : onVariableChange(el.variableId, !isChecked);
              }
            } else {
              onSettingsChange(el.setting, !isChecked);
            }
            if (el.actions && el.actions.length > 0) {
              el.actions.forEach((action) => onAction(action));
            }
          };
          return /* @__PURE__ */ jsxRuntime2.jsxs("div", { style, className: "flex items-center gap-2", children: [
            imageUrl ? /* @__PURE__ */ jsxRuntime2.jsx(
              "img",
              {
                src: imageUrl,
                alt: isChecked ? "checked" : "unchecked",
                onClick: handleToggle,
                className: "h-5 w-5 cursor-pointer object-contain"
              }
            ) : /* @__PURE__ */ jsxRuntime2.jsx(
              "input",
              {
                type: "checkbox",
                checked: isChecked,
                onChange: handleToggle,
                className: "h-5 w-5",
                style: el.checkboxColor ? { accentColor: el.checkboxColor } : {}
              }
            ),
            /* @__PURE__ */ jsxRuntime2.jsx("label", { style: fontSettingsToStyle(el.font), children: /* @__PURE__ */ jsxRuntime2.jsx("span", { style: extractTextGradientStyle(el.font) || void 0, children: el.text }) })
          ] }, el.id);
        }
        case UIElementType.SaveSlotGrid: {
          const el = element;
          const isSaveMode = screenId === project2.ui.saveScreenId && screenId !== project2.ui.loadScreenId;
          return /* @__PURE__ */ jsxRuntime2.jsx(
            SaveSlotGridComponent,
            {
              element: el,
              style,
              isSaveMode,
              gameSaves,
              onAction
            },
            el.id
          );
        }
        case UIElementType.CharacterPreview: {
          const el = element;
          const previewCharacterId = el.characterSource === "player" ? resolvePlayerCharacterId(project2, variables2) || el.characterId : el.characterId;
          const character = previewCharacterId ? project2.characters[previewCharacterId] : null;
          if (!character) return null;
          runtimeDebugLog(\`[CharacterPreview] layerVariableMap:\`, el.layerVariableMap);
          runtimeDebugLog(\`[CharacterPreview] Available variables:\`, Object.keys(variables2));
          const imageUrls = [];
          const videoUrls = [];
          const videoTrims = [];
          let hasVideo = false;
          let videoLoop = false;
          if (character.baseVideoUrl) {
            videoUrls.push(resolveFieldUrl(project2.id, character.baseVideoUrl) || character.baseVideoUrl);
            videoTrims.push({ start: character.baseVideoTrimStart, end: character.baseVideoTrimEnd });
            hasVideo = true;
            videoLoop = !!character.baseVideoLoop;
          } else if (character.baseImageUrl) {
            imageUrls.push(resolveFieldUrl(project2.id, character.baseImageUrl) || character.baseImageUrl);
          }
          const defaultExpression = el.expressionId ? character.expressions[el.expressionId] : null;
          Object.entries(character.layers).forEach(([layerId, layer]) => {
            let variableId = el.layerVariableMap[layerId];
            if (!variableId && el.characterSource === "player" && variables2) {
              const matches = Object.entries(project2.variables).filter(([vid, v]) => v.type === "string" && String(variables2[vid] || "") in layer.assets);
              if (matches.length) variableId = matches[matches.length - 1][0];
            }
            let asset = null;
            runtimeDebugLog(\`[CharacterPreview] Processing layer \${layer.name} (\${layerId}), mapped variableId:\`, variableId);
            if (variableId && variables2) {
              const assetId = String(variables2[variableId] || "");
              runtimeDebugLog(\`[CharacterPreview] Layer \${layer.name} (\${layerId}): variableId=\${variableId}, assetId from variable="\${assetId}"\`);
              runtimeDebugLog(\`[CharacterPreview] Available assets in layer:\`, Object.keys(layer.assets));
              if (assetId) {
                asset = layer.assets[assetId];
                if (asset) {
                  runtimeDebugLog(\`[CharacterPreview] ✓ Found asset: \${asset.name}\`);
                } else {
                  runtimeDebugWarn(\`[CharacterPreview] ✗ Asset ID "\${assetId}" not found in layer \${layer.name}!\`);
                }
              } else {
                runtimeDebugLog(\`[CharacterPreview] Variable \${variableId} is empty, skipping layer\`);
              }
            } else if (defaultExpression && defaultExpression.layerConfiguration[layerId]) {
              const assetId = defaultExpression.layerConfiguration[layerId];
              runtimeDebugLog(\`[CharacterPreview] Layer \${layer.name} using default expression asset: \${assetId}\`);
              asset = assetId ? layer.assets[assetId] : null;
            } else {
              runtimeDebugLog(\`[CharacterPreview] Layer \${layer.name} has no mapping and no default expression\`);
            }
            if (asset) {
              if (asset.videoUrl) {
                videoUrls.push(resolveFieldUrl(project2.id, asset.videoUrl) || asset.videoUrl);
                videoTrims.push({});
                hasVideo = true;
                videoLoop = videoLoop || !!asset.loop;
              } else if (asset.imageUrl) {
                imageUrls.push(resolveFieldUrl(project2.id, asset.imageUrl) || asset.imageUrl);
              }
            }
          });
          const containerStyle = {
            ...style,
            overflow: "hidden"
          };
          return /* @__PURE__ */ jsxRuntime2.jsx("div", { style: containerStyle, children: /* @__PURE__ */ jsxRuntime2.jsx("div", { className: "relative w-full h-full", children: hasVideo && videoUrls.length > 0 ? videoUrls.map((url, index) => {
            var _a3, _b3;
            return /* @__PURE__ */ jsxRuntime2.jsx(
              TrimmedVideo,
              {
                src: url,
                autoPlay: true,
                muted: true,
                loop: videoLoop,
                trimStart: (_a3 = videoTrims[index]) == null ? void 0 : _a3.start,
                trimEnd: (_b3 = videoTrims[index]) == null ? void 0 : _b3.end,
                playsInline: true,
                className: "absolute top-0 left-0 w-full h-full object-contain",
                style: { zIndex: index }
              },
              index
            );
          }) : imageUrls.map((url, index) => /* @__PURE__ */ jsxRuntime2.jsx(
            "img",
            {
              src: url,
              alt: "",
              className: "absolute top-0 left-0 w-full h-full object-contain",
              style: { zIndex: index }
            },
            index
          )) }) }, el.id);
        }
        case UIElementType.TextInput: {
          const el = element;
          const currentValue = String(variables2[el.variableId] || "");
          return /* @__PURE__ */ jsxRuntime2.jsx(
            "div",
            {
              style,
              children: /* @__PURE__ */ jsxRuntime2.jsx(
                "input",
                {
                  type: "text",
                  value: currentValue,
                  onChange: (e) => {
                    onVariableChange == null ? void 0 : onVariableChange(el.variableId, e.target.value);
                  },
                  placeholder: el.placeholder,
                  maxLength: el.maxLength,
                  className: "w-full h-full outline-none",
                  style: {
                    backgroundColor: el.backgroundColor || "#1e293b",
                    color: ((_k = el.font) == null ? void 0 : _k.color) || "#f1f5f9",
                    fontSize: \`calc(var(--font-scale, 1) * \${((_l = el.font) == null ? void 0 : _l.size) || 16}px)\`,
                    fontFamily: ((_m = el.font) == null ? void 0 : _m.family) || "Inter, system-ui, sans-serif",
                    fontWeight: ((_n = el.font) == null ? void 0 : _n.weight) || "normal",
                    fontStyle: ((_o = el.font) == null ? void 0 : _o.italic) ? "italic" : "normal",
                    border: \`2px solid \${el.borderColor || "#475569"}\`,
                    borderRadius: "4px",
                    padding: "8px 12px"
                  }
                }
              )
            },
            el.id
          );
        }
        case UIElementType.Dropdown: {
          const el = element;
          const currentValue = variables2[el.variableId];
          return /* @__PURE__ */ jsxRuntime2.jsx(
            "div",
            {
              style,
              children: /* @__PURE__ */ jsxRuntime2.jsx(
                "select",
                {
                  value: String(currentValue ?? ((_p = el.options[0]) == null ? void 0 : _p.value) ?? ""),
                  onChange: (e) => {
                    const selectedOption = el.options.find((opt) => String(opt.value) === e.target.value);
                    if (selectedOption) {
                      onVariableChange == null ? void 0 : onVariableChange(el.variableId, selectedOption.value);
                      if (el.actions && el.actions.length > 0) {
                        el.actions.forEach((action) => onAction(action));
                      }
                    }
                  },
                  className: "w-full h-full outline-none cursor-pointer",
                  style: {
                    backgroundColor: el.backgroundColor || "#1e293b",
                    color: ((_q = el.font) == null ? void 0 : _q.color) || "#f1f5f9",
                    fontSize: \`calc(var(--font-scale, 1) * \${((_r = el.font) == null ? void 0 : _r.size) || 16}px)\`,
                    fontFamily: ((_s = el.font) == null ? void 0 : _s.family) || "Inter, system-ui, sans-serif",
                    fontWeight: ((_t = el.font) == null ? void 0 : _t.weight) || "normal",
                    fontStyle: ((_u = el.font) == null ? void 0 : _u.italic) ? "italic" : "normal",
                    border: \`2px solid \${el.borderColor || "#475569"}\`,
                    borderRadius: "4px",
                    padding: "8px 12px",
                    direction: el.arrowSide === "left" ? "rtl" : "ltr",
                    textAlign: el.arrowSide === "left" ? "right" : "left"
                  },
                  onMouseEnter: (e) => {
                    if (el.hoverColor) {
                      e.currentTarget.style.backgroundColor = el.hoverColor;
                    }
                  },
                  onMouseLeave: (e) => {
                    e.currentTarget.style.backgroundColor = el.backgroundColor || "#1e293b";
                  },
                  children: el.options.map((opt) => /* @__PURE__ */ jsxRuntime2.jsx("option", { value: String(opt.value), children: opt.label }, opt.id))
                }
              )
            },
            el.id
          );
        }
        case UIElementType.Checkbox: {
          const el = element;
          const currentValue = variables2[el.variableId];
          const isChecked = currentValue === el.checkedValue;
          return /* @__PURE__ */ jsxRuntime2.jsxs(
            "div",
            {
              style,
              className: "flex items-center gap-2 cursor-pointer",
              onClick: () => {
                const newValue = isChecked ? el.uncheckedValue : el.checkedValue;
                onVariableChange == null ? void 0 : onVariableChange(el.variableId, newValue);
                if (el.actions && el.actions.length > 0) {
                  el.actions.forEach((action) => onAction(action));
                }
              },
              children: [
                /* @__PURE__ */ jsxRuntime2.jsx(
                  "input",
                  {
                    type: "checkbox",
                    checked: isChecked,
                    onChange: () => {
                    },
                    className: "w-5 h-5 cursor-pointer",
                    style: {
                      accentColor: el.checkboxColor || "#3b82f6"
                    }
                  }
                ),
                /* @__PURE__ */ jsxRuntime2.jsx(
                  "span",
                  {
                    style: {
                      color: el.labelColor || "#f1f5f9",
                      fontSize: \`calc(var(--font-scale, 1) * \${((_v = el.font) == null ? void 0 : _v.size) || 16}px)\`,
                      fontFamily: ((_w = el.font) == null ? void 0 : _w.family) || "Inter, system-ui, sans-serif",
                      fontWeight: ((_x = el.font) == null ? void 0 : _x.weight) || "normal",
                      fontStyle: ((_y = el.font) == null ? void 0 : _y.italic) ? "italic" : "normal",
                      cursor: "pointer",
                      userSelect: "none"
                    },
                    children: el.label
                  }
                )
              ]
            },
            el.id
          );
        }
        case UIElementType.Customizer: {
          const el = element;
          const character = project2.characters[el.characterId];
          if (!character) return null;
          const fallbackExpr = el.expressionId && character.expressions[el.expressionId] || Object.values(character.expressions)[0] || null;
          const imageUrls = [];
          const videoUrls = [];
          let hasVideo = false, videoLoop = false;
          if (character.baseVideoUrl) {
            videoUrls.push(resolveFieldUrl(project2.id, character.baseVideoUrl) || character.baseVideoUrl);
            hasVideo = true;
            videoLoop = !!character.baseVideoLoop;
          } else if (character.baseImageUrl) {
            imageUrls.push(resolveFieldUrl(project2.id, character.baseImageUrl) || character.baseImageUrl);
          }
          Object.entries(character.layers).forEach(([layerId, layer]) => {
            const cat = (el.categories || []).find((c) => c.layerId === layerId);
            let assetId = null;
            if (cat) assetId = String(variables2[cat.variableId] ?? "") || null;
            if (!assetId && fallbackExpr) assetId = fallbackExpr.layerConfiguration[layerId] || null;
            const asset = assetId ? layer.assets[assetId] : null;
            if (asset == null ? void 0 : asset.videoUrl) {
              videoUrls.push(resolveFieldUrl(project2.id, asset.videoUrl) || asset.videoUrl);
              hasVideo = true;
              videoLoop = videoLoop || !!asset.loop;
            } else if (asset == null ? void 0 : asset.imageUrl) {
              imageUrls.push(resolveFieldUrl(project2.id, asset.imageUrl) || asset.imageUrl);
            }
          });
          const swatchSize = el.swatchSize ?? 48;
          const swatchGap = el.swatchGap ?? 6;
          const selColor = el.selectedColor || "#8a2be2";
          const arrowColor = el.arrowColor || "#ffffff";
          const arrowSize = el.arrowSize ?? 28;
          const arrowUrl = el.arrowImage ? getElementAssetUrl(el.arrowImage) : null;
          const buttonColor = el.buttonColor || "rgba(255,255,255,0.12)";
          const buttonTextColor = el.buttonTextColor || "#ffffff";
          const labelStyle = { ...fontSettingsToStyle(el.font), lineHeight: 1.2 };
          const assetUrlOf = (a) => a.imageUrl ? resolveFieldUrl(project2.id, a.imageUrl) || a.imageUrl : a.videoUrl ? resolveFieldUrl(project2.id, a.videoUrl) || a.videoUrl : null;
          const previewNode = /* @__PURE__ */ jsxRuntime2.jsx("div", { className: "relative w-full h-full", children: hasVideo && videoUrls.length > 0 ? videoUrls.map((u, i) => /* @__PURE__ */ jsxRuntime2.jsx("video", { src: u, autoPlay: true, muted: true, loop: videoLoop, playsInline: true, className: "absolute inset-0 w-full h-full object-contain", style: { zIndex: i } }, i)) : imageUrls.map((u, i) => /* @__PURE__ */ jsxRuntime2.jsx("img", { src: u, alt: "", className: "absolute inset-0 w-full h-full object-contain", style: { zIndex: i } }, i)) });
          const catIdxOfVar = /* @__PURE__ */ new Map();
          (el.categories || []).forEach((c, i) => catIdxOfVar.set(c.variableId, i));
          const catRefs = (el.categories || []).map((c) => {
            const refs = /* @__PURE__ */ new Set();
            const layer = character.layers[c.layerId];
            if (layer) Object.values(layer.assets).forEach((a) => {
              var _a3, _b3;
              (((_b3 = (_a3 = el.optionMeta) == null ? void 0 : _a3[a.id]) == null ? void 0 : _b3.conditions) || []).forEach((cond) => {
                const j = catIdxOfVar.get(cond.variableId);
                if (j !== void 0) refs.add(j);
              });
            });
            return refs;
          });
          const droppedVarsPerCat = (el.categories || []).map((_, i) => {
            var _a3;
            const dropped = /* @__PURE__ */ new Set();
            (_a3 = catRefs[i]) == null ? void 0 : _a3.forEach((j) => {
              var _a4;
              if (j > i && ((_a4 = catRefs[j]) == null ? void 0 : _a4.has(i))) dropped.add((el.categories || [])[j].variableId);
            });
            return dropped;
          });
          const conditionsFor = (catIdx, assetId) => {
            var _a3, _b3;
            const conds = (_b3 = (_a3 = el.optionMeta) == null ? void 0 : _a3[assetId]) == null ? void 0 : _b3.conditions;
            if (!(conds == null ? void 0 : conds.length)) return conds;
            const dropped = droppedVarsPerCat[catIdx];
            if (!dropped || dropped.size === 0) return conds;
            return conds.filter((c) => !dropped.has(c.variableId));
          };
          const processOptions = (cat) => {
            const layer = character.layers[cat.layerId];
            const catIdx = catIdxOfVar.get(cat.variableId) ?? -1;
            if (!layer) return [];
            return Object.values(layer.assets).map((a) => {
              var _a3;
              const meta = (_a3 = el.optionMeta) == null ? void 0 : _a3[a.id];
              const conds = conditionsFor(catIdx, a.id);
              const ok = !(conds == null ? void 0 : conds.length) || evaluateConditions2(conds, variables2);
              const locked = !ok && (meta == null ? void 0 : meta.whenUnmet) === "lock";
              const hidden = !ok && ((meta == null ? void 0 : meta.whenUnmet) ?? "hide") === "hide";
              const swatchUrl = (meta == null ? void 0 : meta.swatchImage) ? getElementAssetUrl(meta.swatchImage) : assetUrlOf(a);
              return { a, locked, hidden, swatchUrl };
            }).filter((x) => !x.hidden);
          };
          const pickAndHeal = (srcCat, id) => {
            onVariableChange == null ? void 0 : onVariableChange(srcCat.variableId, id);
            const next = { ...variables2, [srcCat.variableId]: id };
            (el.categories || []).forEach((c, cIdx) => {
              if (c.variableId === srcCat.variableId) return;
              const layer = character.layers[c.layerId];
              if (!layer) return;
              const isHidden = (assetId) => {
                var _a3;
                const m = (_a3 = el.optionMeta) == null ? void 0 : _a3[assetId];
                const conds = conditionsFor(cIdx, assetId);
                return !!(conds == null ? void 0 : conds.length) && !evaluateConditions2(conds, next) && ((m == null ? void 0 : m.whenUnmet) ?? "hide") === "hide";
              };
              const cur = String(next[c.variableId] ?? "");
              if (!cur || !layer.assets[cur] || !isHidden(cur)) return;
              const firstVisible = Object.values(layer.assets).find((a) => !isHidden(a.id));
              if (firstVisible) {
                next[c.variableId] = firstVisible.id;
                onVariableChange == null ? void 0 : onVariableChange(c.variableId, firstVisible.id);
              }
            });
          };
          const renderPicker = (cat, current) => {
            const opts = processOptions(cat);
            const selectable = opts.filter((x) => !x.locked);
            const pstyle = cat.pickerStyle || "swatches";
            const pick = (id) => pickAndHeal(cat, id);
            if (pstyle === "arrows") {
              const idx = selectable.findIndex((x) => x.a.id === current);
              const cycle = (dir) => {
                const n = selectable.length;
                if (!n) return;
                const ni = (((idx < 0 ? 0 : idx) + dir) % n + n) % n;
                pick(selectable[ni].a.id);
              };
              const cur = idx >= 0 ? selectable[idx] : selectable[0];
              const arrowBtn = (dir, flip) => /* @__PURE__ */ jsxRuntime2.jsx("button", { onClick: () => cycle(dir), style: { flexShrink: 0, cursor: "pointer", background: "transparent", border: "none", padding: 4 }, children: arrowUrl ? /* @__PURE__ */ jsxRuntime2.jsx("img", { src: arrowUrl, alt: "", style: { width: arrowSize, height: arrowSize, objectFit: "contain", transform: flip ? "scaleX(-1)" : void 0 } }) : /* @__PURE__ */ jsxRuntime2.jsx("span", { style: { fontSize: arrowSize, lineHeight: 1, color: arrowColor }, children: flip ? "◀" : "▶" }) });
              return /* @__PURE__ */ jsxRuntime2.jsxs("div", { style: { display: "flex", alignItems: "center", gap: 8 }, children: [
                arrowBtn(-1, true),
                /* @__PURE__ */ jsxRuntime2.jsxs("div", { style: { flex: 1, minWidth: 0, textAlign: "center", display: "flex", flexDirection: "column", alignItems: "center", gap: 2 }, children: [
                  (cur == null ? void 0 : cur.swatchUrl) && /* @__PURE__ */ jsxRuntime2.jsx("div", { style: { width: swatchSize, height: swatchSize }, children: cur.a.videoUrl ? /* @__PURE__ */ jsxRuntime2.jsx("video", { src: cur.swatchUrl, muted: true, loop: true, playsInline: true, className: "w-full h-full object-contain" }) : /* @__PURE__ */ jsxRuntime2.jsx("img", { src: cur.swatchUrl, alt: "", className: "w-full h-full object-contain" }) }),
                  /* @__PURE__ */ jsxRuntime2.jsx("div", { style: { ...labelStyle }, className: "truncate w-full", children: (cur == null ? void 0 : cur.a.name) || "" })
                ] }),
                arrowBtn(1, false)
              ] });
            }
            if (pstyle === "dropdown") {
              return /* @__PURE__ */ jsxRuntime2.jsx("select", { value: current, onChange: (e) => pick(e.target.value), style: { width: "100%", padding: "4px 6px", borderRadius: 6, background: "rgba(0,0,0,0.35)", color: buttonTextColor, border: \`1px solid \${el.borderColor || "rgba(255,255,255,0.2)"}\` }, children: opts.map((x) => /* @__PURE__ */ jsxRuntime2.jsxs("option", { value: x.a.id, disabled: x.locked, children: [
                x.a.name,
                x.locked ? " 🔒" : ""
              ] }, x.a.id)) });
            }
            if (pstyle === "buttons") {
              return /* @__PURE__ */ jsxRuntime2.jsx("div", { style: { display: "flex", flexWrap: "wrap", gap: 6 }, children: opts.map((x) => {
                const sel = current === x.a.id;
                return /* @__PURE__ */ jsxRuntime2.jsxs("button", { disabled: x.locked, onClick: () => !x.locked && pick(x.a.id), style: { ...labelStyle, cursor: x.locked ? "not-allowed" : "pointer", opacity: x.locked ? 0.5 : 1, padding: "4px 10px", borderRadius: 6, background: sel ? selColor : buttonColor, color: buttonTextColor, border: "none" }, children: [
                  x.a.name,
                  x.locked ? " 🔒" : ""
                ] }, x.a.id);
              }) });
            }
            return /* @__PURE__ */ jsxRuntime2.jsx("div", { style: { display: "flex", flexWrap: "wrap", gap: swatchGap }, children: opts.map((x) => {
              const sel = current === x.a.id;
              return /* @__PURE__ */ jsxRuntime2.jsxs(
                "div",
                {
                  onClick: () => !x.locked && pick(x.a.id),
                  title: x.a.name,
                  style: { position: "relative", width: swatchSize, height: swatchSize, flexShrink: 0, cursor: x.locked ? "not-allowed" : "pointer", opacity: x.locked ? 0.55 : 1, borderRadius: 6, overflow: "hidden", background: "rgba(0,0,0,0.3)", boxShadow: sel ? \`0 0 0 3px \${selColor}\` : "inset 0 0 0 1px rgba(255,255,255,0.15)" },
                  children: [
                    x.a.videoUrl ? /* @__PURE__ */ jsxRuntime2.jsx("video", { src: x.swatchUrl || void 0, muted: true, loop: true, playsInline: true, className: "w-full h-full object-contain" }) : x.swatchUrl ? /* @__PURE__ */ jsxRuntime2.jsx("img", { src: x.swatchUrl, alt: x.a.name, className: "w-full h-full object-contain" }) : /* @__PURE__ */ jsxRuntime2.jsx("div", { className: "w-full h-full" }),
                    x.locked && /* @__PURE__ */ jsxRuntime2.jsx("div", { style: { position: "absolute", inset: 0, display: "flex", alignItems: "center", justifyContent: "center", fontSize: Math.min(swatchSize * 0.5, 22) }, children: "🔒" })
                  ]
                },
                x.a.id
              );
            }) });
          };
          const randomizeLook = () => {
            (el.categories || []).forEach((cat) => {
              const layer = character.layers[cat.layerId];
              if (!layer) return;
              const opts = Object.values(layer.assets).filter((a) => {
                var _a3, _b3;
                const m = (_a3 = el.optionMeta) == null ? void 0 : _a3[a.id];
                return !((_b3 = m == null ? void 0 : m.conditions) == null ? void 0 : _b3.length) || evaluateConditions2(m.conditions, variables2);
              });
              if (opts.length) onVariableChange == null ? void 0 : onVariableChange(cat.variableId, opts[Math.floor(Math.random() * opts.length)].id);
            });
          };
          const resetLook = () => {
            (el.categories || []).forEach((cat) => {
              const v = project2.variables[cat.variableId];
              const layer = character.layers[cat.layerId];
              const fallback = layer ? Object.keys(layer.assets)[0] : "";
              const def = v && v.defaultValue !== void 0 && v.defaultValue !== "" ? String(v.defaultValue) : fallback || "";
              onVariableChange == null ? void 0 : onVariableChange(cat.variableId, def);
            });
          };
          const pickersNode = /* @__PURE__ */ jsxRuntime2.jsxs("div", { className: "w-full h-full overflow-y-auto", style: { display: "flex", flexDirection: "column", gap: 10, padding: 8 }, children: [
            (el.categories || []).map((cat) => {
              const layer = character.layers[cat.layerId];
              if (!layer) return null;
              const current = String(variables2[cat.variableId] ?? "");
              return /* @__PURE__ */ jsxRuntime2.jsxs("div", { children: [
                el.showLabels !== false && /* @__PURE__ */ jsxRuntime2.jsx("div", { style: { ...labelStyle, marginBottom: 4 }, children: cat.label || layer.name }),
                renderPicker(cat, current)
              ] }, cat.layerId);
            }),
            (el.showRandomize || el.showReset) && (el.categories || []).length > 0 && /* @__PURE__ */ jsxRuntime2.jsxs("div", { style: { display: "flex", gap: 6, flexWrap: "wrap", marginTop: 4 }, children: [
              el.showRandomize && /* @__PURE__ */ jsxRuntime2.jsx("button", { onClick: randomizeLook, style: { ...labelStyle, cursor: "pointer", padding: "4px 12px", borderRadius: 6, background: buttonColor, color: buttonTextColor, border: "none" }, children: el.randomizeLabel || "Randomize" }),
              el.showReset && /* @__PURE__ */ jsxRuntime2.jsx("button", { onClick: resetLook, style: { ...labelStyle, cursor: "pointer", padding: "4px 12px", borderRadius: 6, background: buttonColor, color: buttonTextColor, border: "none" }, children: el.resetLabel || "Reset" })
            ] })
          ] });
          if (el.layout === "free" && el.previewRect) {
            const pr = el.previewRect;
            const kr = el.pickersRect;
            const previewFrameUrl = el.previewBackgroundImage ? getElementAssetUrl(el.previewBackgroundImage) : null;
            const previewBoxStyle = {
              position: "absolute",
              left: \`\${pr.x}%\`,
              top: \`\${pr.y}%\`,
              width: \`\${pr.width}%\`,
              height: \`\${pr.height}%\`,
              pointerEvents: "none",
              overflow: "hidden",
              ...el.previewBackgroundColor ? { background: el.previewBackgroundColor } : {},
              ...previewFrameUrl ? { backgroundImage: \`url(\${previewFrameUrl})\`, backgroundSize: "cover", backgroundPosition: "center" } : {},
              ...el.previewBorderColor ? { border: \`1px solid \${el.previewBorderColor}\` } : {},
              ...el.previewBorderRadius !== void 0 ? { borderRadius: el.previewBorderRadius } : {}
            };
            const pickersFrameUrl = el.backgroundImage ? getElementAssetUrl(el.backgroundImage) : null;
            const pickersBoxStyle = {
              position: "absolute",
              left: \`\${(kr == null ? void 0 : kr.x) ?? 0}%\`,
              top: \`\${(kr == null ? void 0 : kr.y) ?? 0}%\`,
              width: \`\${(kr == null ? void 0 : kr.width) ?? 0}%\`,
              height: \`\${(kr == null ? void 0 : kr.height) ?? 0}%\`,
              pointerEvents: "auto",
              overflow: "hidden",
              ...el.hidePickersPanel ? {} : {
                background: el.backgroundColor || void 0,
                ...pickersFrameUrl ? { backgroundImage: \`url(\${pickersFrameUrl})\`, backgroundSize: "cover", backgroundPosition: "center" } : {},
                ...el.borderColor ? { border: \`1px solid \${el.borderColor}\` } : {},
                borderRadius: el.borderRadius ?? 8
              }
            };
            return /* @__PURE__ */ jsxRuntime2.jsxs("div", { style: { position: "absolute", left: 0, top: 0, width: "100%", height: "100%", zIndex: style.zIndex, opacity: style.opacity, pointerEvents: "none" }, children: [
              /* @__PURE__ */ jsxRuntime2.jsx("div", { style: previewBoxStyle, children: /* @__PURE__ */ jsxRuntime2.jsx("div", { className: "relative w-full h-full", children: hasVideo && videoUrls.length > 0 ? videoUrls.map((u, i) => /* @__PURE__ */ jsxRuntime2.jsx("video", { src: u, autoPlay: true, muted: true, loop: videoLoop, playsInline: true, className: "absolute inset-0 w-full h-full object-contain", style: { zIndex: i, objectPosition: "bottom" } }, i)) : imageUrls.map((u, i) => /* @__PURE__ */ jsxRuntime2.jsx("img", { src: u, alt: "", className: "absolute inset-0 w-full h-full object-contain", style: { zIndex: i, objectPosition: "bottom" } }, i)) }) }),
              kr && /* @__PURE__ */ jsxRuntime2.jsx("div", { style: pickersBoxStyle, children: pickersNode })
            ] }, el.id);
          }
          const layout = el.layout === "free" ? "preview-left" : el.layout || "preview-left";
          const pv = \`\${el.previewPercent ?? 45}%\`;
          const frameUrl = el.backgroundImage ? getElementAssetUrl(el.backgroundImage) : null;
          const containerStyle = {
            ...style,
            background: el.backgroundColor || void 0,
            ...frameUrl ? { backgroundImage: \`url(\${frameUrl})\`, backgroundSize: "cover", backgroundPosition: "center" } : {},
            ...el.borderColor ? { border: \`1px solid \${el.borderColor}\` } : {},
            borderRadius: el.borderRadius ?? 8,
            overflow: "hidden",
            display: "flex",
            flexDirection: layout === "preview-top" ? "column" : "row"
          };
          const previewWrap = /* @__PURE__ */ jsxRuntime2.jsx("div", { style: layout === "preview-top" ? { height: pv, width: "100%", flexShrink: 0 } : { width: pv, height: "100%", flexShrink: 0 }, children: previewNode });
          const pickersWrap = /* @__PURE__ */ jsxRuntime2.jsx("div", { style: { flex: 1, minWidth: 0, minHeight: 0 }, children: pickersNode });
          return /* @__PURE__ */ jsxRuntime2.jsx("div", { style: containerStyle, children: layout === "preview-right" ? /* @__PURE__ */ jsxRuntime2.jsxs(jsxRuntime2.Fragment, { children: [
            pickersWrap,
            previewWrap
          ] }) : /* @__PURE__ */ jsxRuntime2.jsxs(jsxRuntime2.Fragment, { children: [
            previewWrap,
            pickersWrap
          ] }) }, el.id);
        }
        case UIElementType.AssetCycler: {
          const el = element;
          return /* @__PURE__ */ jsxRuntime2.jsx(
            AssetCyclerElement,
            {
              element: el,
              style,
              variables: variables2,
              onVariableChange,
              project: project2
            },
            el.id
          );
        }
        case UIElementType.CGGallery: {
          const el = element;
          const galleryEntries = Object.values(((_z = project2.cgGallery) == null ? void 0 : _z.entries) || {});
          const filteredEntries = el.categoryFilter ? galleryEntries.filter((e) => e.category === el.categoryFilter) : galleryEntries;
          filteredEntries.sort((a, b) => (a.order ?? 0) - (b.order ?? 0) || a.name.localeCompare(b.name));
          const galleryIsFree = el.slotLayout === "free" && !!el.slotRects && el.slotRects.length > 0;
          const galleryWrapperStyle = galleryIsFree ? { position: "absolute", left: 0, top: 0, width: "100%", height: "100%", zIndex: style.zIndex, opacity: style.opacity, pointerEvents: "none" } : style;
          return /* @__PURE__ */ jsxRuntime2.jsx("div", { style: galleryWrapperStyle, children: /* @__PURE__ */ jsxRuntime2.jsx(
            CGGalleryGridElement,
            {
              element: el,
              entries: filteredEntries,
              variables: variables2,
              project: project2,
              assetResolver,
              viewerPortalRef: screenRootRef
            }
          ) }, el.id);
        }
        case UIElementType.Inventory: {
          const el = element;
          const boundCollection = el.collectionId ? (_A = project2.itemCollections) == null ? void 0 : _A[el.collectionId] : void 0;
          const isPlayerInv = !boundCollection || boundCollection.tracksOwnedItems && boundCollection.entries.length === 0;
          const invUI = project2.ui || {};
          const catOrder = invUI.inventoryCategoryOrder || [];
          const catRank = (c) => {
            const i = catOrder.indexOf(c || "");
            return c && i >= 0 ? i : catOrder.length;
          };
          const sortMode = invUI.inventoryDefaultSort || "manual";
          const byCategory = (a, b) => {
            const ca = a.category || "", cb = b.category || "";
            if (!ca !== !cb) return ca ? -1 : 1;
            const ra = catRank(ca), rb = catRank(cb);
            if (ra !== rb) return ra - rb;
            if (ca !== cb) return ca.localeCompare(cb);
            return a.name.localeCompare(b.name);
          };
          const sortItems = (arr) => [...arr].sort((a, b) => sortMode === "alpha" ? a.name.localeCompare(b.name) : sortMode === "category" ? byCategory(a, b) : (a.order ?? 0) - (b.order ?? 0) || a.name.localeCompare(b.name));
          const allOwnedItems = () => sortItems(Object.values(project2.items || {}));
          const invItems = !boundCollection ? allOwnedItems() : boundCollection.tracksOwnedItems && boundCollection.entries.length === 0 ? allOwnedItems() : boundCollection.entries.map((e) => {
            var _a3;
            const it = (_a3 = project2.items) == null ? void 0 : _a3[e.itemId];
            return it ? { ...it, countVariableId: e.countVariableId } : null;
          }).filter((x) => !!x);
          const isShopList = !!boundCollection && !boundCollection.tracksOwnedItems;
          const effEl = isPlayerInv ? {
            ...el,
            hideUnowned: el.hideUnowned ?? invUI.inventoryDefaultHideUnowned,
            showNames: el.showNames ?? invUI.inventoryDefaultShowNames,
            showQuantity: el.showQuantity ?? invUI.inventoryDefaultShowQuantity,
            columns: el.columns ?? invUI.inventoryDefaultColumns ?? 4
          } : el;
          const gridEl = isShopList ? { ...effEl, hideUnowned: false } : effEl;
          const groupByCategory = isPlayerInv && !!invUI.inventoryGroupByCategory;
          const invGrid = /* @__PURE__ */ jsxRuntime2.jsx(
            InventoryGridElement,
            {
              element: gridEl,
              items: invItems,
              variables: variables2,
              project: project2,
              assetResolver,
              onAction,
              onCommitVariables: onCommitVariables2,
              inventorySlots,
              onReorderSlots,
              selectedItemId,
              selectedElementId,
              onSelectItem,
              groupByCategory,
              categoryOrder: catOrder,
              autoSort: isPlayerInv && sortMode !== "manual",
              style
            }
          );
          return gridEl.slotLayout === "free" && ((_B = gridEl.slotRects) == null ? void 0 : _B.length) ? /* @__PURE__ */ jsxRuntime2.jsx(React2.Fragment, { children: invGrid }, el.id) : /* @__PURE__ */ jsxRuntime2.jsx("div", { style, children: invGrid }, el.id);
        }
        case UIElementType.Meter: {
          const el = element;
          const boundVar = el.variableId ? project2.variables[el.variableId] : void 0;
          const raw = Number((el.variableId ? variables2[el.variableId] : void 0) ?? (boundVar == null ? void 0 : boundVar.defaultValue) ?? 0);
          const min = el.minValue ?? (boundVar == null ? void 0 : boundVar.min) ?? 0;
          const max = el.maxValue ?? (boundVar == null ? void 0 : boundVar.max) ?? 100;
          const pct = Math.max(0, Math.min(1, (raw - min) / (max - min || 1)));
          const dir = el.direction || "ltr";
          const cut = (1 - pct) * 100;
          const clipPath = dir === "rtl" ? \`inset(0 0 0 \${cut}%)\` : dir === "up" ? \`inset(\${cut}% 0 0 0)\` : \`inset(0 \${cut}% 0 0)\`;
          const fillImageUrl = el.fillImage ? getElementAssetUrl(el.fillImage) : null;
          const bgImageUrl = el.backgroundImage ? getElementAssetUrl(el.backgroundImage) : null;
          const fillBackground = fillImageUrl ? void 0 : el.fillColorEnd ? \`linear-gradient(\${dir === "up" ? "0deg" : "90deg"}, \${el.fillColor || "#a78bfa"}, \${el.fillColorEnd})\` : el.fillColor || "#a78bfa";
          const valueText = el.valueFormat === "percent" ? \`\${Math.round(pct * 100)}%\` : el.valueFormat === "valueMax" ? \`\${raw}/\${max}\` : \`\${raw}\`;
          const radius = el.borderRadius ?? 6;
          const prevRaw = meterPrevRef.current[el.id];
          meterPrevRef.current[el.id] = raw;
          const changeDir = prevRaw === void 0 || prevRaw === raw ? "none" : raw > prevRaw ? "up" : "down";
          const lowThresh = (el.lowThresholdPct ?? 25) / 100;
          const lowFx = el.lowAnimations || [];
          const lowActive = lowFx.length > 0 && !!el.variableId && pct <= lowThresh;
          const changeAnim = changeDir === "up" ? el.changeAnimationUp : changeDir === "down" ? el.changeAnimationDown : void 0;
          const alignToFlex = (a) => a === "center" ? "center" : a === "right" || a === "bottom" ? "flex-end" : "flex-start";
          const wrapFx = (inner, opts) => {
            const whole = (opts == null ? void 0 : opts.animateWhole) !== false;
            let node = /* @__PURE__ */ jsxRuntime2.jsx("div", { style: { width: "100%", height: "100%", display: "flex", flexDirection: "column", gap: 2, justifyContent: alignToFlex(el.alignY) }, children: inner });
            if (whole && lowActive) {
              if (lowFx.includes("wave")) node = /* @__PURE__ */ jsxRuntime2.jsx("div", { style: { width: "100%", height: "100%", animation: "vnMeterWave 1.3s ease-in-out infinite", transformOrigin: "center" }, children: node });
              if (lowFx.includes("pulse")) node = /* @__PURE__ */ jsxRuntime2.jsx("div", { style: { width: "100%", height: "100%", animation: "vnMeterPulse 0.9s ease-in-out infinite", transformOrigin: "center" }, children: node });
              if (lowFx.includes("shake")) node = /* @__PURE__ */ jsxRuntime2.jsx("div", { style: { width: "100%", height: "100%", animation: "vnMeterShake 0.4s ease-in-out infinite", transformOrigin: "center" }, children: node });
            }
            if (whole && changeDir !== "none" && changeAnim && changeAnim !== "none" && changeAnim !== "flash") {
              const kf = changeAnim === "pop" ? "vnMeterPop 0.4s ease" : changeAnim === "shake" ? "vnMeterChangeShake 0.4s ease" : changeAnim === "wave" ? "vnMeterChangeWave 0.5s ease" : "";
              if (kf) node = /* @__PURE__ */ jsxRuntime2.jsx("div", { style: { width: "100%", height: "100%", animation: kf, transformOrigin: "center" }, children: node }, \`mfx-\${raw}\`);
            }
            const lowFlash = whole && lowActive && lowFx.includes("flash") ? /* @__PURE__ */ jsxRuntime2.jsx("div", { style: { position: "absolute", inset: 0, background: el.lowFlashColor || "#ef4444", borderRadius: radius, pointerEvents: "none", animation: "vnMeterFlashOverlay 0.9s ease-in-out infinite" } }, "mlowflash") : null;
            const changeFlash = whole && changeDir !== "none" && changeAnim === "flash" ? /* @__PURE__ */ jsxRuntime2.jsx("div", { style: { position: "absolute", inset: 0, background: changeDir === "up" ? el.changeFlashColorUp || "#4ade80" : el.changeFlashColorDown || "#ef4444", borderRadius: radius, pointerEvents: "none", animation: "vnMeterFlashOverlay 0.5s ease" } }, \`mchgflash-\${raw}\`) : null;
            return /* @__PURE__ */ jsxRuntime2.jsx("div", { style: { ...style, overflow: "visible" }, children: /* @__PURE__ */ jsxRuntime2.jsxs("div", { style: { position: "relative", width: "100%", height: "100%" }, children: [
              node,
              lowFlash,
              changeFlash
            ] }) }, el.id);
          };
          const animateIcon = (child) => {
            let node = child;
            if (lowActive) {
              if (lowFx.includes("wave")) node = /* @__PURE__ */ jsxRuntime2.jsx("div", { style: { display: "inline-block", animation: "vnMeterWave 1.3s ease-in-out infinite", transformOrigin: "center" }, children: node });
              if (lowFx.includes("pulse")) node = /* @__PURE__ */ jsxRuntime2.jsx("div", { style: { display: "inline-block", animation: "vnMeterPulse 0.9s ease-in-out infinite", transformOrigin: "center" }, children: node });
              if (lowFx.includes("shake")) node = /* @__PURE__ */ jsxRuntime2.jsx("div", { style: { display: "inline-block", animation: "vnMeterShake 0.4s ease-in-out infinite", transformOrigin: "center" }, children: node });
              if (lowFx.includes("flash")) node = /* @__PURE__ */ jsxRuntime2.jsx("div", { style: { display: "inline-block", ["--glow"]: el.lowFlashColor || "#ef4444", animation: "vnMeterGlow 0.9s ease-in-out infinite" }, children: node });
            }
            if (changeDir !== "none" && changeAnim && changeAnim !== "none") {
              if (changeAnim === "flash") {
                node = /* @__PURE__ */ jsxRuntime2.jsx("div", { style: { display: "inline-block", ["--glow"]: changeDir === "up" ? el.changeFlashColorUp || "#4ade80" : el.changeFlashColorDown || "#ef4444", animation: "vnMeterGlowOnce 0.5s ease" }, children: node }, \`icf-\${raw}\`);
              } else {
                const kf = changeAnim === "pop" ? "vnMeterPop 0.4s ease" : changeAnim === "shake" ? "vnMeterChangeShake 0.4s ease" : changeAnim === "wave" ? "vnMeterChangeWave 0.5s ease" : "";
                if (kf) node = /* @__PURE__ */ jsxRuntime2.jsx("div", { style: { display: "inline-block", animation: kf, transformOrigin: "center" }, children: node }, \`icx-\${raw}\`);
              }
            }
            return node;
          };
          if (el.style === "icons") {
            const n = Math.max(1, el.iconCount ?? 3);
            const stepSize = el.iconStep === "quarter" ? 0.25 : el.iconStep === "half" ? 0.5 : 1;
            const fullUrl = el.iconImage ? getElementAssetUrl(el.iconImage) : null;
            const emptyUrl = el.iconEmptyImage ? getElementAssetUrl(el.iconEmptyImage) : null;
            const iconSize = el.iconSize ?? 24;
            const iconGap = el.iconGap ?? 4;
            const filledUnits = pct * n;
            const labelEl = el.showLabel ? /* @__PURE__ */ jsxRuntime2.jsx("div", { style: { ...fontSettingsToStyle(el.labelFont), lineHeight: 1.1, flexShrink: 0 }, children: el.label || (boundVar == null ? void 0 : boundVar.name) || "" }) : null;
            const fracs = Array.from({ length: n }).map((_, i) => {
              const f = Math.max(0, Math.min(1, filledUnits - i));
              return Math.round(f / stepSize) * stepSize;
            });
            let edgeIdx = 0;
            for (let i = 0; i < n; i++) if (fracs[i] > 0) edgeIdx = i;
            return wrapFx(
              /* @__PURE__ */ jsxRuntime2.jsxs(jsxRuntime2.Fragment, { children: [
                labelEl,
                /* @__PURE__ */ jsxRuntime2.jsxs("div", { style: { display: "flex", flexWrap: "wrap", gap: iconGap, alignItems: "center", justifyContent: alignToFlex(el.alignX) }, children: [
                  Array.from({ length: n }).map((_, i) => {
                    const frac = fracs[i];
                    const heart = /* @__PURE__ */ jsxRuntime2.jsxs("div", { style: { position: "relative", width: iconSize, height: iconSize, flexShrink: 0 }, children: [
                      emptyUrl ? /* @__PURE__ */ jsxRuntime2.jsx("img", { src: emptyUrl, alt: "", style: { position: "absolute", inset: 0, width: "100%", height: "100%", objectFit: "contain" } }) : fullUrl ? /* @__PURE__ */ jsxRuntime2.jsx("img", { src: fullUrl, alt: "", style: { position: "absolute", inset: 0, width: "100%", height: "100%", objectFit: "contain", opacity: 0.25, filter: "grayscale(1)" } }) : /* @__PURE__ */ jsxRuntime2.jsx("div", { style: { position: "absolute", inset: 0, borderRadius: 4, border: \`1px solid \${el.borderColor || el.fillColor || "#a78bfa"}\`, opacity: 0.3 } }),
                      frac > 0 && (fullUrl ? /* @__PURE__ */ jsxRuntime2.jsx("img", { src: fullUrl, alt: "", style: { position: "absolute", inset: 0, width: "100%", height: "100%", objectFit: "contain", clipPath: \`inset(0 \${(1 - frac) * 100}% 0 0)\`, transition: "clip-path 0.3s ease" } }) : /* @__PURE__ */ jsxRuntime2.jsx("div", { style: { position: "absolute", inset: 0, borderRadius: 4, background: fillBackground, clipPath: \`inset(0 \${(1 - frac) * 100}% 0 0)\`, transition: "clip-path 0.3s ease" } }))
                    ] });
                    return /* @__PURE__ */ jsxRuntime2.jsx(React2.Fragment, { children: i === edgeIdx ? animateIcon(heart) : heart }, i);
                  }),
                  el.showValue && /* @__PURE__ */ jsxRuntime2.jsx("div", { style: { ...fontSettingsToStyle(el.valueFont), marginLeft: 4, textShadow: "0 1px 2px rgba(0,0,0,0.6)" }, children: valueText })
                ] })
              ] }),
              { animateWhole: false }
            );
          }
          if (el.style === "battery" || el.style === "segments") {
            const fc = el.borderColor || el.fillColor || "#a78bfa";
            const labelEl = el.showLabel ? /* @__PURE__ */ jsxRuntime2.jsx("div", { style: { ...fontSettingsToStyle(el.labelFont), lineHeight: 1.1, flexShrink: 0 }, children: el.label || (boundVar == null ? void 0 : boundVar.name) || "" }) : null;
            const valueEl = el.showValue ? /* @__PURE__ */ jsxRuntime2.jsx("div", { style: { position: "absolute", inset: 0, display: "flex", alignItems: "center", justifyContent: "center", ...fontSettingsToStyle(el.valueFont), textShadow: "0 1px 2px rgba(0,0,0,0.6)" }, children: valueText }) : null;
            let body;
            if (el.style === "battery") {
              body = /* @__PURE__ */ jsxRuntime2.jsxs("div", { style: { display: "flex", alignItems: "stretch", flex: 1, minHeight: 6, gap: 1 }, children: [
                /* @__PURE__ */ jsxRuntime2.jsxs("div", { style: { position: "relative", flex: 1, borderRadius: Math.min(radius, 4), overflow: "hidden", border: \`2px solid \${fc}\`, backgroundColor: el.backgroundColor || "rgba(0,0,0,0.35)" }, children: [
                  /* @__PURE__ */ jsxRuntime2.jsx("div", { style: { position: "absolute", top: 0, bottom: 0, left: 0, width: \`\${pct * 100}%\`, background: fillBackground, transition: "width 0.3s ease" } }),
                  valueEl
                ] }),
                /* @__PURE__ */ jsxRuntime2.jsx("div", { style: { alignSelf: "center", width: 4, height: "45%", minHeight: 4, borderRadius: "0 2px 2px 0", backgroundColor: fc } })
              ] });
            } else {
              const n = Math.max(1, el.segmentCount ?? 10);
              const filled = Math.round(pct * n);
              body = /* @__PURE__ */ jsxRuntime2.jsxs("div", { style: { position: "relative", display: "flex", flex: 1, minHeight: 6, gap: el.segmentGap ?? 2 }, children: [
                Array.from({ length: n }).map((_, i) => /* @__PURE__ */ jsxRuntime2.jsx("div", { style: { flex: 1, borderRadius: Math.min(radius, 4), border: \`1px solid \${fc}\`, background: i < filled ? fillBackground : el.backgroundColor || "rgba(0,0,0,0.35)", transition: "background 0.3s ease" } }, i)),
                valueEl
              ] });
            }
            return wrapFx(
              /* @__PURE__ */ jsxRuntime2.jsxs(jsxRuntime2.Fragment, { children: [
                labelEl,
                body
              ] })
            );
          }
          return wrapFx(
            /* @__PURE__ */ jsxRuntime2.jsxs(jsxRuntime2.Fragment, { children: [
              el.showLabel && /* @__PURE__ */ jsxRuntime2.jsx("div", { style: { ...fontSettingsToStyle(el.labelFont), lineHeight: 1.1, flexShrink: 0 }, children: el.label || (boundVar == null ? void 0 : boundVar.name) || "" }),
              /* @__PURE__ */ jsxRuntime2.jsxs("div", { style: {
                position: "relative",
                flex: 1,
                minHeight: 4,
                overflow: "hidden",
                borderRadius: radius,
                backgroundColor: el.backgroundColor || "rgba(0,0,0,0.4)",
                ...bgImageUrl ? { backgroundImage: \`url(\${bgImageUrl})\`, backgroundSize: "cover", backgroundPosition: "center" } : {},
                ...el.borderColor ? { border: \`1px solid \${el.borderColor}\` } : {}
              }, children: [
                /* @__PURE__ */ jsxRuntime2.jsx("div", { style: {
                  position: "absolute",
                  inset: 0,
                  clipPath,
                  background: fillBackground,
                  ...fillImageUrl ? { backgroundImage: \`url(\${fillImageUrl})\`, backgroundSize: "cover", backgroundPosition: "center" } : {},
                  borderRadius: radius,
                  // Tween the fill colour too when an appearance state with a transition is active
                  // (e.g. HP bar eases green→red). Solid fills animate via background-color.
                  transition: \`clip-path 0.3s ease\${(activeState == null ? void 0 : activeState.transitionMs) ? \`, background-color \${activeState.transitionMs}ms ease, background \${activeState.transitionMs}ms ease\` : ""}\`
                } }),
                el.showValue && /* @__PURE__ */ jsxRuntime2.jsx("div", { style: {
                  position: "absolute",
                  inset: 0,
                  display: "flex",
                  alignItems: "center",
                  justifyContent: "center",
                  ...fontSettingsToStyle(el.valueFont),
                  textShadow: "0 1px 2px rgba(0,0,0,0.6)"
                }, children: valueText })
              ] })
            ] })
          );
        }
        case UIElementType.Custom: {
          const el = element;
          const def = pluginManager.getUIElementType(el.pluginType);
          if (!def) return null;
          const getVar = (nameOrId) => {
            let id = nameOrId;
            if (!project2.variables[id]) {
              const found = Object.values(project2.variables).find((v) => {
                var _a3;
                return ((_a3 = v.name) == null ? void 0 : _a3.toLowerCase()) === String(nameOrId).toLowerCase();
              });
              if (found) id = found.id;
            }
            return variables2[id];
          };
          let html = "";
          try {
            html = def.render(el.props || {}, { getVariable: getVar, isEditor: false });
          } catch (e) {
            console.error("[Custom element] render failed:", e);
          }
          return /* @__PURE__ */ jsxRuntime2.jsx("div", { style, dangerouslySetInnerHTML: { __html: html } }, el.id);
        }
        default:
          return null;
      }
    };
    const transitionType = isClosing ? screen.transitionOut || "fade" : screen.transitionIn || "fade";
    const duration = isClosing ? screen.transitionOutDuration ?? screen.transitionDuration ?? 300 : screen.transitionInDuration ?? screen.transitionDuration ?? 300;
    const screenTransitionStyle = {
      animation: transitionType !== "none" ? \`screenTransition\${transitionType}\${isClosing ? "Out" : ""} \${duration}ms \${transitionType === "crossfade" ? "linear" : "ease-out"} forwards\` : void 0
    };
    screen.showDialogue && variables;
    return /* @__PURE__ */ jsxRuntime2.jsxs(
      "div",
      {
        ref: screenRootRef,
        className: "absolute inset-0 w-full h-full",
        style: {
          isolation: "isolate",
          ...screenTransitionStyle,
          ...isPassThrough ? { pointerEvents: "none" } : {},
          // Pass-through HUDs normally sit below the dialogue box (z20) + choices (z30). When
          // \`hudAboveDialogue\` is set, lift this overlay above them (but below flash/history z50)
          // so its buttons are visible + clickable while dialogue/choices are on screen. Empty
          // areas stay pointer-events:none, so clicks there still fall through to advance dialogue.
          ...isPassThrough && screen.hudAboveDialogue ? { zIndex: 45 } : {},
          // A pausing overlay (modal-style) sits ABOVE the dialogue box + backdrop so it
          // reads as a popup over a frozen, dimmed scene.
          ...screen.pauseSceneWhileOpen ? { zIndex: 46 } : {}
        },
        children: [
          !isPassThrough && getBackgroundElement(),
          Object.values(screen.elements).map((element) => {
            const el = element;
            if (el.type === "HotSpot" || el.type === "draggableImageElement" || el.draggable === true) return null;
            return renderElement(element, variables, project, onCommitVariables);
          }),
          (Object.values(screen.elements || {}).some(
            (el) => el.type === "HotSpot" || el.type === "draggableImageElement" || el.draggable === true
          ) || !!screen.winCondition) && /* @__PURE__ */ jsxRuntime2.jsx(
            InteractiveRuntime,
            {
              screen,
              onAction,
              variables,
              onVariableChange,
              evaluateConditions: evaluateConditions2,
              assetResolver,
              playSound
            }
          )
        ]
      },
      screenId
    );
  });
  const InGameConfirmDialog = ({ type, settings: s, assetResolver, onConfirm, onCancel }) => {
    var _a;
    const CONFIRM_DEFAULTS = {
      quit: { title: "Quit Game", message: "Are you sure you want to quit?", confirm: "Quit", cancel: "Cancel" },
      newGame: { title: "Start New Game", message: "Any unsaved progress will be lost. Are you sure?", confirm: "New Game", cancel: "Cancel" },
      eraseSave: { title: "Erase Save", message: "Erase this save? This cannot be undone.", confirm: "Erase", cancel: "Cancel" }
    };
    const def = CONFIRM_DEFAULTS[type];
    const title = (s == null ? void 0 : s[\`\${type}Title\`]) || def.title;
    const message = (s == null ? void 0 : s[\`\${type}Message\`]) || def.message;
    const confirmLabel = (s == null ? void 0 : s[\`\${type}ConfirmLabel\`]) || def.confirm;
    const cancelLabel = (s == null ? void 0 : s[\`\${type}CancelLabel\`]) || def.cancel;
    const e = { ...s || {}, ...((_a = s == null ? void 0 : s.variants) == null ? void 0 : _a[type]) || {} };
    const bgColor = e.backgroundColor || "#0f172a";
    const bgOpacity = (e.backgroundOpacity ?? 92) / 100;
    const borderRadius = e.borderRadius ?? 12;
    const overlayColor = e.overlayColor || "rgba(0,0,0,0.75)";
    const confirmBtnColor = e.confirmButtonColor || "";
    const cancelBtnColor = e.cancelButtonColor || "#1e293b";
    const confirmHoverColor = e.confirmHoverColor || "";
    const cancelHoverColor = e.cancelHoverColor || "#334155";
    const btnBorderRadius = e.buttonBorderRadius ?? Math.max(borderRadius - 4, 4);
    const btnPad = e.buttonPadding ?? 8;
    const dialogPad = e.dialogPadding ?? 32;
    const titleStyle = e.titleFont ? fontSettingsToStyle(e.titleFont) : { fontSize: "1.25rem", fontWeight: 600, color: "#fff" };
    const messageStyle = e.messageFont ? fontSettingsToStyle(e.messageFont) : { fontSize: "0.95rem", color: "#cbd5e1" };
    const buttonStyle = e.buttonFont ? fontSettingsToStyle(e.buttonFont) : { fontSize: "0.95rem", fontWeight: 500, color: "#fff" };
    const resolveAsset = (asset) => (asset == null ? void 0 : asset.id) ? assetResolver(asset.id, "image") : null;
    const bgImageUrl = resolveAsset(e.backgroundImage);
    const borderImageUrl = resolveAsset(e.borderImage);
    const confirmBtnImgUrl = resolveAsset(e.confirmButtonImage);
    const cancelBtnImgUrl = resolveAsset(e.cancelButtonImage);
    const confirmHoverImgUrl = resolveAsset(e.confirmHoverImage);
    const cancelHoverImgUrl = resolveAsset(e.cancelHoverImage);
    const sizeMode = e.backgroundSizeMode || "stretch";
    const bgImageStyle = bgImageUrl ? {
      backgroundImage: \`url(\${bgImageUrl})\`,
      backgroundSize: sizeMode === "nine-slice" ? void 0 : sizeMode === "stretch" ? "100% 100%" : sizeMode,
      backgroundPosition: "center",
      backgroundRepeat: "no-repeat",
      ...sizeMode === "nine-slice" ? {
        borderImage: \`url(\${bgImageUrl}) \${e.backgroundSlice ?? 20} fill\`,
        borderImageWidth: \`\${e.backgroundSlice ?? 20}px\`
      } : {}
    } : {};
    const makeBtnImageStyle = (imgUrl) => {
      if (!imgUrl) return {};
      const bsm = e.buttonSizeMode || "stretch";
      return {
        backgroundImage: \`url(\${imgUrl})\`,
        backgroundSize: bsm === "nine-slice" ? void 0 : bsm === "stretch" ? "100% 100%" : bsm,
        backgroundPosition: "center",
        backgroundRepeat: "no-repeat",
        backgroundColor: "transparent",
        ...bsm === "nine-slice" ? { borderImage: \`url(\${imgUrl}) \${e.buttonSlice ?? 10} fill\`, borderImageWidth: \`\${e.buttonSlice ?? 10}px\` } : {}
      };
    };
    const borderPad = e.borderPadding ?? 12;
    const boxStyleBase = {
      backgroundColor: bgImageUrl ? "transparent" : bgColor,
      opacity: bgImageUrl ? 1 : void 0,
      borderRadius,
      textAlign: "center",
      boxShadow: borderImageUrl ? "none" : "0 12px 40px rgba(0,0,0,0.5)",
      ...bgImageStyle,
      ...bgImageUrl ? {} : { background: \`\${bgColor}\${Math.round(bgOpacity * 255).toString(16).padStart(2, "0")}\` }
    };
    const renderBtn = (kind, label, onClick, posStyle) => {
      const isConfirm = kind === "confirm";
      const imgUrl = isConfirm ? confirmBtnImgUrl : cancelBtnImgUrl;
      const hoverImgUrl = isConfirm ? confirmHoverImgUrl : cancelHoverImgUrl;
      const baseColor = isConfirm ? confirmBtnColor || "linear-gradient(to right, #ec4899, #a855f7)" : cancelBtnColor;
      const hoverColor = isConfirm ? confirmHoverColor : cancelHoverColor;
      const setBase = (el) => {
        if (isConfirm) el.style.background = baseColor;
        else el.style.backgroundColor = baseColor;
      };
      return /* @__PURE__ */ jsxRuntime2.jsx(
        "button",
        {
          onClick,
          style: {
            ...buttonStyle,
            padding: \`\${btnPad}px \${btnPad * 3}px\`,
            borderRadius: btnBorderRadius,
            ...isConfirm ? { background: imgUrl ? "transparent" : baseColor, border: "none" } : { backgroundColor: imgUrl ? "transparent" : baseColor, border: imgUrl ? "none" : "1px solid rgba(255,255,255,0.1)" },
            cursor: "pointer",
            transition: "background-color 0.15s, box-shadow 0.15s",
            ...makeBtnImageStyle(imgUrl),
            ...posStyle || {}
          },
          onMouseEnter: (e2) => {
            if (hoverImgUrl) e2.currentTarget.style.backgroundImage = \`url(\${hoverImgUrl})\`;
            else if (hoverColor) {
              if (isConfirm) e2.currentTarget.style.background = hoverColor;
              else e2.currentTarget.style.backgroundColor = hoverColor;
            } else if (isConfirm) e2.currentTarget.style.boxShadow = "0 4px 16px rgba(236,72,153,0.3)";
          },
          onMouseLeave: (e2) => {
            if (imgUrl) e2.currentTarget.style.backgroundImage = \`url(\${imgUrl})\`;
            else if (hoverImgUrl) {
              e2.currentTarget.style.backgroundImage = "none";
              setBase(e2.currentTarget);
            } else if (hoverColor) setBase(e2.currentTarget);
            else if (isConfirm) e2.currentTarget.style.boxShadow = "none";
          },
          children: label
        }
      );
    };
    const free = !!e.independentLayout;
    const boxRect = e.boxRect, confirmRect = e.confirmRect, cancelRect = e.cancelRect;
    const pctStyle = (r) => ({ position: "absolute", left: \`\${r.x}%\`, top: \`\${r.y}%\`, width: \`\${r.width}%\`, height: \`\${r.height}%\` });
    return /* @__PURE__ */ jsxRuntime2.jsx(
      "div",
      {
        className: \`absolute inset-0 \${free && boxRect ? "" : "flex items-center justify-center"}\`,
        style: { backgroundColor: overlayColor, zIndex: 9998, animation: "fade-in 0.2s ease-out" },
        onClick: (e2) => {
          if (e2.target === e2.currentTarget) onCancel();
        },
        children: free && boxRect ? /* @__PURE__ */ jsxRuntime2.jsxs(jsxRuntime2.Fragment, { children: [
          /* @__PURE__ */ jsxRuntime2.jsxs("div", { style: { ...boxStyleBase, ...pctStyle(boxRect), padding: dialogPad, display: "flex", flexDirection: "column", alignItems: "center", justifyContent: "center", gap: "0.5rem", overflow: "hidden" }, children: [
            /* @__PURE__ */ jsxRuntime2.jsx("div", { style: titleStyle, children: title }),
            /* @__PURE__ */ jsxRuntime2.jsx("div", { style: messageStyle, children: message })
          ] }),
          cancelRect && renderBtn("cancel", cancelLabel, onCancel, pctStyle(cancelRect)),
          confirmRect && renderBtn("confirm", confirmLabel, onConfirm, pctStyle(confirmRect))
        ] }) : (
          /* Border image wrapper */
          /* @__PURE__ */ jsxRuntime2.jsx("div", { style: borderImageUrl ? {
            backgroundImage: \`url(\${borderImageUrl})\`,
            backgroundSize: "100% 100%",
            backgroundPosition: "center",
            backgroundRepeat: "no-repeat",
            padding: borderPad,
            borderRadius
          } : {}, children: /* @__PURE__ */ jsxRuntime2.jsxs(
            "div",
            {
              style: {
                ...boxStyleBase,
                padding: dialogPad,
                ...e.dialogWidth ? { width: e.dialogWidth } : { minWidth: 320, maxWidth: 440 }
              },
              children: [
                /* @__PURE__ */ jsxRuntime2.jsx("div", { style: { ...titleStyle, marginBottom: "0.75rem" }, children: title }),
                /* @__PURE__ */ jsxRuntime2.jsx("div", { style: { ...messageStyle, marginBottom: "1.5rem" }, children: message }),
                /* @__PURE__ */ jsxRuntime2.jsxs("div", { style: { display: "flex", gap: "0.75rem", justifyContent: "center" }, children: [
                  renderBtn("cancel", cancelLabel, onCancel),
                  renderBtn("confirm", confirmLabel, onConfirm)
                ] })
              ]
            }
          ) })
        )
      }
    );
  };
  function renderPhoneBadge(ui, count = 1) {
    const size = ui.phoneBadgeSize ?? 16;
    const shape = ui.phoneBadgeShape || "dot";
    const color = ui.phoneBadgeColor || "#ef4444";
    const textColor = ui.phoneBadgeTextColor || "#fff";
    const base = { minWidth: size, height: size, display: "inline-flex", alignItems: "center", justifyContent: "center", color: textColor, fontSize: size * 0.7, fontWeight: 700, boxShadow: "0 2px 8px rgba(0,0,0,0.4)" };
    let shapeEl;
    if (shape === "ring") shapeEl = /* @__PURE__ */ jsxRuntime2.jsx("span", { style: { ...base, width: size, borderRadius: "9999px", border: \`\${Math.max(2, size * 0.18)}px solid \${color}\`, background: "transparent", boxShadow: "none" } });
    else if (shape === "square") shapeEl = /* @__PURE__ */ jsxRuntime2.jsx("span", { style: { ...base, width: size, borderRadius: Math.max(3, size * 0.25), background: color } });
    else if (shape === "count") shapeEl = /* @__PURE__ */ jsxRuntime2.jsx("span", { style: { ...base, padding: "0 5px", borderRadius: "9999px", background: color }, children: Math.max(1, count) });
    else if (shape === "icon") shapeEl = /* @__PURE__ */ jsxRuntime2.jsx("span", { style: { ...base, width: size, borderRadius: "9999px", background: color, fontSize: size * 0.62 }, children: ui.phoneBadgeIcon && PHONE_GLYPHS[ui.phoneBadgeIcon] || "💬" });
    else if (shape === "pulse") shapeEl = /* @__PURE__ */ jsxRuntime2.jsxs("span", { style: { position: "relative", display: "inline-flex" }, children: [
      /* @__PURE__ */ jsxRuntime2.jsx("style", { children: \`@keyframes vn-badge-pulse{0%{transform:scale(1);opacity:.6}70%{transform:scale(2.2);opacity:0}100%{opacity:0}}\` }),
      /* @__PURE__ */ jsxRuntime2.jsx("span", { style: { position: "absolute", inset: 0, borderRadius: "9999px", background: color, animation: "vn-badge-pulse 1.4s ease-out infinite" } }),
      /* @__PURE__ */ jsxRuntime2.jsx("span", { style: { ...base, width: size, borderRadius: "9999px", background: color } })
    ] });
    else shapeEl = /* @__PURE__ */ jsxRuntime2.jsx("span", { style: { ...base, width: size, borderRadius: "9999px", background: color } });
    if (!ui.phoneBadgeLabel) return shapeEl;
    return /* @__PURE__ */ jsxRuntime2.jsxs("span", { style: { display: "inline-flex", flexDirection: "column", alignItems: "center", gap: 2 }, children: [
      shapeEl,
      /* @__PURE__ */ jsxRuntime2.jsx("span", { style: { fontSize: Math.max(9, size * 0.62), fontWeight: 600, color: ui.phoneBadgeLabelColor || textColor, whiteSpace: "nowrap", textShadow: "0 1px 3px rgba(0,0,0,0.6)" }, children: ui.phoneBadgeLabel })
    ] });
  }
  const PhonePanel = ({ ui, project, phone, variables, assetResolver, evaluateConditions: evaluateConditions2, onAction, onReply, onContactMessage, onContactCall, playTap, onOpenApp, onCallReply, onEndCall, onSetWallpaper, onOpenThread }) => {
    const scrollRef = React2.useRef(null);
    React2.useEffect(() => {
      if (scrollRef.current) scrollRef.current.scrollTop = scrollRef.current.scrollHeight;
    }, [phone.messages.length, phone.pendingChoices]);
    const pos = ui.phonePosition || "bottom-right";
    const scale = (ui.phoneScale ?? 100) / 100;
    const w = (ui.phoneWidth ?? 26) * scale;
    const h = (ui.phoneHeight ?? 82) * scale;
    const x = ui.phoneX ?? (pos === "center" ? (100 - w) / 2 : pos.includes("right") ? 100 - w - 2 : 2);
    const y = ui.phoneY ?? (pos === "center" ? (100 - h) / 2 : pos.includes("top") ? 2 : 100 - h - 2);
    const shellImg = ui.phoneShellImage ? assetResolver(ui.phoneShellImage.id, "image") : null;
    const bodyFont = ui.phoneFont ? fontSettingsToStyle(ui.phoneFont) : {};
    const titleFont = ui.phoneTitleFont ? fontSettingsToStyle(ui.phoneTitleFont) : {};
    const buttons = (ui.phoneButtons || []).filter((b) => {
      var _a;
      return b.show !== false && (!((_a = b.conditions) == null ? void 0 : _a.length) || evaluateConditions2(b.conditions, variables));
    });
    const batteryPct = ui.phoneBatteryVariableId != null ? Math.max(0, Math.min(100, Number(variables[ui.phoneBatteryVariableId] ?? 100))) : 100;
    const signalBars = Math.max(1, Math.min(8, ui.phoneSignalBars ?? 4));
    const signalFilled = ui.phoneSignalVariableId != null ? Math.max(0, Math.min(signalBars, Math.round(Number(variables[ui.phoneSignalVariableId] ?? signalBars)))) : signalBars;
    const signalColor = ui.phoneSignalColor || ui.phoneStatusIconColor || "#fff";
    const batteryColor = ui.phoneBatteryColor || ui.phoneStatusIconColor || "#fff";
    const bezel = ui.phoneBezelWidth ?? 8;
    const casingRadius = ui.phoneBorderRadius ?? 28;
    const screenRadius = Math.max(casingRadius - bezel, 6);
    const showHome = ui.phoneShowHomeButton !== false;
    const view = phone.view || "chat";
    const app = resolvePhoneApp(view, ui, project);
    const isHome = app.id === "home";
    const isContacts = app.id === "contacts";
    const ctx = {
      ui,
      project,
      phone,
      variables,
      assetResolver,
      evaluateConditions: evaluateConditions2,
      onAction,
      onReply,
      onContactMessage,
      onContactCall,
      playTap,
      openApp: onOpenApp,
      buttons,
      onCallReply,
      onEndCall,
      onSetWallpaper,
      onOpenThread
    };
    const activeContactId = phone.activeContactId;
    const activeContact = activeContactId ? (ui.phoneContacts || []).find((c) => c.characterId === activeContactId) : void 0;
    const resolvePhoneBg = (ref) => {
      if (!ref) return null;
      const url = assetResolver(ref.id, ref.type === "video" ? "video" : "image");
      return url ? { url, isVideo: ref.type === "video", trimStart: ref.trimStart, trimEnd: ref.trimEnd } : null;
    };
    const pickedWallpaper = phone.wallpaperId ? (ui.phoneWallpapers || []).find((w2) => {
      var _a;
      return w2.id === phone.wallpaperId && (!((_a = w2.conditions) == null ? void 0 : _a.length) || evaluateConditions2(w2.conditions, variables));
    }) : void 0;
    const wallpaper = resolvePhoneBg((pickedWallpaper == null ? void 0 : pickedWallpaper.image) || ui.phoneWallpaperImage);
    const chatBg = resolvePhoneBg((activeContact == null ? void 0 : activeContact.chatBackground) || ui.phoneChatBackgroundImage);
    const screenBg = (app.usesChatBackground ? chatBg : null) || wallpaper;
    const contactsRegion = ui.phoneContactsRegion;
    return (
      // Casing (phone body): the screen is inset by the bezel so it reads as a separate panel.
      /* @__PURE__ */ jsxRuntime2.jsxs("div", { style: {
        position: "absolute",
        left: \`\${x}%\`,
        top: \`\${y}%\`,
        width: \`\${w}%\`,
        height: \`\${h}%\`,
        zIndex: 60,
        borderRadius: casingRadius,
        overflow: "hidden",
        display: "flex",
        flexDirection: "column",
        padding: bezel,
        boxSizing: "border-box",
        backgroundColor: shellImg ? "transparent" : ui.phoneShellColor || "#16181d",
        opacity: (ui.phoneOpacity ?? 100) / 100,
        ...shellImg ? { backgroundImage: \`url(\${shellImg})\`, backgroundSize: "100% 100%", backgroundRepeat: "no-repeat" } : {},
        boxShadow: "0 12px 40px rgba(0,0,0,0.6)",
        animation: "fade-in 0.2s ease-out"
      }, children: [
        /* @__PURE__ */ jsxRuntime2.jsxs("div", { style: {
          flex: 1,
          minHeight: 0,
          display: "flex",
          flexDirection: "column",
          overflow: "hidden",
          borderRadius: screenRadius,
          border: \`1px solid \${ui.phoneScreenBorderColor || "rgba(255,255,255,0.12)"}\`,
          background: ui.phoneScreenColor || "#0b0d12",
          ...bodyFont,
          position: "relative",
          ...screenBg && !screenBg.isVideo ? { backgroundImage: \`url(\${screenBg.url})\`, backgroundSize: "cover", backgroundPosition: "center" } : {},
          ...(screenBg == null ? void 0 : screenBg.isVideo) ? { isolation: "isolate" } : {}
        }, children: [
          (screenBg == null ? void 0 : screenBg.isVideo) && /* @__PURE__ */ jsxRuntime2.jsx(
            TrimmedVideo,
            {
              src: screenBg.url,
              autoPlay: true,
              loop: true,
              muted: true,
              playsInline: true,
              trimStart: screenBg.trimStart,
              trimEnd: screenBg.trimEnd,
              style: { position: "absolute", inset: 0, width: "100%", height: "100%", objectFit: "cover", zIndex: -1, pointerEvents: "none" }
            },
            screenBg.url
          ),
          ui.phoneShowStatusBar !== false && /* @__PURE__ */ jsxRuntime2.jsxs("div", { style: { display: "flex", alignItems: "center", justifyContent: "space-between", padding: "6px 14px", fontSize: "0.8em", color: ui.phoneStatusIconColor || "#fff", backgroundColor: ui.phoneStatusBarColor || "transparent", flexShrink: 0 }, children: [
            /* @__PURE__ */ jsxRuntime2.jsx("span", { children: interpolateVariables(ui.phoneClockText || "", variables, project) }),
            /* @__PURE__ */ jsxRuntime2.jsxs("span", { style: { display: "inline-flex", gap: 8, alignItems: "center" }, children: [
              ui.phoneShowSignal && /* @__PURE__ */ jsxRuntime2.jsx("span", { style: { display: "inline-flex", alignItems: "flex-end", gap: "0.1em", height: "0.85em" }, "aria-hidden": true, title: "Signal", children: Array.from({ length: signalBars }).map((_, i) => /* @__PURE__ */ jsxRuntime2.jsx("span", { style: { width: "0.18em", height: \`\${30 + i / (signalBars - 1 || 1) * 70}%\`, borderRadius: 1, background: signalColor, opacity: i < signalFilled ? 1 : 0.28 } }, i)) }),
              ui.phoneShowBattery && /* @__PURE__ */ jsxRuntime2.jsxs("span", { style: { display: "inline-flex", alignItems: "center", gap: 1 }, title: \`\${Math.round(batteryPct)}%\`, children: [
                /* @__PURE__ */ jsxRuntime2.jsx("span", { style: { display: "inline-block", width: "1.5em", height: "0.8em", border: \`1px solid \${ui.phoneStatusIconColor || "#fff"}\`, borderRadius: 2, position: "relative" }, children: /* @__PURE__ */ jsxRuntime2.jsx("span", { style: { position: "absolute", top: 1, bottom: 1, left: 1, width: \`calc(\${batteryPct}% - 2px)\`, background: batteryColor, borderRadius: 1 } }) }),
                /* @__PURE__ */ jsxRuntime2.jsx("span", { style: { display: "inline-block", width: 2, height: "0.45em", background: ui.phoneStatusIconColor || "#fff", borderRadius: "0 1px 1px 0" } })
              ] })
            ] })
          ] }),
          ui.phoneHeaderText && /* @__PURE__ */ jsxRuntime2.jsx("div", { style: { padding: "2px 16px 6px", ...titleFont }, children: ui.phoneHeaderText }),
          /* @__PURE__ */ jsxRuntime2.jsx("style", { children: \`@keyframes vn-phone-typing{0%,60%,100%{transform:translateY(0);opacity:.4}30%{transform:translateY(-3px);opacity:1}}\` }),
          /* @__PURE__ */ jsxRuntime2.jsx("div", { ref: scrollRef, style: { flex: 1, minHeight: 0, overflowY: "auto", padding: "8px 10px", display: "flex", flexDirection: "column", gap: 8 }, children: app.render(ctx) }),
          isContacts && contactsRegion && /* @__PURE__ */ jsxRuntime2.jsx("div", { style: { position: "absolute", left: \`\${contactsRegion.x}%\`, top: \`\${contactsRegion.y}%\`, width: \`\${contactsRegion.width}%\`, height: \`\${contactsRegion.height}%\`, overflowY: "auto", display: "flex", flexDirection: "column", gap: 8, padding: "4px 6px", zIndex: 2 }, children: renderContactsRoster(ctx) }),
          isHome && ui.phoneButtonLayout === "free" && buttons.map((b) => {
            const customIcon = b.iconImage ? assetResolver(b.iconImage.id, "image") : null;
            return /* @__PURE__ */ jsxRuntime2.jsxs(
              "button",
              {
                onClick: () => fireAppButton(ctx, b),
                title: b.label || "",
                style: { position: "absolute", left: \`\${b.x ?? 8}%\`, top: \`\${b.y ?? 12}%\`, width: \`\${b.width ?? 14}%\`, height: \`\${b.height ?? 14}%\`, containerType: "size", display: "flex", flexDirection: "column", alignItems: "center", justifyContent: "center", gap: "1cqmin", background: "transparent", border: "none", cursor: "pointer", color: ui.phoneButtonIconColor || "#cbd5e1", zIndex: 5 },
                children: [
                  customIcon ? /* @__PURE__ */ jsxRuntime2.jsx("img", { src: customIcon, alt: "", style: { width: "82cqmin", height: "82cqmin", objectFit: "contain" } }) : /* @__PURE__ */ jsxRuntime2.jsx("span", { style: { fontSize: "74cqmin", lineHeight: 1 }, children: b.builtinIcon && PHONE_GLYPHS[b.builtinIcon] || "●" }),
                  b.label && /* @__PURE__ */ jsxRuntime2.jsx("span", { style: { whiteSpace: "nowrap", fontSize: "18cqmin", lineHeight: 1 }, children: b.label })
                ]
              },
              b.id
            );
          }),
          isHome && ui.phoneButtonLayout !== "free" && ui.phoneButtonLayout !== "grid" && buttons.length > 0 && /* @__PURE__ */ jsxRuntime2.jsx("div", { style: { display: "flex", alignItems: "center", justifyContent: "space-around", gap: 4, padding: "6px 4px", backgroundColor: ui.phoneButtonBarColor || "rgba(0,0,0,0.35)", flexShrink: 0 }, children: buttons.map((b) => {
            const customIcon = b.iconImage ? assetResolver(b.iconImage.id, "image") : null;
            return /* @__PURE__ */ jsxRuntime2.jsxs("button", { onClick: () => fireAppButton(ctx, b), title: b.label || "", style: { flex: 1, display: "flex", flexDirection: "column", alignItems: "center", gap: 2, padding: "4px 2px", background: "transparent", border: "none", cursor: "pointer", color: ui.phoneButtonIconColor || "#cbd5e1", fontSize: "0.7em" }, children: [
              customIcon ? /* @__PURE__ */ jsxRuntime2.jsx("img", { src: customIcon, alt: "", style: { width: "1.6em", height: "1.6em", objectFit: "contain" } }) : /* @__PURE__ */ jsxRuntime2.jsx(PhoneGlyph, { name: b.builtinIcon }),
              b.label && /* @__PURE__ */ jsxRuntime2.jsx("span", { style: { whiteSpace: "nowrap" }, children: b.label })
            ] }, b.id);
          }) })
        ] }),
        showHome && !phone.activeCall && /* @__PURE__ */ jsxRuntime2.jsx("div", { style: { flexShrink: 0, display: "flex", alignItems: "center", justifyContent: "center", paddingTop: bezel * 0.6 }, children: /* @__PURE__ */ jsxRuntime2.jsx("button", { onClick: () => {
          playTap();
          onAction({ type: isHome ? UIActionType.HidePhone : UIActionType.ShowPhone });
        }, "aria-label": "Home", title: "Home", style: { width: "1.5em", height: "1.5em", borderRadius: "9999px", border: \`2px solid \${ui.phoneHomeButtonColor || "rgba(255,255,255,0.28)"}\`, background: "transparent", cursor: "pointer", flexShrink: 0 } }) })
      ] })
    );
  };
  const LivePreview = ({ onClose, hideCloseButton = false, autoStartMusic = false, isStandalone = false, startAt = null }) => {
    var _a, _b, _c, _d, _e, _f, _g, _h, _i, _j, _k, _l, _m, _n, _o, _p, _q, _r, _s;
    const { project } = useProject();
    const toast = useToast();
    const notify = React2.useCallback((message, type = "info") => {
      try {
        toast.addToast(message, type);
      } catch {
        console.log(\`[notify] [\${type}] \${message}\`);
      }
    }, [toast]);
    const getValidTitleScreenId = React2.useCallback(() => {
      if (project.ui.titleScreenId && project.uiScreens[project.ui.titleScreenId]) {
        return project.ui.titleScreenId;
      }
      const fallbackByName = Object.values(project.uiScreens).find((s) => s.name.toLowerCase() === "title screen");
      if (fallbackByName) {
        return fallbackByName.id;
      }
      return Object.keys(project.uiScreens)[0] || null;
    }, [project.ui.titleScreenId, project.uiScreens]);
    const titleScreenId = getValidTitleScreenId();
    const [screenStack, setScreenStack] = React2.useState(startAt ? [] : titleScreenId ? [titleScreenId] : []);
    const [hudStack, setHudStack] = React2.useState([]);
    const [elementVisibility, setElementVisibility] = React2.useState({});
    const prevOpenScreensRef = React2.useRef(/* @__PURE__ */ new Set());
    React2.useEffect(() => {
      const open = /* @__PURE__ */ new Set([...screenStack, ...hudStack]);
      const newlyOpened = [];
      open.forEach((id) => {
        if (!prevOpenScreensRef.current.has(id)) newlyOpened.push(id);
      });
      prevOpenScreensRef.current = open;
      if (newlyOpened.length === 0) return;
      setElementVisibility((prev) => {
        let next = prev;
        for (const sid of newlyOpened) {
          const scr = project.uiScreens[sid];
          if (!scr || scr.resetElementVisibilityOnOpen === false) continue;
          for (const elId of Object.keys(scr.elements || {})) {
            if (next[elId] !== void 0) {
              if (next === prev) next = { ...prev };
              delete next[elId];
            }
          }
        }
        return next;
      });
    }, [screenStack, hudStack, project.uiScreens]);
    const [closingScreens, setClosingScreens] = React2.useState(/* @__PURE__ */ new Set());
    const [confirmDialog, setConfirmDialog] = React2.useState(null);
    const [sceneTransitionFading, setSceneTransitionFading] = React2.useState(false);
    const [sceneTransitionType, setSceneTransitionType] = React2.useState("fade");
    const [sceneTransitionDuration, setSceneTransitionDuration] = React2.useState(0.5);
    const [gameStartFade, setGameStartFade] = React2.useState("none");
    const [settings, setSettings] = React2.useState(() => {
      var _a2;
      const projectDefaults = (_a2 = project.ui) == null ? void 0 : _a2.defaultGameSettings;
      if (projectDefaults) {
        return { ...defaultSettings, ...projectDefaults };
      }
      return { ...defaultSettings };
    });
    const [playerState, setPlayerState] = React2.useState(null);
    const playerStateRef = React2.useRef(null);
    const pausingOverlayScreen = (() => {
      for (let i = hudStack.length - 1; i >= 0; i--) {
        const s = project.uiScreens[hudStack[i]];
        if (s == null ? void 0 : s.pauseSceneWhileOpen) return s;
      }
      return null;
    })();
    const scenePaused = !!pausingOverlayScreen;
    const [showVarWatcher, setShowVarWatcher] = React2.useState(false);
    const [, setTweenTick] = React2.useState(0);
    React2.useEffect(() => {
      const unsub = TweenManager.subscribe(() => {
        setTweenTick((t) => t + 1);
      });
      return unsub;
    }, []);
    const updatePlayerState = React2.useCallback((updater) => {
      setPlayerState((prev) => {
        const next = typeof updater === "function" ? updater(prev) : updater;
        playerStateRef.current = next;
        return next;
      });
    }, []);
    const restockPrevConditionRef = React2.useRef({});
    const restockPrevWatchRef = React2.useRef({});
    const hudStackRef = React2.useRef([]);
    hudStackRef.current = hudStack;
    const projectRef = React2.useRef(project);
    projectRef.current = project;
    React2.useEffect(() => {
      playerStateRef.current = playerState;
      if ((playerState == null ? void 0 : playerState.mode) === "playing") {
        const stage = playerState.stageState;
        if (stage.backgroundUrl || Object.keys(stage.characters).length > 0 || stage.textOverlays.length > 0 || stage.imageOverlays.length > 0 || stage.buttonOverlays.length > 0) {
          hasRenderedSceneRef.current = true;
        }
      } else {
        hasRenderedSceneRef.current = false;
      }
    }, [playerState]);
    const [gameSaves, setGameSaves] = React2.useState({});
    const [isJustLoaded, setIsJustLoaded] = React2.useState(false);
    const [menuVariables, setMenuVariables] = React2.useState(() => {
      return getInitialVariablesWithPersistent(project.variables, project.id);
    });
    const [uiVariables, setUiVariables] = React2.useState(() => {
      return getInitialVariablesWithPersistent(project.variables, project.id);
    });
    const uiVariablesRef = React2.useRef(uiVariables);
    React2.useEffect(() => {
      uiVariablesRef.current = uiVariables;
    }, [uiVariables]);
    React2.useEffect(() => {
      const updatedVars = {};
      Object.values(project.variables).forEach((v) => {
        updatedVars[v.id] = menuVariables[v.id] !== void 0 ? menuVariables[v.id] : v.defaultValue;
      });
      setMenuVariables(updatedVars);
    }, [project.variables]);
    React2.useEffect(() => {
      const loadCustomFonts = async () => {
        const projectFonts = project.fonts || {};
        for (const fontId in projectFonts) {
          const font = projectFonts[fontId];
          if ((font == null ? void 0 : font.fontUrl) && (font == null ? void 0 : font.fontFamily)) {
            try {
              const fontFace = new FontFace(font.fontFamily, \`url(\${resolveFieldUrl(project.id, font.fontUrl)})\`);
              await fontFace.load();
              document.fonts.add(fontFace);
              runtimeDebugLog(\`✓ Loaded project font: \${font.fontFamily}\`);
            } catch (error) {
              console.error(\`Failed to load project font \${(font == null ? void 0 : font.name) || fontId}:\`, error);
            }
          }
        }
        for (const charId in project.characters) {
          const char = project.characters[charId];
          if (char.fontUrl && char.fontFamily) {
            try {
              const fontFace = new FontFace(char.fontFamily, \`url(\${resolveFieldUrl(project.id, char.fontUrl)})\`);
              await fontFace.load();
              document.fonts.add(fontFace);
              runtimeDebugLog(\`✓ Loaded custom font: \${char.fontFamily}\`);
            } catch (error) {
              console.error(\`Failed to load custom font for \${char.name}:\`, error);
            }
          }
        }
      };
      loadCustomFonts();
    }, [project.characters, project.fonts]);
    const musicAudioRef = React2.useRef(new Audio());
    const ambientNoiseAudioRef = React2.useRef(new Audio());
    const menuMusicUrlRef = React2.useRef(null);
    const menuAmbientUrlRef = React2.useRef(null);
    const audioFadeInterval = React2.useRef(null);
    const ambientFadeInterval = React2.useRef(null);
    const stageRef = React2.useRef(null);
    const stageSize = useStageSize(stageRef);
    const sceneBasePanX = (playerState == null ? void 0 : playerState.stageState.screen.panX) ?? 0;
    const sceneBasePanY = (playerState == null ? void 0 : playerState.stageState.screen.panY) ?? 0;
    React2.useEffect(() => {
      const stage = stageRef.current;
      if (!stage) return;
      const scene = playerState ? project.scenes[playerState.currentSceneId] : null;
      const px = scene == null ? void 0 : scene.parallax;
      const wantMouse = (px == null ? void 0 : px.mode) === "mouse" || (px == null ? void 0 : px.mode) === "both";
      const wantCamera = (px == null ? void 0 : px.mode) === "camera" || (px == null ? void 0 : px.mode) === "both";
      const active = (wantMouse || wantCamera) && (playerState == null ? void 0 : playerState.mode) === "playing";
      const reduce = typeof window !== "undefined" && window.matchMedia && window.matchMedia("(prefers-reduced-motion: reduce)").matches;
      if (!active || reduce) {
        stage.style.setProperty("--ppx", "0");
        stage.style.setProperty("--ppy", "0");
        return;
      }
      const intensity = (px == null ? void 0 : px.intensity) ?? 1;
      const mouseT = { x: 0, y: 0 };
      const cur = { x: 0, y: 0 };
      let raf = 0;
      const onMove = (e) => {
        const r = stage.getBoundingClientRect();
        if (!r.width || !r.height) return;
        mouseT.x = Math.max(-1, Math.min(1, (e.clientX - r.left) / r.width * 2 - 1));
        mouseT.y = Math.max(-1, Math.min(1, (e.clientY - r.top) / r.height * 2 - 1));
      };
      const onLeave = () => {
        mouseT.x = 0;
        mouseT.y = 0;
      };
      const tick = () => {
        const mx = wantMouse ? mouseT.x : 0;
        const my = wantMouse ? mouseT.y : 0;
        cur.x += (mx - cur.x) * 0.08;
        cur.y += (my - cur.y) * 0.08;
        let cx = 0, cy = 0;
        if (wantCamera) {
          const tw = TweenManager.getCurrentValues("__screen__", "screen");
          const panX = tw && tw.x != null ? tw.x : sceneBasePanX;
          const panY = tw && tw.y != null ? tw.y : sceneBasePanY;
          cx = panX / CAMERA_PAN_REF;
          cy = panY / CAMERA_PAN_REF;
        }
        stage.style.setProperty("--ppx", ((cur.x + cx) * intensity).toFixed(4));
        stage.style.setProperty("--ppy", ((cur.y + cy) * intensity).toFixed(4));
        raf = requestAnimationFrame(tick);
      };
      if (wantMouse) {
        window.addEventListener("mousemove", onMove);
        stage.addEventListener("mouseleave", onLeave);
      }
      raf = requestAnimationFrame(tick);
      return () => {
        cancelAnimationFrame(raf);
        if (wantMouse) {
          window.removeEventListener("mousemove", onMove);
          stage.removeEventListener("mouseleave", onLeave);
        }
        stage.style.setProperty("--ppx", "0");
        stage.style.setProperty("--ppy", "0");
      };
    }, [playerState == null ? void 0 : playerState.currentSceneId, playerState == null ? void 0 : playerState.mode, project.scenes, sceneBasePanX, sceneBasePanY]);
    const playContainerRef = React2.useRef(null);
    const playContainerSize = useStageSize(playContainerRef);
    const [carriedItemId, setCarriedItemId] = React2.useState(null);
    const [carryCursor, setCarryCursor] = React2.useState({ x: 0, y: 0 });
    const carriedItem = carriedItemId ? (_a = project.items) == null ? void 0 : _a[carriedItemId] : null;
    const audioCtxRef = React2.useRef(null);
    const sfxBufferCacheRef = React2.useRef(/* @__PURE__ */ new Map());
    const sfxSourceNodesRef = React2.useRef([]);
    const sfxMasterGainRef = React2.useRef(null);
    React2.useRef(null);
    React2.useRef(/* @__PURE__ */ new Map());
    const MAX_SIMULTANEOUS_SFX = 8;
    const liveSfxRef = React2.useRef(/* @__PURE__ */ new Map());
    const [liveSfxTick, setLiveSfxTick] = React2.useState(0);
    const savesPersistentRef = React2.useRef(true);
    const inMemorySavesRef = React2.useRef({});
    const queuedMusicRef = React2.useRef(null);
    const userGestureDetectedRef = React2.useRef(false);
    const sfxPoolRef = React2.useRef([]);
    const commandSchedulerRef = React2.useRef(new CommandScheduler());
    const hasRenderedSceneRef = React2.useRef(false);
    const runtimeDiagnosticsRef = React2.useRef(new RuntimeDiagnostics());
    const variableStoreRef = React2.useRef(null);
    const uiDirtyVariableIdsRef = React2.useRef(/* @__PURE__ */ new Set());
    const activeEffectTimeoutsRef = React2.useRef([]);
    const timersRef = React2.useRef(/* @__PURE__ */ new Map());
    const blockingTimerRef = React2.useRef(null);
    const fastForwardTargetRef = React2.useRef(null);
    const backwardReplayRef = React2.useRef(null);
    const dnTransitionRef = React2.useRef(0.4);
    const startTimer = (cfg) => {
      const key = (cfg.timerId || "").trim() || "default";
      const mode = cfg.mode === "stopwatch" ? "stopwatch" : "countdown";
      const intervalSec = Math.max(0.05, cfg.interval ?? 1);
      const startVal = mode === "countdown" ? cfg.duration ?? 0 : cfg.from ?? 0;
      const target = mode === "countdown" ? 0 : cfg.duration ?? 0;
      timersRef.current.set(key, { variableId: cfg.variableId || void 0, mode, intervalSec, target, accMs: 0, value: startVal, resetTo: startVal, loop: !!cfg.loop, onComplete: cfg.onComplete });
      if (cfg.variableId) updatePlayerState((p) => p ? { ...p, variables: { ...p.variables, [cfg.variableId]: startVal } } : null);
      return key;
    };
    const stopTimer = (timerId) => {
      var _a2;
      const key = (timerId || "").trim() || "default";
      timersRef.current.delete(key);
      if (((_a2 = blockingTimerRef.current) == null ? void 0 : _a2.key) === key) {
        const r = blockingTimerRef.current.resume;
        blockingTimerRef.current = null;
        try {
          r();
        } catch {
        }
      }
    };
    const applyTimeOfDay = (mode, amount, transitionDuration) => {
      var _a2;
      const tv = (_a2 = project.dayNightCycle) == null ? void 0 : _a2.timeVariableId;
      if (!tv) return;
      dnTransitionRef.current = Math.max(0, transitionDuration ?? 0);
      updatePlayerState((p) => {
        if (!p) return p;
        const cur = Number(p.variables[tv] ?? 0);
        let next = mode === "set" ? amount ?? 0 : cur + (amount ?? 0);
        next = Math.round((next % 24 + 24) % 24 * 1e3) / 1e3;
        return { ...p, variables: { ...p.variables, [tv]: next } };
      });
    };
    const activeCallCmdRef = React2.useRef(null);
    const callRingAudioRef = React2.useRef(null);
    const callTimeoutRef = React2.useRef(null);
    const callRingStartRef = React2.useRef(0);
    const callRingRemainingRef = React2.useRef(0);
    const phoneTimersRef = React2.useRef([]);
    const activeFlashRef = React2.useRef(null);
    const activeShakeRef = React2.useRef(null);
    const scriptInputResolverRef = React2.useRef(null);
    const [flashTrigger, setFlashTrigger] = React2.useState(0);
    const [shakeTrigger, setShakeTrigger] = React2.useState(0);
    const currentVoiceRef = React2.useRef(null);
    const activeLightningRef = React2.useRef(null);
    const [lightningTrigger, setLightningTrigger] = React2.useState(0);
    const activeFireworksRef = React2.useRef(null);
    const [fireworksTrigger, setFireworksTrigger] = React2.useState(0);
    const [flashlight, setFlashlight] = React2.useState(null);
    const flashlightOverlayRef = React2.useRef(null);
    React2.useEffect(() => {
      const key = flashlight == null ? void 0 : flashlight.toggleKey;
      if (!key) return;
      const onKey = (e) => {
        if (e.key.toLowerCase() === key.toLowerCase()) {
          e.preventDefault();
          setFlashlight((f) => f ? { ...f, on: !f.on } : f);
        }
      };
      window.addEventListener("keydown", onKey);
      return () => window.removeEventListener("keydown", onKey);
    }, [flashlight == null ? void 0 : flashlight.toggleKey]);
    React2.useEffect(() => {
      if (!(flashlight == null ? void 0 : flashlight.on)) return;
      const onMove = (e) => {
        var _a2;
        const el = flashlightOverlayRef.current;
        if (!el) return;
        const rect = el.getBoundingClientRect();
        const mx = e.clientX - rect.left;
        const my = e.clientY - rect.top;
        const fVars = (_a2 = playerStateRef.current) == null ? void 0 : _a2.variables;
        const fRadius = resolveVarNumber(fVars, flashlight.radiusVariableId, flashlight.radius, { min: 1, max: 100 });
        const fDarkness = resolveVarNumber(fVars, flashlight.darknessVariableId, flashlight.darkness, { min: 0, max: 1 });
        const radiusPx = fRadius / 100 * Math.min(rect.width, rect.height);
        el.style.background = flashlightBg(mx, my, radiusPx, flashlight.softness, hexToRgba(flashlight.color, fDarkness * 100));
      };
      window.addEventListener("mousemove", onMove);
      return () => window.removeEventListener("mousemove", onMove);
    }, [flashlight == null ? void 0 : flashlight.on, flashlight == null ? void 0 : flashlight.radius, flashlight == null ? void 0 : flashlight.softness, flashlight == null ? void 0 : flashlight.darkness, flashlight == null ? void 0 : flashlight.color]);
    const [spotlights, setSpotlights] = React2.useState({});
    const spotlightRefs = React2.useRef(/* @__PURE__ */ new Map());
    const spotToggleKeys = Object.values(spotlights).map((s) => s.toggleKey || "").join(",");
    React2.useEffect(() => {
      if (!Object.values(spotlights).some((s) => s.toggleKey)) return;
      const onKey = (e) => {
        const tgt = e.target;
        if (tgt && (tgt.tagName === "INPUT" || tgt.tagName === "TEXTAREA" || tgt.isContentEditable)) return;
        const k = e.key.toLowerCase();
        setSpotlights((prev) => {
          let changed = false;
          const next = {};
          for (const id in prev) {
            const s = prev[id];
            if (s.toggleKey && s.toggleKey.toLowerCase() === k) {
              next[id] = { ...s, on: !s.on };
              changed = true;
            } else next[id] = s;
          }
          if (changed) e.preventDefault();
          return changed ? next : prev;
        });
      };
      window.addEventListener("keydown", onKey);
      return () => window.removeEventListener("keydown", onKey);
    }, [spotToggleKeys]);
    const spotFollowSig = Object.entries(spotlights).map(([id, s]) => \`\${id}:\${s.on && s.followMouse ? 1 : 0}:\${s.sourceX},\${s.sourceY},\${s.aimAngle},\${s.swivelMax}\`).join("|");
    React2.useEffect(() => {
      if (!Object.values(spotlights).some((s) => s.on && s.followMouse)) return;
      const onMove = (e) => {
        for (const id in spotlights) {
          const s = spotlights[id];
          if (!s.on || !s.followMouse) continue;
          const el = spotlightRefs.current.get(id);
          if (!el) continue;
          const rect = el.getBoundingClientRect();
          const spx = s.sourceX / 100 * rect.width, spy = s.sourceY / 100 * rect.height;
          const vx = e.clientX - rect.left - spx, vy = e.clientY - rect.top - spy;
          let A = Math.atan2(vx, vy) * 180 / Math.PI;
          A = Math.max(s.aimAngle - s.swivelMax, Math.min(s.aimAngle + s.swivelMax, A));
          el.style.transform = \`rotate(\${-A}deg)\`;
        }
      };
      window.addEventListener("mousemove", onMove);
      return () => window.removeEventListener("mousemove", onMove);
    }, [spotFollowSig]);
    const [activeCreditRoll, setActiveCreditRoll] = React2.useState(null);
    const assetResolver = React2.useCallback((assetId, type) => {
      if (!assetId) return null;
      const raw = (() => {
        var _a2, _b2, _c2, _d2, _e2, _f2;
        switch (type) {
          case "audio":
            return ((_a2 = project.audio[assetId]) == null ? void 0 : _a2.audioUrl) || null;
          case "video":
            return ((_b2 = project.videos[assetId]) == null ? void 0 : _b2.videoUrl) || ((_c2 = project.backgrounds[assetId]) == null ? void 0 : _c2.videoUrl) || ((_e2 = (_d2 = project.images) == null ? void 0 : _d2[assetId]) == null ? void 0 : _e2.videoUrl) || null;
          case "image": {
            if (project.backgrounds[assetId]) {
              const bg = project.backgrounds[assetId];
              return bg.videoUrl || bg.imageUrl || null;
            }
            if (project.images && project.images[assetId]) {
              const img = project.images[assetId];
              return img.videoUrl || img.imageUrl || null;
            }
            if (project.videos[assetId]) {
              return ((_f2 = project.videos[assetId]) == null ? void 0 : _f2.videoUrl) || null;
            }
            for (const charId in project.characters) {
              const char = project.characters[charId];
              if (char.id === assetId) return char.baseVideoUrl || char.baseImageUrl || null;
              for (const layerId in char.layers) {
                const layer = char.layers[layerId];
                if (layer.assets[assetId]) {
                  const asset = layer.assets[assetId];
                  return asset.videoUrl || asset.imageUrl || null;
                }
              }
            }
            return null;
          }
          default:
            return null;
        }
      })();
      return resolveFieldUrl(project.id, raw);
    }, [project]);
    const getAssetMetadata = React2.useCallback((assetId, type) => {
      if (!assetId) return { isVideo: false, loop: false };
      if (project.backgrounds[assetId]) {
        const bg = project.backgrounds[assetId];
        return { isVideo: !!bg.isVideo, loop: !!bg.loop };
      }
      if (project.images && project.images[assetId]) {
        const img = project.images[assetId];
        return { isVideo: !!img.isVideo, loop: !!img.loop };
      }
      if (project.videos && project.videos[assetId]) {
        return { isVideo: true, loop: true };
      }
      for (const charId in project.characters) {
        const char = project.characters[charId];
        if (char.id === assetId) {
          return { isVideo: !!char.isBaseVideo, loop: !!char.baseVideoLoop };
        }
        for (const layerId in char.layers) {
          const layer = char.layers[layerId];
          if (layer.assets[assetId]) {
            const asset = layer.assets[assetId];
            return { isVideo: !!asset.isVideo, loop: !!asset.loop };
          }
        }
      }
      return { isVideo: false, loop: false };
    }, [project]);
    const fadeAudio = React2.useCallback((audioElement, targetVolume, duration, onComplete) => {
      const safeTarget = Number.isFinite(targetVolume) ? Math.max(0, Math.min(1, targetVolume)) : 0.8;
      const intervalRef = audioElement === musicAudioRef.current ? audioFadeInterval : ambientFadeInterval;
      if (intervalRef.current) clearInterval(intervalRef.current);
      const startVolume = audioElement.volume;
      const volumeChange = safeTarget - startVolume;
      if (duration === 0) {
        audioElement.volume = safeTarget;
        onComplete == null ? void 0 : onComplete();
        return;
      }
      const startTime = Date.now();
      intervalRef.current = window.setInterval(() => {
        const elapsedTime = Date.now() - startTime;
        const progress = Math.min(elapsedTime / (duration * 1e3), 1);
        const newVol = startVolume + volumeChange * progress;
        audioElement.volume = Math.max(0, Math.min(1, newVol));
        if (progress >= 1) {
          if (intervalRef.current) clearInterval(intervalRef.current);
          intervalRef.current = null;
          onComplete == null ? void 0 : onComplete();
        }
      }, 30);
    }, []);
    const stopAndResetMusic = React2.useCallback(() => {
      const audio = musicAudioRef.current;
      if (audio && !audio.paused) {
        fadeAudio(audio, 0, 0.5, () => {
          audio.pause();
          audio.src = "";
        });
      } else if (audio) {
        audio.src = "";
      }
      menuMusicUrlRef.current = null;
      const ambientAudio = ambientNoiseAudioRef.current;
      if (ambientAudio && !ambientAudio.paused) {
        fadeAudio(ambientAudio, 0, 0.5, () => {
          ambientAudio.pause();
          ambientAudio.src = "";
        });
      } else if (ambientAudio) {
        ambientAudio.src = "";
      }
      menuAmbientUrlRef.current = null;
    }, [fadeAudio]);
    const savesKey = React2.useMemo(() => \`vn-saves-\${project.id}\`, [project.id]);
    const hasElectronStorage = React2.useCallback(() => {
      var _a2;
      return typeof window !== "undefined" && typeof window.electronAPI !== "undefined" && typeof ((_a2 = window.electronAPI) == null ? void 0 : _a2.storage) !== "undefined";
    }, []);
    const getGameSaves = React2.useCallback(async () => {
      try {
        if (hasElectronStorage()) {
          const raw = await window.electronAPI.storage.getItem(savesKey);
          if (raw == null) return {};
          return raw;
        }
        const savesJson = localStorage.getItem(savesKey);
        return savesJson ? JSON.parse(savesJson) : {};
      } catch (e) {
        runtimeDebugWarn("Failed to load saves from storage:", e);
        return {};
      }
    }, [hasElectronStorage, savesKey]);
    const saveGameSaves = React2.useCallback(async (saves) => {
      try {
        if (hasElectronStorage()) {
          await window.electronAPI.storage.setItem(savesKey, saves);
        } else {
          localStorage.setItem(savesKey, JSON.stringify(saves));
        }
        savesPersistentRef.current = true;
      } catch (e) {
        console.error("Failed to save to storage:", e);
        savesPersistentRef.current = false;
        inMemorySavesRef.current = saves;
      }
    }, [hasElectronStorage, savesKey]);
    React2.useCallback(() => {
      const saves = savesPersistentRef.current ? getGameSaves() : inMemorySavesRef.current;
      const blob = new Blob([JSON.stringify(saves, null, 2)], { type: "application/json" });
      const url = URL.createObjectURL(blob);
      const a = document.createElement("a");
      a.href = url;
      a.download = \`vn-saves-\${project.id}.json\`;
      document.body.appendChild(a);
      a.click();
      a.remove();
      URL.revokeObjectURL(url);
    }, [getGameSaves, project.id]);
    React2.useEffect(() => {
      let cancelled = false;
      (async () => {
        const saves = await getGameSaves();
        if (!cancelled) setGameSaves(saves);
      })();
      return () => {
        cancelled = true;
      };
    }, [getGameSaves, screenStack]);
    const saveGame = (slotNumber) => {
      if (!playerState) return;
      const musicCurrentTime = musicAudioRef.current ? musicAudioRef.current.currentTime : 0;
      const finalMusicState = {
        ...playerState.musicState,
        currentTime: musicCurrentTime,
        isPlaying: !musicAudioRef.current.paused
      };
      const captureScreenshot = () => {
        return new Promise((resolve) => {
          try {
            const stage = playerState.stageState;
            const THUMB_W = 640;
            const THUMB_H = 360;
            const canvas = document.createElement("canvas");
            canvas.width = THUMB_W;
            canvas.height = THUMB_H;
            const ctx = canvas.getContext("2d");
            if (!ctx) {
              resolve(void 0);
              return;
            }
            const imageSources = [];
            if (stage.backgroundUrl && !stage.backgroundIsVideo) {
              imageSources.push({ url: stage.backgroundUrl, x: 0, y: 0, w: THUMB_W, h: THUMB_H });
            }
            for (const char of Object.values(stage.characters)) {
              if (char.isVideo) continue;
              const pos = char.position;
              let xPct = 50, yPct = 10;
              if (typeof pos === "string") {
                const presets = {
                  "left": { x: 25, y: 10 },
                  "center": { x: 50, y: 10 },
                  "right": { x: 75, y: 10 },
                  "off-left": { x: -25, y: 10 },
                  "off-right": { x: 125, y: 10 }
                };
                const p = presets[pos];
                if (p) {
                  xPct = p.x;
                  yPct = p.y;
                }
              } else if (typeof pos === "object") {
                xPct = pos.x;
                yPct = pos.y;
              }
              const charH = THUMB_H * 0.9;
              const charW = charH * 0.75;
              const cx = xPct / 100 * THUMB_W - charW / 2;
              const cy = yPct / 100 * THUMB_H;
              for (const url of char.imageUrls) {
                imageSources.push({ url, x: cx, y: cy, w: charW, h: charH });
              }
            }
            if (imageSources.length === 0) {
              ctx.fillStyle = "#000";
              ctx.fillRect(0, 0, THUMB_W, THUMB_H);
              resolve(canvas.toDataURL("image/jpeg", 0.85));
              return;
            }
            let loaded = 0;
            const images = new Array(imageSources.length).fill(null);
            const onAllLoaded = () => {
              ctx.fillStyle = "#000";
              ctx.fillRect(0, 0, THUMB_W, THUMB_H);
              for (let idx = 0; idx < images.length; idx++) {
                const img = images[idx];
                const src = imageSources[idx];
                if (img && img.complete && img.naturalWidth > 0) {
                  ctx.drawImage(img, src.x, src.y, src.w, src.h);
                }
              }
              resolve(canvas.toDataURL("image/jpeg", 0.85));
            };
            for (let idx = 0; idx < imageSources.length; idx++) {
              const img = new Image();
              img.crossOrigin = "anonymous";
              img.onload = () => {
                images[idx] = img;
                loaded++;
                if (loaded >= imageSources.length) onAllLoaded();
              };
              img.onerror = () => {
                loaded++;
                if (loaded >= imageSources.length) onAllLoaded();
              };
              img.src = imageSources[idx].url;
            }
            setTimeout(() => {
              if (loaded < imageSources.length) onAllLoaded();
            }, 2e3);
          } catch {
            resolve(void 0);
          }
        });
      };
      const createSaveRecord = async () => {
        var _a2;
        const screenshot = await captureScreenshot();
        const saves = await getGameSaves();
        saves[slotNumber] = {
          timestamp: Date.now(),
          sceneName: ((_a2 = project.scenes[playerState.currentSceneId]) == null ? void 0 : _a2.name) || "Unknown Scene",
          screenshot,
          playerStateData: {
            currentSceneId: playerState.currentSceneId,
            currentCommands: playerState.currentCommands,
            currentIndex: playerState.currentIndex,
            commandStack: playerState.commandStack,
            variables: playerState.variables,
            stageState: playerState.stageState,
            musicState: finalMusicState,
            inventorySlots: playerState.inventorySlots,
            selectedItemId: playerState.selectedItemId,
            selectedElementId: playerState.selectedElementId,
            pickedUpItems: playerState.pickedUpItems,
            uiPaletteOverride: playerState.uiPaletteOverride ?? void 0,
            phone: playerState.uiState.phone ?? void 0
          }
        };
        try {
          pluginManager.invokeHook("onSave", saves[slotNumber]);
        } catch {
        }
        if (!savesPersistentRef.current) {
          inMemorySavesRef.current = saves;
        } else {
          await saveGameSaves(saves);
        }
        setGameSaves(saves);
      };
      void createSaveRecord();
    };
    const deleteGameSaveSlot = (slotNumber) => {
      const doDelete = async () => {
        const saves = savesPersistentRef.current ? await getGameSaves() : inMemorySavesRef.current;
        if (!(slotNumber in saves)) return;
        delete saves[slotNumber];
        if (!savesPersistentRef.current) inMemorySavesRef.current = saves;
        else await saveGameSaves(saves);
        setGameSaves({ ...saves });
      };
      void doDelete();
    };
    const loadGame = (slotNumber) => {
      clearPhoneTimers();
      stopRingtone();
      activeCallCmdRef.current = null;
      const audio = musicAudioRef.current;
      if (audioFadeInterval.current) {
        clearInterval(audioFadeInterval.current);
        audioFadeInterval.current = null;
      }
      if (audio) {
        audio.pause();
        audio.volume = 0;
        audio.src = "";
      }
      const ambientAudio = ambientNoiseAudioRef.current;
      if (ambientFadeInterval.current) {
        clearInterval(ambientFadeInterval.current);
        ambientFadeInterval.current = null;
      }
      if (ambientAudio) {
        ambientAudio.pause();
        ambientAudio.src = "";
      }
      menuMusicUrlRef.current = null;
      const doLoad = async () => {
        var _a2, _b2, _c2, _d2, _e2, _f2;
        const saves = savesPersistentRef.current ? await getGameSaves() : inMemorySavesRef.current;
        const saveData = saves[slotNumber];
        if (!saveData) return;
        restockPrevConditionRef.current = {};
        restockPrevWatchRef.current = {};
        const savedPhone = saveData.playerStateData.phone;
        const seededThreadLastRead = savedPhone && savedPhone.threadLastRead === void 0 ? (savedPhone.messages || []).reduce((acc, m) => {
          const k = phoneThreadKey(m);
          acc[k] = (acc[k] || 0) + 1;
          return acc;
        }, {}) : savedPhone == null ? void 0 : savedPhone.threadLastRead;
        updatePlayerState({
          mode: "playing",
          currentSceneId: saveData.playerStateData.currentSceneId,
          currentCommands: saveData.playerStateData.currentCommands || ((_a2 = project.scenes[saveData.playerStateData.currentSceneId]) == null ? void 0 : _a2.commands) || [],
          currentIndex: saveData.playerStateData.currentIndex ?? 0,
          commandStack: saveData.playerStateData.commandStack || [],
          variables: saveData.playerStateData.variables,
          stageState: saveData.playerStateData.stageState,
          inventorySlots: saveData.playerStateData.inventorySlots,
          selectedItemId: saveData.playerStateData.selectedItemId,
          selectedElementId: saveData.playerStateData.selectedElementId,
          pickedUpItems: saveData.playerStateData.pickedUpItems,
          uiPaletteOverride: saveData.playerStateData.uiPaletteOverride ?? null,
          // palette→UI restyle survives save/load
          history: [],
          savedInputs: {},
          uiState: { dialogue: null, choices: null, textInput: null, movieUrl: null, movieLoop: false, isWaitingForInput: false, isTransitioning: false, transitionElement: null, flash: null, showHistory: false, screenSceneId: null, isSkipping: false, phone: savedPhone ? { ...savedPhone, threadLastRead: seededThreadLastRead, typing: null, outgoingCall: null, activeCall: ((_b2 = savedPhone.activeCall) == null ? void 0 : _b2.phase) === "active" ? savedPhone.activeCall : null } : null },
          musicState: saveData.playerStateData.musicState
        });
        setScreenStack([]);
        setHudStack([]);
        setClosingScreens(/* @__PURE__ */ new Set());
        timersRef.current.clear();
        blockingTimerRef.current = null;
        fastForwardTargetRef.current = null;
        backwardReplayRef.current = null;
        setIsJustLoaded(true);
        const savedCall = (_c2 = saveData.playerStateData.phone) == null ? void 0 : _c2.incomingCall;
        if ((savedCall == null ? void 0 : savedCall.phase) === "ringing" && savedCall.cmd) rearmIncomingCall(savedCall.cmd);
        const savedActive = (_d2 = saveData.playerStateData.phone) == null ? void 0 : _d2.activeCall;
        if ((savedActive == null ? void 0 : savedActive.phase) === "active" && !((_e2 = savedActive.pendingReplies) == null ? void 0 : _e2.length)) {
          pushPhoneTimer(playNextCallLine, 900);
        }
        if ((savedPhone == null ? void 0 : savedPhone.activeTextConvo) && !((_f2 = savedPhone.pendingReplies) == null ? void 0 : _f2.length)) {
          pushPhoneTimer(playNextTextLine, 900);
        }
        try {
          pluginManager.invokeHook("onLoadAfterSave", saveData);
        } catch {
        }
      };
      void doLoad();
    };
    const startNewGame = React2.useCallback((startOverride) => {
      var _a2, _b2;
      stopAndResetMusic();
      clearPhoneTimers();
      stopRingtone();
      activeCallCmdRef.current = null;
      restockPrevConditionRef.current = {};
      restockPrevWatchRef.current = {};
      const initialVariables = { ...menuVariables };
      const persistentVars = loadPersistentVariables(project.id);
      Object.values(project.variables).forEach((v) => {
        if ((v.scope || "global") === "persistent" && persistentVars[v.id] !== void 0) {
          initialVariables[v.id] = persistentVars[v.id];
        }
      });
      const resetGameplayVar = (varId) => {
        if (!varId) return;
        const v = project.variables[varId];
        if (!v || (v.scope || "global") === "persistent") return;
        initialVariables[varId] = v.defaultValue;
      };
      Object.values(project.items || {}).forEach((it) => resetGameplayVar(it.countVariableId));
      Object.values(project.itemCollections || {}).forEach((c) => {
        resetGameplayVar(c.currencyVariableId);
        (c.entries || []).forEach((e) => resetGameplayVar(e.countVariableId));
      });
      const hasOverride = !!(startOverride && project.scenes[startOverride.sceneId]);
      let startSceneId = hasOverride ? startOverride.sceneId : project.startSceneId;
      const startScene = project.scenes[startSceneId];
      if (!hasOverride && startScene && startScene.conditions && startScene.conditions.length > 0) {
        const conditionsMet = combineConditions(startScene.conditions, (condition) => {
          const varValue = initialVariables[condition.variableId];
          if (varValue === void 0) return false;
          switch (condition.operator) {
            case "is true":
              return !!varValue;
            case "is false":
              return !varValue;
            case "==":
              return String(varValue).toLowerCase() == String(condition.value).toLowerCase();
            case "!=":
              return String(varValue).toLowerCase() != String(condition.value).toLowerCase();
            case ">":
              return Number(varValue) > Number(condition.value);
            case "<":
              return Number(varValue) < Number(condition.value);
            case ">=":
              return Number(varValue) >= Number(condition.value);
            case "<=":
              return Number(varValue) <= Number(condition.value);
            case "contains":
              return String(varValue).toLowerCase().includes(String(condition.value).toLowerCase());
            case "startsWith":
              return String(varValue).toLowerCase().startsWith(String(condition.value).toLowerCase());
            default:
              return false;
          }
        });
        if (!conditionsMet && startScene.fallbackSceneId) {
          runtimeDebugLog(\`Start scene "\${startScene.name}" conditions not met, using fallback\`);
          startSceneId = startScene.fallbackSceneId;
        }
      }
      setUiVariables(initialVariables);
      uiVariablesRef.current = initialVariables;
      runtimeDebugLog("[CLEAR] Dirty set cleared after startNewGame");
      uiDirtyVariableIdsRef.current.clear();
      updatePlayerState({
        mode: "playing",
        currentSceneId: startSceneId,
        currentCommands: ((_a2 = project.scenes[startSceneId]) == null ? void 0 : _a2.commands) || [],
        currentIndex: 0,
        commandStack: [],
        variables: initialVariables,
        stageState: { backgroundUrl: null, characters: {}, textOverlays: [], imageOverlays: [], buttonOverlays: [], movieOverlays: [], screen: { shake: { active: false, intensity: 0 }, tint: "transparent", zoom: 1, panX: 0, panY: 0, transitionDuration: 0.5, overlayEffects: [] }, particleEffects: {} },
        history: [],
        savedInputs: {},
        uiState: { dialogue: null, choices: null, textInput: null, movieUrl: null, movieLoop: false, isWaitingForInput: false, isTransitioning: false, transitionElement: null, flash: null, showHistory: false, screenSceneId: null, isSkipping: false, phone: null },
        musicState: { audioId: null, loop: false, currentTime: 0, isPlaying: false }
      });
      setScreenStack([]);
      setHudStack([]);
      setClosingScreens(/* @__PURE__ */ new Set());
      timersRef.current.clear();
      blockingTimerRef.current = null;
      fastForwardTargetRef.current = null;
      backwardReplayRef.current = null;
      if (hasOverride) {
        const cmds = ((_b2 = project.scenes[startSceneId]) == null ? void 0 : _b2.commands) || [];
        const idx = Math.max(0, Math.min(startOverride.index, Math.max(0, cmds.length - 1)));
        fastForwardTargetRef.current = idx > 0 ? idx : null;
      }
    }, [project, stopAndResetMusic, menuVariables]);
    const startNewGameWithFade = React2.useCallback(() => {
      setGameStartFade("toBlack");
      window.setTimeout(() => {
        startNewGame();
        setGameStartFade("fromBlack");
        window.setTimeout(() => setGameStartFade("none"), 450);
      }, 400);
    }, [startNewGame]);
    const startAtConsumedRef = React2.useRef(false);
    React2.useEffect(() => {
      if (!startAt || startAtConsumedRef.current) return;
      startAtConsumedRef.current = true;
      startNewGame(startAt);
      setGameStartFade("fromBlack");
      window.setTimeout(() => setGameStartFade("none"), 450);
    }, [startAt]);
    React2.useEffect(() => {
      const onHotReload = (e) => {
        const detail = e.detail || {};
        const sceneId = detail.sceneId;
        const targetScene = sceneId ? project.scenes[sceneId] : void 0;
        if (!targetScene) return;
        const cmds = targetScene.commands || [];
        const requested = typeof detail.commandIndex === "number" ? detail.commandIndex : 0;
        const idx = Math.max(0, Math.min(requested, Math.max(0, cmds.length - 1)));
        const fastForward = detail.fastForward !== false && idx > 0;
        fastForwardTargetRef.current = fastForward ? idx : null;
        const startIndex = fastForward ? 0 : idx;
        updatePlayerState((p) => {
          if (!p) return p;
          return {
            ...p,
            // preserves variables, inventorySlots, selectedItemId, pickedUpItems, history, savedInputs, musicState
            mode: "playing",
            currentSceneId: sceneId,
            currentCommands: cmds,
            currentIndex: startIndex,
            commandStack: [],
            stageState: { backgroundUrl: null, characters: {}, textOverlays: [], imageOverlays: [], buttonOverlays: [], movieOverlays: [], screen: { shake: { active: false, intensity: 0 }, tint: "transparent", zoom: 1, panX: 0, panY: 0, transitionDuration: 0.5, overlayEffects: [] }, particleEffects: {} },
            uiState: { dialogue: null, choices: null, textInput: null, movieUrl: null, movieLoop: false, isWaitingForInput: false, isTransitioning: false, transitionElement: null, flash: null, showHistory: false, screenSceneId: null, isSkipping: false, phone: null }
          };
        });
        setScreenStack([]);
        setHudStack([]);
        setClosingScreens(/* @__PURE__ */ new Set());
        timersRef.current.clear();
        blockingTimerRef.current = null;
      };
      window.addEventListener("flourish:hotReload", onHotReload);
      return () => window.removeEventListener("flourish:hotReload", onHotReload);
    }, [project, updatePlayerState]);
    const getAssetNameFromId2 = React2.useCallback((assetId) => {
      const background = project.backgrounds[assetId];
      if (background) return background.name;
      const image = project.images[assetId];
      if (image) return image.name;
      const video = project.videos[assetId];
      if (video) return video.name;
      const audio = project.audio[assetId];
      if (audio) return audio.name;
      for (const character of Object.values(project.characters)) {
        if (character && character.layers) {
          for (const layer of Object.values(character.layers)) {
            if (layer && layer.assets) {
              const asset = layer.assets[assetId];
              if (asset) return asset.name;
            }
          }
        }
      }
      return null;
    }, [project]);
    const normalizeToBoolean = React2.useCallback((value) => {
      if (typeof value === "boolean") {
        return value;
      }
      if (typeof value === "number") {
        if (value === 1) return true;
        if (value === 0) return false;
        return null;
      }
      if (typeof value === "string") {
        const normalized = value.trim().toLowerCase();
        if (normalized.length === 0) {
          return null;
        }
        if (["true", "1", "yes", "on"].includes(normalized)) return true;
        if (["false", "0", "no", "off"].includes(normalized)) return false;
        return null;
      }
      return null;
    }, []);
    const evaluateConditions2 = React2.useCallback((conditions, variables) => {
      if (!conditions || conditions.length === 0) {
        return true;
      }
      return combineConditions(conditions, (condition) => {
        const varValue = variables[condition.variableId];
        const projectVar = project.variables[condition.variableId];
        const effectiveVarValue = varValue !== void 0 ? varValue : projectVar ? projectVar.defaultValue : void 0;
        runtimeDebugLog("[DEBUG evaluateConditions]", {
          variableId: condition.variableId,
          operator: condition.operator,
          conditionValue: condition.value,
          effectiveVarValue,
          varValue,
          defaultValue: projectVar == null ? void 0 : projectVar.defaultValue
        });
        if (effectiveVarValue === void 0) {
          runtimeDebugLog("[DEBUG evaluateConditions] Variable undefined, returning false");
          return false;
        }
        let result = false;
        const stringVarValue = String(effectiveVarValue);
        const stringCondValue = String(condition.value);
        const normVarString = stringVarValue.toLowerCase();
        const normCondString = stringCondValue.toLowerCase();
        const normalizedVarBool = normalizeToBoolean(effectiveVarValue);
        const normalizedCondBool = normalizeToBoolean(condition.value);
        const boolNumericForComparison = normalizedVarBool === null ? null : normalizedVarBool ? 100 : 0;
        const condBoolNumericForComparison = normalizedCondBool === null ? null : normalizedCondBool ? 100 : 0;
        let assetName = null;
        const varIsNumeric = typeof effectiveVarValue === "number" || typeof effectiveVarValue === "string" && effectiveVarValue.trim().length > 0 && !Number.isNaN(Number(effectiveVarValue));
        const condIsNumeric = typeof condition.value === "number" || typeof condition.value === "string" && condition.value.trim().length > 0 && !Number.isNaN(Number(condition.value));
        if (stringVarValue.startsWith("asset-")) {
          assetName = getAssetNameFromId2(stringVarValue);
          runtimeDebugLog("[DEBUG evaluateConditions] Variable contains asset ID, resolved name:", assetName);
        }
        switch (condition.operator) {
          case "is true": {
            if (normalizedVarBool !== null) {
              result = normalizedVarBool === true;
            } else {
              result = String(effectiveVarValue).trim().toLowerCase() === "true";
            }
            break;
          }
          case "is false": {
            if (normalizedVarBool !== null) {
              result = normalizedVarBool === false;
            } else {
              const normalizedString = String(effectiveVarValue).trim().toLowerCase();
              result = normalizedString === "false" || normalizedString.length === 0;
            }
            break;
          }
          case "==": {
            if (normalizedVarBool !== null && normalizedCondBool !== null) {
              result = normalizedVarBool === normalizedCondBool;
            } else if ((projectVar == null ? void 0 : projectVar.type) === "boolean" && normCondString.length === 0) {
              result = normalizedVarBool === true;
            } else if (varIsNumeric && condIsNumeric) {
              result = Number(effectiveVarValue) === Number(condition.value);
            } else {
              const matchesString = normVarString === normCondString;
              const matchesAsset = assetName ? assetName.toLowerCase() === normCondString : false;
              result = matchesString || matchesAsset;
            }
            break;
          }
          case "!=": {
            if (normalizedVarBool !== null && normalizedCondBool !== null) {
              result = normalizedVarBool !== normalizedCondBool;
            } else if ((projectVar == null ? void 0 : projectVar.type) === "boolean" && normCondString.length === 0) {
              result = normalizedVarBool !== true;
            } else if (varIsNumeric && condIsNumeric) {
              result = Number(effectiveVarValue) !== Number(condition.value);
            } else {
              const matchesString = normVarString === normCondString;
              const matchesAsset = assetName ? assetName.toLowerCase() === normCondString : false;
              result = !matchesString && !matchesAsset;
            }
            break;
          }
          case ">": {
            if ((projectVar == null ? void 0 : projectVar.type) === "boolean" && boolNumericForComparison !== null) {
              const condTarget = condIsNumeric ? Number(condition.value) : condBoolNumericForComparison;
              if (condTarget !== null && Number.isFinite(condTarget)) {
                result = boolNumericForComparison > condTarget;
                runtimeDebugLog("[DEBUG evaluateConditions] Boolean comparison >", {
                  effectiveVarValue,
                  conditionValue: condition.value,
                  boolNumericValue: boolNumericForComparison,
                  numericCond: condTarget,
                  result
                });
                break;
              }
            }
            const numericVar = Number(effectiveVarValue);
            const numericCond = Number(condition.value);
            result = numericVar > numericCond;
            runtimeDebugLog("[DEBUG evaluateConditions] Numeric comparison >", {
              effectiveVarValue,
              conditionValue: condition.value,
              numericVar,
              numericCond,
              result
            });
            break;
          }
          case "<": {
            if ((projectVar == null ? void 0 : projectVar.type) === "boolean" && boolNumericForComparison !== null) {
              const condTarget = condIsNumeric ? Number(condition.value) : condBoolNumericForComparison;
              if (condTarget !== null && Number.isFinite(condTarget)) {
                result = boolNumericForComparison < condTarget;
                runtimeDebugLog("[DEBUG evaluateConditions] Boolean comparison <", {
                  effectiveVarValue,
                  conditionValue: condition.value,
                  boolNumericValue: boolNumericForComparison,
                  numericCond: condTarget,
                  result
                });
                break;
              }
            }
            const numericVar = Number(effectiveVarValue);
            const numericCond = Number(condition.value);
            result = numericVar < numericCond;
            runtimeDebugLog("[DEBUG evaluateConditions] Numeric comparison <", {
              effectiveVarValue,
              conditionValue: condition.value,
              numericVar,
              numericCond,
              result
            });
            break;
          }
          case ">=": {
            if ((projectVar == null ? void 0 : projectVar.type) === "boolean" && boolNumericForComparison !== null) {
              const condTarget = condIsNumeric ? Number(condition.value) : condBoolNumericForComparison;
              if (condTarget !== null && Number.isFinite(condTarget)) {
                result = boolNumericForComparison >= condTarget;
                runtimeDebugLog("[DEBUG evaluateConditions] Boolean comparison >=", {
                  effectiveVarValue,
                  conditionValue: condition.value,
                  boolNumericValue: boolNumericForComparison,
                  numericCond: condTarget,
                  result
                });
                break;
              }
            }
            const numericVar = Number(effectiveVarValue);
            const numericCond = Number(condition.value);
            result = numericVar >= numericCond;
            runtimeDebugLog("[DEBUG evaluateConditions] Numeric comparison >=", {
              effectiveVarValue,
              conditionValue: condition.value,
              numericVar,
              numericCond,
              result
            });
            break;
          }
          case "<=": {
            if ((projectVar == null ? void 0 : projectVar.type) === "boolean" && boolNumericForComparison !== null) {
              const condTarget = condIsNumeric ? Number(condition.value) : condBoolNumericForComparison;
              if (condTarget !== null && Number.isFinite(condTarget)) {
                result = boolNumericForComparison <= condTarget;
                runtimeDebugLog("[DEBUG evaluateConditions] Boolean comparison <=", {
                  effectiveVarValue,
                  conditionValue: condition.value,
                  boolNumericValue: boolNumericForComparison,
                  numericCond: condTarget,
                  result
                });
                break;
              }
            }
            const numericVar = Number(effectiveVarValue);
            const numericCond = Number(condition.value);
            result = numericVar <= numericCond;
            runtimeDebugLog("[DEBUG evaluateConditions] Numeric comparison <=", {
              effectiveVarValue,
              conditionValue: condition.value,
              numericVar,
              numericCond,
              result
            });
            break;
          }
          case "contains": {
            const varContains = normVarString.includes(normCondString);
            const assetContains = assetName ? assetName.toLowerCase().includes(normCondString) : false;
            result = varContains || assetContains;
            break;
          }
          case "startsWith": {
            const varStartsWith = normVarString.startsWith(normCondString);
            const assetStartsWith = assetName ? assetName.toLowerCase().startsWith(normCondString) : false;
            result = varStartsWith || assetStartsWith;
            break;
          }
          default:
            result = false;
        }
        runtimeDebugLog("[DEBUG evaluateConditions] Result:", result);
        return result;
      });
    }, [project.variables, getAssetNameFromId2, normalizeToBoolean]);
    React2.useEffect(() => {
      if (!playerState) return;
      const collections = project.itemCollections;
      if (!collections) return;
      const gameVars = playerState.variables;
      const restockUpdates = {};
      for (const collection of Object.values(collections)) {
        const rule = collection.restock;
        if (!rule) continue;
        if (rule.trigger === "condition") {
          const isTrue = evaluateConditions2(rule.condition, gameVars);
          const prev = restockPrevConditionRef.current[collection.id];
          restockPrevConditionRef.current[collection.id] = isTrue;
          if (prev === void 0) continue;
          if (isTrue && !prev) Object.assign(restockUpdates, computeCollectionRestock(collection, project.variables));
        } else if (rule.trigger === "variableChange" && rule.watchVariableId) {
          const cur = gameVars[rule.watchVariableId];
          const seen = Object.prototype.hasOwnProperty.call(restockPrevWatchRef.current, collection.id);
          const prev = restockPrevWatchRef.current[collection.id];
          restockPrevWatchRef.current[collection.id] = cur;
          if (!seen) continue;
          if (cur !== prev) Object.assign(restockUpdates, computeCollectionRestock(collection, project.variables));
        }
      }
      if (Object.keys(restockUpdates).length > 0) {
        updatePlayerState((p) => p ? { ...p, variables: { ...p.variables, ...restockUpdates } } : null);
      }
    }, [playerState == null ? void 0 : playerState.variables, project.itemCollections, project.variables, evaluateConditions2, updatePlayerState]);
    const navigateToScene = React2.useCallback((targetSceneId, variables) => {
      let sceneToPlay = targetSceneId;
      let attempts = 0;
      const maxAttempts = 50;
      while (attempts < maxAttempts) {
        const scene = project.scenes[sceneToPlay];
        if (!scene) {
          console.error(\`Scene not found: \${sceneToPlay}\`);
          return targetSceneId;
        }
        if (evaluateConditions2(scene.conditions, variables)) {
          return sceneToPlay;
        }
        if (scene.fallbackSceneId && project.scenes[scene.fallbackSceneId]) {
          runtimeDebugLog(\`Scene "\${scene.name}" conditions failed, jumping to fallback: \${scene.fallbackSceneId}\`);
          sceneToPlay = scene.fallbackSceneId;
        } else {
          const sceneIds = Object.keys(project.scenes);
          const currentIndex = sceneIds.indexOf(sceneToPlay);
          if (currentIndex !== -1 && currentIndex < sceneIds.length - 1) {
            sceneToPlay = sceneIds[currentIndex + 1];
            runtimeDebugLog(\`Scene "\${scene.name}" conditions failed, trying next scene: \${sceneToPlay}\`);
          } else {
            runtimeDebugLog(\`Scene "\${scene.name}" conditions failed and no fallback/next scene available\`);
            return sceneToPlay;
          }
        }
        attempts++;
      }
      console.error("Scene navigation exceeded max attempts - possible circular fallback");
      return targetSceneId;
    }, [project.scenes, evaluateConditions2]);
    const startSceneExitTransition = React2.useCallback((currentSceneId, executeChange) => {
      const currentScene = project.scenes[currentSceneId];
      const transType = (currentScene == null ? void 0 : currentScene.outTransition) || "fade";
      const duration = (currentScene == null ? void 0 : currentScene.outTransitionDuration) ?? 0.5;
      const shouldFade = hasRenderedSceneRef.current;
      if (transType === "instant" || !shouldFade) {
        executeChange();
        return;
      }
      const audio = musicAudioRef.current;
      if (audio && !audio.paused) {
        fadeAudio(audio, 0, duration, () => {
          audio.pause();
          audio.currentTime = 0;
        });
      }
      setSceneTransitionType(transType);
      setSceneTransitionDuration(duration);
      setSceneTransitionFading(true);
      setTimeout(() => {
        executeChange();
        setSceneTransitionFading(false);
      }, duration * 1e3);
    }, [project.scenes]);
    React2.useEffect(() => {
      if ((playerState == null ? void 0 : playerState.mode) === "playing") {
        return;
      }
      const audio = musicAudioRef.current;
      const activeScreen = screenStack.length > 0 ? project.uiScreens[screenStack[screenStack.length - 1]] : null;
      if (!activeScreen) {
        if (!audio.paused) {
          fadeAudio(audio, 0, 0.5, () => audio.pause());
        }
        menuMusicUrlRef.current = null;
        return;
      }
      const musicInfo = activeScreen.music;
      if ((playerState == null ? void 0 : playerState.mode) === "paused" && musicInfo.policy === "continue") {
        return;
      }
      const newAudioUrl = (musicInfo == null ? void 0 : musicInfo.audioId) ? assetResolver(musicInfo.audioId, "audio") : null;
      const normalize = (value) => {
        if (!value) return null;
        try {
          return new URL(value, window.location.href).href;
        } catch (e) {
          return value;
        }
      };
      const currentSrcNormalized = audio.src ? normalize(audio.src) : null;
      const newSrcNormalized = normalize(newAudioUrl);
      if (!newAudioUrl) {
        if (!audio.paused) {
          fadeAudio(audio, 0, 0.5, () => audio.pause());
        }
        menuMusicUrlRef.current = null;
        return;
      }
      const startPlayback = () => {
        audio.loop = true;
        audio.play().then(() => {
          menuMusicUrlRef.current = newAudioUrl;
          const screenVol = musicInfo.volume ?? 1;
          fadeAudio(audio, screenVol * settings.musicVolume, 0.5);
        }).catch((e) => {
          console.error("Menu music play failed:", e);
          if (!userGestureDetectedRef.current) {
            queuedMusicRef.current = { url: newAudioUrl, loop: true, fadeDuration: 0.5 };
          }
        });
      };
      if (currentSrcNormalized !== newSrcNormalized) {
        audio.src = newAudioUrl;
        audio.load();
        startPlayback();
      } else if (audio.paused) {
        startPlayback();
      } else {
        menuMusicUrlRef.current = newAudioUrl;
      }
    }, [screenStack, playerState == null ? void 0 : playerState.mode, project.uiScreens, assetResolver, settings.musicVolume, fadeAudio]);
    React2.useEffect(() => {
      var _a2;
      if (!musicAudioRef.current) return;
      const safeVol = Number.isFinite(settings.musicVolume) ? settings.musicVolume : 0.8;
      const activeScreen = screenStack.length > 0 ? project.uiScreens[screenStack[screenStack.length - 1]] : null;
      const screenVol = ((_a2 = activeScreen == null ? void 0 : activeScreen.music) == null ? void 0 : _a2.volume) ?? 1;
      musicAudioRef.current.volume = Math.max(0, Math.min(1, screenVol * safeVol));
    }, [settings.musicVolume, screenStack, project.uiScreens]);
    React2.useEffect(() => {
      const audio = ambientNoiseAudioRef.current;
      const activeScreenId = screenStack.length > 0 ? screenStack[screenStack.length - 1] : hudStack.length > 0 ? hudStack[hudStack.length - 1] : null;
      const activeScreen = activeScreenId ? project.uiScreens[activeScreenId] : null;
      if (!activeScreen || !activeScreen.ambientNoise) {
        if (audio && !audio.paused) {
          fadeAudio(audio, 0, 0.5, () => audio.pause());
        }
        menuAmbientUrlRef.current = null;
        return;
      }
      const ambientInfo = activeScreen.ambientNoise;
      if ((playerState == null ? void 0 : playerState.mode) === "paused" && ambientInfo.policy === "stop") {
        if (audio && !audio.paused) {
          fadeAudio(audio, 0, 0.5, () => audio.pause());
        }
        return;
      }
      const newAudioUrl = (ambientInfo == null ? void 0 : ambientInfo.audioId) ? assetResolver(ambientInfo.audioId, "audio") : null;
      const normalize = (value) => {
        if (!value) return null;
        try {
          return new URL(value, window.location.href).href;
        } catch (e) {
          return value;
        }
      };
      const currentSrcNormalized = (audio == null ? void 0 : audio.src) ? normalize(audio.src) : null;
      const newSrcNormalized = normalize(newAudioUrl);
      if (!newAudioUrl) {
        if (audio && !audio.paused) {
          fadeAudio(audio, 0, 0.5, () => audio.pause());
        }
        menuAmbientUrlRef.current = null;
        return;
      }
      const startAmbientPlayback = () => {
        if (!audio) return;
        audio.loop = true;
        audio.volume = 0;
        audio.play().then(() => {
          menuAmbientUrlRef.current = newAudioUrl;
          const screenVol = ambientInfo.volume ?? 1;
          fadeAudio(audio, screenVol * settings.ambientVolume, 0.5);
        }).catch((e) => {
          console.error("[Ambient] Play failed:", e);
        });
      };
      if (currentSrcNormalized !== newSrcNormalized) {
        if (!audio) return;
        audio.src = newAudioUrl;
        audio.load();
        startAmbientPlayback();
      } else if (audio && audio.paused) {
        startAmbientPlayback();
      } else {
        menuAmbientUrlRef.current = newAudioUrl;
      }
    }, [screenStack, hudStack, playerState == null ? void 0 : playerState.mode, project.uiScreens, assetResolver, settings.ambientVolume, fadeAudio]);
    React2.useEffect(() => {
      var _a2;
      if (!ambientNoiseAudioRef.current) return;
      const safeVol = Number.isFinite(settings.ambientVolume) ? settings.ambientVolume : 0.8;
      const activeScreen = screenStack.length > 0 ? project.uiScreens[screenStack[screenStack.length - 1]] : null;
      const screenVol = ((_a2 = activeScreen == null ? void 0 : activeScreen.ambientNoise) == null ? void 0 : _a2.volume) ?? 1;
      ambientNoiseAudioRef.current.volume = Math.max(0, Math.min(1, screenVol * safeVol));
    }, [settings.ambientVolume, screenStack, project.uiScreens]);
    React2.useEffect(() => {
      if ((playerState == null ? void 0 : playerState.mode) !== "playing" || screenStack.length > 0) {
        return;
      }
      const normalize = (value) => {
        if (!value) return null;
        try {
          return new URL(value, window.location.href).href;
        } catch (e) {
          return value;
        }
      };
      const musicAudio = musicAudioRef.current;
      if (menuMusicUrlRef.current) {
        const currentSrc = (musicAudio == null ? void 0 : musicAudio.src) ? normalize(musicAudio.src) : null;
        const menuSrc = normalize(menuMusicUrlRef.current);
        if (currentSrc && menuSrc && currentSrc === menuSrc && musicAudio && !musicAudio.paused) {
          fadeAudio(musicAudio, 0, 0.5, () => musicAudio.pause());
        }
        menuMusicUrlRef.current = null;
      }
      const ambientAudio = ambientNoiseAudioRef.current;
      if (menuAmbientUrlRef.current && ambientAudio && !ambientAudio.paused) {
        fadeAudio(ambientAudio, 0, 0.5, () => ambientAudio.pause());
        menuAmbientUrlRef.current = null;
      }
    }, [playerState == null ? void 0 : playerState.mode, screenStack, fadeAudio]);
    React2.useEffect(() => {
      const handler = () => {
        userGestureDetectedRef.current = true;
        const queued = queuedMusicRef.current;
        if (queued) {
          const audio = musicAudioRef.current;
          audio.src = queued.url;
          audio.loop = queued.loop;
          audio.load();
          audio.play().then(() => {
            fadeAudio(audio, settings.musicVolume, queued.fadeDuration);
            queuedMusicRef.current = null;
          }).catch((e) => console.error("Queued music play failed:", e));
        }
      };
      window.addEventListener("click", handler, { once: true });
      return () => window.removeEventListener("click", handler);
    }, [fadeAudio, settings.musicVolume]);
    React2.useEffect(() => {
      if (autoStartMusic) {
        userGestureDetectedRef.current = true;
      }
    }, [autoStartMusic]);
    React2.useEffect(() => {
      if (isJustLoaded && (playerState == null ? void 0 : playerState.mode) === "playing") {
        const { musicState } = playerState;
        if (musicState.audioId) {
          const audio = musicAudioRef.current;
          const url = assetResolver(musicState.audioId, "audio");
          if (url) {
            audio.src = url;
            audio.loop = musicState.loop;
            audio.currentTime = musicState.currentTime;
            audio.play().then(() => {
              fadeAudio(audio, settings.musicVolume, 0.5);
            }).catch((e) => console.error("Failed to resume music on load:", e));
          }
        }
        setIsJustLoaded(false);
      }
    }, [isJustLoaded, playerState, assetResolver, fadeAudio, settings.musicVolume]);
    const lastSyncedMusicIdRef = React2.useRef(null);
    React2.useEffect(() => {
      if (!playerState || playerState.mode !== "playing" || isJustLoaded) return;
      const { musicState } = playerState;
      const audio = musicAudioRef.current;
      if (!audio) return;
      const currentAudioId = musicState.audioId || null;
      const previousAudioId = lastSyncedMusicIdRef.current;
      if (currentAudioId === previousAudioId) return;
      lastSyncedMusicIdRef.current = currentAudioId;
      if (!currentAudioId) {
        audio.pause();
        audio.currentTime = 0;
        audio.src = "";
        return;
      }
      const url = assetResolver(currentAudioId, "audio");
      if (!url) return;
      audio.src = url;
      audio.loop = musicState.loop;
      audio.currentTime = musicState.currentTime || 0;
      if (musicState.isPlaying) {
        audio.play().then(() => {
          fadeAudio(audio, settings.musicVolume, 0.3);
        }).catch((e) => console.error("[Music Sync] Failed to play restored music:", e));
      }
    }, [(_b = playerState == null ? void 0 : playerState.musicState) == null ? void 0 : _b.audioId, playerState == null ? void 0 : playerState.mode, isJustLoaded, assetResolver, fadeAudio, settings.musicVolume]);
    const stopAllSfx = React2.useCallback(() => {
      if (currentVoiceRef.current) {
        try {
          currentVoiceRef.current.pause();
          currentVoiceRef.current.currentTime = 0;
          currentVoiceRef.current.src = "";
        } catch (e) {
        }
        currentVoiceRef.current = null;
      }
      try {
        sfxSourceNodesRef.current.forEach((src) => {
          try {
            src.stop();
          } catch (e) {
          }
        });
      } catch (e) {
      }
      sfxSourceNodesRef.current = [];
      sfxPoolRef.current.forEach(({ audio }) => {
        try {
          audio.pause();
          audio.currentTime = 0;
          audio.src = "";
        } catch (e) {
        }
      });
      sfxPoolRef.current = [];
      liveSfxRef.current.forEach((entry) => {
        entry.audio = null;
        entry.lastMet = false;
      });
      liveSfxRef.current.clear();
      sfxBufferCacheRef.current.clear();
    }, []);
    const stopSfx = React2.useCallback((audioId, fadeDuration) => {
      const fade = typeof fadeDuration === "number" && fadeDuration > 0 ? fadeDuration : 0;
      const matched = sfxPoolRef.current.filter((e) => !audioId || e.audioId === audioId);
      sfxPoolRef.current = sfxPoolRef.current.filter((e) => !matched.includes(e));
      if (fade <= 0) {
        matched.forEach(({ audio }) => {
          try {
            audio.pause();
            audio.currentTime = 0;
            audio.src = "";
          } catch (e) {
          }
        });
        if (!audioId) {
          try {
            sfxSourceNodesRef.current.forEach((src) => {
              try {
                src.stop();
              } catch (e) {
              }
            });
          } catch (e) {
          }
          sfxSourceNodesRef.current = [];
        }
        return;
      }
      matched.forEach(({ audio }) => {
        const startVol = audio.volume;
        const startTime = Date.now();
        const iv = window.setInterval(() => {
          const progress = Math.min((Date.now() - startTime) / (fade * 1e3), 1);
          audio.volume = Math.max(0, startVol * (1 - progress));
          if (progress >= 1) {
            clearInterval(iv);
            try {
              audio.pause();
              audio.currentTime = 0;
              audio.src = "";
            } catch (e) {
            }
          }
        }, 30);
      });
    }, []);
    const playSound = React2.useCallback((soundId, volume, loop) => {
      runtimeDebugLog("[SFX] playSound called with soundId:", soundId, "volume:", volume, "loop:", loop);
      if (!soundId) return null;
      try {
        const url = assetResolver(soundId, "audio");
        runtimeDebugLog("[SFX] assetResolver returned URL:", url, "for soundId:", soundId);
        if (!url) {
          runtimeDebugWarn(\`[SFX] No audio URL found for soundId: \${soundId}\`);
          return null;
        }
        runtimeDebugLog("[SFX] Creating HTMLAudio element for playback");
        const audio = new Audio(url);
        audio.loop = !!loop;
        audio.volume = (typeof volume === "number" ? Math.max(0, Math.min(1, volume)) : 1) * (Number.isFinite(settings.sfxVolume) ? settings.sfxVolume : 0.8);
        if (!loop) {
          const oneShots = sfxPoolRef.current.filter((e) => !e.audio.loop);
          if (oneShots.length >= MAX_SIMULTANEOUS_SFX) {
            const oldest = oneShots[0];
            sfxPoolRef.current = sfxPoolRef.current.filter((e) => e !== oldest);
            try {
              oldest == null ? void 0 : oldest.audio.pause();
              if (oldest) oldest.audio.currentTime = 0;
            } catch (e) {
            }
          }
        }
        sfxPoolRef.current.push({ audio, audioId: soundId });
        runtimeDebugLog("[SFX] Playing audio, volume:", audio.volume);
        audio.play().then(() => {
          runtimeDebugLog("[SFX] Audio playback started successfully");
        }).catch((e) => {
          console.error("[SFX] Audio playback failed:", e);
        });
        audio.addEventListener("ended", () => {
          runtimeDebugLog("[SFX] Audio playback ended");
          sfxPoolRef.current = sfxPoolRef.current.filter((e) => e.audio !== audio);
        }, { once: true });
        return audio;
      } catch (outerError) {
        console.error("[SFX] Critical error in playSound:", outerError);
        console.error("[SFX] Error stack:", outerError instanceof Error ? outerError.stack : "N/A");
        return null;
      }
    }, [assetResolver, settings.sfxVolume]);
    const stopVoice = React2.useCallback(() => {
      const a = currentVoiceRef.current;
      if (a) {
        try {
          a.pause();
          a.currentTime = 0;
          a.src = "";
        } catch (e) {
        }
      }
      currentVoiceRef.current = null;
    }, []);
    const playVoice = React2.useCallback((soundId, voiceVol) => {
      stopVoice();
      if (!soundId) return null;
      try {
        const url = assetResolver(soundId, "audio");
        if (!url) return null;
        const audio = new Audio(url);
        audio.volume = typeof voiceVol === "number" ? Math.max(0, Math.min(1, voiceVol)) : 1;
        currentVoiceRef.current = audio;
        audio.play().catch((e) => console.error("[Voice] playback failed:", e));
        audio.addEventListener("ended", () => {
          if (currentVoiceRef.current === audio) currentVoiceRef.current = null;
        }, { once: true });
        return audio;
      } catch (e) {
        console.error("[Voice] error:", e);
        return null;
      }
    }, [assetResolver, stopVoice]);
    React2.useEffect(() => {
      var _a2;
      if (sfxMasterGainRef.current) {
        try {
          sfxMasterGainRef.current.gain.setTargetAtTime(settings.sfxVolume, ((_a2 = audioCtxRef.current) == null ? void 0 : _a2.currentTime) || 0, 0.01);
        } catch (e) {
        }
      }
    }, [settings.sfxVolume]);
    const prevLifecycleSceneRef = React2.useRef(null);
    const autoRanSceneRef = React2.useRef(null);
    const runLifecycleScripts = React2.useCallback(async (trigger, sceneId) => {
      const scripts = Object.values(project.scripts || {}).filter((s) => s.enabled && s.trigger === trigger);
      if (scripts.length === 0) return;
      const store = variableStoreRef.current;
      const baseVars = store ? store.snapshot().globals : { ...(playerState == null ? void 0 : playerState.variables) || {} };
      const variableUpdates = { ...baseVars };
      const resolveVarId = (nameOrId) => {
        if (project.variables[nameOrId]) return nameOrId;
        const lower = nameOrId.toLowerCase();
        for (const [id, v] of Object.entries(project.variables)) if (v.name.toLowerCase() === lower) return id;
        return nameOrId;
      };
      const resolveAudioId = (nameOrId) => {
        if (project.audio[nameOrId]) return nameOrId;
        const lower = nameOrId.toLowerCase();
        for (const [id, a] of Object.entries(project.audio)) if (a.name.toLowerCase() === lower) return id;
        return nameOrId;
      };
      const LIFECYCLE_MAX_DEPTH = 16;
      const execLifecycle = async (scr, depth) => {
        if (depth > LIFECYCLE_MAX_DEPTH) {
          console.error(\`[Lifecycle:\${trigger}] script recursion limit reached at "\${scr.name}"\`);
          return;
        }
        const ctx = {
          project,
          variables: variableUpdates,
          currentSceneId: sceneId,
          args: {},
          onSetVariable: (nameOrId, value) => {
            variableUpdates[resolveVarId(nameOrId)] = value;
          },
          onJumpToScene: () => {
          },
          onJumpToLabel: () => {
          },
          onShowDialogue: (characterName, text) => {
            const match = Object.values(project.characters).find((c) => c.name.toLowerCase() === (characterName || "").toLowerCase());
            updatePlayerState((p) => p ? { ...p, uiState: { ...p.uiState, dialogue: { characterName: characterName || "Narrator", characterColor: (match == null ? void 0 : match.color) || "#FFFFFF", characterId: (match == null ? void 0 : match.id) || null, text } } } : null);
          },
          onPlaySFX: (nameOrId, volume) => {
            playSound(resolveAudioId(nameOrId), volume);
          },
          onPlayMusic: (nameOrId, loop, volume) => {
            const url = assetResolver(resolveAudioId(nameOrId), "audio");
            const audio = musicAudioRef.current;
            if (!url || !audio) return;
            audio.src = url;
            audio.load();
            audio.loop = !!loop;
            audio.volume = 0;
            audio.play().then(() => fadeAudio(audio, typeof volume === "number" ? volume : settings.musicVolume, 1)).catch(() => {
            });
          },
          onStopMusic: (fade) => {
            if (musicAudioRef.current) fadeAudio(musicAudioRef.current, 0, fade || 1);
          },
          onNotify: (message, type) => {
            notify(message, type);
          },
          onRunScript: (nameOrId) => {
            const lower = String(nameOrId).toLowerCase();
            const target = Object.values(project.scripts || {}).find((s) => s.id === nameOrId || s.name.toLowerCase() === lower);
            if (target && target.enabled) return execLifecycle(target, depth + 1);
            console.warn(\`[Lifecycle:\${trigger}] runScript: not found/disabled "\${nameOrId}"\`);
            return void 0;
          },
          onCallCommonEvent: () => {
            console.warn(\`[Lifecycle:\${trigger}] game.callCommonEvent is not available from lifecycle scripts — use a Common Event's "auto" trigger instead.\`);
          }
        };
        const result = await executeScript(scr, ctx);
        if (!result.success) {
          console.error(\`[Lifecycle:\${trigger}] Script "\${scr.name}" failed:\`, result.error);
          notify(\`Script "\${scr.name}" error: \${result.error}\`, "error");
        }
      };
      for (const scr of scripts) await execLifecycle(scr, 0);
      if (store) {
        store.applyWrites(Object.entries(variableUpdates).map(([variableId, value]) => ({ variableId, value, scope: "global", sourceCommandId: \`lifecycle-\${trigger}\` })));
      }
      updatePlayerState((p) => p ? { ...p, variables: { ...p.variables, ...variableUpdates } } : null);
    }, [project, playerState == null ? void 0 : playerState.variables, updatePlayerState, assetResolver, playSound, fadeAudio, settings.musicVolume, notify]);
    React2.useEffect(() => {
      if (!playerState || playerState.mode !== "playing") {
        prevLifecycleSceneRef.current = null;
        return;
      }
      const sceneId = playerState.currentSceneId;
      if (!sceneId) return;
      const prev = prevLifecycleSceneRef.current;
      if (prev === sceneId) return;
      if (prev) runLifecycleScripts("onSceneExit", prev);
      runLifecycleScripts("onSceneEnter", sceneId);
      try {
        pluginManager.invokeHook("onSceneChange", prev || "", sceneId);
      } catch {
      }
      prevLifecycleSceneRef.current = sceneId;
    }, [playerState == null ? void 0 : playerState.currentSceneId, playerState == null ? void 0 : playerState.mode, runLifecycleScripts]);
    const parallelStateRef = React2.useRef(/* @__PURE__ */ new Map());
    const parallelWarnedRef = React2.useRef(/* @__PURE__ */ new Set());
    React2.useEffect(() => {
      const ALLOWED = /* @__PURE__ */ new Set([
        CommandType.SetVariable,
        CommandType.RunScript,
        CommandType.Wait,
        CommandType.PlayMusic,
        CommandType.StopMusic,
        CommandType.PlaySoundEffect,
        CommandType.StopSoundEffect,
        CommandType.CallCommonEvent
      ]);
      const PARALLEL_CALL_MAX_DEPTH = 8;
      const isTruthy = (v) => !(v === void 0 || v === null || v === false || v === 0 || v === "" || v === "false");
      const tick = () => {
        var _a2, _b2;
        const ps = playerStateRef.current;
        if (!ps || ps.mode !== "playing") return;
        try {
          pluginManager.invokeHook("onRuntimeTick", 120);
        } catch {
        }
        const blockingHudOpen = hudStackRef.current.some((id) => {
          var _a3;
          return !((_a3 = project.uiScreens[id]) == null ? void 0 : _a3.hudNonBlocking);
        });
        if (ps.uiState.isTransitioning || ps.uiState.choices || ps.uiState.textInput || blockingHudOpen) return;
        const events = Object.values(project.commonEvents || {});
        const active = events.filter((ce) => ce.enabled && ce.trigger === "parallel" && ce.commands && ce.commands.length > 0 && (!ce.conditionVariableId || isTruthy(ps.variables[ce.conditionVariableId])));
        const activeIds = new Set(active.map((c) => c.id));
        for (const id of Array.from(parallelStateRef.current.keys())) {
          if (!activeIds.has(id)) parallelStateRef.current.delete(id);
        }
        if (active.length === 0) return;
        const now = Date.now();
        let varAccum = null;
        let musicAccum = null;
        const buildCtx = () => ({
          project,
          playerState: { ...ps, variables: { ...ps.variables, ...varAccum || {} } },
          assetResolver,
          getAssetMetadata,
          musicAudioRef,
          fadeAudio,
          playSound,
          playVoice,
          stopAllSfx,
          stopSfx,
          settings,
          advance: () => {
          },
          setPlayerState: updatePlayerState,
          activeEffectTimeoutsRef,
          evaluateConditions: evaluateConditions2,
          notify
        });
        const resolveCe = (idOrName) => {
          const ces = project.commonEvents || {};
          return ces[idOrName] || Object.values(ces).find((e) => {
            var _a3;
            return ((_a3 = e.name) == null ? void 0 : _a3.toLowerCase()) === String(idOrName).toLowerCase();
          });
        };
        const runCalledCe = (idOrName, depth, chain) => {
          var _a3, _b3;
          if (depth > PARALLEL_CALL_MAX_DEPTH) return;
          const target = resolveCe(idOrName);
          if (!target || !target.enabled || chain.has(target.id)) return;
          const nextChain = new Set(chain);
          nextChain.add(target.id);
          for (const c of target.commands || []) {
            if (c.type === CommandType.Wait) continue;
            if (c.type === CommandType.CallCommonEvent) {
              runCalledCe(c.commonEventId, depth + 1, nextChain);
              continue;
            }
            if (!ALLOWED.has(c.type)) continue;
            try {
              const ctx = buildCtx();
              let r = null;
              switch (c.type) {
                case CommandType.SetVariable:
                  r = handleSetVariable(c, ctx);
                  break;
                case CommandType.RunScript:
                  handleRunScript(c, ctx).then((rr) => {
                    var _a4;
                    if ((_a4 = rr.updates) == null ? void 0 : _a4.variables) updatePlayerState((p) => p ? { ...p, variables: { ...p.variables, ...rr.updates.variables } } : null);
                  }).catch(() => {
                  });
                  break;
                case CommandType.PlayMusic:
                  r = handlePlayMusic(c, ctx);
                  break;
                case CommandType.StopMusic:
                  r = handleStopMusic(c, ctx);
                  break;
                case CommandType.PlaySoundEffect:
                  r = handlePlaySoundEffect(c, ctx);
                  break;
                case CommandType.StopSoundEffect:
                  r = handleStopSoundEffect(c, ctx);
                  break;
              }
              if ((_a3 = r == null ? void 0 : r.updates) == null ? void 0 : _a3.variables) varAccum = { ...varAccum || {}, ...r.updates.variables };
              if ((_b3 = r == null ? void 0 : r.updates) == null ? void 0 : _b3.musicState) musicAccum = { ...musicAccum || {}, ...r.updates.musicState };
            } catch (e) {
              console.error("[Parallel CE call] command error:", e);
            }
          }
        };
        for (const ce of active) {
          let st = parallelStateRef.current.get(ce.id);
          if (!st) {
            st = { index: 0 };
            parallelStateRef.current.set(ce.id, st);
          }
          if (st.waitUntil && now < st.waitUntil) continue;
          st.waitUntil = void 0;
          const cmds = ce.commands;
          if (st.index >= cmds.length) st.index = 0;
          const cmd = cmds[st.index];
          st.index = (st.index + 1) % cmds.length;
          if (!cmd) continue;
          if (cmd.type === CommandType.Wait) {
            const secs = typeof cmd.duration === "number" ? cmd.duration : 0;
            st.waitUntil = now + Math.max(0, secs) * 1e3;
            continue;
          }
          if (!ALLOWED.has(cmd.type)) {
            const key = \`\${ce.id}:\${cmd.type}\`;
            if (!parallelWarnedRef.current.has(key)) {
              parallelWarnedRef.current.add(key);
              console.warn(\`[Parallel CE "\${ce.name}"] command "\${cmd.type}" skipped (not background-safe).\`);
              notify(\`"\${cmd.type}" won't run in the Parallel event "\${ce.name}". Parallel events only run background commands (variables, audio, scripts). Use a "Called" or "Auto" event for on-screen commands.\`, "warning");
            }
            continue;
          }
          try {
            const ctx = buildCtx();
            let result = null;
            switch (cmd.type) {
              case CommandType.SetVariable:
                result = handleSetVariable(cmd, ctx);
                break;
              case CommandType.RunScript:
                handleRunScript(cmd, ctx).then((r) => {
                  var _a3;
                  if ((_a3 = r.updates) == null ? void 0 : _a3.variables) updatePlayerState((p) => p ? { ...p, variables: { ...p.variables, ...r.updates.variables } } : null);
                }).catch((e) => console.error(\`[Parallel CE "\${ce.name}"] script error:\`, e));
                break;
              case CommandType.PlayMusic:
                result = handlePlayMusic(cmd, ctx);
                break;
              case CommandType.StopMusic:
                result = handleStopMusic(cmd, ctx);
                break;
              case CommandType.PlaySoundEffect:
                result = handlePlaySoundEffect(cmd, ctx);
                break;
              case CommandType.StopSoundEffect:
                result = handleStopSoundEffect(cmd, ctx);
                break;
              case CommandType.CallCommonEvent:
                runCalledCe(cmd.commonEventId, 1, /* @__PURE__ */ new Set([ce.id]));
                break;
            }
            if ((_a2 = result == null ? void 0 : result.updates) == null ? void 0 : _a2.variables) varAccum = { ...varAccum || {}, ...result.updates.variables };
            if ((_b2 = result == null ? void 0 : result.updates) == null ? void 0 : _b2.musicState) musicAccum = { ...musicAccum || {}, ...result.updates.musicState };
          } catch (e) {
            console.error(\`[Parallel CE "\${ce.name}"] command error:\`, e);
          }
        }
        if (varAccum || musicAccum) {
          updatePlayerState((p) => p ? {
            ...p,
            ...varAccum ? { variables: { ...p.variables, ...varAccum } } : {},
            ...musicAccum ? { musicState: { ...p.musicState, ...musicAccum } } : {}
          } : null);
        }
      };
      const interval = window.setInterval(tick, 120);
      return () => window.clearInterval(interval);
    }, [project, assetResolver, getAssetMetadata, fadeAudio, playSound, playVoice, stopAllSfx, stopSfx, settings, updatePlayerState, evaluateConditions2, notify]);
    React2.useEffect(() => {
      const resolveVarId = (nameOrId) => {
        const p = projectRef.current;
        if (p.variables[nameOrId]) return nameOrId;
        const lower = nameOrId.toLowerCase();
        for (const [id, v] of Object.entries(p.variables)) if (v.name.toLowerCase() === lower) return id;
        return nameOrId;
      };
      pluginManager.setRuntime({
        getVariable: (nameOrId) => {
          var _a2;
          const id = resolveVarId(nameOrId);
          const store = variableStoreRef.current;
          const v = store ? store.get(id) : (_a2 = playerStateRef.current) == null ? void 0 : _a2.variables[id];
          return v === null ? void 0 : v;
        },
        setVariable: (nameOrId, value) => {
          var _a2, _b2;
          const id = resolveVarId(nameOrId);
          const old = (_a2 = playerStateRef.current) == null ? void 0 : _a2.variables[id];
          (_b2 = variableStoreRef.current) == null ? void 0 : _b2.applyWrites([{ variableId: id, value, scope: "global", sourceCommandId: "plugin" }]);
          updatePlayerState((p) => p ? { ...p, variables: { ...p.variables, [id]: value } } : null);
          if (old !== value) {
            try {
              pluginManager.invokeHook("onVariableChange", id, old, value);
            } catch {
            }
          }
        },
        notify: (message, type) => notify(message, type)
      });
      pluginManager.ensureLoaded(projectRef.current);
      try {
        pluginManager.invokeHook("onRuntimeInit");
      } catch {
      }
      return () => pluginManager.setRuntime(null);
    }, [updatePlayerState, notify]);
    React2.useEffect(() => {
      var _a2;
      const scheduler = commandSchedulerRef.current;
      const diagnostics = runtimeDiagnosticsRef.current;
      if (!playerState || playerState.mode !== "playing") {
        scheduler.reset();
        variableStoreRef.current = null;
        return;
      }
      if (playerState.uiState.isWaitingForInput || playerState.uiState.isTransitioning || playerState.uiState.choices) {
        return;
      }
      if (scenePaused) {
        return;
      }
      if (hudStack.some((id) => {
        var _a3;
        return !((_a3 = project.uiScreens[id]) == null ? void 0 : _a3.hudNonBlocking);
      })) {
        return;
      }
      const baseVariables = mergeDirtyUiVariables(playerState.variables);
      const variableStore = new RuntimeVariableStore({ globals: { ...baseVariables } });
      variableStoreRef.current = variableStore;
      const getRuntimeVariables = () => variableStore.snapshot().globals;
      if (playerState.currentIndex === 0 && playerState.commandStack.length === 0 && autoRanSceneRef.current !== playerState.currentSceneId) {
        autoRanSceneRef.current = playerState.currentSceneId;
        const autoVars = getRuntimeVariables();
        const isTruthy = (v) => !(v === void 0 || v === null || v === false || v === 0 || v === "" || v === "false");
        const autoCmds = Object.values(project.commonEvents || {}).filter((ce) => ce.enabled && ce.trigger === "auto" && (!ce.conditionVariableId || isTruthy(autoVars[ce.conditionVariableId]))).flatMap((ce) => ce.commands || []);
        if (autoCmds.length > 0) {
          updatePlayerState((p) => {
            if (!p) return null;
            const newStack = [...p.commandStack, { sceneId: p.currentSceneId, commands: p.currentCommands, index: 0 }];
            return { ...p, commandStack: newStack, currentCommands: autoCmds, currentIndex: 0 };
          });
          return;
        }
      }
      const command = playerState.currentCommands[playerState.currentIndex];
      if (!command) {
        if (playerState.commandStack.length > 0) {
          updatePlayerState((p) => {
            if (!p || p.commandStack.length === 0) return p;
            const frame = p.commandStack[p.commandStack.length - 1];
            const newStack = p.commandStack.slice(0, -1);
            let variables = p.variables;
            if (frame.savedVariables || frame.clearedVariables) {
              variables = { ...p.variables };
              if (frame.savedVariables) Object.assign(variables, frame.savedVariables);
              if (frame.clearedVariables) for (const k of frame.clearedVariables) delete variables[k];
            }
            return { ...p, currentSceneId: frame.sceneId, currentCommands: frame.commands, currentIndex: frame.index, commandStack: newStack, variables };
          });
        } else {
          runtimeDebugLog("End of scene - trying to advance to next scene");
          const sceneIds = Object.keys(project.scenes);
          const currentSceneIndex = sceneIds.indexOf(playerState.currentSceneId);
          if (currentSceneIndex !== -1 && currentSceneIndex < sceneIds.length - 1) {
            const nextSceneId = navigateToScene(sceneIds[currentSceneIndex + 1], playerState.variables);
            const nextScene = project.scenes[nextSceneId];
            if (nextScene) {
              runtimeDebugLog(\`Advancing to next scene: \${nextSceneId}\`);
              startSceneExitTransition(playerState.currentSceneId, () => {
                updatePlayerState((p) => p ? {
                  ...p,
                  currentSceneId: nextSceneId,
                  currentCommands: nextScene.commands,
                  currentIndex: 0,
                  stageState: {
                    backgroundUrl: null,
                    characters: {},
                    textOverlays: [],
                    imageOverlays: [],
                    buttonOverlays: [],
                    movieOverlays: [],
                    screen: {
                      shake: { active: false, intensity: 0 },
                      tint: "transparent",
                      zoom: 1,
                      panX: 0,
                      panY: 0,
                      transitionDuration: 0.5,
                      overlayEffects: []
                    },
                    particleEffects: {}
                  },
                  uiState: {
                    dialogue: null,
                    choices: null,
                    textInput: null,
                    movieUrl: null,
                    movieLoop: false,
                    isWaitingForInput: false,
                    isTransitioning: false,
                    transitionElement: null,
                    flash: null,
                    showHistory: false,
                    screenSceneId: null
                  }
                } : null);
              });
            } else {
              runtimeDebugLog("No valid next scene - returning to title");
              const audio = musicAudioRef.current;
              if (audio) {
                audio.pause();
                audio.currentTime = 0;
                audio.src = "";
              }
              stopAllSfx();
              updatePlayerState(null);
              if (project.ui.titleScreenId) {
                setScreenStack([project.ui.titleScreenId]);
              }
            }
          } else {
            runtimeDebugLog("Last scene completed - returning to title");
            const audio = musicAudioRef.current;
            if (audio) {
              audio.pause();
              audio.currentTime = 0;
              audio.src = "";
            }
            stopAllSfx();
            updatePlayerState(null);
            if (project.ui.titleScreenId) {
              setScreenStack([project.ui.titleScreenId]);
            }
          }
        }
        scheduler.reset();
        return;
      }
      const commandSignature = {
        sceneId: playerState.currentSceneId,
        index: playerState.currentIndex,
        commandId: command.id
      };
      if (!scheduler.shouldProcess(commandSignature)) {
        return;
      }
      scheduler.markProcessed(commandSignature);
      diagnostics.emit("command-start", {
        sceneId: commandSignature.sceneId,
        commandId: commandSignature.commandId,
        index: commandSignature.index
      });
      if (command.type === CommandType.BranchStart) {
        const cmds = playerState.currentCommands;
        const startIdx = playerState.currentIndex;
        const branchId = command.branchId;
        const vars = getRuntimeVariables();
        const endIdx = cmds.findIndex(
          (c, i) => i > startIdx && c.type === CommandType.BranchEnd && c.branchId === branchId
        );
        const nextMarker = (from) => cmds.findIndex(
          (c, i) => i > from && c.branchId === branchId && (c.type === CommandType.BranchElseIf || c.type === CommandType.BranchElse || c.type === CommandType.BranchEnd)
        );
        let segIdx = startIdx;
        let target;
        while (true) {
          const seg = cmds[segIdx];
          const segMet = seg.type === CommandType.BranchElse ? true : evaluateConditions2(seg.conditions, vars);
          if (segMet) {
            target = segIdx + 1;
            break;
          }
          const nm = nextMarker(segIdx);
          if (nm === -1) {
            target = endIdx !== -1 ? endIdx + 1 : startIdx + 1;
            break;
          }
          if (cmds[nm].type === CommandType.BranchEnd) {
            target = nm + 1;
            break;
          }
          segIdx = nm;
        }
        updatePlayerState((p) => p ? { ...p, currentIndex: target } : null);
        return;
      }
      const conditionsMet = evaluateConditions2(command.conditions, getRuntimeVariables());
      const isLiveReactive = !!command.liveConditions && (REACTIVE_VISUAL_TYPES.has(command.type) || command.type === CommandType.PlaySoundEffect);
      runtimeDebugLog("[DEBUG] Command:", command.type, "Index:", playerState.currentIndex, "Conditions met:", conditionsMet, "live:", isLiveReactive, "Variables:", getRuntimeVariables());
      if (!conditionsMet && !isLiveReactive) {
        runtimeDebugLog("[DEBUG] Skipping command due to failed conditions");
        updatePlayerState((p) => p ? { ...p, currentIndex: p.currentIndex + 1 } : null);
        return;
      }
      {
        const ffTarget = fastForwardTargetRef.current;
        if (ffTarget != null) {
          if (playerState.currentIndex >= ffTarget) {
            fastForwardTargetRef.current = null;
          } else if (!FF_VISUAL_TYPES.has(command.type)) {
            updatePlayerState((p) => p ? { ...p, currentIndex: p.currentIndex + 1 } : null);
            return;
          }
        }
      }
      const advance = () => {
        runtimeDebugLog("[DEBUG advance()] Called from command:", command.type, "Current index:", playerState.currentIndex);
        if (scheduler.alreadyAdvancedPast(playerState.currentIndex)) {
          const last = scheduler.getLastProcessed();
          if (last) {
            runtimeDebugLog("[DEBUG advance()] Skipping - already advanced to", last.index);
          }
          return;
        }
        const nextIndex = playerState.currentIndex + 1;
        if (nextIndex >= playerState.currentCommands.length) {
          if (playerState.commandStack.length > 0) {
            updatePlayerState((p) => {
              if (!p || p.commandStack.length === 0) return p;
              const frame = p.commandStack[p.commandStack.length - 1];
              const newStack = p.commandStack.slice(0, -1);
              let variables = p.variables;
              if (frame.savedVariables || frame.clearedVariables) {
                variables = { ...p.variables };
                if (frame.savedVariables) Object.assign(variables, frame.savedVariables);
                if (frame.clearedVariables) for (const k of frame.clearedVariables) delete variables[k];
              }
              return { ...p, currentSceneId: frame.sceneId, currentCommands: frame.commands, currentIndex: frame.index, commandStack: newStack, variables };
            });
          } else {
            const sceneIds = Object.keys(project.scenes);
            const currentSceneIndex = sceneIds.indexOf(playerState.currentSceneId);
            if (currentSceneIndex !== -1 && currentSceneIndex < sceneIds.length - 1) {
              const nextSceneId = navigateToScene(sceneIds[currentSceneIndex + 1], getRuntimeVariables());
              const nextScene = project.scenes[nextSceneId];
              if (nextScene) {
                startSceneExitTransition(playerState.currentSceneId, () => {
                  updatePlayerState((p) => p ? {
                    ...p,
                    currentSceneId: nextSceneId,
                    currentCommands: nextScene.commands,
                    currentIndex: 0,
                    stageState: {
                      backgroundUrl: null,
                      characters: {},
                      textOverlays: [],
                      imageOverlays: [],
                      buttonOverlays: [],
                      movieOverlays: [],
                      screen: {
                        shake: { active: false, intensity: 0 },
                        tint: "transparent",
                        zoom: 1,
                        panX: 0,
                        panY: 0,
                        transitionDuration: 0.5,
                        overlayEffects: []
                      },
                      particleEffects: {}
                    },
                    uiState: {
                      dialogue: null,
                      choices: null,
                      textInput: null,
                      movieUrl: null,
                      movieLoop: false,
                      isWaitingForInput: false,
                      isTransitioning: false,
                      transitionElement: null,
                      flash: null,
                      showHistory: false,
                      screenSceneId: null
                    }
                  } : null);
                });
                return;
              }
            }
            const audio = musicAudioRef.current;
            if (audio) {
              audio.pause();
              audio.currentTime = 0;
              audio.src = "";
            }
            stopAllSfx();
            updatePlayerState(null);
            if (project.ui.titleScreenId) {
              setScreenStack([project.ui.titleScreenId]);
            }
            scheduler.reset();
          }
        } else {
          updatePlayerState((p) => p ? { ...p, currentIndex: nextIndex } : null);
        }
      };
      const shouldRunAsync = ((_a2 = command.modifiers) == null ? void 0 : _a2.runAsync) === true;
      const commandContext = {
        project,
        playerState,
        assetResolver,
        getAssetMetadata,
        musicAudioRef,
        fadeAudio,
        playSound,
        playVoice,
        stopAllSfx,
        stopSfx,
        settings,
        advance,
        setPlayerState: updatePlayerState,
        activeEffectTimeoutsRef,
        evaluateConditions: evaluateConditions2,
        notify,
        isStandalone
      };
      commandContext.runCommand = async (type, params) => {
        var _a3, _b2, _c2, _d2, _e2, _f2, _g2, _h2, _i2, _j2, _k2;
        try {
          const base = createCommand(type, project, {});
          if (!base) {
            console.warn("[Script] runCommand: cannot build command", type);
            return;
          }
          const cmd = { ...base, ...params, id: \`script-cmd-\${Math.random().toString(36).slice(2, 9)}\` };
          const hctx = { ...commandContext, advance: () => {
          } };
          let result;
          switch (type) {
            case "ShowCharacter":
              result = handleShowCharacter(cmd, hctx);
              break;
            case "HideCharacter":
              result = handleHideCharacter(cmd, hctx);
              break;
            case "SetBackground":
              result = await handleSetBackground(cmd, hctx);
              break;
            case "ShowImage":
              result = handleShowImage(cmd, hctx);
              break;
            case "SpawnParticles":
              result = handleSpawnParticles(cmd, hctx);
              break;
            case "StopParticles":
              result = handleStopParticles(cmd, hctx);
              break;
            case "TweenElement":
              result = handleTweenElement(cmd, hctx);
              break;
            case "MoveCharacter":
              result = handleMoveCharacter(cmd, hctx);
              break;
            // Additional pure handlers (Phase A): each returns a CommandResult applied via the
            // decoupled applier below (stage/ui/music/variables + stagePatch). Safe to bridge
            // because none depend on the main loop's advance/index.
            case "SetCharacterLayer":
              result = handleSetCharacterLayer(cmd, hctx);
              break;
            case "ShowText":
              result = handleShowText(cmd, hctx);
              break;
            case "HideText":
              result = handleHideText(cmd, hctx);
              break;
            case "HideImage":
              result = handleHideImage(cmd, hctx);
              break;
            case "ShowButton":
              result = handleShowButton(cmd, hctx);
              break;
            case "HideButton":
              result = handleHideButton(cmd, hctx);
              break;
            case "ShowItem":
              result = handleShowItem(cmd, hctx);
              break;
            case "PlayMusic":
              result = handlePlayMusic(cmd, hctx);
              break;
            case "StopMusic":
              result = handleStopMusic(cmd, hctx);
              break;
            case "PlaySoundEffect":
              result = handlePlaySoundEffect(cmd, hctx);
              break;
            case "StopSoundEffect":
              result = handleStopSoundEffect(cmd, hctx);
              break;
            case "ShowPhone":
              result = handleShowPhone(cmd, hctx);
              break;
            case "HidePhone":
              result = handleHidePhone(cmd, hctx);
              break;
            case "ShowPhoneText":
              result = handleShowPhoneText(cmd, hctx);
              break;
            case "HidePhoneText":
              result = handleHidePhoneText(cmd, hctx);
              break;
            case "GiveItem":
            case "UseItem":
            case "DestroyItem":
              result = handleItemCommand(cmd, hctx);
              break;
            case "RestockCollection":
              result = handleRestockCollectionCommand(cmd, hctx);
              break;
            case "BuyItem":
              result = handleBuyItemCommand(cmd, hctx);
              break;
            case "SellItem":
              result = handleSellItemCommand(cmd, hctx);
              break;
            case "PlaceLights":
              result = { advance: true, stagePatch: () => ({ lights: cmd.lights || [], lightsAbove: !!cmd.aboveCharacters, lightsBrightnessVariableId: cmd.brightnessVariableId ?? null }) };
              break;
            case "ClearLights":
              result = { advance: true, stagePatch: () => ({ lights: [], lightsBrightnessVariableId: null }) };
              break;
            case "CreditRoll": {
              setActiveCreditRoll(cmd);
              result = handleCreditRoll(cmd, hctx);
              break;
            }
            // Screen effects are INLINE in the main loop (component refs/state), so we replicate
            // the exact same effect here (identical refs/render path) rather than touch the loop.
            case "ShakeScreen": {
              activeShakeRef.current = { intensity: resolveVarNumber((_a3 = playerStateRef.current) == null ? void 0 : _a3.variables, cmd.intensityVariableId, cmd.intensity, { min: 1, max: 10 }), duration: cmd.duration };
              setShakeTrigger((prev) => prev + 1);
              if (cmd.duration > 0) {
                const tid = window.setTimeout(() => {
                  activeShakeRef.current = null;
                  setShakeTrigger((prev) => prev + 1);
                  activeEffectTimeoutsRef.current = activeEffectTimeoutsRef.current.filter((id) => id !== tid);
                }, cmd.duration * 1e3);
                activeEffectTimeoutsRef.current.push(tid);
              }
              return;
            }
            case "FlashScreen": {
              activeFlashRef.current = { color: cmd.color, duration: resolveVarNumber((_b2 = playerStateRef.current) == null ? void 0 : _b2.variables, cmd.durationVariableId, cmd.duration, { min: 0.05, max: 30 }), key: Date.now() };
              setFlashTrigger((prev) => prev + 1);
              return;
            }
            case "TintScreen": {
              updatePlayerState((p) => p ? { ...p, stageState: { ...p.stageState, screen: { ...p.stageState.screen, tint: cmd.color, tintOpacity: cmd.opacity ?? 100, tintOpacityVariableId: cmd.opacityVariableId ?? null, transitionDuration: cmd.duration } } } : p);
              return;
            }
            case "PanZoomScreen": {
              const pzVars = (_c2 = playerStateRef.current) == null ? void 0 : _c2.variables;
              const pzZoom = resolveVarNumber(pzVars, cmd.zoomVariableId, cmd.zoom, { min: 0.1, max: 5 });
              const pzX = resolveVarNumber(pzVars, cmd.panXVariableId, cmd.panX, { min: -100, max: 100 });
              const pzY = resolveVarNumber(pzVars, cmd.panYVariableId, cmd.panY, { min: -100, max: 100 });
              updatePlayerState((p) => p ? { ...p, stageState: { ...p.stageState, screen: { ...p.stageState.screen, zoom: pzZoom, panX: pzX, panY: pzY, transitionDuration: cmd.duration } } } : p);
              return;
            }
            case "ResetScreenEffects": {
              updatePlayerState((p) => p ? { ...p, stageState: { ...p.stageState, screen: { ...p.stageState.screen, tint: "transparent", tintOpacity: 100, tintOpacityVariableId: null, zoom: 1, panX: 0, panY: 0, transitionDuration: cmd.duration, overlayEffects: [] } } } : p);
              return;
            }
            // More inline-effect commands (refs/component state) — replicate the main loop's exact
            // behaviour so the same render path runs, without touching the loop.
            case "Flashlight": {
              if (cmd.enabled) {
                setFlashlight({
                  radius: cmd.radius ?? 22,
                  softness: cmd.softness ?? 0.6,
                  darkness: cmd.darkness ?? 0.85,
                  radiusVariableId: cmd.radiusVariableId ?? null,
                  darknessVariableId: cmd.darknessVariableId ?? null,
                  color: cmd.color || "#000000",
                  toggleKey: cmd.toggleKey,
                  affectsDialogue: cmd.affectsDialogue !== false,
                  darkWhenOff: cmd.darkWhenOff === true,
                  on: true
                });
                if (cmd.sfxId) playSound(cmd.sfxId);
              } else {
                setFlashlight(null);
              }
              return;
            }
            case "Spotlight": {
              const sid = cmd.spotlightId || "main";
              if (cmd.enabled) {
                setSpotlights((prev) => ({ ...prev, [sid]: makeSpotlightState(cmd) }));
                if (cmd.sfxId) playSound(cmd.sfxId);
              } else {
                setSpotlights((prev) => {
                  const n = { ...prev };
                  delete n[sid];
                  return n;
                });
              }
              return;
            }
            case "Lightning": {
              activeLightningRef.current = {
                color: cmd.color || "#EAF2FF",
                intensity: resolveVarNumber((_d2 = playerStateRef.current) == null ? void 0 : _d2.variables, cmd.intensityVariableId, cmd.intensity ?? 0.9, { min: 0, max: 1 }),
                duration: cmd.duration ?? 0.7,
                flashes: cmd.flashes ?? 2,
                affectsDialogue: cmd.affectsDialogue !== false,
                key: Date.now()
              };
              setLightningTrigger((prev) => prev + 1);
              if (cmd.thunderSfxId) {
                const tid = window.setTimeout(() => playSound(cmd.thunderSfxId, cmd.thunderVolume), Math.max(0, (cmd.thunderDelay ?? 0.6) * 1e3));
                activeEffectTimeoutsRef.current.push(tid);
              }
              return;
            }
            case "Fireworks": {
              activeFireworksRef.current = {
                colors: cmd.colors && cmd.colors.length ? cmd.colors : [],
                intensity: resolveVarNumber((_e2 = playerStateRef.current) == null ? void 0 : _e2.variables, cmd.intensityVariableId, cmd.intensity ?? 1, { min: 0, max: 1 }),
                bursts: Math.round(resolveVarNumber((_f2 = playerStateRef.current) == null ? void 0 : _f2.variables, cmd.burstsVariableId, Math.max(1, cmd.bursts ?? 3), { min: 1, max: 20 })),
                duration: cmd.duration ?? 2.5,
                burstHeight: resolveVarNumber((_g2 = playerStateRef.current) == null ? void 0 : _g2.variables, cmd.burstHeightVariableId, cmd.burstHeight ?? 0.7, { min: 0, max: 1 }),
                affectsDialogue: cmd.affectsDialogue !== false,
                sfxId: cmd.sfxId ?? null,
                sfxVolume: cmd.sfxVolume,
                sfxPerBurst: !!cmd.sfxPerBurst,
                key: Date.now()
              };
              setFireworksTrigger((prev) => prev + 1);
              if (cmd.sfxId && !cmd.sfxPerBurst) {
                const tid = window.setTimeout(() => playSound(cmd.sfxId, cmd.sfxVolume), Math.max(0, (cmd.sfxDelay ?? 0.3) * 1e3));
                activeEffectTimeoutsRef.current.push(tid);
              }
              return;
            }
            case "SetScreenOverlayEffect": {
              updatePlayerState((p) => p ? { ...p, stageState: { ...p.stageState, screen: { ...p.stageState.screen, overlayEffects: upsertOverlayEffect(p.stageState.screen.overlayEffects, { type: cmd.effectType, intensity: cmd.intensity, intensityVariableId: cmd.intensityVariableId ?? null, variant: cmd.variant, color: cmd.color, params: cmd.params }) } } } : p);
              const overlayDur = cmd.duration ?? 0;
              if (overlayDur > 0) {
                const effType = cmd.effectType;
                const tid = window.setTimeout(() => {
                  updatePlayerState((p) => p ? { ...p, stageState: { ...p.stageState, screen: { ...p.stageState.screen, overlayEffects: upsertOverlayEffect(p.stageState.screen.overlayEffects, { type: effType, intensity: 0 }) } } } : p);
                  activeEffectTimeoutsRef.current = activeEffectTimeoutsRef.current.filter((id) => id !== tid);
                }, overlayDur * 1e3);
                activeEffectTimeoutsRef.current.push(tid);
              }
              return;
            }
            case "ShowHotSpot": {
              updatePlayerState((p) => p ? { ...p, stageState: { ...p.stageState, hotSpotOverlays: [
                ...(p.stageState.hotSpotOverlays || []).filter((h) => h.commandId !== cmd.id),
                {
                  id: cmd.id,
                  commandId: cmd.id,
                  name: cmd.name,
                  x: cmd.x,
                  y: cmd.y,
                  width: cmd.width,
                  height: cmd.height,
                  shape: cmd.shape,
                  trigger: cmd.trigger,
                  actions: cmd.actions,
                  conditions: cmd.conditions,
                  acceptedTag: cmd.acceptedTag,
                  highlightColor: cmd.highlightColor,
                  visible: cmd.visible,
                  advanceOnTrigger: cmd.advanceOnTrigger,
                  layer: cmd.layer
                }
              ] } } : p);
              return;
            }
            case "HideHotSpot": {
              updatePlayerState((p) => p ? { ...p, stageState: { ...p.stageState, hotSpotOverlays: (p.stageState.hotSpotOverlays || []).filter((h) => h.commandId !== cmd.targetCommandId) } } : p);
              return;
            }
            // BLOCKING commands: show the UI (real handler), then PAUSE the script until the player
            // responds. The input handlers resolve \`scriptInputResolverRef\` (set just below). When
            // no script is awaiting, that ref is null, so normal playback is untouched.
            case "Dialogue": {
              const r = handleDialogue(cmd, hctx);
              if ((_h2 = r.updates) == null ? void 0 : _h2.uiState) updatePlayerState((p) => p ? { ...p, uiState: { ...p.uiState, ...r.updates.uiState } } : p);
              return new Promise((resolve) => {
                scriptInputResolverRef.current = { kind: "dialogue", resolve: () => resolve() };
              });
            }
            case "Choice": {
              const r = handleChoice(cmd, hctx);
              if ((_i2 = r.updates) == null ? void 0 : _i2.uiState) updatePlayerState((p) => p ? { ...p, uiState: { ...p.uiState, ...r.updates.uiState } } : p);
              return new Promise((resolve) => {
                scriptInputResolverRef.current = { kind: "choice", resolve: (idx) => resolve(typeof idx === "number" ? idx : 0) };
              });
            }
            case "TextInput": {
              const r = handleTextInput(cmd, hctx);
              if ((_j2 = r.updates) == null ? void 0 : _j2.uiState) updatePlayerState((p) => p ? { ...p, uiState: { ...p.uiState, ...r.updates.uiState } } : p);
              return new Promise((resolve) => {
                scriptInputResolverRef.current = { kind: "textInput", resolve: (val) => resolve(typeof val === "string" ? val : String(val ?? "")) };
              });
            }
            // Movie playback (inline in the main loop — replicate here). Overlay = non-blocking;
            // fullscreen + waitsForCompletion = AWAIT until the movie ends (resolved by the movie's
            // onEnded/onClick guards) — the 'movie' resolver. Non-wait fullscreen = fire-and-forget.
            case "PlayMovie": {
              const movieUrl = assetResolver(cmd.videoId, "video");
              const sTrim = resolveVideoTrim(cmd, project.videos[cmd.videoId]);
              if (cmd.displayMode === "overlay") {
                updatePlayerState((p) => p ? { ...p, stageState: { ...p.stageState, movieOverlays: [
                  ...p.stageState.movieOverlays || [],
                  { url: movieUrl || "", loop: cmd.loop ?? false, trimStart: sTrim.start, trimEnd: sTrim.end, holdLastFrame: cmd.holdLastFrame ?? false, transition: cmd.transition, transitionDuration: cmd.transitionDuration, commandId: cmd.id, parallaxDepth: cmd.parallaxDepth, x: cmd.x, y: cmd.y, width: cmd.width, height: cmd.height, opacity: cmd.opacity, objectFit: cmd.objectFit }
                ] } } : p);
                return;
              }
              updatePlayerState((p) => p ? { ...p, uiState: { ...p.uiState, movieUrl, movieLoop: cmd.loop ?? false, movieTrimStart: sTrim.start, movieTrimEnd: sTrim.end, movieHoldLastFrame: cmd.holdLastFrame ?? false, movieTransition: cmd.transition, movieTransitionDuration: cmd.transitionDuration, movieExiting: false } } : p);
              if (cmd.waitsForCompletion !== false && !(cmd.loop ?? false)) {
                return new Promise((resolve) => {
                  scriptInputResolverRef.current = { kind: "movie", resolve: () => resolve() };
                });
              }
              return;
            }
            case "StopMovie": {
              updatePlayerState((p) => p ? { ...p, stageState: { ...p.stageState, movieOverlays: [] }, uiState: { ...p.uiState, movieUrl: null, movieLoop: false } } : p);
              return;
            }
            default:
              console.warn("[Script] runCommand: unsupported command type", type);
              return;
          }
          if (!result) return;
          if (((_k2 = result.updates) == null ? void 0 : _k2.variables) && variableStoreRef.current) {
            variableStoreRef.current.applyWrites(Object.entries(result.updates.variables).map(([variableId, value]) => ({ variableId, value, scope: "global", sourceCommandId: cmd.id })));
          }
          if (result.updates || result.stagePatch) {
            updatePlayerState((p) => {
              var _a4, _b3, _c3, _d3;
              if (!p) return p;
              let nextStage = ((_a4 = result.updates) == null ? void 0 : _a4.stageState) !== void 0 ? { ...p.stageState, ...result.updates.stageState } : void 0;
              if (result.stagePatch) nextStage = { ...nextStage ?? p.stageState, ...result.stagePatch(p.stageState) };
              const mergedVars = ((_b3 = result.updates) == null ? void 0 : _b3.variables) ? variableStoreRef.current ? variableStoreRef.current.snapshot().globals : { ...p.variables, ...result.updates.variables } : void 0;
              return {
                ...p,
                ...nextStage !== void 0 ? { stageState: nextStage } : {},
                ...mergedVars !== void 0 ? { variables: mergedVars } : {},
                ...((_c3 = result.updates) == null ? void 0 : _c3.uiState) !== void 0 ? { uiState: { ...p.uiState, ...result.updates.uiState } } : {},
                ...((_d3 = result.updates) == null ? void 0 : _d3.musicState) !== void 0 ? { musicState: { ...p.musicState, ...result.updates.musicState } } : {}
              };
            });
          }
          if (result.delay && result.callback) {
            const tid = window.setTimeout(result.callback, result.delay);
            activeEffectTimeoutsRef.current.push(tid);
          } else if (result.callback) {
            result.callback();
          }
        } catch (e) {
          console.error("[Script] runCommand error:", e);
        }
      };
      commandContext.runUIAction = (actionType, params) => {
        var _a3;
        try {
          (_a3 = handleUIActionRef.current) == null ? void 0 : _a3.call(handleUIActionRef, { type: actionType, ...params || {} });
        } catch (e) {
          console.error("[Script] runUIAction error:", e);
        }
      };
      let instantAdvance = true;
      (async () => {
        var _a3, _b2, _c2, _d2, _e2, _f2, _g2, _h2, _i2, _j2, _k2, _l2, _m2;
        try {
          const applyResult = (result) => {
            var _a4, _b3, _c3, _d3;
            const variableStore2 = variableStoreRef.current;
            const previousSceneId = playerState == null ? void 0 : playerState.currentSceneId;
            if (((_a4 = result.updates) == null ? void 0 : _a4.variables) && variableStore2) {
              const writes = Object.entries(result.updates.variables).map(([variableId, value]) => ({
                variableId,
                value,
                scope: "global",
                sourceCommandId: command.id
              }));
              variableStore2.applyWrites(writes);
            }
            if ((_b3 = result.updates) == null ? void 0 : _b3.variables) {
              const prevVars = (playerState == null ? void 0 : playerState.variables) || {};
              for (const [vid, val] of Object.entries(result.updates.variables)) {
                if (prevVars[vid] !== val) {
                  try {
                    pluginManager.invokeHook("onVariableChange", vid, prevVars[vid], val);
                  } catch {
                  }
                }
              }
            }
            if (result.updates || result.stagePatch) {
              const isSceneChange = ((_c3 = result.updates) == null ? void 0 : _c3.currentSceneId) !== void 0 && result.updates.currentSceneId !== previousSceneId;
              updatePlayerState((p) => {
                var _a5, _b4, _c4, _d4, _e3, _f3, _g3, _h3, _i3, _j3;
                if (!p) return null;
                let mergedVariables = ((_a5 = result.updates) == null ? void 0 : _a5.variables) && variableStore2 ? variableStore2.snapshot().globals : { ...p.variables, ...((_b4 = result.updates) == null ? void 0 : _b4.variables) ?? {} };
                if (isSceneChange) {
                  const localDefaults = getLocalVariableDefaults(project.variables);
                  mergedVariables = { ...mergedVariables, ...localDefaults };
                  runtimeDebugLog("[Variable Scope] Reset local variables on scene change:", Object.keys(localDefaults));
                }
                let nextStage = ((_c4 = result.updates) == null ? void 0 : _c4.stageState) !== void 0 ? { ...p.stageState, ...result.updates.stageState } : void 0;
                if (result.stagePatch) {
                  nextStage = { ...nextStage ?? p.stageState, ...result.stagePatch(p.stageState) };
                }
                return {
                  ...p,
                  ...((_d4 = result.updates) == null ? void 0 : _d4.currentSceneId) !== void 0 ? { currentSceneId: result.updates.currentSceneId } : {},
                  ...((_e3 = result.updates) == null ? void 0 : _e3.currentCommands) !== void 0 ? { currentCommands: result.updates.currentCommands } : {},
                  ...((_f3 = result.updates) == null ? void 0 : _f3.currentIndex) !== void 0 ? { currentIndex: result.updates.currentIndex } : {},
                  ...((_g3 = result.updates) == null ? void 0 : _g3.commandStack) !== void 0 ? { commandStack: result.updates.commandStack } : {},
                  ...((_h3 = result.updates) == null ? void 0 : _h3.variables) !== void 0 || isSceneChange ? { variables: mergedVariables } : {},
                  ...nextStage !== void 0 ? { stageState: nextStage } : {},
                  ...((_i3 = result.updates) == null ? void 0 : _i3.musicState) !== void 0 ? { musicState: { ...p.musicState, ...result.updates.musicState } } : {},
                  ...((_j3 = result.updates) == null ? void 0 : _j3.uiState) !== void 0 ? { uiState: { ...p.uiState, ...result.updates.uiState } } : {}
                };
              });
              if (((_d3 = result.updates) == null ? void 0 : _d3.currentSceneId) !== void 0 && result.updates.currentSceneId !== previousSceneId) {
                runtimeDebugLog("[Scene Cleanup] Scene changed from", previousSceneId, "to", result.updates.currentSceneId, "- clearing UI stacks");
                setScreenStack([]);
                setHudStack([]);
                activeEffectTimeoutsRef.current.forEach((timeoutId) => clearTimeout(timeoutId));
                activeEffectTimeoutsRef.current = [];
                activeFlashRef.current = null;
                setFlashTrigger(0);
                activeLightningRef.current = null;
                activeFireworksRef.current = null;
                setFlashlight(null);
                setSpotlights({});
                activeShakeRef.current = null;
                scheduler.reset();
                variableStoreRef.current = null;
                if (scriptInputResolverRef.current) {
                  const r = scriptInputResolverRef.current;
                  scriptInputResolverRef.current = null;
                  r.resolve(void 0);
                }
              }
            }
            instantAdvance = result.advance;
            if (result.delay && result.callback) {
              const timeoutId = window.setTimeout(result.callback, result.delay);
              activeEffectTimeoutsRef.current.push(timeoutId);
            } else if (result.callback) {
              result.callback();
            }
            diagnostics.emit("command-finish", {
              commandId: command.id,
              sceneId: playerState.currentSceneId,
              index: playerState.currentIndex,
              advance: result.advance
            });
          };
          const hw = backwardReplayRef.current;
          if (hw && (playerState.currentSceneId !== hw.sceneId || playerState.currentIndex >= hw.index)) {
            backwardReplayRef.current = null;
          }
          const replayingBackward = backwardReplayRef.current != null;
          const savedInputKey = \`\${playerState.currentSceneId}:\${playerState.currentIndex}\`;
          const savedInput = playerState.savedInputs[savedInputKey];
          if (replayingBackward && savedInput && command.type === CommandType.Choice && savedInput.type === "choice") {
            runtimeDebugLog("[BACKWARD REPLAY] Auto-replaying saved choice:", savedInput.choice.text);
            handleChoiceSelect(savedInput.choice);
            return;
          }
          if (replayingBackward && savedInput && command.type === CommandType.TextInput && savedInput.type === "textInput") {
            runtimeDebugLog("[BACKWARD REPLAY] Auto-replaying saved text input:", savedInput.value);
            const cmd = command;
            updatePlayerState((p) => {
              if (!p) return p;
              const historyEntry = {
                timestamp: Date.now(),
                type: "textInput",
                text: \`Input: \${savedInput.value}\`,
                inputValue: savedInput.value,
                variableId: cmd.variableId,
                sceneId: p.currentSceneId,
                commandIndex: p.currentIndex,
                stageSnapshot: JSON.parse(JSON.stringify(p.stageState)),
                variablesSnapshot: { ...p.variables },
                musicSnapshot: { ...p.musicState }
              };
              const newHistory = [...p.history, historyEntry];
              if (newHistory.length > 200) newHistory.splice(0, newHistory.length - 200);
              return {
                ...p,
                currentIndex: p.currentIndex + 1,
                variables: { ...p.variables, [cmd.variableId]: savedInput.value },
                history: newHistory,
                uiState: { ...p.uiState, isWaitingForInput: false, textInput: null }
              };
            });
            return;
          }
          try {
            pluginManager.invokeHook("onBeforeCommand", command);
          } catch {
          }
          switch (command.type) {
            case CommandType.Group: {
              const result = handleGroup();
              applyResult(result);
              break;
            }
            case CommandType.BranchStart: {
              const result = handleBranchStart();
              applyResult(result);
              break;
            }
            case CommandType.BranchElseIf: {
              const result = handleBranchElseIf(command, commandContext);
              applyResult(result);
              break;
            }
            case CommandType.BranchElse: {
              const result = handleBranchElse(command, commandContext);
              applyResult(result);
              break;
            }
            case CommandType.BranchEnd: {
              const result = handleBranchEnd();
              applyResult(result);
              break;
            }
            case CommandType.Dialogue: {
              const result = handleDialogue(command, commandContext);
              applyResult(result);
              break;
            }
            case CommandType.SetBackground: {
              const result = await handleSetBackground(command, commandContext);
              applyResult(result);
              break;
            }
            case CommandType.ShowCharacter: {
              const result = handleShowCharacter(command, commandContext);
              applyResult(result);
              break;
            }
            case CommandType.HideCharacter: {
              const result = handleHideCharacter(command, commandContext);
              applyResult(result);
              break;
            }
            case CommandType.SetCharacterLayer: {
              const result = handleSetCharacterLayer(command, commandContext);
              applyResult(result);
              break;
            }
            case CommandType.Choice: {
              const result = handleChoice(command, commandContext);
              applyResult(result);
              break;
            }
            case CommandType.SetVariable: {
              const result = handleSetVariable(command, commandContext);
              applyResult(result);
              const setVarCmd = command;
              const varDef = project.variables[setVarCmd.variableId];
              if (varDef && varDef.scope === "persistent" && ((_a3 = result.updates) == null ? void 0 : _a3.variables)) {
                const persistentSnapshot = {};
                Object.values(project.variables).forEach((v) => {
                  if ((v.scope || "global") === "persistent" && result.updates.variables[v.id] !== void 0) {
                    persistentSnapshot[v.id] = result.updates.variables[v.id];
                  }
                });
                savePersistentVariables(project.id, { ...loadPersistentVariables(project.id), ...persistentSnapshot });
                runtimeDebugLog("[Variable Scope] Saved persistent variable:", varDef.name);
              }
              break;
            }
            case CommandType.TextInput: {
              const result = handleTextInput(command, commandContext);
              applyResult(result);
              break;
            }
            case CommandType.GiveItem:
            case CommandType.UseItem:
            case CommandType.DestroyItem: {
              applyResult(handleItemCommand(command, commandContext));
              break;
            }
            case CommandType.RestockCollection: {
              applyResult(handleRestockCollectionCommand(command, commandContext));
              break;
            }
            case CommandType.BuyItem: {
              applyResult(handleBuyItemCommand(command, commandContext));
              break;
            }
            case CommandType.SellItem: {
              applyResult(handleSellItemCommand(command, commandContext));
              break;
            }
            case CommandType.Jump: {
              if (playerState.uiState.isSkipping) {
                updatePlayerState((p) => p ? { ...p, uiState: { ...p.uiState, isSkipping: false } } : null);
              }
              startSceneExitTransition(playerState.currentSceneId, () => {
                const result = handleJump(command, commandContext);
                applyResult(result);
              });
              break;
            }
            case CommandType.PlayMusic: {
              const result = handlePlayMusic(command, commandContext);
              applyResult(result);
              break;
            }
            case CommandType.StopMusic: {
              const result = handleStopMusic(command, commandContext);
              applyResult(result);
              break;
            }
            case CommandType.PlaySoundEffect: {
              const sfxCmd = command;
              if (sfxCmd.liveConditions) {
                liveSfxRef.current.set(sfxCmd.id, {
                  audioId: sfxCmd.audioId,
                  conditions: sfxCmd.conditions,
                  loop: !!sfxCmd.loop,
                  volume: sfxCmd.volume,
                  audio: null,
                  lastMet: false
                });
                setLiveSfxTick((t) => t + 1);
                applyResult({ advance: true });
              } else {
                const result = handlePlaySoundEffect(sfxCmd, commandContext);
                applyResult(result);
              }
              break;
            }
            case CommandType.StopSoundEffect: {
              const result = handleStopSoundEffect(command, commandContext);
              applyResult(result);
              break;
            }
            case CommandType.PlayMovie: {
              const movieCmd = command;
              const movieUrl = assetResolver(movieCmd.videoId, "video");
              const isOverlay = movieCmd.displayMode === "overlay";
              const shouldLoop = movieCmd.loop ?? false;
              const holdLastFrame = movieCmd.holdLastFrame ?? false;
              const movieTransition = movieCmd.transition;
              const movieTransitionDuration = movieCmd.transitionDuration;
              const movieTrim = resolveVideoTrim(movieCmd, project.videos[movieCmd.videoId]);
              if (isOverlay) {
                updatePlayerState((p) => {
                  if (!p) return null;
                  const existing = p.stageState.movieOverlays || [];
                  return {
                    ...p,
                    stageState: {
                      ...p.stageState,
                      movieOverlays: [...existing, {
                        url: movieUrl || "",
                        loop: shouldLoop,
                        trimStart: movieTrim.start,
                        trimEnd: movieTrim.end,
                        holdLastFrame,
                        transition: movieTransition,
                        transitionDuration: movieTransitionDuration,
                        commandId: movieCmd.id,
                        parallaxDepth: movieCmd.parallaxDepth,
                        x: movieCmd.x,
                        y: movieCmd.y,
                        width: movieCmd.width,
                        height: movieCmd.height,
                        opacity: movieCmd.opacity,
                        objectFit: movieCmd.objectFit
                      }]
                    }
                  };
                });
              } else {
                if (movieCmd.waitsForCompletion) {
                  instantAdvance = false;
                  updatePlayerState((p) => p ? {
                    ...p,
                    uiState: { ...p.uiState, isWaitingForInput: true, movieUrl, movieLoop: shouldLoop, movieTrimStart: movieTrim.start, movieTrimEnd: movieTrim.end, movieHoldLastFrame: holdLastFrame, movieTransition, movieTransitionDuration, movieExiting: false }
                  } : null);
                } else {
                  updatePlayerState((p) => p ? {
                    ...p,
                    uiState: { ...p.uiState, movieUrl, movieLoop: shouldLoop, movieTrimStart: movieTrim.start, movieTrimEnd: movieTrim.end, movieHoldLastFrame: holdLastFrame, movieTransition, movieTransitionDuration, movieExiting: false }
                  } : null);
                }
              }
              break;
            }
            case CommandType.StopMovie: {
              updatePlayerState((p) => {
                if (!p) return null;
                return {
                  ...p,
                  stageState: {
                    ...p.stageState,
                    movieOverlays: []
                  },
                  uiState: {
                    ...p.uiState,
                    movieUrl: null,
                    movieLoop: false
                  }
                };
              });
              break;
            }
            case CommandType.Wait: {
              instantAdvance = false;
              const cmd = command;
              const durationMs = (cmd.duration ?? 1) * 1e3;
              if (cmd.waitForCondition) {
                const conditionMet = () => {
                  var _a4;
                  return evaluateConditions2(cmd.waitConditions, mergeDirtyUiVariables(((_a4 = playerStateRef.current) == null ? void 0 : _a4.variables) ?? {}));
                };
                if (conditionMet()) {
                  advance();
                } else {
                  const poll = () => {
                    if (conditionMet()) {
                      advance();
                      return;
                    }
                    const tid2 = window.setTimeout(poll, 150);
                    activeEffectTimeoutsRef.current.push(tid2);
                  };
                  const tid = window.setTimeout(poll, 150);
                  activeEffectTimeoutsRef.current.push(tid);
                }
              } else if (cmd.waitForItems) {
                const targets2 = Array.isArray(cmd.targetItemIds) ? cmd.targetItemIds.filter(Boolean) : [];
                const mode = cmd.itemsMode === "any" ? "any" : "all";
                const isCollected = (itemId) => {
                  var _a4, _b3, _c3;
                  const item = (_a4 = project.items) == null ? void 0 : _a4[itemId];
                  if (!item) return true;
                  const need = Math.max(1, Number(((_b3 = cmd.targetItemCounts) == null ? void 0 : _b3[itemId]) ?? 1) || 1);
                  const c = Number(mergeDirtyUiVariables(((_c3 = playerStateRef.current) == null ? void 0 : _c3.variables) ?? {})[item.countVariableId] ?? 0);
                  return c >= need;
                };
                const conditionMet = () => targets2.length === 0 ? true : mode === "any" ? targets2.some(isCollected) : targets2.every(isCollected);
                if (conditionMet()) {
                  advance();
                } else {
                  const poll = () => {
                    if (conditionMet()) {
                      advance();
                      return;
                    }
                    const tid2 = window.setTimeout(poll, 150);
                    activeEffectTimeoutsRef.current.push(tid2);
                  };
                  const tid = window.setTimeout(poll, 150);
                  activeEffectTimeoutsRef.current.push(tid);
                }
              } else if (cmd.waitIndefinitelyForInput) {
                let hasAdvanced = false;
                const onUserAdvance = () => {
                  if (hasAdvanced) return;
                  hasAdvanced = true;
                  advance();
                  removeListeners();
                };
                const keyHandler = (e) => {
                  if (e.key === " " || e.key === "Enter" || e.key === "Escape") {
                    e.preventDefault();
                    e.stopPropagation();
                    onUserAdvance();
                  }
                };
                const clickHandler = (e) => {
                  var _a4, _b3;
                  if ((_b3 = (_a4 = e.target) == null ? void 0 : _a4.closest) == null ? void 0 : _b3.call(_a4, "[data-vn-no-advance]")) return;
                  if (stageRef.current && stageRef.current.contains(e.target)) {
                    onUserAdvance();
                  }
                };
                const removeListeners = () => {
                  window.removeEventListener("keydown", keyHandler, true);
                  window.removeEventListener("click", clickHandler, true);
                };
                window.addEventListener("keydown", keyHandler, true);
                window.addEventListener("click", clickHandler, true);
              } else if (cmd.waitForInput) {
                let hasAdvanced = false;
                let timeoutId = window.setTimeout(() => {
                  if (!hasAdvanced) {
                    hasAdvanced = true;
                    advance();
                  }
                  removeListeners();
                }, durationMs);
                const onUserAdvance = () => {
                  if (hasAdvanced) return;
                  hasAdvanced = true;
                  if (timeoutId) {
                    clearTimeout(timeoutId);
                    timeoutId = null;
                  }
                  advance();
                  removeListeners();
                };
                const keyHandler = (e) => {
                  if (e.key === " " || e.key === "Enter" || e.key === "Escape") {
                    e.preventDefault();
                    e.stopPropagation();
                    onUserAdvance();
                  }
                };
                const clickHandler = (e) => {
                  var _a4, _b3;
                  if ((_b3 = (_a4 = e.target) == null ? void 0 : _a4.closest) == null ? void 0 : _b3.call(_a4, "[data-vn-no-advance]")) return;
                  if (stageRef.current && stageRef.current.contains(e.target)) {
                    onUserAdvance();
                  }
                };
                const removeListeners = () => {
                  window.removeEventListener("keydown", keyHandler, true);
                  window.removeEventListener("click", clickHandler, true);
                };
                window.addEventListener("keydown", keyHandler, true);
                window.addEventListener("click", clickHandler, true);
              } else {
                setTimeout(() => advance(), durationMs);
              }
              break;
            }
            case CommandType.ShakeScreen: {
              const cmd = command;
              activeShakeRef.current = { intensity: resolveVarNumber(playerState.variables, cmd.intensityVariableId, cmd.intensity, { min: 1, max: 10 }), duration: cmd.duration };
              setShakeTrigger((prev) => prev + 1);
              if (cmd.duration > 0) {
                const timeoutId = window.setTimeout(() => {
                  activeShakeRef.current = null;
                  setShakeTrigger((prev) => prev + 1);
                  activeEffectTimeoutsRef.current = activeEffectTimeoutsRef.current.filter((id) => id !== timeoutId);
                }, cmd.duration * 1e3);
                activeEffectTimeoutsRef.current.push(timeoutId);
              }
              break;
            }
            case CommandType.TintScreen: {
              const cmd = command;
              updatePlayerState((p) => p ? { ...p, stageState: { ...p.stageState, screen: { ...p.stageState.screen, tint: cmd.color, tintOpacity: cmd.opacity ?? 100, tintOpacityVariableId: cmd.opacityVariableId ?? null, transitionDuration: cmd.duration } } } : null);
              break;
            }
            case CommandType.PanZoomScreen: {
              const cmd = command;
              updatePlayerState((p) => p ? { ...p, stageState: { ...p.stageState, screen: { ...p.stageState.screen, zoom: resolveVarNumber(p.variables, cmd.zoomVariableId, cmd.zoom, { min: 0.1, max: 5 }), panX: resolveVarNumber(p.variables, cmd.panXVariableId, cmd.panX, { min: -100, max: 100 }), panY: resolveVarNumber(p.variables, cmd.panYVariableId, cmd.panY, { min: -100, max: 100 }), transitionDuration: cmd.duration } } } : null);
              break;
            }
            case CommandType.ResetScreenEffects: {
              const cmd = command;
              updatePlayerState((p) => p ? { ...p, stageState: { ...p.stageState, screen: { ...p.stageState.screen, tint: "transparent", tintOpacity: 100, tintOpacityVariableId: null, zoom: 1, panX: 0, panY: 0, transitionDuration: cmd.duration, overlayEffects: [] } } } : null);
              break;
            }
            case CommandType.FlashScreen: {
              const cmd = command;
              activeFlashRef.current = { color: cmd.color, duration: resolveVarNumber(playerState.variables, cmd.durationVariableId, cmd.duration, { min: 0.05, max: 30 }), key: Date.now() };
              setFlashTrigger((prev) => prev + 1);
              break;
            }
            case CommandType.Lightning: {
              const cmd = command;
              activeLightningRef.current = {
                color: cmd.color || "#EAF2FF",
                intensity: resolveVarNumber(playerState.variables, cmd.intensityVariableId, cmd.intensity ?? 0.9, { min: 0, max: 1 }),
                duration: cmd.duration ?? 0.7,
                flashes: cmd.flashes ?? 2,
                affectsDialogue: cmd.affectsDialogue !== false,
                key: Date.now()
              };
              setLightningTrigger((prev) => prev + 1);
              if (cmd.thunderSfxId) {
                const tid = window.setTimeout(() => {
                  playSound(cmd.thunderSfxId, cmd.thunderVolume);
                }, Math.max(0, (cmd.thunderDelay ?? 0.6) * 1e3));
                activeEffectTimeoutsRef.current.push(tid);
              }
              break;
            }
            case CommandType.Fireworks: {
              const cmd = command;
              activeFireworksRef.current = {
                colors: cmd.colors && cmd.colors.length ? cmd.colors : [],
                intensity: resolveVarNumber(playerState.variables, cmd.intensityVariableId, cmd.intensity ?? 1, { min: 0, max: 1 }),
                bursts: Math.round(resolveVarNumber(playerState.variables, cmd.burstsVariableId, Math.max(1, cmd.bursts ?? 3), { min: 1, max: 20 })),
                duration: cmd.duration ?? 2.5,
                burstHeight: resolveVarNumber(playerState.variables, cmd.burstHeightVariableId, cmd.burstHeight ?? 0.7, { min: 0, max: 1 }),
                affectsDialogue: cmd.affectsDialogue !== false,
                sfxId: cmd.sfxId ?? null,
                sfxVolume: cmd.sfxVolume,
                sfxPerBurst: !!cmd.sfxPerBurst,
                key: Date.now()
              };
              setFireworksTrigger((prev) => prev + 1);
              if (cmd.sfxId && !cmd.sfxPerBurst) {
                const tid = window.setTimeout(() => {
                  playSound(cmd.sfxId, cmd.sfxVolume);
                }, Math.max(0, (cmd.sfxDelay ?? 0.3) * 1e3));
                activeEffectTimeoutsRef.current.push(tid);
              }
              break;
            }
            case CommandType.PlaceLights: {
              const cmd = command;
              applyResult({ advance: true, stagePatch: () => ({ lights: cmd.lights || [], lightsAbove: !!cmd.aboveCharacters, lightsBrightnessVariableId: cmd.brightnessVariableId ?? null }) });
              break;
            }
            case CommandType.ClearLights: {
              applyResult({ advance: true, stagePatch: () => ({ lights: [], lightsBrightnessVariableId: null }) });
              break;
            }
            case CommandType.ShowPhone: {
              applyResult(handleShowPhone(command, commandContext));
              break;
            }
            case CommandType.HidePhone: {
              applyResult(handleHidePhone(command, commandContext));
              break;
            }
            case CommandType.ShowPhoneText: {
              applyResult(handleShowPhoneText(command, commandContext));
              break;
            }
            case CommandType.HidePhoneText: {
              applyResult(handleHidePhoneText(command, commandContext));
              break;
            }
            case CommandType.PhoneIncomingText: {
              const cmd = command;
              const pausesForReply = cmd.presentation === "open" && !!(cmd.replies && cmd.replies.length > 0);
              if (pausesForReply) instantAdvance = false;
              startIncomingText(cmd);
              break;
            }
            case CommandType.PhoneIncomingCall: {
              const cmd = command;
              if ((cmd.mode || "modal") === "modal") instantAdvance = false;
              startIncomingCall(cmd);
              break;
            }
            case CommandType.PhoneNotify: {
              startPhoneNotify(command);
              break;
            }
            case CommandType.ShowMap: {
              const cmd = command;
              if (!((_b2 = project.maps) == null ? void 0 : _b2[cmd.mapId])) {
                runtimeDebugWarn(\`[ShowMap] Map \${cmd.mapId} not found — skipping.\`);
                break;
              }
              instantAdvance = false;
              updatePlayerState((p) => p ? { ...p, uiState: { ...p.uiState, isWaitingForInput: true, mapOverlay: { mapId: cmd.mapId, allowCancel: cmd.allowCancel, fromCommand: true } } } : null);
              break;
            }
            case CommandType.ShowMiniGame: {
              const cmd = command;
              const game = (_c2 = project.miniGames) == null ? void 0 : _c2[cmd.gameId];
              if (!game || !((_d2 = game.stages) == null ? void 0 : _d2.length)) {
                runtimeDebugWarn(\`[ShowMiniGame] Mini game \${cmd.gameId} \${game ? "has no stages" : "not found"} — skipping.\`);
                break;
              }
              instantAdvance = false;
              updatePlayerState((p) => p ? { ...p, uiState: { ...p.uiState, isWaitingForInput: true, miniGameOverlay: { gameId: cmd.gameId, fromCommand: true } } } : null);
              break;
            }
            case CommandType.StartPhoneCall: {
              const cmd = command;
              const contact = (project.ui.phoneContacts || []).find((c) => c.characterId === cmd.contactId);
              const contactPick = !((_f2 = (_e2 = cmd.conversation) == null ? void 0 : _e2.lines) == null ? void 0 : _f2.length) && contact ? pickConversation(contact.callConversations, contact.callConversation, ((_g2 = playerStateRef.current) == null ? void 0 : _g2.variables) || {}, ((_i2 = (_h2 = playerStateRef.current) == null ? void 0 : _h2.uiState.phone) == null ? void 0 : _i2.playedConversations) || []) : null;
              const conversation = ((_k2 = (_j2 = cmd.conversation) == null ? void 0 : _j2.lines) == null ? void 0 : _k2.length) ? cmd.conversation : contactPick == null ? void 0 : contactPick.conversation;
              if (!((_l2 = conversation == null ? void 0 : conversation.lines) == null ? void 0 : _l2.length)) {
                runtimeDebugWarn("[StartPhoneCall] No conversation on the command or the contact — skipping.");
                break;
              }
              if ((_m2 = contactPick == null ? void 0 : contactPick.entry) == null ? void 0 : _m2.once) markConversationPlayed(contactPick.entry.id);
              const blocking = cmd.blocking !== false;
              if (blocking) instantAdvance = false;
              startOutgoingTranscriptCall({ contactId: cmd.contactId, portrait: cmd.portrait ?? (contact == null ? void 0 : contact.avatar), conversation, dialingMs: cmd.dialingMs, blocking });
              break;
            }
            case CommandType.Flashlight: {
              const cmd = command;
              if (cmd.enabled) {
                setFlashlight({
                  radius: cmd.radius ?? 22,
                  softness: cmd.softness ?? 0.6,
                  darkness: cmd.darkness ?? 0.85,
                  radiusVariableId: cmd.radiusVariableId ?? null,
                  darknessVariableId: cmd.darknessVariableId ?? null,
                  color: cmd.color || "#000000",
                  toggleKey: cmd.toggleKey,
                  affectsDialogue: cmd.affectsDialogue !== false,
                  darkWhenOff: cmd.darkWhenOff === true,
                  on: true
                });
                if (cmd.sfxId) playSound(cmd.sfxId);
              } else {
                setFlashlight(null);
              }
              break;
            }
            case CommandType.Spotlight: {
              const cmd = command;
              const sid = cmd.spotlightId || "main";
              if (cmd.enabled) {
                setSpotlights((prev) => ({ ...prev, [sid]: makeSpotlightState(cmd) }));
                if (cmd.sfxId) playSound(cmd.sfxId);
              } else {
                setSpotlights((prev) => {
                  const n = { ...prev };
                  delete n[sid];
                  return n;
                });
              }
              break;
            }
            case CommandType.SetScreenOverlayEffect: {
              const cmd = command;
              updatePlayerState((p) => p ? {
                ...p,
                stageState: {
                  ...p.stageState,
                  screen: {
                    ...p.stageState.screen,
                    overlayEffects: upsertOverlayEffect(p.stageState.screen.overlayEffects, {
                      type: cmd.effectType,
                      intensity: cmd.intensity,
                      intensityVariableId: cmd.intensityVariableId ?? null,
                      variant: cmd.variant,
                      color: cmd.color,
                      params: cmd.params
                    })
                  }
                }
              } : null);
              const effectDuration = cmd.duration ?? 0;
              if (effectDuration > 0) {
                const effectType = cmd.effectType;
                const overlayTimeoutId = window.setTimeout(() => {
                  updatePlayerState((p) => p ? {
                    ...p,
                    stageState: {
                      ...p.stageState,
                      screen: {
                        ...p.stageState.screen,
                        overlayEffects: upsertOverlayEffect(p.stageState.screen.overlayEffects, {
                          type: effectType,
                          intensity: 0
                        })
                      }
                    }
                  } : null);
                  activeEffectTimeoutsRef.current = activeEffectTimeoutsRef.current.filter((id) => id !== overlayTimeoutId);
                }, effectDuration * 1e3);
                activeEffectTimeoutsRef.current.push(overlayTimeoutId);
              }
              break;
            }
            case CommandType.ShowScreen: {
              const cmd = command;
              const screenToShow = project.uiScreens[cmd.screenId];
              if (!(screenToShow == null ? void 0 : screenToShow.hudNonBlocking)) {
                instantAdvance = false;
              }
              updatePlayerState((p) => p ? {
                ...p,
                uiState: {
                  ...p.uiState,
                  screenSceneId: p.currentSceneId
                }
              } : null);
              if (playerState && playerState.mode === "playing") {
                setHudStack((s) => [...s, cmd.screenId]);
              } else {
                setScreenStack((s) => [...s, cmd.screenId]);
              }
              break;
            }
            case CommandType.HideScreen: {
              const cmd = command;
              if (cmd.all) {
                setHudStack([]);
                setScreenStack([]);
              } else if (cmd.screenId) {
                setHudStack((s) => s.filter((id) => id !== cmd.screenId));
                setScreenStack((s) => s.filter((id) => id !== cmd.screenId));
              } else if (hudStack.length > 0) {
                setHudStack((s) => s.slice(0, -1));
              } else if (screenStack.length > 0) {
                setScreenStack((s) => s.slice(0, -1));
              }
              break;
            }
            case CommandType.ShowText: {
              const result = handleShowText(command, commandContext);
              applyResult(result);
              break;
            }
            case CommandType.ShowImage: {
              const result = handleShowImage(command, commandContext);
              applyResult(result);
              break;
            }
            case CommandType.Label: {
              const result = handleLabel(command, commandContext);
              applyResult(result);
              break;
            }
            case CommandType.JumpToLabel: {
              const result = handleJumpToLabel(command, commandContext);
              applyResult(result);
              break;
            }
            case CommandType.HideText: {
              const result = handleHideText(command, commandContext);
              applyResult(result);
              break;
            }
            case CommandType.HideImage: {
              const result = handleHideImage(command, commandContext);
              applyResult(result);
              break;
            }
            case CommandType.ShowButton: {
              const result = handleShowButton(command, commandContext);
              applyResult(result);
              break;
            }
            case CommandType.HideButton: {
              const result = handleHideButton(command, commandContext);
              applyResult(result);
              break;
            }
            case CommandType.ShowItem: {
              const result = handleShowItem(command, commandContext);
              applyResult(result);
              break;
            }
            case CommandType.ShowHotSpot: {
              const cmd = command;
              updatePlayerState((p) => p ? {
                ...p,
                stageState: {
                  ...p.stageState,
                  hotSpotOverlays: [
                    ...(p.stageState.hotSpotOverlays || []).filter((h) => h.commandId !== cmd.id),
                    {
                      id: cmd.id,
                      commandId: cmd.id,
                      name: cmd.name,
                      x: cmd.x,
                      y: cmd.y,
                      width: cmd.width,
                      height: cmd.height,
                      shape: cmd.shape,
                      trigger: cmd.trigger,
                      actions: cmd.actions,
                      conditions: cmd.conditions,
                      acceptedTag: cmd.acceptedTag,
                      highlightColor: cmd.highlightColor,
                      visible: cmd.visible,
                      advanceOnTrigger: cmd.advanceOnTrigger,
                      layer: cmd.layer
                    }
                  ]
                }
              } : null);
              break;
            }
            case CommandType.HideHotSpot: {
              const cmd = command;
              updatePlayerState((p) => p ? {
                ...p,
                stageState: {
                  ...p.stageState,
                  hotSpotOverlays: (p.stageState.hotSpotOverlays || []).filter((h) => h.commandId !== cmd.targetCommandId)
                }
              } : null);
              break;
            }
            case CommandType.CreditRoll: {
              const cmd = command;
              setActiveCreditRoll(cmd);
              const result = handleCreditRoll(cmd, commandContext);
              applyResult(result);
              break;
            }
            case CommandType.RunScript: {
              const runScriptCmd = command;
              const waitForScript = runScriptCmd.waitForCompletion !== false;
              if (waitForScript) {
                const result = await handleRunScript(runScriptCmd, commandContext);
                applyResult(result);
              } else {
                instantAdvance = true;
                handleRunScript(runScriptCmd, commandContext).then((result) => {
                  if (result.updates) applyResult({ advance: true, updates: result.updates });
                }).catch((err) => console.error("[RunScript] background script error:", err));
              }
              break;
            }
            case CommandType.SpawnParticles: {
              const result = handleSpawnParticles(command, commandContext);
              applyResult(result);
              break;
            }
            case CommandType.StopParticles: {
              const result = handleStopParticles(command, commandContext);
              applyResult(result);
              break;
            }
            case CommandType.CallCommonEvent: {
              const result = handleCallCommonEvent(command, commandContext);
              applyResult(result);
              break;
            }
            case CommandType.TweenElement: {
              const result = handleTweenElement(command, commandContext);
              applyResult(result);
              break;
            }
            case CommandType.MoveCharacter: {
              const result = handleMoveCharacter(command, commandContext);
              applyResult(result);
              break;
            }
            case CommandType.StartTimer: {
              const cmd = command;
              const key = startTimer(cmd.blockEngine ? { ...cmd, loop: false } : cmd);
              if (cmd.blockEngine) {
                instantAdvance = false;
                blockingTimerRef.current = { key, resume: advance };
              }
              break;
            }
            case CommandType.StopTimer: {
              stopTimer(command.timerId);
              break;
            }
            case CommandType.SetTimeOfDay: {
              const cmd = command;
              applyTimeOfDay(cmd.mode, cmd.mode === "set" ? cmd.hour : cmd.hours, cmd.transitionDuration);
              break;
            }
            default: {
              const customDef = pluginManager.getCommand(command.type);
              if (customDef) {
                const ownerId = pluginManager.getCommandOwner(command.type);
                const api2 = ownerId ? pluginManager.getApi(ownerId) : void 0;
                if (api2) {
                  try {
                    const params = command.params || command.parameters || {};
                    const ret = customDef.handler(params, api2);
                    if (ret && typeof ret.then === "function") {
                      ret.catch((e) => console.error(\`[Custom command \${command.type}] handler error:\`, e));
                    } else if (ret && ret.advance === false) {
                      instantAdvance = false;
                    }
                  } catch (e) {
                    console.error(\`[Custom command \${command.type}] handler error:\`, e);
                  }
                }
              }
              break;
            }
          }
          try {
            pluginManager.invokeHook("onAfterCommand", command, null);
          } catch {
          }
          runtimeDebugLog("[DEBUG] Command execution complete:", command.type, "| shouldRunAsync:", shouldRunAsync, "| instantAdvance:", instantAdvance);
          if (shouldRunAsync) {
            runtimeDebugLog("[DEBUG] Running async - advancing immediately");
            advance();
          } else if (instantAdvance) {
            runtimeDebugLog("[DEBUG] Instant advance - advancing now");
            advance();
          } else {
            runtimeDebugLog("[DEBUG] Waiting for command to handle advancement (callback/user input)");
          }
        } catch (error) {
          console.error("[CRITICAL ERROR] Command execution failed:", {
            commandType: command.type,
            commandId: command.id,
            index: playerState.currentIndex,
            error: error instanceof Error ? error.message : String(error),
            stack: error instanceof Error ? error.stack : void 0
          });
          advance();
        }
      })();
    }, [playerState, project, assetResolver, playSound, playVoice, evaluateConditions2, fadeAudio, settings.musicVolume, startNewGame, stopAndResetMusic, stopAllSfx, stopSfx, hudStack]);
    const handleDialogueAdvance = () => {
      var _a2, _b2, _c2;
      if (scenePaused) return;
      if (((_b2 = (_a2 = playerState == null ? void 0 : playerState.uiState.phone) == null ? void 0 : _a2.incomingCall) == null ? void 0 : _b2.phase) === "ringing" && playerState.uiState.phone.incomingCall.modal) return;
      if (((_c2 = scriptInputResolverRef.current) == null ? void 0 : _c2.kind) === "dialogue") {
        const r = scriptInputResolverRef.current;
        scriptInputResolverRef.current = null;
        stopVoice();
        updatePlayerState((p) => p ? { ...p, uiState: { ...p.uiState, dialogue: null, isWaitingForInput: false, isSkipping: false } } : p);
        r.resolve(void 0);
        return;
      }
      stopVoice();
      updatePlayerState((p) => {
        if (!p || !p.uiState.dialogue) return p;
        const scene = project.scenes[p.currentSceneId];
        const currentCmd = scene == null ? void 0 : scene.commands[p.currentIndex];
        const nextCmd = scene == null ? void 0 : scene.commands[p.currentIndex + 1];
        const shouldKeepDialogueDuringChoices = (currentCmd == null ? void 0 : currentCmd.keepOpenDuringChoices) && (nextCmd == null ? void 0 : nextCmd.type) === CommandType.Choice;
        if (shouldKeepDialogueDuringChoices) {
          return {
            ...p,
            currentIndex: p.currentIndex + 1,
            uiState: { ...p.uiState, isWaitingForInput: false, isSkipping: false }
            // Keep dialogue open!
          };
        }
        const historyEntry = {
          timestamp: Date.now(),
          type: "dialogue",
          characterName: p.uiState.dialogue.characterName,
          characterColor: p.uiState.dialogue.characterColor,
          text: p.uiState.dialogue.text,
          sceneId: p.currentSceneId,
          commandIndex: p.currentIndex,
          // Full state snapshots for backward navigation
          stageSnapshot: JSON.parse(JSON.stringify(p.stageState)),
          variablesSnapshot: { ...p.variables },
          musicSnapshot: { ...p.musicState }
        };
        const newHistory = [...p.history, historyEntry];
        if (newHistory.length > 200) newHistory.splice(0, newHistory.length - 200);
        return {
          ...p,
          currentIndex: p.currentIndex + 1,
          history: newHistory,
          uiState: { ...p.uiState, isWaitingForInput: false, dialogue: null, isSkipping: false }
        };
      });
    };
    const handleChoiceSelect = (choice) => {
      var _a2, _b2, _c2;
      if (((_a2 = scriptInputResolverRef.current) == null ? void 0 : _a2.kind) === "choice") {
        const r = scriptInputResolverRef.current;
        scriptInputResolverRef.current = null;
        const idx = ((playerState == null ? void 0 : playerState.uiState.choices) || []).findIndex((c) => c.id === choice.id);
        updatePlayerState((p) => p ? { ...p, uiState: { ...p.uiState, choices: null, isWaitingForInput: false } } : p);
        r.resolve(idx >= 0 ? idx : 0);
        return;
      }
      runtimeDebugLog("[CHOICE] Selected:", choice.text, "Actions:", ((_b2 = choice.actions) == null ? void 0 : _b2.length) || 0);
      const allActions = [...choice.actions || []];
      if (!choice.actions && choice.targetSceneId) {
        allActions.push({ type: UIActionType.JumpToScene, targetSceneId: choice.targetSceneId });
      }
      const INLINE_CHOICE_ACTIONS = /* @__PURE__ */ new Set([UIActionType.SetVariable, UIActionType.JumpToScene, UIActionType.JumpToLabel, UIActionType.OpenURL]);
      const willJumpScene = allActions.some((a) => a.type === UIActionType.JumpToScene);
      const runSelection = () => {
        updatePlayerState((p) => {
          if (!p) return null;
          let newState = { ...p };
          const historyEntry = {
            timestamp: Date.now(),
            type: "choice",
            text: \`Choice: \${choice.text}\`,
            choiceText: choice.text,
            choiceOption: choice,
            sceneId: p.currentSceneId,
            commandIndex: p.currentIndex,
            // Full state snapshots for backward navigation
            stageSnapshot: JSON.parse(JSON.stringify(p.stageState)),
            variablesSnapshot: { ...p.variables },
            musicSnapshot: { ...p.musicState }
          };
          newState.history = [...newState.history, historyEntry];
          if (newState.history.length > 200) newState.history.splice(0, newState.history.length - 200);
          const inputKey = \`\${p.currentSceneId}:\${p.currentIndex}\`;
          newState.savedInputs = { ...newState.savedInputs, [inputKey]: { type: "choice", choice } };
          newState.uiState = { ...newState.uiState, dialogue: null, choices: null };
          const actions = allActions;
          for (const action of actions) {
            runtimeDebugLog("[CHOICE] Processing action:", action.type, action);
            if (action.type === UIActionType.SetVariable) {
              const setVarAction = action;
              const variable = project.variables[setVarAction.variableId];
              if (!variable) {
                runtimeDebugWarn(\`SetVariable action failed: Variable with ID \${setVarAction.variableId} not found.\`);
                continue;
              }
              const originalOperator = setVarAction.operator;
              const effectiveOperator = normalizeSetVariableOperator(variable.type, variable.name, originalOperator);
              const wasCoercedOperator = originalOperator !== effectiveOperator;
              const currentVal = newState.variables[setVarAction.variableId];
              const newVal = calculateVariableValue(
                effectiveOperator,
                variable.type,
                currentVal,
                setVarAction.value,
                setVarAction.randomMin,
                setVarAction.randomMax,
                wasCoercedOperator ? originalOperator : void 0,
                variable.min,
                variable.max
              );
              newState.variables = { ...newState.variables, [setVarAction.variableId]: newVal };
              runtimeDebugLog(
                "[CHOICE] Set variable result:",
                setVarAction.variableId,
                "=>",
                newVal,
                "(type:",
                typeof newVal,
                "| operator:",
                \`\${setVarAction.operator} => \${effectiveOperator}\`,
                ")"
              );
              if (variable.scope === "persistent") {
                const prevPersistent = loadPersistentVariables(project.id);
                savePersistentVariables(project.id, { ...prevPersistent, [setVarAction.variableId]: newVal });
                runtimeDebugLog("[Variable Scope] Saved persistent variable from choice:", variable.name);
              }
            }
          }
          runtimeDebugLog("[CHOICE] Variables after actions:", JSON.stringify(newState.variables, null, 2));
          newState.uiState = { ...newState.uiState, choices: null };
          const jumpAction = actions.find((a) => a.type === UIActionType.JumpToScene);
          const labelAction = actions.find((a) => a.type === UIActionType.JumpToLabel);
          const openUrlActions = actions.filter((a) => a.type === UIActionType.OpenURL);
          openUrlActions.forEach((urlAction) => {
            if (urlAction.url) {
              if (urlAction.newTab !== false) {
                window.open(urlAction.url, "_blank", "noopener,noreferrer");
              } else {
                window.location.href = urlAction.url;
              }
            }
          });
          if (labelAction) {
            const targetLabel = labelAction.targetLabel;
            const targetSceneId = newState.currentSceneId;
            const targetScene = project.scenes[targetSceneId];
            if (targetScene) {
              const labelIndex = targetScene.commands.findIndex(
                (cmd) => cmd.type === CommandType.Label && cmd.labelId === targetLabel
              );
              if (labelIndex !== -1) {
                runtimeDebugLog(\`[CHOICE] JumpToLabel: Jumping to label "\${targetLabel}" at index \${labelIndex}\`);
                newState.currentSceneId = targetSceneId;
                newState.currentCommands = targetScene.commands;
                newState.currentIndex = labelIndex;
                newState.stageState = {
                  ...newState.stageState,
                  buttonOverlays: [],
                  imageOverlays: [],
                  textOverlays: []
                };
              } else {
                runtimeDebugWarn(\`[CHOICE] JumpToLabel failed: Label "\${targetLabel}" not found in scene "\${targetScene.name}"\`);
                newState.currentIndex = newState.currentIndex + 1;
              }
            } else {
              console.error(\`[CHOICE] Scene not found for JumpToLabel: \${targetSceneId}\`);
              newState.currentIndex = newState.currentIndex + 1;
            }
          } else if (jumpAction) {
            const actualSceneId = navigateToScene(jumpAction.targetSceneId, newState.variables);
            const newScene = project.scenes[actualSceneId];
            if (newScene) {
              const localDefaults = getLocalVariableDefaults(project.variables);
              newState.variables = { ...newState.variables, ...localDefaults };
              runtimeDebugLog("[Variable Scope] Reset local variables on choice/button scene jump:", Object.keys(localDefaults));
              newState.currentSceneId = actualSceneId;
              newState.currentCommands = newScene.commands;
              newState.currentIndex = 0;
              newState.stageState = {
                backgroundUrl: null,
                characters: {},
                textOverlays: [],
                imageOverlays: [],
                buttonOverlays: [],
                movieOverlays: [],
                screen: { shake: { active: false, intensity: 0 }, tint: "transparent", zoom: 1, panX: 0, panY: 0, transitionDuration: 0.5, overlayEffects: [] },
                particleEffects: {}
              };
            } else {
              console.error(\`Scene not found for choice jump: \${actualSceneId}\`);
              newState.currentIndex = newState.currentIndex + 1;
            }
          } else {
            newState.currentIndex = newState.currentIndex + 1;
          }
          return newState;
        });
        for (const action of allActions) {
          if (!INLINE_CHOICE_ACTIONS.has(action.type)) {
            handleUIAction(action, action.type === UIActionType.CallCommonEvent ? { resumeAtCurrent: true } : void 0);
          }
        }
      };
      const sceneToExit = (_c2 = playerStateRef.current) == null ? void 0 : _c2.currentSceneId;
      if (willJumpScene && sceneToExit) {
        startSceneExitTransition(sceneToExit, runSelection);
      } else {
        runSelection();
      }
    };
    const handleTextInputSubmit = (value) => {
      var _a2;
      if (((_a2 = scriptInputResolverRef.current) == null ? void 0 : _a2.kind) === "textInput") {
        const r = scriptInputResolverRef.current;
        scriptInputResolverRef.current = null;
        updatePlayerState((p) => p ? { ...p, uiState: { ...p.uiState, textInput: null, isWaitingForInput: false } } : p);
        r.resolve(value);
        return;
      }
      updatePlayerState((p) => {
        if (!p || !p.uiState.textInput) return p;
        const historyEntry = {
          timestamp: Date.now(),
          type: "textInput",
          text: \`Input: \${value}\`,
          inputValue: value,
          variableId: p.uiState.textInput.variableId,
          sceneId: p.currentSceneId,
          commandIndex: p.currentIndex,
          // Full state snapshots for backward navigation
          stageSnapshot: JSON.parse(JSON.stringify(p.stageState)),
          variablesSnapshot: { ...p.variables },
          musicSnapshot: { ...p.musicState }
        };
        const inputKey = \`\${p.currentSceneId}:\${p.currentIndex}\`;
        return {
          ...p,
          currentIndex: p.currentIndex + 1,
          variables: { ...p.variables, [p.uiState.textInput.variableId]: value },
          history: (() => {
            const h = [...p.history, historyEntry];
            if (h.length > 200) h.splice(0, h.length - 200);
            return h;
          })(),
          savedInputs: { ...p.savedInputs, [inputKey]: { type: "textInput", value } },
          uiState: { ...p.uiState, isWaitingForInput: false, textInput: null }
        };
      });
    };
    const handleSkipBackward = React2.useCallback(() => {
      const cur = playerStateRef.current;
      if (cur && cur.history.some((h) => h.type === "dialogue")) {
        const hw = backwardReplayRef.current;
        if (!(hw && hw.sceneId === cur.currentSceneId && hw.index > cur.currentIndex)) {
          backwardReplayRef.current = { sceneId: cur.currentSceneId, index: cur.currentIndex };
        }
      }
      updatePlayerState((p) => {
        if (!p || p.history.length === 0) return p;
        let targetIdx = p.history.length - 1;
        while (targetIdx >= 0 && p.history[targetIdx].type !== "dialogue") {
          targetIdx--;
        }
        if (targetIdx < 0) return p;
        const target = p.history[targetIdx];
        let newSceneId = target.sceneId || p.currentSceneId;
        let newCommands = p.currentCommands;
        let newCommandIndex = target.commandIndex ?? p.currentIndex;
        const targetScene = project.scenes[newSceneId];
        if (targetScene) {
          newCommands = targetScene.commands;
        } else if (target.sceneId && target.sceneId !== p.currentSceneId) {
          return p;
        }
        const trimmedHistory = p.history.slice(0, targetIdx);
        const restoredStage = target.stageSnapshot ? JSON.parse(JSON.stringify(target.stageSnapshot)) : p.stageState;
        const restoredVariables = target.variablesSnapshot ? { ...target.variablesSnapshot } : p.variables;
        const restoredMusic = target.musicSnapshot ? { ...target.musicSnapshot } : p.musicState;
        return {
          ...p,
          currentSceneId: newSceneId,
          currentCommands: newCommands,
          currentIndex: newCommandIndex,
          history: trimmedHistory,
          // Restore full visual/audio state from snapshot
          stageState: restoredStage,
          variables: restoredVariables,
          musicState: restoredMusic,
          uiState: {
            ...p.uiState,
            dialogue: {
              characterName: target.characterName || "Narrator",
              characterColor: target.characterColor || "#FFFFFF",
              characterId: null,
              text: target.text
            },
            choices: null,
            textInput: null,
            isWaitingForInput: true,
            isSkipping: false,
            showHistory: false,
            movieUrl: null,
            movieLoop: false
          }
        };
      });
      commandSchedulerRef.current.reset();
      variableStoreRef.current = null;
    }, [project.scenes]);
    const handleUIAction = (action, opts) => {
      runtimeDebugLog("handleUIAction called with:", action.type, action);
      if (action.conditions && action.conditions.length > 0) {
        const vars = playerState ? mergeDirtyUiVariables(playerState.variables) : menuVariables;
        if (!evaluateConditions2(action.conditions, vars)) {
          runtimeDebugLog("[UIAction] Skipped — conditions not met:", action.type, action.conditions);
          return;
        }
      }
      if (action.type === UIActionType.QuitToTitle && playerState) {
        setConfirmDialog({ type: "quit", pendingAction: action });
        return;
      }
      if (action.type === UIActionType.StartNewGame && (playerState || gameSaves[0])) {
        setConfirmDialog({ type: "newGame", pendingAction: action });
        return;
      }
      if (action.type === UIActionType.DeleteSave) {
        setConfirmDialog({ type: "eraseSave", pendingAction: action });
        return;
      }
      executeUIAction(action, opts);
    };
    const executeUIAction = (action, opts) => {
      var _a2, _b2, _c2, _d2, _e2, _f2, _g2, _h2, _i2, _j2, _k2, _l2, _m2, _n2, _o2, _p2, _q2, _r2, _s2, _t, _u, _v, _w;
      if (action.type === UIActionType.StartNewGame) {
        startNewGameWithFade();
      } else if (!playerState && action.type === UIActionType.ContinueGame) {
        const doLoad = async () => {
          const saves = savesPersistentRef.current ? await getGameSaves() : inMemorySavesRef.current;
          if (saves[0]) {
            loadGame(0);
          } else {
            runtimeDebugLog("[ContinueGame] No auto-save found, starting new game instead");
            startNewGameWithFade();
          }
        };
        void doLoad();
      } else if (playerState && action.type === UIActionType.ReturnToGame) {
        const wasPaused = playerState.mode === "paused";
        const closingId = screenStack.length > 0 ? screenStack[screenStack.length - 1] : hudStack.length > 0 ? hudStack[hudStack.length - 1] : null;
        const closingScreen = closingId ? project.uiScreens[closingId] : null;
        const transOut = (closingScreen == null ? void 0 : closingScreen.transitionOut) || "fade";
        const transDur = (closingScreen == null ? void 0 : closingScreen.transitionOutDuration) ?? (closingScreen == null ? void 0 : closingScreen.transitionDuration) ?? 300;
        const closeBehavior = (closingScreen == null ? void 0 : closingScreen.onCloseBehavior) || "default";
        const advanceOnReturn = closeBehavior === "advance" || closeBehavior === "default" && !wasPaused && !(closingScreen == null ? void 0 : closingScreen.hudNonBlocking);
        const finishReturn = () => {
          reactDom.flushSync(() => {
            updatePlayerState((p) => p ? { ...p, mode: "playing", variables: mergeDirtyUiVariables(p.variables) } : null);
          });
          uiDirtyVariableIdsRef.current.clear();
          setScreenStack([]);
          setHudStack([]);
          if (wasPaused && musicAudioRef.current && musicAudioRef.current.paused && playerState.musicState.isPlaying) {
            musicAudioRef.current.play().catch((e) => console.error("Failed to resume music:", e));
          }
          if (closeBehavior === "runActions") {
            ((closingScreen == null ? void 0 : closingScreen.onCloseActions) || []).forEach((a) => executeUIAction(a));
          } else if (advanceOnReturn) {
            updatePlayerState((p) => p ? { ...p, currentIndex: p.currentIndex + 1, uiState: { ...p.uiState, isWaitingForInput: false, dialogue: null, isSkipping: false } } : null);
          }
        };
        if (closingId && transOut !== "none") {
          setClosingScreens((prev) => new Set(prev).add(closingId));
          setTimeout(() => {
            setClosingScreens((prev) => {
              const next = new Set(prev);
              next.delete(closingId);
              return next;
            });
            finishReturn();
          }, transDur + 50);
        } else {
          finishReturn();
        }
      } else if (action.type === UIActionType.OpenPauseMenu) {
        if (playerState && playerState.mode === "playing") {
          updatePlayerState((p) => p ? { ...p, mode: "paused" } : null);
          if (musicAudioRef.current && !musicAudioRef.current.paused) {
            musicAudioRef.current.pause();
          }
          if (project.ui.pauseScreenId) {
            setScreenStack([project.ui.pauseScreenId]);
          }
        }
      } else if (action.type === UIActionType.ShowElement || action.type === UIActionType.HideElement) {
        const targetId = action.targetElementId;
        if (targetId) {
          const visible = action.type === UIActionType.ShowElement;
          setElementVisibility((prev) => prev[targetId] === visible ? prev : { ...prev, [targetId]: visible });
        }
      } else if (action.type === UIActionType.ToggleScreen) {
        const targetId = action.targetScreenId;
        if (!targetId || !project.uiScreens[targetId]) {
          runtimeDebugWarn(\`ToggleScreen failed: Screen with ID \${targetId} not found\`);
          return;
        }
        if (project.ui.pauseScreenId && targetId === project.ui.pauseScreenId && (playerState == null ? void 0 : playerState.mode) === "playing") {
          updatePlayerState((p) => p ? { ...p, mode: "paused" } : null);
          if (musicAudioRef.current && !musicAudioRef.current.paused) musicAudioRef.current.pause();
          setScreenStack([targetId]);
          return;
        }
        if (playerState && playerState.mode === "playing") {
          const isClosing = hudStack.includes(targetId);
          setHudStack((s) => s.includes(targetId) ? s.filter((id) => id !== targetId) : [...s, targetId]);
          if (isClosing) {
            reactDom.flushSync(() => {
              updatePlayerState((p) => p ? { ...p, variables: mergeDirtyUiVariables(p.variables) } : null);
            });
            uiDirtyVariableIdsRef.current.clear();
            const cs = project.uiScreens[targetId];
            const b = (cs == null ? void 0 : cs.onCloseBehavior) || "default";
            if (b === "advance") updatePlayerState((p) => p ? { ...p, currentIndex: p.currentIndex + 1, uiState: { ...p.uiState, isWaitingForInput: false, dialogue: null, isSkipping: false } } : null);
            else if (b === "runActions") ((cs == null ? void 0 : cs.onCloseActions) || []).forEach((a) => executeUIAction(a));
          }
        } else {
          setScreenStack((s) => s.includes(targetId) ? s.filter((id) => id !== targetId) : [...s, targetId]);
        }
      } else if (action.type === UIActionType.GoToScreen) {
        const targetId = action.targetScreenId;
        const targetScreen = project.uiScreens[targetId];
        if (!targetScreen) {
          runtimeDebugWarn(\`GoToScreen failed: Screen with ID \${targetId} not found\`);
          return;
        }
        if (project.ui.pauseScreenId && targetId === project.ui.pauseScreenId && (playerState == null ? void 0 : playerState.mode) === "playing") {
          updatePlayerState((p) => p ? { ...p, mode: "paused" } : null);
          if (musicAudioRef.current && !musicAudioRef.current.paused) musicAudioRef.current.pause();
          setScreenStack([targetId]);
          return;
        }
        if (playerState && playerState.mode === "playing") {
          const screenMusicInfo = targetScreen.music;
          const hasMusicChange = screenMusicInfo && screenMusicInfo.audioId;
          if (hasMusicChange) {
            const audio = musicAudioRef.current;
            const newMusicUrl = screenMusicInfo.audioId ? assetResolver(screenMusicInfo.audioId, "audio") : null;
            const currentMusic = audio.src;
            const normalize = (value) => {
              if (!value) return null;
              try {
                return new URL(value, window.location.href).href;
              } catch (e) {
                return value;
              }
            };
            const currentNormalized = currentMusic ? normalize(currentMusic) : null;
            const newNormalized = normalize(newMusicUrl);
            if (currentNormalized !== newNormalized && newMusicUrl) {
              fadeAudio(audio, 0, 0.5, () => {
                audio.src = newMusicUrl;
                audio.load();
                audio.loop = true;
                audio.play().then(() => {
                  fadeAudio(audio, settings.musicVolume, 0.5);
                }).catch((e) => {
                  console.error("Screen music play failed:", e);
                });
              });
            }
          }
          setHudStack((s) => {
            const departingId = s.length > 0 ? s[s.length - 1] : null;
            if (departingId) {
              const departingScreen = project.uiScreens[departingId];
              const depTransOut = (departingScreen == null ? void 0 : departingScreen.transitionOut) || "fade";
              if (depTransOut !== "none") {
                const duration = (departingScreen == null ? void 0 : departingScreen.transitionOutDuration) ?? (departingScreen == null ? void 0 : departingScreen.transitionDuration) ?? 300;
                setClosingScreens((prev) => new Set(prev).add(departingId));
                setTimeout(() => {
                  setClosingScreens((prev) => {
                    const next = new Set(prev);
                    next.delete(departingId);
                    return next;
                  });
                }, duration + 100);
              }
            }
            return [...s, targetId];
          });
        } else {
          setScreenStack((stack) => {
            const departingId = stack.length > 0 ? stack[stack.length - 1] : null;
            if (departingId) {
              const departingScreen = project.uiScreens[departingId];
              const depTransOut = (departingScreen == null ? void 0 : departingScreen.transitionOut) || "fade";
              if (depTransOut !== "none") {
                const duration = (departingScreen == null ? void 0 : departingScreen.transitionOutDuration) ?? (departingScreen == null ? void 0 : departingScreen.transitionDuration) ?? 300;
                setClosingScreens((prev) => new Set(prev).add(departingId));
                setTimeout(() => {
                  setClosingScreens((prev) => {
                    const next = new Set(prev);
                    next.delete(departingId);
                    return next;
                  });
                }, duration + 100);
              }
            }
            return [...stack, targetId];
          });
        }
      } else if (action.type === UIActionType.ReturnToPreviousScreen) {
        if (playerState && playerState.mode === "playing") {
          if (hudStack.length > 0) {
            const closingScreenId = hudStack[hudStack.length - 1];
            const closingScreen = project.uiScreens[closingScreenId];
            const transitionDuration = (closingScreen == null ? void 0 : closingScreen.transitionOutDuration) ?? (closingScreen == null ? void 0 : closingScreen.transitionDuration) ?? 300;
            const effectiveTransitionOut = (closingScreen == null ? void 0 : closingScreen.transitionOut) || "fade";
            const hasTransition = effectiveTransitionOut !== "none";
            const closeBehavior = (closingScreen == null ? void 0 : closingScreen.onCloseBehavior) || "default";
            const advanceOnClose = closeBehavior === "advance" || closeBehavior === "default" && !(closingScreen == null ? void 0 : closingScreen.hudNonBlocking);
            const runCloseActions = () => {
              if (closeBehavior === "runActions") ((closingScreen == null ? void 0 : closingScreen.onCloseActions) || []).forEach((a) => executeUIAction(a));
            };
            if (hasTransition) {
              setClosingScreens((prev) => new Set(prev).add(closingScreenId));
              setTimeout(() => {
                setHudStack((s) => s.slice(0, -1));
                runCloseActions();
                setClosingScreens((prev) => {
                  const next = new Set(prev);
                  next.delete(closingScreenId);
                  return next;
                });
                if (hudStack.length === 1) {
                  reactDom.flushSync(() => {
                    updatePlayerState((p) => {
                      if (!p) return null;
                      runtimeDebugLog("ReturnToPreviousScreen (transition): BEFORE merge - playerState.variables:", JSON.stringify(p.variables, null, 2));
                      runtimeDebugLog("ReturnToPreviousScreen (transition): uiVariables to merge:", JSON.stringify(uiVariablesRef.current, null, 2));
                      runtimeDebugLog("ReturnToPreviousScreen (transition): dirty variable IDs:", Array.from(uiDirtyVariableIdsRef.current));
                      const mergedVariables = mergeDirtyUiVariables(p.variables);
                      runtimeDebugLog("ReturnToPreviousScreen (transition): AFTER merge - merged variables:", JSON.stringify(mergedVariables, null, 2));
                      return {
                        ...p,
                        variables: mergedVariables,
                        // Merge UI variables into game variables
                        currentIndex: advanceOnClose ? p.currentIndex + 1 : p.currentIndex,
                        // Explicit "advance" clears the waiting-dialogue gate so the next command actually runs
                        // (a bare index bump does nothing while the loop is paused on isWaitingForInput/dialogue).
                        ...closeBehavior === "advance" ? { uiState: { ...p.uiState, isWaitingForInput: false, dialogue: null, isSkipping: false } } : {},
                        stageState: {
                          ...p.stageState,
                          buttonOverlays: [],
                          imageOverlays: []
                        }
                      };
                    });
                  });
                  runtimeDebugLog("[CLEAR] Dirty set cleared after ReturnToPreviousScreen (with transition)");
                  uiDirtyVariableIdsRef.current.clear();
                }
              }, transitionDuration);
            } else {
              setHudStack((s) => s.slice(0, -1));
              runCloseActions();
              if (hudStack.length === 1) {
                setTimeout(() => {
                  reactDom.flushSync(() => {
                    updatePlayerState((p) => {
                      if (!p) return null;
                      runtimeDebugLog("ReturnToPreviousScreen (no transition): BEFORE merge - playerState.variables:", JSON.stringify(p.variables, null, 2));
                      runtimeDebugLog("ReturnToPreviousScreen (no transition): uiVariables to merge:", JSON.stringify(uiVariablesRef.current, null, 2));
                      runtimeDebugLog("ReturnToPreviousScreen (no transition): dirty variable IDs:", Array.from(uiDirtyVariableIdsRef.current));
                      const mergedVariables = mergeDirtyUiVariables(p.variables);
                      runtimeDebugLog("ReturnToPreviousScreen (no transition): AFTER merge - merged variables:", JSON.stringify(mergedVariables, null, 2));
                      return {
                        ...p,
                        variables: mergedVariables,
                        // Merge UI variables into game variables
                        currentIndex: advanceOnClose ? p.currentIndex + 1 : p.currentIndex,
                        // Explicit "advance" clears the waiting-dialogue gate so the next command actually runs
                        // (a bare index bump does nothing while the loop is paused on isWaitingForInput/dialogue).
                        ...closeBehavior === "advance" ? { uiState: { ...p.uiState, isWaitingForInput: false, dialogue: null, isSkipping: false } } : {},
                        stageState: {
                          ...p.stageState,
                          buttonOverlays: [],
                          imageOverlays: []
                        }
                      };
                    });
                  });
                  runtimeDebugLog("[CLEAR] Dirty set cleared after ReturnToPreviousScreen (no transition)");
                  uiDirtyVariableIdsRef.current.clear();
                }, 0);
              }
            }
          }
        } else {
          if (screenStack.length > 1) {
            const closingScreenId = screenStack[screenStack.length - 1];
            const closingScreen = project.uiScreens[closingScreenId];
            const transitionDuration = (closingScreen == null ? void 0 : closingScreen.transitionOutDuration) ?? (closingScreen == null ? void 0 : closingScreen.transitionDuration) ?? 300;
            const effectiveTransitionOut = (closingScreen == null ? void 0 : closingScreen.transitionOut) || "fade";
            const hasTransition = effectiveTransitionOut !== "none";
            if (hasTransition) {
              setClosingScreens((prev) => new Set(prev).add(closingScreenId));
              setTimeout(() => {
                setScreenStack((stack) => stack.slice(0, -1));
                setClosingScreens((prev) => {
                  const next = new Set(prev);
                  next.delete(closingScreenId);
                  return next;
                });
              }, transitionDuration);
            } else {
              setScreenStack((stack) => stack.slice(0, -1));
            }
          } else if (screenStack.length === 1 && (playerState == null ? void 0 : playerState.mode) === "paused") {
            const closingScreenId = screenStack[0];
            const closingScreen = project.uiScreens[closingScreenId];
            const transitionDuration = (closingScreen == null ? void 0 : closingScreen.transitionOutDuration) ?? (closingScreen == null ? void 0 : closingScreen.transitionDuration) ?? 300;
            const hasTransition = ((closingScreen == null ? void 0 : closingScreen.transitionOut) || "fade") !== "none";
            const wasPlaying = !!((_a2 = playerState == null ? void 0 : playerState.musicState) == null ? void 0 : _a2.isPlaying);
            const finishResume = () => {
              updatePlayerState((p) => p ? { ...p, mode: "playing" } : null);
              setScreenStack([]);
              if (wasPlaying && musicAudioRef.current && musicAudioRef.current.src && musicAudioRef.current.paused) {
                musicAudioRef.current.play().catch((e) => console.error("Failed to resume music:", e));
              }
            };
            if (hasTransition) {
              setClosingScreens((prev) => new Set(prev).add(closingScreenId));
              setTimeout(() => {
                setClosingScreens((prev) => {
                  const next = new Set(prev);
                  next.delete(closingScreenId);
                  return next;
                });
                finishResume();
              }, transitionDuration + 50);
            } else {
              finishResume();
            }
          }
        }
      } else if (action.type === UIActionType.QuitToTitle) {
        if (playerState) {
          saveGame(0);
        }
        const audio = musicAudioRef.current;
        if (audio) {
          audio.pause();
          audio.currentTime = 0;
          audio.src = "";
        }
        stopAllSfx();
        const performQuit = () => {
          updatePlayerState(null);
          setHudStack([]);
          const resetVars = getInitialVariablesWithPersistent(project.variables, project.id);
          setUiVariables(resetVars);
          uiVariablesRef.current = resetVars;
          setMenuVariables(resetVars);
          runtimeDebugLog("[CLEAR] Dirty set cleared after QuitToTitle");
          uiDirtyVariableIdsRef.current.clear();
          if (project.ui.titleScreenId) setScreenStack([project.ui.titleScreenId]);
          setClosingScreens(/* @__PURE__ */ new Set());
        };
        const quitClosingIds = [];
        const topScreenForQuit = screenStack.length > 0 ? screenStack[screenStack.length - 1] : null;
        const topHudForQuit = hudStack.length > 0 ? hudStack[hudStack.length - 1] : null;
        if (topScreenForQuit) quitClosingIds.push(topScreenForQuit);
        if (topHudForQuit) quitClosingIds.push(topHudForQuit);
        let quitScreenOutDuration = 0;
        if (quitClosingIds.length > 0) {
          setClosingScreens((prev) => {
            const next = new Set(prev);
            quitClosingIds.forEach((id) => next.add(id));
            return next;
          });
          for (const id of quitClosingIds) {
            const s = project.uiScreens[id];
            if (s && s.transitionOut !== "none") {
              const dur = s.transitionOutDuration ?? s.transitionDuration ?? 300;
              quitScreenOutDuration = Math.max(quitScreenOutDuration, dur);
            }
          }
        }
        if (quitScreenOutDuration > 0) {
          setTimeout(performQuit, quitScreenOutDuration);
        } else {
          performQuit();
        }
      } else if (action.type === UIActionType.ExitGame) {
        const audio = musicAudioRef.current;
        if (audio) {
          audio.pause();
          audio.currentTime = 0;
          audio.src = "";
        }
        stopAllSfx();
        if (isStandalone) {
          const electronAPI = window.electronAPI;
          if (electronAPI == null ? void 0 : electronAPI.quitApp) {
            electronAPI.quitApp();
            return;
          }
          try {
            window.close();
          } catch {
          }
          window.setTimeout(() => {
            if (typeof document !== "undefined" && !document.hidden) {
              executeUIAction({ type: UIActionType.QuitToTitle });
            }
          }, 60);
          return;
        }
        onClose();
      } else if (action.type === UIActionType.ContinueGame) {
        const doLoad = async () => {
          const saves = savesPersistentRef.current ? await getGameSaves() : inMemorySavesRef.current;
          if (saves[0]) {
            loadGame(0);
          } else {
            runtimeDebugLog("[ContinueGame] No auto-save found, starting new game instead");
            startNewGameWithFade();
          }
        };
        void doLoad();
      } else if (action.type === UIActionType.SaveGame) {
        saveGame(action.slotNumber);
      } else if (action.type === UIActionType.DeleteSave) {
        deleteGameSaveSlot(action.slotNumber);
      } else if (action.type === UIActionType.ShowPhone) {
        if (project.ui.phoneOpenSoundId && !((_b2 = playerState == null ? void 0 : playerState.uiState.phone) == null ? void 0 : _b2.open)) playSound(project.ui.phoneOpenSoundId, void 0, false);
        updatePlayerState((p) => p ? { ...p, uiState: { ...p.uiState, phone: { ...p.uiState.phone || { open: false, messages: [] }, open: true, view: "home", notification: null, unread: false } } } : null);
      } else if (action.type === UIActionType.ShowPhoneText) {
        updatePlayerState((p) => p ? { ...p, uiState: { ...p.uiState, phone: { ...p.uiState.phone || { open: false, messages: [] }, open: true, view: "chat", notification: null, unread: false } } } : null);
      } else if (action.type === UIActionType.ShowPhoneHistory) {
        updatePlayerState((p) => {
          var _a3;
          return p ? { ...p, uiState: { ...p.uiState, phone: { ...p.uiState.phone || { open: false, messages: [] }, open: true, view: "history", notification: null, unread: false, notifications: (((_a3 = p.uiState.phone) == null ? void 0 : _a3.notifications) || []).map((e) => e.read ? e : { ...e, read: true }) } } } : null;
        });
      } else if (action.type === UIActionType.ShowPhoneContacts) {
        updatePlayerState((p) => p ? { ...p, uiState: { ...p.uiState, phone: { ...p.uiState.phone || { open: false, messages: [] }, open: true, view: "contacts", activeContactId: null, notification: null, unread: false } } } : null);
      } else if (action.type === UIActionType.OpenPhoneApp) {
        const appId = action.appId || "home";
        if (project.ui.phoneOpenSoundId && !((_c2 = playerState == null ? void 0 : playerState.uiState.phone) == null ? void 0 : _c2.open)) playSound(project.ui.phoneOpenSoundId, void 0, false);
        updatePlayerState((p) => {
          var _a3;
          return p ? { ...p, uiState: { ...p.uiState, phone: { ...p.uiState.phone || { open: false, messages: [] }, open: true, view: appId, ...appId === "contacts" ? { activeContactId: null } : {}, ...appId === "history" ? { notifications: (((_a3 = p.uiState.phone) == null ? void 0 : _a3.notifications) || []).map((e) => e.read ? e : { ...e, read: true }) } : {}, notification: null, unread: false } } } : null;
        });
      } else if (action.type === UIActionType.ShowMap) {
        const mapId = action.mapId;
        if ((_d2 = project.maps) == null ? void 0 : _d2[mapId]) {
          updatePlayerState((p) => p ? { ...p, uiState: { ...p.uiState, mapOverlay: { mapId, allowCancel: true, fromCommand: false } } } : null);
        } else {
          runtimeDebugWarn(\`[ShowMap action] Map \${mapId} not found\`);
        }
      } else if (action.type === UIActionType.ShowMiniGame) {
        const gameId = action.gameId;
        if ((_g2 = (_f2 = (_e2 = project.miniGames) == null ? void 0 : _e2[gameId]) == null ? void 0 : _f2.stages) == null ? void 0 : _g2.length) {
          updatePlayerState((p) => p ? { ...p, uiState: { ...p.uiState, miniGameOverlay: { gameId, fromCommand: false } } } : null);
        } else {
          runtimeDebugWarn(\`[ShowMiniGame action] Mini game \${gameId} not found or has no stages\`);
        }
      } else if (action.type === UIActionType.ClearUiPalette) {
        updatePlayerState((p) => p ? { ...p, uiPaletteOverride: null } : null);
      } else if (action.type === UIActionType.HidePhone) {
        if (project.ui.phoneCloseSoundId && ((_h2 = playerState == null ? void 0 : playerState.uiState.phone) == null ? void 0 : _h2.open)) playSound(project.ui.phoneCloseSoundId, void 0, false);
        updatePlayerState((p) => p ? { ...p, uiState: { ...p.uiState, phone: { ...p.uiState.phone || { open: false, messages: [] }, open: false, waiting: false, pendingChoices: void 0, notification: null } } } : null);
        if (project.ui.phoneOnCloseBehavior === "advance" && (playerState == null ? void 0 : playerState.mode) === "playing") {
          const d0 = (_i2 = playerStateRef.current) == null ? void 0 : _i2.uiState.dialogue;
          if (!((d0 == null ? void 0 : d0.timeLimitLocked) && (d0.timeLimit ?? 0) > 0)) {
            updatePlayerState((p) => p ? { ...p, currentIndex: p.currentIndex + 1, uiState: { ...p.uiState, isWaitingForInput: false, dialogue: null, isSkipping: false } } : null);
          }
        }
      } else if (action.type === UIActionType.HidePhoneText) {
        updatePlayerState((p) => p ? { ...p, uiState: { ...p.uiState, phone: { ...p.uiState.phone || { open: false, messages: [] }, messages: [], waiting: false, pendingChoices: void 0, pendingReplies: void 0, typing: null } } } : null);
      } else if (action.type === UIActionType.LoadGame) {
        loadGame(action.slotNumber);
      } else if (action.type === UIActionType.JumpToScene) {
        runtimeDebugLog("[JumpToScene] Action triggered");
        const jumpAction = action;
        const targetScene = project.scenes[jumpAction.targetSceneId];
        runtimeDebugLog("JumpToScene handler triggered:", {
          targetSceneId: jumpAction.targetSceneId,
          sceneExists: !!targetScene,
          sceneName: targetScene == null ? void 0 : targetScene.name,
          currentSceneId: playerState == null ? void 0 : playerState.currentSceneId,
          hasPlayerState: !!playerState
        });
        if (!targetScene) {
          runtimeDebugWarn(\`JumpToScene action failed: Scene with ID \${jumpAction.targetSceneId} not found.\`);
          return;
        }
        const executeJump = () => {
          runtimeDebugLog("[JumpToScene] Clearing screen and HUD stacks");
          setScreenStack([]);
          setHudStack([]);
          activeEffectTimeoutsRef.current.forEach((timeoutId) => clearTimeout(timeoutId));
          activeEffectTimeoutsRef.current = [];
          activeFlashRef.current = null;
          setFlashTrigger(0);
          activeLightningRef.current = null;
          activeFireworksRef.current = null;
          setFlashlight(null);
          setSpotlights({});
          activeShakeRef.current = null;
          commandSchedulerRef.current.reset();
          variableStoreRef.current = null;
          if (!playerState) {
            runtimeDebugLog("Initializing playerState for scene jump from title");
            const initialVariables = {};
            for (const varId in project.variables) {
              const v = project.variables[varId];
              const customizedValue = menuVariables[v.id];
              initialVariables[v.id] = customizedValue !== void 0 ? customizedValue : v.defaultValue;
            }
            updatePlayerState({
              mode: "playing",
              currentSceneId: jumpAction.targetSceneId,
              currentCommands: targetScene.commands,
              currentIndex: 0,
              commandStack: [],
              variables: initialVariables,
              history: [],
              stageState: {
                backgroundUrl: null,
                characters: {},
                textOverlays: [],
                imageOverlays: [],
                buttonOverlays: [],
                movieOverlays: [],
                screen: {
                  shake: { active: false, intensity: 0 },
                  tint: "transparent",
                  zoom: 1,
                  panX: 0,
                  panY: 0,
                  transitionDuration: 0.5,
                  overlayEffects: []
                },
                particleEffects: {}
              },
              uiState: {
                dialogue: null,
                choices: null,
                textInput: null,
                movieUrl: null,
                movieLoop: false,
                isWaitingForInput: false,
                isTransitioning: false,
                transitionElement: null,
                flash: null
              },
              musicState: {
                audioId: null,
                isPlaying: false,
                loop: false,
                currentTime: 0
              }
            });
            runtimeDebugLog("[CLEAR] Dirty set cleared after title screen jump");
            uiDirtyVariableIdsRef.current.clear();
          } else {
            runtimeDebugLog("Jumping to new scene from existing game state");
            runtimeDebugLog("[DEBUG Jump] Current variables before jump:", playerState.variables);
            const isSameScene = playerState.currentSceneId === jumpAction.targetSceneId;
            runtimeDebugLog("[DEBUG Jump] Same scene?", isSameScene, "Current:", playerState.currentSceneId, "Target:", jumpAction.targetSceneId);
            reactDom.flushSync(() => {
              updatePlayerState((p) => {
                if (!p) return null;
                runtimeDebugLog("Setting new scene:", {
                  targetSceneId: jumpAction.targetSceneId,
                  commandCount: targetScene.commands.length,
                  commands: targetScene.commands.map((c) => ({ type: c.type, id: c.id }))
                });
                runtimeDebugLog("[DEBUG Jump] Variables being carried over:", p.variables);
                runtimeDebugLog("[DEBUG Jump] uiVariables snapshot:", JSON.stringify(uiVariablesRef.current, null, 2));
                runtimeDebugLog("[DEBUG Jump] dirty variable IDs:", Array.from(uiDirtyVariableIdsRef.current));
                const mergedVariables = mergeDirtyUiVariables(p.variables);
                runtimeDebugLog("[DEBUG Jump] Variables after merge with uiVariables:", mergedVariables);
                const newIndex = isSameScene ? p.currentIndex + 1 : 0;
                runtimeDebugLog("[DEBUG Jump] Setting currentIndex to:", newIndex, "(was:", p.currentIndex, ")");
                return {
                  ...p,
                  currentSceneId: jumpAction.targetSceneId,
                  currentCommands: targetScene.commands,
                  currentIndex: newIndex,
                  // Reset stage state to clean slate
                  stageState: {
                    backgroundUrl: null,
                    characters: {},
                    textOverlays: [],
                    imageOverlays: [],
                    buttonOverlays: [],
                    movieOverlays: [],
                    screen: {
                      shake: { active: false, intensity: 0 },
                      tint: "transparent",
                      zoom: 1,
                      panX: 0,
                      panY: 0,
                      transitionDuration: 0.5,
                      overlayEffects: []
                    },
                    particleEffects: {}
                  },
                  // Clear any active UI state (dialogue, choices, etc.)
                  uiState: {
                    dialogue: null,
                    choices: null,
                    textInput: null,
                    movieUrl: null,
                    movieLoop: false,
                    isWaitingForInput: false,
                    isTransitioning: false,
                    transitionElement: null,
                    flash: null
                  },
                  variables: mergedVariables
                };
              });
            });
            runtimeDebugLog("[CLEAR] Dirty set cleared after JumpToScene");
            uiDirtyVariableIdsRef.current.clear();
          }
        };
        const jumpClosingIds = [];
        const topScreenForJump = screenStack.length > 0 ? screenStack[screenStack.length - 1] : null;
        const topHudForJump = hudStack.length > 0 ? hudStack[hudStack.length - 1] : null;
        if (topScreenForJump) jumpClosingIds.push(topScreenForJump);
        if (topHudForJump) jumpClosingIds.push(topHudForJump);
        let jumpScreenOutDuration = 0;
        if (jumpClosingIds.length > 0) {
          setClosingScreens((prev) => {
            const next = new Set(prev);
            jumpClosingIds.forEach((id) => next.add(id));
            return next;
          });
          for (const id of jumpClosingIds) {
            const s = project.uiScreens[id];
            if (s && s.transitionOut !== "none") {
              const dur = s.transitionOutDuration ?? s.transitionDuration ?? 300;
              jumpScreenOutDuration = Math.max(jumpScreenOutDuration, dur);
            }
          }
        }
        if (playerState == null ? void 0 : playerState.currentSceneId) {
          startSceneExitTransition(playerState.currentSceneId, executeJump);
        } else if (jumpScreenOutDuration > 0) {
          setTimeout(executeJump, jumpScreenOutDuration);
        } else {
          executeJump();
        }
      } else if (action.type === UIActionType.SetVariable) {
        const setVarAction = action;
        const variable = project.variables[setVarAction.variableId];
        if (!variable) {
          runtimeDebugWarn(\`SetVariable action failed: Variable with ID \${setVarAction.variableId} not found.\`);
          return;
        }
        runtimeDebugLog("[SetVariable] RAW ACTION:", {
          variableId: setVarAction.variableId,
          variableName: variable.name,
          variableType: variable.type,
          actionValue: setVarAction.value,
          actionValueType: typeof setVarAction.value,
          operator: setVarAction.operator
        });
        const originalOperator = setVarAction.operator;
        const effectiveOperator = normalizeSetVariableOperator(variable.type, variable.name, originalOperator);
        const wasCoercedOperator = originalOperator !== effectiveOperator;
        const computeNewValue = (currentVal) => {
          return calculateVariableValue(
            effectiveOperator,
            variable.type,
            currentVal,
            setVarAction.value,
            setVarAction.randomMin,
            setVarAction.randomMax,
            wasCoercedOperator ? originalOperator : void 0,
            variable.min,
            variable.max
          );
        };
        reactDom.flushSync(() => {
          if (playerState) {
            uiDirtyVariableIdsRef.current.add(setVarAction.variableId);
            setUiVariables((prev) => {
              const currentVal = prev[setVarAction.variableId];
              const newVal = computeNewValue(currentVal);
              runtimeDebugLog("[SetVariable] Details (uiVariables):", {
                variable: variable.name,
                variableId: setVarAction.variableId,
                rawValue: setVarAction.value,
                operator: \`\${setVarAction.operator} => \${effectiveOperator}\`,
                previousValue: currentVal,
                nextValue: newVal,
                type: variable.type
              });
              const next = { ...prev, [setVarAction.variableId]: newVal };
              uiVariablesRef.current = next;
              return next;
            });
          } else {
            setMenuVariables((prev) => {
              const currentVal = prev[setVarAction.variableId] ?? variable.defaultValue;
              const newVal = computeNewValue(currentVal);
              runtimeDebugLog("[SetVariable] Details (menu):", {
                variable: variable.name,
                variableId: setVarAction.variableId,
                rawValue: setVarAction.value,
                operator: \`\${setVarAction.operator} => \${effectiveOperator}\`,
                previousValue: currentVal,
                nextValue: newVal,
                type: variable.type
              });
              return { ...prev, [setVarAction.variableId]: newVal };
            });
          }
        });
        if (variable.scope === "persistent") {
          const currentVars = playerState ? uiVariablesRef.current : menuVariables;
          const prevPersistent = loadPersistentVariables(project.id);
          savePersistentVariables(project.id, { ...prevPersistent, [setVarAction.variableId]: currentVars[setVarAction.variableId] });
          runtimeDebugLog("[Variable Scope] Saved persistent variable from UI action:", variable.name);
        }
      } else if (action.type === UIActionType.ResetVariable) {
        const resetAction = action;
        const targetIds = resetAction.variableId === RESET_ALL_VARIABLES ? Object.keys(project.variables) : [resetAction.variableId];
        const validTargets = targetIds.filter((id) => !!project.variables[id]);
        if (validTargets.length === 0) {
          runtimeDebugWarn(\`ResetVariable action: no valid variables to reset (\${resetAction.variableId}).\`);
          return;
        }
        runtimeDebugLog("[ResetVariable] Resetting to defaults:", validTargets);
        reactDom.flushSync(() => {
          if (playerState) {
            setUiVariables((prev) => {
              const next = { ...prev };
              for (const id of validTargets) {
                uiDirtyVariableIdsRef.current.add(id);
                next[id] = project.variables[id].defaultValue;
              }
              uiVariablesRef.current = next;
              return next;
            });
          } else {
            setMenuVariables((prev) => {
              const next = { ...prev };
              for (const id of validTargets) {
                next[id] = project.variables[id].defaultValue;
              }
              return next;
            });
          }
        });
        const persistentTargets = validTargets.filter((id) => project.variables[id].scope === "persistent");
        if (persistentTargets.length > 0) {
          const prevPersistent = loadPersistentVariables(project.id);
          const merged = { ...prevPersistent };
          for (const id of persistentTargets) merged[id] = project.variables[id].defaultValue;
          savePersistentVariables(project.id, merged);
          runtimeDebugLog("[Variable Scope] Saved reset persistent variables:", persistentTargets);
        }
      } else if (action.type === UIActionType.PlaySound) {
        const soundAction = action;
        if (soundAction.audioId) {
          runtimeDebugLog("[PlaySound] action triggered:", soundAction.audioId, "volume:", soundAction.volume, "loop:", soundAction.loop);
          playSound(soundAction.audioId, soundAction.volume, soundAction.loop);
        }
      } else if (action.type === UIActionType.PlayMusic) {
        const a = action;
        if (a.audioId) {
          const cmd = { type: CommandType.PlayMusic, audioId: a.audioId, loop: a.loop ?? true, fadeDuration: a.fadeDuration ?? 1, volume: a.volume };
          const r = handlePlayMusic(cmd, { playerState: playerStateRef.current, assetResolver, musicAudioRef, fadeAudio, settings, setPlayerState: updatePlayerState });
          if ((_j2 = r.updates) == null ? void 0 : _j2.musicState) updatePlayerState((p) => p ? { ...p, musicState: { ...p.musicState, ...r.updates.musicState } } : p);
        }
      } else if (action.type === UIActionType.StopMusic) {
        const a = action;
        const cmd = { type: CommandType.StopMusic, fadeDuration: a.fadeDuration ?? 1 };
        const r = handleStopMusic(cmd, { musicAudioRef, fadeAudio, playerState: playerStateRef.current });
        if ((_k2 = r.updates) == null ? void 0 : _k2.musicState) updatePlayerState((p) => p ? { ...p, musicState: { ...p.musicState, ...r.updates.musicState } } : p);
      } else if (action.type === UIActionType.ShowSpotlight) {
        const a = action;
        const sid = a.spotlightId || "main";
        setSpotlights((prev) => ({ ...prev, [sid]: makeSpotlightState(a) }));
        if (a.sfxId) playSound(a.sfxId);
      } else if (action.type === UIActionType.HideSpotlight) {
        const sid = action.spotlightId || "main";
        setSpotlights((prev) => {
          const n = { ...prev };
          delete n[sid];
          return n;
        });
      } else if (action.type === UIActionType.ShowFlashlight) {
        const a = action;
        setFlashlight({ radius: a.radius ?? 22, softness: a.softness ?? 0.6, darkness: a.darkness ?? 0.85, radiusVariableId: null, darknessVariableId: null, color: a.color || "#000000", toggleKey: a.toggleKey, affectsDialogue: a.affectsDialogue !== false, darkWhenOff: false, on: true });
        if (a.sfxId) playSound(a.sfxId);
      } else if (action.type === UIActionType.HideFlashlight) {
        setFlashlight((f) => f ? { ...f, on: false } : f);
      } else if (action.type === UIActionType.CycleLayerAsset) {
        runtimeDebugLog("CycleLayerAsset handler triggered, playerState exists:", !!playerState);
        const cycleAction = action;
        runtimeDebugLog("CycleLayerAsset action details:", {
          characterId: cycleAction.characterId,
          layerId: cycleAction.layerId,
          variableId: cycleAction.variableId,
          direction: cycleAction.direction
        });
        const character = project.characters[cycleAction.characterId];
        if (!character) {
          runtimeDebugWarn(\`CycleLayerAsset action failed: Character with ID \${cycleAction.characterId} not found.\`);
          return;
        }
        runtimeDebugLog("Character found:", character.name);
        const layer = character.layers[cycleAction.layerId];
        if (!layer) {
          runtimeDebugWarn(\`CycleLayerAsset action failed: Layer with ID \${cycleAction.layerId} not found.\`);
          return;
        }
        runtimeDebugLog("Layer found:", layer.name);
        const assetsCount = Object.keys(layer.assets || {}).length;
        runtimeDebugLog("Assets count:", assetsCount);
        if (assetsCount === 0) {
          runtimeDebugWarn(\`CycleLayerAsset action failed: Layer "\${layer.name}" has no assets.\`);
          return;
        }
        if (playerState) {
          updatePlayerState((p) => {
            if (!p) return null;
            const currentIndex = Number(p.variables[cycleAction.variableId]) || 0;
            let newIndex;
            if (cycleAction.direction === "next") {
              newIndex = (currentIndex + 1) % assetsCount;
            } else {
              newIndex = (currentIndex - 1 + assetsCount) % assetsCount;
            }
            runtimeDebugLog(\`CycleLayerAsset (in-game): \${character.name} layer "\${layer.name}" from index \${currentIndex} to \${newIndex} (\${cycleAction.direction}), total assets: \${assetsCount}\`);
            return {
              ...p,
              variables: { ...p.variables, [cycleAction.variableId]: newIndex }
            };
          });
        } else {
          const currentIndex = Number(menuVariables[cycleAction.variableId]) || 0;
          let newIndex;
          if (cycleAction.direction === "next") {
            newIndex = (currentIndex + 1) % assetsCount;
          } else {
            newIndex = (currentIndex - 1 + assetsCount) % assetsCount;
          }
          runtimeDebugLog(\`CycleLayerAsset (menu): \${character.name} layer "\${layer.name}" from index \${currentIndex} to \${newIndex} (\${cycleAction.direction}), total assets: \${assetsCount}\`);
          setMenuVariables((vars) => ({
            ...vars,
            [cycleAction.variableId]: newIndex
          }));
        }
      } else if (action.type === UIActionType.JumpToLabel && playerState) {
        const jumpToLabelAction = action;
        const targetLabel = jumpToLabelAction.targetLabel;
        const targetSceneId = playerState.uiState.screenSceneId || playerState.currentSceneId;
        runtimeDebugLog("JumpToLabel handler triggered:", {
          targetLabel,
          currentSceneId: playerState.currentSceneId,
          currentSceneName: (_l2 = project.scenes[playerState.currentSceneId]) == null ? void 0 : _l2.name,
          screenSceneId: playerState.uiState.screenSceneId,
          targetSceneId,
          targetSceneName: (_m2 = project.scenes[targetSceneId]) == null ? void 0 : _m2.name
        });
        const targetScene = project.scenes[targetSceneId];
        if (!targetScene) {
          runtimeDebugWarn("JumpToLabel failed: Target scene not found");
          return;
        }
        const allLabels = targetScene.commands.filter((cmd) => cmd.type === CommandType.Label).map((cmd) => cmd.labelId);
        runtimeDebugLog("JumpToLabel: Available labels in target scene:", allLabels);
        const labelIndex = targetScene.commands.findIndex(
          (cmd) => cmd.type === CommandType.Label && cmd.labelId === targetLabel
        );
        if (labelIndex === -1) {
          runtimeDebugWarn(\`JumpToLabel failed: Label "\${targetLabel}" not found in scene "\${targetScene.name}"\`);
          runtimeDebugWarn("Looking for label:", targetLabel);
          runtimeDebugWarn("Available labels:", allLabels);
          return;
        }
        runtimeDebugLog(\`JumpToLabel: Jumping to label "\${targetLabel}" at index \${labelIndex} in scene "\${targetScene.name}"\`);
        runtimeDebugLog("JumpToLabel: Label command at that index:", targetScene.commands[labelIndex]);
        const performJumpToLabel = () => {
          setHudStack([]);
          reactDom.flushSync(() => {
            updatePlayerState((p) => {
              if (!p) return null;
              runtimeDebugLog("JumpToLabel: Setting new state - currentIndex from", p.currentIndex, "to", labelIndex);
              runtimeDebugLog("JumpToLabel: BEFORE merge - playerState.variables:", JSON.stringify(p.variables, null, 2));
              runtimeDebugLog("JumpToLabel: uiVariables to merge:", JSON.stringify(uiVariablesRef.current, null, 2));
              runtimeDebugLog("JumpToLabel: dirty variable IDs:", Array.from(uiDirtyVariableIdsRef.current));
              const mergedVariables = mergeDirtyUiVariables(p.variables);
              runtimeDebugLog("JumpToLabel: AFTER merge - merged variables:", JSON.stringify(mergedVariables, null, 2));
              return {
                ...p,
                currentSceneId: targetSceneId,
                currentCommands: targetScene.commands,
                currentIndex: labelIndex,
                variables: mergedVariables,
                // Merge UI variables into game variables
                stageState: {
                  ...p.stageState,
                  buttonOverlays: [],
                  imageOverlays: [],
                  textOverlays: []
                },
                uiState: {
                  ...p.uiState,
                  dialogue: null,
                  choices: null,
                  isWaitingForInput: false,
                  screenSceneId: null
                  // Clear the stored scene ID after jumping
                }
              };
            });
          });
          runtimeDebugLog("[CLEAR] Dirty set cleared after JumpToLabel");
          uiDirtyVariableIdsRef.current.clear();
        };
        const topHudForJTL = hudStack.length > 0 ? hudStack[hudStack.length - 1] : null;
        let jtlScreenOutDuration = 0;
        if (topHudForJTL) {
          const s = project.uiScreens[topHudForJTL];
          if (s && s.transitionOut !== "none") {
            jtlScreenOutDuration = s.transitionOutDuration ?? s.transitionDuration ?? 300;
            setClosingScreens((prev) => new Set(prev).add(topHudForJTL));
          }
        }
        if (jtlScreenOutDuration > 0) {
          setTimeout(performJumpToLabel, jtlScreenOutDuration);
        } else {
          performJumpToLabel();
        }
      } else if (action.type === UIActionType.StartTimer) {
        startTimer(action);
      } else if (action.type === UIActionType.StopTimer) {
        stopTimer(action.timerId);
      } else if (action.type === UIActionType.SetTimeOfDay) {
        const a = action;
        applyTimeOfDay(a.mode, a.mode === "set" ? a.hour : a.hours, a.transitionDuration);
      } else if (action.type === UIActionType.OpenURL) {
        const openUrlAction = action;
        if (openUrlAction.url) {
          runtimeDebugLog("OpenURL action triggered:", openUrlAction.url, "newTab:", openUrlAction.newTab);
          if (openUrlAction.newTab !== false) {
            window.open(openUrlAction.url, "_blank", "noopener,noreferrer");
          } else {
            window.location.href = openUrlAction.url;
          }
        }
      } else if (action.type === UIActionType.CallCommonEvent) {
        const ccAction = action;
        const ce = (project.commonEvents || {})[ccAction.commonEventId];
        if (!ce || !ce.enabled || !ce.commands || ce.commands.length === 0) {
          runtimeDebugWarn("[CallCommonEvent action] event not found / disabled / empty");
          notify('A "Call Common Event" action points to a missing, disabled, or empty event.', "warning");
          return;
        }
        if (!playerState || playerState.mode !== "playing") {
          notify("Call Common Event only works during gameplay", "warning");
          return;
        }
        if (playerState.commandStack.length >= MAX_CALL_DEPTH || playerState.commandStack.some((f) => f.commonEventId === ce.id)) {
          notify(\`Common Event call blocked (depth/cycle): "\${ce.name}"\`, "error");
          return;
        }
        const overrides = {};
        const savedVariables = {};
        const clearedVariables = [];
        for (const param of ce.parameters || []) {
          const raw = (_n2 = ccAction.arguments) == null ? void 0 : _n2[param.id];
          overrides[param.id] = raw !== void 0 ? coerceParam(raw, param.type) : param.defaultValue;
          if (Object.prototype.hasOwnProperty.call(playerState.variables, param.id)) savedVariables[param.id] = playerState.variables[param.id];
          else clearedVariables.push(param.id);
        }
        updatePlayerState((p) => {
          if (!p) return null;
          const newStack = [...p.commandStack, {
            sceneId: p.currentSceneId,
            commands: p.currentCommands,
            // Return point: normally the command AFTER the current (waiting) one. When the
            // caller already advanced the index to the next un-run command (choice flow),
            // resume AT it — +1 here would skip the command right after the choice.
            index: (opts == null ? void 0 : opts.resumeAtCurrent) ? p.currentIndex : p.currentIndex + 1,
            commonEventId: ce.id,
            ...Object.keys(savedVariables).length > 0 ? { savedVariables } : {},
            ...clearedVariables.length > 0 ? { clearedVariables } : {}
          }];
          return { ...p, currentCommands: ce.commands, currentIndex: 0, commandStack: newStack, variables: { ...p.variables, ...overrides } };
        });
      } else if (action.type === UIActionType.GiveItem) {
        const a = action;
        const item = (_o2 = project.items) == null ? void 0 : _o2[a.itemId];
        if (item) executeUIAction(item.unique ? { type: UIActionType.SetVariable, variableId: item.countVariableId, operator: "set", value: 1 } : { type: UIActionType.SetVariable, variableId: item.countVariableId, operator: "add", value: a.quantity ?? 1 });
      } else if (action.type === UIActionType.UseItem) {
        const a = action;
        const item = (_p2 = project.items) == null ? void 0 : _p2[a.itemId];
        if (item) {
          if (item.consumeOnUse !== false) executeUIAction({ type: UIActionType.SetVariable, variableId: item.countVariableId, operator: "subtract", value: 1 });
          (item.useEffect || []).forEach((eff) => executeUIAction(eff));
        }
      } else if (action.type === UIActionType.DestroyItem) {
        const a = action;
        const item = (_q2 = project.items) == null ? void 0 : _q2[a.itemId];
        if (item) executeUIAction(a.all ? { type: UIActionType.SetVariable, variableId: item.countVariableId, operator: "set", value: 0 } : { type: UIActionType.SetVariable, variableId: item.countVariableId, operator: "subtract", value: a.quantity ?? 1 });
      } else if (action.type === UIActionType.UseSelectedItem) {
        const selId = (_r2 = playerStateRef.current) == null ? void 0 : _r2.selectedItemId;
        const item = selId ? (_s2 = project.items) == null ? void 0 : _s2[selId] : void 0;
        if (item && item.usable) {
          if (item.consumeOnUse !== false) executeUIAction({ type: UIActionType.SetVariable, variableId: item.countVariableId, operator: "subtract", value: 1 });
          (item.useEffect || []).forEach((eff) => executeUIAction(eff));
        }
      } else if (action.type === UIActionType.CarryItem) {
        startCarry(action.itemId);
      } else if (action.type === UIActionType.RestockCollection) {
        const a = action;
        const collection = (_t = project.itemCollections) == null ? void 0 : _t[a.collectionId];
        if (collection) {
          const restocked = computeCollectionRestock(collection, project.variables);
          Object.entries(restocked).forEach(([varId, val]) => {
            executeUIAction({ type: UIActionType.SetVariable, variableId: varId, operator: "set", value: val });
          });
        }
      } else if (action.type === UIActionType.BuyItem || action.type === UIActionType.SellItem || action.type === UIActionType.BuySelectedItem || action.type === UIActionType.SellSelectedItem) {
        const a = action;
        const collection = (_u = project.itemCollections) == null ? void 0 : _u[a.collectionId];
        const isBuy = action.type === UIActionType.BuyItem || action.type === UIActionType.BuySelectedItem;
        const isSelected = action.type === UIActionType.BuySelectedItem || action.type === UIActionType.SellSelectedItem;
        const itemId = isSelected ? (_v = playerStateRef.current) == null ? void 0 : _v.selectedItemId : a.itemId;
        if (collection && itemId) {
          const curVars = ((_w = playerStateRef.current) == null ? void 0 : _w.variables) || {};
          const res = isBuy ? computeBuy(itemId, collection, project, curVars) : computeSell(itemId, collection, project, curVars);
          if (!("blocked" in res)) {
            Object.entries(res.updates).forEach(([varId, val]) => {
              executeUIAction({ type: UIActionType.SetVariable, variableId: varId, operator: "set", value: val });
            });
          }
        }
      } else if (action.type === UIActionType.ShowLog) {
        updatePlayerState((p) => p ? { ...p, uiState: { ...p.uiState, showHistory: true } } : null);
      } else if (action.type === UIActionType.ToggleAutoAdvance) {
        setSettings((s) => ({ ...s, autoAdvance: !s.autoAdvance }));
      } else if (action.type === UIActionType.ToggleSkip) {
        updatePlayerState((p) => p ? { ...p, uiState: { ...p.uiState, isSkipping: !p.uiState.isSkipping } } : null);
      } else if (action.type === UIActionType.SkipBackward) {
        handleSkipBackward();
      }
    };
    const reorderSlots = (slots) => {
      updatePlayerState((p) => p ? { ...p, inventorySlots: slots } : null);
    };
    const selectItem = (itemId, elementId) => {
      updatePlayerState((p) => p ? { ...p, selectedItemId: itemId, selectedElementId: itemId ? elementId : null } : null);
    };
    const startCarry = (itemId) => {
      var _a2;
      const item = (_a2 = project.items) == null ? void 0 : _a2[itemId];
      if (!item) return;
      if (Number(screenVariables[item.countVariableId] ?? 0) < 1) return;
      setCarriedItemId(itemId);
      if (hudStack.length > 0) setHudStack((s) => s.slice(0, -1));
      else if (screenStack.length > 1) setScreenStack((s) => s.slice(0, -1));
      updatePlayerState((p) => p ? { ...p, selectedItemId: null, selectedElementId: null } : null);
    };
    const resolveCarryClick = (clientX, clientY) => {
      var _a2, _b2;
      const item = carriedItemId ? (_a2 = project.items) == null ? void 0 : _a2[carriedItemId] : null;
      const rect = (_b2 = playContainerRef.current) == null ? void 0 : _b2.getBoundingClientRect();
      if (!item || !carriedItemId || !rect || !rect.width || !rect.height) {
        setCarriedItemId(null);
        return;
      }
      const pctX = (clientX - rect.left) / rect.width * 100;
      const pctY = (clientY - rect.top) / rect.height * 100;
      const target = hitTestDropTarget({ x: pctX, y: pctY }, carriedItemId, item.dragTag || void 0);
      if (target) {
        if (item.consumeOnUse !== false) {
          executeUIAction({ type: UIActionType.SetVariable, variableId: item.countVariableId, operator: "subtract", value: 1 });
        }
        (item.useEffect || []).forEach((eff) => executeUIAction(eff));
        target.onDrop(carriedItemId, item.dragTag);
      }
      setCarriedItemId(null);
    };
    React2.useEffect(() => {
      if (!carriedItemId) return;
      const onMove = (e) => setCarryCursor({ x: e.clientX, y: e.clientY });
      const onKey = (e) => {
        if (e.key === "Escape") {
          e.preventDefault();
          e.stopPropagation();
          setCarriedItemId(null);
        }
      };
      window.addEventListener("mousemove", onMove);
      window.addEventListener("keydown", onKey, true);
      return () => {
        window.removeEventListener("mousemove", onMove);
        window.removeEventListener("keydown", onKey, true);
      };
    }, [carriedItemId]);
    const [itemDragGhost, setItemDragGhost] = React2.useState(null);
    const handleItemDragMove = (url, x, y, w, h) => setItemDragGhost({ url, x, y, w, h });
    const handleItemDragEnd = () => setItemDragGhost(null);
    const handleItemOverlayDrop = (overlay, clientX, clientY) => {
      var _a2, _b2;
      const itemId = overlay.dragItemId || overlay.giveItemId || null;
      const item = itemId ? (_a2 = project.items) == null ? void 0 : _a2[itemId] : null;
      const rect = (_b2 = playContainerRef.current) == null ? void 0 : _b2.getBoundingClientRect();
      if (!rect || !rect.width || !rect.height) return;
      const pctX = (clientX - rect.left) / rect.width * 100;
      const pctY = (clientY - rect.top) / rect.height * 100;
      const tag = (item == null ? void 0 : item.dragTag) || void 0;
      const target = hitTestDropTarget({ x: pctX, y: pctY }, itemId || overlay.id, tag);
      if (!target) return;
      target.onDrop(itemId || overlay.id, tag);
      updatePlayerState((p) => p ? {
        ...p,
        stageState: overlay.removeAfterClick ? { ...p.stageState, buttonOverlays: p.stageState.buttonOverlays.filter((b) => b.id !== overlay.id) } : p.stageState,
        pickedUpItems: overlay.pickUpOnceId ? [...p.pickedUpItems || [], overlay.pickUpOnceId] : p.pickedUpItems
      } : null);
    };
    const phoneNavigates = (acts) => acts.some((a) => a.type === UIActionType.JumpToScene || a.type === UIActionType.JumpToLabel || a.type === UIActionType.GoToScreen || a.type === UIActionType.CallCommonEvent || a.type === UIActionType.StartNewGame || a.type === UIActionType.LoadGame || a.type === UIActionType.QuitToTitle);
    const clearPhoneTimers = () => {
      phoneTimersRef.current.forEach((id) => clearTimeout(id));
      phoneTimersRef.current = [];
    };
    const pushPhoneTimer = (fn, ms) => {
      const id = window.setTimeout(fn, ms);
      phoneTimersRef.current.push(id);
      return id;
    };
    const appendPhoneMessage = (m, open) => {
      updatePlayerState((p) => {
        if (!p) return null;
        const ph = p.uiState.phone || { open: false, messages: [] };
        const msg = m.senderId === "player" && !m.contactId && ph.activeContactId && ph.activeContactId !== "__misc__" ? { ...m, contactId: ph.activeContactId } : m;
        const nextMessages = [...ph.messages, msg];
        const key = phoneThreadKey(msg);
        const watching = ph.activeContactId === key || ph.activeContactId === "__misc__" && key === "";
        const threadLastRead = watching ? { ...ph.threadLastRead || {}, [key]: countPhoneThread(nextMessages, key) } : ph.threadLastRead;
        return { ...p, uiState: { ...p.uiState, phone: { ...ph, open, view: "chat", messages: nextMessages, cameraRoll: collectToCameraRoll(ph.cameraRoll, msg), threadLastRead, typing: null } } };
      });
    };
    const playPhoneFollowUps = (followUps, onDone) => {
      if (!followUps.length) {
        onDone();
        return;
      }
      let delay = 0;
      followUps.forEach((f, idx) => {
        const typeMs = Math.max(0, f.delayMs ?? 900);
        delay += 150;
        pushPhoneTimer(() => updatePlayerState((p) => p && p.uiState.phone ? { ...p, uiState: { ...p.uiState, phone: { ...p.uiState.phone, open: true, typing: { senderId: f.senderId } } } } : p), delay);
        delay += typeMs;
        pushPhoneTimer(() => {
          appendPhoneMessage({ id: \`fu-\${Date.now()}-\${idx}\`, senderId: f.senderId, text: f.text, ...f.portrait ? { portrait: f.portrait } : {}, ...f.image ? { image: f.image } : {} }, true);
          if (f.soundId) playSound(f.soundId, void 0, false);
          if (idx === followUps.length - 1) pushPhoneTimer(onDone, 60);
        }, delay);
      });
    };
    const handlePhoneReply = (reply) => {
      var _a2, _b2;
      clearPhoneTimers();
      const wasWaiting = !!((_b2 = (_a2 = playerStateRef.current) == null ? void 0 : _a2.uiState.phone) == null ? void 0 : _b2.waiting);
      updatePlayerState((p) => {
        if (!p) return null;
        const ph = p.uiState.phone || { open: true, messages: [] };
        const msgs = ph.messages;
        const replyMsg = { id: \`reply-\${msgs.length}\`, senderId: "player", text: reply.text, ...ph.activeContactId ? { contactId: ph.activeContactId } : {} };
        return { ...p, uiState: { ...p.uiState, phone: { ...ph, open: ph.open ?? true, messages: [...msgs, replyMsg], waiting: false, pendingChoices: void 0, pendingReplies: void 0 } } };
      });
      const acts = reply.actions || [];
      acts.forEach((a) => handleUIAction(a));
      const navigates = phoneNavigates(acts);
      const resume = () => {
        var _a3, _b3;
        if (wasWaiting && !navigates) updatePlayerState((p) => p ? { ...p, currentIndex: p.currentIndex + 1, uiState: { ...p.uiState, isWaitingForInput: false } } : null);
        if (!navigates && ((_b3 = (_a3 = playerStateRef.current) == null ? void 0 : _a3.uiState.phone) == null ? void 0 : _b3.activeTextConvo)) pushPhoneTimer(playNextTextLine, 300);
      };
      const followUps = reply.followUps || [];
      if (followUps.length && !navigates) playPhoneFollowUps(followUps, resume);
      else resume();
    };
    const pickConversation = (entries, legacy, variables, played) => {
      var _a2, _b2, _c2, _d2;
      for (const e of entries || []) {
        if (!((_b2 = (_a2 = e.conversation) == null ? void 0 : _a2.lines) == null ? void 0 : _b2.length)) continue;
        if (e.once && played.includes(e.id)) continue;
        if (((_c2 = e.conditions) == null ? void 0 : _c2.length) && !evaluateConditions2(e.conditions, variables)) continue;
        return { entry: e, conversation: e.conversation };
      }
      if ((_d2 = legacy == null ? void 0 : legacy.lines) == null ? void 0 : _d2.length) return { conversation: legacy };
      return null;
    };
    const markConversationPlayed = (id) => updatePlayerState((p) => p && p.uiState.phone && !(p.uiState.phone.playedConversations || []).includes(id) ? { ...p, uiState: { ...p.uiState, phone: { ...p.uiState.phone, playedConversations: [...p.uiState.phone.playedConversations || [], id] } } } : p);
    const playNextTextLine = () => {
      var _a2, _b2;
      const p0 = playerStateRef.current;
      const at = (_a2 = p0 == null ? void 0 : p0.uiState.phone) == null ? void 0 : _a2.activeTextConvo;
      if (!p0 || !at) return;
      const lines = at.conversation.lines || [];
      let idx = at.lineIndex;
      while (idx < lines.length) {
        const cand = lines[idx];
        if (!((_b2 = cand.conditions) == null ? void 0 : _b2.length) || evaluateConditions2(cand.conditions, p0.variables)) break;
        idx++;
      }
      if (idx >= lines.length) {
        const endActions = at.conversation.endActions || [];
        updatePlayerState((p) => p && p.uiState.phone ? { ...p, uiState: { ...p.uiState, phone: { ...p.uiState.phone, activeTextConvo: null, typing: null } } } : p);
        endActions.forEach((a) => handleUIAction(a));
        return;
      }
      const line = lines[idx];
      const landAndContinue = () => {
        var _a3;
        appendPhoneMessage({ id: \`tc-\${Date.now()}-\${idx}\`, senderId: line.speakerId, text: line.text, ...line.portrait ? { portrait: line.portrait } : {}, ...line.image ? { image: line.image } : {}, contactId: at.contactId }, true);
        if (line.soundId) playSound(line.soundId, void 0, false);
        const hasReplies = !!((_a3 = line.replies) == null ? void 0 : _a3.length);
        updatePlayerState((p) => p && p.uiState.phone ? { ...p, uiState: { ...p.uiState, phone: { ...p.uiState.phone, activeTextConvo: p.uiState.phone.activeTextConvo ? { ...p.uiState.phone.activeTextConvo, lineIndex: idx + 1 } : null, ...hasReplies ? { pendingReplies: line.replies } : {} } } } : p);
        if (!hasReplies) pushPhoneTimer(playNextTextLine, 250);
      };
      const typeMs = Math.max(0, line.delayMs ?? 900);
      if (line.speakerId !== "player" && typeMs > 0) {
        updatePlayerState((p) => p && p.uiState.phone ? { ...p, uiState: { ...p.uiState, phone: { ...p.uiState.phone, typing: { senderId: line.speakerId } } } } : p);
        pushPhoneTimer(landAndContinue, typeMs);
      } else if (typeMs > 0) {
        pushPhoneTimer(landAndContinue, typeMs);
      } else {
        landAndContinue();
      }
    };
    const startTextConversation = (contactId, pick) => {
      var _a2;
      if ((_a2 = pick.entry) == null ? void 0 : _a2.once) markConversationPlayed(pick.entry.id);
      updatePlayerState((p) => {
        var _a3;
        return p && p.uiState.phone ? { ...p, uiState: { ...p.uiState, phone: { ...p.uiState.phone, activeTextConvo: { contactId, conversation: pick.conversation, lineIndex: 0, entryId: (_a3 = pick.entry) == null ? void 0 : _a3.id } } } } : p;
      });
      pushPhoneTimer(playNextTextLine, 400);
    };
    const handleOpenThread = (contactId) => {
      var _a2, _b2, _c2, _d2;
      updatePlayerState((p) => {
        if (!p) return null;
        const ph = p.uiState.phone || { open: false, messages: [] };
        if (contactId === null) return { ...p, uiState: { ...p.uiState, phone: { ...ph, view: "chat", activeContactId: null } } };
        const key = contactId === "__misc__" ? "" : contactId;
        const active = contactId === "" ? "__misc__" : contactId;
        return {
          ...p,
          uiState: {
            ...p.uiState,
            phone: {
              ...ph,
              open: true,
              view: "chat",
              activeContactId: active,
              notification: null,
              unread: false,
              threadLastRead: { ...ph.threadLastRead || {}, [key]: countPhoneThread(ph.messages, key) }
            }
          }
        };
      });
      if (contactId && contactId !== "__misc__") {
        const ph = (_a2 = playerStateRef.current) == null ? void 0 : _a2.uiState.phone;
        if (!(ph == null ? void 0 : ph.activeTextConvo) && !((_b2 = ph == null ? void 0 : ph.pendingReplies) == null ? void 0 : _b2.length) && !((_c2 = ph == null ? void 0 : ph.pendingChoices) == null ? void 0 : _c2.length)) {
          const contact = (project.ui.phoneContacts || []).find((c) => c.characterId === contactId);
          const pick = contact ? pickConversation(contact.textConversations, void 0, ((_d2 = playerStateRef.current) == null ? void 0 : _d2.variables) || {}, (ph == null ? void 0 : ph.playedConversations) || []) : null;
          if (pick) startTextConversation(contactId, pick);
        }
      }
    };
    const handleContactMessage = (contactId) => handleOpenThread(contactId);
    const handleContactCall = (contactId) => {
      var _a2, _b2, _c2, _d2;
      clearPhoneTimers();
      const contactWithConvo = (project.ui.phoneContacts || []).find((c) => c.characterId === contactId);
      const pick = contactWithConvo ? pickConversation(contactWithConvo.callConversations, contactWithConvo.callConversation, ((_a2 = playerStateRef.current) == null ? void 0 : _a2.variables) || {}, ((_c2 = (_b2 = playerStateRef.current) == null ? void 0 : _b2.uiState.phone) == null ? void 0 : _c2.playedConversations) || []) : null;
      if (pick) {
        if ((_d2 = pick.entry) == null ? void 0 : _d2.once) markConversationPlayed(pick.entry.id);
        startOutgoingTranscriptCall({ contactId, portrait: contactWithConvo.avatar, conversation: pick.conversation, blocking: false });
        return;
      }
      updatePlayerState((p) => p ? { ...p, uiState: { ...p.uiState, phone: { ...p.uiState.phone || { open: false, messages: [] }, outgoingCall: { contactId } } } } : null);
      const contact = (project.ui.phoneContacts || []).find((c) => c.characterId === contactId);
      if (contact == null ? void 0 : contact.callAction) {
        pushPhoneTimer(() => {
          updatePlayerState((p) => p ? { ...p, uiState: { ...p.uiState, phone: p.uiState.phone ? { ...p.uiState.phone, outgoingCall: null } : p.uiState.phone } } : null);
          handleUIAction(contact.callAction);
        }, 1200);
      }
    };
    const endOutgoingCall = () => {
      clearPhoneTimers();
      updatePlayerState((p) => p ? { ...p, uiState: { ...p.uiState, phone: p.uiState.phone ? { ...p.uiState.phone, outgoingCall: null } : p.uiState.phone } } : null);
    };
    const patchActiveCall = (fn) => {
      updatePlayerState((p) => {
        var _a2;
        const ac = (_a2 = p == null ? void 0 : p.uiState.phone) == null ? void 0 : _a2.activeCall;
        if (!p || !ac) return p;
        const patch = fn(ac);
        return { ...p, uiState: { ...p.uiState, phone: { ...p.uiState.phone, activeCall: patch === null ? null : { ...ac, ...patch } } } };
      });
    };
    const landCallLine = (line, then, pacingMs) => {
      patchActiveCall((ac) => ({ transcript: [...ac.transcript, { id: \`cl-\${Date.now()}-\${ac.transcript.length}\`, senderId: line.speakerId, text: line.text, ...line.portrait ? { portrait: line.portrait } : {}, ...line.image ? { image: line.image } : {} }] }));
      if (line.soundId) playSound(line.soundId, void 0, false);
      const speaker = line.speakerId === "player" ? null : project.characters[line.speakerId];
      const voiceId = line.voiceAudioId ?? (speaker == null ? void 0 : speaker.defaultVoiceId) ?? null;
      const audio = voiceId ? playVoice ? playVoice(voiceId, settings.voiceVolume ?? 1) : null : null;
      if (line.waitForVoice && audio) {
        audio.onended = () => pushPhoneTimer(then, 350);
        audio.onerror = () => pushPhoneTimer(then, pacingMs);
      } else {
        pushPhoneTimer(then, pacingMs);
      }
    };
    const playNextCallLine = () => {
      var _a2, _b2, _c2, _d2;
      const p = playerStateRef.current;
      const ac = (_a2 = p == null ? void 0 : p.uiState.phone) == null ? void 0 : _a2.activeCall;
      if (!p || !ac || ac.phase !== "active") return;
      const queue = ac.queue || [];
      if (queue.length > 0) {
        const [f, ...rest] = queue;
        patchActiveCall(() => ({ queue: rest }));
        landCallLine({ speakerId: f.senderId, text: f.text, portrait: f.portrait, voiceAudioId: f.voiceAudioId, waitForVoice: !!f.voiceAudioId, soundId: f.soundId }, playNextCallLine, Math.max(250, f.delayMs ?? 900));
        return;
      }
      const lines = ((_b2 = ac.conversation) == null ? void 0 : _b2.lines) || [];
      let idx = ac.lineIndex;
      while (idx < lines.length) {
        const candidate = lines[idx];
        if (!((_c2 = candidate.conditions) == null ? void 0 : _c2.length) || evaluateConditions2(candidate.conditions, p.variables)) break;
        idx++;
      }
      if (idx >= lines.length) {
        if (((_d2 = ac.conversation) == null ? void 0 : _d2.autoEnd) !== false) endActiveCall();
        return;
      }
      const line = lines[idx];
      patchActiveCall(() => ({ lineIndex: idx + 1 }));
      landCallLine(line, () => {
        var _a3, _b3, _c3;
        const cur = (_b3 = (_a3 = playerStateRef.current) == null ? void 0 : _a3.uiState.phone) == null ? void 0 : _b3.activeCall;
        if (!cur || cur.phase !== "active") return;
        if ((_c3 = line.replies) == null ? void 0 : _c3.length) {
          patchActiveCall(() => ({ pendingReplies: line.replies }));
        } else {
          playNextCallLine();
        }
      }, Math.max(250, line.delayMs ?? 900));
    };
    const beginActiveCall = (opts) => {
      updatePlayerState((p) => {
        if (!p) return null;
        const ph = p.uiState.phone || { open: false, messages: [] };
        return { ...p, uiState: { ...p.uiState, phone: { ...ph, open: true, view: "call", outgoingCall: null, notification: null, activeCall: { contactId: opts.contactId, direction: opts.direction, portrait: opts.portrait, phase: "active", conversation: opts.conversation, lineIndex: 0, transcript: [], elapsedMs: 0, blocking: opts.blocking } } } };
      });
      pushPhoneTimer(playNextCallLine, 500);
    };
    const handleCallReply = (reply) => {
      var _a2, _b2;
      const ac = (_b2 = (_a2 = playerStateRef.current) == null ? void 0 : _a2.uiState.phone) == null ? void 0 : _b2.activeCall;
      if (!ac) return;
      patchActiveCall((cur) => ({
        transcript: [...cur.transcript, { id: \`cr-\${Date.now()}\`, senderId: "player", text: reply.text }],
        pendingReplies: void 0,
        queue: [...cur.queue || [], ...reply.followUps || []]
      }));
      (reply.actions || []).forEach((a) => handleUIAction(a));
      if (reply.endsCall && !(reply.followUps || []).length) {
        endActiveCall();
        return;
      }
      if (reply.endsCall) {
        patchActiveCall((cur) => ({ conversation: { ...cur.conversation || { lines: [] }, lines: [], autoEnd: true }, lineIndex: 0 }));
      }
      pushPhoneTimer(playNextCallLine, 300);
    };
    const endActiveCall = (_reason) => {
      var _a2, _b2;
      const ac = (_b2 = (_a2 = playerStateRef.current) == null ? void 0 : _a2.uiState.phone) == null ? void 0 : _b2.activeCall;
      if (!ac || ac.phase === "ended") return;
      stopVoice == null ? void 0 : stopVoice();
      const durationMs = ac.elapsedMs;
      patchActiveCall(() => ({ phase: "ended", pendingReplies: void 0, queue: [] }));
      pushPhoneTimer(() => {
        var _a3, _b3;
        const cur = (_b3 = (_a3 = playerStateRef.current) == null ? void 0 : _a3.uiState.phone) == null ? void 0 : _b3.activeCall;
        const conv = cur == null ? void 0 : cur.conversation;
        const blocking = cur == null ? void 0 : cur.blocking;
        updatePlayerState((p) => {
          if (!p) return null;
          const ph = p.uiState.phone || { open: false, messages: [] };
          const log2 = ph.callLog || [];
          const entry = { id: \`call-\${Date.now()}\`, callerId: ac.contactId, status: "accepted", portrait: ac.portrait, order: log2.length, direction: ac.direction, durationMs };
          return { ...p, uiState: { ...p.uiState, phone: { ...ph, activeCall: null, view: "home", callLog: [...log2, entry] } } };
        });
        const acts = (conv == null ? void 0 : conv.endActions) || [];
        acts.forEach((a) => handleUIAction(a));
        if (blocking && !phoneNavigates(acts)) {
          updatePlayerState((p) => p ? { ...p, currentIndex: p.currentIndex + 1, uiState: { ...p.uiState, isWaitingForInput: false } } : null);
        }
      }, 1200);
    };
    const startOutgoingTranscriptCall = (opts) => {
      clearPhoneTimers();
      updatePlayerState((p) => {
        if (!p) return null;
        const ph = p.uiState.phone || { open: false, messages: [] };
        return { ...p, uiState: { ...p.uiState, phone: { ...ph, open: true, view: "call", outgoingCall: null, activeCall: { contactId: opts.contactId, direction: "outgoing", portrait: opts.portrait, phase: "dialing", conversation: opts.conversation, lineIndex: 0, transcript: [], elapsedMs: 0, blocking: opts.blocking } } } };
      });
      pushPhoneTimer(() => {
        var _a2, _b2;
        const cur = (_b2 = (_a2 = playerStateRef.current) == null ? void 0 : _a2.uiState.phone) == null ? void 0 : _b2.activeCall;
        if (!cur || cur.phase !== "dialing") return;
        patchActiveCall(() => ({ phase: "active" }));
        pushPhoneTimer(playNextCallLine, 400);
      }, Math.max(200, opts.dialingMs ?? 1200));
    };
    React2.useEffect(() => {
      var _a2, _b2;
      const active = ((_b2 = (_a2 = playerState == null ? void 0 : playerState.uiState.phone) == null ? void 0 : _a2.activeCall) == null ? void 0 : _b2.phase) === "active" && (playerState == null ? void 0 : playerState.mode) === "playing";
      if (!active) return;
      const id = window.setInterval(() => patchActiveCall((ac) => ac.phase === "active" ? { elapsedMs: ac.elapsedMs + 1e3 } : {}), 1e3);
      return () => window.clearInterval(id);
    }, [(_d = (_c = playerState == null ? void 0 : playerState.uiState.phone) == null ? void 0 : _c.activeCall) == null ? void 0 : _d.phase, playerState == null ? void 0 : playerState.mode]);
    const notifEntry = (ph, e) => {
      const list = ph.notifications || [];
      return [...list, { ...e, id: \`nt-\${Date.now()}-\${list.length}\`, order: list.length }];
    };
    const startPhoneNotify = (cmd) => {
      const ding = cmd.soundId ?? project.ui.phoneNotifSoundId ?? null;
      const silent = cmd.presentation === "silent";
      updatePlayerState((p) => {
        if (!p) return null;
        const ph = p.uiState.phone || { open: false, messages: [] };
        return { ...p, uiState: { ...p.uiState, phone: {
          ...ph,
          notifications: notifEntry(ph, { title: cmd.title, text: cmd.text, icon: cmd.icon, iconImage: cmd.iconImage, tapActions: cmd.tapActions }),
          ...silent ? {} : { notification: { text: cmd.text, title: cmd.title, icon: cmd.icon, iconImage: cmd.iconImage, tapActions: cmd.tapActions, visible: true } },
          unread: cmd.showBadge !== false ? true : ph.unread
        } } };
      });
      if (ding && !silent) playSound(ding, void 0, false);
      const autoMs = project.ui.phoneNotifAutoMs ?? 6e3;
      if (!silent && autoMs > 0) pushPhoneTimer(() => updatePlayerState((p) => {
        var _a2;
        return p && ((_a2 = p.uiState.phone) == null ? void 0 : _a2.notification) ? { ...p, uiState: { ...p.uiState, phone: { ...p.uiState.phone, notification: { ...p.uiState.phone.notification, visible: false } } } } : p;
      }), autoMs);
    };
    const startIncomingText = (cmd) => {
      const presentation = cmd.presentation || "notify";
      const replies = cmd.replies || [];
      const hasReplies = replies.length > 0;
      const msg = { id: \`it-\${Date.now()}\`, senderId: cmd.senderId, text: cmd.text, ...cmd.portrait ? { portrait: cmd.portrait } : {}, ...cmd.image ? { image: cmd.image } : {}, ...cmd.senderId !== "player" ? { contactId: cmd.senderId } : {} };
      const ding = cmd.soundId ?? project.ui.phoneNotifSoundId ?? null;
      if (presentation === "notify") {
        updatePlayerState((p) => {
          if (!p) return null;
          const ph = p.uiState.phone || { open: false, messages: [] };
          return { ...p, uiState: { ...p.uiState, phone: { ...ph, messages: [...ph.messages, msg], cameraRoll: collectToCameraRoll(ph.cameraRoll, msg), notification: { senderId: cmd.senderId, text: cmd.text || (cmd.image ? "📷 Photo" : ""), portrait: cmd.portrait, visible: true }, notifications: notifEntry(ph, { text: cmd.text || (cmd.image ? "📷 Photo" : ""), senderId: cmd.senderId, portrait: cmd.portrait }), unread: cmd.showBadge !== false ? true : ph.unread, waiting: false, pendingReplies: hasReplies ? replies : ph.pendingReplies } } };
        });
        if (ding) playSound(ding, void 0, false);
        const autoMs = project.ui.phoneNotifAutoMs ?? 6e3;
        if (autoMs > 0) pushPhoneTimer(() => updatePlayerState((p) => {
          var _a2;
          return p && ((_a2 = p.uiState.phone) == null ? void 0 : _a2.notification) ? { ...p, uiState: { ...p.uiState, phone: { ...p.uiState.phone, notification: { ...p.uiState.phone.notification, visible: false } } } } : p;
        }), autoMs);
      } else {
        const threadFocus = cmd.senderId !== "player" ? cmd.senderId : void 0;
        const landMessage = () => {
          updatePlayerState((p) => {
            if (!p) return null;
            const ph = p.uiState.phone || { open: false, messages: [] };
            const nextMessages = [...ph.messages, msg];
            const key = phoneThreadKey(msg);
            return { ...p, uiState: { ...p.uiState, phone: { ...ph, open: true, view: "chat", ...threadFocus ? { activeContactId: threadFocus } : {}, messages: nextMessages, cameraRoll: collectToCameraRoll(ph.cameraRoll, msg), threadLastRead: { ...ph.threadLastRead || {}, [key]: countPhoneThread(nextMessages, key) }, waiting: hasReplies, pendingReplies: hasReplies ? replies : void 0, notification: null, typing: null } } };
          });
          if (ding) playSound(ding, void 0, false);
        };
        const typingMs = Math.max(0, cmd.typingMs ?? 0);
        if (typingMs > 0) {
          updatePlayerState((p) => {
            if (!p) return null;
            const ph = p.uiState.phone || { open: false, messages: [] };
            return { ...p, uiState: { ...p.uiState, phone: { ...ph, open: true, view: "chat", ...threadFocus ? { activeContactId: threadFocus } : {}, notification: null, typing: { senderId: cmd.senderId } } } };
          });
          pushPhoneTimer(landMessage, typingMs);
        } else {
          landMessage();
        }
      }
    };
    const startIncomingCall = (cmd) => {
      clearPhoneTimers();
      if (callTimeoutRef.current) {
        clearTimeout(callTimeoutRef.current);
        callTimeoutRef.current = null;
      }
      activeCallCmdRef.current = cmd;
      const modal = (cmd.mode || "modal") === "modal";
      const caller = project.characters[cmd.callerId];
      const ringtone = cmd.ringtoneId ?? (caller == null ? void 0 : caller.phoneRingtoneAudioId) ?? project.ui.phoneCallRingtoneId ?? null;
      if (ringtone) callRingAudioRef.current = playSound(ringtone, void 0, true);
      updatePlayerState((p) => {
        if (!p) return null;
        const ph = p.uiState.phone || { open: false, messages: [] };
        return { ...p, uiState: { ...p.uiState, phone: { ...ph, incomingCall: { callerId: cmd.callerId, portrait: cmd.portrait, phase: "ringing", modal, cmd } } } };
      });
      const dur = cmd.ringDurationMs ?? 12e3;
      callRingRemainingRef.current = dur;
      callRingStartRef.current = Date.now();
      callTimeoutRef.current = window.setTimeout(() => resolveIncomingCall("missed"), dur);
    };
    const rearmIncomingCall = (cmd) => {
      if (callTimeoutRef.current) {
        clearTimeout(callTimeoutRef.current);
        callTimeoutRef.current = null;
      }
      activeCallCmdRef.current = cmd;
      const caller = project.characters[cmd.callerId];
      const ringtone = cmd.ringtoneId ?? (caller == null ? void 0 : caller.phoneRingtoneAudioId) ?? project.ui.phoneCallRingtoneId ?? null;
      if (ringtone) callRingAudioRef.current = playSound(ringtone, void 0, true);
      const dur = cmd.ringDurationMs ?? 12e3;
      callRingRemainingRef.current = dur;
      callRingStartRef.current = Date.now();
      callTimeoutRef.current = window.setTimeout(() => resolveIncomingCall("missed"), dur);
    };
    const stopRingtone = () => {
      if (callTimeoutRef.current) {
        clearTimeout(callTimeoutRef.current);
        callTimeoutRef.current = null;
      }
      const cmd = activeCallCmdRef.current;
      const caller = cmd ? project.characters[cmd.callerId] : null;
      const ringtone = cmd ? cmd.ringtoneId ?? (caller == null ? void 0 : caller.phoneRingtoneAudioId) ?? project.ui.phoneCallRingtoneId ?? null : null;
      if (ringtone) stopSfx(ringtone);
      callRingAudioRef.current = null;
    };
    const resolveIncomingCall = (outcome) => {
      var _a2, _b2;
      const cmd = activeCallCmdRef.current;
      if (!cmd) return;
      stopRingtone();
      activeCallCmdRef.current = null;
      const hasConversation = outcome === "accepted" && !!((_b2 = (_a2 = cmd.conversation) == null ? void 0 : _a2.lines) == null ? void 0 : _b2.length);
      updatePlayerState((p) => {
        var _a3;
        if (!p) return null;
        const ph = p.uiState.phone || { open: false, messages: [] };
        const log2 = ph.callLog || [];
        const entry = { id: \`call-\${Date.now()}\`, callerId: cmd.callerId, status: outcome, portrait: cmd.portrait, order: log2.length, direction: "incoming" };
        const missed = outcome === "missed";
        const callerName = cmd.callerId === "player" ? "" : ((_a3 = project.characters[cmd.callerId]) == null ? void 0 : _a3.name) || "";
        return { ...p, uiState: { ...p.uiState, phone: {
          ...ph,
          incomingCall: null,
          ...hasConversation ? {} : { callLog: [...log2, entry] },
          // A missed call now leaves a trace: a banner + a notification-list row.
          ...missed ? {
            notification: { senderId: cmd.callerId, title: "Missed call", text: callerName || "Unknown caller", portrait: cmd.portrait, icon: "call", visible: true },
            notifications: notifEntry(ph, { title: "Missed call", text: callerName || "Unknown caller", senderId: cmd.callerId, portrait: cmd.portrait, icon: "call" })
          } : {},
          unread: missed && cmd.showBadge !== false ? true : ph.unread
        } } };
      });
      const acts = (outcome === "accepted" ? cmd.acceptActions : outcome === "declined" ? cmd.declineActions : cmd.onTimeout === "runActions" ? cmd.timeoutActions : []) || [];
      acts.forEach((a) => handleUIAction(a));
      const navigates = phoneNavigates(acts);
      if (hasConversation) {
        beginActiveCall({ contactId: cmd.callerId, direction: "incoming", portrait: cmd.portrait, conversation: cmd.conversation, blocking: (cmd.mode || "modal") === "modal" && !navigates });
        return;
      }
      if ((cmd.mode || "modal") === "modal" && !navigates) updatePlayerState((p) => p ? { ...p, currentIndex: p.currentIndex + 1, uiState: { ...p.uiState, isWaitingForInput: false } } : null);
    };
    React2.useEffect(() => {
      var _a2, _b2, _c2, _d2;
      const ringing = ((_b2 = (_a2 = playerState == null ? void 0 : playerState.uiState.phone) == null ? void 0 : _a2.incomingCall) == null ? void 0 : _b2.phase) === "ringing";
      if ((playerState == null ? void 0 : playerState.mode) !== "playing") {
        try {
          (_c2 = callRingAudioRef.current) == null ? void 0 : _c2.pause();
        } catch {
        }
        if (ringing && callTimeoutRef.current) {
          clearTimeout(callTimeoutRef.current);
          callTimeoutRef.current = null;
          callRingRemainingRef.current = Math.max(0, callRingRemainingRef.current - (Date.now() - callRingStartRef.current));
        }
      } else if (ringing) {
        if ((_d2 = callRingAudioRef.current) == null ? void 0 : _d2.paused) callRingAudioRef.current.play().catch(() => {
        });
        if (callTimeoutRef.current === null) {
          callRingStartRef.current = Date.now();
          callTimeoutRef.current = window.setTimeout(() => resolveIncomingCall("missed"), callRingRemainingRef.current);
        }
      }
    }, [playerState == null ? void 0 : playerState.mode, (_f = (_e = playerState == null ? void 0 : playerState.uiState.phone) == null ? void 0 : _e.incomingCall) == null ? void 0 : _f.phase]);
    const handleVariableChange = (variableId, value) => {
      runtimeDebugLog("[handleVariableChange] Called with:", { variableId, value, hasPlayerState: !!playerState });
      if (playerState) {
        runtimeDebugLog("[handleVariableChange] Updating uiVariables");
        uiDirtyVariableIdsRef.current.add(variableId);
        runtimeDebugLog("[handleVariableChange] ✓ Added to dirty set. Size now:", uiDirtyVariableIdsRef.current.size, "IDs:", Array.from(uiDirtyVariableIdsRef.current));
        setUiVariables((prev) => {
          const newVars = {
            ...prev,
            [variableId]: value
          };
          runtimeDebugLog("[handleVariableChange] uiVariables BEFORE:", JSON.stringify(prev, null, 2));
          runtimeDebugLog("[handleVariableChange] uiVariables AFTER:", JSON.stringify(newVars, null, 2));
          uiVariablesRef.current = newVars;
          return newVars;
        });
      } else {
        runtimeDebugLog("[handleVariableChange] Updating menuVariables");
        setMenuVariables((prev) => {
          const newVars = {
            ...prev,
            [variableId]: value
          };
          runtimeDebugLog("[handleVariableChange] New menuVariables:", JSON.stringify(newVars, null, 2));
          return newVars;
        });
      }
    };
    const setTestVariable = React2.useCallback((id, value) => {
      if (playerStateRef.current) {
        updatePlayerState((p) => p ? { ...p, variables: { ...p.variables, [id]: value } } : p);
        if (uiDirtyVariableIdsRef.current.has(id)) {
          setUiVariables((prev) => {
            const n = { ...prev, [id]: value };
            uiVariablesRef.current = n;
            return n;
          });
        }
      } else {
        setMenuVariables((prev) => ({ ...prev, [id]: value }));
      }
    }, [updatePlayerState]);
    const mergeDirtyUiVariables = React2.useCallback((base) => {
      const dirtyIds = uiDirtyVariableIdsRef.current;
      if (dirtyIds.size === 0) {
        return base;
      }
      const sourceVariables = uiVariablesRef.current;
      const merged = { ...base };
      dirtyIds.forEach((id) => {
        if (Object.prototype.hasOwnProperty.call(sourceVariables, id)) {
          merged[id] = sourceVariables[id];
        }
      });
      return merged;
    }, []);
    const commitUiVariablesToPlayerState = React2.useCallback(() => {
      if (uiDirtyVariableIdsRef.current.size === 0) {
        runtimeDebugLog("[commitUiVariables] No dirty variables to commit");
        return;
      }
      runtimeDebugLog("[commitUiVariables] Committing dirty variables:", Array.from(uiDirtyVariableIdsRef.current));
      runtimeDebugLog("[commitUiVariables] uiVariables snapshot:", JSON.stringify(uiVariablesRef.current, null, 2));
      reactDom.flushSync(() => {
        updatePlayerState((p) => {
          if (!p) {
            runtimeDebugLog("[commitUiVariables] No playerState, skipping commit");
            return null;
          }
          runtimeDebugLog("[commitUiVariables] BEFORE merge - playerState.variables:", JSON.stringify(p.variables, null, 2));
          const mergedVariables = mergeDirtyUiVariables(p.variables);
          runtimeDebugLog("[commitUiVariables] AFTER merge - merged variables:", JSON.stringify(mergedVariables, null, 2));
          return {
            ...p,
            variables: mergedVariables
          };
        });
      });
      runtimeDebugLog("[commitUiVariables] Clearing dirty set after successful commit");
      uiDirtyVariableIdsRef.current.clear();
    }, [mergeDirtyUiVariables]);
    const screenVariables = React2.useMemo(() => {
      if (!playerState) return menuVariables;
      const base = { ...playerState.variables };
      uiDirtyVariableIdsRef.current.forEach((id) => {
        if (Object.prototype.hasOwnProperty.call(uiVariablesRef.current, id)) {
          base[id] = uiVariablesRef.current[id];
        }
      });
      return base;
    }, [playerState, playerState == null ? void 0 : playerState.variables, menuVariables, uiVariables]);
    React2.useEffect(() => {
      if (!playerState) return;
      liveSfxRef.current.forEach((entry) => {
        const met = !entry.conditions || entry.conditions.length === 0 || evaluateConditions2(entry.conditions, screenVariables);
        if (entry.loop) {
          if (met && !entry.audio) {
            entry.audio = playSound(entry.audioId, entry.volume, true) || null;
          } else if (!met && entry.audio) {
            try {
              entry.audio.pause();
              entry.audio.currentTime = 0;
              entry.audio.src = "";
            } catch (e) {
            }
            sfxPoolRef.current = sfxPoolRef.current.filter((e) => e.audio !== entry.audio);
            entry.audio = null;
          }
        } else if (met && !entry.lastMet) {
          playSound(entry.audioId, entry.volume, false);
        }
        entry.lastMet = met;
      });
    }, [screenVariables, liveSfxTick, evaluateConditions2, playSound, playerState]);
    React2.useEffect(() => {
      return () => {
        liveSfxRef.current.forEach((entry) => {
          if (entry.audio) {
            try {
              entry.audio.pause();
              entry.audio.src = "";
            } catch (e) {
            }
          }
        });
        liveSfxRef.current.clear();
      };
    }, [playerState == null ? void 0 : playerState.currentSceneId]);
    const handleUIActionRef = React2.useRef(handleUIAction);
    handleUIActionRef.current = handleUIAction;
    React2.useEffect(() => {
      const TICK_MS = 250;
      const id = window.setInterval(() => {
        const timers = timersRef.current;
        if (timers.size === 0) return;
        const varWrites = {};
        const toRun = [];
        const toRemove = [];
        const resumes = [];
        timers.forEach((t, key) => {
          var _a2;
          t.accMs += TICK_MS;
          const stepMs = Math.max(0.05, t.intervalSec) * 1e3;
          while (t.accMs >= stepMs) {
            t.accMs -= stepMs;
            t.value += t.mode === "countdown" ? -1 : 1;
            const finished = t.mode === "countdown" ? t.value <= t.target : t.target > 0 && t.value >= t.target;
            if (finished) {
              t.value = t.target;
              if (t.variableId) varWrites[t.variableId] = t.value;
              if (t.onComplete && t.onComplete.length) toRun.push(...t.onComplete);
              if (t.loop) {
                t.value = t.resetTo;
                if (t.variableId) varWrites[t.variableId] = t.value;
                t.accMs = 0;
              } else {
                toRemove.push(key);
                if (((_a2 = blockingTimerRef.current) == null ? void 0 : _a2.key) === key) {
                  resumes.push(blockingTimerRef.current.resume);
                  blockingTimerRef.current = null;
                }
                break;
              }
            } else {
              if (t.variableId) varWrites[t.variableId] = t.value;
            }
          }
        });
        toRemove.forEach((k) => timers.delete(k));
        if (Object.keys(varWrites).length) {
          updatePlayerState((p) => p ? { ...p, variables: { ...p.variables, ...varWrites } } : null);
        }
        if (toRun.length) {
          const run = handleUIActionRef.current;
          if (run) toRun.forEach((a) => {
            try {
              run(a);
            } catch {
            }
          });
        }
        resumes.forEach((r) => {
          try {
            r();
          } catch {
          }
        });
      }, TICK_MS);
      return () => window.clearInterval(id);
    }, [updatePlayerState]);
    React2.useEffect(() => {
      if (blockingTimerRef.current) {
        timersRef.current.delete(blockingTimerRef.current.key);
        blockingTimerRef.current = null;
      }
    }, [playerState == null ? void 0 : playerState.currentSceneId]);
    React2.useEffect(() => {
      const dn = project.dayNightCycle;
      const aa = dn == null ? void 0 : dn.autoAdvance;
      const timeVarId = dn == null ? void 0 : dn.timeVariableId;
      if (!(dn == null ? void 0 : dn.enabled) || !(aa == null ? void 0 : aa.enabled) || !timeVarId || !(aa.secondsPerHour > 0)) return;
      const TICK_MS = 250;
      const hoursPerTick = TICK_MS / 1e3 / aa.secondsPerHour;
      const id = window.setInterval(() => {
        updatePlayerState((p) => {
          if (!p || p.mode !== "playing") return p;
          const cur = Number(p.variables[timeVarId] ?? 0);
          const next = Math.round(((cur + hoursPerTick) % 24 + 24) % 24 * 1e3) / 1e3;
          return { ...p, variables: { ...p.variables, [timeVarId]: next } };
        });
      }, TICK_MS);
      return () => window.clearInterval(id);
    }, [project.dayNightCycle, updatePlayerState]);
    React2.useEffect(() => {
      var _a2;
      const sceneId = playerState == null ? void 0 : playerState.currentSceneId;
      const dn = project.dayNightCycle;
      const sdn = sceneId ? (_a2 = project.scenes[sceneId]) == null ? void 0 : _a2.dayNight : void 0;
      if ((dn == null ? void 0 : dn.enabled) && dn.timeVariableId && (sdn == null ? void 0 : sdn.mode) === "fixed" && typeof sdn.fixedHour === "number") {
        const tv = dn.timeVariableId, fh = sdn.fixedHour;
        updatePlayerState((p) => p && Number(p.variables[tv] ?? -1) !== fh ? { ...p, variables: { ...p.variables, [tv]: fh } } : p);
      }
    }, [playerState == null ? void 0 : playerState.currentSceneId, project.dayNightCycle, project.scenes, updatePlayerState]);
    React2.useEffect(() => {
      const handleKeyDown = (e) => {
        var _a2, _b2, _c2, _d2;
        if (!playerState) return;
        if ((e.key === " " || e.key === "Enter") && playerState.mode === "playing" && playerState.uiState.dialogue && !playerState.uiState.choices && !playerState.uiState.textInput) {
          e.preventDefault();
          const d = playerState.uiState.dialogue;
          if (!(d.timeLimitLocked && (d.timeLimit ?? 0) > 0)) handleDialogueAdvance();
          return;
        }
        if ((e.key === "h" || e.key === "H") && playerState.mode === "playing" && !playerState.uiState.textInput) {
          e.preventDefault();
          updatePlayerState((p) => p ? { ...p, uiState: { ...p.uiState, showHistory: !p.uiState.showHistory } } : null);
          return;
        }
        if (e.key === "Control" && playerState.mode === "playing" && !playerState.uiState.showHistory && settings.enableSkip) {
          e.preventDefault();
          updatePlayerState((p) => p ? { ...p, uiState: { ...p.uiState, isSkipping: !p.uiState.isSkipping } } : null);
          return;
        }
        if ((e.key === "ArrowUp" || e.key === "PageUp") && playerState.mode === "playing" && !playerState.uiState.showHistory && !playerState.uiState.choices && !playerState.uiState.textInput) {
          e.preventDefault();
          handleSkipBackward();
          return;
        }
        if (playerState.mode === "playing" && !playerState.uiState.textInput && !playerState.uiState.showHistory && !e.ctrlKey && !e.altKey && !e.metaKey) {
          const ae = document.activeElement;
          const typing = !!ae && (ae.tagName === "INPUT" || ae.tagName === "TEXTAREA" || ae.isContentEditable);
          if (!typing) {
            const pressed = e.key.toLowerCase();
            if (project.ui.phoneOpenHotkey && project.ui.phoneOpenHotkey.toLowerCase() === pressed) {
              e.preventDefault();
              const wasOpen = !!((_b2 = (_a2 = playerStateRef.current) == null ? void 0 : _a2.uiState.phone) == null ? void 0 : _b2.open);
              const opening = !wasOpen;
              const snd = opening ? project.ui.phoneOpenSoundId : project.ui.phoneCloseSoundId;
              if (snd) playSound(snd, void 0, false);
              updatePlayerState((p) => {
                if (!p) return null;
                const ph = p.uiState.phone || { open: false, messages: [] };
                return { ...p, uiState: { ...p.uiState, phone: { ...ph, open: opening, ...opening ? { view: "home", notification: null, unread: false } : { waiting: false, pendingChoices: void 0, notification: null } } } };
              });
              if (!opening && project.ui.phoneOnCloseBehavior === "advance" && ((_c2 = playerStateRef.current) == null ? void 0 : _c2.mode) === "playing") {
                const d0 = (_d2 = playerStateRef.current) == null ? void 0 : _d2.uiState.dialogue;
                if (!((d0 == null ? void 0 : d0.timeLimitLocked) && (d0.timeLimit ?? 0) > 0)) {
                  updatePlayerState((p) => p ? { ...p, currentIndex: p.currentIndex + 1, uiState: { ...p.uiState, isWaitingForInput: false, dialogue: null, isSkipping: false } } : null);
                }
              }
              return;
            }
            const target = Object.values(project.uiScreens).find((s) => !!s.openHotkey && s.openHotkey.toLowerCase() === pressed);
            if (target) {
              e.preventDefault();
              handleUIActionRef.current({ type: UIActionType.ToggleScreen, targetScreenId: target.id });
              return;
            }
          }
        }
        if (e.key === "Escape") {
          if (playerState.uiState.showHistory) {
            updatePlayerState((p) => p ? { ...p, uiState: { ...p.uiState, showHistory: false } } : null);
            return;
          }
          if (playerState.mode === "playing") {
            updatePlayerState((p) => p ? { ...p, mode: "paused" } : null);
            if (musicAudioRef.current && !musicAudioRef.current.paused) {
              musicAudioRef.current.pause();
            }
            if (project.ui.pauseScreenId) {
              setScreenStack([project.ui.pauseScreenId]);
            }
          } else if (playerState.mode === "paused") {
            if (screenStack.length > 1) {
              setScreenStack((s) => s.slice(0, -1));
            } else {
              updatePlayerState((p) => p ? { ...p, mode: "playing" } : null);
              if (musicAudioRef.current && musicAudioRef.current.src && playerState.musicState.isPlaying) {
                musicAudioRef.current.play().catch((e2) => console.error("Failed to resume music:", e2));
              }
              setScreenStack([]);
            }
          }
        }
      };
      window.addEventListener("keydown", handleKeyDown);
      return () => window.removeEventListener("keydown", handleKeyDown);
    }, [playerState, project.ui.pauseScreenId, project.uiScreens, screenStack, handleDialogueAdvance, handleSkipBackward, settings.enableSkip]);
    React2.useEffect(() => {
      if (!settings.autoAdvance || !playerState || playerState.mode !== "playing" || scenePaused) return;
      if (!playerState.uiState.dialogue || playerState.uiState.choices || playerState.uiState.textInput) return;
      if ((playerState.uiState.dialogue.timeLimit ?? 0) > 0) return;
      const voice = currentVoiceRef.current;
      if (voice && !voice.ended) {
        let advanced = false;
        let graceTimer = 0;
        const doAdvance = () => {
          if (advanced) return;
          advanced = true;
          handleDialogueAdvance();
        };
        const onEnded = () => {
          graceTimer = window.setTimeout(doAdvance, 400);
        };
        voice.addEventListener("ended", onEnded, { once: true });
        const fallbackMs = (Number.isFinite(voice.duration) && voice.duration > 0 ? voice.duration : settings.autoAdvanceDelay) * 1e3 + 2e3;
        const fallback = window.setTimeout(doAdvance, fallbackMs);
        return () => {
          voice.removeEventListener("ended", onEnded);
          clearTimeout(fallback);
          if (graceTimer) clearTimeout(graceTimer);
        };
      }
      const timer = window.setTimeout(() => {
        handleDialogueAdvance();
      }, settings.autoAdvanceDelay * 1e3);
      return () => clearTimeout(timer);
    }, [settings.autoAdvance, settings.autoAdvanceDelay, playerState == null ? void 0 : playerState.uiState.dialogue, playerState == null ? void 0 : playerState.uiState.choices, playerState == null ? void 0 : playerState.uiState.textInput, playerState == null ? void 0 : playerState.mode, scenePaused, handleDialogueAdvance]);
    React2.useEffect(() => {
      var _a2;
      if (!playerState || playerState.mode !== "playing" || !playerState.uiState.isSkipping || scenePaused) return;
      if (!settings.enableSkip) {
        updatePlayerState((p) => p ? { ...p, uiState: { ...p.uiState, isSkipping: false } } : null);
        return;
      }
      if (playerState.uiState.choices || playerState.uiState.textInput) {
        updatePlayerState((p) => p ? { ...p, uiState: { ...p.uiState, isSkipping: false } } : null);
        return;
      }
      if (((_a2 = playerState.uiState.dialogue) == null ? void 0 : _a2.timeLimitLocked) && (playerState.uiState.dialogue.timeLimit ?? 0) > 0) {
        updatePlayerState((p) => p ? { ...p, uiState: { ...p.uiState, isSkipping: false } } : null);
        return;
      }
      if (playerState.uiState.dialogue) {
        const timer = setTimeout(() => {
          handleDialogueAdvance();
        }, 50);
        return () => clearTimeout(timer);
      }
    }, [playerState == null ? void 0 : playerState.uiState.isSkipping, playerState == null ? void 0 : playerState.uiState.dialogue, playerState == null ? void 0 : playerState.uiState.choices, playerState == null ? void 0 : playerState.uiState.textInput, playerState == null ? void 0 : playerState.mode, scenePaused, settings.enableSkip, handleDialogueAdvance]);
    const renderStage = () => {
      var _a2, _b2, _c2, _d2;
      if (!playerState) return null;
      const state = playerState.stageState;
      const liveVars = mergeDirtyUiVariables(playerState.variables);
      const matchedBgLayer = (state.backgroundLayers || []).filter((l) => !l.conditions || evaluateConditions2(l.conditions, liveVars)).slice(-1)[0];
      const effBgUrl = matchedBgLayer ? matchedBgLayer.url : state.backgroundUrl;
      const effBgColor = matchedBgLayer ? matchedBgLayer.color : state.backgroundColor;
      const effBgIsVideo = matchedBgLayer ? matchedBgLayer.isVideo : state.backgroundIsVideo;
      const effBgLoop = matchedBgLayer ? matchedBgLayer.loop : state.backgroundLoop;
      const effBgTrimStart = matchedBgLayer ? matchedBgLayer.trimStart : state.backgroundTrimStart;
      const effBgTrimEnd = matchedBgLayer ? matchedBgLayer.trimEnd : state.backgroundTrimEnd;
      const effBgDepth = (matchedBgLayer ? matchedBgLayer.parallaxDepth : state.backgroundParallaxDepth) ?? 0;
      const sceneParallaxIntensity = ((_b2 = (_a2 = project.scenes[playerState.currentSceneId]) == null ? void 0 : _a2.parallax) == null ? void 0 : _b2.intensity) ?? 1;
      const bgParallaxScale = (depth) => {
        if (!depth) return 1;
        const maxShiftPx = depth * PARALLAX_MAX_PX * Math.max(1, sceneParallaxIntensity) * 1.1;
        const minDim = Math.min((stageSize == null ? void 0 : stageSize.width) || 1280, (stageSize == null ? void 0 : stageSize.height) || 720);
        return 1 + 2 * maxShiftPx / minDim;
      };
      const getPositionStyle2 = (position) => {
        if (typeof position === "object") {
          return {
            left: \`\${position.x}%\`,
            top: \`\${position.y}%\`
          };
        } else {
          const presetStyles = {
            "left": { top: "10%", left: "25%" },
            "center": { top: "10%", left: "50%" },
            "right": { top: "10%", left: "75%" },
            "off-left": { top: "10%", left: "-25%" },
            "off-right": { top: "10%", left: "125%" }
          };
          return presetStyles[position];
        }
      };
      const shakeClass = activeShakeRef.current ? "shake" : "";
      const intensityPx = activeShakeRef.current ? activeShakeRef.current.intensity * 1.5 : 0;
      const screenTween = TweenManager.getCurrentValues("__screen__", "screen");
      const tweenedZoom = (screenTween == null ? void 0 : screenTween.scaleX) ?? state.screen.zoom;
      const tweenedPanX = (screenTween == null ? void 0 : screenTween.x) ?? state.screen.panX;
      const tweenedPanY = (screenTween == null ? void 0 : screenTween.y) ?? state.screen.panY;
      const panZoomStyle = { transform: \`scale(\${tweenedZoom}) translate(\${tweenedPanX}%, \${tweenedPanY}%)\`, transition: screenTween ? "none" : \`transform \${state.screen.transitionDuration}s ease-in-out\`, width: "100%", height: "100%" };
      const shakeIntensityStyle = activeShakeRef.current ? { "--shake-intensity-x": \`\${intensityPx}px\`, "--shake-intensity-y": \`\${intensityPx * 0.7}px\` } : {};
      const tintStyle = { backgroundColor: state.screen.tint, opacity: resolveVarNumber(liveVars, state.screen.tintOpacityVariableId, state.screen.tintOpacity ?? 100, { min: 0, max: 100 }) / 100, transition: \`background-color \${state.screen.transitionDuration}s ease-in-out, opacity \${state.screen.transitionDuration}s ease-in-out\` };
      const dnc = project.dayNightCycle;
      const sceneDN = (_c2 = project.scenes[playerState.currentSceneId]) == null ? void 0 : _c2.dayNight;
      const dnActive = !!((dnc == null ? void 0 : dnc.enabled) && ((_d2 = dnc.phases) == null ? void 0 : _d2.length) && (sceneDN == null ? void 0 : sceneDN.mode) !== "off");
      const dnHour = (sceneDN == null ? void 0 : sceneDN.mode) === "fixed" ? sceneDN.fixedHour ?? 12 : (dnc == null ? void 0 : dnc.timeVariableId) ? Number((liveVars == null ? void 0 : liveVars[dnc.timeVariableId]) ?? 0) : 0;
      const dnGrade = dnActive ? computeGrade(dnHour, dnc.phases) : null;
      const dnBg = dnGrade ? gradeToBackgroundStyle(dnGrade.background) : null;
      const dnCharFilter = dnGrade ? gradeToCharacterFilter(dnGrade.sprites) : "";
      const dnSpriteTint = dnGrade ? gradeToSpriteTint(dnGrade.sprites) : null;
      const dnTrans = dnTransitionRef.current;
      const dnBgFilterStyle = dnBg ? { filter: dnBg.filter, transition: \`filter \${dnTrans}s ease-in-out\` } : {};
      const handleStageClick = () => {
        const d = playerState.uiState.dialogue;
        if (d && !playerState.uiState.choices && !playerState.uiState.textInput && !playerState.uiState.showHistory) {
          if (d.timeLimitLocked && (d.timeLimit ?? 0) > 0) return;
          handleDialogueAdvance();
        }
      };
      const handleWheel = (e) => {
        if (e.deltaY < 0 && playerState.uiState.dialogue && !playerState.uiState.choices && !playerState.uiState.textInput && !playerState.uiState.showHistory) {
          e.preventDefault();
          handleSkipBackward();
        }
      };
      return /* @__PURE__ */ jsxRuntime2.jsxs(
        "div",
        {
          ref: stageRef,
          className: "w-full h-full relative overflow-hidden bg-black",
          onClick: handleStageClick,
          onWheel: handleWheel,
          style: {
            cursor: playerState.uiState.dialogue && !playerState.uiState.choices && !playerState.uiState.textInput ? "pointer" : "default",
            // Overlay design-reference scale (stageW / 1280) — mirrors the editor's
            // scaleFontSize/scaledBorderRadius so ShowButton/ShowText overlays render
            // identically in the built game and on the scene canvas, at any stage size.
            ["--ovl-scale"]: (stageSize == null ? void 0 : stageSize.width) ? stageSize.width / 1280 : 1
          },
          children: [
            /* @__PURE__ */ jsxRuntime2.jsx("div", { style: panZoomStyle, children: /* @__PURE__ */ jsxRuntime2.jsxs("div", { className: \`w-full h-full \${shakeClass} z-10\`, style: { ...shakeIntensityStyle, backgroundColor: effBgColor }, children: [
              effBgUrl && (() => {
                const bgMedia = effBgIsVideo ? /* @__PURE__ */ jsxRuntime2.jsx(
                  TrimmedVideo,
                  {
                    src: effBgUrl || void 0,
                    autoPlay: true,
                    muted: true,
                    loop: effBgLoop,
                    trimStart: effBgTrimStart,
                    trimEnd: effBgTrimEnd,
                    playsInline: true,
                    className: "absolute w-full h-full object-cover"
                  }
                ) : /* @__PURE__ */ jsxRuntime2.jsx("img", { src: effBgUrl, alt: "background", className: "absolute w-full h-full object-cover" });
                const inner = effBgDepth ? /* @__PURE__ */ jsxRuntime2.jsx("div", { className: "absolute inset-0", style: { transform: \`scale(\${bgParallaxScale(effBgDepth)})\${parallaxTransform(effBgDepth)}\`, transformOrigin: "center" }, children: bgMedia }) : bgMedia;
                return /* @__PURE__ */ jsxRuntime2.jsx("div", { className: "absolute inset-0 overflow-hidden", style: { zIndex: 0, ...dnBgFilterStyle }, children: inner });
              })(),
              playerState == null ? void 0 : playerState.uiState.transitionElement,
              (state.backgroundStack || []).map((plane) => {
                if (!plane.url && !plane.color) return null;
                const planeMedia = plane.url ? plane.isVideo ? /* @__PURE__ */ jsxRuntime2.jsx(TrimmedVideo, { src: plane.url || void 0, autoPlay: true, muted: true, loop: plane.loop, trimStart: plane.trimStart, trimEnd: plane.trimEnd, playsInline: true, className: "absolute w-full h-full object-cover" }) : /* @__PURE__ */ jsxRuntime2.jsx("img", { src: plane.url, alt: "background layer", className: "absolute w-full h-full object-cover" }) : null;
                const d = plane.parallaxDepth ?? 0;
                const planeInner = d ? /* @__PURE__ */ jsxRuntime2.jsx("div", { className: "absolute inset-0", style: { transform: \`scale(\${bgParallaxScale(d)})\${parallaxTransform(d)}\`, transformOrigin: "center" }, children: planeMedia }) : planeMedia;
                const dur = plane.duration ?? 1;
                const anim = (() => {
                  switch (plane.transition) {
                    case void 0:
                    case "":
                    case "instant":
                      return void 0;
                    case "slide":
                      return \`slide-in-right \${dur}s forwards\`;
                    case "iris-in":
                      return \`iris-in \${dur}s forwards\`;
                    case "wipe-right":
                      return \`wipe-right \${dur}s forwards\`;
                    default:
                      return \`dissolve-in \${dur}s forwards\`;
                  }
                })();
                return /* @__PURE__ */ jsxRuntime2.jsx("div", { className: "absolute inset-0 overflow-hidden", style: { zIndex: plane.layer ?? 0, backgroundColor: plane.color, animation: anim, ...dnBgFilterStyle }, children: planeInner }, plane.commandId);
              }),
              dnBg && dnBg.overlayColor !== "transparent" && /* @__PURE__ */ jsxRuntime2.jsx("div", { className: "absolute inset-0 pointer-events-none", style: { zIndex: 3, backgroundColor: dnBg.overlayColor, transition: \`background-color \${dnTrans}s ease-in-out\` } }),
              state.movieOverlays && state.movieOverlays.length > 0 && state.movieOverlays.map((movie, idx) => {
                if (!movie.url) return null;
                const isCustom = movie.objectFit === "custom";
                const movieAnim = movieEntryAnim(movie.transition, movie.transitionDuration);
                const mtw = movie.commandId ? TweenManager.getCurrentValues(movie.commandId, "movie") : null;
                const mX = (mtw == null ? void 0 : mtw.x) ?? movie.x ?? 0;
                const mY = (mtw == null ? void 0 : mtw.y) ?? movie.y ?? 0;
                const mW = (mtw == null ? void 0 : mtw.width) ?? movie.width ?? 100;
                const mH = (mtw == null ? void 0 : mtw.height) ?? movie.height ?? 100;
                const mOpacity = (mtw == null ? void 0 : mtw.opacity) ?? movie.opacity ?? 1;
                const mRot = (mtw == null ? void 0 : mtw.rotation) ?? 0;
                const mSX = (mtw == null ? void 0 : mtw.scaleX) ?? 1;
                const mSY = (mtw == null ? void 0 : mtw.scaleY) ?? 1;
                const mPx = parallaxTransform(movie.parallaxDepth);
                const innerTransform = \`\${mRot || mSX !== 1 || mSY !== 1 ? \`rotate(\${mRot}deg) scale(\${mSX}, \${mSY})\` : ""}\${mPx}\`.trim() || void 0;
                const containerStyle = isCustom ? { position: "absolute", left: \`\${mX}%\`, top: \`\${mY}%\`, width: \`\${mW}%\`, height: \`\${mH}%\`, zIndex: 2, animation: movieAnim } : { position: "absolute", inset: 0, width: "100%", height: "100%", zIndex: 2, animation: movieAnim };
                const videoFit = isCustom ? "contain" : movie.objectFit || "cover";
                const exitDur = movie.transition && movie.transition !== "instant" ? movie.transitionDuration ?? 0.5 : 0;
                const removeOverlay = () => updatePlayerState((p) => {
                  if (!p) return null;
                  const overlays = (p.stageState.movieOverlays || []).filter((o) => o.commandId ? o.commandId !== movie.commandId : o !== movie);
                  return { ...p, stageState: { ...p.stageState, movieOverlays: overlays } };
                });
                const handleOverlayEnded = () => {
                  if (movie.loop || movie.holdLastFrame) return;
                  if (exitDur > 0 && !movie.exiting) {
                    updatePlayerState((p) => {
                      if (!p) return null;
                      const overlays = (p.stageState.movieOverlays || []).map((o) => (o.commandId ? o.commandId === movie.commandId : o === movie) ? { ...o, exiting: true } : o);
                      return { ...p, stageState: { ...p.stageState, movieOverlays: overlays } };
                    });
                    const tid = window.setTimeout(removeOverlay, exitDur * 1e3);
                    activeEffectTimeoutsRef.current.push(tid);
                  } else {
                    removeOverlay();
                  }
                };
                return /* @__PURE__ */ jsxRuntime2.jsx("div", { className: "pointer-events-none", style: containerStyle, children: /* @__PURE__ */ jsxRuntime2.jsx(
                  TrimmedVideo,
                  {
                    src: movie.url,
                    autoPlay: true,
                    muted: true,
                    loop: movie.loop,
                    playsInline: true,
                    trimStart: movie.trimStart,
                    trimEnd: movie.trimEnd,
                    style: { width: "100%", height: "100%", objectFit: videoFit, opacity: movie.exiting ? 0 : mOpacity, transform: innerTransform, transformOrigin: "center", display: "block", transition: movie.exiting ? \`opacity \${exitDur}s ease-out\` : void 0 },
                    onSegmentEnd: handleOverlayEnded,
                    onEnded: handleOverlayEnded
                  }
                ) }, \`movie-overlay-\${idx}-\${movie.url}\`);
              }),
              (() => {
                var _a3;
                const allChars = Object.values(state.characters).filter((c) => !c.live || !c.conditions || evaluateConditions2(c.conditions, liveVars));
                const arranged = project.autoArrangeCharacters ? computeArrangedPositions(allChars.filter((c) => !c.charId.startsWith("__ghost")).map((c) => ({ id: c.charId, position: c.position }))) : null;
                const emphasisOn = !!project.ui.speakerEmphasisEnabled;
                const emphasisSpeakerId = emphasisOn ? ((_a3 = playerState.uiState.dialogue) == null ? void 0 : _a3.characterId) ?? null : null;
                const emphasisDim = project.ui.speakerEmphasisDim ?? 0.5;
                const emphasisScale = project.ui.speakerEmphasisScale ?? 1.04;
                return allChars.map((char) => {
                  var _a4, _b3;
                  let transitionClass = "";
                  let animationDuration = "1s";
                  let slideStyle = {};
                  const arrangedX = arranged == null ? void 0 : arranged.get(char.charId);
                  let positionStyle = arrangedX !== void 0 ? { top: "10%", left: \`\${arrangedX}%\` } : getPositionStyle2(char.position);
                  const charTween = TweenManager.getCurrentValues(char.charId, "character");
                  const hasTweenPosition = charTween && (charTween.x !== void 0 || charTween.y !== void 0);
                  if (hasTweenPosition) {
                    const isCustomBase = typeof char.position === "object";
                    const basePos = typeof char.position === "object" ? char.position : { x: arrangedX ?? (char.position === "left" ? 25 : char.position === "right" ? 75 : char.position === "center" ? 50 : char.position === "off-left" ? -25 : 125), y: 10 };
                    positionStyle = {
                      left: \`\${charTween.x ?? basePos.x}%\`,
                      top: \`\${charTween.y ?? basePos.y}%\`,
                      // Match the character's OWN anchor convention so the tween lines up with where
                      // the sprite rests before and after the move: custom {x,y} coords are LEFT-EDGE
                      // anchored (same as the static custom render), presets are CENTER anchored.
                      // Mismatching these (always centering) caused a half-width shift when the move
                      // started and a half-width jump to the final spot when it ended.
                      transform: isCustomBase ? "translate(0, 0)" : "translate(-50%, 0)"
                    };
                  }
                  const isCustomPosition = typeof char.position === "object" || hasTweenPosition;
                  if (!char.transition || char.transition.type !== "slide") {
                    if (!isCustomPosition) {
                      positionStyle = { ...positionStyle, transform: "translate3d(-50%, 0, 0)" };
                    }
                  }
                  if (char.transition) {
                    const isHideTransition = char.transition.action === "hide";
                    switch (char.transition.type) {
                      case "fade":
                        transitionClass = isHideTransition ? "transition-fade-out" : "transition-dissolve";
                        break;
                      case "dissolve":
                        transitionClass = isHideTransition ? "transition-dissolve-out" : "transition-dissolve";
                        break;
                      case "slide":
                        transitionClass = "transition-slide";
                        const startPos = char.transition.startPosition || char.position;
                        const endPos = char.transition.endPosition || char.position;
                        let startOffsetX = 0;
                        let startOffsetY = 0;
                        if (typeof startPos === "object" && typeof endPos === "object") {
                          startOffsetX = startPos.x - endPos.x;
                          startOffsetY = startPos.y - endPos.y;
                        } else {
                          const startPreset = typeof startPos === "string" ? startPos : "center";
                          const endPreset = typeof endPos === "string" ? endPos : "center";
                          const presetCoords = {
                            "left": { x: 25, y: 10 },
                            "center": { x: 50, y: 10 },
                            "right": { x: 75, y: 10 },
                            "off-left": { x: -25, y: 10 },
                            "off-right": { x: 125, y: 10 }
                          };
                          const startCoords = presetCoords[startPreset];
                          const endCoords = presetCoords[endPreset];
                          startOffsetX = startCoords.x - endCoords.x;
                          startOffsetY = startCoords.y - endCoords.y;
                        }
                        if (startOffsetX === 0 && ((_a4 = char.transition) == null ? void 0 : _a4.action) === "show") {
                          let endX = 50;
                          if (typeof endPos === "object") endX = endPos.x;
                          else if (typeof endPos === "string") {
                            const presetMap = { left: 25, center: 50, right: 75, "off-left": -25, "off-right": 125 };
                            endX = presetMap[endPos] ?? 50;
                          }
                          startOffsetX = endX <= 50 ? -60 : 60;
                        }
                        slideStyle = {
                          "--slide-start-x": \`\${startOffsetX}%\`,
                          "--slide-start-y": \`\${startOffsetY}%\`,
                          "--slide-end-x": \`0%\`,
                          "--slide-end-y": \`0%\`
                        };
                        if (stageSize && stageSize.width > 0) {
                          const pxStartX = startOffsetX / 100 * stageSize.width;
                          const pxStartY = startOffsetY / 100 * stageSize.height;
                          slideStyle["--slide-start-px"] = \`\${pxStartX}px\`;
                          slideStyle["--slide-end-px"] = \`0px\`;
                          slideStyle["--slide-start-py"] = \`\${pxStartY}px\`;
                          slideStyle["--slide-end-py"] = \`0px\`;
                        }
                        break;
                      case "iris-in":
                        transitionClass = isHideTransition ? "transition-iris-out" : "transition-iris-in";
                        break;
                      case "wipe-right":
                        transitionClass = isHideTransition ? "transition-wipe-out-right" : "transition-wipe-right";
                        break;
                    }
                    animationDuration = \`\${char.transition.duration}s\`;
                  }
                  const effectsList = char.visualEffects || char.visualEffect ? char.visualEffects && char.visualEffects.length > 0 ? char.visualEffects : char.visualEffect && char.visualEffect.type !== "none" ? [char.visualEffect] : [] : [];
                  const transformEffects = [];
                  let combinedFilter = "";
                  let combinedFilterAnimation = "";
                  let flickerAnimation = "";
                  let filterVars = {};
                  for (const eff of effectsList) {
                    if (!eff || eff.type === "none") continue;
                    const speed = eff.speed ?? 1;
                    const intensity = eff.intensity ?? 1;
                    switch (eff.type) {
                      case "shake": {
                        const s = {};
                        s.animation = \`vnCharShake \${0.15 / speed}s ease-in-out infinite\`;
                        s["--char-shake-px"] = \`\${2 * intensity}px\`;
                        transformEffects.push({ style: s });
                        break;
                      }
                      case "bounce": {
                        const s = {};
                        s.animation = \`vnCharBounce \${0.6 / speed}s ease-in-out infinite\`;
                        s["--char-bounce-h"] = \`\${-8 * intensity}px\`;
                        transformEffects.push({ style: s });
                        break;
                      }
                      case "float": {
                        const s = {};
                        s.animation = \`vnCharFloat \${2 / speed}s ease-in-out infinite\`;
                        s["--char-float-h"] = \`\${-10 * intensity}px\`;
                        transformEffects.push({ style: s });
                        break;
                      }
                      case "pulse": {
                        const s = {};
                        s.animation = \`vnCharPulse \${1 / speed}s ease-in-out infinite\`;
                        s["--char-pulse-scale"] = \`\${1 + 0.05 * intensity}\`;
                        transformEffects.push({ style: s });
                        break;
                      }
                      case "breathing": {
                        const s = {};
                        s.animation = \`vnCharBreathing \${2 / speed}s ease-in-out infinite\`;
                        s["--char-breathe-scale"] = \`\${1 + 0.02 * intensity}\`;
                        transformEffects.push({ style: s });
                        break;
                      }
                      case "glow":
                        combinedFilter += \` drop-shadow(0 0 \${8 * intensity}px \${eff.color || "#FFFFFF"})\`;
                        combinedFilterAnimation = \`vnCharGlow \${1.5 / speed}s ease-in-out infinite\`;
                        filterVars["--char-glow-color"] = eff.color || "#FFFFFF";
                        filterVars["--char-glow-size"] = \`\${8 * intensity}px\`;
                        filterVars["--char-glow-size-max"] = \`\${14 * intensity}px\`;
                        break;
                      case "tint":
                        if (eff.color) {
                          const tintOpacity = 0.3 * intensity;
                          combinedFilter += \` brightness(\${1 - tintOpacity * 0.3}) sepia(\${tintOpacity}) hue-rotate(\${getHueFromHex(eff.color)}deg) saturate(\${1 + intensity})\`;
                        }
                        break;
                      case "silhouette":
                        combinedFilter += \` brightness(0)\${eff.color ? \` drop-shadow(0 0 2px \${eff.color})\` : ""}\`;
                        break;
                      case "flicker":
                        flickerAnimation = \`vnCharFlicker \${0.1 / speed}s step-end infinite\`;
                        break;
                    }
                  }
                  if (dnCharFilter && !char.charId.startsWith("__ghost")) combinedFilter += \` \${dnCharFilter}\`;
                  const contentEffectStyle = {};
                  if (combinedFilter) contentEffectStyle.filter = combinedFilter.trim();
                  if (combinedFilterAnimation) contentEffectStyle.animation = combinedFilterAnimation;
                  if (flickerAnimation) {
                    contentEffectStyle.animation = contentEffectStyle.animation ? \`\${contentEffectStyle.animation}, \${flickerAnimation}\` : flickerAnimation;
                  }
                  Object.assign(contentEffectStyle, filterVars);
                  if (dnSpriteTint && !char.isVideo) contentEffectStyle.isolation = "isolate";
                  const hasContentEffect = combinedFilter || combinedFilterAnimation || flickerAnimation || !!dnSpriteTint && !char.isVideo;
                  const spriteContent = /* @__PURE__ */ jsxRuntime2.jsxs(jsxRuntime2.Fragment, { children: [
                    char.isVideo && char.videoUrls ? char.videoUrls.map((url, index) => {
                      var _a5, _b4, _c3, _d3;
                      return /* @__PURE__ */ jsxRuntime2.jsx(
                        TrimmedVideo,
                        {
                          src: url,
                          autoPlay: true,
                          muted: true,
                          loop: char.videoLoop,
                          trimStart: (_b4 = (_a5 = char.videoTrims) == null ? void 0 : _a5[index]) == null ? void 0 : _b4.start,
                          trimEnd: (_d3 = (_c3 = char.videoTrims) == null ? void 0 : _c3[index]) == null ? void 0 : _d3.end,
                          playsInline: true,
                          className: "absolute top-0 left-0 w-full h-full object-contain",
                          style: { zIndex: index }
                        },
                        index
                      );
                    }) : char.imageUrls.map((url, index) => /* @__PURE__ */ jsxRuntime2.jsx(
                      "img",
                      {
                        src: url,
                        alt: "",
                        className: "absolute top-0 left-0 w-full h-full object-contain",
                        style: { zIndex: index }
                      },
                      index
                    )),
                    dnSpriteTint && !char.isVideo && char.imageUrls.map((url, index) => /* @__PURE__ */ jsxRuntime2.jsx("div", { "aria-hidden": true, style: {
                      position: "absolute",
                      top: 0,
                      left: 0,
                      width: "100%",
                      height: "100%",
                      zIndex: index,
                      backgroundColor: dnSpriteTint.color,
                      opacity: dnSpriteTint.opacity,
                      mixBlendMode: "multiply",
                      pointerEvents: "none",
                      WebkitMaskImage: \`url("\${url}")\`,
                      maskImage: \`url("\${url}")\`,
                      WebkitMaskSize: "contain",
                      maskSize: "contain",
                      WebkitMaskRepeat: "no-repeat",
                      maskRepeat: "no-repeat",
                      WebkitMaskPosition: "center",
                      maskPosition: "center",
                      transition: \`opacity \${dnTrans}s ease-in-out, background-color \${dnTrans}s ease-in-out\`
                    } }, \`dn-tint-\${index}\`))
                  ] });
                  let wrappedContent = hasContentEffect ? /* @__PURE__ */ jsxRuntime2.jsx("div", { className: "w-full h-full relative", style: contentEffectStyle, children: spriteContent }) : spriteContent;
                  for (let tIdx = transformEffects.length - 1; tIdx >= 0; tIdx--) {
                    wrappedContent = /* @__PURE__ */ jsxRuntime2.jsx("div", { className: "w-full h-full relative", style: transformEffects[tIdx].style, children: wrappedContent });
                  }
                  if (emphasisOn) {
                    const isSpeaker = !!emphasisSpeakerId && char.charId === emphasisSpeakerId;
                    const active = !!emphasisSpeakerId;
                    const brightness = active ? isSpeaker ? 1 : emphasisDim : 1;
                    const eScale = active && isSpeaker ? emphasisScale : 1;
                    wrappedContent = /* @__PURE__ */ jsxRuntime2.jsx("div", { className: "w-full h-full relative", style: {
                      filter: brightness !== 1 ? \`brightness(\${brightness})\` : void 0,
                      transform: eScale !== 1 ? \`scale(\${eScale})\` : void 0,
                      transformOrigin: "center bottom",
                      transition: "filter 250ms ease, transform 250ms ease"
                    }, children: wrappedContent });
                  }
                  const charScale = (charTween == null ? void 0 : charTween.scale) ?? char.scale ?? 1;
                  const charOpacity = charTween == null ? void 0 : charTween.opacity;
                  const charInverted = char.inverted ?? false;
                  const charFlipY = char.flipY ?? false;
                  const charRotation = (charTween == null ? void 0 : charTween.rotation) ?? char.rotation ?? 0;
                  let orientTransform = "";
                  if (charRotation) orientTransform = \`rotate(\${charRotation}deg)\`;
                  if (charScale !== 1 || charInverted || charFlipY) {
                    const scaleX = (charInverted ? -1 : 1) * charScale;
                    const scaleY = (charFlipY ? -1 : 1) * charScale;
                    orientTransform = \`\${orientTransform} scale(\${scaleX}, \${scaleY})\`.trim();
                  }
                  orientTransform = orientTransform.trim();
                  const slideOnInner = ((_b3 = char.transition) == null ? void 0 : _b3.type) === "slide" && !!orientTransform;
                  let transformStr = positionStyle.transform || "";
                  if (!slideOnInner && orientTransform) {
                    transformStr = \`\${transformStr} \${orientTransform}\`.trim();
                  }
                  transformStr = (transformStr + parallaxTransform(char.parallaxDepth)).trim();
                  if (slideOnInner) {
                    wrappedContent = /* @__PURE__ */ jsxRuntime2.jsx("div", { className: "w-full h-full relative", style: { transform: orientTransform, transformOrigin: "center bottom" }, children: wrappedContent });
                  }
                  return /* @__PURE__ */ jsxRuntime2.jsx(
                    "div",
                    {
                      className: \`absolute h-[90%] w-auto aspect-[3/4] \${transitionClass} transition-base\`,
                      style: {
                        ...positionStyle,
                        animationDuration,
                        ...slideStyle,
                        zIndex: 5 + (char.layer ?? 0) * 100,
                        ...transformStr ? { transform: transformStr, transformOrigin: "center bottom" } : {},
                        // A running/forwards-filled entrance animation animates opacity and would
                        // override inline opacity, so disable it when a tween controls opacity.
                        ...charOpacity !== void 0 ? { opacity: charOpacity, animationName: "none" } : {}
                      },
                      children: wrappedContent
                    },
                    char.transition ? \`\${char.charId}-\${char.expressionId}-\${char.imageUrls.join(",")}-\${char.transition.action}\` : char.charId
                  );
                });
              })(),
              (() => {
                const pEffects = state.particleEffects;
                const hasParticles = pEffects && Object.keys(pEffects).length > 0;
                if (hasParticles) {
                  const resolvedEffects = Object.fromEntries(Object.entries(pEffects).map(([tag, entry]) => {
                    const vb = entry.varBindings;
                    if (!vb) return [tag, entry];
                    return [tag, { ...entry, config: {
                      ...entry.config,
                      emitRate: resolveVarNumber(liveVars, vb.emitRate, entry.config.emitRate, { min: 0, max: 500 }),
                      wind: resolveVarNumber(liveVars, vb.wind, entry.config.wind, { min: -500, max: 500 }),
                      gravity: resolveVarNumber(liveVars, vb.gravity, entry.config.gravity, { min: -500, max: 500 }),
                      opacity: resolveVarNumber(liveVars, vb.opacity, entry.config.opacity ?? 1, { min: 0, max: 1 })
                    } }];
                  }));
                  return /* @__PURE__ */ jsxRuntime2.jsx(
                    ParticleSystem,
                    {
                      effects: resolvedEffects,
                      width: stageSize.width,
                      height: stageSize.height
                    }
                  );
                }
                return null;
              })(),
              state.lights && state.lights.length > 0 && (() => {
                const lb = state.lightsBrightnessVariableId ? resolveVarNumber(liveVars, state.lightsBrightnessVariableId, 1, { min: 0, max: 2 }) : 1;
                const lights = lb === 1 ? state.lights : state.lights.map((l) => ({ ...l, brightness: (l.brightness ?? 1) * lb }));
                return /* @__PURE__ */ jsxRuntime2.jsx("div", { className: "absolute inset-0 pointer-events-none", style: { zIndex: state.lightsAbove ? 40 : 4 }, children: /* @__PURE__ */ jsxRuntime2.jsx(LightsLayer, { lights, stageW: stageSize.width, stageH: stageSize.height }) });
              })(),
              state.textOverlays.filter((o) => !o.live || !o.conditions || evaluateConditions2(o.conditions, liveVars)).map((overlay) => {
                const liveText = overlay.live && overlay.rawText !== void 0 ? interpolateVariables(overlay.rawText, liveVars, project) : overlay.text;
                return /* @__PURE__ */ jsxRuntime2.jsx(
                  TextOverlayElement,
                  {
                    overlay: liveText !== overlay.text ? { ...overlay, text: liveText } : overlay,
                    stageSize
                  },
                  overlay.id
                );
              }),
              state.imageOverlays.filter((o) => !o.live || !o.conditions || evaluateConditions2(o.conditions, liveVars)).map((overlay) => /* @__PURE__ */ jsxRuntime2.jsx(
                ImageOverlayElement,
                {
                  overlay,
                  stageSize
                },
                overlay.id
              )),
              state.buttonOverlays.filter((o) => !o.live || !o.conditions || evaluateConditions2(o.conditions, liveVars)).map((overlay) => /* @__PURE__ */ jsxRuntime2.jsx(
                ButtonOverlayElement,
                {
                  overlay,
                  onAction: handleUIAction,
                  playSound,
                  onCommitVariables: commitUiVariablesToPlayerState,
                  onItemDrop: handleItemOverlayDrop,
                  onItemDragMove: handleItemDragMove,
                  onItemDragEnd: handleItemDragEnd,
                  onAdvance: overlay.waitForClick ? () => {
                    updatePlayerState((p) => {
                      if (!p) return null;
                      return {
                        ...p,
                        currentIndex: p.currentIndex + 1,
                        uiState: { ...p.uiState, isWaitingForInput: false }
                      };
                    });
                  } : void 0,
                  onPickup: (ov) => {
                    updatePlayerState((p) => {
                      var _a3;
                      if (!p) return null;
                      let variables = p.variables;
                      if (ov.giveItemId) {
                        const item = (_a3 = project.items) == null ? void 0 : _a3[ov.giveItemId];
                        if (item && item.countVariableId) {
                          const countVar = project.variables[item.countVariableId];
                          const min = (countVar == null ? void 0 : countVar.min) ?? 0;
                          const max = countVar == null ? void 0 : countVar.max;
                          const cur = Number(p.variables[item.countVariableId] ?? 0);
                          let next = item.unique ? 1 : cur + (ov.giveQuantity ?? 1);
                          next = Math.max(min, next);
                          if (max !== void 0) next = Math.min(max, next);
                          variables = { ...variables, [item.countVariableId]: next };
                        }
                      }
                      return {
                        ...p,
                        variables,
                        stageState: ov.removeAfterClick ? { ...p.stageState, buttonOverlays: p.stageState.buttonOverlays.filter((b) => b.id !== ov.id) } : p.stageState,
                        pickedUpItems: ov.pickUpOnceId ? [...p.pickedUpItems || [], ov.pickUpOnceId] : p.pickedUpItems
                      };
                    });
                  }
                },
                overlay.id
              )),
              (state.hotSpotOverlays || []).map((overlay) => /* @__PURE__ */ jsxRuntime2.jsx(
                HotSpotOverlayElement,
                {
                  overlay,
                  onAction: handleUIAction,
                  evaluateConditions: evaluateConditions2,
                  variables: liveVars,
                  onAdvance: () => updatePlayerState((p) => p ? { ...p, currentIndex: p.currentIndex + 1, uiState: { ...p.uiState, isWaitingForInput: false } } : null)
                },
                overlay.id
              ))
            ] }) }),
            /* @__PURE__ */ jsxRuntime2.jsx("div", { className: "absolute inset-0 pointer-events-none", style: tintStyle })
          ]
        }
      );
    };
    const CreditScrollContent = ({ command, hasBgs, hasMedia, onFinish }) => {
      const contentRef = React2.useRef(null);
      const containerRef = React2.useRef(null);
      const [animStyle, setAnimStyle] = React2.useState({});
      const finishedRef = React2.useRef(false);
      React2.useEffect(() => {
        finishedRef.current = false;
        const raf = requestAnimationFrame(() => {
          const content = contentRef.current;
          const container = containerRef.current;
          if (!content || !container) return;
          const contentHeight = content.scrollHeight;
          const containerHeight = container.clientHeight;
          const totalDistance = containerHeight + contentHeight;
          const speed = command.scrollSpeed || 60;
          const calcDuration = totalDistance / speed;
          const maxDuration = command.duration || 300;
          const finalDuration = Math.min(calcDuration, maxDuration);
          setAnimStyle({
            animation: \`credit-scroll-dynamic \${finalDuration}s linear forwards\`,
            // Use CSS custom properties for start and end translate values (in pixels)
            ["--credit-scroll-start"]: \`\${containerHeight}px\`,
            ["--credit-scroll-end"]: \`-\${contentHeight}px\`
          });
        });
        return () => cancelAnimationFrame(raf);
      }, [command]);
      const handleAnimEnd = React2.useCallback((e) => {
        if (e.target === e.currentTarget && !finishedRef.current) {
          finishedRef.current = true;
          onFinish();
        }
      }, [onFinish]);
      React2.useEffect(() => {
        command.scrollSpeed || 60;
        const maxDuration = command.duration || 300;
        const timeout = window.setTimeout(() => {
          if (!finishedRef.current) {
            finishedRef.current = true;
            onFinish();
          }
        }, (maxDuration + 5) * 1e3);
        return () => clearTimeout(timeout);
      }, [command, onFinish]);
      return /* @__PURE__ */ jsxRuntime2.jsxs("div", { ref: containerRef, className: "absolute inset-0 overflow-hidden z-[2]", children: [
        /* @__PURE__ */ jsxRuntime2.jsx("style", { children: \`
                    @keyframes credit-scroll-dynamic {
                        from { transform: translateY(var(--credit-scroll-start, 100%)); }
                        to { transform: translateY(var(--credit-scroll-end, -100%)); }
                    }
                \` }),
        /* @__PURE__ */ jsxRuntime2.jsx(
          "div",
          {
            ref: contentRef,
            className: "text-center px-8 w-full",
            style: {
              color: command.textColor || "#FFFFFF",
              textShadow: hasBgs || hasMedia ? "0 2px 8px rgba(0,0,0,0.8), 0 0 20px rgba(0,0,0,0.5)" : "none",
              willChange: "transform",
              ...animStyle
            },
            onAnimationEnd: handleAnimEnd,
            children: command.entries.map(
              (entry, i) => entry.kind === "heading" ? /* @__PURE__ */ jsxRuntime2.jsx("h2", { className: "text-2xl font-bold mt-8 mb-4", style: { color: "#FFD700" }, children: entry.label }, i) : /* @__PURE__ */ jsxRuntime2.jsxs("div", { className: "mb-2", children: [
                /* @__PURE__ */ jsxRuntime2.jsx("span", { className: "text-sm opacity-70", children: entry.label }),
                entry.value && /* @__PURE__ */ jsxRuntime2.jsxs(jsxRuntime2.Fragment, { children: [
                  /* @__PURE__ */ jsxRuntime2.jsx("br", {}),
                  /* @__PURE__ */ jsxRuntime2.jsx("span", { className: "text-lg", children: entry.value })
                ] })
              ] }, i)
            )
          }
        )
      ] });
    };
    const CreditRollOverlay = ({ command, project: project2, assetResolver: assetResolver2, getAssetMetadata: getAssetMetadata2, onFinish }) => {
      const bgs = command.backgrounds || [];
      const mediaItems = command.media || [];
      const hasBgs = bgs.length > 0;
      const hasMedia = mediaItems.length > 0;
      const [bgIndex, setBgIndex] = React2.useState(0);
      const [prevBgIndex, setPrevBgIndex] = React2.useState(null);
      const [transitioning, setTransitioning] = React2.useState(false);
      const bgTimerRef = React2.useRef(null);
      const bgTransTimerRef = React2.useRef(null);
      const [elapsed, setElapsed] = React2.useState(0);
      const startTimeRef = React2.useRef(Date.now());
      React2.useEffect(() => {
        if (!hasMedia) return;
        startTimeRef.current = Date.now();
        const interval = window.setInterval(() => {
          setElapsed((Date.now() - startTimeRef.current) / 1e3);
        }, 200);
        return () => clearInterval(interval);
      }, [hasMedia]);
      React2.useEffect(() => {
        if (!hasBgs || bgs.length <= 1) return;
        const scheduleNext = (idx) => {
          const slide = bgs[idx];
          const displayMs = ((slide == null ? void 0 : slide.displayDuration) || 5) * 1e3;
          bgTimerRef.current = window.setTimeout(() => {
            const nextIdx = (idx + 1) % bgs.length;
            const nextSlide = bgs[nextIdx];
            const transDur2 = ((nextSlide == null ? void 0 : nextSlide.transitionDuration) || 0.5) * 1e3;
            const transType = (nextSlide == null ? void 0 : nextSlide.transition) || "fade";
            if (transType === "instant" || transDur2 === 0) {
              setBgIndex(nextIdx);
              scheduleNext(nextIdx);
            } else {
              setPrevBgIndex(idx);
              setBgIndex(nextIdx);
              setTransitioning(true);
              bgTransTimerRef.current = window.setTimeout(() => {
                setTransitioning(false);
                setPrevBgIndex(null);
                scheduleNext(nextIdx);
              }, transDur2);
            }
          }, displayMs);
        };
        scheduleNext(bgIndex);
        return () => {
          if (bgTimerRef.current) clearTimeout(bgTimerRef.current);
          if (bgTransTimerRef.current) clearTimeout(bgTransTimerRef.current);
        };
      }, [hasBgs, bgs.length]);
      const renderBgSlide = (slide, opacity, transitionDuration) => {
        const url = assetResolver2(slide.assetId, "image");
        if (!url) return null;
        const meta = getAssetMetadata2(slide.assetId, "image");
        const isCustom = slide.objectFit === "custom";
        const style = isCustom ? {
          position: "absolute",
          left: \`\${slide.x ?? 0}%\`,
          top: \`\${slide.y ?? 0}%\`,
          width: \`\${slide.width ?? 100}%\`,
          height: \`\${slide.height ?? 100}%\`,
          objectFit: "fill",
          opacity: (slide.opacity ?? 1) * opacity,
          transition: transitionDuration > 0 ? \`opacity \${transitionDuration}s ease-in-out\` : "none"
        } : {
          position: "absolute",
          inset: 0,
          width: "100%",
          height: "100%",
          objectFit: slide.objectFit || "cover",
          opacity: (slide.opacity ?? 1) * opacity,
          transition: transitionDuration > 0 ? \`opacity \${transitionDuration}s ease-in-out\` : "none"
        };
        if (meta.isVideo) {
          return /* @__PURE__ */ jsxRuntime2.jsx(TrimmedVideo, { src: url, autoPlay: true, muted: true, loop: meta.loop, trimStart: slide.trimStart, trimEnd: slide.trimEnd, playsInline: true, style });
        }
        return /* @__PURE__ */ jsxRuntime2.jsx("img", { src: url, alt: "", style });
      };
      const currentSlide = hasBgs ? bgs[bgIndex] : null;
      const prevSlide = prevBgIndex !== null && hasBgs ? bgs[prevBgIndex] : null;
      const transDur = (currentSlide == null ? void 0 : currentSlide.transitionDuration) || 0.5;
      return /* @__PURE__ */ jsxRuntime2.jsxs(
        "div",
        {
          className: "absolute inset-0 z-40 flex items-end justify-center overflow-hidden",
          style: { backgroundColor: command.backgroundColor || "#000000FF", cursor: command.allowSkip ? "pointer" : "default" },
          onClick: () => {
            if (!command.allowSkip) return;
            onFinish();
          },
          children: [
            hasBgs && /* @__PURE__ */ jsxRuntime2.jsxs("div", { className: "absolute inset-0 z-0", children: [
              prevSlide && transitioning && renderBgSlide(prevSlide, 1, 0),
              currentSlide && renderBgSlide(currentSlide, transitioning ? currentSlide.transition === "instant" ? 1 : 1 : 1, transitioning ? transDur : 0),
              transitioning && currentSlide && currentSlide.transition !== "instant" && /* @__PURE__ */ jsxRuntime2.jsx(
                "div",
                {
                  className: "absolute inset-0",
                  style: {
                    backgroundColor: currentSlide.transition === "dissolve" ? "transparent" : command.backgroundColor || "#000000FF",
                    animation: \`credit-bg-fade-in \${transDur}s ease-in-out both\`
                  }
                }
              )
            ] }),
            hasBgs && /* @__PURE__ */ jsxRuntime2.jsx("div", { className: "absolute inset-0 z-[1]", style: { backgroundColor: "rgba(0,0,0,0.4)" } }),
            hasMedia && mediaItems.map((item, idx) => {
              const url = assetResolver2(item.assetId, "image");
              if (!url) return null;
              const meta = getAssetMetadata2(item.assetId, "image");
              const showAt = item.showAt || 0;
              const hideAt = item.hideAt || 0;
              const isVisible = elapsed >= showAt && (hideAt <= 0 || elapsed < hideAt);
              const isFading = item.transition === "fade";
              const itemOpacity = isVisible ? item.opacity ?? 1 : 0;
              const isCustomItem = item.objectFit === "custom";
              const mediaStyle = isCustomItem ? {
                position: "absolute",
                left: \`\${item.x}%\`,
                top: \`\${item.y}%\`,
                width: \`\${item.width}%\`,
                height: \`\${item.height}%\`,
                objectFit: "fill",
                opacity: itemOpacity,
                transition: isFading ? \`opacity \${item.transitionDuration || 0.5}s ease-in-out\` : "none",
                zIndex: 1,
                pointerEvents: "none"
              } : {
                position: "absolute",
                inset: 0,
                width: "100%",
                height: "100%",
                objectFit: item.objectFit || "cover",
                opacity: itemOpacity,
                transition: isFading ? \`opacity \${item.transitionDuration || 0.5}s ease-in-out\` : "none",
                zIndex: 1,
                pointerEvents: "none"
              };
              if (meta.isVideo) {
                return /* @__PURE__ */ jsxRuntime2.jsx(TrimmedVideo, { src: url, autoPlay: true, muted: true, loop: true, trimStart: item.trimStart, trimEnd: item.trimEnd, playsInline: true, style: mediaStyle }, \`credit-media-\${idx}\`);
              }
              return /* @__PURE__ */ jsxRuntime2.jsx("img", { src: url, alt: "", style: mediaStyle }, \`credit-media-\${idx}\`);
            }),
            /* @__PURE__ */ jsxRuntime2.jsx(CreditScrollContent, { command, hasBgs, hasMedia, onFinish }),
            command.allowSkip && /* @__PURE__ */ jsxRuntime2.jsx("div", { className: "absolute bottom-4 right-4 text-xs opacity-50 z-[3]", style: { color: command.textColor || "#FFFFFF" }, children: "Click to skip" })
          ]
        }
      );
    };
    const HistoryPanel = ({ history, onClose: onClose2, onJumpTo }) => {
      const scrollRef = React2.useRef(null);
      React2.useEffect(() => {
        if (scrollRef.current) {
          scrollRef.current.scrollTop = scrollRef.current.scrollHeight;
        }
      }, []);
      return /* @__PURE__ */ jsxRuntime2.jsxs("div", { className: "absolute inset-0 bg-black z-50 flex flex-col", children: [
        /* @__PURE__ */ jsxRuntime2.jsxs(
          "div",
          {
            className: "flex items-center justify-between px-6 py-4 border-b border-slate-700/60",
            style: { background: "linear-gradient(180deg, rgb(15,23,42) 0%, rgb(15,23,42) 100%)" },
            children: [
              /* @__PURE__ */ jsxRuntime2.jsxs("div", { className: "flex items-center gap-3", children: [
                /* @__PURE__ */ jsxRuntime2.jsx("svg", { className: "w-5 h-5 text-sky-400", fill: "none", viewBox: "0 0 24 24", stroke: "currentColor", strokeWidth: 2, children: /* @__PURE__ */ jsxRuntime2.jsx("path", { strokeLinecap: "round", strokeLinejoin: "round", d: "M12 6v6l4 2m6-2a9 9 0 11-18 0 9 9 0 0118 0z" }) }),
                /* @__PURE__ */ jsxRuntime2.jsx("h2", { className: "text-white text-xl font-bold tracking-wide", children: "Text History" }),
                /* @__PURE__ */ jsxRuntime2.jsxs("span", { className: "text-slate-500 text-sm", children: [
                  "(",
                  history.length,
                  " entries)"
                ] })
              ] }),
              /* @__PURE__ */ jsxRuntime2.jsx(
                "button",
                {
                  onClick: onClose2,
                  className: "text-slate-300 hover:text-white text-sm px-4 py-2 bg-slate-800/60 hover:bg-slate-700/60 border border-slate-600/40 rounded-lg transition-colors",
                  children: "Close (H / ESC)"
                }
              )
            ]
          }
        ),
        /* @__PURE__ */ jsxRuntime2.jsx("div", { ref: scrollRef, className: "flex-1 overflow-y-auto px-6 py-4 space-y-2", children: history.length === 0 ? /* @__PURE__ */ jsxRuntime2.jsxs("div", { className: "flex flex-col items-center justify-center h-full text-slate-500", children: [
          /* @__PURE__ */ jsxRuntime2.jsx("svg", { className: "w-12 h-12 mb-3 opacity-40", fill: "none", viewBox: "0 0 24 24", stroke: "currentColor", strokeWidth: 1.5, children: /* @__PURE__ */ jsxRuntime2.jsx("path", { strokeLinecap: "round", strokeLinejoin: "round", d: "M19.5 14.25v-2.625a3.375 3.375 0 00-3.375-3.375h-1.5A1.125 1.125 0 0113.5 7.125v-1.5a3.375 3.375 0 00-3.375-3.375H8.25m0 12.75h7.5m-7.5 3H12M10.5 2.25H5.625c-.621 0-1.125.504-1.125 1.125v17.25c0 .621.504 1.125 1.125 1.125h12.75c.621 0 1.125-.504 1.125-1.125V11.25a9 9 0 00-9-9z" }) }),
          /* @__PURE__ */ jsxRuntime2.jsx("p", { className: "text-sm", children: "No history yet. Play through some dialogue first." })
        ] }) : history.map((entry, index) => /* @__PURE__ */ jsxRuntime2.jsx(
          "div",
          {
            className: \`group p-3 rounded-lg transition-colors \${entry.type === "choice" ? "bg-blue-900/40 border-l-3 border-blue-500/60 hover:bg-blue-900/50" : entry.type === "textInput" ? "bg-emerald-900/40 border-l-3 border-emerald-500/60 hover:bg-emerald-900/50" : "bg-slate-800/50 hover:bg-slate-800/60"}\`,
            style: { cursor: onJumpTo ? "pointer" : "default" },
            onClick: () => onJumpTo == null ? void 0 : onJumpTo(index),
            children: /* @__PURE__ */ jsxRuntime2.jsxs("div", { className: "flex items-start gap-3", children: [
              /* @__PURE__ */ jsxRuntime2.jsxs("div", { className: "mt-0.5 flex-shrink-0", children: [
                entry.type === "dialogue" && /* @__PURE__ */ jsxRuntime2.jsx("svg", { className: "w-4 h-4 text-slate-500", fill: "none", viewBox: "0 0 24 24", stroke: "currentColor", strokeWidth: 2, children: /* @__PURE__ */ jsxRuntime2.jsx("path", { strokeLinecap: "round", strokeLinejoin: "round", d: "M8 12h.01M12 12h.01M16 12h.01M21 12c0 4.418-4.03 8-9 8a9.863 9.863 0 01-4.255-.949L3 20l1.395-3.72C3.512 15.042 3 13.574 3 12c0-4.418 4.03-8 9-8s9 3.582 9 8z" }) }),
                entry.type === "choice" && /* @__PURE__ */ jsxRuntime2.jsx("svg", { className: "w-4 h-4 text-blue-400", fill: "none", viewBox: "0 0 24 24", stroke: "currentColor", strokeWidth: 2, children: /* @__PURE__ */ jsxRuntime2.jsx("path", { strokeLinecap: "round", strokeLinejoin: "round", d: "M9 5l7 7-7 7" }) }),
                entry.type === "textInput" && /* @__PURE__ */ jsxRuntime2.jsx("svg", { className: "w-4 h-4 text-emerald-400", fill: "none", viewBox: "0 0 24 24", stroke: "currentColor", strokeWidth: 2, children: /* @__PURE__ */ jsxRuntime2.jsx("path", { strokeLinecap: "round", strokeLinejoin: "round", d: "M11 5H6a2 2 0 00-2 2v11a2 2 0 002 2h11a2 2 0 002-2v-5m-1.414-9.414a2 2 0 112.828 2.828L11.828 15H9v-2.828l8.586-8.586z" }) })
              ] }),
              /* @__PURE__ */ jsxRuntime2.jsxs("div", { className: "flex-1 min-w-0", children: [
                entry.type === "dialogue" && entry.characterName && entry.characterName !== "Narrator" && /* @__PURE__ */ jsxRuntime2.jsx("div", { className: "font-semibold text-sm mb-0.5", style: { color: entry.characterColor || "#94a3b8" }, children: entry.characterName }),
                /* @__PURE__ */ jsxRuntime2.jsx("div", { className: "text-white/90 text-sm leading-relaxed", children: entry.type === "choice" ? entry.choiceText || entry.text : entry.type === "textInput" ? \`"\${entry.inputValue}"\` : entry.text }),
                entry.type === "choice" && /* @__PURE__ */ jsxRuntime2.jsx("div", { className: "text-blue-400/70 text-xs mt-1 font-medium", children: "Selected choice" }),
                entry.type === "textInput" && /* @__PURE__ */ jsxRuntime2.jsx("div", { className: "text-emerald-400/70 text-xs mt-1 font-medium", children: "Text input" })
              ] })
            ] })
          },
          index
        )) })
      ] });
    };
    const renderPlayerUI = () => {
      if (!playerState || playerState.mode !== "playing") return null;
      const { uiState } = playerState;
      const currentHudScreenId = hudStack.length > 0 ? hudStack[hudStack.length - 1] : project.ui.gameHudScreenId;
      const currentHudScreen = currentHudScreenId ? project.uiScreens[currentHudScreenId] : null;
      const isHudPassThrough = currentHudScreen ? currentHudScreen.passThrough ?? (currentHudScreenId === project.ui.gameHudScreenId || !!currentHudScreen.hudNonBlocking) : false;
      const shouldShowDialogueOnHud = (currentHudScreen == null ? void 0 : currentHudScreen.showDialogue) || isHudPassThrough;
      const movieExitDur = uiState.movieTransition && uiState.movieTransition !== "instant" ? uiState.movieTransitionDuration ?? 0.5 : 0;
      return /* @__PURE__ */ jsxRuntime2.jsxs(jsxRuntime2.Fragment, { children: [
        uiState.showHistory && /* @__PURE__ */ jsxRuntime2.jsx(
          HistoryPanel,
          {
            history: playerState.history,
            onClose: () => updatePlayerState((p) => p ? { ...p, uiState: { ...p.uiState, showHistory: false } } : null)
          }
        ),
        uiState.movieUrl && (() => {
          var _a2;
          const handleMovieEnded = () => {
            var _a3;
            if (((_a3 = scriptInputResolverRef.current) == null ? void 0 : _a3.kind) === "movie") {
              const r = scriptInputResolverRef.current;
              scriptInputResolverRef.current = null;
              updatePlayerState((p) => p ? { ...p, uiState: { ...p.uiState, movieUrl: null, movieLoop: false, movieExiting: false } } : null);
              r.resolve(void 0);
              return;
            }
            if (uiState.movieLoop) return;
            if (uiState.movieHoldLastFrame) return;
            const wasWaiting = uiState.isWaitingForInput;
            if (movieExitDur > 0 && !uiState.movieExiting) {
              updatePlayerState((p) => p ? { ...p, uiState: { ...p.uiState, movieExiting: true } } : null);
              const tid = window.setTimeout(() => {
                updatePlayerState((p) => p ? { ...p, ...wasWaiting ? { currentIndex: p.currentIndex + 1 } : {}, uiState: { ...p.uiState, isWaitingForInput: false, movieUrl: null, movieLoop: false, movieExiting: false } } : null);
              }, movieExitDur * 1e3);
              activeEffectTimeoutsRef.current.push(tid);
            } else if (wasWaiting) {
              updatePlayerState((p) => p ? { ...p, currentIndex: p.currentIndex + 1, uiState: { ...p.uiState, isWaitingForInput: false, movieUrl: null, movieLoop: false } } : null);
            } else {
              updatePlayerState((p) => p ? { ...p, uiState: { ...p.uiState, movieUrl: null, movieLoop: false } } : null);
            }
          };
          return /* @__PURE__ */ jsxRuntime2.jsxs(
            "div",
            {
              className: "absolute inset-0 bg-black z-40 flex flex-col items-center justify-center text-white",
              style: { opacity: uiState.movieExiting ? 0 : 1, transition: uiState.movieExiting ? \`opacity \${movieExitDur}s ease-out\` : void 0 },
              onClick: () => {
                var _a3;
                if (((_a3 = scriptInputResolverRef.current) == null ? void 0 : _a3.kind) === "movie") {
                  const r = scriptInputResolverRef.current;
                  scriptInputResolverRef.current = null;
                  updatePlayerState((p) => p ? { ...p, uiState: { ...p.uiState, movieUrl: null, movieLoop: false, movieExiting: false } } : null);
                  r.resolve(void 0);
                  return;
                }
                if (!uiState.isWaitingForInput) return;
                updatePlayerState((p) => p ? { ...p, currentIndex: p.currentIndex + 1, uiState: { ...p.uiState, isWaitingForInput: false, movieUrl: null, movieLoop: false, movieExiting: false } } : null);
              },
              children: [
                /* @__PURE__ */ jsxRuntime2.jsx(
                  TrimmedVideo,
                  {
                    src: uiState.movieUrl,
                    autoPlay: true,
                    playsInline: true,
                    trimStart: uiState.movieTrimStart,
                    trimEnd: uiState.movieTrimEnd,
                    ref: (el) => {
                      if (!el) return;
                      el.play().catch(() => {
                        el.muted = true;
                        el.play().catch(() => {
                        });
                      });
                    },
                    loop: uiState.movieLoop ?? false,
                    style: { width: "100%", height: "100%", objectFit: "contain", animation: movieEntryAnim(uiState.movieTransition, uiState.movieTransitionDuration) },
                    onSegmentEnd: handleMovieEnded,
                    onEnded: handleMovieEnded
                  }
                ),
                (uiState.isWaitingForInput || ((_a2 = scriptInputResolverRef.current) == null ? void 0 : _a2.kind) === "movie") && /* @__PURE__ */ jsxRuntime2.jsx("div", { className: "absolute bottom-4 right-4 text-xs opacity-50 pointer-events-none", children: "Click to skip" })
              ]
            }
          );
        })(),
        activeCreditRoll && /* @__PURE__ */ jsxRuntime2.jsx(
          CreditRollOverlay,
          {
            command: activeCreditRoll,
            project,
            assetResolver,
            getAssetMetadata,
            onFinish: () => {
              const onComplete = activeCreditRoll.onComplete;
              setActiveCreditRoll(null);
              if (onComplete === "title") {
                const audio = musicAudioRef.current;
                if (audio) {
                  audio.pause();
                  audio.currentTime = 0;
                  audio.src = "";
                }
                stopAllSfx();
                updatePlayerState(null);
                setHudStack([]);
                if (project.ui.titleScreenId) setScreenStack([project.ui.titleScreenId]);
              } else {
                updatePlayerState((p) => p ? { ...p, currentIndex: p.currentIndex + 1 } : null);
              }
            }
          }
        ),
        uiState.dialogue && (!currentHudScreen || shouldShowDialogueOnHud) && /* @__PURE__ */ jsxRuntime2.jsxs(jsxRuntime2.Fragment, { children: [
          (() => {
            var _a2, _b2;
            const qmPosition = project.ui.quickMenuPosition ?? "above-dialogue";
            if (qmPosition === "hidden") return null;
            if (uiState.showHistory) return null;
            const qmReactive = pickReactiveTextboxState(project.ui.quickMenuReactiveStates, playerState.variables, evaluateConditions2);
            if (qmReactive == null ? void 0 : qmReactive.hide) return null;
            const qmReactiveMs = (project.ui.quickMenuReactiveStates || []).reduce(
              (m, s) => s.transitionMs ? Math.max(m, s.transitionMs) : m,
              0
            );
            const qmColor = (qmReactive == null ? void 0 : qmReactive.color) ?? project.ui.quickMenuColor ?? "#0f172a";
            const qmOpacity = (qmReactive == null ? void 0 : qmReactive.opacity) ?? project.ui.quickMenuOpacity ?? 75;
            const qmRadius = project.ui.quickMenuBorderRadius ?? 4;
            const qmBg = hexToRgba(qmColor, qmOpacity);
            const qmBgDisabled = hexToRgba(qmColor, Math.max(10, qmOpacity - 35));
            ((_a2 = project.gameResolution) == null ? void 0 : _a2.width) || 1920;
            const lpGameH = ((_b2 = project.gameResolution) == null ? void 0 : _b2.height) || 1080;
            const dlgW = project.ui.dialogueBoxWidth ?? 100;
            const dlgH = project.ui.dialogueBoxHeight ? project.ui.dialogueBoxHeight * 100 / lpGameH : 20;
            const dlgX = project.ui.dialogueBoxX ?? (100 - dlgW) / 2;
            const dlgBm = (project.ui.dialogueBoxBottomMargin ?? 20) * 100 / lpGameH;
            const qmWPct = project.ui.quickMenuWidth ?? 40;
            const qmHPct = project.ui.quickMenuHeight ?? 4;
            const isBottomQmPreset = qmPosition === "bottom-right" || qmPosition === "bottom-left";
            const shouldFloatQm = project.ui.quickMenuFloatOverDialogue ?? false;
            const qmBottomReservePct = !shouldFloatQm && isBottomQmPreset && project.ui.quickMenuY === void 0 ? qmHPct + 2 : 0;
            const dlgY = project.ui.dialogueBoxY ?? 100 - dlgH - dlgBm - qmBottomReservePct;
            const getDefaultPos = () => {
              if (qmPosition === "top-right") return { x: 100 - qmWPct - 1, y: 1 };
              if (qmPosition === "top-left") return { x: 1, y: 1 };
              if (qmPosition === "bottom-right") return { x: 100 - qmWPct - 1, y: 100 - qmHPct - 1 };
              if (qmPosition === "bottom-left") return { x: 1, y: 100 - qmHPct - 1 };
              return { x: dlgX, y: dlgY - qmHPct - 1 };
            };
            const defPos = getDefaultPos();
            const qmX = project.ui.quickMenuX ?? defPos.x;
            const qmY = project.ui.quickMenuY ?? defPos.y;
            const qmZIndex = shouldFloatQm || isBottomQmPreset ? 50 : 25;
            const pillBase = "flex items-center gap-1 font-medium transition-all";
            const defBorder = "1px solid rgba(148,163,184,0.2)";
            const commonPill = {
              borderRadius: scalePx(qmRadius),
              padding: \`\${scalePx(4)} \${scalePx(10)}\`,
              fontSize: scalePx(12),
              backdropFilter: "blur(4px)",
              ...qmReactiveMs > 0 ? { transition: \`background \${qmReactiveMs}ms ease, background-color \${qmReactiveMs}ms ease, border-color \${qmReactiveMs}ms ease\` } : {}
            };
            const hasHistory = playerState.history.length > 0;
            const qmButtonCfgs = project.ui.quickMenuButtons || {};
            const qmCustomButtons = project.ui.quickMenuCustomButtons || [];
            const qmCfgFor = (key) => qmButtonCfgs[key] || qmCustomButtons.find((cb) => cb.id === key);
            const iconStyle = { width: "1.15em", height: "1.15em", flexShrink: 0 };
            const descriptors = [
              {
                key: "skipBackward",
                show: project.ui.quickMenuShowSkipBackward !== false,
                onClick: (e) => {
                  e.stopPropagation();
                  handleSkipBackward();
                },
                disabled: !hasHistory,
                title: "Skip Backward (Arrow Up)",
                label: "Back",
                icon: /* @__PURE__ */ jsxRuntime2.jsx("svg", { style: iconStyle, fill: "none", viewBox: "0 0 24 24", stroke: "currentColor", strokeWidth: 2, children: /* @__PURE__ */ jsxRuntime2.jsx("path", { strokeLinecap: "round", strokeLinejoin: "round", d: "M11 19l-7-7 7-7m8 14l-7-7 7-7" }) }),
                pillClassName: pillBase,
                pillStyle: { ...commonPill, background: hasHistory ? qmBg : qmBgDisabled, border: defBorder, color: hasHistory ? "rgba(255,255,255,0.8)" : "rgba(255,255,255,0.3)", cursor: hasHistory ? "pointer" : "default" }
              },
              {
                key: "log",
                show: project.ui.quickMenuShowLog !== false,
                onClick: (e) => {
                  e.stopPropagation();
                  updatePlayerState((pp) => pp ? { ...pp, uiState: { ...pp.uiState, showHistory: true } } : null);
                },
                title: "Text History (H)",
                label: "Log",
                icon: /* @__PURE__ */ jsxRuntime2.jsx("svg", { style: iconStyle, fill: "none", viewBox: "0 0 24 24", stroke: "currentColor", strokeWidth: 2, children: /* @__PURE__ */ jsxRuntime2.jsx("path", { strokeLinecap: "round", strokeLinejoin: "round", d: "M12 6v6l4 2m6-2a9 9 0 11-18 0 9 9 0 0118 0z" }) }),
                pillClassName: \`\${pillBase} hover:brightness-125\`,
                pillStyle: { ...commonPill, background: qmBg, border: defBorder, color: "rgba(255,255,255,0.8)" }
              },
              {
                key: "autoAdvance",
                show: project.ui.quickMenuShowAutoAdvance !== false,
                onClick: (e) => {
                  e.stopPropagation();
                  setSettings((s) => ({ ...s, autoAdvance: !s.autoAdvance }));
                },
                title: "Auto-Advance",
                label: "Auto",
                icon: /* @__PURE__ */ jsxRuntime2.jsxs("svg", { style: iconStyle, fill: "none", viewBox: "0 0 24 24", stroke: "currentColor", strokeWidth: 2, children: [
                  /* @__PURE__ */ jsxRuntime2.jsx("path", { strokeLinecap: "round", strokeLinejoin: "round", d: "M14.752 11.168l-3.197-2.132A1 1 0 0010 9.87v4.263a1 1 0 001.555.832l3.197-2.132a1 1 0 000-1.664z" }),
                  /* @__PURE__ */ jsxRuntime2.jsx("path", { strokeLinecap: "round", strokeLinejoin: "round", d: "M21 12a9 9 0 11-18 0 9 9 0 0118 0z" })
                ] }),
                pillClassName: pillBase,
                pillStyle: { ...commonPill, background: settings.autoAdvance ? "rgba(14,165,233,0.3)" : qmBg, border: \`1px solid \${settings.autoAdvance ? "rgba(14,165,233,0.5)" : "rgba(148,163,184,0.2)"}\`, color: settings.autoAdvance ? "rgba(125,211,252,0.95)" : "rgba(255,255,255,0.8)" }
              },
              {
                key: "skipForward",
                show: !!settings.enableSkip && project.ui.quickMenuShowSkipForward !== false,
                onClick: (e) => {
                  e.stopPropagation();
                  updatePlayerState((pp) => pp ? { ...pp, uiState: { ...pp.uiState, isSkipping: !pp.uiState.isSkipping } } : null);
                },
                title: "Skip Forward (Ctrl)",
                label: "Skip",
                icon: /* @__PURE__ */ jsxRuntime2.jsx("svg", { style: iconStyle, fill: "none", viewBox: "0 0 24 24", stroke: "currentColor", strokeWidth: 2, children: /* @__PURE__ */ jsxRuntime2.jsx("path", { strokeLinecap: "round", strokeLinejoin: "round", d: "M13 5l7 7-7 7M5 5l7 7-7 7" }) }),
                pillClassName: pillBase,
                pillStyle: { ...commonPill, background: uiState.isSkipping ? "rgba(239,68,68,0.3)" : qmBg, border: \`1px solid \${uiState.isSkipping ? "rgba(239,68,68,0.5)" : "rgba(148,163,184,0.2)"}\`, color: uiState.isSkipping ? "rgba(252,165,165,0.95)" : "rgba(255,255,255,0.8)" }
              },
              {
                key: "save",
                show: project.ui.quickMenuShowSave !== false,
                onClick: (e) => {
                  e.stopPropagation();
                  handleUIAction({ type: UIActionType.SaveGame, slotNumber: 1 });
                },
                title: "Save Game",
                label: "Save",
                icon: /* @__PURE__ */ jsxRuntime2.jsx("svg", { style: iconStyle, fill: "none", viewBox: "0 0 24 24", stroke: "currentColor", strokeWidth: 2, children: /* @__PURE__ */ jsxRuntime2.jsx("path", { strokeLinecap: "round", strokeLinejoin: "round", d: "M8 7H5a2 2 0 00-2 2v9a2 2 0 002 2h14a2 2 0 002-2V9a2 2 0 00-2-2h-3m-1 4l-3 3m0 0l-3-3m3 3V3" }) }),
                pillClassName: \`\${pillBase} hover:brightness-125\`,
                pillStyle: { ...commonPill, background: qmBg, border: defBorder, color: "rgba(255,255,255,0.8)" }
              },
              {
                key: "load",
                show: project.ui.quickMenuShowLoad !== false,
                onClick: (e) => {
                  e.stopPropagation();
                  handleUIAction({ type: UIActionType.LoadGame, slotNumber: 1 });
                },
                title: "Load Game",
                label: "Load",
                icon: /* @__PURE__ */ jsxRuntime2.jsx("svg", { style: iconStyle, fill: "none", viewBox: "0 0 24 24", stroke: "currentColor", strokeWidth: 2, children: /* @__PURE__ */ jsxRuntime2.jsx("path", { strokeLinecap: "round", strokeLinejoin: "round", d: "M4 7v10a2 2 0 002 2h12a2 2 0 002-2V7M9 9l3 3m0 0l3-3m-3 3V1" }) }),
                pillClassName: \`\${pillBase} hover:brightness-125\`,
                pillStyle: { ...commonPill, background: qmBg, border: defBorder, color: "rgba(255,255,255,0.8)" }
              }
            ];
            const customDescriptors = qmCustomButtons.filter((cb) => cb.show !== false).map((cb) => ({
              key: cb.id,
              show: true,
              onClick: (e) => {
                e.stopPropagation();
                if (cb.action && cb.action.type !== UIActionType.None) handleUIAction(cb.action);
              },
              title: cb.label,
              label: cb.label,
              icon: null,
              pillClassName: \`\${pillBase} hover:brightness-125\`,
              pillStyle: { ...commonPill, background: qmBg, border: defBorder, color: "rgba(255,255,255,0.8)" }
            }));
            const visible = [...descriptors, ...customDescriptors].filter((d) => d.show);
            const qmOnClick = (d) => {
              const cfg = qmCfgFor(d.key);
              if ((cfg == null ? void 0 : cfg.action) && cfg.action.type !== UIActionType.None) {
                return (e) => {
                  e.stopPropagation();
                  handleUIAction(cfg.action);
                };
              }
              return d.onClick;
            };
            if (project.ui.quickMenuIndependentLayout) {
              const count = visible.length || 1;
              const slotW = qmWPct / count;
              return /* @__PURE__ */ jsxRuntime2.jsx("div", { className: "absolute inset-0", style: { pointerEvents: "none", zIndex: qmZIndex }, children: visible.map((d, i) => {
                const cfg = qmCfgFor(d.key) || {};
                const bx = cfg.x ?? qmX + i * slotW;
                const by = cfg.y ?? qmY;
                const bw = cfg.width ?? Math.max(4, slotW - 1);
                const bh = cfg.height ?? qmHPct;
                const fit = !!cfg.fitToContent;
                const slotStyle = fit ? { position: "absolute", left: \`\${bx}%\`, top: \`\${by}%\`, width: \`\${bw}%\`, height: \`\${bh}%\`, pointerEvents: "none", display: "flex", alignItems: "center", justifyContent: "center" } : { position: "absolute", left: \`\${bx}%\`, top: \`\${by}%\`, width: \`\${bw}%\`, height: \`\${bh}%\`, pointerEvents: "auto", overflow: "hidden" };
                return /* @__PURE__ */ jsxRuntime2.jsx("div", { style: slotStyle, children: /* @__PURE__ */ jsxRuntime2.jsx(
                  QuickMenuButtonEl,
                  {
                    label: d.label,
                    title: d.title,
                    icon: d.icon,
                    onClick: qmOnClick(d),
                    disabled: d.disabled,
                    config: cfg,
                    assetResolver,
                    artButtonStyle: fit ? { maxWidth: "100%", maxHeight: "100%", pointerEvents: "auto" } : { width: "100%", height: "100%" },
                    artImgStyle: fit ? { maxWidth: "100%", maxHeight: "100%", width: "auto", height: "auto", objectFit: "contain", display: "block" } : { width: "100%", height: "100%", objectFit: "contain", display: "block" },
                    pillClassName: d.pillClassName,
                    pillStyle: { ...d.pillStyle, width: "100%", height: "100%", justifyContent: "center", ...fit ? { pointerEvents: "auto" } : {} }
                  }
                ) }, d.key);
              }) });
            }
            return /* @__PURE__ */ jsxRuntime2.jsx(
              "div",
              {
                className: "flex items-center justify-center gap-2",
                style: {
                  position: "absolute",
                  left: \`\${qmX}%\`,
                  top: \`\${qmY}%\`,
                  width: \`\${qmWPct}%\`,
                  height: \`\${qmHPct}%\`,
                  pointerEvents: "none",
                  zIndex: qmZIndex
                },
                children: /* @__PURE__ */ jsxRuntime2.jsx("div", { className: "flex flex-wrap items-center justify-center gap-1.5", style: { pointerEvents: "auto", maxWidth: "100%" }, children: visible.map((d) => /* @__PURE__ */ jsxRuntime2.jsx(
                  QuickMenuButtonEl,
                  {
                    label: d.label,
                    title: d.title,
                    icon: d.icon,
                    onClick: qmOnClick(d),
                    disabled: d.disabled,
                    config: qmCfgFor(d.key),
                    assetResolver,
                    artButtonStyle: { width: "auto", height: "auto" },
                    artImgStyle: { height: scalePx(28), width: "auto", objectFit: "contain", display: "block" },
                    pillClassName: d.pillClassName,
                    pillStyle: d.pillStyle
                  },
                  d.key
                )) })
              }
            );
          })(),
          /* @__PURE__ */ jsxRuntime2.jsx(
            DialogueBox,
            {
              dialogue: uiState.dialogue,
              settings,
              projectUI: project.ui,
              onFinished: handleDialogueAdvance,
              variables: playerState.variables,
              project,
              reactiveState: pickReactiveTextboxState(project.ui.dialogueReactiveStates, playerState.variables, evaluateConditions2),
              timerPaused: scenePaused || !!uiState.choices || !!uiState.textInput || !!uiState.showHistory,
              uiPalette: playerState.uiPaletteOverride,
              voiceRef: currentVoiceRef
            }
          )
        ] }),
        uiState.choices && /* @__PURE__ */ jsxRuntime2.jsx(
          ChoiceMenu,
          {
            choices: uiState.choices,
            projectUI: project.ui,
            onSelect: handleChoiceSelect,
            variables: playerState.variables,
            project,
            layout: uiState.choiceLayout,
            timeLimit: uiState.choiceTimeLimit,
            showTimer: uiState.choiceShowTimer,
            uiPalette: playerState.uiPaletteOverride,
            onTimeout: () => {
              const opts = uiState.choices || [];
              if ((uiState.choiceTimeoutBehavior || "option") === "actions") {
                handleChoiceSelect({ id: "__timeout__", text: "", actions: uiState.choiceTimeoutActions || [] });
              } else {
                const picked = opts.find((o) => o.id === uiState.choiceTimeoutOptionId) || opts[0];
                if (picked) handleChoiceSelect(picked);
              }
            }
          }
        ),
        uiState.textInput && /* @__PURE__ */ jsxRuntime2.jsx(TextInputForm, { textInput: uiState.textInput, onSubmit: handleTextInputSubmit, variables: playerState.variables, project, projectUI: project.ui }),
        activeFlashRef.current && /* @__PURE__ */ jsxRuntime2.jsx(
          "div",
          {
            className: "absolute inset-0 z-50 pointer-events-none",
            style: { backgroundColor: activeFlashRef.current.color, animation: \`flash-anim \${activeFlashRef.current.duration}s ease-in-out\` },
            onAnimationEnd: (e) => {
              if (e.target === e.currentTarget) {
                activeFlashRef.current = null;
                setFlashTrigger((prev) => prev + 1);
              }
            }
          },
          activeFlashRef.current.key
        ),
        activeLightningRef.current && /* @__PURE__ */ jsxRuntime2.jsx("div", { className: "absolute inset-0 pointer-events-none", style: { opacity: activeLightningRef.current.intensity, zIndex: activeLightningRef.current.affectsDialogue ? 50 : 15 }, children: /* @__PURE__ */ jsxRuntime2.jsx(
          "div",
          {
            className: "absolute inset-0",
            style: { backgroundColor: activeLightningRef.current.color, animation: \`vn-lightning-\${activeLightningRef.current.flashes} \${activeLightningRef.current.duration}s ease-out\` },
            onAnimationEnd: (e) => {
              if (e.target === e.currentTarget) {
                activeLightningRef.current = null;
                setLightningTrigger((prev) => prev + 1);
              }
            }
          }
        ) }, activeLightningRef.current.key),
        activeFireworksRef.current && /* @__PURE__ */ jsxRuntime2.jsx("div", { className: "absolute inset-0 pointer-events-none", style: { zIndex: activeFireworksRef.current.affectsDialogue ? 50 : 15 }, children: /* @__PURE__ */ jsxRuntime2.jsx(
          FireworksBurst,
          {
            colors: activeFireworksRef.current.colors,
            intensity: activeFireworksRef.current.intensity,
            bursts: activeFireworksRef.current.bursts,
            duration: activeFireworksRef.current.duration,
            heightFrac: activeFireworksRef.current.burstHeight,
            width: stageSize.width,
            height: stageSize.height,
            onExplode: activeFireworksRef.current.sfxPerBurst && activeFireworksRef.current.sfxId ? () => playSound(activeFireworksRef.current.sfxId, activeFireworksRef.current.sfxVolume) : void 0,
            onDone: () => {
              activeFireworksRef.current = null;
              setFireworksTrigger((prev) => prev + 1);
            }
          },
          activeFireworksRef.current.key
        ) }),
        !(screenStack.length > 0 || hudStack.some((id) => {
          var _a2;
          return !((_a2 = project.uiScreens[id]) == null ? void 0 : _a2.hudNonBlocking);
        })) && /* @__PURE__ */ jsxRuntime2.jsxs(jsxRuntime2.Fragment, { children: [
          flashlight && (flashlight.on || flashlight.darkWhenOff) && (() => {
            const fVars = playerState == null ? void 0 : playerState.variables;
            const fRadius = resolveVarNumber(fVars, flashlight.radiusVariableId, flashlight.radius, { min: 1, max: 100 });
            const fDarkness = resolveVarNumber(fVars, flashlight.darknessVariableId, flashlight.darkness, { min: 0, max: 1 });
            return /* @__PURE__ */ jsxRuntime2.jsx(
              "div",
              {
                ref: flashlightOverlayRef,
                className: "absolute inset-0 pointer-events-none",
                style: {
                  zIndex: flashlight.affectsDialogue ? 45 : 15,
                  background: flashlight.on ? flashlightBg((typeof window !== "undefined" ? window.innerWidth : 1280) / 2, (typeof window !== "undefined" ? window.innerHeight : 720) / 2, fRadius / 100 * (typeof window !== "undefined" ? Math.min(window.innerWidth, window.innerHeight) : 720), flashlight.softness, hexToRgba(flashlight.color, fDarkness * 100)) : hexToRgba(flashlight.color, fDarkness * 100)
                }
              }
            );
          })(),
          (() => {
            const active = Object.entries(spotlights).filter(([, s]) => s.on);
            if (!active.length) return null;
            const flashActive = !!(flashlight && (flashlight.on || flashlight.darkWhenOff));
            const maxIntensity = Math.max(...active.map(([, s]) => Math.max(0, Math.min(1, s.intensity))));
            const affectsDialogue = active.some(([, s]) => s.affectsDialogue);
            const vpMin = typeof window !== "undefined" ? Math.min(window.innerWidth, window.innerHeight) : 720;
            return /* @__PURE__ */ jsxRuntime2.jsxs("div", { className: "absolute inset-0 pointer-events-none overflow-hidden", style: { zIndex: affectsDialogue ? 45 : 15 }, children: [
              !flashActive && /* @__PURE__ */ jsxRuntime2.jsx("div", { className: "absolute inset-0", style: { background: hexToRgba("#000000", maxIntensity * 100) } }),
              active.map(([id, s]) => {
                const half = Math.max(2, Math.min(100, s.beamWidth)) / 2;
                const len = Math.max(5, Math.min(200, s.height));
                const srcHalf = Math.max(0, Math.min(60, s.sourceWidth)) / 2;
                const inner = Math.round(Math.max(0, Math.min(1, 1 - s.falloff)) * 100);
                const blurPx = Math.round((0.015 + Math.max(0, Math.min(1, s.falloff)) * 0.05) * vpMin);
                const sx = s.sourceX, sy = s.sourceY;
                return /* @__PURE__ */ jsxRuntime2.jsx(
                  "div",
                  {
                    ref: (el) => {
                      spotlightRefs.current.set(id, el);
                    },
                    className: "absolute inset-0",
                    style: {
                      mixBlendMode: "screen",
                      transformOrigin: \`\${sx}% \${sy}%\`,
                      transform: \`rotate(\${-s.aimAngle}deg)\`,
                      transition: s.followMouse ? "none" : "transform 0.15s ease-out",
                      filter: \`blur(\${blurPx}px)\`,
                      willChange: "transform"
                    },
                    children: /* @__PURE__ */ jsxRuntime2.jsx("div", { className: "absolute inset-0", style: {
                      clipPath: \`polygon(\${sx - srcHalf}% \${sy}%, \${sx + srcHalf}% \${sy}%, \${sx + half}% \${sy + len}%, \${sx - half}% \${sy + len}%)\`,
                      background: \`radial-gradient(120% \${len}% at \${sx}% \${sy}%, \${hexToRgba(s.color, 95)} 0%, \${hexToRgba(s.color, 55)} \${inner}%, \${hexToRgba(s.color, 0)} 100%)\`
                    } })
                  },
                  id
                );
              })
            ] });
          })()
        ] })
      ] });
    };
    const currentScreenId = !playerState || playerState.mode === "paused" ? screenStack.length > 0 ? screenStack[screenStack.length - 1] : null : null;
    const hudScreenId = (playerState == null ? void 0 : playerState.mode) === "playing" ? hudStack.length > 0 ? hudStack[hudStack.length - 1] : project.ui.gameHudScreenId : null;
    const activeMenuScreen = currentScreenId ? project.uiScreens[currentScreenId] : null;
    const activeHudScreen = hudScreenId ? project.uiScreens[hudScreenId] : null;
    const activeOverlayEffects = normalizeOverlayEffects([
      // Scene screen-FX belong to the scene stage, which isn't rendered while paused.
      // Including them in pause let a restored (Continue/load) effect fade in over the
      // pause menu and black it out — so gate them to playing mode.
      ...((playerState == null ? void 0 : playerState.mode) === "playing" ? playerState == null ? void 0 : playerState.stageState.screen.overlayEffects : []) ?? [],
      ...(activeHudScreen == null ? void 0 : activeHudScreen.effects) ?? [],
      ...(activeMenuScreen == null ? void 0 : activeMenuScreen.effects) ?? []
    ]).map((e) => e.intensityVariableId ? { ...e, intensity: resolveVarNumber(mergeDirtyUiVariables((playerState == null ? void 0 : playerState.variables) || {}), e.intensityVariableId, e.intensity, { min: 0, max: 1 }) } : e);
    const FOG_LAYER_TYPES = /* @__PURE__ */ new Set(["fog", "haze", "smoke"]);
    const belowCharEffects = activeOverlayEffects.filter((e) => {
      var _a2;
      return FOG_LAYER_TYPES.has(e.type) && !((_a2 = e.params) == null ? void 0 : _a2.aboveCharacters);
    });
    const aboveCharEffects = activeOverlayEffects.filter((e) => {
      var _a2;
      return !(FOG_LAYER_TYPES.has(e.type) && !((_a2 = e.params) == null ? void 0 : _a2.aboveCharacters));
    });
    const overlayWidth = (stageSize == null ? void 0 : stageSize.width) && stageSize.width > 0 ? stageSize.width : 1280;
    const overlayHeight = (stageSize == null ? void 0 : stageSize.height) && stageSize.height > 0 ? stageSize.height : 720;
    const handleClose = () => {
      const audio = musicAudioRef.current;
      if (audio) {
        audio.pause();
        audio.currentTime = 0;
        audio.src = "";
      }
      const ambientAudio = ambientNoiseAudioRef.current;
      if (ambientAudio) {
        ambientAudio.pause();
        ambientAudio.currentTime = 0;
        ambientAudio.src = "";
      }
      if (audioFadeInterval.current) {
        clearInterval(audioFadeInterval.current);
        audioFadeInterval.current = null;
      }
      if (ambientFadeInterval.current) {
        clearInterval(ambientFadeInterval.current);
        ambientFadeInterval.current = null;
      }
      stopAllSfx();
      const allVideos = document.querySelectorAll("video");
      allVideos.forEach((video) => {
        video.pause();
        video.src = "";
        video.load();
      });
      onClose();
    };
    React2.useEffect(() => {
      return () => {
        const audio = musicAudioRef.current;
        if (audio) {
          audio.pause();
          audio.src = "";
        }
        const ambientAudio = ambientNoiseAudioRef.current;
        if (ambientAudio) {
          ambientAudio.pause();
          ambientAudio.src = "";
        }
        if (audioFadeInterval.current) {
          clearInterval(audioFadeInterval.current);
        }
        if (ambientFadeInterval.current) {
          clearInterval(ambientFadeInterval.current);
        }
        sfxSourceNodesRef.current.forEach((src) => {
          try {
            src.stop();
          } catch (e) {
          }
        });
        sfxSourceNodesRef.current = [];
        activeEffectTimeoutsRef.current.forEach((timeoutId) => {
          try {
            clearTimeout(timeoutId);
          } catch (e) {
          }
        });
        activeEffectTimeoutsRef.current = [];
        TweenManager.cancelAll();
        const allVideos = document.querySelectorAll("video");
        allVideos.forEach((video) => {
          video.pause();
          video.src = "";
          video.load();
        });
      };
    }, []);
    if (!titleScreenId) {
      return /* @__PURE__ */ jsxRuntime2.jsxs("div", { className: "fixed inset-0 bg-black z-[9000] flex flex-col items-center justify-center text-white p-8 text-center", children: [
        /* @__PURE__ */ jsxRuntime2.jsx("h2", { className: "text-2xl text-red-500 font-bold mb-4", children: "Playback Error" }),
        /* @__PURE__ */ jsxRuntime2.jsx("p", { className: "max-w-md", children: "Could not start the game because no valid Title Screen is set. Please ensure a Title Screen exists and is configured in the Project Settings." }),
        /* @__PURE__ */ jsxRuntime2.jsx("button", { onClick: handleClose, className: "mt-8 bg-[var(--bg-tertiary)] hover:bg-[var(--accent-purple)] px-6 py-2 rounded-lg font-bold", children: "Return to Editor" })
      ] });
    }
    return /* @__PURE__ */ jsxRuntime2.jsxs("div", { className: "fixed inset-0 bg-black z-[9000] flex items-center justify-center", children: [
      /* @__PURE__ */ jsxRuntime2.jsx("style", { children: \`
                @keyframes elementTransitionfade {
                    from { opacity: 0; }
                    to { opacity: 1; }
                }
                @keyframes elementTransitionslideUp {
                    from { opacity: 0; transform: translate(-50%, 20%); }
                    to { opacity: 1; transform: translate(-50%, -50%); }
                }
                @keyframes elementTransitionslideDown {
                    from { opacity: 0; transform: translate(-50%, -70%); }
                    to { opacity: 1; transform: translate(-50%, -50%); }
                }
                @keyframes elementTransitionslideLeft {
                    from { opacity: 0; transform: translate(-20%, -50%); }
                    to { opacity: 1; transform: translate(-50%, -50%); }
                }
                @keyframes elementTransitionslideRight {
                    from { opacity: 0; transform: translate(-80%, -50%); }
                    to { opacity: 1; transform: translate(-50%, -50%); }
                }
                @keyframes elementTransitionscale {
                    from { opacity: 0; transform: translate(-50%, -50%) scale(0.5); }
                    to { opacity: 1; transform: translate(-50%, -50%) scale(1); }
                }
                /* Appearance-state image swap crossfade (new image fades in over the old) */
                @keyframes vnImgCrossfade {
                    from { opacity: 0; }
                    to { opacity: 1; }
                }

                /* Screen IN transitions */
                @keyframes screenTransitionfade {
                    from { opacity: 0; }
                    to { opacity: 1; }
                }
                @keyframes screenTransitionslideUp {
                    from { opacity: 0; transform: translateY(100%); }
                    to { opacity: 1; transform: translateY(0); }
                }
                @keyframes screenTransitionslideDown {
                    from { opacity: 0; transform: translateY(-100%); }
                    to { opacity: 1; transform: translateY(0); }
                }
                @keyframes screenTransitionslideLeft {
                    from { opacity: 0; transform: translateX(100%); }
                    to { opacity: 1; transform: translateX(0); }
                }
                @keyframes screenTransitionslideRight {
                    from { opacity: 0; transform: translateX(-100%); }
                    to { opacity: 1; transform: translateX(0); }
                }
                @keyframes screenTransitioncrossfade {
                    from { opacity: 0; }
                    to { opacity: 1; }
                }
                
                /* New-game title→scene fade (defined here so exported games have it too) */
                @keyframes vnGameStartToBlack { from { opacity: 0; } to { opacity: 1; } }
                @keyframes vnGameStartFromBlack { from { opacity: 1; } to { opacity: 0; } }

                /* Screen OUT transitions */
                @keyframes screenTransitionfadeOut {
                    from { opacity: 1; }
                    to { opacity: 0; }
                }
                @keyframes screenTransitioncrossfadeOut {
                    from { opacity: 1; }
                    to { opacity: 1; }
                }
                @keyframes screenTransitionslideUpOut {
                    from { opacity: 1; transform: translateY(0); }
                    to { opacity: 0; transform: translateY(-100%); }
                }
                @keyframes screenTransitionslideDownOut {
                    from { opacity: 1; transform: translateY(0); }
                    to { opacity: 0; transform: translateY(100%); }
                }
                @keyframes screenTransitionslideLeftOut {
                    from { opacity: 1; transform: translateX(0); }
                    to { opacity: 0; transform: translateX(-100%); }
                }
                @keyframes screenTransitionslideRightOut {
                    from { opacity: 1; transform: translateX(0); }
                    to { opacity: 0; transform: translateX(100%); }
                }

                /* Runtime effects (editor + standalone) */
                @keyframes shake {
                    0%, 100% { transform: translate(0, 0); }
                    25% { transform: translate(var(--shake-intensity-x, 5px), var(--shake-intensity-y, 5px)); }
                    50% { transform: translate(calc(-1 * var(--shake-intensity-x, 5px)), calc(-1 * var(--shake-intensity-y, 5px))); }
                    75% { transform: translate(var(--shake-intensity-x, 5px), calc(-1 * var(--shake-intensity-y, 5px))); }
                }
                .shake {
                    animation: shake 0.2s ease-in-out infinite;
                }

                @keyframes flash-anim {
                    0%, 100% { opacity: 0; }
                    50% { opacity: 0.9; }
                }

                /* Meter resource animations — low-state loops + on-change one-shots (editor + standalone) */
                @keyframes vnMeterShake { 0%,100%{transform:translateX(0)} 20%{transform:translateX(-3px)} 40%{transform:translateX(3px)} 60%{transform:translateX(-2px)} 80%{transform:translateX(2px)} }
                @keyframes vnMeterPulse { 0%,100%{transform:scale(1)} 50%{transform:scale(1.12)} }
                @keyframes vnMeterWave { 0%,100%{transform:rotate(0deg)} 25%{transform:rotate(-4deg)} 75%{transform:rotate(4deg)} }
                @keyframes vnMeterPop { 0%{transform:scale(1)} 40%{transform:scale(1.3)} 100%{transform:scale(1)} }
                @keyframes vnMeterChangeShake { 0%,100%{transform:translateX(0)} 25%{transform:translateX(-5px)} 50%{transform:translateX(5px)} 75%{transform:translateX(-3px)} }
                @keyframes vnMeterChangeWave { 0%,100%{transform:rotate(0deg)} 30%{transform:rotate(-8deg)} 70%{transform:rotate(8deg)} }
                @keyframes vnMeterFlashOverlay { 0%{opacity:0} 50%{opacity:0.55} 100%{opacity:0} }
                /* Shape-hugging glow for icon/heart flashes (drop-shadow follows the image alpha, no rectangle) */
                @keyframes vnMeterGlow { 0%,100%{filter:drop-shadow(0 0 0 rgba(0,0,0,0))} 50%{filter:drop-shadow(0 0 6px var(--glow,#ef4444)) drop-shadow(0 0 3px var(--glow,#ef4444))} }
                @keyframes vnMeterGlowOnce { 0%{filter:drop-shadow(0 0 0 rgba(0,0,0,0))} 40%{filter:drop-shadow(0 0 8px var(--glow,#4ade80)) drop-shadow(0 0 4px var(--glow,#4ade80))} 100%{filter:drop-shadow(0 0 0 rgba(0,0,0,0))} }
                /* Lightning flicker patterns (1 = single strike, 2 = double, 3 = stormy triple). */
                @keyframes vn-lightning-1 {
                    0% { opacity: 0; }
                    6% { opacity: 1; }
                    16% { opacity: 0.15; }
                    24% { opacity: 0.6; }
                    45% { opacity: 0; }
                    100% { opacity: 0; }
                }
                @keyframes vn-lightning-2 {
                    0% { opacity: 0; }
                    4% { opacity: 1; }
                    10% { opacity: 0.1; }
                    16% { opacity: 0.85; }
                    24% { opacity: 0.2; }
                    34% { opacity: 0.5; }
                    50% { opacity: 0; }
                    100% { opacity: 0; }
                }
                @keyframes vn-lightning-3 {
                    0% { opacity: 0; }
                    3% { opacity: 0.9; }
                    8% { opacity: 0.1; }
                    13% { opacity: 1; }
                    19% { opacity: 0.15; }
                    26% { opacity: 0.7; }
                    33% { opacity: 0.2; }
                    42% { opacity: 0.55; }
                    60% { opacity: 0; }
                    100% { opacity: 0; }
                }

                .vnfx-canvas {
                    position: absolute;
                    inset: 0;
                    width: 100%;
                    height: 100%;
                    pointer-events: none;
                }

                /* Twinkling lights (PlaceLights) */
                @keyframes vnfx-candle {
                    0%   { opacity: 0.85; transform: translate(-50%, -50%) scale(0.96); }
                    25%  { opacity: 1;    transform: translate(-50%, -51%) scale(1.04); }
                    45%  { opacity: 0.8;  transform: translate(-50%, -50%) scale(0.98); }
                    65%  { opacity: 0.95; transform: translate(-51%, -49%) scale(1.02); }
                    100% { opacity: 0.85; transform: translate(-50%, -50%) scale(0.96); }
                }
                @keyframes vnfx-star {
                    0%   { opacity: 0.45; transform: translate(-50%, -50%) scale(0.85); }
                    50%  { opacity: 1;    transform: translate(-50%, -50%) scale(1.12); }
                    100% { opacity: 0.45; transform: translate(-50%, -50%) scale(0.85); }
                }
                @keyframes vnfx-bulb-fade {
                    0%   { opacity: 0.25; }
                    50%  { opacity: 1; }
                    100% { opacity: 0.25; }
                }
                @keyframes vnfx-bulb-blink {
                    0%, 49%   { opacity: 1; }
                    50%, 100% { opacity: 0.12; }
                }

                .vnfx-scanlines {
                    position: absolute;
                    inset: 0;
                    background: repeating-linear-gradient(
                        to bottom,
                        rgba(0, 0, 0, 0.35) 0px,
                        rgba(0, 0, 0, 0.35) 1px,
                        rgba(0, 0, 0, 0) 3px,
                        rgba(0, 0, 0, 0) 4px
                    );
                    mix-blend-mode: overlay;
                    animation: vnfx-scanlines-scroll 6s linear infinite;
                }
                @keyframes vnfx-scanlines-scroll {
                    from { background-position: 0 0; }
                    to { background-position: 0 60px; }
                }

                .vnfx-chromatic {
                    position: absolute;
                    inset: -2%;
                    background:
                        radial-gradient(circle at 20% 40%, rgba(255, 0, 80, 0.22), transparent 40%),
                        radial-gradient(circle at 80% 55%, rgba(0, 220, 255, 0.20), transparent 45%),
                        repeating-linear-gradient(
                            to bottom,
                            rgba(255, 255, 255, 0.06),
                            rgba(255, 255, 255, 0.06) 2px,
                            transparent 6px,
                            transparent 10px
                        );
                    mix-blend-mode: screen;
                    filter: blur(0.6px);
                    animation: vnfx-chromatic-jitter 0.9s steps(2, end) infinite;
                }
                @keyframes vnfx-chromatic-jitter {
                    0% { transform: translate3d(0, 0, 0); }
                    20% { transform: translate3d(1px, -1px, 0); }
                    40% { transform: translate3d(-1px, 1px, 0); }
                    60% { transform: translate3d(2px, 0, 0); }
                    80% { transform: translate3d(-2px, 1px, 0); }
                    100% { transform: translate3d(0, 0, 0); }
                }

                .vnfx-sunbeams {
                    position: absolute;
                    inset: -30%;
                    background: conic-gradient(
                        from 0deg,
                        rgba(255, 220, 120, 0.0),
                        rgba(255, 220, 120, 0.35),
                        rgba(255, 220, 120, 0.0) 18%,
                        rgba(255, 190, 90, 0.28) 25%,
                        rgba(255, 220, 120, 0.0) 40%,
                        rgba(255, 220, 120, 0.32) 52%,
                        rgba(255, 220, 120, 0.0) 68%,
                        rgba(255, 200, 100, 0.26) 78%,
                        rgba(255, 220, 120, 0.0)
                    );
                    mix-blend-mode: screen;
                    filter: blur(10px);
                    animation: vnfx-sunbeams-spin 18s linear infinite;
                    transform-origin: 50% 50%;
                }
                @keyframes vnfx-sunbeams-spin {
                    from { transform: rotate(0deg) scale(1); }
                    to { transform: rotate(360deg) scale(1); }
                }

                .vnfx-shimmer {
                    position: absolute;
                    inset: 0;
                    background: linear-gradient(
                        120deg,
                        transparent 0%,
                        rgba(255, 255, 255, 0.10) 14%,
                        transparent 28%,
                        transparent 100%
                    );
                    background-size: 240% 240%;
                    mix-blend-mode: overlay;
                    filter: blur(0.4px);
                    animation: vnfx-shimmer-move 2.8s ease-in-out infinite;
                }
                @keyframes vnfx-shimmer-move {
                    0% { background-position: 0% 0%; }
                    50% { background-position: 100% 100%; }
                    100% { background-position: 0% 0%; }
                }
            \` }),
      /* @__PURE__ */ jsxRuntime2.jsxs("div", { ref: playContainerRef, className: "relative overflow-hidden", style: { width: \`min(100vw, calc(100vh * \${((_g = project.gameResolution) == null ? void 0 : _g.width) || 1920} / \${((_h = project.gameResolution) == null ? void 0 : _h.height) || 1080}))\`, height: \`min(100vh, calc(100vw * \${((_i = project.gameResolution) == null ? void 0 : _i.height) || 1080} / \${((_j = project.gameResolution) == null ? void 0 : _j.width) || 1920}))\`, "--font-scale": playContainerSize.width > 0 ? playContainerSize.width / (((_k = project.gameResolution) == null ? void 0 : _k.width) || 1920) : 1 }, children: [
        (playerState == null ? void 0 : playerState.mode) === "playing" ? renderStage() : null,
        (!playerState || playerState.mode === "paused") && (() => {
          const ordered = [];
          const topClosingMenu = !!currentScreenId && closingScreens.has(currentScreenId);
          if (topClosingMenu && screenStack.length >= 2) {
            ordered.push({ id: currentScreenId, isClosing: true });
            ordered.push({ id: screenStack[screenStack.length - 2], isClosing: false });
          } else {
            for (const id of screenStack) {
              if (id !== currentScreenId && closingScreens.has(id)) {
                ordered.push({ id, isClosing: true });
              }
            }
            if (currentScreenId) {
              ordered.push({ id: currentScreenId, isClosing: closingScreens.has(currentScreenId) });
            }
          }
          return ordered.map(({ id, isClosing }) => /* @__PURE__ */ jsxRuntime2.jsx(
            UIScreenRenderer,
            {
              screenId: id,
              onAction: handleUIAction,
              elementVisibility,
              settings,
              onSettingsChange: (key, value) => setSettings((s) => ({ ...s, [key]: value })),
              assetResolver,
              gameSaves,
              playSound,
              variables: screenVariables,
              onVariableChange: handleVariableChange,
              isClosing,
              evaluateConditions: evaluateConditions2,
              onCommitVariables: commitUiVariablesToPlayerState,
              inventorySlots: playerState == null ? void 0 : playerState.inventorySlots,
              onReorderSlots: reorderSlots,
              selectedItemId: playerState == null ? void 0 : playerState.selectedItemId,
              selectedElementId: playerState == null ? void 0 : playerState.selectedElementId,
              onSelectItem: selectItem
            },
            \`\${id}-\${isClosing ? "closing" : "open"}\`
          ));
        })(),
        (playerState == null ? void 0 : playerState.mode) === "playing" && (() => {
          const topHud = hudStack.length > 0 ? hudStack[hudStack.length - 1] : null;
          const activeHudId = topHud ?? project.ui.gameHudScreenId ?? null;
          const ordered = [];
          const topClosingHud = !!activeHudId && closingScreens.has(activeHudId);
          if (topClosingHud && hudStack.length >= 2) {
            ordered.push({ id: activeHudId, isClosing: true });
            ordered.push({ id: hudStack[hudStack.length - 2], isClosing: false });
          } else {
            for (const id of hudStack) {
              if (id !== topHud && closingScreens.has(id)) {
                ordered.push({ id, isClosing: true });
              }
            }
            if (activeHudId) {
              ordered.push({ id: activeHudId, isClosing: closingScreens.has(activeHudId) });
            }
          }
          return ordered.map(({ id, isClosing }) => /* @__PURE__ */ jsxRuntime2.jsx(
            UIScreenRenderer,
            {
              screenId: id,
              onAction: handleUIAction,
              elementVisibility,
              settings,
              onSettingsChange: (key, value) => setSettings((s) => ({ ...s, [key]: value })),
              assetResolver,
              gameSaves,
              playSound,
              variables: screenVariables,
              onVariableChange: handleVariableChange,
              isClosing,
              evaluateConditions: evaluateConditions2,
              onCommitVariables: commitUiVariablesToPlayerState,
              inventorySlots: playerState == null ? void 0 : playerState.inventorySlots,
              onReorderSlots: reorderSlots,
              selectedItemId: playerState == null ? void 0 : playerState.selectedItemId,
              selectedElementId: playerState == null ? void 0 : playerState.selectedElementId,
              onSelectItem: selectItem
            },
            \`\${id}-\${isClosing ? "closing" : "open"}\`
          ));
        })(),
        scenePaused && pausingOverlayScreen && /* @__PURE__ */ jsxRuntime2.jsx(
          "div",
          {
            className: "absolute inset-0",
            style: {
              zIndex: 44,
              background: pausingOverlayScreen.backdropOpacity ? \`rgba(0,0,0,\${pausingOverlayScreen.backdropOpacity})\` : "transparent",
              backdropFilter: pausingOverlayScreen.backdropBlur ? \`blur(\${pausingOverlayScreen.backdropBlur}px)\` : void 0,
              WebkitBackdropFilter: pausingOverlayScreen.backdropBlur ? \`blur(\${pausingOverlayScreen.backdropBlur}px)\` : void 0
            },
            onClick: (e) => e.stopPropagation()
          }
        ),
        belowCharEffects.length > 0 && /* @__PURE__ */ jsxRuntime2.jsx(
          ScreenOverlayEffects,
          {
            effects: belowCharEffects,
            width: overlayWidth,
            height: overlayHeight,
            className: "absolute inset-0 pointer-events-none z-[4]"
          }
        ),
        aboveCharEffects.length > 0 && /* @__PURE__ */ jsxRuntime2.jsx(
          ScreenOverlayEffects,
          {
            effects: aboveCharEffects,
            width: overlayWidth,
            height: overlayHeight,
            className: "absolute inset-0 pointer-events-none z-40"
          }
        ),
        renderPlayerUI(),
        gameStartFade !== "none" && /* @__PURE__ */ jsxRuntime2.jsx(
          "div",
          {
            className: "absolute inset-0 pointer-events-none z-[60] bg-black",
            style: { animation: \`\${gameStartFade === "toBlack" ? "vnGameStartToBlack" : "vnGameStartFromBlack"} 0.4s ease-in-out forwards\` }
          }
        ),
        sceneTransitionFading && /* @__PURE__ */ jsxRuntime2.jsx(
          "div",
          {
            className: \`absolute inset-0 pointer-events-none z-50 \${sceneTransitionType === "fade" ? "bg-black transition-base transition-dissolve" : sceneTransitionType === "dissolve" ? "bg-black transition-base transition-dissolve" : sceneTransitionType === "iris-out" ? "bg-black transition-base transition-iris-out" : sceneTransitionType === "wipe-right" ? "bg-black transition-base transition-wipe-right" : sceneTransitionType === "slide-left" ? "bg-black transition-base transition-slide-out-left" : "bg-black"}\`,
            style: { animationDuration: \`\${sceneTransitionDuration}s\` }
          }
        )
      ] }),
      (playerState == null ? void 0 : playerState.mode) === "playing" && ((_l = playerState.uiState.phone) == null ? void 0 : _l.open) && /* @__PURE__ */ jsxRuntime2.jsx(
        PhonePanel,
        {
          ui: project.ui,
          project,
          phone: playerState.uiState.phone,
          variables: screenVariables,
          assetResolver,
          evaluateConditions: evaluateConditions2,
          onAction: handleUIAction,
          onReply: handlePhoneReply,
          onContactMessage: handleContactMessage,
          onContactCall: handleContactCall,
          playTap: () => {
            if (project.ui.phoneTapSoundId) playSound(project.ui.phoneTapSoundId, void 0, false);
          },
          onOpenApp: (id) => handleUIAction({ type: UIActionType.OpenPhoneApp, appId: id }),
          onCallReply: handleCallReply,
          onEndCall: () => endActiveCall(),
          onSetWallpaper: (id) => updatePlayerState((p) => p ? { ...p, uiState: { ...p.uiState, phone: { ...p.uiState.phone || { open: false, messages: [] }, wallpaperId: id } } } : null),
          onOpenThread: handleOpenThread
        }
      ),
      (playerState == null ? void 0 : playerState.mode) === "playing" && playerState.uiState.mapOverlay && (() => {
        var _a2;
        const mo = playerState.uiState.mapOverlay;
        const map = (_a2 = project.maps) == null ? void 0 : _a2[mo.mapId];
        if (!map) return null;
        return /* @__PURE__ */ jsxRuntime2.jsxs("div", { className: "absolute inset-0 z-[45]", style: { animation: "fade-in 0.25s ease-out" }, children: [
          /* @__PURE__ */ jsxRuntime2.jsx(
            MapSurface,
            {
              map,
              project,
              variables: screenVariables,
              assetResolver,
              evaluateConditions: evaluateConditions2,
              onLocationTap: (loc) => {
                const acts = loc.actions || [];
                updatePlayerState((p) => p ? { ...p, uiState: { ...p.uiState, mapOverlay: null, isWaitingForInput: false } } : null);
                acts.forEach((a) => handleUIAction(a));
                if (loc.targetSceneId) {
                  handleUIAction({ type: UIActionType.JumpToScene, targetSceneId: loc.targetSceneId });
                } else if (mo.fromCommand && !phoneNavigates(acts)) {
                  updatePlayerState((p) => p ? { ...p, currentIndex: p.currentIndex + 1 } : null);
                }
              }
            }
          ),
          !!mo.allowCancel && /* @__PURE__ */ jsxRuntime2.jsx(
            "button",
            {
              onClick: () => updatePlayerState((p) => p ? { ...p, ...mo.fromCommand ? { currentIndex: p.currentIndex + 1 } : {}, uiState: { ...p.uiState, mapOverlay: null, isWaitingForInput: false } } : null),
              "aria-label": "Close map",
              className: "absolute top-3 right-3 w-9 h-9 rounded-full bg-black/60 hover:bg-black/80 text-white text-lg flex items-center justify-center",
              style: { zIndex: 5 },
              children: "✕"
            }
          )
        ] });
      })(),
      (playerState == null ? void 0 : playerState.mode) === "playing" && playerState.uiState.miniGameOverlay && (() => {
        var _a2;
        const mg = playerState.uiState.miniGameOverlay;
        const game = (_a2 = project.miniGames) == null ? void 0 : _a2[mg.gameId];
        if (!game) return null;
        return /* @__PURE__ */ jsxRuntime2.jsx("div", { className: "absolute inset-0 z-[46]", style: { animation: "fade-in 0.25s ease-out" }, children: /* @__PURE__ */ jsxRuntime2.jsx(
          MiniGameFrame,
          {
            game,
            project,
            variables: screenVariables,
            assetResolver,
            playSound: (id) => {
              if (id) playSound(id, void 0, false);
            },
            onResolve: (kind, slotColors, score) => {
              var _a3, _b2;
              const baseActs = (kind === "win" ? game.winActions : kind === "skip" ? game.skipActions : game.failActions) || [];
              let tierActs = [];
              if (kind === "win" && score && ((_b2 = (_a3 = game.score) == null ? void 0 : _a3.tiers) == null ? void 0 : _b2.length)) {
                const match = [...game.score.tiers].filter((t) => score.accuracy >= (t.minAccuracy ?? 0)).sort((a, b) => (b.minAccuracy ?? 0) - (a.minAccuracy ?? 0))[0];
                if (match) tierActs = match.actions || [];
              }
              const acts = [...baseActs, ...tierActs];
              const slots = kind === "win" ? slotColors || {} : {};
              const slotNames = Object.keys(slots);
              const uiPatch = {};
              (game.paletteToUi || []).forEach((m) => {
                if (m.slot && slots[m.slot]) uiPatch[m.target] = slots[m.slot];
              });
              updatePlayerState((p) => p ? {
                ...p,
                ...Object.keys(uiPatch).length ? { uiPaletteOverride: { ...p.uiPaletteOverride || {}, ...uiPatch } } : {},
                uiState: { ...p.uiState, miniGameOverlay: null, isWaitingForInput: false }
              } : null);
              slotNames.forEach((slot) => {
                const def = Object.values(project.variables).find((v) => {
                  var _a4;
                  return ((_a4 = v.name) == null ? void 0 : _a4.toLowerCase()) === slot.toLowerCase();
                });
                if (def) handleUIAction({ type: UIActionType.SetVariable, variableId: def.id, operator: "set", value: slots[slot] });
              });
              if (score && game.score) {
                const setNum = (vid, val) => {
                  if (vid && project.variables[vid]) handleUIAction({ type: UIActionType.SetVariable, variableId: vid, operator: "set", value: val });
                };
                setNum(game.score.hitsVariableId, score.hits);
                setNum(game.score.missesVariableId, score.misses);
                setNum(game.score.accuracyVariableId, score.accuracy);
              }
              acts.forEach((a) => handleUIAction(a));
              if (mg.fromCommand && !phoneNavigates(acts)) {
                updatePlayerState((p) => p ? { ...p, currentIndex: p.currentIndex + 1 } : null);
              }
            }
          }
        ) });
      })(),
      (playerState == null ? void 0 : playerState.mode) === "playing" && ((_n = (_m = playerState.uiState.phone) == null ? void 0 : _m.notification) == null ? void 0 : _n.visible) && !playerState.uiState.phone.open && (() => {
        const n = playerState.uiState.phone.notification;
        const nchar = !n.senderId || n.senderId === "player" ? null : project.characters[n.senderId];
        const nurls = resolvePhonePortrait(n.portrait, nchar, assetResolver);
        const nIconImg = n.iconImage ? assetResolver(n.iconImage.id, "image") : null;
        const freePos = project.ui.phoneNotifX != null || project.ui.phoneNotifY != null;
        const atTop = (project.ui.phoneNotifPosition || "top") === "top";
        const posStyle = freePos ? { left: \`\${project.ui.phoneNotifX ?? 50}%\`, top: \`\${project.ui.phoneNotifY ?? 2}%\` } : { left: "50%", transform: "translateX(-50%)", ...atTop ? { top: "2%" } : { bottom: "2%" } };
        return /* @__PURE__ */ jsxRuntime2.jsxs(
          "div",
          {
            onClick: () => {
              const taps = n.tapActions || [];
              updatePlayerState((p) => p && p.uiState.phone ? { ...p, uiState: { ...p.uiState, phone: { ...p.uiState.phone, ...taps.length ? {} : { open: true, view: "chat" }, notification: null, unread: false } } } : p);
              taps.forEach((a) => handleUIAction(a));
            },
            style: { position: "absolute", ...posStyle, zIndex: 65, cursor: "pointer", minWidth: "40%", maxWidth: "72%", display: "flex", gap: 10, alignItems: "center", padding: "10px 14px", borderRadius: 14, background: project.ui.phoneNotifColor || "rgba(18,20,26,0.96)", color: project.ui.phoneNotifTextColor || "#fff", boxShadow: "0 8px 30px rgba(0,0,0,0.5)", animation: "fade-in 0.25s ease-out", ...project.ui.phoneNotifFont ? fontSettingsToStyle(project.ui.phoneNotifFont) : {} },
            children: [
              nurls.length > 0 ? /* @__PURE__ */ jsxRuntime2.jsx(PhonePortrait, { urls: nurls, size: "2.4em" }) : nIconImg ? /* @__PURE__ */ jsxRuntime2.jsx("img", { src: nIconImg, alt: "", style: { width: "2.4em", height: "2.4em", objectFit: "contain", flexShrink: 0 } }) : n.icon ? /* @__PURE__ */ jsxRuntime2.jsx("span", { style: { fontSize: "1.6em", flexShrink: 0 }, children: PHONE_GLYPHS[n.icon] || "🔔" }) : null,
              /* @__PURE__ */ jsxRuntime2.jsxs("div", { style: { flex: 1, minWidth: 0 }, children: [
                (n.title || (nchar == null ? void 0 : nchar.name)) && /* @__PURE__ */ jsxRuntime2.jsx("div", { style: { fontWeight: 700, fontSize: "0.85em" }, children: n.title || (nchar == null ? void 0 : nchar.name) }),
                /* @__PURE__ */ jsxRuntime2.jsx("div", { style: { fontSize: "0.85em", opacity: 0.92, whiteSpace: "nowrap", overflow: "hidden", textOverflow: "ellipsis" }, children: interpolateVariables(n.text, screenVariables, project) })
              ] }),
              /* @__PURE__ */ jsxRuntime2.jsx("span", { style: { fontSize: "1.2em" }, children: n.title ? "🔔" : "💬" })
            ]
          }
        );
      })(),
      (playerState == null ? void 0 : playerState.mode) === "playing" && ((_p = (_o = playerState.uiState.phone) == null ? void 0 : _o.incomingCall) == null ? void 0 : _p.phase) === "ringing" && (() => {
        const call = playerState.uiState.phone.incomingCall;
        const cchar = call.callerId === "player" ? null : project.characters[call.callerId];
        const curls = resolvePhonePortrait(call.portrait, cchar, assetResolver);
        const shape = project.ui.phoneCallPortraitShape || "circle";
        const acceptImg = project.ui.phoneCallAcceptImage ? assetResolver(project.ui.phoneCallAcceptImage.id, "image") : null;
        const declineImg = project.ui.phoneCallDeclineImage ? assetResolver(project.ui.phoneCallDeclineImage.id, "image") : null;
        const acceptGlyph = acceptImg ? null : project.ui.phoneCallAcceptIcon && PHONE_GLYPHS[project.ui.phoneCallAcceptIcon] || "📞";
        const declineGlyph = declineImg ? null : project.ui.phoneCallDeclineIcon && PHONE_GLYPHS[project.ui.phoneCallDeclineIcon] || "⊘";
        const callBtn = (color) => ({ display: "flex", flexDirection: "column", alignItems: "center", gap: 6, background: "transparent", border: "none", cursor: "pointer", color: "#fff", fontSize: "0.85em" });
        const circle = (color) => ({ width: "3em", height: "3em", borderRadius: "9999px", background: color, display: "flex", alignItems: "center", justifyContent: "center", fontSize: "1.3em" });
        const acceptBtn = /* @__PURE__ */ jsxRuntime2.jsxs("button", { onClick: () => resolveIncomingCall("accepted"), style: callBtn(project.ui.phoneCallAcceptColor || "#22c55e"), children: [
          /* @__PURE__ */ jsxRuntime2.jsx("span", { style: circle(project.ui.phoneCallAcceptColor || "#22c55e"), children: acceptImg ? /* @__PURE__ */ jsxRuntime2.jsx("img", { src: acceptImg, alt: "", style: { width: "1.4em", height: "1.4em", objectFit: "contain" } }) : acceptGlyph }),
          /* @__PURE__ */ jsxRuntime2.jsx("span", { children: project.ui.phoneCallAcceptLabel || "Accept" })
        ] });
        const declineBtn = /* @__PURE__ */ jsxRuntime2.jsxs("button", { onClick: () => resolveIncomingCall("declined"), style: callBtn(project.ui.phoneCallDeclineColor || "#ef4444"), children: [
          /* @__PURE__ */ jsxRuntime2.jsx("span", { style: circle(project.ui.phoneCallDeclineColor || "#ef4444"), children: declineImg ? /* @__PURE__ */ jsxRuntime2.jsx("img", { src: declineImg, alt: "", style: { width: "1.4em", height: "1.4em", objectFit: "contain" } }) : declineGlyph }),
          /* @__PURE__ */ jsxRuntime2.jsx("span", { children: project.ui.phoneCallDeclineLabel || "Decline" })
        ] });
        const nameStyle = project.ui.phoneCallNameFont ? fontSettingsToStyle(project.ui.phoneCallNameFont) : { fontSize: "1.5em", fontWeight: 700 };
        if (call.modal) {
          const bgImg = project.ui.phoneCallBgImage ? assetResolver(project.ui.phoneCallBgImage.id, "image") : null;
          return /* @__PURE__ */ jsxRuntime2.jsxs("div", { onClick: (e) => e.stopPropagation(), style: { position: "absolute", inset: 0, zIndex: 80, display: "flex", flexDirection: "column", alignItems: "center", justifyContent: "center", gap: 18, color: "#fff", background: bgImg ? \`url(\${bgImg}) center/cover no-repeat\` : project.ui.phoneCallBgColor || "rgba(8,10,14,0.96)", animation: "fade-in 0.25s ease-out" }, children: [
            /* @__PURE__ */ jsxRuntime2.jsx("div", { style: { ...project.ui.phoneCallPortraitX != null || project.ui.phoneCallPortraitY != null ? { position: "absolute", left: \`\${project.ui.phoneCallPortraitX ?? 50}%\`, top: \`\${project.ui.phoneCallPortraitY ?? 18}%\` } : { position: "relative" }, width: \`\${project.ui.phoneCallPortraitSize ?? 22}%\`, aspectRatio: "1", borderRadius: shape === "circle" ? "9999px" : "16px", overflow: "hidden", background: "rgba(255,255,255,0.06)" }, children: curls.map((u, i) => /* @__PURE__ */ jsxRuntime2.jsx("img", { src: u, alt: "", style: { position: "absolute", inset: 0, width: "100%", height: "100%", objectFit: project.ui.phoneCallPortraitFit || "cover", objectPosition: project.ui.phoneCallPortraitPosition || "center" } }, i)) }),
            /* @__PURE__ */ jsxRuntime2.jsx("div", { style: { textAlign: "center", ...nameStyle }, children: (cchar == null ? void 0 : cchar.name) || "Unknown" }),
            /* @__PURE__ */ jsxRuntime2.jsx("div", { style: { opacity: 0.7, fontSize: "0.9em" }, children: "Incoming call…" }),
            /* @__PURE__ */ jsxRuntime2.jsxs("div", { style: { display: "flex", gap: 48, marginTop: 8 }, children: [
              declineBtn,
              acceptBtn
            ] })
          ] });
        }
        return /* @__PURE__ */ jsxRuntime2.jsxs("div", { style: { position: "absolute", right: "2%", top: "6%", zIndex: 80, width: "26%", minWidth: 180, display: "flex", flexDirection: "column", gap: 8, padding: "12px", borderRadius: 16, color: "#fff", background: project.ui.phoneCallBgColor || "rgba(8,10,14,0.96)", boxShadow: "0 10px 34px rgba(0,0,0,0.55)", animation: "fade-in 0.25s ease-out" }, children: [
          /* @__PURE__ */ jsxRuntime2.jsxs("div", { style: { display: "flex", gap: 10, alignItems: "center" }, children: [
            /* @__PURE__ */ jsxRuntime2.jsx(PhonePortrait, { urls: curls, size: "2.6em" }),
            /* @__PURE__ */ jsxRuntime2.jsxs("div", { style: { minWidth: 0 }, children: [
              /* @__PURE__ */ jsxRuntime2.jsx("div", { style: { ...nameStyle, fontSize: "1em" }, children: (cchar == null ? void 0 : cchar.name) || "Unknown" }),
              /* @__PURE__ */ jsxRuntime2.jsx("div", { style: { opacity: 0.7, fontSize: "0.75em" }, children: "Incoming call…" })
            ] })
          ] }),
          /* @__PURE__ */ jsxRuntime2.jsxs("div", { style: { display: "flex", justifyContent: "space-around", marginTop: 2 }, children: [
            declineBtn,
            acceptBtn
          ] })
        ] });
      })(),
      (playerState == null ? void 0 : playerState.mode) === "playing" && ((_q = playerState.uiState.phone) == null ? void 0 : _q.outgoingCall) && (() => {
        const oc = playerState.uiState.phone.outgoingCall;
        const contact = (project.ui.phoneContacts || []).find((c) => c.characterId === oc.contactId);
        const ochar = project.characters[oc.contactId];
        const ourls = resolvePhonePortrait(contact == null ? void 0 : contact.avatar, ochar, assetResolver);
        const shape = project.ui.phoneCallPortraitShape || "circle";
        const bgImg = project.ui.phoneCallBgImage ? assetResolver(project.ui.phoneCallBgImage.id, "image") : null;
        const nameStyle = project.ui.phoneCallNameFont ? fontSettingsToStyle(project.ui.phoneCallNameFont) : { fontSize: "1.5em", fontWeight: 700 };
        return /* @__PURE__ */ jsxRuntime2.jsxs("div", { onClick: (e) => e.stopPropagation(), style: { position: "absolute", inset: 0, zIndex: 80, display: "flex", flexDirection: "column", alignItems: "center", justifyContent: "center", gap: 18, color: "#fff", background: bgImg ? \`url(\${bgImg}) center/cover no-repeat\` : project.ui.phoneCallBgColor || "rgba(8,10,14,0.96)", animation: "fade-in 0.25s ease-out" }, children: [
          /* @__PURE__ */ jsxRuntime2.jsx("div", { style: { ...project.ui.phoneCallPortraitX != null || project.ui.phoneCallPortraitY != null ? { position: "absolute", left: \`\${project.ui.phoneCallPortraitX ?? 50}%\`, top: \`\${project.ui.phoneCallPortraitY ?? 18}%\` } : { position: "relative" }, width: \`\${project.ui.phoneCallPortraitSize ?? 22}%\`, aspectRatio: "1", borderRadius: shape === "circle" ? "9999px" : "16px", overflow: "hidden", background: "rgba(255,255,255,0.06)" }, children: ourls.map((u, i) => /* @__PURE__ */ jsxRuntime2.jsx("img", { src: u, alt: "", style: { position: "absolute", inset: 0, width: "100%", height: "100%", objectFit: project.ui.phoneCallPortraitFit || "cover", objectPosition: project.ui.phoneCallPortraitPosition || "center" } }, i)) }),
          /* @__PURE__ */ jsxRuntime2.jsx("div", { style: { textAlign: "center", ...nameStyle }, children: (contact == null ? void 0 : contact.displayName) || (ochar == null ? void 0 : ochar.name) || "Unknown" }),
          /* @__PURE__ */ jsxRuntime2.jsx("div", { style: { opacity: 0.7, fontSize: "0.9em" }, children: "Calling…" }),
          /* @__PURE__ */ jsxRuntime2.jsxs("button", { onClick: endOutgoingCall, style: { display: "flex", flexDirection: "column", alignItems: "center", gap: 6, background: "transparent", border: "none", cursor: "pointer", color: "#fff", fontSize: "0.85em", marginTop: 8 }, children: [
            /* @__PURE__ */ jsxRuntime2.jsx("span", { style: { width: "3em", height: "3em", borderRadius: "9999px", background: project.ui.phoneCallDeclineColor || "#ef4444", display: "flex", alignItems: "center", justifyContent: "center", fontSize: "1.3em" }, children: "⊘" }),
            /* @__PURE__ */ jsxRuntime2.jsx("span", { children: "Hang up" })
          ] })
        ] });
      })(),
      (playerState == null ? void 0 : playerState.mode) === "playing" && ((_r = playerState.uiState.phone) == null ? void 0 : _r.unread) && !playerState.uiState.phone.open && !playerState.uiState.phone.incomingCall && (() => {
        const bx = project.ui.phoneBadgeX ?? 95;
        const by = project.ui.phoneBadgeY ?? 4;
        const unreadCount = (playerState.uiState.phone.notifications || []).filter((e) => !e.read).length;
        const badge = renderPhoneBadge(project.ui, unreadCount);
        return /* @__PURE__ */ jsxRuntime2.jsx(
          "div",
          {
            onClick: () => updatePlayerState((p) => p && p.uiState.phone ? { ...p, uiState: { ...p.uiState, phone: { ...p.uiState.phone, open: true, view: "chat", notification: null, unread: false } } } : p),
            title: "New message",
            style: { position: "absolute", left: \`\${bx}%\`, top: \`\${by}%\`, zIndex: 66, cursor: "pointer" },
            children: badge
          }
        );
      })(),
      confirmDialog && /* @__PURE__ */ jsxRuntime2.jsx(
        InGameConfirmDialog,
        {
          type: confirmDialog.type,
          settings: project.ui.confirmDialogs,
          assetResolver,
          onConfirm: () => {
            const action = confirmDialog.pendingAction;
            setConfirmDialog(null);
            executeUIAction(action);
          },
          onCancel: () => setConfirmDialog(null)
        }
      ),
      !isStandalone && /* @__PURE__ */ jsxRuntime2.jsxs("div", { className: "absolute top-4 left-4 z-[10000] flex flex-col items-start gap-2 max-w-[18rem]", children: [
        /* @__PURE__ */ jsxRuntime2.jsxs(
          "button",
          {
            onClick: () => setShowVarWatcher((s) => !s),
            className: \`flex items-center gap-1.5 pl-2 pr-2.5 py-1.5 rounded-lg text-xs font-medium transition-all border shadow-lg \${showVarWatcher ? "bg-sky-500/90 border-sky-400/60 text-white" : "bg-slate-800/80 border-slate-600/60 text-slate-100 hover:bg-slate-700/90"}\`,
            title: "Variable Tracker — watch your variables' live values change as you play. (Editor only — not shown in exported games.)",
            children: [
              /* @__PURE__ */ jsxRuntime2.jsx(VariablesIcon, { className: "w-4 h-4 flex-shrink-0" }),
              /* @__PURE__ */ jsxRuntime2.jsx("span", { children: "Variables" })
            ]
          }
        ),
        showVarWatcher && (() => {
          const defs = Object.values(project.variables);
          if (defs.length === 0) return /* @__PURE__ */ jsxRuntime2.jsx("div", { className: "bg-black/85 backdrop-blur-sm p-2.5 rounded-lg text-xs w-full border border-white/10 shadow-xl", children: /* @__PURE__ */ jsxRuntime2.jsx("p", { className: "text-slate-400 italic", children: "No variables yet — add some in the Variables tab." }) });
          const liveVars = screenVariables;
          const scopeColor = { local: "bg-emerald-400", global: "bg-sky-400", persistent: "bg-amber-400" };
          return /* @__PURE__ */ jsxRuntime2.jsxs("div", { className: "bg-black/85 backdrop-blur-sm p-2.5 rounded-lg text-xs w-full max-h-[60vh] overflow-y-auto border border-white/10 shadow-xl", children: [
            /* @__PURE__ */ jsxRuntime2.jsx("p", { className: "text-[10px] text-slate-500 mb-1.5", children: "Tap −/＋ or Flip to change values live" }),
            /* @__PURE__ */ jsxRuntime2.jsx("ul", { className: "space-y-1", children: defs.map((def) => {
              const raw = def.id in liveVars ? liveVars[def.id] : def.defaultValue;
              const bl = resolveBoolLabels(def, "Yes", "No");
              const display = def.type === "boolean" ? raw ? bl.yes : bl.no : String(raw);
              const isNum = def.type === "number";
              const isBool = def.type === "boolean";
              const clampNum = (val) => {
                let r = val;
                if (typeof def.min === "number") r = Math.max(def.min, r);
                if (typeof def.max === "number") r = Math.min(def.max, r);
                return r;
              };
              const btn = "w-5 h-5 flex items-center justify-center rounded bg-slate-700 hover:bg-sky-600 active:bg-sky-500 text-white leading-none transition-colors";
              return /* @__PURE__ */ jsxRuntime2.jsxs("li", { className: "flex items-center justify-between gap-3", children: [
                /* @__PURE__ */ jsxRuntime2.jsxs("span", { className: "flex items-center gap-1.5 min-w-0", children: [
                  /* @__PURE__ */ jsxRuntime2.jsx("span", { className: \`w-1.5 h-1.5 rounded-full flex-shrink-0 \${scopeColor[def.scope || "global"] || "bg-slate-400"}\` }),
                  /* @__PURE__ */ jsxRuntime2.jsx("span", { className: "text-slate-300 truncate", title: def.name, children: def.name })
                ] }),
                /* @__PURE__ */ jsxRuntime2.jsxs("span", { className: "flex items-center gap-1 flex-shrink-0", children: [
                  isNum && /* @__PURE__ */ jsxRuntime2.jsx("button", { title: "−1", className: btn, onClick: () => setTestVariable(def.id, clampNum((Number(raw) || 0) - 1)), children: "−" }),
                  /* @__PURE__ */ jsxRuntime2.jsx("span", { className: "font-mono text-white text-right", style: { minWidth: "2.75rem" }, children: display }),
                  isNum && /* @__PURE__ */ jsxRuntime2.jsx("button", { title: "+1", className: btn, onClick: () => setTestVariable(def.id, clampNum((Number(raw) || 0) + 1)), children: "＋" }),
                  isBool && /* @__PURE__ */ jsxRuntime2.jsx("button", { title: "Toggle", className: "px-1.5 h-5 flex items-center justify-center rounded bg-slate-700 hover:bg-sky-600 active:bg-sky-500 text-white text-[10px] leading-none transition-colors", onClick: () => setTestVariable(def.id, !raw), children: "Flip" })
                ] })
              ] }, def.id);
            }) })
          ] });
        })()
      ] }),
      !hideCloseButton && /* @__PURE__ */ jsxRuntime2.jsx("button", { onClick: handleClose, className: "absolute top-4 right-4 bg-slate-800/50 p-2 rounded-full hover:bg-slate-700/80 transition-colors z-50", children: /* @__PURE__ */ jsxRuntime2.jsx(XMarkIcon, { className: "w-8 h-8" }) }),
      itemDragGhost && itemDragGhost.url && /* @__PURE__ */ jsxRuntime2.jsx(
        "img",
        {
          src: itemDragGhost.url,
          alt: "",
          draggable: false,
          className: "fixed z-[10060] pointer-events-none select-none",
          style: { left: itemDragGhost.x, top: itemDragGhost.y, width: itemDragGhost.w, height: itemDragGhost.h, transform: "translate(-50%, -50%)", objectFit: "contain", filter: "drop-shadow(0 6px 10px rgba(0,0,0,0.55))" }
        }
      ),
      carriedItem && /* @__PURE__ */ jsxRuntime2.jsxs(jsxRuntime2.Fragment, { children: [
        /* @__PURE__ */ jsxRuntime2.jsx(
          "div",
          {
            className: "absolute inset-0 z-[10050]",
            style: { cursor: "pointer" },
            onClick: (e) => resolveCarryClick(e.clientX, e.clientY),
            onContextMenu: (e) => {
              e.preventDefault();
              setCarriedItemId(null);
            }
          }
        ),
        /* @__PURE__ */ jsxRuntime2.jsx(
          "img",
          {
            src: assetResolver(((_s = carriedItem.icon) == null ? void 0 : _s.id) || null, "image") || "",
            alt: "",
            draggable: false,
            className: "fixed z-[10052] pointer-events-none select-none",
            style: { left: carryCursor.x, top: carryCursor.y, maxWidth: 64, maxHeight: 64, width: "auto", height: "auto", objectFit: "contain", transform: "translate(-50%, -50%)", filter: "drop-shadow(0 4px 8px rgba(0,0,0,0.6))" }
          }
        ),
        /* @__PURE__ */ jsxRuntime2.jsxs(
          "div",
          {
            className: "fixed z-[10052] pointer-events-none px-2 py-1 rounded bg-black/75 text-white text-xs whitespace-nowrap",
            style: { left: carryCursor.x + 36, top: carryCursor.y + 24 },
            children: [
              carriedItem.name,
              " — click a spot to use · Esc to cancel"
            ]
          }
        )
      ] })
    ] });
  };
  const flourishLogo = "data:image/png;base64,";
  const StandalonePlayer = ({ project }) => {
    const [isReady, setIsReady] = React2.useState(false);
    const [showSplash, setShowSplash] = React2.useState(true);
    const [splashFadingOut, setSplashFadingOut] = React2.useState(false);
    React2.useEffect(() => {
      console.log("[Standalone Player] Loaded project:", project.title);
      setIsReady(true);
      const preventContextMenu = (e) => {
        e.preventDefault();
      };
      document.addEventListener("contextmenu", preventContextMenu);
      return () => {
        document.removeEventListener("contextmenu", preventContextMenu);
      };
    }, [project]);
    const handleSplashClick = () => {
      setSplashFadingOut(true);
      setTimeout(() => {
        setShowSplash(false);
      }, 800);
    };
    if (!isReady) {
      return /* @__PURE__ */ jsxRuntime2.jsx("div", { style: {
        width: "100vw",
        height: "100vh",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        background: "#1a1a2e",
        color: "#fff"
      }, children: /* @__PURE__ */ jsxRuntime2.jsx("div", { children: "Initializing game..." }) });
    }
    if (showSplash) {
      return /* @__PURE__ */ jsxRuntime2.jsxs(
        "div",
        {
          onClick: handleSplashClick,
          style: {
            width: "100vw",
            height: "100vh",
            display: "flex",
            flexDirection: "column",
            alignItems: "center",
            justifyContent: "center",
            background: "#000",
            cursor: "pointer",
            opacity: splashFadingOut ? 0 : 1,
            transition: "opacity 0.8s ease-in-out"
          },
          children: [
            /* @__PURE__ */ jsxRuntime2.jsxs("div", { style: {
              textAlign: "center",
              animation: splashFadingOut ? "none" : "fadeIn 1s ease-in"
            }, children: [
              /* @__PURE__ */ jsxRuntime2.jsx("p", { style: {
                color: "#fff",
                fontSize: "1.5rem",
                fontFamily: "sans-serif",
                marginBottom: "2rem",
                letterSpacing: "0.2em",
                fontWeight: "300"
              }, children: "MADE USING" }),
              /* @__PURE__ */ jsxRuntime2.jsx(
                "img",
                {
                  src: flourishLogo,
                  alt: "Flourish",
                  style: {
                    maxWidth: "400px",
                    width: "80vw",
                    height: "auto"
                  }
                }
              ),
              /* @__PURE__ */ jsxRuntime2.jsx("p", { style: {
                color: "#888",
                fontSize: "0.9rem",
                fontFamily: "sans-serif",
                marginTop: "3rem",
                fontStyle: "italic"
              }, children: "Click to continue" })
            ] }),
            /* @__PURE__ */ jsxRuntime2.jsx("style", { children: \`
                    @keyframes fadeIn {
                        from { opacity: 0; transform: translateY(10px); }
                        to { opacity: 1; transform: translateY(0); }
                    }
                \` })
          ]
        }
      );
    }
    return /* @__PURE__ */ jsxRuntime2.jsxs("div", { style: {
      width: "100vw",
      height: "100vh",
      background: "#000",
      overflow: "hidden"
    }, children: [
      /* @__PURE__ */ jsxRuntime2.jsx("style", { children: \`
                body {
                    user-select: none;
                    -webkit-user-select: none;
                    -moz-user-select: none;
                    -ms-user-select: none;
                }
                input, textarea, [contenteditable="true"] {
                    user-select: text;
                    -webkit-user-select: text;
                    -moz-user-select: text;
                    -ms-user-select: text;
                }
            \` }),
      /* @__PURE__ */ jsxRuntime2.jsx(ToastProvider, { children: /* @__PURE__ */ jsxRuntime2.jsx(ProjectProvider, { initialProject: project, children: /* @__PURE__ */ jsxRuntime2.jsx(LivePreview, { onClose: () => {
      }, hideCloseButton: true, autoStartMusic: true, isStandalone: true }) }) })
    ] });
  };
  const GameEngine2 = {
    /**
     * Mount the game to a DOM element
     * @param container - The DOM element to mount to
     * @param projectData - The VNProject data
     */
    mount: (container, projectData) => {
      if (!container) {
        throw new Error("Container element not found");
      }
      if (!projectData) {
        throw new Error("Project data is required");
      }
      const root = ReactDOM2.createRoot(container);
      root.render(
        /* @__PURE__ */ jsxRuntime2.jsx(React2.StrictMode, { children: /* @__PURE__ */ jsxRuntime2.jsx(StandalonePlayer, { project: projectData }) })
      );
    },
    /**
     * Get version information
     */
    version: "3.6.5",
    /**
     * Check if the engine is ready
     */
    isReady: () => {
      return typeof React2 !== "undefined" && typeof ReactDOM2 !== "undefined";
    }
  };
  if (typeof window !== "undefined") {
    window.GameEngine = GameEngine2;
  }
  exports.GameEngine = GameEngine2;
  exports.default = StandalonePlayer;
  Object.defineProperties(exports, { __esModule: { value: true }, [Symbol.toStringTag]: { value: "Module" } });
  return exports;
})({}, jsxRuntime, React, ReactDOM, ReactDOM);
`;

export function getGameEngineCode(): string {
  return GAME_ENGINE_BUNDLE;
}
